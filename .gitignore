# Git
.secret_git

# Rust / Cargo
target/
**/target/
*.rs.bk

# Bootloader / ISO build output (src/boot/.build)
.build/

# Build artifacts
*.o
*.elf
*.img
*.iso
*.bin
*.log
src/proto/shared/
src/user/*/shared/

# ...but the embedded console font is a required source asset, not a build output
!src/term/src/unscii16.bin

# Image / distribution archives
*.zip
*.gz
*.xz
*.7z
*.tar

# Editor
*.swp
*~
*.bak
*.tmp

# OS
.DS_Store
Thumbs.db
desktop.ini
# Component SDK build artifact staged into the ramdisk volume (built by `just sdk`)
src/volume/app.wasm
