# P02M0093 - Shared system libraries (dynamic linking)

Status: REOPENED (2026-08-16) by a fourth-party audit round, recorded in AI/audit/bootproto.md. The findings are appended as open tasks in the section at the end of this file; each was checked against the tree before being written down, and where a check contradicted the audit that is recorded with the task rather than dropped. Previously: DONE (2026-07-14) **as the loader/provider pilot**. The broad executable rollout the original
wording implied did not land here; P02M0096a owns and hard-gates it after the full `/bin` conversion.

*Condensed 2026-08-08.*

**The compatibility model was decided first and written down**, in `docs/DYNAMIC_LINKING.md`, before
anything was built. That ordering is the point: a dynamic linking scheme whose compatibility rule is
discovered afterwards has already shipped incompatible artifacts.

The constraint that shaped everything: **the bare targets support neither Rust `dylib` nor
`cdylib`**. So the pinned builder emits full-graph PIC rlibs and links deterministic ELF shared
objects from them, rather than relying on a Rust-level dynamic library that does not exist for these
targets.

The library set is atomized - one library, one concern, with real dependency edges between them
rather than a single bag of shared code.

## Fourth audit (2026-08-16): `AI/audit/bootproto.md`

Three findings, two High, both in the compatibility decision that authorises hot replacement.

- [x] **Compatibility compares provider NAMES and throws their identity digests away (BTP-001,
  High).** VERIFIED: `Identity::providers` splits each `provider=<name>:<digest>` record at the
  colon and yields only the name - its comment says so - and `decide` compares exactly that. Two
  images whose providers differ ONLY by digest are therefore reported `Compatible`, which is the one
  thing the digest exists to prevent. The process loader has a stricter, canonical parser that
  requires field order, hexadecimal widths, sorted unique providers and provider digests, and
  compares them - so the two components implement DIFFERENT IDENTITY LANGUAGES, and a publication
  reported compatible can then fail to launch. Use one parser and one typed `Identity` for both, and
  compare ordered `(name, digest)` pairs.
- [ ] **Export comparison rescans the whole symbol table for every symbol whose order diverged
  (BTP-002, High).** VERIFIED, with a caveat worth keeping: the audit calls it "billions of
  comparisons", and the code's own comment records the measured cost of rescanning per symbol as
  ~810 ms at 672 exports against ~8 ms for the walk. So the real number is known and is large rather
  than astronomical - and a candidate whose exports are in reverse order reaches the rescan path for
  every symbol. Build a bounded sorted index of candidate exports once instead.
- [x] **`e_ident[EI_VERSION]` and `e_version` are never validated (BTP-003).** An image that does not
  declare the ELF version whose layout is then used to read it is accepted.

### Resolution of BTP-001 and BTP-003 (2026-08-16)

**Provider entries are compared with their digests.** `Identity::providers` yields the whole
`<name>:<identity-digest>` record; `provider_names` is the name-only view for the one caller that
wants it (`dev_protocol`, matching a DT_NEEDED stem against the closure). The rule compares the full
ordered list, so a provider rebuilt from different sources is a different closure under the same
name - which is what the digest is for, and what the process loader has always required.

WATCHED TO FAIL, and the fixture had to be fixed before it could be: the helper appended the SAME
placeholder digest to every provider, so no test could vary one, which is why comparing names alone
went unnoticed. It now accepts a provider that brings its own digest.
`a_changed_provider_closure_or_order_requires_the_cold_path` gained the case the audit named - same
providers, same order, one digest changed - and restoring the truncation turns it red.

**Both ELF version fields are validated.** `parse_for_machine` requires `e_ident[EI_VERSION]` and
`e_version` to be 1, and `declared_machine` checks the identification byte too, because reading
`e_machine` out of a file that never declared this layout is reading a field at an offset the file
did not promise.

THE EVIDENCE THAT NOTHING CHECKED IT IS IN THE FIXTURES: every ELF fixture in the crate built
`e_ident` from magic, class and byte order and left the version byte at ZERO, and all of them
parsed. Eleven had to be corrected for the check to pass, and not one had ever been a legal ELF
file. `an_image_that_declares_no_elf_version_is_refused` covers zero and an unknown version in both
fields.

Measured: bootproto 27/27.
