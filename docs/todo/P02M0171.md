# P02M0171 - A signed release can still be old: enforce a monotonic boot floor

Status: FUTURE TRACK. X86_64 OVMF BOOT SECURITY. NOT A PHASE-2 COMPLETION GATE.

## Goal

Extend P02M0150's authenticated QEMU boot so an attacker who can replace boot disks cannot boot a
correctly signed release older than the highest security generation this machine has accepted.

The claim is deliberately narrower than general update security: firmware authenticates a current,
anti-rollback-aware loader; the firmware-backed monotonic state and kernel/hypervisor are trusted;
and the attacker controls persistent boot media, not firmware/NVRAM administration. The QEMU proof
must prevent substitution of a pre-policy loader, because an older firmware-authorized loader which
ignores the floor would bypass the property before the new code ran.

The manifest format, signer and shared loader validation remain common to all three loader targets.
The enforcing completion profile is x86_64 OVMF, however, because P02M0150 qualifies firmware Secure
Boot only there. This milestone does not imply an anti-rollback chain on AArch64 or RISC-V firmware.

## What is there now, and what it forces

- Loader trust is COMPILE-TIME. The profile comes from `LIBER_TRUST_PROFILE`, defaulting to
  `test-trust`, and the accepted roots are compiled in - a release build without a key does not
  compile. So enforcement is already a property of the binary rather than of anything on the disk,
  and this milestone must keep it that way rather than inventing a second selector.
- Ordinary x86_64 QEMU runs COPY a fresh OVMF variables image on every boot and sweep stale copies,
  and the Secure Boot checker clones its base image and removes the clone. Under that lifecycle a
  monotonic floor is reset every boot, so monotonicity is vacuous unless a profile owns a persistent
  store. This is the reason the enforcing behavior cannot simply be switched on globally.
- The UEFI layer types exactly one Runtime Services entry - `GetVariable` - for exactly two
  hard-coded global variable names, and collapses every status and size failure to `None`.
  `SetVariable` is deliberately left as an untyped pointer, with the reason written in the file: a
  loader that could set variables could enrol its own key. Adding a write path is therefore a
  deliberate widening of that boundary and must be argued, scoped and tested as one.
- A manifest key has no purpose or role. `root_for` selects a root by key id, and verification
  checks product, architecture, source kind, volume identity and release. Manifest v2 carries no
  generation and no recovery discriminator.
- The product identity the loader compares against is a compiled-in constant, and its own comment
  records that it is one of four copies of the product name and names the milestone that will
  remove it. This milestone uses that constant; it does not add a fifth copy and it does not take
  over the deduplication.

## Work

- [ ] **M1 - freshness is a signed number, not a display version.** Introduce a new versioned signed
  manifest format carrying a canonical unsigned 64-bit `security-generation`. It is covered by the
  manifest signature and is independent of the human release string. External-release signing
  requires an explicit generation; malformed, missing, wrapped, or non-canonical values are
  refused. The shared parser, signer and loader agree on exactly the same bytes. Every medium and
  volume manifest composed into one boot must carry the same generation; latch and compare it
  independently of the human release string. An enforcing trust profile does not silently interpret
  a v2 manifest as generation zero or choose one value from a mixed-generation artifact set.

- [ ] **M2 - the enforcing profile is a build identity with a stated provisioning ceremony.** The
  anti-rollback profile is selected the way trust already is: a compile-time profile identifier and
  compiled-in roots, never a value read from replaceable media. Define, in this order:

  - which profile identifiers enforce the floor and which do not, and that `test-trust` and the
    development builds do NOT, so the ordinary fresh-variables boot paths keep working unchanged;
  - the UNPROVISIONED state and how it is distinguished from a failed read - an enforcing build
    that finds no state has either never been provisioned or is being attacked, and those are not
    the same answer;
  - the one-time PROVISIONING ceremony, who performs it, and the initial floor it writes; and
  - that after provisioning, missing state is a refusal rather than a reset to zero.

  **AND THE VALUES ARE FIXED HERE RATHER THAN LEFT TO THE IMPLEMENTER** (2026-08-30). A previous
  version listed what to define and defined none of it, which is the same defect as not deciding.

    enforcing profiles          `rollback-enforcing` only. It is a THIRD `LIBER_TRUST_PROFILE`
                                  value beside `test-trust` and the external-release profile, so
                                  enforcement is a build identity exactly as trust already is
    non-enforcing profiles      `test-trust` (the default) and the plain external-release profile.
                                  Both keep today's behaviour unchanged, including the per-run fresh
                                  OVMF variables copy, so every current boot path on this machine is
                                  untouched by this milestone
    the signing identity        A SEPARATE SIGNER FOR ENFORCING LOADERS, and it is what makes the
                                  floor hold (added 2026-08-31). Secure Boot authenticates a SIGNER,
                                  never a `LIBER_TRUST_PROFILE`: the firmware cannot read the profile
                                  and has no way to ask. So if one signer signs both an enforcing and
                                  a non-enforcing loader, a CURRENT, correctly signed, non-enforcing
                                  loader on replacement media is accepted by the firmware and ignores
                                  the floor - bypassing this milestone's whole claim as completely as
                                  the pre-policy loader M6 revokes, and without touching NVRAM. The
                                  gates as they stand cannot tell them apart either: the Secure Boot
                                  gate signs whichever loader occupies the shared output path without
                                  consulting its profile, and the trust-profile gate builds several
                                  profiles to that same path. Therefore: `rollback-enforcing` loaders
                                  are signed by a signing identity of their own, that identity is the
                                  only loader signer enrolled on a machine provisioned for
                                  enforcement, and the non-enforcing profiles keep a different signer
                                  that such a machine does not authorise. The signing step names the
                                  profile it is signing rather than inferring it from a path
    the provisioned marker      a separate `LiberSystemRollbackProvisioned` variable holding one
                                  byte, written ONCE by the ceremony and never cleared by a boot.
                                  It exists because "no floor record" and "the floor record was
                                  deleted" are otherwise observationally identical, and a rollback
                                  defence that cannot tell them apart is defeated by deleting state
    the marker's BYTES          ONE OCTET, VALUE `0x01`, in a variable of exactly one byte, under
                                  EXACTLY the mask M3 fixes for the slots and nothing else:
                                  `NON_VOLATILE | BOOTSERVICE_ACCESS` (added 2026-09-01, mask
                                  corrected the same day). The 64-byte exact-size rule beside it is
                                  the SLOT record's and was being read as though it covered this too,
                                  which left the marker with no defined valid form at all.
                                  THE FIRST VERSION OF THIS ROW SAID "the same attributes as the slot
                                  records" AND THEN LISTED DIFFERENT ONES - "runtime access,
                                  authenticated writes". Both additions contradict M3: it fixes the
                                  mask as `NON_VOLATILE | BOOTSERVICE_ACCESS`, says NOT
                                  `RUNTIME_ACCESS` in as many words because keeping the running
                                  system out of this state is the whole defence, and makes any other
                                  mask INVALID. So the row demanded a variable no conforming
                                  implementation could produce, and following it would have reopened
                                  the running-system write path this milestone exists to close.
                                  There are no authenticated writes anywhere in this design: no
                                  signing path, key or descriptor is defined for the ceremony or for
                                  an ordinary floor advance, and inventing one is not this
                                  milestone's work. BOOTSERVICE_ACCESS is what excludes the running
                                  system, and it is sufficient for that.
                                  AND EVERY NON-ABSENT FAILURE REFUSES, WHICH IS THE HALF THAT
                                  MATTERS. A marker that is short, long, holds any other value,
                                  carries the wrong attributes, or whose read returns access-denied
                                  or a device error is NOT absence: absence is a machine that was
                                  never provisioned, and each of these is a machine that says
                                  something this loader cannot believe. Collapsing them into absence
                                  is the whole attack - a previously provisioned machine boots as
                                  unprovisioned and its floor is gone - so they refuse the boot and
                                  say which one it was. Only a read that SUCCEEDS and reports the
                                  variable as not present is absence.
    unprovisioned               marker ABSENT - meaning that exact case and no other: the machine has
                                  never completed provisioning,
                                  WHATEVER THE SLOTS CONTAIN. An enforcing build BOOTS and REFUSES TO
                                  ADVANCE, and says so, because refusing outright would brick a
                                  machine whose ceremony has not run. It does not treat that as floor
                                  zero and does not write a floor
    provisioned                 marker PRESENT: from here the M4 rules apply in full, and BOTH floor
                                  records missing or unreadable is a REFUSAL rather than a reset -
                                  one of the two is the survivable case the table below covers
    the ceremony                one operator action against firmware NVRAM, outside any booted
                                  LiberSystem: it writes both slots and then the marker, at the
                                  generation of the release being installed. It is an administrator
                                  operation by definition - this milestone's threat model already
                                  excludes an attacker who administers firmware NVRAM - and it is
                                  performed for the gate by the harness fixture that owns the
                                  persistent image
    the commit order            THE MARKER IS WRITTEN LAST AND IS THE COMMIT POINT (added
                                  2026-08-31). A previous version defined unprovisioned as marker
                                  absent AND both slots absent, and had the ceremony write three
                                  independent variables in no stated order - so power loss after one
                                  or both slots but before the marker left a machine that was neither
                                  state, and no rule said what it does. The ceremony is therefore
                                  ordered and idempotent:
                                    1. write slot A, read it back and validate it;
                                    2. write slot B, read it back and validate it;
                                    3. only then write the marker.
                                  A readback that does not validate ABORTS the ceremony before the
                                  marker is written, so a machine whose NVRAM is failing stays
                                  unprovisioned rather than becoming a provisioned machine that
                                  refuses every boot. This ordering is why "marker absent" alone
                                  means unprovisioned: everything written before the marker is by
                                  definition uncommitted, and re-running the ceremony OVERWRITES both
                                  slots unconditionally rather than reading what a previous attempt
                                  left. Writing the marker first is the one order that is forbidden,
                                  because it produces a provisioned machine with missing records and
                                  therefore an immediate, permanent refusal
    the partial states          every combination is classified, and there is no fourth answer:

                                    marker  slots            state and behaviour
                                    absent  neither          unprovisioned; boots, refuses to
                                                               advance; the ceremony may run
                                    absent  one or both,     unprovisioned - an interrupted
                                              valid or not     ceremony. Identical behaviour to the
                                                               row above, and the ceremony re-runs
                                                               from step 1 overwriting both
                                    present both valid       provisioned; the M4 rules apply
                                    present one valid        provisioned; the valid slot is
                                                               authoritative and the other is
                                                               rewritten from it AND READ BACK
                                                               BEFORE CONTROL IS TRANSFERRED on
                                                               THIS boot - not on the next
                                                               successful advance (corrected
                                                               2026-09-02). M4 owes convergence
                                                               to every accepted boot, and
                                                               deferring the repair to an advance
                                                               left the machine one more failure
                                                               away from an unconverged floor:
                                                               boot with `{N, N+1}`, take the
                                                               floor `N+1`, boot the equal
                                                               generation - which M4 admits -
                                                               with `A` still at `N`, then lose
                                                               `B`, and `N` is authoritative
                                                               again and a correctly signed `N`
                                                               boots. That is the rollback this
                                                               milestone exists to refuse,
                                                               reached without a single
                                                               mis-signed artifact. A boot that
                                                               cannot complete the repair - the
                                                               readback disagrees, or the write
                                                               fails - REFUSES, on the same terms
                                                               as the row below: a machine that
                                                               cannot make its own state
                                                               believable does not hand over
                                                               control
                                    present neither valid    provisioned with unreadable state:
                                                               REFUSES the boot, and is not reset by
                                                               it. Recovery is the ceremony, not a
                                                               boot
    DELETING THE MARKER IS  OUTSIDE THIS MILESTONE'S THREAT MODEL, AND SAYING OTHERWISE MADE THE
      NOT DEFENDED AGAINST    TABLE SELF-CONTRADICTORY (corrected 2026-08-31). A previous version
                              said a marker-present machine with both slots invalid is "not
                              recoverable by deleting the marker", and M7 asked for a fixture proving
                              it goes on refusing after exactly that deletion. Both are impossible
                              against this table's own first row: once the marker is gone there is no
                              durable fact left that distinguishes such a machine from one that was
                              never provisioned, so the stated algorithm must classify it as
                              UNPROVISIONED, boot it, and let a ceremony overwrite both slots.
                              The resolution is to say what the threat model already says rather than
                              to build a mechanism against it. The marker lives in firmware NVRAM,
                              and this milestone's head states that the attacker controls persistent
                              boot media and NOT firmware/NVRAM administration - so an attacker who
                              can delete the marker is one this milestone does not defend against by
                              construction, in the same way it does not defend against one who can
                              enroll their own Secure Boot key. What deleting the marker costs is
                              AVAILABILITY of the rollback floor, not its integrity: the machine
                              becomes unprovisioned and refuses to advance until the ceremony runs
                              again, and every artifact it accepts is still signature-checked by the
                              loader.
                              A LATER MILESTONE that wants this defended needs the provisioned fact
                              in state the operating system cannot clear - a monotonic counter, a
                              fuse, or a TPM NV index with a write-once policy - which is a hardware
                              root this QEMU milestone does not have and does not claim.

    the initial floor           the generation of the artifact set the ceremony installs, never zero
                                  and never a constant
    the variables-image owner   the named x86_64 OVMF gate owns ONE persistent image for the whole
                                  boot sequence, created by the ceremony step and kept across every
                                  boot in that sequence. `qemu-run.sh`'s ordinary per-run fresh copy
                                  is untouched and stays the behaviour of every non-enforcing profile

  A negative fixture proves that replacement media cannot select a non-enforcing path or downgrade
  the profile - and it is a CURRENT, correctly signed, NON-ENFORCING loader that it offers, because
  that is the case with a mechanism behind it: an old-signer loader is M6's fixture and proves only
  that revocation works. A second proves that deleting BOTH floor records on a provisioned machine
  refuses the boot rather than resetting the floor.

  BOTH, AND THE PREVIOUS WORDING SAID "THE FLOOR RECORD" AS THOUGH THERE WERE ONE (corrected
  2026-09-01). There are two, and the state table makes a single valid slot AUTHORITATIVE: a machine
  with one record deleted BOOTS from the survivor and rewrites the missing one at its next successful
  advance, which is what two slots are for. So "deleting the floor record refuses" and "one valid slot
  boots" asked for opposite behaviour from the same machine, and an implementer following either
  would fail the other's test.
  The rule, stated once: ONE slot missing or invalid is a machine that boots from the other and
  repairs it BEFORE handing over control, on that boot; BOTH missing or invalid is a provisioned
  machine whose state cannot be believed, and it refuses. A THIRD fixture deletes ONE and requires
  the boot to succeed AND the missing record to be restored and read back BEFORE control is
  transferred - not on a later advance, which is what this fixture used to allow and which left the
  single-failure case untested for the boot that actually meets it (corrected 2026-09-02). A FOURTH
  fixture is what the correction is really about: delete one slot and boot the SAME generation. No
  advance happens, so a fixture that only watched an advancing boot proved nothing about it, and this
  is the exact state an interrupted advance leaves behind. Both slots must equal the selected floor
  before the artifact runs, and a run whose readback disagrees must refuse rather than continue -
  because a redundancy rule with no test for the single-failure case is a rule nobody is holding.

- [ ] **M3 - the state record has canonical bytes and detectable torn writes.** The schema is frozen
  HERE, in bytes, because a previous version listed the fields an implementation "must choose" and
  called that a freeze - and whether a torn write is detectable and whether absence is
  distinguishable from firmware failure are decided by exactly these details, not by a field list.

    offset  size  field
    0       8     magic, the ASCII bytes `LSROLLB1`. A record that does not start with these is
                    not this format and is INVALID, never "an older version to be tolerated"
    8       4     format version, little-endian `u32`, value 1
    12      4     reserved, MUST be zero on write and MUST be zero to validate - a nonzero
                    reserved word is an invalid record, which is what keeps the field usable later
    16      32    product identity: the SHA-256 of the loader's existing compiled-in product
                    constant. A fixed width rather than a string, so the record's size never
                    depends on the product name, and the constant rather than a fifth copy of it
    48      8     the accepted floor, little-endian `u64`
    56      8     commit tag: SHA-256 of bytes 0..56 with this record's SLOT INDEX BYTE appended,
                    TRUNCATED to its first 8 bytes. The hashed input is therefore exactly 57 bytes
    total   64

  THE SLOT INDEX IS ONE BYTE AND ITS TWO VALUES ARE FIXED HERE (2026-08-31). The record said "the
  slot index appended" and left its width, encoding and values undefined - so the loader, the
  provisioning tool, the mocked firmware and the OVMF fixture could each produce an incompatible tag
  while every one of them followed this plan, and the cross-slot protection the tag exists for would
  hold only by luck. It is a single octet, appended after byte 55, with exactly two legal values:

    slot A   `0x00`
    slot B   `0x01`

  No other value exists and no other width is legal; a `u32`, an ASCII digit and a little-endian word
  are all wrong, and this sentence is here so none of the four producers invents one. The two slots
  are otherwise byte-identical formats, which is the point: the index is the ONLY thing that makes
  slot A's bytes fail validation in slot B. M7's host fixtures cover that directly - a valid slot A
  record copied verbatim into slot B is REFUSED, and so is the reverse - because a cross-slot copy
  that validated would force the floor down with two individually well-formed records.

  EVERY MULTI-BYTE FIELD IS LITTLE-ENDIAN, on every target, because the record is written by one
  loader and read by another and a byte order that follows the host is a record that stops validating
  when the machine changes.

  THE COMMIT TAG IS THE LOAD-BEARING FIELD and it is last on purpose: a write that stops partway
  leaves the tag absent or partial, so the record fails validation instead of parsing as a plausible
  lower floor. The slot index is inside the tag so that copying slot A's bytes over slot B does not
  produce a valid slot B - which is otherwise a way to force the floor down with two valid records.

  UEFI CONSTANTS, fixed here for the same reason:

    vendor GUID     `4c696265-7253-7973-2d52-6f6c6c626b31` - allocated to this product for these
                      variables and written HERE as the value rather than as an instruction to pick
                      one (corrected 2026-08-31: the previous text described the GUID and supplied
                      none, so a loader, a tool and a fixture could each follow the plan and use a
                      different namespace). It is never the EFI global namespace, which is shared and
                      which any other vendor may also define these names in. The loader, the
                      provisioning tool, the mocked-firmware host fixtures and the OVMF gate all use
                      this literal
    slot names      `LiberSystemRollbackA` and `LiberSystemRollbackB`
    marker name     `LiberSystemRollbackProvisioned`, one byte, same GUID
    attributes      `NON_VOLATILE | BOOTSERVICE_ACCESS`. NOT `RUNTIME_ACCESS`: nothing after
                      ExitBootServices may read or write this state, which removes the running
                      system from the set of things that can lower the floor. A variable read back
                      with a different attribute mask is INVALID, not merely surprising
    maximum size    64 bytes exactly; a longer or shorter read is invalid

  Product identity is taken from the loader's existing compiled-in product constant rather than a new
  derivation, for the reason its own comment gives.
  Selecting the highest "valid" slot is only a rollback defence if a torn write cannot parse as a
  plausible lower value, so the discriminator is the load-bearing field and not a checksum added
  for tidiness.

  Store it in the two independently validated nonvolatile vendor slots named above. Extend the UEFI
  variables layer with a bounded typed `GetVariable`/`SetVariable`/readback
  path that returns DISTINCT outcomes for absent, short, oversized, wrong-attribute, access-denied,
  device-error, write-failed and readback-mismatch - the current collapse to `None` would make a
  firmware error indistinguishable from a provisionable absence. The write path is the only new
  Runtime Services entry this milestone adds, and the file's existing argument for why writes were
  excluded is answered in the same change rather than deleted.

  On read, select the highest valid floor; one damaged slot never authorizes the lower value in it,
  and two invalid slots refuse boot. Wrong product identity is a refusal.

- [ ] **M4 - compare and advance in a fail-closed order.** Verify the current loader, signed
  manifests, cross-manifest generation equality, product/architecture/source and every selected
  artifact first. The one generation of that complete selected set is the only input to the floor:
  refuse it below the stored value. For a higher generation, write and read back the older/inactive
  slot before treating it as current; never overwrite the only valid highest slot first. Selecting
  the maximum independently valid slot makes a torn/failed update leave either the previous valid
  floor or the new one, never an accepted lower value.

  THEN CONVERGE THE SECOND SLOT, AND ONLY THEN TRANSFER CONTROL (corrected 2026-09-01). Writing one
  slot and transferring left a floor that could be UNDONE BY DELETING ONE VARIABLE. From `{A=N, B=N}`,
  accepting `N+1` left `{N, N+1}`; the partial-state table then treats a machine with one valid slot
  as authoritative on the survivor and repairs the other "on the next successful advance" - an advance
  that may never come. So deleting or damaging the `N+1` slot leaves `N` authoritative and a correctly
  signed `N` artifact boots. That is the rollback-by-deleting-state this milestone's marker was added
  to defend against, reappearing one level down at the SLOT.

  The advance is therefore TWO writes and the boot is not accepted until both land: write and read
  back the older/inactive slot; then write and read back the OTHER slot to the same generation, so
  that the steady state after any accepted advance is `{N+1, N+1}`; then transfer control. The order
  stays fail-closed - the second write overwrites the LOWER of the two, never the only valid highest -
  and every interruption is safe: torn before the first readback leaves `{N, N}` and the floor is `N`;
  torn between the two leaves `{N, N+1}` and the floor is `N+1`; and losing either slot from
  `{N+1, N+1}` leaves `N+1` authoritative, which is the property one write did not have.

  The "one valid slot" row in the partial-state table is therefore a DAMAGE case and not a normal
  resting state, and its repair-from-the-survivor rule is correct precisely BECAUSE convergence means
  the survivor carries the current floor.
  AND CONVERGENCE IS OWED BY EVERY ACCEPTED BOOT, NOT ONLY BY AN ADVANCE (corrected 2026-09-01). The
  rule above is written inside "for a higher generation", and that leaves the hole it was written to
  close open one step further along. The intermediate state it names - `{A=N, B=N+1}`, reached when
  power fails between the two writes - is a state a machine can BOOT from: the selected floor is the
  maximum valid slot, `N+1`, and the same `N+1` artifact then compares EQUAL, which the next clause
  says is bootable. Nothing rewrites `A`. Delete `B` afterwards and `N` is authoritative again and a
  correctly signed `N` boots, which is the rollback this milestone exists to refuse.
  So the rule is on the STATE and not on the transition: before control is transferred to any
  accepted artifact, both slots must equal the selected floor; where they do not, the lagging slot is
  written and read back first. An advance reaches that through the two writes above; an
  equal-generation boot of an interrupted advance reaches it by completing the write the power cut
  interrupted. Both are the same rule read at the same moment, and stating it once is what stops the
  next reader finding a third path into an unconverged floor.
  Equal generation remains bootable so a reinstall of the same release is not mistaken for a
  rollback - bootable, and converging first, like every other accepted boot.

- [ ] **M5 - recovery is a SIGNED PURPOSE FIELD, and the fork is closed.** A manifest key currently
  has no role, so "separately authorized" has to be built rather than assumed. A previous version
  offered two designs and told the implementer to choose; that is not a choice a test matrix can be
  written against, because the two change the signed schema, the root roles, firmware enrolment, the
  artifacts and the M6 signer transition differently.

  **DECIDED: a signed PURPOSE field, enforced against purpose-scoped trust roots.** The manifest
  gains a `purpose` field beside the generation M1 adds - one `u32`, `1` = ordinary boot, `2` =
  recovery, every other value refused - covered by the same signature and validated in the same
  place as product, architecture and source kind. Each compiled-in root carries the purposes it may
  sign for, so a root accepted for recovery is NOT accepted for ordinary boot and the reverse.

  The separate recovery-loader profile is REJECTED, not deferred, and for the reason M1 already
  gives about the manifest: it would need its own firmware enrolment, its own signer transition and
  its own persistent-state contract, doubling M6 and M7 to express what one signed field expresses -
  and it would put a SECOND loader binary in the firmware's accepted set, which is the exact class of
  thing M6 exists to remove. One manifest evolution carries the generation and the purpose together,
  which is also what P02M0172 needs if it lands in the same period.

  **AND THE PURPOSE IS LATCHED ACROSS THE WHOLE SELECTED SET, EXACTLY AS THE GENERATION IS**
  (added 2026-08-31). M1 already requires every medium and volume manifest composed into one boot to
  carry the same generation, and latches it from the first verified manifest. The purpose needs the
  same rule and did not have it: validating each manifest's purpose against purpose-scoped roots
  makes each one individually legitimate and says nothing about the SET, so an attacker holding a
  correctly signed recovery volume could compose it with an ordinary medium and have both signatures
  check out. That is recovery-authorised content entering part of an ordinary boot, which is the
  cross-use the Definition of done says cannot happen.

  So: the first verified manifest latches the purpose, every later one must match, and a mixed set
  REFUSES the boot without advancing the floor - the same shape and the same failure mode as the
  generation latch, sharing its code rather than restating it. M7 adds a mixed-purpose refusal
  fixture beside the mixed-generation one.

  Its relationship to the firmware signer and to the generation floor: a recovery
  image must itself carry a generation at least as high as the floor, and recovery can never clear
  or lower the state. Cross-use negatives prove that recovery credentials cannot sign an ordinary
  boot set and ordinary credentials cannot claim recovery. Adding a recovery key to the ordinary
  accepted-root set is not an implementation of this item.

  Because this milestone does not build an A/B updater or health-commit protocol, a validly signed
  but broken higher-generation release may require recovery; document that availability tradeoff
  instead of weakening rollback checks.

- [ ] **M6 - the old-loader bypass is closed in the QEMU claim.** Rotate the QEMU policy-loader
  signer at this boundary: enroll the current signer and place the previously trusted loader signer
  in the firmware revocation database. The negative fixture is a real pre-policy loader which was
  accepted under the previous variables image and remains correctly signed by that formerly trusted
  signer; the updated persistent image must refuse it while accepting the current anti-rollback-
  aware loader. Record the signer transition and revocation as part of the trusted initial state.
  Production firmware enrollment/revocation operations and physical recovery remain outside this
  QEMU milestone, so documentation must not generalize the gate to arbitrary machines.

- [ ] **M7 - persistent multi-boot evidence.** A named x86_64 OVMF gate, using ONE persistent
  variables image across the whole sequence, boots generation N, refuses N-1 from both the original
  and a cloned disk, accepts N+1 and advances the floor, accepts N+1 again, DELETES EITHER SLOT AFTER
  THAT ACCEPTED ADVANCE - one per run, both covered - and requires N to still be refused, which is the
  case that fails if an advance converged only one slot. IT ALSO RUNS THE INTERRUPTED SEQUENCE, which
  is the one the uninterrupted case cannot reach: cut power BETWEEN the two writes, reboot the same
  N+1 artifact, delete either slot, and require N to still be refused - which fails unless the
  equal-generation boot converged the slot the interruption left behind. It refuses a damaged or
  substituted state record. Power-loss injection around the state write must not produce an accepted
  lower floor. The recovery fixture succeeds at N+1 and is refused below it, and the cross-use
  negatives from M5 run here. A mixed-generation medium/volume set is refused without advancing the
  floor. The previously authorized pre-policy loader is shown accepted with the old variables image
  and refused after the M6 signer transition, and a CURRENT, correctly signed, NON-ENFORCING loader is
  refused by the same image - which is M2's media-downgrade fixture, and the one that needs the
  separate enforcing signer to have any mechanism behind it at all.

  EVERY PARTIAL PROVISIONING STATE IS BOOTED, not only the two complete ones: the ceremony is
  interrupted after slot A, after slot B and immediately before the marker, and each interrupted
  machine boots as UNPROVISIONED and is then provisioned by re-running the ceremony from step 1. A
  machine with the marker present and both slots invalid refuses, and a ceremony is what recovers it.
  THE "GOES ON REFUSING AFTER THE MARKER IS DELETED" FIXTURE IS STRUCK (2026-08-31): it asked for
  behaviour the state table cannot produce, because a deleted marker is indistinguishable from one
  that was never written - see the threat-model row in M2. Deleting it is an NVRAM-administration
  action this milestone does not defend against, and the fixture that replaces it proves the
  AVAILABILITY consequence instead: after the marker is deleted the machine boots, refuses to
  ADVANCE the floor, and says it is unprovisioned.

  Host fixtures over a mocked firmware cover what QEMU cannot be made to produce on demand: torn,
  short, oversized, wrong-product, wrong-attribute, absent, access-denied, write-failed and
  readback-mismatch reads and writes, and both-slots-invalid. AND TWO MARKER CASES THIS LIST OMITTED
  (added 2026-09-01): a marker read that returns a DEVICE ERROR, and a one-byte marker holding a
  value that is not `0x01`. M2 requires either to refuse rather than read as absent and M3 makes
  `device-error` a distinct result, but neither was evidenced - and the current variable reader
  collapses every unsuccessful status and every wrong size to `None`, which is precisely the answer
  it gives for a variable nobody ever wrote. Without these two watched failures the marker's
  fail-closed distinction is asserted rather than shown, and the distinction is the whole reason the
  marker exists. Shared parser/loader fixtures keep all
  three target builds agreeing on the new manifest. Mutating the loader to skip comparison or
  readback makes the gate fail. Prove separately that a non-enforcing profile still boots with a
  fresh variables image, because that is every other run on this machine.

## Dependencies

- P02M0150 remains complete and owns origin/authenticity through signed manifest v2. This milestone
  adds freshness without changing what P02M0150 claimed.
- P02M0129 and P02M0151 own the loader/platform boundary and typed architecture contract.
- P02M0172 also proposes an authenticated loader-to-kernel value. If both are implemented, the
  manifest evolves once and the compatibility matrix covers both fields together rather than
  churning the format twice.

## Definition of done

- Under the named persistent x86_64 OVMF trust profile, a disk attacker cannot boot a signed
  manifest below the stored security generation or reset that generation by replacing media.
- Enforcement is selected by the loader build - the `rollback-enforcing` profile - never by the
  medium, and the non-enforcing profiles keep their current fresh-variables behavior.
- A provisioned machine whose BOTH floor records are deleted REFUSES, and one whose single surviving
  record is valid BOOTS from it and repairs the other - which never lowers the floor, because an
  accepted advance converges BOTH slots before control is transferred, so whichever slot survives
  carries the current generation. An unprovisioned machine boots and refuses to
  advance. The three are distinguishable, which is what the provisioned marker and the second slot are
  for. ("whose floor record is deleted" stood here and read as though there were one record, which is
  the contradiction corrected 2026-09-01.)
- A higher generation advances atomically only after the complete authenticated artifact set is
  verified, and recovery is a signed PURPOSE enforced against purpose-scoped roots which never lowers
  the floor: recovery credentials cannot sign an ordinary boot set and ordinary credentials cannot
  claim recovery.
- A pre-policy loader is not authorized in the profile used to make the claim.

## What this milestone refuses

- A/B installation, online update transport, health commit, package-level rollback or automatic
  rollback after a failed application start.
- TPM measured boot, remote attestation, production HSM/key ceremonies or physical-hardware
  qualification.
- A general UEFI variable API. One typed write path for these two slots, with the existing argument
  against a broader one answered rather than removed.
- Deduplicating the product-name constant; that belongs to the milestone the loader's own comment
  already names.
- AArch64/RISC-V firmware anti-rollback enforcement before those profiles acquire their own
  qualified firmware-authenticated loader and protected persistent-state gates.
- A claim against an administrator who resets/replaces firmware NVRAM or deliberately changes the
  firmware trust database.
