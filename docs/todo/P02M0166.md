# P02M0166 - Everything that went wrong is reported as `failed`

Status: PLANNED AFTER P02M0165. OBSERVABILITY, TYPED FAILURES AND OPERATOR POLICY. LAST OF SEVEN - see P02M0098.

## Goal

Make the state of every binding readable from outside, with one vocabulary for why something is not
running, and let an operator choose among the drivers a device already declared - without being able
to widen what any of them may do.

## What is there now

- **TWO CAUSES CAN BE TOLD APART AND THE REST CANNOT.** `STATE_UNBOUND` and `STATE_DRIVER_MISSING`
  were separated under P02M0098 on 2026-08-12, and they are DeviceManager's own states, printed.
  Everything else - a claim that failed, a protocol mismatch, a dependency not met, a driver that
  hung - has nowhere to be said.
- **`lsdev` AND THE SYSTEM GRAPH SHOW WHAT WAS TRUE AT BOOT.** They do not show the selected artifact,
  the match rule, the generation, the granted resources or the published providers.
- **THERE IS NO OPERATOR CONTROL AT ALL.** Which driver binds a device is decided entirely by the
  compiled registry.
- **THE GRAPH REPORTS A CONSTANT WHERE A STATE SHOULD BE.** Every device node it emits carries
  `ComponentState::Running` whatever its binding is doing, so the surface that looks most like a
  status display is the one showing none.

## Work

- [ ] **M1 - the record is rendered as what it is: a state, and if it failed, a cause.**
  `BindingRecord { state, failure: Option<FailureCause> }` is defined in P02M0162; this milestone
  renders it. `Unbound`, `Binding`, `Online`, `Stopping`, `Backoff`, `Quarantined`, `Removed` say
  WHERE a binding is; `driver-missing`, `artifact-mismatch`, `protocol-mismatch`,
  `dependency-pending`, `iommu-required`, `resource-exhausted`, `hung` say WHY it got there. One list
  holding both makes "quarantined" and "hung" look like alternatives when they are two halves of one
  answer. Every surface renders both without collapsing either.

- [ ] **M2 - `lsdev` shows the binding.** Per node: the selected artifact, the match rule that chose
  it, the binding unit, the generation, the state and its cause, the restart count, the granted
  resources and the published providers. A controller's health is its own: an xHCI with only a
  keyboard on it is online.

- [ ] **M3 - `lsdev` and the System Graph read one typed snapshot.** Both ask DeviceManager for the
  same `BindingRecord` set rather than each deriving a state - which is what makes the graph's
  unconditional `Running` impossible to write. The log is not a third rendering to compare against:
  it is a history of transitions and the snapshot is a moment, so the test compares the same enums
  and the same transition records, not live text against an instant.

- [ ] **M4 - operator policy, bounded by the registry, stored in ConfigService.** An operator may
  disable a device or select among the candidates the registry already declared for it; selecting
  something the registry did not declare is refused, because policy narrows and never widens. Only
  those two are PERSISTENT - a retry is an action, not a stored preference. What is stored is a
  stable device identity plus a registry entry id, and every load re-checks that the entry is still
  among that device's candidates, so a stored choice naming an entry that no longer matches is stale
  rather than an override. Boot-critical bindings are OUT: the policy lives on a volume that is not
  mounted when those bindings are made, which is a dependency the wrong way round.

- [ ] **M5 - the diagnostic snapshot is displayed.** P02M0165 captures it, bounded and free of device
  payload, before the process is gone; ServiceManager holds the last one so it survives both the
  driver's death and DeviceManager's. This milestone shows it. That division is what keeps it from
  being a new diagnostic subsystem: the capture belongs where the teardown is, and the display
  belongs here.

## Definition of done

- Every state and every failure cause has one name, and `lsdev` and the System Graph render both from
  the same snapshot - no surface reports a constant where a state belongs.
- `lsdev` shows the artifact, rule, unit, generation, state and cause, resources and providers of
  every binding.
- An operator can disable a device and choose among declared candidates, cannot grant a driver
  anything the registry did not, and a stored choice that no longer matches is reported as stale
  rather than applied.
- No boot-critical binding depends on a policy stored on a volume that binding has to mount.
- A failed or hung binding's snapshot is visible after the driver and DeviceManager have both died.

## What this milestone refuses, and why

- **Owning the system volume's identity.** Which volume is the system one is a boot-correctness
  question, not an observability one, and it is an item in P02M0165 where cold reconstruction needs
  it. It is also smaller than it looks: P02M0150 put the paired volume's uuid in the boot medium's
  signed manifest and the loader verifies and reads it, so what is left is carrying it past the
  loader and probing block providers against it.
- **A monitoring or metrics subsystem.** These are three existing surfaces showing state that already
  exists in one place.
- **An operator policy language.** Disable, retry and choose-among-declared is the whole of it. A
  policy that can express more than the registry declared is a way to widen authority from a
  configuration file.
- **Persistent history.** The snapshot is about the failure that just happened. Keeping a record over
  time is a logging question and this tree has one.
