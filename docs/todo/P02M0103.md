# P02M0103 - The graphics rendering foundation

Status: PHASE-4 FUTURE VISION. NOT AN ACTIVE PRODUCT MILESTONE AND NOT A PHASE-2 COMPLETION GATE.
ACTIVATED ONLY AFTER THE SERVER PHASE AND BY EXPLICIT PROJECT-OWNER APPROVAL, PART BY PART.

Corrected 2026-08-30 after the 2026-08-30 plan audit. The status line said only "PLANNED AFTER
P02M0096a AND P02M0097", and both of those are complete - so as written it authorised a ten-part
subsystem to start immediately. That is not what the roadmap says: `docs/todo/TODO.md` records that
server, general real-hardware, desktop and AI directions remain VISION rather than active product
milestones, and `docs/CONCEPT_EN.md` orders server before desktop and puts the desktop platform in
Phase 4. A dependency on two finished milestones is not an activation gate; this line is.

WHICH PARTS MAY START EARLIER, AND ON WHOSE APPROVAL. The whole is Phase 4, but three pieces are
genuine cross-phase foundations that other milestones already depend on, and each is separately
approvable:

    P02M0103s (the normative specifications)   may be written at any time; it produces documents
    P02M0103a-common (image and colour model)  what `P02M0136` and every renderer need; no display
    P02M0103b + c (Profile 1 and soft2d)       the 2D foundation a compositor is built ON, and
                                                 the renderer `P02M0136` proves text against

AND `a-wsi` IS NOT ONE OF THEM, WHICH THIS LIST CLAIMED AND THE ROADMAP NEVER SAID (corrected
2026-09-04). It was added here with a note that `docs/todo/TODO.md` "carries the same correction";
that file's P02M0103 line permits exactly `s`, `a-common` and `b`+`c` as cross-phase foundations and
does not name `a-wsi`. Two documents cannot give different answers about whether WSI implementation
may begin before the desktop phase, and the roadmap is the one that decides scope - so the entry is
withdrawn rather than the roadmap widened to match a claim made here. `a-wsi` remains Phase 4 like
the rest, and `d`, `g` and `i` remain gated on it.

Everything from `P02M0103e` onward - render-math, Render3D, scene3d, soft3d, the 3D conformance suite
and the interactive 3D application - is Phase 4 and is not approved by approving the 2D foundation.

AND THE SCOPE IS INVERTED RELATIVE TO WHAT PHASE 4 ASKS FOR, which is recorded here because it
decides what gets built first rather than merely how it is labelled. Phase 4's requirement is a
GUI/compositor, a window manager and shell, and accelerated graphics on the reference virtual
platform. This file DEFERS all three - the compositor, multi-application presentation, the widget and
accessibility stack, every Mesa/OpenGL/Vulkan path, virgl/Venus and virtio-gpu 3D execution - and in
their place makes a complete vector-effects engine, a custom programmable shader IR, a full software
3D pipeline and an engine-class retained scene layer MANDATORY before completion, with no mandatory
profile feature permitted to return `Unsupported`. (Animation, skinning, morph targets, PBR/IBL,
shadows, HDR and postprocessing have since been moved out of that mandatory set into `f-ext`.)
It is possible to spend years completing that and still not have the thing Phase 4 was defined to
deliver.

So the ORDER is fixed here, and the part boundaries now express it: the compositor-facing 2D
foundation (`s-common`, `a-common`, `s-2d`, `b`, `c`) is delivered and exercised FIRST, and
`a-wsi`/presentation and compositor integration do not GATE it - they hang off `s-wsi` and
`a-common` and are delivered beside `b` and `c`, on their own Done gate.
(The word was "follow", and three other places in this file read that as three different graphs -
the matrix kept one undivided `a` row, the build-order paragraph still said `s -> a -> b`, and the
`a` section said `a-wsi` runs beside `b` and `c`. "Beside, gating nothing" is the one that matches
the freeze section and the part headers. Corrected 2026-09-01.)
AND "GATING NOTHING" IS ABOUT THE TRACKS' API WORK, NOT ABOUT EVERY PART IN THEM (corrected
2026-09-02). `a-wsi` gates neither track's specifications or API work - which is what lets `b`, `c`,
`e` and `f` be finished with no window system - and it DOES gate the three parts that end at a
live surface: `d`, `g` and `i`. `h` also waits for `a-wsi` transitively through `g`; a headless
conformance suite cannot complete before the backend whose integration it tests. The 2026-09-01 correction put that edge in the matrix, said it had
put it in the build-order paragraph as well, and had not; the two then described different schedules.
All four places now carry the same sentence, and the canonical-review list at the end of this file
carries it too. `a` is divided into `a-common`
and `a-wsi` with a Done gate each, because the order above was impossible while one heading carried
both. The 3D API stays EXPERIMENTAL - it may be designed and
implemented, and it may not be declared stable - until it is co-designed with a concrete accelerated
backend or validated by a second implementation, because this file's own rule says a shape with
fewer than two real consumers is a guess and `render3d` has exactly one, `soft3d`, with every
accelerated implementation deferred. Scene-engine features are part `f-ext`, a separately activated
part with its own `Scene3D Extended Profile 1` and its own gates, driven by a real consumer rather
than sitting in Profile 1's mandatory set - and they are absent from every core work item,
conformance list, demo scene and aggregate Done clause, which is what makes "optional" mean
something.
And the title is narrowed with it, in the heading rather than only in this paragraph: this is the
graphics RENDERING FOUNDATION. Text is `P02M0136`, pointer and touch are elsewhere, and the
compositor and application platform are not here - so "the platform desktop and mobile applications
are built on" overstated what completing this file produces. `docs/todo/TODO.md` carries the same
name and the same correction.

The reconciliation record at the end maps the earlier plan corrections to their authoritative
sections. The correction checkboxes record decisions in this plan, not implemented graphics or frozen
specifications. The unchecked work below writes and freezes the normative profiles before any backend
implements them; there is one current contract for each item.

**REWRITTEN 2026-08-15 to be a single statement of the work.** It carried its outside reviews inline
for two rounds, and the third review's sharpest finding was about the document rather than the
design: the main text still said "24- or 32-bit depth" and "textures remain optional future work"
while a review section two hundred lines below said "pick one depth format" and "textures are not
optional" - both as open checkboxes, with nothing to say which was newer. The reviews are gone;
their conclusions are the items below and their reasoning is stated where it changes a decision.
Everything in this file is what to build.

A fourth pass closed items that required a value to be CHOSEN without choosing it. "State the
handedness", "a small cap", "pick a depth format" are not normative. Where such an item appears below
it now carries the number: `MAX_DAMAGE_RECTS = 16`, 8 subpixel bits, the clip-plane inequalities,
`MAX_CLIPPED_VERTICES = 9`, column-major `M * v` right-handed with `[0, 1]` clip depth and CCW front
faces.

A fifth pass drew the line those numbers need. "Two implementations must produce the same pixels" is
true of the INTEGER half of a rasteriser and is not achievable for the floating-point half - operand
order, FMA contraction, the libm in use and the optimiser all move the last bit. So the contract is
split and each item says which side it is on - and the split is PER DIMENSION, because 2D and 3D
coverage are not the same problem. In 3D, coverage is discrete sample-point classification against
quantised edges and IS bit-exact. In 2D, coverage is an antialiased area estimate, and this file
deliberately lets a backend choose its coverage algorithm - so 2D coverage is TOLERANCE-BASED while
2D path topology, clip classification and quantised geometry stay deterministic. Requiring a
particular coverage number from an unspecified algorithm was a contradiction between the split and
the antialiasing item. BIT-EXACT: geometry classification, clipping topology,
viewport quantisation, 3D coverage (sample-point classification), shared-edge ownership, and depth
clear, storage and comparison. 2D COVERAGE IS NOT IN THAT LIST and the word appeared unqualified
here for two passes while the paragraph above it said the opposite (corrected 2026-08-30).
TOLERANCE-BASED: interpolated floating attributes, filtering, shading and the final RGB conversion.

## The per-part prerequisite matrix, and the two things nothing supplies

Added 2026-08-30. The status line named two finished milestones and nothing else, while parts of this
file require driver-death handling, backing reacquisition after a driver restart, complete cleanup,
DisplayService surviving its own restart, SystemGraph accuracy and trustworthy tri-architecture
evidence. None of that comes from `P02M0096a` or `P02M0097`.

    part                       hard prerequisites
    s                          none; it produces documents
    a-common                   `s-common`
    b                          `s-2d` AND `a-common`. NOT the self-thread runtime: a single-threaded
                                 tiled rasteriser conforms, and the worker pool is a later
                                 performance item (below)
    c                          `b`
    d                          `c` AND `a-wsi`
    e                          `s-3d` AND `a-common`
    f                          `e` - the part's own header says so, and it is the edge the three
                                 ordering statements disagreed about
    g                          `e` AND `a-wsi`. NOT `f`: `soft3d` implements the Render3D API that
                                 `e` freezes, and `scene3d` is a separate CONSUMER of that API which
                                 generates command lists for it - so a backend waiting for a
                                 convenience layer built on top of it is backwards. `g`'s own item
                                 says it is done without `f-ext`'s features, and `f`'s header says it
                                 depends on `e`; the linear `e -> f -> g -> h -> i` in the build-order
                                 paragraph was a listing of part LETTERS read as a dependency chain
                                 (resolved 2026-09-02, in favour of the branched graph the canonical
                                 review had already reasoned its way to)
    h                          `g` AND `f` AND `c` - a conformance suite needs the implementation it
                                 tests, the retained layer whose behaviour it covers, and the 2D
                                 backend used by its mandatory 2D/3D interop scenes
    i                          `h` AND `a-wsi` (`c` is inherited through `h` for the render2d HUD)
    f-ext                      `f` AND `g` AND `s-3d-ext` - ADDED 2026-09-04. The matrix calls itself
                                 the complete edge set and did not contain this part at all, while
                                 its own section states the first two of those prerequisites. The
                                 third is new and is the point of the correction: see below

    THE EDGES ARE IMMEDIATE PREDECESSORS AND THE RELATION IS TRANSITIVE: `c` waits for `b`, which
    waits for `s-2d` and `a-common`, so `c` waits for those too and the table does not repeat them.
    This is the complete edge set, and until 2026-09-02 it was not - the rows carried the freeze and
    common prerequisites and `a-wsi` and NO intra-track edge at all, while the build-order paragraph
    called this table authoritative. A table declared authoritative has to contain the edges.

    AND `a-wsi` GATES THE THREE PARTS THAT PRESENT, WHICH WAS THE 2026-09-02 CORRECTION - `d`, `g`
    and `i` above. Its reasoning, kept because it is what a reader will question:
                                 MISSING (corrected 2026-09-02). The previous rows gave the whole 3D
                                 track one line and `d` none, on the reading that `a-wsi` gates
                                 neither track. It gates neither track's API work, which is what
                                 that sentence was about; it does gate the three parts that PRESENT.
                                 `d` must run a live display application, exercise a second surface,
                                 scale and resize, and pass a live QEMU presentation gate; `g` calls
                                 itself the integration gate for acquire/present/release and frame
                                 pacing; `i` ends at a `Surface` and its Done condition requires
                                 resize, focus, console restoration and a focused QEMU presentation.
                                 None of those is reachable without the present queue, the completion
                                 pairs and the DisplayService migration `a-wsi` owns, so an
                                 implementer following the old matrix would have reached a Done gate
                                 with no mechanism to satisfy it.
                                 THE ALTERNATIVE WAS CONSIDERED AND REJECTED: splitting each of the
                                 three into a headless half and a presenting half would let `e -> f`
                                 proceed without WSI, and it buys that by turning three parts into
                                 six with two Done gates each, for a track whose whole point is that
                                 a conformance suite and a test application are single deliverables.
                                 The edge is the smaller thing to carry
    a-wsi / presentation       `s-wsi` AND `a-common` - both, and this row named neither until
                                 2026-09-01 while the authoritative subsection required both. Plus
                                 P02M0141 (service restart), P02M0165 (stop and drain), P02M0166
                                 (removal and graph state), and the COMPLETION PAIRS (below).
                                 NOT P02M0164 (corrected 2026-08-31): the catalogue mechanism is
                                 complete, and the DisplayService migration that WSI needs is a
                                 DELIVERABLE of `a-wsi` rather than a prerequisite of it - see the
                                 P02M0164 row below and the `a-wsi` header, which this line
                                 contradicted
    imported or DMA images     P02M0153's portable synchronisation contract - OR Profile 1 is
                                 limited to coherent CPU MemoryObjects and cache maintenance is
                                 assigned by name to the GPU-import milestone. THE SECOND IS THE
                                 ANSWER for v1.
    any accepted evidence      P02M0167 (isolated run identity and artifacts)

MARKED BY CONTRACT, NOT BY STATUS LINE (corrected 2026-08-30). The previous version of this matrix
read the milestones' `COMPLETE` labels and declared the WSI's prerequisites met except for one. A
status line is a claim about a milestone; what this file needs is a behaviour, and three of them are
qualified below in the current tree, irrespective of status labels:

    P02M0164   THE CATALOGUE MECHANISM AND A PARTIAL DISPLAY MIGRATION ARE IMPLEMENTED
                 (reviewed 2026-09-08). DisplayService receives `CATALOGUE`, subscribes to display
                 providers, opens a publication, releases scanout on driver-channel close and adopts
                 a later publication while its slot is empty. It also reports withdrawal frames.
                 The old claim that it still receives only a fixed `GPU` bootstrap handle is stale.
                 What remains for `a-wsi` is the full consumer contract: track provider identity and
                 generation, detach the matching withdrawn provider, retain/select alternatives,
                 fail over or reopen after channel loss, recover the subscription, and validate the
                 typed versioned display-device contract. The existing one-slot consumer ignores
                 another live provider while occupied and does not implement that complete policy.
                 P02M0099 and `a-wsi` continue to co-own this work: whichever first adds a second
                 display provider or WSI performs the remaining migration; the other consumes it.
                 There is no circular prerequisite on the migration this part itself owns. Reuse
                 the landed catalogue seam, and prove the remaining identity/failover behavior here.
    P02M0167   LIVE SELECTION/TAG PROOF PASSED; PRODUCER-SAFE ACQUISITION REMAINS OPEN
                 (reviewed 2026-09-08). Selection-specific kernels and loaders are staged under
                 build locks; ISO paths are content-keyed, assembly is serialized and cache hits
                 verify recorded output digests. USB/system-disk templates now have content-keyed
                 generations and run-private writable copies. Those repairs discharge their own
                 races; they do not snapshot every medium producer. `mkimage.sh` still reads the
                 shared init/volume packages, bootstrap files and manifest while the kernel build
                 lock is no longer held. An assembly lock and post-assembly key comparison detect
                 movement rather than stabilizing those inputs, including a same-content rewrite.
                 P02M0170's M1/M2 own that complete acquisition boundary and its producer/acquisition
                 mutation proof. UNTIL THAT BOUNDARY AND PROOF ARE COMPLETE, ACCEPTANCE RUNS ONE GUEST
                 ARCHITECTURE AT A TIME WITH NO OVERLAPPING SAME-ARCHITECTURE SELECTION OR
                 MEDIUM-INPUT PRODUCTION. Serial guest selection alone does not exclude a package,
                 bootstrap or manifest producer from rewriting an input during assembly.

                 The scheduler's registered host gate exercises the actual executor, including
                 prerequisite order, failed-descendant suppression, shared prerequisites, status
                 precedence, seeded cost and guest-slot accounting. Catalogue narrowing is also
                 repaired: `candidate::catalogue_losses` compares every runnable variant's
                 `(check, architecture, environment, configuration)` identity per covered component.
                 Activation calls `evidence_failures` before materialisation, so removing a variant
                 while keeping its check ID and `covers` still invokes the evidence requirements.
                 The real-registry regression
                 `a_candidate_that_narrows_only_the_registry_is_still_a_narrowing` constructs a
                 `host_configuration_unrunnable` narrowing and passes in this review. No catalogue
                 repair belongs in this milestone.

                 The actual live same-architecture selection/tag gate PASSED after the private-file
                 admission fix, as recorded by P02M0167's combined verification closure and
                 `/tmp/libersystem-final-concurrency-recheck.log`. Two x86_64 suites with disjoint
                 selections and `dma`/`domain` tags passed with distinct run logs; the production
                 `check-concurrent-selection.sh` checks each guest ran its own selected IDs and none
                 of the other's. That executed proof is discharged, not still owed or inferred from
                 host tests. It does not prove input stability during producer publication or
                 acquisition, staged-digest binding, or P02M0170's hostile replacement cases. The
                 remaining serial restriction above is for that unsatisfied producer/acquisition
                 boundary, not for absence of this completed live selection/tag check.
    P02M0141   UNMET, and unambiguously: its own status line says PLANNED, its Definition of Done
                 says no previously non-restartable service can be restarted safely, and
                 DisplayService is still `restart = "escalate"`.

The remaining prerequisite claims are narrow. P02M0153's non-coherent/DMA import contract is not
required by this CPU-only v1; such imports remain outside its admitted image set. P02M0163 supplies
the current discovery/bind model, not general hotplug discovery. P02M0166 supplies removal and graph
transitions, including refusal of enable during any planned stop; the prior dependency-stop hole is
repaired. P02M0162 supplies the teardown/rebind mechanism, but its M5 target-window measurement is
still handed to consumers: the 300/400/4000 constants were round counts and are not accepted timing
evidence merely because they are now consumed as ticks. Before accepting driver-loss/reacquisition
on a target, record its measured window and run the relevant recovery gate. This does not add a
second deadline mechanism or block writing the graphics specifications.

P02M0165 supplies the stop-and-drain implementation; its current evidence is qualified rather than
inferred from its status (reviewed 2026-09-08). `stop_all` drives nodes with bindings or pending
teardowns through `settle_shutdown_node`, sharing the normal event queue and Pending ledger. Each
pass reads ready process and claim confirmations before advancing or classifying expiry. A process
is ready when `wait(process, now.max(1)) == 0`; `wait_any` is only a bounded wake, so its zero-based
index is not discarded. The overall shutdown bound covers the existing stop allowance and teardown
fallback; unconfirmed outcomes land in quarantine. The earlier `ready > 0` defect is repaired.

The host regression covers shutdown selection with a binding, an earlier stop and a pending teardown.
The development-only `device_manager::tests::pending_shutdown_outcomes` additionally drives the
production settlement function with a sole ready handle and a non-ready timeout and checks the
confirmed/quarantined distinction. `dev-gpu-restart.py` requires its success marker. These are
implemented oracles; this plan review does not claim a fresh guest or mutation-test pass. `a-wsi`
acceptance must run the registered driver-restart gate against the tree it consumes and must not
infer teardown confirmation from a `STOPPED` message alone.

Withdrawal also has two different ownership cases. `driver_binding::apply_withdrawal` orders close
before announce, with host recorder tests for ordering and completeness. An unopened offered endpoint
is still catalogue-owned: the development-only `unopened_provider_withdrawal` test calls the actual
`Catalogue::withdraw_binding`, checks the real peer closes, checks no stale late-subscription replay,
and checks an old-generation withdrawal cannot close a replacement. It also decodes the withdrawal
frame. The driver-restart gate requires this oracle's marker; claiming that production close has no
test is stale. Once `open` transfers the offered endpoint, the catalogue no longer owns that handle,
and closing its entry cannot revoke the consumer's endpoint. Driver peer-close remains the data-path
loss signal. WSI must process both catalogue withdrawal metadata and peer-close idempotently, release
a withdrawn connection itself, and reopen a live matching provider by identity. It must progress on
peer-close even if an announcement is delayed or absent. Its own migration gate proves observable
withdrawal, detach and reacquisition; the existing tests do not prove that future consumer behavior.

`docs/todo/TODO.md` ticks P02M0141 as complete while its own file says PLANNED, which is a
discrepancy this file must not rely on in either direction. The WSI's restart and rebind semantics
are blocked on P02M0141 actually closing, and until it does this file may not describe them as
available. They are NOT additionally blocked on P02M0164 (corrected 2026-08-31): its consumer
migration is `a-wsi`'s own deliverable, so naming it here as a second block made the part wait for
work it performs itself.

AND ONE THING IS OWNED BY NOBODY, down from two (2026-08-30). The completion primitive is DECIDED -
ordinary channel pairs, TWO of them per SURFACE (corrected 2026-09-03: this said "per present", which
was the arithmetic before the endpoint became per-surface, see the present-queue item) - so it needs
no new kernel object, no ABI number and no owner beyond this file; pass 10's asynchronous submission
model shares that same decision.

What remains unowned is the SELF-THREAD RUNTIME, shared with `P02M0135`. It is no longer a
prerequisite of anything mandatory here: multithreading has been removed from `c`'s and `g`'s Done
conditions, because a completion condition that requires an unowned mechanism cannot be met and its
presence made both parts unschedulable. Tiled single-threaded rasterisers are conforming; the worker
pools are a later performance item that consumes the runtime when somebody numbers it.

## Who consumes this API, and who instantiates the backend

Added 2026-08-30. This file promises public `render2d` and `render3d` APIs "for ordinary applications
written by other people" and does not say through what. The project's application ABI is WebAssembly
components; the existing `.lslib` Rust ABI is explicitly an image-internal build optimisation and not
a cross-release third-party ABI. So as written there is no supported route by which a third-party
application reaches the promised API at all, and the in-process `DrawList` decided above does not
create one.

The answer this file takes, stated so it stops being ambiguous: **these are INTERNAL SAME-IMAGE RUST
APIs in v1.** Their consumers are in-tree applications and toolkits built from the same tree, and
that is what "public" means here - public within the image, versioned with it, not an ABI anyone
outside the release links against. A native SDK boundary or a Wasm-host graphics adapter, each with
its own version, resource, copy, lifetime and capability semantics, is a SEPARATELY APPROVED
milestone and is the only thing that makes the third-party claim true. Until one exists this file
does not make that claim.

AND SOMETHING HAS TO INSTANTIATE THE BACKEND. This file says applications never name `soft2d` or
`soft3d` and that only a backend depends on them, while deferring the OS graphics device and factory
vocabulary and naming no crate or service that selects and creates the software backend - so an
in-tree application must either name the backend directly, breaking the layering, or invent a
factory. v1 permits COMPILE-TIME SELECTION: one small composition crate owns the choice, every
consumer takes the trait, and the crate is the single place `soft2d` is named. A runtime
device/factory vocabulary with manifest and provider edges arrives with the first second backend and
not before, because that is the milestone that can test it.

AND THE 3D PROFILE IS NOT DECLARED STABLE AGAINST ONE BACKEND. The command, resource and submission model
is validated against a concrete SECOND backend - a GPU backend, or a deliberately different-shaped
second implementation - before "Profile 1 is stable" is said of it, by this file's own rule that a
shape with fewer than two real consumers is a guess.

A sixth pass changed what this milestone is FOR. Every pass before it improved the inside of a
document whose purpose was "enough graphics to prove a present queue and then draw a software cube".
The real purpose is a platform for a desktop environment, a mobile shell and ordinary applications
written by other people. That pass introduced `render2d` above `soft2d`, because `soft2d` was both
the public API and the CPU implementation of it, and it un-deferred paths, antialiasing, transforms,
clipping, layers, compositing and text - which are not advanced features a desktop wants eventually
but what an icon, a button, a scroll viewport and a line of text are MADE OF.

**A seventh pass finished that correction, because the sixth applied it to one dimension and not the
other, and because "complete" was still an adjective rather than a gate.** Two changes:

  - **`render3d` was missing, and its absence was the same mistake the sixth pass had just fixed.**
    The layering read `render-scene -> soft3d` and was described as the equivalent of
    `render2d -> soft2d`. It is not. `render2d` is a DRAWING AND COMMAND API; `render-scene` is a
    retained DATA MODEL. The 3D counterpart of `render2d` is a resource, pipeline, pass and command
    API - and with it missing, an application wanting a CAD viewport, a custom engine, a
    postprocessing chain, render-to-texture or a non-standard material had nowhere to go except the
    scene graph or the CPU rasteriser. The stack is now `scene3d -> render3d -> soft3d`, and a future
    GPU backend implements `render3d` rather than a whole scene graph.
  - **"Complete" is now two finite PROFILES rather than a word.** `Render2D Core Profile 1` and
    `Render3D Core Profile 1` each enumerate a closed list of features that the public API expresses,
    the software backend implements ENTIRELY, and a conformance suite covers. Inside a profile a
    backend may NOT answer `Unsupported`; that variant exists only for post-profile extensions. This
    is what converts "a complete software library" from an intention into something a gate can refuse.

That pass also removed three phrases that quietly capped the design: `conic gradient in the type
space even if the first backend refuses it`, `non-separable blend modes are deferred by name`, and -
the largest - `soft3d implements a subset and returns a typed Unsupported for the rest`, together
with the scene-model item that required hierarchy, passes, offscreen targets, instancing and skinning
only to be ADMISSIBLE rather than implemented. A library whose backend may decline its own API is a
specification for a demo.

**An eighth pass closed the contracts INSIDE those layers, and corrected a guarantee the seventh pass
had quietly broken.** The architecture did not move; four direct contradictions and one whole class of
under-specification did. The contradictions: the compositing line said "12 Porter-Duff operators" over
a list of thirteen (`Plus` is additive and is not one of them); the 3D profile named five primitive
topologies and the backend then promised "all six"; `sRGB8A8` sat in the format list while
`ColorSpace` already carried the transfer function, so two places could disagree about whether bytes
are encoded; and the 2D part required loading a real scalable font while the milestone that parses
font files, `P02M0136`, depends on this one - a dependency cycle in which each milestone waits for the
other or writes a throwaway parser twice.

The broken guarantee is the one worth naming on its own. The fifth pass promised BIT-EXACT geometry
classification and clipping topology, and that was achievable when the vertex pipeline was
fixed-function. The seventh pass added a PROGRAMMABLE vertex stage, whose output position is computed
by floating-point shader code that the same split puts on the tolerance side - so the input to a
bit-exact stage stopped being bit-exact, and two architectures could classify a vertex at
`z = +/-1e-8` differently and BOTH conform. A guarantee that its own inputs cannot support is worse
than no guarantee. The fix is to make the seam explicit: the clip-space POSITION output has strict
floating-point semantics of its own - fixed operand order, no FMA contraction, canonical NaN, stated
rounding - and is then QUANTISED into a canonical clip representation before any classification runs.
Everything downstream of that quantisation is bit-exact; every other varying stays tolerance-based.

The under-specified class is everything a profile entry names without defining: what a tone-mapping
operator or a dither actually is, what a blend factor set contains, where MSAA samples sit, what a
shader IR may express, what a resource descriptor holds. Those are listed below with values, because
a profile whose entries cannot be implemented twice the same way is a list of intentions.

**On scale, stated plainly rather than discovered later:** the profiles below are the work of a
compact 2D vector engine and a compact software 3D pipeline with programmable shading. That is a
subsystem, not an ordinary milestone, and the parts exist so it can be built and gated in
order rather than attempted at once. The build order is `s-common -> a-common`, then
`s-2d -> b -> c -> d` for the 2D track, and for the 3D one a BRANCH rather than a chain:
`s-3d -> e`, then `e -> f` and `e -> g` in parallel, both meeting at `h`, and `h -> i`. `h` additionally consumes `c` for the 2D/3D interop
scenes, and `i` inherits that backend for its HUD. `d` gates nothing in 3D and `e` can start as soon
as `a-common` and `s-3d` are done.
THE 3D TRACK WAS WRITTEN HERE AS `e -> f -> g -> h -> i` AND THAT WAS A LIST OF LETTERS, NOT A GRAPH
(resolved 2026-09-02). `f` is `scene3d`, a convenience layer that GENERATES command lists for the
API `e` freezes; `g` is `soft3d`, a backend that IMPLEMENTS that API. A backend waiting for a layer
built on top of it is backwards, `f`'s own header says it depends on `e`, and the canonical review at
the end of this file had already frozen the branched form. Three places said two different things
about whether `g` waits for `f`; they now all say it does not. `a-wsi` hangs
off `s-wsi` and `a-common` and blocks NEITHER TRACK'S API WORK: it is delivered beside `b` and `c`
rather than before them.
IT DOES GATE THE THREE PARTS THAT PRESENT, and this paragraph said "neither track" without that
qualification (corrected 2026-09-02 - the correction was claimed on 2026-09-01, was written and lost
before the file was saved, and the matrix and this paragraph therefore described two different
schedules for a day). `d`, `g` and `i` each wait for `a-wsi` IN ADDITION to the predecessor their
track's chain above gives them. The distinction is the whole reason the split exists: `b`, `c`, `e`
and `f` produce and conform APIs that draw into images and present nothing, and can be finished with
no window system at all; `d`, `g` and `i` each end at a live surface on a screen.
AND `h` IS NOT IN THE FIRST GROUP, WHICH IS AN ORDERING ERROR AND NOT A WORDING ONE (corrected
2026-09-04). What `h` DOES present is nothing - it is a conformance suite - but the matrix gives it
`g` AND `f` as hard prerequisites, `g` waits for `a-wsi`, and the matrix states in as many words that
the relation is TRANSITIVE. So `h` cannot complete without a window system, and listing it among the
parts that can was the two statements describing different schedules again, which is the defect the
2026-09-02 correction was made for. A conformance suite needs the implementation it tests; that
implementation presents; so the suite inherits the gate. What a part presents does not decide when it
may finish - its prerequisites do. The matrix is
where the complete edge set is recorded and is authoritative where a chain here is read against it.
(This said `s -> a -> b` and `s -> a -> e`, written when `a` was one part and `s` one freeze. Read
after the split it made all of `a-wsi` - presentation, the compositor seam, the whole WSI profile -
precede two APIs that present nothing, which is the opposite of the order the header fixes. Three
places said three different things about where `a-wsi` sits; they now say this one. Corrected
2026-09-01.) `s` comes first in both because it is the normative specifications, and a
backend written before its specification is the specification. What is NOT negotiable in either track is that no part is called done while its
backend can decline a command its profile requires.

Three things stay DEFERRED, all by the same rule this tree applies elsewhere - a shape with fewer
than two real consumers is a guess written down:

  - the graphics adapter/device/context/queue vocabulary, which had one consumer and it was the null
    backend written to exercise it. That is the OS-level device model, and it is distinct from
    `render3d`, which is a userspace rendering model with two planned implementations;
  - the generic timeline/fence capability. What presentation needs is a completion mechanism for
    presentation, which is specified in `a`; a system-wide timeline is worth extracting when GPU,
    video decode or DMA also want one;
  - the resource accounting for device-local bytes, contexts, queues, command bytes and in-flight
    submissions, none of which exist once the above are deferred.

The C/C++/TLS/Mesa substrate lives in `P02M0135` and the text shaping stack in `P02M0136`. Neither
gates anything here: `render2d` consumes an already-shaped `GlyphRun`, and the Rust renderers never
needed a C++ substrate.

The architectural split is deliberate: OpenGL/OpenGL ES/Vulkan are userspace API libraries; WSI,
presentable images, synchronisation, governed memory and access to a capability-scoped graphics
device are OS contracts; virgl/Venus/native-device protocols belong to a backend/driver. The software
renderers exercise the same window-system, resource-lifetime, resize and frame-pacing contracts but
remain ordinary CPU clients of DisplayService. They are not a fake OpenGL implementation and no
provisional `gl*`/`vk*` API is introduced merely to draw a demo.

The layering, once, so no item below has to restate it:

    application / UI toolkit / game / CAD / editor
         |                                    |
         |                                 scene3d          retained hierarchy, cameras,
         |                                    |             lights, materials, culling,
         |                                    |             animation, pass graph
      render2d                            render3d          resources, pipelines, passes,
    Canvas, DrawList, paths,                  |             shaders, command lists
    paints, clips, layers,                    |
    filters, glyph runs                       |
         |                                    |
      soft2d ... future gpu2d           soft3d ... future gpu3d
         |                                    |
         +------------ graphics-core ---------+
              images, colour, sampling, compositing
                            |
                   Surface / present queue
                            |
                      DisplayService
                            |
                      display driver

## P02M0103s - The normative specifications, frozen before any backend exists

**IT IS FIVE FREEZE POINTS, AND THEY ARE DECLARABLE** (2026-08-30, corrected to four 2026-09-01 and
to five 2026-09-04 - see `s-3d-ext` below, which `f-ext` needed and which nothing froze).
`s-common`, `s-2d`, `s-3d`, `s-wsi` and `s-3d-ext` have the freeze criteria below and individual
Done clauses. The first four govern the core; the fifth governs only the separately activated
Extended profile. None is declared frozen merely because its plan is complete.

**AND `s-wsi` WAS MISSING, WHICH LEFT THE WSI THE ONE NORMATIVE DOCUMENT NOTHING FROZE** (added
2026-09-01). This section's own rule is that a normative specification is frozen before the part that
implements it. `WSI_PROFILE_1.md` is a normative specification item of this section, and the still-open
WSI rows assign material contract choices to it - memory and cache ordering for shared surfaces, the
atomic surface-configuration rule, and pre-compositor multi-surface semantics - while `a-wsi`'s
prerequisites named `a-common`, P02M0141, P02M0165, P02M0166 and the completion object and no freeze
at all. Its Done clause asked only that those contracts be "documented", which is a weaker thing than
frozen and is exactly what a freeze gate exists to make checkable: an implementation could begin, and
the document could still change under it.

    s-common   `docs/GRAPHICS.md` and `docs/graphics/IMAGE_COLOR_PROFILE_1.md`: the layer names, the
                 image and colour model, the format and alpha sets, the coordinate spaces and the
                 canonical linear intermediate. FROZEN when both documents are written and reviewed
                 and the machine-readable format/alpha/colour registry they describe exists.
                 Releases `a-common`.
    s-2d       `docs/graphics/RENDER2D_PROFILE_1.md` and the `Render2D Core Profile 1` registry
                 entry. FROZEN when every mandatory command, paint, clip, layer, blend mode, filter
                 and image mode is listed with its expected behaviour and its conformance
                 requirement. Releases `b`.
    s-3d       `docs/graphics/RENDER3D_PROFILE_1.md`, `SHADER_IR_1.md`, `SCENE3D_PROFILE_1.md` (the
                 CORE scene profile) and their registry entries. Releases `e`.
    s-wsi      `docs/graphics/WSI_PROFILE_1.md`, and it is frozen only when the choices the WSI rows
                 leave open have ONE answer each: the scale-factor representation and the exact
                 logical-to-physical rounding rules, the surface event set, the present state
                 machine, damage semantics, the completion contract, the MEMORY AND CACHE ORDERING
                 for a surface shared between a client and the compositor, the ATOMIC
                 surface-configuration rule (which properties change together and what a partial
                 application means), and the pre-compositor MULTI-SURFACE semantics. Releases
                 `a-wsi`.
                 The five shared requirements are those in the per-part Done list, including
                 numerical minima and tolerances. No implementation starts against a draft.

    s-3d-ext   `docs/graphics/SCENE3D_EXTENDED_1.md` and the `Scene3D Extended Profile 1`
                 registry. Freezes the Extended feature equations, limits, tolerances and lifecycle
                 choices on the same five requirements as the other freezes. Releases only
                 `f-ext`; no core freeze or Done condition depends on it.

A freeze is declared only when the document and registry exist and all five requirements in the
per-part Done list pass. Document presence or matching feature names alone does not freeze unresolved
constants or tolerances. Corrections to a frozen profile follow the semantic-version/hash rules below;
the dependent part may start only against that declared profile.

A ninth pass found a defect that is about this document rather than about the design, and it is the
same shape as the one the third pass found: the file had grown a large number of sentences of the
form "ONE named tone-mapping operator", "the exact 2x and 4x sample positions", "a stated number of
anisotropic taps", "either strict semantics or quantise through one rule - choose one and say which",
"sampling while rendering is refused or defined". Every one of those correctly identifies something a
specification must decide. **None of them decides it.** A milestone that enumerates the open
questions reads like a specification and gates like a wish, and two implementations reading it still
produce different pixels - which is the exact failure the fourth pass was written to end.

The answer is NOT to inflate this file with matrices, kernels and sample coordinates. It is to admit
what this file is - a ROADMAP and a definition of done - and to put the actual values in normative
documents that are written and FROZEN BEFORE the backends they govern. `docs/GRAPHICS_2D.md` and
`docs/GRAPHICS_3D.md` currently appear as the last item of the last part, which is backwards: a
specification published after the thing it specifies is a description.

- [ ] Write `docs/graphics/IMAGE_COLOR_PROFILE_1.md` and freeze it before `P02M0103a` builds anything
  on it. It carries what the colour item only promises: the Bradford adaptation matrices, the
  reference white luminance, the named tone-mapping operator WITH its equation and parameters, the
  gamut-mapping equation, the exact PQ and HLG transfer functions and inverses, the ordered-dither
  matrix and its phase, and the half-float rounding, subnormal, NaN and infinity policy. It also
  specifies the valid `PixelStorage x ImageSemantics x operations` matrix, allocation/padding rules,
  and the three separate byte-span quantities defined in `a-common`.

  Its YUV chapter fixes NV12/I420/P010 plane count and order, per-plane extent and pitch, odd-extent
  rules, P010 bit placement and byte order, BT.601/709/2020 coefficients, full/limited nominal ranges,
  chroma siting and reconstruction filter, crop alignment and applicable PQ/HLG metadata. Plane
  reconstruction and the YUV matrix produce encoded RGB before transfer decoding, primary conversion
  and premultiplication; freeze that order and its numeric rules rather than applying the RGB-only
  sampling recipe directly to YUV bytes. Missing or short planes and invalid layouts are typed
  refusals. All per-plane sizes use checked arithmetic and exported padding is canonical.

- [ ] Write `docs/graphics/WSI_PROFILE_1.md`: the scale-factor representation and the exact
  logical-to-physical rounding rules, the surface event set, the present state machine, damage
  semantics, and the completion contract. Include the atomic `SurfaceConfiguration` lifecycle,
  coherent CPU-memory restriction and release/acquire ordering, and the pre-compositor visibility
  rule defined in `a-wsi`. Freeze them before that implementation begins.

- [ ] Write `docs/graphics/RENDER2D_PROFILE_1.md`: the equation for every Porter-Duff operator and
  every blend mode including the non-separable four with their luminance and saturation models and
  gamut clipping; the flattening tolerance as a NUMBER, the maximum subdivision depth as a number,
  and the projective `w` epsilon as a number with the horizon rule chosen; the boolean-operation
  answers rather than the boolean-operation questions; the per-node filter contract; the LCD gamma;
  and `Render2DLimits` with its guaranteed minima. Freeze the prepared-list compatibility and
  mutable-content rules, and the bounded reusable-builder contract defined in `b`.

- [ ] Write `docs/graphics/RENDER3D_PROFILE_1.md`: the format capability table (sampled, filterable,
  renderable, blendable, MSAA, vertex, readback, attachment - per format, with full names rather than
  `R8`/`RGBA8` shorthand); the vertex format table; the rasteriser state table; the exact 2x and 4x
  MSAA sample positions with the shading frequency, centroid rule, mask order, alpha-to-coverage
  mapping and integer/depth resolve rules; the per-format depth and stencil semantics with the
  `Depth32F` question ANSWERED; the sampler contract with tap counts, footprint, LOD formula,
  cube-face orientation and seam rule, mip-generation kernel, depth-compare return and border-colour
  representation; and the resource hazard policy stated as rules rather than as open alternatives.
  This freeze chooses the Depth32F storage/comparison rule, defines `ClipCoordQ` completely, and binds
  the submission/readback lifecycle in `e` before backend or scene-layer implementation starts.
  Freeze the qualifier-aware clipping equations in `g`: homogeneous edge interpolation for position
  and `smooth`, projected edge interpolation for `noperspective`, and the original assembled
  primitive's provoking value for `flat`, including when its provoking vertex is clipped away.
  Specify provoking-vertex selection for every topology and preserve that value through all clip
  planes and fan triangulation. Define intersection/attribute rounding consistently with `ClipCoordQ`
  and the existing non-projectable-vertex refusal; no backend chooses its own qualifier rule.

- [ ] Write `docs/graphics/SHADER_IR_1.md` with its own version and profile hash: integer overflow,
  division and modulo by zero, NaN, infinity, signed zero and subnormal behaviour, float-to-integer
  conversion, transcendental accuracy bounds, uninitialised values, out-of-bounds array access,
  uniform-buffer layout and alignment, matrix memory layout, helper-lane and post-`discard` derivative
  behaviour, implicit versus explicit LOD per stage, and the versioned serialised encoding with
  instruction IDs and canonical constant forms. Every data and control dependency contributing to
  position is validated against the StrictF32 rules in `e`, including transcendental operations,
  conversions, memory indices and any sampling permitted to contribute. An operation without a
  deterministic strict definition is refused on that dependency path; relaxed fragment arithmetic
  does not acquire bit-exact requirements merely because it shares the IR.
  Specify `smooth`, `noperspective`, `flat`, `centroid` and `sample` semantics together with the
  Render3D profile's clipping rules: interpolation qualifiers retain their declared meaning after
  clipping; centroid/sample select evaluation locations and do not replace those rules.

- [ ] Write `docs/graphics/SCENE3D_PROFILE_1.md` - the CORE scene profile, which is what `s-3d`
  freezes and `f` implements: the node and hierarchy model and its transform composition order, the
  camera and projection conventions, the render-queue definitions and their ordering rules, the
  bounding-volume and frustum-culling tests, the instancing model, the `Unlit`/`VertexColor`/
  `Lambert`/`BlinnPhong` material definitions with `BlinnPhong`'s exact equation, unshadowed
  multi-light accumulation, the picking and readback contract, and `Scene3DLimits` with its
  guaranteed minima. (Corrected 2026-08-30: this item consisted ENTIRELY of Extended content - PBR,
  environment prefiltering, shadows, bloom, fog and root motion - so the core profile had no
  specification at all and its freeze depended on optional work.)

- [ ] Write `docs/graphics/SCENE3D_EXTENDED_1.md` for `f-ext`, carrying what moved out of the core
  profile: the PBR equations and constants - GGX/Trowbridge-Reitz, Smith visibility, Schlick Fresnel,
  the roughness remapping and minimum, environment prefiltering, the BRDF LUT - plus shadow-map
  format, constant and slope bias, PCF kernel, the cascade decision, the bloom threshold and kernel,
  the fog equation, and the root-motion policy chosen rather than required. It is part of `f-ext` and
  is frozen by `s-3d-ext` before `f-ext` implementation and is NOT in `s-3d`'s freeze.

- [ ] Then move `docs/GRAPHICS_2D.md` and `docs/GRAPHICS_3D.md` to what they should be: the
  human-facing overview and the place performance tables, conformance results and the screenshot
  land at the END. The normative content lives in the profile documents above and is frozen first.

- [ ] State the GUARANTEED MINIMUM limits in each profile document, because implementation-defined
  limits without a floor let a backend pass conformance while being useless: a `max_texture_extent`
  of 16, a `max_draws` of 1 or a `max_shader_instructions` of 8 would satisfy every other sentence
  here. `RENDER2D_PROFILE_1_MIN_LIMITS`, `RENDER3D_PROFILE_1_MIN_LIMITS` and
  `SCENE3D_PROFILE_1_MIN_LIMITS` are part of the profile, and the conformance suites run against
  those minima rather than against whatever the backend happens to offer.

- [ ] Bind each profile to a canonical semantic hash, using the project's SHA-256 algorithm. The
  machine-readable feature registry, constants, equations and lookup data are the semantic input;
  Markdown is the normative explanation, bound through a manifest, and is not hashed as raw text.
  Each freeze defines the canonical encoding, sorted input inventory, numeric and string encoding,
  newline normalization where text is semantic, and version-change rules. Hash fields are excluded
  from their own input. A prose rewrap or non-normative example correction does not change the
  profile; a semantic correction changes the semantic input and version together. The gate checks
  matching versions/hashes across the document manifest, registry, backend and suite, and generated
  coverage tables cannot claim unknown features.

- [ ] Freeze numerical conformance thresholds in each profile's conformance chapter: applicable
  absolute, relative and ULP error for arithmetic, 2D antialiasing coverage bounds, final color error
  in a named comparison space, filter-kernel tolerance, and the exact image comparison mask and
  edge policy. Bit-exact classifications and 3D sample ownership have no tolerance. PBR, shadows,
  HDR postprocessing and tone-map tolerances specific to Extended scenes belong to `s-3d-ext`;
  shared color conversion and tone mapping remain in `s-common`. No tolerance may be loosened per
  architecture. Writing these numbers is specification work, and no affected freeze or backend may
  be declared complete until its thresholds are fixed and exercised.

## P02M0103a - Common graphics platform

The common image model and WSI are separate sub-parts. Their individual prerequisites and consumers
are the matrix above; neither requires a GPU rendering backend.

**AND IT IS TWO PARTS FOR ORDERING PURPOSES, `a-common` AND `a-wsi`** (2026-08-30). The header of this
file says the compositor-facing 2D foundation is delivered first and WSI follows it, and that was
impossible as the parts stood: WSI, surfaces, presentation, lifecycle and accounting all live in `a`,
and `b` depends on the whole of `a` - so the 2D renderer was blocked on WSI, on P02M0141, and on a
completion primitive nobody owns. A stated order that the part boundaries contradict is not an order.

    a-common   the image and colour model, `graphics-core`, the shared conversion, sampling and
               compositing, the typed error, the resource vocabulary. Nothing here needs a display,
               a driver, a service restart or a completion object.
    a-wsi      surfaces, the present queue, presentation timing, the typed display-device interface,
               the client-Domain accounting and the lifecycle. This is what needs P02M0141, the
               completion primitive and the provider reacquisition path.

`b` depends on `a-common` and its own `s-2d` freeze, and not on `a-wsi`. `a-wsi` runs beside
`b` and `c` and is required before any part that presents.

**AND THEY ARE ACTUAL SUBSECTIONS WITH THEIR OWN DONE GATES** (2026-08-30). The previous round called
the split "a dependency statement" and left every item in one interleaved list with one combined Done
condition - which gave no objective point at which `a-common` could be declared complete and release
`b` while `a-wsi` was still blocked on restart and reacquisition work. An ordering that cannot be
declared is not an ordering. The items below are divided under the two headings, each item belongs to
exactly one of them, and the Definition of Done at the end of this file carries a separate condition
for each.

This is the split the previous round rejected as "the parts already are the split". That answer was
right about `s`-through-`i` and wrong here: `a` was carrying two things with different prerequisites,
and one of them was blocking the other for no reason but the heading they shared.

### P02M0103a-common - the image, colour and conversion model

Depends on `s-common`. Needs no display, no driver, no service restart and no completion object.
This is the half `b` waits for.

- [ ] Write `docs/GRAPHICS.md` first, and name the layers in it, because "framebuffer" currently
  means five things: the UEFI boot surface in `bootproto`, the virtio-gpu resource's DMA backing,
  the DisplayService scanout, an application's surface, and `pix::Target`. FRAMEBUFFER is the legacy
  linear boot surface only; SCANOUT is what the display controller is showing; IMAGE is any 2D
  region of pixels; SURFACE is a presentable application object; RENDER TARGET is an image a
  renderer draws into. Freeze the ownership diagram above and the future integration routes -
  OpenGL/ES through EGL and a Mesa state tracker, Vulkan through a loader and ICD with Venus as the
  likely transport, DisplayService as the WSI and presentation owner, the display driver as device
  transport - and record which boundary validates every untrusted length, opcode and resource
  reference. Do not invent a common GL/Vulkan command language or expose virtqueue descriptors to
  applications.

- [ ] Define the canonical single-plane image model separately from future GPU descriptors:

      struct ImageLayout {
          extent: Extent2D, pitch: u32, storage: PixelStorage,
          origin: RowOrigin, semantics: ImageSemantics,
      }
      enum ImageSemantics {
          Color { color_space: ColorSpace, alpha_mode: AlphaMode },
          Data, NormalMap { convention: NormalMapConvention }, Depth,
          Mask { interpretation: MaskInterpretation },
      }

  Alpha compositing exists only in `Color`; non-color semantics cannot carry an ignored alpha or
  color-space field. `IMAGE_COLOR_PROFILE_1.md` defines the valid storage/semantics/operation
  matrix, including mask interpretation, and checked constructors enforce it.

  Every offset and size uses checked arithmetic. `minimum_visible_bytes()` is exactly
  `(height - 1) * pitch + minimum_row_bytes`, for a nonempty validated extent. A borrowed CPU view
  needs only that span, so a legal final row without padding is accepted. `backend_access_span()`
  separately states every byte the selected display or DMA backend may touch, including final-row
  pitch padding if it accesses that. `allocation_len()` is the actual owned allocation length.
  A presentable or DMA image must own at least its declared backend span; a driver may never touch
  beyond that span. Padding is not pixel content. Allocation size is not a second stored answer to
  the CPU view's computed minimum.

  A terminal raster and a decoded PNG need none of mip levels, array layers, sample counts, tiling,
  usage or memory domain - those belong to a `GpuImageDesc` that a later backend milestone adds ON
  TOP of this layout, sharing its format and extent. Building the ordinary image model out of the GPU
  one replaces four over-specific representations with one over-general one.

- [ ] Use TWO coordinate spaces and say which is which, because a platform that only has integer
  pixel coordinates cannot do HiDPI, fractional scaling, zoom, rotation or smooth animation - and
  those are not desktop luxuries, they are what a phone does on every orientation change. The PUBLIC
  drawing coordinate is `f32` and SIGNED: geometry has to be positionable between pixels and has to
  clip off the left and top edges as naturally as off the right and bottom, and an unsigned integer
  parameter makes "half a pixel to the left of the origin" unrepresentable. Integer coordinates stay
  INSIDE the rasterising backend, where quantisation is defined. Both kinds are HALF-OPEN,
  `[x, x + width) x [y, y + height)`, so adjacent rectangles tile without overlapping and an empty
  rectangle is width or height zero. Presentable surfaces have a fixed `TopLeft` row origin in v1,
  with `RowOrigin` on the layout for images that are not presentable.

  Give the two spaces two TYPES, because one `Rect` cannot serve both and the version that tried was
  wrong at one end or the other: a damage rectangle crossing an IDL boundary must be whole physical
  pixels, and a drawing rectangle must be fractional and signed. So `PointF`/`RectF` (`f32`) for
  geometry, transforms, paths and drawing bounds; `PixelOffset` (`i32`) and `PixelRect` (`u32`) for
  damage, post-rasterisation scissor, surface extents and everything on the wire. `liber:graphics@1`
  carries `pixel-rect` and never the float one - a wire format with an `f32` rectangle whose meaning
  depends on a transform stack the receiver cannot see is not a wire format.

- [ ] Carry the FORMAT SET both profiles need, not only the ones a scanout accepts. Eight-bit RGB is
  what a display takes; it is not what a mask, a glyph cache, a filter intermediate, a wide-gamut
  composite or an HDR target is made of, and adding those formats after the compositing code exists
  means rewriting the compositing code. The mandatory set:

      A8_UNORM            coverage masks, glyph masks, alpha-only layers
      R8_UNORM            single-channel data images
      R8G8_UNORM
      B8G8R8X8_UNORM      scanout
      R8G8B8X8_UNORM      scanout (GOP PIXEL_RGB)
      B8G8R8A8_UNORM
      R8G8B8A8_UNORM      encoding is never a second storage format - see below
      R10G10B10A2_UNORM   wide-gamut scanout
      R16G16B16A16_UNORM
      R16G16B16A16_FLOAT  the canonical intermediate
      R32_UINT           integer data and object-ID images
      R32G32B32A32_FLOAT  full-float CPU-visible images (RGBA32F in the 3D shorthand)

  `graphics-core::PixelFormat` owns all single-plane CPU-visible storage described by `ImageLayout`.
  `render3d::DepthStencilFormat` owns specialized depth/stencil attachment storage, and
  `render3d::TextureFormat` wraps the common formats and specialized set with an explicit per-format
  capability table. It must not redeclare an unrelated private list for the same storage.

  And name the canonical INTERMEDIATE explicitly, because the choice is invisible until the artefacts
  appear: layers, filter intermediates and offscreen composition are premultiplied LINEAR
  `R16G16B16A16_FLOAT`. Repeated compositing through 8-bit sRGB intermediates bands and loses
  precision, and a blur over an 8-bit intermediate is where it shows first.

  A second arm handles what firmware hands over. `src/uefi/src/gop.rs` accepts `PIXEL_RGB`,
  `PIXEL_BGR` and `PIXEL_BIT_MASK` with validated contiguous disjoint channel masks whose ELEMENT
  SIZE comes from the masks, so 16- and 24-bit modes are live; `bootproto::Framebuffer` carries that
  as `bpp` plus six shift/size fields and `term::Geometry`/`term::Raster` carry it again. Narrowing
  the model to fixed formats would reintroduce a defect this tree already fixed - the comment at
  `gop.rs:72` records that a literal `32` there gave a firmware-reported 24- or 16-bit layout "a
  diagonal smear rather than a picture". So:

      enum PixelStorage {
          Known(PixelFormat),                 // the list above
          PackedRgbUnorm(PackedRgbLayout),    // bytes_per_pixel + per-channel shift/bits + reserved
      }

  `PackedRgbUnorm` exists for BOOT AND SCANOUT ADAPTERS - the GOP hand-off and the terminal - and is
  not a texture format, not a presentable application surface format and not something a renderer
  samples.

- [ ] Say which alpha combinations are VALID and which are merely unimplemented, and fix the
  byte-level rules two implementations otherwise answer differently. `AlphaMode` is `Opaque`,
  `Straight`, `Premultiplied`; an `X8` format is `Opaque` only, so `Straight` and `Premultiplied` on
  one are a type error and not an unsupported case; an `A8` format carries alpha and nothing else.
  Under `Opaque` an alpha channel reads and writes 1.0 in its storage representation (the maximum
  channel integer for UNORM, 1.0 for float), rather than being carried through unexamined.
  Under `Premultiplied`, normalized bounded color storage may treat a channel above alpha as clamped
  to alpha. Floating linear HDR/wide-gamut intermediates retain valid negative or above-alpha values
  until explicit output conversion; the alpha rule must not erase the color model's allowed range.
  Freeze zero-alpha canonicalization and non-finite handling in the storage/semantics matrix.
  Channel order is defined by the format
  NAME and never by host endianness; float values convert to integer by round-half-away-from-zero
  after clamping; NaN converts to zero and infinities clamp rather than wrapping or panicking; and
  the `X` byte of an `X8` format and reserved bits of a packed layout are ignored on read and
  written with all their bits set (`0xff` for X8), so a producer cannot leak stale bytes into them
  and a consumer cannot start depending on uninitialized content.

- [ ] Colour management is a MODEL, not two transfer functions. `Linear`/`Srgb` over implied sRGB
  primaries is right for a first scanout and wrong for a phone, and retrofitting a colour space into
  a compositor that assumed one is a rewrite of every blend. `ColorSpace` carries primaries, white
  point and transfer function, and computes conversion matrices; the mandatory set is sRGB, Display
  P3, Rec. 2020 and their linear variants, plus the PQ and HLG transfer functions. ICC profile
  parsing is a separate later module; what this item forbids is a public model that makes one
  impossible.

  Naming the spaces is the easy half. "Supports tone mapping and dithering" is not a contract - two
  implementations can satisfy that sentence and produce visibly different images - so Profile 1 picks
  the algorithms: BRADFORD chromatic adaptation between white points, with the matrices written down;
  a stated reference white luminance and whether HDR values are absolute cd/m² or a named relative
  model; linear values permitted to go negative and above one INSIDE the pipeline, clamped only at
  output; ONE named tone-mapping operator with its parameters; the exact PQ and HLG transfer
  formulae and their inverse; and a stated gamut-mapping rule for out-of-gamut colours.

  Dithering picks ONE and it is ORDERED, with a fixed matrix and a fixed phase relative to the target
  origin. "Ordered or error-diffusion" was another unchosen alternative, and here the alternative is
  not merely vague - error diffusion carries state ACROSS pixels, which makes a tile-parallel
  renderer's output depend on tile decomposition. A backend built to render tiles independently and
  deterministically cannot use it without giving up both properties. Half-float behaviour is stated
  with the rest: NaN, infinity, subnormals and the rounding mode used when converting into
  `R16G16B16A16_FLOAT`.

- [ ] Say what an image MEANS, not only how it is stored, because a colour-managed pipeline that
  cannot tell a colour from a measurement will transform the measurement. A normal map, a roughness
  map, a coverage mask and a depth buffer are all just bytes, and running any of them through an sRGB
  decode corrupts them silently - the artefact looks like a lighting bug, which is where the days go.
  Every image carries the `ImageSemantics` defined above, and every conversion path refuses to apply
  a transfer function to anything but `Color`. Mask interpretation and normal-map convention are
  explicit and validated against storage.

- [ ] Provide `ImageView<'a>` and `ImageViewMut<'a>` - a borrowed slice plus a layout - whose
  constructors refuse a pitch below the minimum row bytes, a length below `minimum_visible_bytes()`, a
  zero extent or an unknown format. `surface::Mapping` hands out one of these (or runs a closure
  over one) and `Mapping::addr()` is DELETED rather than left beside it: today
  `imgview.rs` turns that `u64` into a target with
  `core::slice::from_raw_parts_mut(surface.addr() as *mut u8, target_len)`, an application computing
  a length and building an aliasing mutable slice from a raw pointer.

- [ ] Provide OWNED images and offscreen render targets, not only borrowed views. An application has
  to be able to create an image and draw into it without a Surface and without DisplayService, or
  every one of these needs a special case: component caches, thumbnails, chart and map tiles, an
  image editor, shadow maps, render-to-texture, a 3D frame a 2D pass composites, a 2D texture a 3D
  pass samples, screenshots and every host test of every drawing routine. `OwnedImage` carries a
  layout and its bytes, is charged to the creating Domain, and hands out `ImageView`/`ImageViewMut`.

  It is a 2D, SINGLE-SAMPLE, single-plane image with any valid `ImageSemantics`, so an A8 mask,
  integer picking image or normal-map cache has an owner as well as a color image. Color-only entry
  points validate `Color` semantics. It remains distinct from multisample storage, specialized
  depth/stencil attachments, cube/array/3D textures and mip chains, which belong to `render3d`'s
  resources. `ImageLayout` has no sample count. The interop path is explicit:

      3D MSAA / HDR attachments  --resolve + colour conversion-->  OwnedImage  -->  render2d

  Saying "a 3D attachment is an `OwnedImage`" would have made `soft3d`'s own requirement to validate
  a target's SAMPLE COUNT unsatisfiable against a type that cannot express one.

- [ ] Zero newly allocated `OwnedImage` storage and present-image backing before exposure. A buffer
  recycled between Domains is cleared. Pixel writes and export canonicalize X/reserved channels
  according to the format rules; row padding is never interpreted as pixels or returned uninitialized
  by readback or screenshots. External buffers still undergo layout/span validation. First-frame
  `Full` damage does not substitute for initialization: applications need not overwrite padding.

- [ ] Converge the pixel-plane types that already exist rather than adding a fifth. `pix::Target`
  carries `data/width/height/pitch/bytes_per_pixel` plus six channel shift/size fields; `pix::Image`
  carries `data/width/height/pitch`; `term::Raster` and `term::Geometry` are a third; and
  `surface::Mapping` rebuilds a `Framebuffer` with the shifts hard-coded to 16/8/0. Each becomes the
  new layout and view, or is deleted. `bootproto::Framebuffer` stays a boot-protocol type with a
  one-way adapter into the image model - a boot hand-off and a general image are different layers.

  The dependency graph, decided rather than offered as an alternative. `graphics-core` owns
  `Extent2D`, `Point2D`/`Vector2D`, `Rect`, `RoundedRect`, `Transform2D`, `Color`, `ColorSpace`,
  `PixelFormat`, `AlphaMode`, `ImageLayout`, `ImageView`/`ImageViewMut`, `OwnedImage`, `Sampler`,
  `DamageRegion`, checked pixel conversion, compositing and sampling. `pix` DEPENDS on it and stays
  an image utility library - owned `RgbaImage`, animation `Frame`/`Animation`/`Compositor`, crop,
  scale, blit - while its own `Image`, `Target` and `Rect` become aliases or are deleted. `render2d`
  and `render3d` depend on `graphics-core`; `soft2d` depends on `render2d`, `soft3d` on `render3d`;
  `scene3d` depends on `render3d` and never on `soft3d`. Nothing depends on `pix` in the other
  direction, and nothing but a backend depends on `soft2d` or `soft3d`.

  That graph settles the RUST side and leaves the WIRE side to grow a duplicate. These values cross
  the application display interface, the display-device interface and their generated crates, and
  `liber:display@1` already declares its own `enum pixel-format { b8g8r8x8 }`. So there is a
  value-only IDL package, `liber:graphics@1`, owning the wire-safe types - `extent-2d`, `offset-2d`,
  `rect`, `pixel-format`, `color-space`, `alpha-mode`, `row-origin`, `damage-region`, `image-layout`
  - which every graphics interface imports instead of redeclaring. `graphics-proto` is the generated
  wire identity. `graphics-core` does NOT re-export those types and add methods to them - Rust does
  not permit a crate to write an inherent `impl` for a type it does not own, and re-exporting does
  not transfer ownership, so as written this item does not compile (corrected 2026-08-30). Treating
  the unvalidated wire DTO as the public core type would also delete the boundary that proves a
  hostile descriptor was checked, and would tie the in-process layout to the wire schema this file
  wants versioned independently.
  So there are TWO representations and one conversion. `graphics-proto` owns the generated wire DTOs.
  `graphics-core` owns VALIDATED NEWTYPES with checked constructors, `minimum_visible_bytes()`, views over
  real slices, and the conversion, sampling and compositing routines; every trust boundary crosses
  from the DTO to the newtype through a mandatory `TryFrom` whose error is the typed refusal. Every
  renderer accepts only the validated core form and never the DTO. The CANONICAL representation for
  hashing and for the semantic registries is the wire DTO, because that is the one whose byte layout
  a version is declared against. `liber:display@1`'s own `pixel-format` is deleted in the same change that
  introduces the package, not left beside it.

- [ ] Add the MULTI-PLANE image model, which is the largest functional hole left in the 2D half: every
  mandatory format above is RGB or RGBA, so a video player, a camera preview and a hardware decoder
  must convert every frame to RGBA before `render2d` will draw it - a full-frame conversion per frame
  on the CPU, for the one workload where that cost is least affordable. It is a SEPARATE type rather
  than a widening of `ImageLayout`, so a single-plane image never grows fields it cannot use:
  `MultiPlaneImage` with its planes, a planar format, a YUV colour model, range and chroma siting.
  Profile 1 carries `NV12`, `I420` and `P010`, BT.601/BT.709/BT.2020 conversion, full and limited
  range and explicit chroma siting, and `render2d` draws one through the SAME shared sampling and
  conversion pipeline as everything else rather than through a private video path.

- [ ] Migrate `pix` rather than leaving it beside the model. `pix::RgbaImage` as it stands is
  `width`/`height`/`pitch`/`pixels` with no format, alpha mode, colour space or meaning, so keeping it
  unchanged leaves a back door through which an image with no colour metadata enters the new system -
  and the item above converges the pixel-plane types precisely to close doors like that. Either
  `RgbaImage` becomes `OwnedImage`, or it wraps one and is always RGBA8 with an explicit alpha mode
  and meaning. The animation `Frame` and `Compositor` go the same way.

### P02M0103a-wsi - surfaces, presentation and the display-device interface

Depends on `s-wsi` (the frozen WSI profile - added 2026-09-01, see the freeze section: this part
implements a normative document and may not start against a draft of it), on `a-common`, on P02M0141
(service restart), P02M0165 (stop and drain), P02M0166 (removal and graph state), and on the
completion object above. Runs beside `b` and `c`; required before any
part that presents.

P02M0164's catalogue mechanism is available. DisplayService already subscribes and reacquires a
replacement after driver-channel loss; this part completes the identity-aware withdrawal, alternative
provider selection, subscription recovery and typed device protocol described in the prerequisite
assessment. P02M0099 assigns the remaining migration jointly to this part and the first second-display-
provider item, whichever reaches it first. Consume what has landed and implement only what remains;
the WSI reacquisition/failover gate proves the complete contract either way.

- [ ] Carry OUTPUT COLOUR metadata on the surface, because a colour-space NAME is not enough to tone
  map with - a conversion that does not know the destination's luminance is guessing at the one
  number that decides how the image looks. The surface reports its colour space plus SDR white
  luminance, minimum and maximum luminance and maximum frame-average luminance, and the HDR-to-SDR
  and HDR-to-HDR paths in the colour item above consume those rather than assuming a display.

- [ ] Break the ONE-SURFACE-PER-CONNECTION assumption. `liber:display@1` says it in its own words -
  "One display connection owns at most one surface" - with `acquire`/`present`/`release`/
  `input-focus` all at connection level and a synchronous `present`. Every desktop and mobile
  application is more than one surface: a main window, a dialog, a popup, a menu, a tooltip, a video
  surface, offscreen content. Replacing the synchronous present with a queue and leaving the object
  model at one surface would rebuild the same wall one layer up. The model is:

      DisplayConnection
          create_surface(...) -> Surface capability
      Surface
          its own present queue, its own event stream,
          its own extent, scale, transform and colour metadata

  A compositor is still a later milestone; what this item forbids is an object model a compositor
  would have to break. Foreground input focus is a property of a surface rather than of a connection,
  so P02M0091's focus capability binds to the surface that actually has it.

  Before a compositor exists, at most ONE surface is visible and scanout-bound. Other surfaces keep
  independent resources, generations and queues while hidden/occluded; acquiring from them returns
  `NotVisible`. Frames accepted before a visibility change still settle in FIFO order with the
  appropriate discarded outcome. Focus belongs to the visible surface. There is no popup position,
  z-order or simultaneous composition contract in v1; those arrive with the compositor. Tests switch
  the visible surface and prove independent lifetimes instead of claiming to compose a popup.

- [ ] Separate LOGICAL size, PHYSICAL extent and SCALE FACTOR, and put all three in the surface
  contract. `liber:display@1` today has one extent and calls it "preferred logical size", which is
  the physical one wearing the other's name - there is no scale anywhere in the interface. A phone
  surface is 400 x 800 logical at scale 3.0 and 1200 x 2400 physical, and an application that cannot
  see those as three numbers either renders at the wrong size or renders blurred. Applications lay
  out in logical units, `render2d` transforms by the scale, and the backend rasterises physical
  pixels. Configuration arrives as one `SurfaceConfiguration` snapshot carrying serial, generation,
  logical and physical extent, scale, transform, output, presentable pixel format, color information,
  subpixel layout, visibility and focus. A client never applies a new scale with an old extent.
  The lifecycle is
  `configure(snapshot) -> rebuild -> ack_configure(serial) -> first present Full`; stale or unknown
  serials are refused. Each present identifies its configuration serial and image generation, and
  both must match the acknowledged current configuration at acceptance. Changes to extent,
  scale, orientation or presentable format invalidate the old image generation. Other configuration
  changes carry a new serial and are applied atomically without an unnecessary image allocation.
  Close requests and presentation completions remain separate events.

  The scale factor is a RATIO on the wire, `{ numerator: u32, denominator: u32 }`, not a bare `f32`:
  a fractional scale that two sides round differently produces an off-by-one extent, and that is the
  bug that shows as a one-pixel seam nobody can reproduce. With it, state which of logical and
  physical is authoritative and freeze one logical-to-physical edge rule used by both neighbors of
  a shared edge. Damage bounds round outward (left/top down, right/bottom up); that conservative
  bound is not a promise of non-overlap between independently rounded geometry. Freeze inverse input
  mapping and orientation together with this rule. A scale change bumps the image generation just
  like resize, through the atomic configuration lifecycle above.

- [ ] Replace the current display contract with the present queue, with NO compatibility path for
  the single-surface API. Nothing is versioned before the first release and no external software
  depends on it. A presentable surface has a pixel format immutable within its generation, an explicit extent, a resize
  generation and a NEGOTIATED image count - the service advertises minimum and maximum, the client
  requests, the service answers with the actual, and no number is written into the interface.
  `present_mode` is an ENUM with `Fifo` defined now, so `Mailbox` and `Immediate` have a place to be
  added rather than a meaning to be argued about. Advertise `min_images = 2` and `max_images = 3` in
  v1 - negotiated stays negotiated, but a service free to answer "one" turns the
  double-or-triple-buffering gate into a test of nothing. Specify the image state machine as states:
  `Available -> acquire -> Acquired -> present -> PendingPresent -> released -> Available`, with
  `Stale` after a generation change and a rule that a stale image is never presented into a new
  generation. Define FIFO ordering, maximum frames in flight, backpressure, `OUT_OF_DATE` handling,
  what process death returns, and console restoration.

  Three things that state machine is missing. An `Acquired -> abandon -> Available` edge, because a
  client that acquires an image and then decides not to draw must have a way back that is not a
  present. The resize transitions in full: `Available` in an old generation becomes `Stale`;
  `Acquired` in an old generation becomes `Stale` and the client discards it rather than presenting
  it; `PendingPresent` in an old generation completes or is discarded and then becomes `Stale`. And
  the Rust shape that makes the machine unrepresentable-when-wrong rather than merely documented:
  `acquire_next` returns an owned `AcquiredImage`, drawing borrows it, and `present` CONSUMES it, so
  after presenting there is no safe path to the pixels until the queue hands them back.

  `Fifo` as written promises something the occlusion rule takes away - "every present is displayed"
  cannot hold for a background client that must also not be blocked forever. Split the promise: FIFO
  is the ordering of ACCEPTED presents, by present-call order and not acquire order; while visible,
  every accepted frame reaches logical presentation; while occluded or in the background, new
  acquires answer `NotVisible`, and frames already accepted still complete IN ORDER but may
  complete as discarded. That needs a per-present outcome rather than a bare completion -
  `Displayed`, `DiscardedOccluded`, `ReplacedByResize`, `DriverLost`.

  **`acquire_next` DOES NOT BLOCK IN v1, AND THIS SAID IT COULD** (corrected 2026-08-30). LSIDL
  handlers are synchronous and DisplayService dispatches the GPU, the admin channel, the kill control
  and EVERY client from ONE `wait_any` loop. Blocking inside an acquire handler stops the only loop
  that could deliver the release, the resize, the peer death or the driver message that would unblock
  it - so a conforming implementation deadlocks the whole service under ordinary queue pressure. The
  v1 model is therefore NONBLOCKING: `acquire_next` answers `Again` when no image is available and
  `NotVisible` when the surface is occluded or backgrounded, and the client waits on an
  IMAGE-AVAILABLE event rather than inside the call. Deferred replies with a bounded pending-call
  table and an independent progress pump are the alternative, and they are a named extension with
  their own async-dispatch design - not something an implementer may assume from the word "block".

  **AND "DISPLAYED" IS THREE DIFFERENT FACTS.** The current virtio-gpu path acknowledges
  `TRANSFER_TO_HOST_2D` and `RESOURCE_FLUSH` and nothing else: there is no vblank, no page-flip
  timing and no evidence that a scanout happened, so a flush reply cannot supply a presentation
  timestamp and reporting one would be reporting a command acknowledgement as physical presentation.
  The three outcomes are named separately - ACCEPTED (the service took the frame), DRIVER-COMPLETED
  (the backend acknowledged the command), and PHYSICALLY DISPLAYED (scanout is observed) - and a
  backend that cannot observe the third says so. On the current backend the presentation timestamp is
  reported as UNAVAILABLE or explicitly ESTIMATED, and `Displayed` means driver-completed. A real
  timestamp requires a vblank/timing capability added to the typed display-device contract with its
  own tests, and that is a later item, not an assumption of this one.

- [ ] Give the frame loop a TIMING contract rather than leaving every application to sleep on the
  monotonic clock: a preferred frame deadline, the output's refresh interval, the presentation
  timestamp of a completed present, and the `PresentOutcome` above. EVERY ONE OF THOSE IS OPTIONAL
  AND TYPED AS SUCH, because the current backend can supply none of them: the contract carries
  `Unavailable`, `Estimated(t)` and `Measured(t)` rather than a bare timestamp, and a client that
  requires `Measured` refuses on a backend that cannot produce it instead of pacing against a
  fabricated number. A compositor will later supply
  frame callbacks synchronised to scanout, and the contract has to have a place for them now -
  retrofitting a callback into a design whose only pacing primitive is `sleep` means every
  application that animates is rewritten.

- [ ] Damage is a BOUNDED LIST of rectangles with an explicit whole-surface fallback, not one
  rectangle. Both sides of the driver protocol carry a single `(u32, u32, u32, u32)` today and the
  driver unions everything it receives (`union_rect` in `virtio_gpu.rs`), so a client updating two
  corners of a screen sends their bounding box - which is most of the screen.

  "A small cap" is not an ABI. Fix `MAX_DAMAGE_RECTS = 16` and state the semantics: an EMPTY list
  means nothing changed and the present is still ordered and still completes; an explicit `Full`
  variant means the whole extent, and is not spelled as one rectangle covering it; a rectangle
  outside the extent is a TYPED INVALID rather than a silent clamp; exceeding the cap is the caller's
  problem to solve by merging or sending `Full`, not the service's to solve by dropping rectangles;
  overlapping rectangles are legal and may be normalised; and coordinates are always in the image
  space of the generation the present belongs to. The backend MAY merge rectangles when merging is
  cheaper than transferring them separately, but the unconditional union of everything is exactly
  what this item removes.

  And answer the question all of that skips: damage relative to WHAT, when consecutive frames are
  drawn into different images? With images A, B, A, a client that draws two rectangles into B has
  said nothing about the rest of B. The v1 rule is the simple one: **every presented image contains a
  COMPLETE valid frame.** Damage is a HINT describing which pixels changed against the previously
  presented frame; it is not permission to leave the rest of the image undefined. From that: the
  first present of a generation is `Full`; an empty damage list asserts the submitted frame is
  pixel-identical to the previous one, which lets DisplayService release the image without copying;
  and a client wanting to redraw only part of an image needs to know what that image already holds,
  which is a `buffer_age` extension a later milestone can add without changing this rule.

  Give the bounded merging one implementation - `DamageRegion::{None, Rects { rects: [Rect; 16],
  len: u8 }, Full}` in `graphics-core` with a `push_or_full()` helper that degrades to `Full` on the
  seventeenth rectangle. Otherwise every application writes its own bounded merge and the cap becomes
  sixteen different behaviours at the cap.

- [ ] Implement presentation completion with ordinary CHANNEL PAIRS, two pairs per SURFACE,
  created once when the surface is admitted. No new kernel object, generic timeline, ABI number or
  signal right is introduced. `Event` is not the completion mechanism: it lacks the authority split
  and peer-close lifecycle required here.

      PRODUCER_READY   client: SEND only; service: RECEIVE | WAIT
      PRESENT_DONE     service: SEND only; client: RECEIVE | WAIT

  These are four endpoint objects, four handle slots and four inboxes per surface; attenuation makes
  exactly two inboxes capable of receiving messages. Each endpoint is created with depth
  `max_images`, so at three images at most six messages can be queued across both directions.
  Client endpoints carry neither DUPLICATE nor TRANSFER. Moving the safe completion token inside
  its process is allowed; the raw endpoint is never exposed alongside it.

  Completion authority and lifecycle are enforced at two boundaries:

  - An ownership-consuming per-present token implements signal-once in safe producer code. The
    token carries `PresentKey { surface_generation, image_index, present_serial }`; the serial is
    unique for that surface lifetime, is not reused when an image is reacquired, and exhaustion
    refuses another present rather than wrapping. The surface owns the reusable endpoint, so
    signaling consumes the token without closing the channel.
  - DisplayService reserves and acknowledges that key when accepting `present`, before the client
    publishes `PRODUCER_READY`. The receiver processes at most one outcome for the exact outstanding
    key. A stale generation, stale serial, unknown image or duplicate is a protocol violation that
    fails the surface and releases its pending records. Naming only the image index is insufficient:
    a delayed duplicate must not complete a later use of that same image.
  - The client sends producer-ready only after rendering has completed. DisplayService may use the
    image only after receiving that message; the client may reuse it only after the matching
    `PRESENT_DONE` carries its typed outcome and optional timing. Accepted presents settle in FIFO
    call order even if readiness arrives in another order.
  - Dropping a token without signaling reports a failed outcome for that key. Ordinary completion
    sends are bounded and nonblocking in the service loop. If a protocol failure prevents delivery,
    closing the endpoint fails the whole surface; neither a destructor nor an LSIDL handler may
    block the only progress loop.
  - Peer-close before destruction ends the surface, fails each outstanding present exactly once,
    releases its image references and wakes waiters. A successful frame never closes the endpoint.
    Closing the surface after the last outcome retires the pairs. Timeout/cancellation follows the
    same bounded cleanup; dropping the waiter endpoint cancels the whole surface, while dropping
    one token cancels that present. `WSI_PROFILE_1.md` fixes the deadline policy before implementation.

  The waiter needs RECEIVE | WAIT: the existing wait syscall refuses receive-only handles. The
  safe wrapper cannot constrain a hostile raw sender, so receiver validation is mandatory. A sender
  that fills an undrained depth-`max_images` queue gets `Full` on the next send and transfers no
  capabilities with that refused message. A duplicate admitted before the queue is full is handled
  as the protocol violation above, not mistaken for kernel-enforced signal-once.

  The wake budget belongs to the SERVICE, not only to individual clients. Its set includes every
  actual fixed member (serve/admin roots, focus/kill controls, scanout, provider subscription and any
  added waitable control), one per live connection, one waitable process identity per live client
  task, and TWO per live surface: the surface channel and its `PRODUCER_READY` receiver. Enforce
  `fixed + connections + client_tasks + 2 * surfaces <= abi::MAX_WAIT_SET_MEMBERS` at admission;
  publish the available bound and return a typed refusal before accepting work that exceeds it.
  A WaitSet cannot contain another WaitSet. Clients similarly budget their fixed handles plus two
  members per surface (surface channel and `PRESENT_DONE`) against their own wait mechanism's limit.
  No wait list may be truncated and no runnable poller substitutes for a missing wake member.

  Freeze the memory contract in `WSI_PROFILE_1.md`: a successful channel send has release semantics,
  and the receive observing it has acquire semantics, in BOTH directions. Receiving `PRESENT_DONE`
  precedes the image becoming Available and the next acquire. v1 present images use coherent ordinary
  CPU-visible MemoryObjects only; non-coherent/DMA imports require the separate portable-sync
  contract and are not silently accepted. Verify shared-memory visibility and the syscall/compiler
  ordering on all three architectures; do not assume a correct state machine alone proves cache
  coherence. No new flush/invalidate API is part of this CPU-only v1.

  Tests cover signal/drop order, cancellation, token move then signal, compile-time duplicate-signal
  refusal, receive-without-wait denial, each peer-close direction, timeout and cleanup. Hostile
  clients fill a paused receiver's queue, send duplicate and stale keys (including reuse of the same
  image), and attach capabilities to invalid frames; assert exactly one outcome or terminal surface
  failure and return to the resource baseline. Admission tests open multiple clients and surfaces
  past the service ceiling, count the actual wait members and prove every admitted surface still
  receives progress. These tests distinguish source-level token ownership from receiver enforcement.

- [ ] Replace the DisplayService <-> display-driver byte protocol with a typed LSIDL interface, and
  specify its lifecycle rather than only its calls. `display_service.rs` writes `b"PRESENT"` and four
  little-endian `u32`s into a 23-byte buffer; `virtio_gpu.rs` decodes it with `rd32_le(m, 7)` and a
  length check, with `FB`, `FBNEW`, `RESIZE`, `FLUSH`, `OK` and `ERR` beside it. Every other service
  boundary in this tree is LSIDL with typed capabilities. `liber:display-device@1` states: who owns
  the backing and for how long the capability is valid; how a backing binds to a generation; whether
  a resize changes the visible extent or delivers a new backing, and what happens to the old one;
  what present completion means; what driver death does to in-flight presents; how DisplayService
  finds a new backing after a driver restart; that the event stream is bounded; and that a backing
  from an old generation can never be presented into a new one.

- [ ] **Account through the KERNEL's Domain counters and OBSERVE through SystemGraph. ResourceManager
  is not extended** (decided 2026-08-30; the item is written around the decision rather than
  requiring and then rejecting the same integration). The kernel already charges MemoryObject and IPC
  bytes to the Domain that CREATED them, so the counter that enforces already exists - and moving
  creation out of DisplayService is what makes it charge the right side. It charges the SUPPLIER,
  which is ordinarily the client and is not guaranteed to be (see the sponsorship row below); this
  sentence used to say "with client-created images that Domain is the client's" and call the counter
  "already correct", which asserted in the opening what the same item withdrew two rows later
  (corrected 2026-08-31). This milestone adds no second byte budget, no graphics classification and
  no graphics-specific quota: a quota with no consumer is policy this file's own rule calls a guess.

  What this item owns is therefore narrower and fully specified:
  - SystemGraph reports bounded COUNTS and reset/fault state - surfaces, present images, queued
    presents, damage metadata entries and waiters - as observation, never as enforcement, and without
    exposing image contents;
  - every DisplayService-side structure that is not a MemoryObject (the present queue, damage lists,
    waiter records) has a stated per-connection BOUND, because those are not charged by the kernel
    and are what an adversarial client can multiply;
  - exhaustion of any of those bounds returns a typed error and releases the partial transaction; and
  - reclamation on client death is specified in the item below rather than asserted here.

  **THE ACCOUNTING CONTRACT IS SPONSORSHIP, AND THE INTRODUCTION SAYS SO** (corrected 2026-08-30).
  `SYS_MEMORY_OBJECT_CREATE` charges the SYSCALL CALLER's Domain, and DisplayService creates and
  retains every surface MemoryObject today - so those bytes are charged to DisplayService, not to
  the requesting application, and one client can exhaust the service's quota while its own budget
  still looks healthy. That is the defect this item fixes.

  WHICH DOMAIN PAYS IS NOT CLAIMED, AND THE SENTENCE ABOVE NO LONGER CLAIMS IT (corrected
  2026-08-31 - an earlier version said client-created images "necessarily" make the client's Domain
  the charged one and the enforcing counter therefore correct, and then withdrew exactly that two
  rows later, so one item asserted and retracted the same guarantee). The SUPPLIER of an image pays
  for it, and the supplier need not be the requester - `ObjectInfo` carries no charged Domain, so
  the service cannot tell the two apart. The property this item delivers is
  therefore stated exactly: DISPLAYSERVICE IS NOT CHARGED FOR ITS CLIENTS' IMAGES, and no Domain pays
  for storage it did not itself allocate. "Necessarily charged to the client's Domain, so the
  enforcing counter is correct" was the previous wording and it is withdrawn here as well as in the
  protocol row below, where it was already withdrawn - one item may not assert and withdraw the same
  claim.

  Pick one and write it down:
  (a) THE CLIENT CREATES AND IMPORTS - the application creates the MemoryObject in its own Domain and
  hands it in, with strict validation of size, rights and layout and a stated rule about which
  handles the service may retain; or
  (b) ALLOCATE-IN / CHARGE-TRANSFER - a capability-authorised operation that allocates against the
  CLIENT's Domain on the service's behalf, which requires a kernel mechanism that does not exist and
  is therefore itself a prerequisite.
  **DECIDED 2026-08-30: (a), CLIENT-CREATED AND IMPORTED, and here is the protocol** - the previous
  round chose it in one clause and left the API describing a service-managed pool, so implementing
  what was written would have preserved the accounting defect it was meant to fix.

    negotiation      unchanged: the service advertises `min_images`/`max_images`, the client
                       requests, the service answers with the ACTUAL count. What changes is who
                       allocates the images that count names.
    supply           after negotiation the SUPPLIER creates exactly that many MemoryObjects, each
                       of the extent, format and stride the service answered with, and imports them
                       in one `provide_images` call. The supplier is ordinarily the client itself;
                       "in ITS OWN Domain" was the previous wording and it stated as a requirement
                       something the service cannot check (corrected 2026-08-30).
    who pays         SPONSORSHIP IS THE CONTRACT, AND REQUESTER ATTRIBUTION IS NOT CLAIMED
                       (corrected 2026-08-30). `SYS_MEMORY_OBJECT_CREATE` charges the Domain that
                       CREATED the object and the charge stays with it when the capability is
                       transferred, so what this design guarantees is the property that matters:
                       DisplayService no longer pays for its clients' images, and NO DOMAIN PAYS FOR
                       MEMORY IT DID NOT ITSELF ALLOCATE. What it does NOT guarantee is that the
                       paying Domain is the requesting one: `ObjectInfo` carries koid, type, rights,
                       generation and size and NO charged Domain, so the service cannot tell a
                       client-created buffer from one another Domain created and handed over. A
                       third Domain sponsoring a client is therefore possible, and it is a
                       capability-system consequence rather than a hole: it requires that Domain to
                       allocate and transfer deliberately, which is what a capability is. The
                       previous version said attribution was enforceable "by construction" and
                       required the service to reject cross-Domain handles; the first was untrue and
                       the second was unimplementable, and both are withdrawn rather than left as a
                       gate nobody can build. Adding a kernel provenance field to `ObjectInfo` would
                       make requester attribution checkable and is a separate proposal against a
                       shared ABI this milestone does not own.
    validation       the service checks count, the full `backend_access_span()`, and rights, and
                       refuses the whole set rather than a subset - a partially supplied queue is a
                       queue that cannot be presented from.
    rights           the service needs READ and MAP and does not need WRITE; the client keeps its
                       own handles. The service may not TRANSFER an imported image onward, which is
                       what makes delegation a decision rather than an accident (below).
    acquire_next     unchanged in shape: it answers with an image the client already owns, by index
                       and generation, not with a handle it is being given.
    resize and
      generation     a generation change INVALIDATES the whole set. The service releases every
                       imported handle and answers `OUT_OF_DATE`; the client supplies a new set for
                       the new extent. No image ever crosses a generation, which is the rule the
                       state machine already states for `Stale`.
    reclamation      the service releases its imported handles when the surface is destroyed, when
                       the client's process dies, or on a generation change - whichever comes first.
                       The client's Domain gets the memory back when its own handles go, which is
                       what makes process death sufficient.
    delegation       DECIDED ONCE AND CLOSED (2026-08-30), AND ENFORCED STRUCTURALLY RATHER THAN
                       BY DETECTION (corrected 2026-08-30): the surface channel's client endpoint is
                       handed over WITHOUT `RIGHT_TRANSFER` and WITHOUT `RIGHT_DUPLICATE`, so moving
                       it to another process FAILS AT THE SYSCALL. The previous version said the
                       service "refuses a surface whose channel has moved process", which it cannot
                       do: a message identifies only the endpoint it arrived on, neither the message
                       nor `ObjectInfo` reports the holder, and watching the creator detects DEATH
                       and not a live transfer. An earlier version permitted the channel to move
                       while forbidding re-import, and then a later paragraph said the item must
                       still decide whether delegation is legal - three answers to one question.
                       One process identity owns
                       a surface's images, its resize authority and its cleanup, and a channel that
                       could move would leave "client death" with no unique process to name. A client
                       that wants another process to draw gives it the images through its own
                       protocol; the surface stays where it was created.

  RESOURCEMANAGER IS NOT EXTENDED, and the accounting item above is now written that way rather than
  requiring the integration this paragraph rejects - which is how one item came to instruct two
  incompatible implementations.
  And "killing a Domain reclaims surfaces, images, mappings and waiters" is not structural today:
  DisplayService's bound task is duplicated with `MANAGE|TRANSFER` rather than `WAIT`, and its wait
  set watches channels and not client processes, so a service-retained MemoryObject can outlive the
  process the plan says owns it. This item owns giving DisplayService a WAITABLE task identity and a
  bounded per-task wait set, and cleanup proved INDEPENDENTLY of channel peer lifetime and of driver
  progress. Delegation is not an open question here - the protocol above closes it, and closes it in
  the rights the endpoint is minted with rather than in a check the service cannot perform. The
  waitable identity does the OTHER half: the endpoint's rights keep the surface where it was created,
  and watching that process is how its death is noticed.

- [ ] Add hostile-input and lifecycle tests before anything is built on this: malformed format,
  extent and pitch descriptors, overflowed image sizes, invalid generations, duplicate acquire and
  present, completion races, driver death, truncated messages, forged imports, a surface channel
  offered from a second process, per-connection bound exhaustion and cleanup during in-flight
  presentation. Fuzz every parser and every checked
  layout helper; prove that a malicious client cannot map another surface, retain foreground input
  or leave the console scanout blank.

## P02M0103b - Render2D Core Profile 1

The public, backend-neutral 2D API. This part defines the PROFILE; `P02M0103c` implements all of it.
Depends on the `s-2d` FREEZE and on `P02M0103a-common`, and on no backend - NOT on `a-wsi`, which is
the correction that makes the stated delivery order possible: recording and validating a display list
needs the image and colour model and needs no display, no present queue and no completion pairs.
(The `s-2d` half was missing from this header while the freeze section called it a hard prerequisite,
so a reader starting here could begin `b` against an unfrozen profile. Added 2026-09-01.)

- [ ] Define `Render2D Core Profile 1` as a closed, enumerated list, and make "complete" mean this
  list. A conforming backend implements every entry; `Unsupported` is reserved for extensions added
  after Profile 1 and may never be returned for anything in it. The profile:

      geometry     paths (move/line/quad/cubic/close), non-zero and even-odd fill,
                   rect, rounded rect, circle, ellipse, arc, line, polyline, polygon
      strokes      width, butt/round/square caps, miter/bevel/round joins, miter limit,
                   dash pattern and offset
      antialiasing analytic coverage for fills, strokes and glyphs
      transforms   affine AND projective (3x3 with perspective terms)
      paints       solid, linear/radial/conic gradient, image pattern;
                   multi-stop, clamp/repeat/mirror spread, own transform, linear-light stops
      clipping     rect, rounded rect, path, nested, alpha mask, inverse
      layers       group opacity, blend mode, bounded nesting
      compositing  the 12 Porter-Duff operators plus additive Plus, and every separable
                   and non-separable blend mode below
      images       source/dest rect, arbitrary projective transform, nearest/bilinear/
                   bicubic, mipmapped minification, clamp/repeat/mirror, opacity,
                   colour-space conversion
      filters      a bounded FilterGraph, not three special cases
      text         glyph runs: outlines, grayscale masks, bitmap strikes, colour-layer
                   glyphs, embedded colour bitmaps, glyph transforms, subpixel positioning
      queries      path boolean ops, hit testing, tight and stroke bounds, length,
                   point and tangent at distance

- [ ] Make BOTH profiles machine-readable, because a profile that exists only as a list in a Markdown
  file drifts away from the backend and the conformance suite the first time someone adds a feature
  in one place. The canonical form is code - `enum Render2DFeature`, `enum Render3DFeature`, and a
  `RENDER2D_CORE_PROFILE_1` / `RENDER3D_CORE_PROFILE_1` constant listing exactly what each contains -
  and the documentation table, the capability report, the backend checklist and the conformance
  matrix are all GENERATED from it, with a profile hash so a change is visible. A gate then checks
  three things no reviewer reliably catches: every mandatory feature has at least one conformance
  test, every profile feature has a backend handler, and no test claims a feature outside the
  profile. This is what keeps "complete" true a year after it is first true, and it is cheap only if
  it exists before the two backends do.

  Each feature names its OWNER - `GraphicsCore`, `Render2D`, `Render3D`, `Scene3D` or `Backend` -
  because the "every feature has a backend handler" check is wrong for features no backend
  implements. Path boolean operations, hit testing, bounds, length and point-at-distance are
  geometry-core functions that `soft2d` never sees; demanding a backend handler for them would force
  a stub whose only purpose is to satisfy a gate. The handler check applies to `Backend`-owned
  features; the conformance-coverage check applies to all of them.

- [ ] Bound `render2d` with a `Render2DLimits` value, which the 3D side has and this side does not:
  maximum commands, resources, path verbs, path points, subpaths, clip depth, layer depth, filter
  nodes, filter radius, glyphs per run, image extent, layer pixels, prepared scratch bytes, cache
  bytes and display-list bytes. A recording API without limits is an unbounded allocation driven by
  application data, which is the shape this tree bounds everywhere else. The guaranteed minima are in
  the profile document so that "supports Profile 1" cannot mean "accepts ten path points".

- [x] DECIDED 2026-08-30, and the decision is the item below it. What `DrawList` transport actually
  has to do in v1, rather than promising the largest version of it. "Transportable to a compositor" is currently required, and it drags in a whole
  contract - capability transfer for every referenced resource, font, image and path lifetime across
  processes, schema compatibility, a validating parser as a security surface, cache invalidation -
  for a consumer that does not exist, while a real compositor mostly wants finished surfaces rather
  than every application's vector command stream. Profile 1 requires an IMMUTABLE, VERSIONED,
  CACHEABLE list replayable in-process, which is what the caching, damage analysis and backend
  independence above actually need. Cross-process transport becomes a named `render2d-wire` extension
  designed against a real consumer - and if it is kept mandatory instead, then canonical byte order
  and alignment, resource ID plus generation, capability ownership and rights, immutable snapshot
  semantics, unknown-command and version handling, resource hashing and prepared-list invalidation
  are all part of Profile 1 and none of them is written yet.

- [ ] State what a stroke width MEANS under a transform, which decides what a scaled or projectively
  transformed stroke looks like and is currently unsaid: whether width, dash lengths and the miter
  limit are measured in user space before the transform or in device space after it, with an explicit
  `StrokeScaling::{WithTransform, NonScaling}` if both are wanted. Under a projective transform the
  two differ enough that a UI drawn with one and tested against the other is visibly wrong.

- [ ] Introduce `render2d` as a separate library from any rasteriser, and make that the boundary a
  future GPU backend replaces. An application, a UI toolkit and a game talk to `render2d`; they never
  name `soft2d`.

- [ ] Give it a `Canvas` with saved state and a recorded `DrawList`. The immediate-mode surface is
  `save`, `restore`, `set_transform`, `concat_transform`, `set_clip`, `set_opacity`, `set_blend_mode`,
  `begin_layer`, `end_layer` plus the drawing calls - but what it PRODUCES is a bounded, validated
  display list rather than pixels. That indirection is what makes the same drawing replayable by a
  CPU backend now and a GPU backend later, cacheable per component, analysable for damage,
  and testable without any backend at all - and SERIALISABLE LATER without a redesign, which is the
  weaker and true version of the "transportable to a compositor" this line used to claim (corrected
  2026-08-30: that phrasing was the third of the three places the file promised a transport Profile 1
  does not have; see the `DrawList` item below). A canvas that writes
  straight into a slice is a CPU-only API however carefully it is written.

- [ ] Specify the `DrawList` as Profile 1 requires it: IMMUTABLE, VERSIONED, CACHEABLE and
  REPLAYABLE IN-PROCESS. DECIDED 2026-08-30, and this item is what the decision replaced - it
  previously required a TRANSPORTABLE cross-process format, in the same part that decides against
  one, so the file carried two mandatory and mutually exclusive `DrawList` contracts and `P02M0136`
  could not build the shared font-resource seam against either.
  What Profile 1 keeps from the transportable form, because caching, damage analysis and backend
  independence need it: a versioned command schema; a resource TABLE with typed handles for paths,
  images, fonts and filters rather than inline references; stated resource lifetime and immutability;
  deduplication; bounded command bytes and bounded nesting; a defined validation boundary; and
  thread-safe replay and cancellation.
  What it drops, because a cross-process consumer does not exist and a compositor mostly wants
  finished surfaces rather than every application's vector command stream: canonical byte order and
  alignment, capability ownership and rights per referenced resource, cross-process snapshot
  semantics, and the validating parser as a security surface.
  A typed handle into a process-local resource table is NOT a raw Rust reference, and that is what
  makes the same list serialisable later without a redesign: cross-process transport becomes the
  named `render2d-wire` EXTENSION, designed against a real consumer, and adds byte order, rights and
  snapshot semantics on top of this schema rather than instead of it.

  AND PROFILE 1 STILL HAS A CANONICAL ENCODING, which is what two surviving requirements elsewhere in
  this file were actually asking for (decided 2026-08-30). The backend-free host tests asked for a
  "display-list serialisation round-trip" and part `b`'s Done asked the list to "round-trip through
  its transportable form", both of which read as the cross-process format this item just deferred.
  They are kept, renamed, and given a meaning that does not contradict it.

  THE CANONICAL ENCODING IS AN INTERNAL BINARY FORM, NOT A WIRE ABI. Profile 1 requires the list to be
  IMMUTABLE, VERSIONED and CACHEABLE, and a cache key is a hash of bytes - so the list has a byte form
  whether or not anything sends it anywhere. What that form is NOT: stable across releases, endian-
  defined, rights-bearing, or safe to accept from another process. It carries no capability, and
  nothing validates it as untrusted input, because its only producer and only consumer are the same
  process's own `render2d`.
  What it IS: the representation the version and the semantic hash are declared against, and the one a
  round-trip test exercises - encode, decode, replay, and get the same draw. That test is worth having
  for the same reason the hash is: a list whose encoding loses a field silently produces a cache hit
  on a drawing that is not the one recorded.

- [ ] Define prepared-list compatibility in the backend-neutral contract. A `PreparedDrawList` is
  bound to the profile version/hash, backend identity/version, target format, color space, physical
  extent and scale, immutable resource identity/layout generations, glyph-cache generation and
  filter parameters. `is_compatible` checks these before replay; a mismatch returns a typed
  re-prepare requirement. Track mutable image content separately: a new video frame refreshes the
  affected upload/sampling cache without re-flattening unchanged paths. Tests change each dependency
  alone and distinguish content refresh from structural invalidation.

- [ ] Provide a bounded reusable `DrawList` builder that retains its capacity. Re-recording changes
  to transform, opacity, color, scroll offset or image frame within its reserved command/resource
  bounds allocates nothing; exceeding capacity returns the typed limit/reservation outcome before
  replay. Only one immutable snapshot is mutated/replaced at a time, and any live prepared snapshot
  retains its own resources. This is an in-process recording API, not a retained UI framework or a
  new cross-process transport. Re-preparation reuses reserved scratch when sufficient and explicitly
  reports a larger requirement when the changed geometry needs it.

- [ ] Paths carry OPERATIONS and QUERIES, not only construction. Union, intersection, difference,
  xor and simplify; fill and stroke hit testing; tight bounds and stroke bounds; length; point and
  tangent at a distance. A vector editor, a map, a chart, a diagram and every custom control that
  responds to a click needs these, and an application that cannot ask a path whether it contains a
  point implements its own geometry - which is the private-rasteriser failure one level up.

  Boolean operations need their edge cases decided or two backends produce different unions: how
  self-intersections are resolved, what an OPEN subpath means as an operand, how two operands with
  different fill rules combine, whether the result preserves curves or is polygonised, the tolerance
  used, the canonical ordering and winding of the output contours, what happens to degenerate and
  zero-length edges, and that the whole operation is deterministic for a given input.

- [ ] Flattening tolerance is ONE number shared by everything that consults geometry, and projective
  transforms give it a singularity to answer for. Curves are flattened ADAPTIVELY IN DEVICE SPACE
  with the tolerance stated in PHYSICAL PIXELS and a bounded maximum subdivision depth, and reaching
  that depth has a stated outcome rather than an implementation's guess. Under a projective
  transform, a control point at or near `w == 0` is on the projective horizon: define the epsilon,
  and define whether such a segment is clipped or the primitive refused - a curve whose control point
  projects to infinity has no useful flattening. The same tolerance governs fills, strokes, boolean
  operations, path length and hit testing; if a bounds query flattens differently from the
  rasteriser, an application asks where something is and draws it somewhere else.

- [ ] Antialiasing is REQUIRED with a stated contract rather than a stated algorithm: coverage in
  `[0, 255]`, composited premultiplied, deterministic clipping, bounded scratch memory, applied to
  path fills, strokes, ellipses, rounded rectangles, transformed edges and glyphs. The backend picks
  scanline coverage or another method; the public layer fixes that an edge HAS a coverage value and
  that two backends agree on the geometry producing it. Aliased one-pixel drawing stays available as
  an explicit fast path, not as the only thing there is.

- [ ] Compositing is the full set. Porter-Duff: `Clear`, `Source`, `Destination`, `SourceOver`,
  `DestinationOver`, `SourceIn`, `DestinationIn`, `SourceOut`, `DestinationOut`, `SourceAtop`,
  `DestinationAtop`, `Xor`, `Plus`. Separable blend modes: `Multiply`, `Screen`, `Overlay`, `Darken`,
  `Lighten`, `ColorDodge`, `ColorBurn`, `HardLight`, `SoftLight`, `Difference`, `Exclusion`.
  Non-separable: `Hue`, `Saturation`, `Color`, `Luminosity`. That is twelve Porter-Duff operators
  plus additive `Plus`, which is not one of them - the count and the list have to agree. The
  non-separable four are NOT deferred: they are what a theme tint, a highlight and a duotone are,
  they need the whole pixel rather than a per-channel function, and adding them later changes the
  shape of the compositing loop.

  Every mode carries its EQUATION and not only its name, because `SoftLight`, `ColorDodge` and
  `ColorBurn` have several implementations in the wild that disagree at the ends of the range, and
  the non-separable four need their luminance and saturation model and their gamut-clipping rule
  written down or they are four different pictures. State also the working space each is evaluated
  in and what happens to an out-of-range result.

- [ ] Layers and group opacity: `begin_layer(bounds, opacity, blend_mode, filter)` / `end_layer`.
  This is basic UI semantics rather than an effect - two overlapping children inside a parent at
  50 % opacity must be composited together and blended once, not blended individually against the
  background. A design without layers cannot add them later without changing every drawing call that
  has an opacity.

- [ ] Filters are a bounded `FilterGraph` rather than three named effects, because a filter chain over
  a layer changes the shape of a display list and retrofitting one is a rewrite of the model. Nodes:
  `GaussianBlur`, `DropShadow`, `ColorMatrix`, `Composite`, `Blend`, `Convolution`,
  `MorphologyDilate`, `MorphologyErode`, `DisplacementMap`, `Crop`, `Tile`. A BACKDROP filter has a
  place in the command model - background blur is what every current shell does behind a panel - and
  the graph is depth- and cost-bounded so a hostile document cannot ask for an unbounded chain.

  Every node declares its BOUNDS ALGEBRA, and a backdrop filter is why this is not bookkeeping: an
  input-bounds transform, an output-bounds expansion, a dependency-bounds function and a maximum
  kernel radius and cost. A blurred panel does not change only when its own content changes - it
  changes when the content UNDER it changes, because that is what it samples. Without dependency
  bounds an application redraws the panel, reports its rectangle as damage, and leaves a stale blur
  of yesterday's background behind it; with them, damage under the panel expands through the kernel
  and out to the panel's own bounds. Damage computation over a `DrawList` consults this algebra
  rather than assuming a node touches only what it covers.

- [ ] Text enters through `draw_glyph_run(glyph_run, paint, transform)`, where a `GlyphRun` is
  already-shaped, already-positioned glyphs. Shaping, OpenType parsing, fallback, BiDi, line breaking
  and layout are `P02M0136`. But `render2d` must render EVERYTHING that stack can produce, not only
  the easy case: outline glyphs, grayscale masks, LCD subpixel masks where a surface supports them,
  bitmap strikes, colour-layer glyphs, embedded colour bitmaps (emoji), transformed glyphs, and
  fractional subpixel positioning. A grayscale-outline-only glyph run is not a text rendering layer,
  it is the first quarter of one.

  Colour glyphs mean the specific formats and not a category: `COLR` v0 layer lists, and `COLR` v1's
  paint graph with solids, gradients, transforms, clips and composites - which is why it is specified
  HERE, since a v1 paint graph is drawn with the very paints, clips and composites this profile
  already has. Bitmap strikes and embedded colour bitmaps come with it. SVG glyphs stay out, because
  SVG parsing is deferred by name.

  LCD subpixel masks need something from the surface or they cannot be implemented at all, so the
  surface carries `SubpixelLayout::{None, RgbHorizontal, BgrHorizontal, RgbVertical, BgrVertical}`
  and the rules that follow: an unknown layout means grayscale; a rotated or non-axis-aligned
  transform means grayscale; rendering into a transparent offscreen layer means grayscale, because
  three coverage values against an unknown backdrop cannot be composited correctly later; and the
  gamma the masks are blended in is stated. Every one of those falls back rather than refusing - a
  mandatory profile entry that a surface cannot satisfy must degrade, not fail.

  This milestone proves the RASTERISATION half end to end and stops there: the fixtures are
  pre-decoded glyph outlines, a bitmap strike and a colour glyph, and what is demonstrated is that a
  positioned `GlyphRun` of each kind renders and caches. It does NOT load a font file. Requiring that
  here would create a cycle - `P02M0136` parses fonts and depends on this part for drawing, so this
  part depending on font parsing means each waits for the other, or a throwaway parser gets written
  twice and the second one inherits the first one's bugs. Real font file to shaped run to pixels is
  proven once, in `P02M0136`, against the renderer this part delivers.

- [ ] The public error type is typed and distinguishes what the caller can act on: an out-of-range
  coordinate is CLIPPED and is not an error, while an unusable target, an unsupported paint or format
  OUTSIDE the profile, a non-finite transform, an unrepresentable size and an exhausted budget are
  each their own variant. Recording into a `DrawList` validates eagerly, so an application learns at
  the call that produced the mistake rather than at replay.

- [ ] Host tests for this part run WITHOUT a backend: recording, validation, transform algebra
  including projective composition and inversion, path construction, flattening tolerance, boolean
  operations against known results, hit testing, bounds, clip stack nesting, layer nesting, state
  save/restore balance, filter-graph bounds, display-list CANONICAL-ENCODING round-trip - which is
  not a wire ABI, see the `DrawList` item - and damage
  computation. Property and fuzz tests feed hostile transforms, unbounded paths, degenerate curves
  and deep nesting under strict allocation limits.

## P02M0103c - soft2d, the complete CPU implementation of Profile 1

The software backend. `no_std` core, no DisplayService dependency, draws into an `ImageViewMut` or an
`OwnedImage`. It implements the WHOLE profile; a command in Profile 1 that it cannot execute is a
defect, not an `Unsupported`.

- [ ] Implement the `render2d` backend interface in TWO phases, `prepare` then `render`, because
  "no steady-state allocation" and a profile containing layers, clip masks, blur, backdrop filters,
  path flattening, glyph caches and image pyramids cannot both be true without saying WHEN the
  scratch is worked out:

      let prepared = backend.prepare(&draw_list, target_desc, limits)?;
      backend.render(&prepared, target)?;          // allocates nothing

  `prepare` validates the resource table, flattens and caches paths, computes tile bins, resolves
  filter and dependency bounds, builds image pyramids, reserves layer and filter scratch, and returns
  either the exact memory requirement or a typed refusal. `render` replays. A frame that repeats with
  the same list and the same target then genuinely allocates nothing, and a frame that cannot fit
  says so before it starts drawing instead of failing halfway through a filter chain. It is one
  backend among future others and its public surface is the backend trait, not an application-facing
  drawing API. The composition crate alone selects `soft2d`; consumers use the trait. Prepared-list
  compatibility and content refresh follow `b`, including its negative invalidation tests.

- [ ] Implement a coverage antialiasing rasteriser for filled paths under both fill rules, with
  bounded scratch storage, no allocation during a draw, and deterministic output for a given input.
  This is the core of the whole 2D stack: shapes, strokes, rounded rectangles, ellipses and glyph
  masks are all paths through it.

- [ ] Implement stroking by converting a stroke to a fill - caps, joins, miter limit and dashing -
  rather than by a second rasterisation path with its own rules. Keep the aliased integer line as an
  explicit fast path with the rule it already had: integer Bresenham with BOTH endpoints included,
  ties at `error == 0` broken toward the smaller minor coordinate, and clipping applied before
  rasterising so a clipped line covers the same pixels as the visible part of the unclipped one.

- [ ] Implement clipping as a coverage mask rather than a rectangle test, so rounded, path, nested
  and inverse clips cost the same machinery as rectangular ones and masks intersect. The
  axis-aligned rectangle case keeps a fast path.

- [ ] Implement every paint: solid, linear, radial and CONIC gradients with multi-stop interpolation
  in linear light, `Clamp`/`Repeat`/`Mirror` spread, per-paint transform, and image patterns. Conic
  is in the profile, so it is implemented here rather than typed and refused.

- [ ] Implement image drawing at quality, not only at correctness: nearest, bilinear and bicubic
  sampling; mipmapped or pyramid-based minification so a large downscale does not alias; and
  anisotropic sampling or a bounded multi-tap approximation for projective transforms. A general
  image renderer that only has bilinear produces shimmering thumbnails, which is the most visible
  defect a 2D library can ship with.

- [ ] Implement layers, group opacity, every Porter-Duff operator, every separable and non-separable
  blend mode, and the whole `FilterGraph` over offscreen layer targets from `P02M0103a-common`. Layer and
  filter intermediates use the canonical premultiplied linear `R16G16B16A16_FLOAT` format, and the
  final conversion to the surface format applies tone mapping and dithering where the target is
  narrower.

- [ ] Implement glyph rendering for every glyph kind the profile lists, with a bounded glyph cache
  and a mask cache for repeated shapes. **THE GLYPH CACHE KEY IS THE ONE `P02M0136` DEFINES, COPIED
  HERE RATHER THAN SUMMARISED** (2026-08-30). It was face, size, transform and subpixel phase, which
  omits the glyph identifier itself as well as everything that distinguishes two instances of one
  face - so a variable, colour, bitmap or replaced-face glyph collided with another and the cache
  returned stale pixels. The complete key is:

    face IDENTITY and GENERATION   content-derived, so two loads of one file are one face and a
                                     replaced file is a different one; the generation is what
                                     invalidates everything derived from a face that was replaced
    FACE INDEX                     which face within the file (added 2026-08-31). A TrueType or
                                     OpenType COLLECTION is one file, so every face in it shares the
                                     content-derived identity, and two of them can carry the same
                                     glyph index meaning different glyphs with every other field of
                                     this key equal. `P02M0136`'s resource contract already defines a
                                     face as identity PLUS index; leaving the index implicit here
                                     made the key and the contract disagree about what a face is
    glyph                          the glyph index, which was missing outright
    size                           and the transform, as before
    variation coordinates          the normalised axis coordinates of the instance
    subpixel phase                 as before
    glyph kind                     outline, grayscale or subpixel mask, bitmap strike, `COLR` layers
    strike and palette             where the kind uses them - the bitmap strike selected, and the
                                     `COLR` palette index
    RASTERISATION MODE             which for an LCD mask is the `SubpixelLayout` it was rasterised
                                     for (added 2026-08-30). This file requires LCD output to differ
                                     by RGB/BGR and horizontal/vertical, and to fall back to
                                     grayscale for an unknown layout, a rotated or non-axis-aligned
                                     transform and a transparent offscreen layer - so a key without
                                     it lets an RGB-horizontal mask and a BGR-vertical mask for one
                                     glyph share a cache entry, which is the stale-pixel case this
                                     key exists to prevent

  This key is jointly owned with `P02M0136` and is ONE normative definition: neither side may
  restate it in its own words, and a change to it is a change to both files in the same edit. The
  cache tests here and the shaping tests there exercise the same key, and each side has a negative
  case proving that two entries differing only in variation coordinates, generation, kind, strike,
  palette, rasterisation mode or FACE INDEX do not collide.

- [ ] ONE implementation of pixel conversion, compositing and sampling, living in `graphics-core` and
  shared by `pix`, `soft2d`, `soft3d` and DisplayService's own copy and scale path; none carries a
  private second copy. This is already real: `pix::blend_over` is an integer straight-alpha
  source-over on sRGB-encoded bytes while a correct compositor needs linear light, so the shared
  primitive takes its WORKING SPACE as part of the contract and each caller states which it uses.
  Fix the stage ORDER once: decode transfer function -> convert straight to premultiplied -> sample
  and filter -> blend in the stated working space -> convert back to the target's alpha mode ->
  encode transfer function -> quantise with dithering. Filtering premultiplies BEFORE interpolating -
  interpolating straight alpha pulls the colour of fully transparent texels into the result and rings
  dark halos around transparent edges. And "shared" means CALLED.

- [ ] Rectangles and gradients are exact rather than approximately agreed. `Rect` is half-open, a
  fill covers every sample point inside it, a one-pixel aliased `stroke_rect` lies INSIDE the
  rectangle rather than straddling its boundary, and a zero-width or zero-height rectangle draws
  nothing and is not an error.

- [ ] Damage returned from a draw is CONSERVATIVE, not exact. Exact damage would mean comparing old
  and new pixels - a source-over with alpha zero changes nothing while covering a rectangle - and
  that cost defeats the purpose. Every draw returns the smallest inexpensive conservative raster
  bound: it contains every pixel that may have changed, is clipped to the target and clip stack, and
  is empty when no sample was reached.

- [ ] Make it a REAL software backend, not a reference implementation: tile-based rendering with
  per-tile command binning, SIMD fast paths, cancellation when a surface is closed or resized
  mid-frame, and caches for paths, masks, glyphs and filter intermediates. A BOUNDED WORKER POOL IS
  NOT REQUIRED BY THIS ITEM and is not in `c`'s Done condition; it is added by the later performance
  item when the self-thread runtime exists (see below). Tiling, binning, SIMD and cancellation are
  what this item owns, and they are the work that makes a pool worth adding later.

  **AND THE WORKER POOL HAS NOTHING TO BUILD ITSELF FROM** (2026-08-30). `soft2d` and `soft3d` both
  make a bounded worker pool mandatory and both differential-test their parallel paths, and ordinary
  userspace has no way to create a second thread. `SYS_THREAD_CREATE` requires `MANAGE` on a Process
  capability, which an application does not hold over itself; the only wrapper is
  `rt::process_prepare`, the process-LAUNCH path, which always passes the one `USER_STACK_TOP`;
  demand stack growth is bound to that same single span, so a second thread cannot have a growable
  stack; Thread is not a waitable object, so there is no join; and Event's only reset is
  `#[cfg(test)]`, so there is no reusable wakeup for a pool. The three ways out of that without a
  mechanism are all forbidden here: granting each renderer client broad process-management authority
  over itself, hand-rolling unsafe stacks and polling inside the renderer, or silently falling back
  to the single-threaded path the Definition of Done rules out.
  So the SELF-THREAD RUNTIME is a prerequisite OF THE LATER PERFORMANCE ITEM and of nothing in `c`
  or `g`, and it is not owned here: narrow self-spawn authority that is not process-management
  authority, independently placed guarded stacks with their own Domain accounting, join and
  cancellation, allocator and per-thread state rules under concurrency, crash propagation, and
  deterministic shutdown - tested on all three architectures.

  MULTITHREADING IS OUT OF PROFILE 1 EVERYWHERE, NOT ONLY IN THE DONE CLAUSES (2026-08-30). A
  previous round removed it from `c`'s and `g`'s completion conditions and left it mandatory in the
  prerequisite matrix, in both software-renderer work items, in the 2D and 3D test items and in the
  aggregate 2D completion clause - so both parts remained unschedulable in every place that actually
  governs the work. Every one of those has been corrected: the matrix says NOT the runtime, the two
  work items own tiling and SIMD and not a pool, and the differential tests are conditional on a
  parallel path existing rather than requiring one. `c` and `g` are done when they run TILED, and a
  correct single-threaded tiled rasteriser is a conforming one.

  `P02M0135` needs the same mechanism for its foreign thread facade IF its derived inventory names
  thread creation; it has pinned a configuration that does not, so neither file is now blocked on an
  unnumbered runtime. Whoever numbers it owns one contract for both consumers; two private thread
  runtimes for one system is what this paragraph exists to prevent.

  With one rule that keeps it honest: there is a DETERMINISTIC SCALAR REFERENCE path, and every
  parallel or SIMD path THAT EXISTS is differential-tested against it - unconditional once one
  exists, and not a requirement that one be created. The bit-exact/tolerance split at the top
  of this file says which parts must match exactly - geometry classification, clipping topology and
  quantised geometry - and which match within tolerance. 2D COVERAGE IS TOLERANCE-BASED: this said
  "coverage and geometry classification" must match exactly, which contradicts the split it cites,
  because this file deliberately lets a 2D backend choose its coverage algorithm (corrected
  2026-08-30). The scalar reference and its parallel paths must still agree bit-exactly WITH EACH
  OTHER - they are one algorithm - and that is a different claim from a conformance tolerance
  against another backend.

- [ ] Host tests cover path filling under both rules, coverage values along known edges, stroke caps
  and joins, dashing, clip nesting, rounded and inverse clips, layer opacity against manually
  composited reference values, every Porter-Duff operator and every blend mode against its algebraic
  definition, projectively transformed image drawing and minification, every spread mode,
  colour-space conversion and tone mapping, dithering, every FilterGraph node individually,
  representative composed graphs and backdrop-dependency propagation, glyph masks of
  every kind, returned damage bounds, guarded canaries around the target, and - where a parallel or
  SIMD path exists - scalar-versus-parallel agreement. Property and fuzz tests feed hostile
  coordinates, extents, pitches, transforms,
  degenerate curves and deep filter graphs and prove no panic and no out-of-bounds write.

- [ ] Meet an enforceable soft2d performance floor with a HEADLESS benchmark runner owned by `c`.
  It records bounded `DrawList` fixtures once and replays into `OwnedImage` without a Surface,
  DisplayService, `test2d-sw.lsexe` or part `d`. The same four fixtures are reused by `d` for the
  interactive proof; the live application does not become a prerequisite of this headless backend.

      UI-basic       rectangles, pre-decoded glyphs, images and clips
      UI-effects     layers, shadows and backdrop blur
      vector-stress  paths and curves at a fixed stroke width and join set
      image-stress   scaling and color conversion, including one YUV source

  Freeze each fixture's command/resource counts, extents, kernels and deterministic content before
  recording acceptance measurements, so a later simplification cannot lower the workload silently.
  Document the x86_64 reference host already used in `docs/PERF.md`, CPU model/core count, build flags,
  runner environment, clock, warmup and measured sample count. Measure prepared replay at 640x480:
  preparation/upload and live presentation are reported separately. Record median and 99th percentile.

      UI-basic, image-stress     median <= 16.7 ms
      UI-effects, vector-stress  median <= 66.7 ms

  These ceilings are fixed independently of implementation. The initial frozen budget per scene is
  the first accepted measurement or the ceiling, whichever is lower, and may only be lowered later.
  The registered benchmark gate exits non-zero if any median exceeds its frozen budget OR ceiling;
  a first implementation that misses a ceiling fails `c`, rather than defining a passing baseline.
  Any intentional ceiling or fixture change requires an explicit change to this plan and its recorded
  performance contract. Measure one fixed HiDPI scale and emulated aarch64/riscv64 results separately;
  those are reported and are not held to the 640x480 native reference-host ceiling.

## P02M0103d - 2D conformance and 2D Test SW

The gate that says Profile 1 is real. Nothing in the 3D track needs it. In-tree headless consumers
may exercise the `b`/`c` foundation; a complete presenting 2D application additionally passes this
part, which depends on `c` and `a-wsi`.

- [ ] Build a 2D CONFORMANCE suite as a governed PIE named **2D Conformance SW**, canonical artifact
  `test2d-conformance-sw.lsexe`, so it is named and auditable on the same terms as the 3D one rather
  than being "a suite" with no artifact. It states its grants, its deterministic control protocol,
  its result format, its timeout, how a failing feature is identified by name, and its architecture
  matrix. It walks the profile entry by entry - driven by the machine-readable registry, so a feature
  added to the profile without a test here is a gate failure - with a scene per feature and a stated
  pass condition per scene rather than one screenshot. It runs on all three
  architectures, tolerance-based where floating point demands it and exact where the bit-exact half
  applies, and it FAILS if any profile entry reports `Unsupported`. Coverage: both fill rules,
  curves, every cap/join/dash, projective transforms, all four paint kinds with every spread mode,
  rect/rounded/path/nested/inverse clipping, layer nesting and group opacity, every Porter-Duff
  operator, every separable and non-separable blend mode, nearest/bilinear/bicubic and minification,
  every filter node and a composed graph, backdrop filtering, wide-gamut and linear composition,
  HDR-to-SDR output with dithering, path boolean operations and hit testing, and every glyph kind
  including a colour emoji.

- [ ] Add one governed PIE named **2D Test SW**, canonical artifact `test2d-sw.lsexe`, only
  `display + input-keys` grants, Esc/q exits, Space pauses, R resets. It renders THROUGH `render2d` -
  an application-local rasteriser or a duplicate drawing loop is not accepted, and that includes
  generating any test image. This is the INTERACTIVE proof; the suite above is the exhaustive one,
  and neither substitutes for the other.

- [ ] The scene demonstrates the platform an application would actually use: filled and stroked
  Bezier paths under both fill rules, antialiased edges at a shallow angle, a projectively
  transformed image, a multi-stop conic gradient, a rounded-rectangle clip with content scrolling
  under it, a nested and an inverse clip, a group-opacity layer containing two overlapping children,
  several non-`SourceOver` blend modes including a non-separable one, a backdrop-blurred panel, and a
  line of text including a colour glyph.

- [ ] It runs in PHASES, because "an animated background" and "two non-contiguous damage rectangles
  that reach the driver as two" contradict each other - a background that changes every frame changes
  every pixel, and its only honest damage is `Full`:

      1. full        everything drawn, damage = Full
      2. partial     background held still, objects move, damage = old bounds + new bounds
      3. multi-rect  exactly two distant regions change; the driver stand-in sees TWO rectangles
                     before any backend merging
      4. full-again  background parameters change, damage = Full
      5. resize      new generation, and its first frame is Full by the rule above
      6. scale       the scale factor changes with the logical size held constant; layout is
                     unchanged and every edge is resolved at the new physical resolution

- [ ] Prove the multi-surface model, since `P02M0103a-wsi` introduced it and an untested object model is
  a claim: open a SECOND surface and switch which surface is visible, presenting to each while it
  owns scanout. Hidden surfaces refuse new acquires with `NotVisible`; already accepted frames
  complete or discard in order. Resize or close one and verify the other's resources and generation
  survive. This proves independent lifetimes and queues, not simultaneous popup composition.

- [ ] It passes only when: the conformance suite passes on all three architectures with no profile
  entry unsupported; clean exit AND crash both restore the text console; a resize never presents a
  stale image; phase 3's two rectangles travel the whole path from application to driver as two; a
  live QEMU capture proves animation, antialiasing, blending, filtering and text; the package audit
  proves ET_DYN, provider identity and order, the exact grants and no statically duplicated drawing
  code; there is no steady-state allocation once the queue and resources exist; and a measured
  steady-state live frame budget at 640x480 and one HiDPI scale is recorded in `docs/PERF.md`.
  The accepted implementation also passes the shared HEADLESS benchmark from `c`, under its exact
  reference-host conditions, at or under each 640x480 scene's ceiling and frozen budget. Live
  presentation and HiDPI measurements are reported separately and do not redefine that floor.

## P02M0103e - render-math and Render3D Core Profile 1

The public, backend-neutral 3D API - resources, pipelines, passes, shaders and command lists. This is
the layer whose absence the seventh pass was written to correct. Depends on the `s-3d` FREEZE and on `P02M0103a-common`, and NOT
on `a-wsi` (2026-08-30; the `s-3d` half added 2026-09-01, for the reason the `b` header gives): 
`render3d` is a non-presenting consumer - it renders into targets and returns - so it needs the
image, format and colour model and nothing that owns a display.

- [ ] `render-math` is a CONTRACT and not a type list, and this file picks the values rather than
  requiring that someone pick them - "state the handedness" is not normative, a handedness is:
  angles in RADIANS everywhere, COLUMN-MAJOR storage, `M * v` as the transform, RIGHT-HANDED world
  and view space, clip-space depth in `[0, 1]`, counter-clockwise front faces. Constructors are named
  for what they encode - `perspective_rh_zo`, `orthographic_rh_zo` - so a caller cannot pick the
  wrong convention silently. Quaternions state their normalisation policy: constructors normalise,
  operations preserve unit length, and a zero-length quaternion is an error rather than a NaN
  generator. Inversion of a singular matrix returns a typed error and never a matrix of infinities,
  and every entry point states what it does with NaN and infinite inputs.

  Four of those choices only become unambiguous together, and leaving the last one out inverts what
  gets culled: `+Y` is up in world and view space, the camera looks along `-Z`, NDC `+1` is the TOP
  of the image, the surface row origin is `TopLeft`, and the viewport maps
  `y_window = (1 - (y_ndc * 0.5 + 0.5)) * height`. Front-face culling is evaluated BEFORE that Y
  inversion. A Y flip reverses apparent winding, so an implementation that culls after it culls the
  opposite faces while satisfying every other sentence in this file.

- [ ] Define `Render3D Core Profile 1` as a closed, enumerated list on the same terms as the 2D one:
  a conforming backend implements every entry, and `Unsupported` is reserved for post-profile
  extensions. The profile:

      resources    buffers, 2D textures, cube maps, 2D arrays, 3D textures, samplers,
                   mipmaps, render targets
      geometry     u16/u32 indices, multiple vertex streams, configurable attributes,
                   triangle list/strip/fan, line list/strip, point list, primitive
                   restart, indexed and non-indexed, instancing, base vertex,
                   base instance
      pipeline     programmable vertex and fragment stages, vertex layout, primitive
                   topology, rasteriser state, depth/stencil state, blend state per
                   attachment, colour write masks, viewport and scissor
      passes       render passes with load (clear/load/discard) and store (store/discard)
                   per attachment, multiple colour attachments, depth/stencil attachment,
                   MSAA resolve, offscreen render targets, render-to-texture
      depth        Depth16, Depth24, Depth32F, Depth24Stencil8, Depth32FStencil8;
                   all compare ops, depth write, depth bias; stencil compare, read/write
                   masks, fail/depth-fail/pass ops, separate front and back state
      msaa         1x, 2x, 4x; sample masks; alpha-to-coverage; resolve
      sampling     nearest/linear min and mag, nearest/linear mip, trilinear,
                   clamp/repeat/mirror/border, LOD bias and min/max LOD, depth-compare
                   sampling, anisotropic filtering to max_anisotropy 8
      formats      R8, RG8, RGBA8, RGB10A2, RGBA16F, RGBA32F, R32_UINT, the depth formats
                   (encoding is ColorSpace's, never a second sRGB storage format)
      readback     colour, depth and object-ID attachment readback

- [ ] Introduce a PROGRAMMABLE shader model, `render-shader`, and treat it as the largest single item
  in this part - fixed-function Lambert and Blinn-Phong cannot be a complete renderer, because a
  procedural material, a picking pass, a scientific visualisation, a postprocess and every effect an
  application invents are all "a different fragment function". The model is a VALIDATED PORTABLE IR:
  vertex and fragment stages, typed uniforms, textures and samplers, varyings with `smooth`,
  `noperspective` and `flat` interpolation, bounded loops, no recursion, and checked memory access.
  `soft3d` interprets it first; a JIT or a translation to a GPU shader representation comes later
  without changing the API.

  Explicitly NOT Rust closures. A closure is the obvious shortcut and it is a dead end: a future GPU
  backend cannot take one, so the day the GPU backend arrives every shader in every application is
  rewritten. Ship a programmatic IR BUILDER so this milestone does not also have to write a GLSL or
  WGSL front end - a text shading language is a later, separate, optional piece.

  "A validated portable IR" is a description, and Profile 1 needs the enumeration. TYPES: `bool`,
  `i32`, `u32`, `f32`, their 2/3/4-component vectors, `mat2`/`mat3`/`mat4`, and arrays with a
  compile-time length. OPERATIONS: arithmetic, comparison, logical and bitwise, conversions, vector
  and matrix products, `dot`, `cross`, `normalize`, `min`, `max`, `clamp`, `mix`, `exp`, `log`,
  `pow` and the trigonometric set. CONTROL FLOW: `if`, `switch`, bounded `for` and `while`, `break`,
  `continue`, `return`, `discard`. VERTEX BUILT-INS: vertex and instance index, base vertex and base
  instance, the position output, point size. FRAGMENT BUILT-INS: fragment coordinate, front-facing,
  sample index, sample mask, and depth output. OUTPUTS: several colour attachments, an INTEGER
  attachment for object IDs, depth, and the sample mask.

  Two additions the profile's own requirements force. Interpolation needs `centroid` and `sample`
  beside `smooth`, `noperspective` and `flat` - without them MSAA shades at sample points that can
  lie outside the primitive and edges go wrong in a way that looks like a rasteriser bug. And
  DERIVATIVES - `dFdx`, `dFdy`, `fwidth` - are not optional here: the profile mandates implicit mip
  level selection and anisotropic footprints, and both are computed from derivatives. State how they
  are evaluated for a partially covered quad.

  Resource binding gets a real model rather than one `bind_resources` call: a `BindingLayout`
  declaring binding index, resource type, array length and stage visibility; `BindGroup`s built
  against a layout; and dynamic offsets. Shader IR and pipeline state otherwise have no shared
  contract about what binding 3 means.

- [ ] Enforce StrictF32 on every data and control dependency contributing to position, including
  transcendental operations such as sin/cos/pow/sqrt/normalize, conversions and indices that select
  contributing values. `SHADER_IR_1.md` supplies the deterministic operation semantics, operand
  order, rounding, no-FMA policy and non-finite handling. A contributing operation with no strict
  profile definition is rejected by IR validation, not silently evaluated with host libm tolerance.
  Non-position values may use RelaxedF32. This applies to the complete dependency slice, not only
  the final matrix multiply.

  Quantize position into `ClipCoordQ` before classification. `RENDER3D_PROFILE_1.md` freezes its bit
  width, fractional bits, signed range, rounding, overflow policy and intersection arithmetic before
  `e` starts. Classification, clipping topology and quantized raster coverage consume that canonical
  value and are bit-exact; relaxed varyings remain tolerance-based. Cross-architecture cases place
  strict transcendental-derived vertices on both sides of clip boundaries.

- [ ] Provide the command model: `CommandList`, `RenderPass`, `RenderTargetSet`, `GraphicsPipeline`,
  `ShaderProgram`, `VertexLayout`, `Buffer`, `Texture`, `Sampler`, `PipelineState`, with
  `begin_render_pass`, `end_render_pass`, `bind_pipeline`, `bind_vertex_buffer`, `bind_index_buffer`,
  `bind_resources`, `set_viewport`, `set_scissor`, `draw`, `draw_indexed`, `draw_instanced`. This is
  a USERSPACE rendering model and is not the OS-level adapter/device/queue vocabulary that stays
  deferred - the difference is that this one has two planned implementations and that one has none.

- [ ] Define asynchronous-capable submission in `render3d` before implementing either backend:
  `prepare -> submit -> Submission { completion, status }`, plus readback tickets using the same
  completion/status contract. Completion may already be ready for synchronous software execution;
  pending results expose a waitable progress source and a typed terminal result. Retain resources
  while work is in flight, bound outstanding submissions and readbacks, define cancellation,
  backend loss and release ordering, and refuse overflow without partial publication.

  Reuse the ordinary-channel completion primitive and ownership-consuming tokens chosen above;
  a renderer's bounded completion storage is reserved when preparing/creating the backend, not
  allocated once per steady-state frame. This is an internal backend API and needs no surface or
  DisplayService. The graphics-app helper waits for renderer completion before publishing WSI
  producer-ready; a render submission and a displayed-frame outcome are different completions.
  `s-3d` freezes the exact ticket and resource-lifetime rules; the software backend and a pending
  test implementation exercise the same public shape without adding a GPU driver or thread pool.

- [ ] Complete the RESOURCE model, because naming resource kinds in a profile and naming `Buffer` and
  `Texture` in a command list does not yet say how one is created or what part of it a pass writes.
  Descriptors: `BufferDesc` with size, usage and host visibility; `TextureDesc` with dimension,
  extent, mip levels, array layers, sample count, format and usage; `TextureViewDesc` with aspect,
  base mip and count, base layer and count, and view dimension; and `RenderTargetView` /
  `DepthStencilView` built from views rather than from whole textures - a shadow pass renders to one
  cube face and one mip, and a model without subresource views cannot say that. Operations: create
  and destroy, map and unmap, write buffer, write texture, buffer-to-buffer, texture-to-texture and
  buffer-to-texture copies, blit, resolve, generate mipmaps, clear and readback.

  Hazard behavior follows the single rules frozen in `RENDER3D_PROFILE_1.md`: simultaneous sampling
  and rendering of a subresource, attachment aliasing, visibility between passes, discard/read-after-
  discard and view/resource lifetime. That specification chooses each refusal or defined dependency
  before implementation; neither backend may decide locally. Reading discarded contents is a typed
  error. Tests cover each rule and resource lifetime across pending submissions.

- [ ] Enumerate the BLEND STATE, since "blend state per attachment" cannot be conformance-tested
  without knowing what a state may contain. Factors: `Zero`, `One`, `SrcColor`, `OneMinusSrcColor`,
  `DstColor`, `OneMinusDstColor`, `SrcAlpha`, `OneMinusSrcAlpha`, `DstAlpha`, `OneMinusDstAlpha`,
  `ConstantColor`, `OneMinusConstantColor`, `ConstantAlpha`, `OneMinusConstantAlpha`,
  `SrcAlphaSaturate`. Operations: `Add`, `Subtract`, `ReverseSubtract`, `Min`, `Max`. Separate colour
  and alpha state, a blend constant, per-attachment enable and a colour write mask.

- [ ] Fix the MSAA geometry rather than the sample count alone: the exact 2x and 4x sample positions,
  whether shading runs once per covered pixel or once per sample, the centroid location rule, the
  order in which the sample mask and alpha-to-coverage are applied, the alpha-to-coverage mapping for
  each count, and that resolve is an arithmetic average in linear premultiplied space. "4x MSAA"
  without sample positions is two different coverage results that both satisfy the words.

- [ ] Implement each depth/stencil format's mapping from clip depth, rounding, packing, clear,
  comparison, bias and stencil-operation semantics exactly as frozen in `RENDER3D_PROFILE_1.md`.
  `s-3d` selects one deterministic Depth32F storage/comparison rule before implementation; it must
  preserve the plan's bit-exact depth contract rather than allow backend-specific rounding or FMA.
  Depth16/Depth24 and stencil remain bit-exact too. The format table states stencil width/packing
  and depth-bias units. Conformance tests cover every format and comparison with boundary values.

- [ ] Bound the device rather than the frame: `Render3DLimits` states maximum attachments, texture
  extents and bytes, samples, vertex streams, attributes, uniform bytes, bound resources, draws and
  command bytes per list, shader instruction and loop bounds. Every request is checked against the
  implementation's hard maximum, what the raster arithmetic can represent, and the Domain's budget -
  but a failed check REFUSES or NEGOTIATES and never silently clamps: a caller given a quiet
  reduction finds out three frames later in a failure that names neither number. Over the hard
  maximum is `LimitExceeded`; over the Domain budget is `OutOfMemory`; and where less is granted than
  asked, construction returns the ACTUAL limits alongside the device.

- [ ] Typed errors throughout: `InvalidMesh`, `InvalidTransform`, `UnsupportedFormat`,
  `TargetMismatch`, `NonFinite`, `LimitExceeded`, `OutOfMemory`, `InvalidTexture`,
  `InvalidRenderState`, `InvalidShader`, `IncompatiblePipeline` - and `Unsupported`, which within
  Profile 1 is unreachable by construction and exists for extensions beyond it.

- [ ] 2D and 3D share `graphics-core`'s image/resource vocabulary and need no ad-hoc bridge API.
  A compatible 2D single-sample color target can be an `OwnedImage` that either layer consumes.
  Multisample, HDR or otherwise incompatible attachments undergo explicit resolve or conversion
  whenever sample count, storage, alpha representation or color space differs. The reverse path
  likewise validates texture format and usage instead of promising that every image is sampled
  without conversion. Interop tests cover both the compatible path and required conversions.

- [ ] Host tests for this part run WITHOUT a backend: vector, matrix and quaternion identities,
  camera and frustum transforms, pipeline and vertex-layout validation, render-pass attachment
  compatibility, shader IR validation including rejection of unbounded loops, recursion and
  out-of-range access, command-list bounds, submission/readback state transitions, and every typed
  refusal. An asynchronous stand-in and an already-completed software result exercise the same API. Property and fuzz tests feed
  hostile shader IR, malformed vertex layouts and incompatible attachment sets.

## P02M0103f - scene3d, the retained scene layer

The convenience layer applications use when they do not want to build command lists themselves. It
generates `render3d` command lists and contains NO backend-specific path. Depends on `P02M0103e`.

**THE ENGINE FEATURES ARE PART `f-ext`, NOT THIS PART** (2026-08-30). Two rounds ago the header
moved LOD, skinning, morph targets, animation, PBR and shadowed lighting, sorted
transparency, HDR and postprocessing into an optional later profile, and one round ago the `f` Done
clause said the same - while every mandatory item in this section still required them, and so did
the conformance suite in `h`, the demo in `i` and the aggregate 3D gate. A feature that is optional
in three sentences and required in ten items is not optional. They now live in their own separately
activated part below, with their own profile, their own gates and their own dependency on this one.
Nothing in `f` implements or waits for them.

- [ ] `scene3d` IMPLEMENTS - not "admits" - hierarchy with parent/child local and world transforms,
  cameras, lights, mesh instances, materials, render queues, bounding spheres and axis-aligned boxes,
  frustum culling, instancing, visibility masks, offscreen targets and a render-pass graph. The
  previous draft required these only to be possible to add later, which is the same deferral the
  seventh pass removed from `soft3d`: a scene layer that cannot express a hierarchy is one every
  application replaces. LOD selection, skinning, morph targets and an animation pose input are
  `f-ext`.

- [ ] Give `scene3d` its own closed profile, `Scene3D Core Profile 1`, in the same machine-readable
  registry as the other two. `Render3D Core Profile 1` is a low-level pass and pipeline API and does
  not contain hierarchy, materials, lights, sorted queues or picking - yet those are exactly what "a
  complete 3D library" means to an application, and with them outside every registry they are the one
  part free to drift away from its conformance suite and its documentation. A third
  `enum Scene3DFeature` and its constant closes that gap on the same terms: no `Unsupported` inside
  the profile, a test per feature, and the guaranteed minima in `SCENE3D_PROFILE_1.md`. The Extended
  features get their own closed `Scene3D Extended Profile 1` constant in the same registry, so
  "optional" is a profile a build either implements entirely or does not claim - not a set of
  features that may be half-present.

- [ ] Ship a STANDARD MATERIAL library over the programmable pipeline, so an ordinary application
  does not write shader IR to draw an ordinary object: `Unlit`, `VertexColor`, `Lambert` and
  `BlinnPhong`. `PbrMetallicRoughness` and its equation set are `f-ext`.

  `BlinnPhong` keeps the exact equation earlier passes fixed, now as a LIBRARY material rather than
  as the renderer's only path, so its tests keep their single expected answer:

      base    = material.base * vertex_colour * texture_rgb
      N = normalize(normal),  L = normalize(light_pos - frag_pos)
      V = normalize(eye_pos - frag_pos),  H = normalize(L + V)
      diffuse  = max(dot(N, L), 0)
      specular = if diffuse > 0 { pow(max(dot(N, H), 0), shininess) } else { 0 }
      atten    = 1 / max(kc + kl*d + kq*d*d, EPS)
      out      = clamp(ambient*base + atten*(diffuse*base + specular*spec_colour), 0, 1)

  with `EPS` and the `shininess` range named, specular carrying its OWN colour rather than
  multiplying the base, lighting never changing alpha, and the clamp applied before the
  transfer-function conversion.

- [ ] Lighting has no fixed count. Multiple directional, point and spot lights and an ambient term.
  "One directional and one point light" was a demo's requirement written as a design. Environment
  maps, image-based lighting and shadow maps are `f-ext`.

  No fixed count is NOT no limit, and this layer needs its own `Scene3DLimits` for the same reason
  `render3d` does - a scene is built from application data, and unbounded application data reaching a
  renderer is the shape every other part of this tree bounds: maximum nodes, hierarchy depth, mesh
  instances, lights per type, visible draws and passes, each with a stated refusal on overflow. With
  the structural checks beside them: CYCLE DETECTION in the hierarchy and in the pass graph, and
  dirty-flag propagation and update ordering. `f-ext` extends the same limit structure with its own
  fields rather than introducing a second one.

- [ ] Transparency is the library's job, not the caller's. An opaque queue and an alpha-mask queue,
  with per-draw blend state and colour write masks. A sorted transparent queue is `f-ext`; what is
  not acceptable is telling the application to sort its own geometry and calling that a design
  decision, which is why `f-ext` owns it rather than nobody.

- [ ] Picking and readback, because a desktop 3D application, an editor and a CAD viewport all need
  them: an object-ID attachment, a selection pass, and depth and pixel readback. The software backend
  may return an already-completed ticket; a future GPU backend returns a pending ticket through
  the submission/readback contract `e` defines, without changing the API shape.

- [ ] Host tests: hierarchy transform composition, culling against known frusta, queue ordering,
  instancing, picking, limit refusals, hierarchy and pass-graph cycle detection, and that every scene
  feature emits only `render3d` commands.

## P02M0103f-ext - Scene3D Extended: materials, lighting, animation and postprocessing

SEPARATELY ACTIVATED, like every other optional part in this file: it is designed and may be
implemented, and it is in no core Done gate. Depends on `P02M0103f`, `P02M0103g` and the
`s-3d-ext` freeze. Activated by
explicit approval when a real consumer asks for it - this file's own rule is that a shape with no
real consumer is a guess, and these features currently have none.

It carries its own closed `Scene3D Extended Profile 1` in the machine-readable registry, on the same
terms as every other profile here: no `Unsupported` inside it, a test per feature, and stated minima.
A build claims the Extended profile entirely or not at all.

AND A NORMATIVE PROFILE OF ITS OWN NEEDS A FREEZE OF ITS OWN, WHICH THIS PART DID NOT HAVE (added
2026-09-04). This file's governing rule is that a normative specification is frozen before the part
that implements it, and `s` previously defined only the four core freeze points - `s-common`, `s-2d`, `s-3d` and `s-wsi` -
none of which covers `SCENE3D_EXTENDED_1.md`; the `s-3d` Done clause names the CORE
`SCENE3D_PROFILE_1.md` and says the Extended document is outside it. So this part could be approved
and started with its own normative profile still mutable, which is the exact ordering defect those
gates exist to prevent, and it is the same omission `s-wsi` was added for on 2026-09-01.

`s-3d-ext` is the fifth freeze point: it is done when `SCENE3D_EXTENDED_1.md` exists with its registry
entry on the same terms as every other profile here, the five requirements the `s` section states hold
for it, and the choices its own rows leave open have one answer each. It is a prerequisite of `f-ext`
and of nothing else - no core Done clause waits for it, which is what keeps an optional part optional.

- [ ] LOD selection with stated thresholds, skinning, morph targets, and dynamic bounds recomputed
  for skinned and morphed geometry rather than trusting the rest pose.

- [ ] `PbrMetallicRoughness` over the programmable pipeline. The PBR material carries base-colour,
  metallic-roughness, normal, occlusion and emissive maps, `Opaque`/`Mask`/`Blend` alpha modes and a
  double-sided flag - which is the glTF data model, chosen deliberately as the reality check, because
  a material model that cannot consume the assets the world already has is a material model for one
  demo.

  "PBR metallic-roughness" is a family of implementations that disagree, so this profile picks: GGX /
  Trowbridge-Reitz normal distribution, Smith visibility, Schlick Fresnel, the roughness remapping
  and its minimum, the metallic workflow's base-colour interpretation, how occlusion is applied,
  the units of emissive, tangent-space handedness and the normal-map convention, how a double-sided
  surface flips its normal, the alpha-mask threshold, the environment prefiltering and BRDF LUT, and
  the colour space of EVERY map - base colour and emissive are `Color`, metallic-roughness, normal
  and occlusion are `Data` and are never transfer-decoded. Without those, a conformance scene has no
  single expected answer and the material is only a name.

- [ ] Environment maps, image-based lighting, and shadow maps: directional, spot, and cube shadows
  for point lights. With the parameters that give a conformance scene one expected answer: the
  shadow-map format, the constant and slope-scaled bias, the PCF kernel, the cube-face orientation
  for point lights, and whether directional cascades are inside this profile or outside it. Plus a
  shadow-resource budget in `Scene3DLimits`.

- [ ] Animation belongs to Extended alongside skinning and morph targets; core does not require a
  skeletal pose input or an animation player. The clip data model and playback here are: `AnimationClip` with channels, translation/rotation/scale
  tracks and morph-weight tracks; step, linear and cubic interpolation; loop, clamp and ping-pong;
  cross-fade and blending between several clips; skeleton pose evaluation; and a stated root-motion
  policy. Asset importers stay deferred - reading a glTF file is not this milestone - but the runtime
  that plays a clip is what makes the scene layer usable without one.

- [ ] A sorted transparent queue with stable back-to-front sorting. Weighted-blended OIT is an
  extension beyond this profile.

- [ ] HDR and postprocessing: a linear HDR render target, tone mapping (the same operator
  `P02M0103a` fixes), the output transfer conversion, full-screen postprocess passes and fog, with
  bloom as a built-in effect and its threshold and kernel named. The general pass and shader model
  means further effects need no architectural change.

- [ ] `Scene3DLimits` gains skeleton joints, morph targets, animation tracks, shadow maps and
  transparent items, each with a stated refusal on overflow.

- [ ] Host tests: LOD thresholds, skinning and morph evaluation against hand-computed poses,
  shadow-map matrix derivation, transparent sorting stability, PBR against its stated equations, and
  that every Extended feature emits only `render3d` commands.

- [ ] Its own conformance and demo coverage, in its own executables or its own clearly separated
  phases - it does NOT extend `h`'s or `i`'s mandatory scene lists, because those are core gates.

## P02M0103g - soft3d, the complete CPU implementation of Profile 1

The software 3D backend. It implements the WHOLE of `Render3D Core Profile 1`; a profile feature it
cannot execute is a defect, not an `Unsupported`.

- [ ] Split `soft3d` into PREPARE and EXECUTE the way `soft2d` is split, and for the same reason:
  resource upload, shader validation and lowering, pipeline creation, command-list validation and
  tile/bin scratch reservation all happen BEFORE submission, and `submit(prepared)` then allocates
  nothing in steady state. Its internal execute phase fulfills the `Submission` contract from `e`. Without that boundary, programmable shaders, pipeline creation, mipmap
  generation and the pass graph each become a place a frame can start allocating, and the
  no-steady-state-allocation requirement in this milestone becomes untestable on the 3D side while
  being enforced on the 2D one.

- [ ] Implement the rasteriser with these conventions fixed: sample points at pixel centres
  `(x + 0.5, y + 0.5)` at 1x, and the frozen profile positions at 2x/4x; fixed-point edge
  equations rather than floating-point comparisons, so
  coverage is exact and shared edges cannot double-fill or leave a seam; the top-left fill rule;
  deterministic back-face culling by winding; perspective-correct interpolation. Degenerate and
  off-screen primitives are skipped deterministically; non-finite positions are typed refusals
  under the clipper/IR contract. A SUB-PIXEL triangle is NOT
  skipped: it either covers a sample point or it does not, and dropping it by size makes small
  geometry flicker as the camera moves.

  The numbers, because "fixed-point" without them is half a contract and identical coverage on three
  architectures is what the other half decides: `SUBPIXEL_BITS = 8`, `MAX_RASTER_EXTENT = 16_384`,
  float viewport coordinates quantised by round-half-away-from-zero, edge equations in checked `i64`.
  Those constants make the arithmetic checkable: clipped viewport coordinates lie in `0..=2^22`
  subpixel units, so coordinate differences are at most `2^22` in magnitude, each edge product is
  at most `2^44`, and subtracting two products is bounded by `2^45`, within `i64`. Validate that
  coordinate bound before edge evaluation and record the proof beside the constants; a coordinate
  equal to `2^22` does not fit a signed 22-bit representation. All of this is on the BIT-EXACT side of the
  split at the top of this file.

- [ ] Implement qualifier-aware clipping of position and every varying. For edge endpoints `P0` and
  `P1`, the homogeneous intersection parameter `t` gives `P = (1-t)*P0 + t*P1`. Position and `smooth`
  varyings use that parameter. A `noperspective` varying uses the projected parameter
  `u = t*w1 / ((1-t)*w0 + t*w1)` and value `(1-u)*a0 + u*a1`, so it remains linear after perspective
  division. Use the profile's canonical clip coordinates and intersection/rounding rules; the
  existing `w == 0` refusal covers a non-projectable intersection rather than substituting `t`.
  These varying calculations remain on the tolerance-based side of the geometry/shading split.

  For `flat`, capture the original assembled primitive's provoking-vertex value before clipping and
  copy it unchanged to every resulting primitive. Never interpolate it or choose a new provoking
  value when clipping removes the original vertex or when the clipped polygon is fan-triangulated.
  The frozen per-topology provoking rule applies to triangles, lines and points alike. Repeated
  clipping preserves the same field and original flat value through every plane; normal, UV and
  colour semantics do not override the shader's declared interpolation qualifier.

  The six planes for the chosen right-handed `[0, 1]` clip space, each as the inequality a vertex
  satisfies to be inside:

      left    x + w >= 0        bottom  y + w >= 0        near    z     >= 0
      right   w - x >= 0        top     w - y >= 0        far     w - z >= 0

  With the degenerate cases decided: a vertex with `w == 0` is not projectable and its primitive is
  REFUSED; a zero intersection denominator produces no new vertex and the result follows from the
  inside/outside classification; a non-finite coordinate fails the primitive with `NonFinite`. The
  bound is a number, not a promise of one: Sutherland-Hodgman against a convex volume adds at most
  one vertex per half-space, so `MAX_CLIPPED_VERTICES = 3 + 6 = 9` and
  `MAX_TRIANGLES_AFTER_CLIP = 9 - 2 = 7` under fan triangulation. Those live in the clipper and are
  proven, not configured.

- [ ] Implement the shader interpreter for `render-shader` IR - both stages, all interpolation
  qualifiers, typed uniform and resource binding, bounded loops and checked access - with a
  deterministic scalar path. Position dependencies use the StrictF32/ClipCoordQ contract above;
  relaxed shading results are on the tolerance side. The interpreter cannot classify geometry from
  a position evaluated under relaxed transcendental semantics.

- [ ] Implement every pass and attachment feature in the profile: multiple colour attachments,
  load/clear/discard and store/discard per attachment, offscreen targets and render-to-texture, all
  five depth/stencil formats with the full compare, write, mask and stencil-operation set, depth
  bias, and MSAA at 1x/2x/4x with sample masks, alpha-to-coverage and resolve.

- [ ] Implement the full sampler and texture set: 2D, cube, array and 3D textures; mipmap generation
  and selection; nearest and linear min/mag and mip filtering; trilinear; clamp, repeat, mirror and
  border addressing; LOD bias and min/max LOD; depth-compare sampling for shadow maps; and
  anisotropic filtering to `max_anisotropy` 8 by a stated number of taps along the major axis, with
  the LOD and footprint derived from the shader derivatives. Cube-map face selection and orientation,
  seam handling across faces, border colours, the depth-compare return value, 3D filtering, and the
  filter used when generating mipmaps - including that generation happens in LINEAR light and that a
  `Data` or `NormalMap` image is never transfer-decoded on the way - are all stated. Texel centres are at
  `(i + 0.5) / extent`, and `repeat` is defined for NEGATIVE and large coordinates by Euclidean
  remainder so `-0.25` and `0.75` sample the same texel rather than differing by the sign convention
  of the language's `%`. sRGB texels are DECODED to linear before filtering and premultiplied before
  interpolation; blending is evaluated in LINEAR LIGHT.

- [ ] Implement the geometry set: all six topologies - triangle list, strip and fan, line list and
  strip, point list - with primitive restart, u16 and u32 indices, multiple vertex streams,
  configurable attributes, and instancing with base vertex and base instance. Multiple vertex streams
  and configurable attributes are what a skinned or morphed mesh later needs, and naming them that way
  is the whole of `g`'s relationship to `f-ext`: this item owns the PRIMITIVES, not the features
  built on them, and `g` is done without either (clarified 2026-08-30).

  The clipping and raster rules above are written for TRIANGLES, and the other two kinds need their
  own or they are undefined: the provoking vertex for `flat` interpolation, line rasterisation rules
  and width, point size and its rasterisation, how lines and points clip against the same six planes,
  and that back-face culling applies to triangles ONLY. A profile that mandates six topologies and
  specifies one is a profile with five gaps.

- [ ] Make it a REAL software renderer, not a reference implementation: tile and bin rasterisation,
  SIMD interpolation and shading, a texture cache, mipmap selection and hierarchical depth. A bounded
  worker pool is NOT required here, for the reason `soft2d`'s item gives. With the same honesty rule
  as `soft2d`: a DETERMINISTIC SCALAR REFERENCE path exists, EVERY PARALLEL OR SIMD PATH THAT EXISTS
  is differential-tested against it, and the bit-exact half of the split must match exactly while the
  shading half matches within tolerance. The differential test is conditional on such a path existing
  and unconditional once one does; it is not a requirement that one be created.

- [ ] Validate indices, attachment compatibility and finite transforms at ingestion; account peak
  memory to the process Domain; reuse frame allocations; allocate nothing per primitive during
  traversal and clipping. The renderer VALIDATES its borrowed colour target - format, extent, sample
  count and layout - and uses checked allocation only for what it owns: depth/stencil, scratch, tile
  and bin storage. It does not allocate a target it does not own.

- [ ] Frame scheduling lives ABOVE the renderer and not inside it. `soft3d` knows nothing of
  DisplayService: it renders into targets and returns. Acquire, present, pacing, completion, resize,
  `OUT_OF_DATE` and focus behaviour belong to the application or to a graphics-app helper shared with
  the 2D side - putting the present queue inside `soft3d` is what would make the later GPU renderer
  reimplement a window system to draw a triangle. This item is the INTEGRATION gate for that helper:
  render into the acquired image, send `PRODUCER_READY` on the client's send end when the render is
  complete, wait for the `PRESENT_DONE` that releases that image before reusing it - two pairs, one
  per direction, and not the single `CONSUMER-RELEASE` this line used to name (corrected 2026-09-02) -
  keep at most the
  negotiated frames in flight, pace against the timing contract rather than busy-spinning, reacquire
  surface images OUTSIDE the renderer on resize while the renderer transactionally recreates what it
  owns, keep or cleanly release the old scene on allocation failure, and block or throttle in the
  background while consuming no raw input.

- [ ] Host tests cover clipping against every plane, winding and culling, every interpolation qualifier,
  depth ordering and every compare op, stencil operations, shared-edge fill, sub-pixel coverage,
  every topology, instancing, every addressing and filtering mode, mip selection, MSAA coverage and
  resolve, alpha-to-coverage, shader IR execution including every interpolation qualifier, blending
  for every state, readback, non-finite and degenerate input, and guarded canaries around every
  attachment. Property and fuzz tests feed hostile meshes, indices, transforms, extents, UVs,
  viewport rectangles and shader IR under strict allocation limits.
  Include the analytic unequal-`w` edge `(x,w,a) = (-2,1,0)` to `(0,2,1)` clipped at `x+w=0`:
  `t=1/3`, projected `x/w=-1`, `smooth a=1/3`, and `noperspective a=1/2`. Check the surviving
  primitive's interpolated field, not only the new vertex, against the analytic unclipped field
  within the frozen attribute tolerance. Cover repeated-plane clipping and `flat` preservation when
  the original provoking vertex is removed and the remaining polygon produces multiple fan triangles.

## P02M0103h - 3D conformance suite

The gate for BOTH `Render3D Core Profile 1` and `Scene3D Core Profile 1`. It depends on `g`, `f`
and `c`: the backends and retained layer used by its mandatory interop scenes. Through `g` it also
waits for `a-wsi`, even though the conformance executable itself can render headlessly.

- [ ] Add a governed PIE named **3D Conformance SW**, canonical artifact
  `test3d-conformance-sw.lsexe`, driven separately by `RENDER3D_CORE_PROFILE_1` and
  `SCENE3D_CORE_PROFILE_1`. Every mandatory feature in either registry has a scene and stated pass
  condition; the registry gate checks each namespace independently. It FAILS on `Unsupported` in
  either core profile. Scenes:
  rasterisation and depth; clipping with all interpolation qualifiers, including `g`'s unequal-`w`
  analytic case and original flat-value preservation through clipping/fan triangulation;
  stencil operations; MSAA at each supported count; every primitive
  topology; a programmable vertex and fragment shader; mipmaps, trilinear and anisotropic filtering;
  a cube map; multiple lights of each kind; instancing; render-to-texture; a multi-pass graph;
  object-ID picking; and 2D/3D interop. The Extended features - PBR with normal mapping, shadows,
  skinning, morph targets, postprocessing, HDR and tone mapping, and sorted transparency - are NOT in
  this list (2026-08-30); they are covered by `f-ext`'s own conformance scenes, which run when that
  part is activated. A core gate that requires an optional part is not an optional part.

- [ ] Each scene is checked by a property that can fail for one reason rather than by a single golden
  image - a golden frame is a REGRESSION test and not a correctness oracle, it proves the output has
  not changed and not that it was ever right. Golden frames stay, tolerance-based, BESIDE per-stage
  differential tests and small analytic checks. The cheapest one worth having first is two triangles
  forming a quad: across the shared edge there must be neither a gap nor a doubly-covered pixel,
  which needs no reference image at all.

- [ ] Run it on all three architectures, with the bit-exact half compared exactly and the shading
  half within tolerance, and with the scalar reference and - where a parallel or SIMD backend exists
  - both exercised and compared.

## P02M0103i - 3D Test SW, the interactive application and interop proof

- [ ] Add one governed PIE named **3D Test SW**, canonical artifact `test3d-sw.lsexe`, only
  `display + input-keys` grants. It renders a continuously rotating indexed cube above a ground
  plane, with visibly distinct per-face material colours, correct shared-edge depth and occlusion,
  ambient plus directional plus animated point lighting, specular highlights, a perspective camera
  and a dark non-flat background; no face-order painter shortcut is accepted. This is the DEMO, and
  the conformance suite above is the proof - neither substitutes for the other.

- [ ] The scene exercises textures and blending: one cube face carries a checkerboard texture, the
  ground a repeated one, and a small semi-transparent panel stands in front - together putting UV
  interpolation, sampling, both addressing modes, alpha blending and the depth test under
  transparency through the whole path from scene to scanout. The textures are generated
  procedurally, so the application still needs only `display + input-keys`. A PBR sphere with a
  normal map and a cast shadow are `f-ext`'s demo phase, added to this scene when that part is
  activated and not before (2026-08-30).

- [ ] The scene MUST prove 2D/3D interop, because that is what an actual application looks like and
  the shared image model is otherwise an untested claim: `soft3d` renders into an `OwnedImage` and
  `render2d` composites a HUD over it - antialiased shapes, a translucent panel and a line of text as
  a glyph run - before the result is presented. One frame therefore travels
  `scene3d -> render3d -> soft3d -> OwnedImage -> render2d -> soft2d -> Surface`, which is the path a
  game, an editor and a map application all take.

- [ ] Make the scene interactive and recoverable: Esc/q exits, Space pauses, R resets, arrow keys
  orbit the camera, +/- changes distance or field of view, and a key toggles the object-ID picking
  overlay. Key-down/up state comes through the P02M0091 focus capability; focus loss clears held
  state. Resize recreates targets and preserves camera/animation state. Clean exit, crash, allocation
  failure, emergency kill and DisplayService restart always release images and restore the text
  console.

- [ ] Provide deterministic demo and test controls without exposing them in normal UI: fixed
  simulation time per frame, a fixed camera and a fixed light seed. The live application stays
  clock-driven and visibly animated.

- [ ] Governed kernel tests launch the staged PIE against stand-in Display/Input services and assert
  acquire -> multiple distinct presents -> resize/recreate -> key interaction -> release/exit, plus
  crash restoration and denied-capability behaviour. Package audits prove ET_DYN, provider identity
  and order, exact grants and no static duplicate renderer or runtime.

- [ ] Live QEMU validation on x86_64 captures at least three timed frames and uses pixel checks to
  prove the scene is nonblank, the projected cube occupies a bounded central region, depth occlusion
  and lighting gradients exist, and successive frames differ while the background and console do not
  leak through. The shadow check belongs to `f-ext`'s phase. There is deliberately NO "all
  six material colours appear across the captured rotation" check: that assertion cannot mean what it
  says, because which faces are visible depends on the rotation axis, the camera and the lighting -
  the bottom face may never be visible from an ordinary camera - and shading changes the RGB any
  classifier looks for. What replaces it is checks that can each fail for one reason: DETERMINISTIC
  TEST POSES in which the visible face is known in advance and verified per pose; the checkerboard
  face sampled for its alternating pattern; the ground texture sampled for its periodicity; the
  semi-transparent panel sampled for a blend of panel and background rather than either alone; and
  the HUD text sampled where a glyph is known to land. The LIVE animation check asserts only that the
  image changes between frames. Capture after resize on desktop and mobile-like aspect ratios; q must
  restore the console. AArch64 and RISC-V cross-build and run the deterministic tests; live
  screenshot parity uses tolerances where emulation cost is practical.

- [ ] Measure release performance and memory at 320x240, 640x480 and 800x600, and add a real
  benchmark beside the demo floor - a rotating cube at 640x480 is light enough that a slow rasteriser
  passes it. A deterministic benchmark scene with a fixed triangle count, deliberate overdraw,
  clipping and textures - NOT shadows and NOT a postprocess pass, which are `f-ext` and would make an
  optional part a core gate through the frame budget `i` closes on (corrected 2026-08-30) -
  reporting triangles per second, shaded fragments per second and nanoseconds per stage; and pin
  the environment those numbers mean anything in: vCPU count, QEMU version and configuration, CPU
  model, profile flags, and whether the clock is
  the guest's or the host's. The required floor is a stable 30 FPS at 640x480 on the documented
  x86_64 QEMU/KVM host with NO STEADY-STATE ALLOCATION while extent, scene and resources are
  unchanged - a resize and a texture upload legitimately allocate and are measured separately - plus
  bounded present latency and a reported colour/depth/scene peak. Record transform, clipping, raster,
  shading, texture sampling, blending and present times separately in `docs/PERF.md`. `f-ext` adds
  its own postprocess and shadow measurements to the same table when it is activated.
  Optimise tile and bin traversal only from measurements. Fixed-point edge evaluation is NOT on that
  list: it is a correctness mechanism this milestone requires for exact shared-edge coverage, so a
  measurement may not decide to drop it - only its implementation may be tuned.

- [ ] Publish `docs/GRAPHICS_2D.md` and `docs/GRAPHICS_3D.md`. The 2D document covers the public
  drawing model, coordinate spaces and scale, paths and fill rules, the antialiasing contract, paints
  and gradients, clipping and layers, compositing operators, the filter graph, the glyph-run seam,
  the colour and HDR model, the backend interface a GPU implementation replaces, and the full
  `Render2D Core Profile 1` list with its conformance results. The 3D document covers coordinate and
  colour conventions, the resource/pipeline/pass model, the shader IR, raster and clipping rules,
  the standard material library, the scene layer, memory formulas, controls, the performance table,
  the full `Render3D Core Profile 1` list with its conformance results, and the exact boundary a
  future Mesa/Gallium/Vulkan backend replaces. Both include a screenshot of the actual application
  and a tri-architecture validation matrix.

- Done when, PER PART:

  **WHAT EVERY `s` FREEZE GATE REQUIRES, STATED ONCE HERE RATHER THAN IN PASS 10** (corrected
  2026-08-31). The freeze gates used to ask only for documents and registry entries, while a
  checked pass-10 entry carried the harder half - one chosen answer per alternative, a semantic hash,
  the guaranteed minima, the tolerances - so the authoritative boundary and the record disagreed
  about what freezing means. The requirements are here now, and pass 10 points at this list instead
  of restating it. For each `s` sub-part:

    ONE ANSWER        every "choose one", "refused or defined" and open alternative INSIDE that
                        sub-part's documents is resolved to a single stated answer. A freeze with an
                        alternative left in it is not a freeze, and this is the clause that makes
                        `Depth32F`, the hazard set and the 2D/3D conversion question blocking rather
                        than decorative
    A SEMANTIC HASH   each document carries a version and a canonical semantic hash taken over the
                        MACHINE-READABLE registry it describes, not over the Markdown - prose
                        reflows, and a hash that changes when a sentence is rewrapped is a hash
                        nobody will keep current
    MINIMA            the guaranteed minimum limits the profile promises are numbers in the registry
    TOLERANCES        the conformance tolerance for every entry whose result is compared within one,
                        so "passes conformance" has a boundary rather than a judgement
    THE GATE AGREES   the profile-registry gate finds every mandatory feature named in both the
                        document and the registry, and their hashes match

  - `s-common` is done when `docs/GRAPHICS.md` and `IMAGE_COLOR_PROFILE_1.md` are written, the
    format, alpha and colour registry they describe exists and passes the registry gate, and the
    five requirements above hold for them, including the semantics, byte spans, initialization and
    YUV contracts in the specification and `a-common` sections.
  - `s-2d` is done when `RENDER2D_PROFILE_1.md` and the `Render2D Core Profile 1` registry entry
    exist, every mandatory entry is named in both, and the five requirements above hold - including
    the prepared-list, mutable-content and reusable-builder contracts in `b`.
  - `s-3d` is done when `RENDER3D_PROFILE_1.md`, `SHADER_IR_1.md` and the CORE `SCENE3D_PROFILE_1.md`
    exist with their registry entries on the same terms and the five requirements above hold -
    including strict position/ClipCoordQ, deterministic depth, hazards and asynchronous submission
    in `e`, qualifier-aware clipping and provoking-value preservation in `g`, and the separate core
    registry coverage with analytic clipping/interpolation checks in `h`.
  - `s-3d-ext` is done when `SCENE3D_EXTENDED_1.md` exists with its registry entry on the same terms
    and the five requirements above hold for it. It gates `f-ext` and nothing else: no core Done
    clause waits for it, which is what keeps an optional part optional. Added 2026-09-04 because
    `f-ext` carries a closed normative profile of its own and this section's rule is that such a
    profile is frozen before the part that implements it - the same omission `s-wsi` was added for.
  - `s-wsi` is done when `WSI_PROFILE_1.md` exists with its registry entry on the same terms, the
    five requirements above hold, and the choices its own row leaves open have one answer each -
    memory and cache ordering for a shared surface, the atomic surface-configuration rule, and the
    pre-compositor multi-surface semantics, alongside the scale-factor, event-set, present-state,
    damage and per-surface completion contracts specified in `a-wsi`.
  - `a-common` is done when `docs/GRAPHICS.md` names the layers; the canonical image model, the two
    coordinate spaces, the format and alpha sets, the colour model with its canonical linear
    intermediate, borrowed views, owned images and offscreen targets, the multi-plane model and the
    typed error all exist in `graphics-core`; `pix` is migrated onto them rather than living beside
    them; and the shared conversion, compositing and sampling have their host tests. NOTHING in this
    condition needs a display, a driver, a service restart or a completion object - which is what
    lets `b` start against it while `a-wsi` is still open.
  - `a-wsi` is done when the WSI, synchronisation, resource and lifecycle contracts are bounded,
    capability-scoped, documented and hostile-input tested; multiple surfaces per connection work;
    logical size, physical extent and scale factor are three separate numbers in the interface; the
    present queue, timing contract, damage list and completion wrapper behave as specified; the
    typed display-device interface has replaced the byte protocol; and the surface's output colour
    metadata is carried. It additionally requires `s-wsi` FROZEN - not merely "documented", which is
    what this clause used to ask and is the weaker thing a freeze gate exists to replace - along with
    P02M0141, P02M0165 and P02M0166, which `a-common`
    does not. It completes or consumes the remaining DisplayService catalogue migration named in its
    header; subscription alone does not satisfy identity-aware detach, failover and reacquisition.
  - `b` is done when `render2d` records and validates a display list covering every entry of
    `Render2D Core Profile 1`, the `DrawList` round-trips through its CANONICAL ENCODING - which is
    not a wire ABI, see the `DrawList` item - and the backend-free host tests pass.
  - `c` is done when `soft2d` implements EVERY Profile 1 entry with no `Unsupported`, shares
    `graphics-core`'s conversion, compositing and sampling rather than duplicating them, runs
    TILED, and passes its HEADLESS four-scene benchmark at or under each frozen budget and fixed
    ceiling. Multithreading is NOT in this condition: the self-thread runtime it needs has
    no owner, and a Done that requires an unowned mechanism cannot be met - it is a later performance
    item, and a correct single-threaded tiled rasteriser is a conforming one. The scalar-reference
    agreement stays and applies to whatever parallel or SIMD paths exist.
  - `d` is done when `test2d-conformance-sw.lsexe` passes on all three architectures with no entry
    unsupported, the profile registry gate finds every mandatory feature tested and handled, and
    `test2d-sw.lsexe` passes its host, package and live QEMU gates including the atomic scale-change
    and independent second-surface phases. The shared headless benchmark must also pass `c`'s
    performance floor on the documented reference host for the accepted implementation.
  - `e` is done when `render-math` fixes every convention above and `render3d` plus `render-shader`
    express every entry of `Render3D Core Profile 1` with validated IR and typed refusals.
  - `f` is done when `scene3d` IMPLEMENTS hierarchy, culling, queues, instancing and picking -
    emitting only `render3d` commands, under `Scene3DLimits`, with the `Unlit`/`VertexColor`/
    `Lambert`/`BlinnPhong` material set and unshadowed multi-light lighting.
    The engine features are part `f-ext`, separately activated, with its own `Scene3D Extended
    Profile 1` and its own gates; `f` neither implements nor waits for them, and no core item,
    conformance list, demo scene or aggregate gate in this file requires them (2026-08-30).
  - `f-ext` is done when `Scene3D Extended Profile 1` is implemented ENTIRELY - LOD, skinning, morph
    targets, PBR, environment maps and IBL, shadow maps, animation clips and blending, the sorted
    transparent queue, HDR and postprocessing - against `SCENE3D_EXTENDED_1.md`, with its own
    conformance scenes, its own demo phase and its own performance rows, and with no core work item,
    conformance list, demo scene or aggregate gate depending on any of it. It is separately activated
    and its absence is not an incomplete `f`.
  - `g` is done when `soft3d` implements EVERY `Render3D Core Profile 1` entry with no
    `Unsupported` and runs TILED. Multithreading is not in this condition either, for the reason `c`
    gives.
  - `h` is done when the 3D conformance suite passes on all three architectures and separately
    covers every entry of both the Render3D and Scene3D core registries.
  - `i` is done when `test3d-sw.lsexe` continuously shows the interactive scene with a `render2d` HUD
    composited over it, survives resize, focus loss and failure, restores the console on every exit
    path, meets the measured frame budget and passes host, package, focused QEMU and tri-architecture
    gates.

  **2D is complete** when no mandatory Profile 1 command returns `Unsupported`, everything that
  should be antialiased is, every paint, clip, layer, blend mode, filter and image mode passes
  conformance, any parallel or SIMD path that exists agrees with the scalar reference, no test scene
  needs private raster code, and every benchmark scene meets its frozen floor on the reference host.
  **3D is complete** when no mandatory Profile 1 feature returns `Unsupported`, programmable vertex
  and fragment stages work, passes and offscreen targets and depth/stencil and MSAA and mipmaps and
  instancing all pass conformance, `scene3d` contains no backend-specific path, and no ordinary 3D
  application needs to write its own rasteriser. Skinning, PBR, shadows and postprocessing are NOT in
  this condition: they are `f-ext`, and no core specification document, work item, conformance list,
  demo scene, benchmark or aggregate gate in this file requires any of them, and `f-ext` has its own
  completion gate in the per-part list above (2026-08-30).

- Explicitly deferred, each to a named milestone or as a named post-Profile-1 extension rather than
  to "later": full text shaping, OpenType, font fallback, BiDi and line layout (`P02M0136`); the
  foreign C/C++/TLS/Mesa substrate (`P02M0135`); a widget toolkit, UI layout engine, themes,
  accessibility and gesture recognition; the compositor, window decorations, desktop shell and
  multi-application presentation; SVG and PDF parsing; asset importers and model editors; ICC profile
  parsing; a text shading language front end for `render-shader`; weighted-blended OIT; occlusion
  culling; physics and gameplay systems; compute and ray tracing; and actual EGL/OpenGL/OpenGL
  ES/Vulkan entry points and conformance, Mesa/Gallium, LLVM shader compilation, GLSL/SPIR-V
  compilation, virglrenderer/Venus, virtio-gpu context/blob/3D command execution and native hardware
  drivers. Each requires a separately approved milestone and must consume P02M0103's contracts rather
  than weakening capability, validation, accounting or identity gates.
- Concept: P02M0014/P02M0038 (virtio-gpu 2D scanout and resize), P02M0091 (surface/input app platform),
  P02M0093/P02M0096a (shared providers and PIE ownership), P02M0097 (final userspace/system layout),
  P02M0008/P02M0010 (observability and governed resources), P02M0135 (foreign graphics stack),
  P02M0136 (text foundation), and the future desktop/GPU phases.

## P02M0103 pass 10 - canonical plan reconciliation

Reconciled 2026-09-08. This is a record of PLAN corrections, not another implementation list.
A checked entry means its requirement has been incorporated into the authoritative work section
named below. It does not declare a profile frozen, a backend implemented or a runtime gate passed.
The unchecked specification and implementation work remains in those sections, and the prerequisite
matrix and per-part Done clauses govern when each part may start and complete.

There are FIVE freeze gates: `s-common`, `s-2d`, `s-3d`, `s-wsi` and `s-3d-ext`. The first four
cover the core; only separately activated `f-ext` waits for the fifth. There is no `s-scene`: the
CORE scene specification is included in `s-3d`, while `SCENE3D_EXTENDED_1.md` is frozen exclusively
by `s-3d-ext`. No core completion condition requires the Extended document.

The shared freeze requirements are ONE ANSWER, SEMANTIC HASH, MINIMA, TOLERANCES and THE GATE
AGREES, defined in the per-part Done section. Numeric constants and algorithms still to be selected
are explicit specification deliverables in `s`; an implementation may not start against an unresolved
alternative. `b`, `c`, `e` and `f` can complete headlessly. `d`, `g` and `i` require `a-wsi` directly;
`h` inherits it through `g` and also consumes `f` and `c` for its retained-layer and 2D/3D tests.

- [x] **Definition of Done for specifications.** The per-part Done list declares each freeze
  independently and includes the five requirements above. Extended work remains separately gated.
- [x] **Split specification freeze.** The `s` table, prerequisite matrix, `f-ext` header and Done
  list all name the five freezes; core Scene3D belongs to `s-3d`, Extended Scene3D to `s-3d-ext`.
- [x] **One DrawList contract.** `b` requires immutable, versioned, cached in-process replay and
  internal encoding round-trip; cross-process `render2d-wire` remains an extension.
- [x] **Remove backend-local specification choices.** `s-3d` chooses deterministic depth and hazard
  rules; `e` implements those frozen rules instead of independently choosing alternatives.
- [x] **Cover every filter.** `c` tests every FilterGraph node, representative compositions and
  backdrop-dependency propagation; the obsolete three-filter restriction is removed.
- [x] **Valid image semantics.** `a-common` uses tagged `ImageSemantics`; alpha and color space live
  only in Color, and `s-common` freezes the storage/semantics/operations matrix.
- [x] **Owned non-color images.** `OwnedImage` owns any valid single-plane, single-sample semantics,
  including masks/data; specialized Render3D attachment resources remain separate.
- [x] **Shared format ownership.** `graphics-core::PixelFormat` includes R32_UINT and RGBA32F storage;
  `render3d` wraps common formats and its specialized depth/stencil formats with a capability table.
- [x] **Explicit image conversion.** `e` requires resolve/conversion when samples, storage, alpha or
  color space differ; sharing image types does not promise zero conversion for incompatible targets.
- [x] **Both core registries.** `h` and its Done clause independently cover Render3D Core and Scene3D
  Core. Extended feature coverage remains in `f-ext`.
- [x] **Three byte spans.** `a-common` separates minimum_visible_bytes, backend_access_span and actual
  allocation_len. CPU views use the minimum; present imports validate the full backend span.
- [x] **WSI memory visibility.** `a-wsi` specifies send-release/receive-acquire in both directions,
  coherent CPU-visible v1 imports and tri-architecture visibility checks; `s-wsi` freezes it.
- [x] **Initialization and padding.** `a-common` requires zero allocation, inter-Domain clearing and
  canonical reserved/padding bytes on export/readback. Full damage is not an initialization proof.
- [x] **Atomic surface configuration.** `a-wsi` specifies snapshots, serial acknowledgment,
  generation invalidation and Full first presentation, with separate close/completion events.
- [x] **Pre-compositor surfaces.** `a-wsi` permits one visible scanout-bound surface; `d` tests
  visibility switching, hidden acquisition refusal and independent queues/lifetimes, not composition.
- [x] **Normative YUV.** `s-common` covers plane layout, ranges, siting, filters, matrices, ordering,
  HDR metadata and malformed-plane refusal before `a-common` implements MultiPlaneImage.
- [x] **Prepared-list invalidation.** `b` binds prepared state to profile/backend/target/resource and
  glyph/filter dependencies while separating mutable image content from immutable geometry/layout.
- [x] **Cheap property changes.** `b` requires a bounded reusable builder with allocation-free
  re-recording within reserved capacity and explicit scratch-growth outcomes; no retained UI is added.
- [x] **Submission and readback.** `e` exposes already-ready or pending completion through one bounded
  API, owns resource lifetime/cancellation rules and is frozen by `s-3d`; `g` implements it.
- [x] **Strict position computation.** `e` and SHADER_IR_1 include all contributing data/control
  dependencies, including transcendentals, and gate implementation on a fully defined ClipCoordQ.
- [x] **Conformance thresholds.** `s` assigns numerical tolerances, image masks and comparison rules
  to each owning profile before its freeze. Extended-only thresholds cannot block a core freeze.
- [x] **Semantic hashing.** `s` requires SHA-256 over canonical machine-readable semantic inputs,
  with a document manifest, explicit encoding/version rules and no raw-Markdown or self-hash cycle.
- [x] **Enforceable soft2d floor.** `c` owns the headless four-scene benchmark, fixed 16.7/66.7 ms
  ceilings and frozen budgets. `d` reuses those fixtures and requires the same benchmark gate;
  `c` does not depend on the live application or WSI to measure its performance.
