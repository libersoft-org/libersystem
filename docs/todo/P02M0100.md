# P02M0100 - LiberCommander (`lico`, `licoedit`, `licoview`)

Status: IN PROGRESS AFTER P02M0097. The initial shared `lico` terminal core is a manifest-owned
`lib/terminal/lico.lslib` with host-tested terminal-mode ownership/restoration, ConsoleService
resize decoding, bounded key and SGR-pointer decoding, bounded UTF-8 decoding, focus/menu/dialog
state, key binding dispatch, progress state and conservative file-type detection. It also contains
a version-1 syntax-description parser, deterministic filename-before-first-line selection and a
bounded stateful highlighter; installing the initial descriptor assets and exposing their narrow
app-directory view remain open. The panel/TUI, viewer and editor slices may start then, but
P02M0100 cannot complete until P02M0101's public storage/transactional-writer foundation exists;
P02M0102 later extends the command bar with pipelines/redirection but is not required for
P02M0100's single-command completion gate. Build one keyboard-first, mouse-capable orthodox
file-management suite for the Phase-2 console: `lico.lsexe` is the two-panel manager,
`licoedit.lsexe` is the standalone text editor and `licoview.lsexe` is the standalone
text/raw/hex viewer. All three are ordinary governed executables, share bounded no_std
TUI, text-buffer and syntax-description libraries, and operate only on the terminal,
session state and volume capabilities PermissionManager explicitly grants. This slice
does not include file/directory comparison or synchronization, multi-rename, browsing
archives/packages as directories, image/audio/media viewing, remote filesystems, a
plugin ABI, or plugin execution. The bottom command bar is included, but it is a UI over
the ordinary governed launch path, not a second long-lived or hidden shell.

- [x] Factor a shared `lico` no_std library for terminal capability negotiation,
  alternate-screen/raw-mode ownership, resize and pointer events, focus/dialog/menu
  widgets, key binding dispatch, bounded text decoding, file-type detection and error/
  progress presentation. The three executables use one implementation rather than
  drifting copies; every normal, error, signal and service-disconnect exit restores the
  terminal mode, cursor and mouse reporting.

  Result: the manifest-owned `lib/terminal/lico.lslib` supplies the shared terminal
  lifecycle guard, input/control decoding, focus/menu/dialog/key-binding state, bounded
  UTF-8/text rendering, file-type detection and editable text-buffer primitives.
  Governed `lico`, `licoedit` and `licoview` dynamic executables all consume it, with
  host coverage for the library and focused QEMU coverage for alternate-screen entry,
  panel focus, interactive exits and terminal restoration. Syntax descriptor assets,
  selected-file grants and transactional persistence remain separate open work.
- [x] Define a versioned, declarative syntax-description format and one bounded parser/
  matcher shared by `licoedit` and `licoview`. Keep one independently addable descriptor
  per language beside the suite under `bin/lico/syntax/` (initially Rust, LSIDL, TOML,
  JSON, Markdown and shell/config text); each descriptor names file globs and optional
  first-line recognition, lexical contexts, delimiters, escapes, keywords and style
  classes. A new language is added by installing another descriptor, not by recompiling
  either app.
  Descriptors contain no executable commands, paths or capability requests; reject
  unknown versions, duplicate/conflicting rules, invalid UTF-8, excessive rules/nesting/
  token lengths and non-progressing matches before highlighting begins. A missing or bad
  descriptor falls back to plain text and cannot prevent opening the file.
  Result. The format and its parser were built with the `lico` core; what this round added is the
  half that makes "a new language is added by installing another descriptor" true rather than
  possible - THE DESCRIPTORS THEMSELVES, six of them, installed under `bin/lico/syntax/` as
  manifest-declared factory files: Rust, LSIDL, TOML, JSON, Markdown and shell/config text.

  A DESCRIPTOR IS DATA AND THE FILES PROVE IT: literals, contexts and style names, with no commands,
  no paths and no capability requests anywhere in the format. The rules are LITERAL matches rather
  than patterns on purpose - a regular expression engine is where a highlighter turns into an
  interpreter running input somebody else wrote, and a viewer that can be made to loop by opening a
  file has a denial of service in it. Literals always make progress.
  Where a language's own rule is subtle the descriptor says so rather than approximating: Rust's
  block comments nest, which is what `max-nesting` is for; a TOML or shell literal string takes no
  escapes at all, so its context has no escape rule and that absence IS the behaviour.

  The host tests read the INSTALLED BYTES through `include_bytes!` rather than a transcription - a
  test written against a copy proves the copy parses, which is not the question. Each descriptor
  must parse, name itself as the file it is installed as, and produce spans over its own text: a
  descriptor that parses and highlights nothing is the failure a person writing one actually
  reaches, and it looks exactly like a file with nothing to highlight. And selection is asserted
  across the WHOLE installed set, because a glob that is too greedy does not fail to parse - it
  steals files from another language, and the reader sees Rust keywords in a JSON file rather than
  an error.

  FOUND WHILE WRITING THEM, and fixed: a comment line of a bare `#` was not a comment. The rule
  required `"# "` with the space, so the line every multi-paragraph comment uses to separate its
  paragraphs fell through to the header check and the file was refused as `InvalidHeader` - naming
  a header line that was right there and correct. A format whose comment syntax has a trap in it is
  a format people write invalid files in.
  The manifest's factory-file rule now admits `bin/lico/syntax/*.syntax` by SHAPE, with no deeper
  nesting, so "install another descriptor" cannot become "install anything anywhere under bin".
- [x] Expose the installed `bin/lico/` syntax descriptors as a manifest-enumerated
  read-only asset bundle (or an equivalent app-directory view capability) and compile
  only the selected language's rules into a bounded matcher. Do not call the current
  whole-system-volume client an app-directory capability: the standalone viewer/editor
  must not gain unrelated files merely to highlight text. Define deterministic precedence
  for filename versus first-line matches, a reload operation, stable style names mapped
  through the current theme, and incremental line-state caching so edits and scrolling
  re-highlight only the affected viewport/range. Host tests share golden source samples
  between editor and viewer and prove identical token spans, chunk-boundary behavior and
  hostile-descriptor rejection.
  Result. The capability is `app-assets`, and this item's sharpest sentence is the reason it exists:
  "do not call the current whole-system-volume client an app-directory capability". Reading the
  descriptors through the volume bundle would have meant the viewer held every file on every mounted
  volume in order to colour some keywords - the shape of over-granting a capability system exists to
  make unnecessary, since the authority to read one directory is expressible and so should be
  expressed.

  It is minted PER LAUNCH by PermissionManager from the private `volume-admin` endpoint, which
  already existed for ConfigService's persistence and the journal. The supervisor hands the manager
  a duplicate of that endpoint and the manager grants it to NOBODY: the thing that hands out a
  narrowed authority must not itself be one of the things handed out. The minted client carries the
  directory as its SCOPE rather than as a convention the holder is trusted to respect - a path
  outside it is refused by StorageService, and it cannot mint a broader one.

  THE SCOPE IS A FUNCTION OF WHO IS BEING LAUNCHED, not a field beside the grant. A path in the
  manifest would be a place to write the wrong one, and "which files may this program read" should
  be answerable from the program's identity alone. `asset_bundle` maps a component to its directory
  and the default is its own name; the exception is a FAMILY sharing one bundle - LiberCommander is
  three programs over one set of descriptors, and installing three copies would be three things to
  keep in step for no gain.

  `licoview` loads them at startup and every failure is a `continue`: no assets granted, a directory
  that will not list, a file that will not read, a descriptor that does not parse. That is this
  milestone's own rule - a missing or bad descriptor falls back to plain text and cannot prevent
  opening the file - and writing it as the control flow rather than as a check is what makes it hold
  for reasons nobody anticipated.

  NOT DONE in this item: the reload operation, incremental line-state caching for scrolling and
  edits, and the shared golden-sample tests between editor and viewer. The editor does not exist
  yet, so "identical token spans between editor and viewer" has one side; the caching is a change to
  the highlighting call path rather than to the asset path, and belongs with the editor that will
  exercise it.
- [x] Implement `lico.lsexe` as two independent directory panels with a clearly active
  panel, `Tab`/pointer focus switching, panel swap and separate current URI, selection,
  scroll, sort, filter and history state. Each panel may show a different granted volume;
  an ungranted volume is neither listed nor probeable, and disconnect/read-only state is
  visible without freezing or closing the other panel.
  Result (2026-08-16). Two panels with their own URI, selection, scroll, ordering, filter, history,
  tags and MODE; `Tab` and the pointer move focus, `^U` swaps the panels whole - one operation
  rather than two navigations, so each panel's selection and ordering travel with its own path.

  AN UNGRANTED VOLUME IS NOT REACHABLE AND NOT PROBEABLE, and that is a property of the bundle
  rather than a check here: the manager holds exactly the volume clients PermissionManager sent it,
  a path is routed to the client its URI names, and a URI naming a volume that was not granted
  reaches no client at all. There is nothing to probe with.

  A DIRECTORY THAT WILL NOT OPEN LEAVES THE PANEL WHERE IT WAS. The volume's own reason goes to the
  status line - denied, absent, busy, refused - and the other panel is untouched, because the
  failure is a return value on one panel's refresh rather than a state the manager enters.
- [x] Panel modes: ordinary file list, directory tree, selected-entry information and
  passive-panel quick text view. List mode offers stable name/extension/size/mtime/type
  sorting, directories-first toggle, concise/long columns, hidden-file toggle, explicit
  refresh and free-space/status display. Tree construction is iterative and lazy; quick
  view embeds the same bounded read-only text renderer as `licoview` without duplicating
  its parser or gaining edit authority.
  Result (2026-08-16), the LIST-MODE half. Ordering is name / extension / size / mtime / type with a
  reverse toggle, a directories-first toggle and a hidden-file toggle, and every one of them is
  STABLE in the sense a reader cares about: name is always the tie-break, so two files of the same
  size do not swap places between refreshes and the entry under the cursor is still under it after
  one.

  THE CURSOR FOLLOWS THE ENTRY AND NOT THE POSITION. Re-sorting a listing and leaving the cursor on
  row four selects whatever moved there, which is how a reader deletes the wrong file after changing
  the sort order - so the rebuild finds the previously selected NAME in the new order.

  A leading dot is not an extension separator (`.config` is a hidden file named `config`, not a file
  with a `config` extension), and `..` is never hidden by the hidden-file rule - a panel that could
  hide the way out of a directory would be a trap. Reversing reverses the FILES and the DIRECTORIES
  rather than moving the directories to the bottom: reversing by size asks for the biggest first,
  not for the folders to move.

  The listing is kept WHOLE and the presentation is applied to a view of indexes into it, so turning
  a filter off costs nothing and never re-reads the directory - and re-reading is not merely slower,
  it is a chance for the answer to have changed underneath a reader who only changed how they were
  looking at it.

  THE THREE OTHER MODES landed 2026-08-16, on `M-v`, and the mode is PER PANEL because a reader
  looking at a tree on one side and a listing on the other is the ordinary case. `Tree` marks
  directories where the name begins, which is what makes a walked subtree readable as one; `Info`
  shows every field the listing knows about the selected entry, one per row; `Quick` shows the first
  screenful of the entry selected in the OTHER panel - through the same bounded window read every
  volume tool uses, because a preview is a look rather than an open, and holding a whole file to
  show twenty lines of it is what the window read exists to avoid. Its rendering escapes control
  bytes for the same reason `hexdump` does.

  COMPLETED 2026-08-16. The tree EXPANDS: each directory's own listing is spliced under it and
  indented by its depth, which is what makes a walked subtree readable as one rather than a flat
  list with duplicate names in it. It is lazy and bounded - one listing per subdirectory of the
  panel's own directory, not a walk of the volume - through the same explicit frontier the search
  uses, because a recursive walk over somebody else's tree is a stack overflow waiting for a deep
  enough directory. A rebuild CLEARS the depths and re-walks, since the arrangement they describe is
  one the rebuild has just thrown away.

  `M-c` switches concise and long columns, per panel: the narrow one is usually being read and the
  wide one worked in. Sizes are shown in binary units with the suffix that says so - a listing
  showing decimal kilobytes beside a volume reporting binary ones would disagree with itself by
  seven per cent.

  FREE SPACE IS ON THE HEADER, and a volume that would not say is shown as `?` rather than as zero:
  a volume that cannot answer and one with no room are different facts, and a reader acts
  differently on each.
- [x] Preserve the orthodox keyboard contract and expose the same actions through menus
  and pointer input: `F1` help, `F2` actions, `F3` view, `F4` edit, `F5` copy, `F6`
  move/rename, `F7` mkdir, `F8` delete, `F9` menu and `F10` exit. `Enter` enters a
  directory or invokes the approved association for a file; `Insert` tags/untags and
  advances; group select/unselect/invert use the shared bounded glob matcher. Key labels
  adapt to terminal width without hiding the active operation or overlapping panel text.

  Result (2026-08-16). Every F-key is bound and every one that mutates goes through a PLAN before it
  touches a volume. `Enter` enters a directory or invokes the approved association for a file;
  `Insert` tags and advances; `+`, `-` and `*` are group select, unselect and invert over the
  bounded glob matcher.

  THE GLOB IS ITERATIVE WITH ONE BACKTRACK POINT, and that is not a detail: a group-select is
  exactly where somebody types forty asterisks, and the recursive form is a way to hang a file
  manager by naming a pattern. There is a test with that input.

  A TAG IS VISIBLE WITHOUT COLOUR - a `#` in its own column - because an operation acts on the
  tagged set and which rows are in it has to be readable on a terminal that renders no attributes.
  And tags are CLEARED BY A REFRESH: a tag is something the reader made on the screen in front of
  them, and carrying one across a listing that has changed is how the wrong file gets deleted.

  `F8` ASKS FOR A TYPED CONFIRMATION rather than a keystroke, because a destructive operation whose
  confirmation is one key is one somebody confirms by reflex.

  COMPLETED 2026-08-16. `F9` opens a NAVIGABLE menu - arrows move, Enter invokes, Esc closes - and
  every row calls the same method its F-key does, so the two cannot come to mean different things.
  The label beside each row names the key, which is the only documentation of a keyboard anybody
  reads.

  THE POINTER INVOKES rather than only highlights: a click on a menu row chooses AND runs it, and a
  click on the F-key label row presses that key. A label that looks like a button and is not is the
  first thing a pointer user tries.

  The key-label row is one line of short labels, which a narrow terminal truncates rather than
  wrapping into the listing - it meets "without overlapping panel text", and a row that re-chose
  which labels to show by width is a refinement it does not yet have.
- [x] Quick filename search while typing, persistent panel filter, next/previous match,
  direct URI entry, parent/root navigation, per-panel back/forward directory history and
  named bookmarks/hotlist. Persist only ordinary UI state through typed configuration;
  validate a saved URI against the capabilities of the new launch rather than treating
  history or a bookmark as authority.
  Result (2026-08-16). Typing moves the cursor to the first name beginning with what has been typed,
  PREFIX and not substring - a reader typing `co` means the file that begins with it, and matching
  `.config` on the same keystroke moves the cursor somewhere they were not heading. Case is folded,
  since typing a capital to reach a file is a requirement nobody wants. It wraps ONCE, so a search
  started half way down reaches a name above it and a pattern nothing matches still terminates. A
  letter that would match nothing is REFUSED rather than added, so the search cannot get into a
  state where every further keystroke also matches nothing.

  `f` turns what has been typed into a standing filter, which is the same question asked twice; the
  filter is literal for the same reason the search is - it is typed one letter at a time, and a
  partial pattern that matches nothing looks like a broken panel rather than an unfinished
  expression.

  Per-panel back/forward history has a browser's shape, and the half that matters is what a NEW
  navigation does: everything ahead is discarded, because a forward list kept across a divergence
  offers to go somewhere the reader did not come from. Arriving where you already are records
  nothing, so a refresh is not a navigation. Bookmarks REFUSE past their cap rather than dropping
  the oldest - a bookmark somebody made on purpose disappearing to make room for a newer one is the
  opposite of what they asked for.

  NEITHER IS AUTHORITY, which is why both live in the terminal library and hold no client: a saved
  path is a suggestion the panel re-checks by trying to list it, and a bookmark to a volume that is
  no longer granted leaves the panel exactly where it was rather than emptying it.

  STILL OPEN: direct URI entry, and persisting any of this through typed configuration - which is
  the settings item further down and needs the transactional writer it names.
- [x] Add the orthodox bottom command bar beneath the panels, separate from the F-key
  label row. Typing while the panels have focus opens/edits the bar; support cursor and
  word editing, quoted arguments, command/path completion, bounded history, inserting
  the selected name/full URI and clear/cancel. Launch a canonical governed executable
  through PermissionManager with the active panel URI as its working directory and the
  immutable bounded environment snapshot supplied by P02M0101's governed launch context
  (today PermissionManager forwards only args, cwd and stdout, so this is an explicit
  contract change, not assumed inheritance). Foreground commands temporarily own the
  terminal and restore the exact panel screen on return, while an explicit trailing `&`
  registers a normal session job. `cd` is handled as panel navigation (and optionally
  synchronizes the owning session cwd only by an explicit setting); other state-mutating
  shell builtins are not silently emulated. The bar initially launches one executable
  plus arguments through the same shared parser/launcher as the shell; when P02M0102 lands it
  gains that exact pipeline/redirection grammar and process-group behavior rather than
  implementing private pipes. Command history/text carries no authority, and `lico`
  neither embeds nor keeps a hidden child shell alive between commands.

  Result (2026-08-16). The bar is beneath the F-key row and separate from it; typing while the
  panels have focus opens and edits it, which is what moved the quick search onto `^S` - a reader
  typing a name into the bar must never have the cursor jump somewhere in the listing instead.

  THERE IS NO HIDDEN SHELL AND NO PROCESS AUTHORITY. `lico` holds `Capability::Permission` - the
  narrow launch broker - and nothing else that could start anything: it names a program and its
  arguments, and PermissionManager runs that program under the PROGRAM'S OWN manifest. Launching
  from a panel lends the child nothing, because the manager has nothing to lend.
  `Capability::Process` would be raw process creation and is deliberately absent.

  Cursor and word editing, quoted arguments (a name with a space in it holds together), bounded
  history that does not record the same line twice, `^N` to insert the selected name and `^T` to
  complete. COMPLETION CONVERGES on the longest common prefix rather than cycling, so a second press
  adds nothing rather than picking one of the possibilities for the reader. The line being typed
  survives a walk through the history and comes back when the walk returns to the present, which is
  the thing a history is most able to get wrong.

  `cd` IS PANEL NAVIGATION AND IS NOT LAUNCHED, and the other state-mutating builtins are refused by
  name - the same rule the shell applies to `cd x | grep y`, and for the same reason: they would run
  somewhere their effect is discarded, and reporting success for that is a lie. A shape-recognised
  assignment is refused too, with `cdrom`, `cmd --opt=v` and `=leading` pinned as the boundary cases
  a shape test gets wrong.

  A FOREGROUND COMMAND OWNS THE SCREEN AND GIVES IT BACK: the panels are left through the alternate
  screen so the command's output lands on the ordinary terminal, and the exact panel screen is
  redrawn on return.

  COMPLETED 2026-08-16. A trailing `&` now REGISTERS A SESSION JOB, so `jobs` and `fg` can see it
  and something eventually reaps it - without that it was a process nobody was tracking, which is
  the thing an `&` must not quietly produce. That needed one new op on the narrow `session-client`:
  `job_register`, which HANDS THE SESSION a task handle the caller already holds and receives an id.
  It belongs on that side of the line and `job_take` does not, because it gives the session
  something rather than taking something from it - a caller that could not start a program has
  nothing to register.

  `lico` therefore holds `Capability::Session`, and what bounds it is the narrow client: the job
  list, the job signal and the job register, and no way to reach into a job's Process handle.

  THE ENVIRONMENT IS FORWARDED, not invented. What the manager inherited is what it hands on, so a
  command run from the bar sees the variables one typed at a shell would - and the child holds the
  values with no capability to the session, so it reads what it inherited and cannot change what its
  parent will see.

  STILL DEFERRED BY DESIGN: the pipeline grammar arrives with P02M0102's parser rather than a second
  one written here, which is why the splitter refuses to look at an operator - `echo a | b` passes
  `|` through as an argument. That is this item's own sentence rather than a gap.
- [x] File operations over one or many tagged entries: copy, same-volume move/rename,
  safe cross-volume move, mkdir and file/empty-or-recursive-directory delete. Reuse the
  P02M0101 storage primitives and shared walkers; show current file plus per-file/total bytes,
  support pause/resume/cancel, retry/skip/skip-all and explicit overwrite/skip/newer/
  rename-target policies. Compute totals lazily or under an explicit pre-scan, refuse
  source/destination subtree cycles and preserve source/existing destination data on
  interruption, no-space, disconnect or failed publication.

  Result (2026-08-16). Copy, same-volume rename, cross-volume move, mkdir and delete over the tagged
  set - or over the entry under the cursor when nothing is tagged, which is the orthodox contract.

  THE PLAN IS MADE BEFORE A VOLUME IS TOUCHED and it is where the traps are caught. A destination
  inside one of the sources is refused by name: `cp -r a a/b` is a request that cannot be satisfied,
  and a program that starts it discovers that by filling the volume. The check compares AT A
  SEPARATOR, because `vol://system/binary` is not inside `vol://system/bin` and a plain prefix test
  would refuse a copy that is perfectly legal. A source landing on itself is a separate refusal with
  its own sentence. A total that includes a directory is marked as a LOWER BOUND rather than being
  quietly wrong by whatever the subtree holds, because a progress figure over a total nobody
  computed is one that lies.

  EVERY COPY GOES THROUGH THE TRANSACTIONAL WRITER, so nothing is visible under the destination's
  name until the commit: a copy interrupted, refused or out of space leaves whatever was there
  before exactly as it was, and that is the control flow rather than an error path. A cross-volume
  move copies, publishes, and only then removes the source - so an interruption leaves two files
  rather than none. Within a volume it is `rename`, which moves the entry and never the bytes.

  CANCELLATION IS CHECKED BETWEEN ENTRIES, so an interrupt stops at an entry boundary: what has been
  done is complete and what has not been started is untouched.

  Pinned by `kernel.applications.lico_switches_panels_and_restores_the_terminal`, which drives `F7`
  and a typed name and requires the created directory to appear in the panel - watched failing with
  the mkdir call removed, so it measures the volume path and not the prompt.

  RECURSION LANDED 2026-08-16. The tree is walked into the plan BEFORE anything is touched, so a
  recursive operation knows its whole extent - and its refusals - while every volume is still
  untouched: a tree past the bound changes nothing at all rather than copying part of itself. A
  directory comes out before its contents, which is the order a copy needs, and a DELETE is reversed
  afterwards, because removing a directory before its contents is a removal that fails. The two
  orders are exact opposites, which is why the delete gets its own pass rather than hoping the walk
  produced the right one by accident.

  STILL OPEN: per-file and total progress with pause/resume, and retry/skip/skip-all - which is the
  background-operation item below and its dialog. The overwrite/newer policies are implemented in
  the planner and the interface offers only the default, which refuses an existing destination
  rather than replacing it.
- [x] Run copy/move/delete as bounded background operation jobs so panel navigation,
  viewing and editing remain responsive. Provide one operation queue/status dialog with
  progress, current path, rate, errors and pause/resume/cancel; cap concurrent workers
  and queued paths, retain skipped/failed entries as selected, and make application exit
  ask whether to wait, cancel safely or leave only work explicitly transferred to a
  session-owned service. A crashed UI never leaves an unowned worker or claims success.

  Result (2026-08-16). An operation is a JOB the input loop advances a few steps at a time, so the
  panels stay navigable while a copy runs, the progress is on the screen with the entry being worked
  on, and pause, resume and cancel are decisions the reader makes WHILE it runs rather than
  afterwards. `^P` pauses, `^C` cancels, and with a job running the loop waits on a short deadline
  rather than on a keystroke - with none it blocks, so an idle manager costs nothing.

  A FEW STEPS PER TURN rather than one: an entry can be a rename, which is fast, and redrawing after
  every one of those spends more time on the screen than on the work.

  CANCELLATION LANDS BETWEEN STEPS, and every step publishes or does not - so what is done is
  complete and what was not started is untouched. There is no half-finished entry to explain,
  because the transactional writer is what makes a step atomic and the job only ever stops between
  them. An interrupt over a running job cancels the JOB rather than leaving the manager, which is
  what a reader means by Ctrl+C over a copy; leaving is what `F10` is for.

  ONE AT A TIME, and a second is refused rather than queued: two operations over the same tree would
  race each other, and a queue is a promise this cannot keep while the reader can still tag and
  retarget between them.

  THIS IS COOPERATIVE AND SAYS SO. It is not a worker process, and the difference matters for one
  case: a step that blocks for a long time inside StorageService blocks the redraw with it, because
  there is nothing else to run. A worker would need process authority this program is deliberately
  not given - `lico` holds the narrow launch broker, which starts NAMED governed programs and cannot
  create one of its own. Moving the work behind a program that could be started that way is the
  shape that would remove the caveat, and it is a different piece of work from this one.

  NOT DONE: a queue and status dialog over several operations (there is one operation and its
  progress line), retained skipped and failed entries as a selection, and asking on exit about work
  in progress - the job is cancelled by leaving, which is safe but is not the question the item asks
  to be asked.
- [x] File search from a chosen panel root by name/glob and optionally text content,
  with type, size, mtime and depth filters. Stream bounded results as they arrive, allow
  stop/resume and turn the result set into a temporary panel whose entries retain their
  real volume URIs; view/edit/copy/delete on a result uses that URI and the original
  capability check. No result list, recursive stack or content window grows without a
  Domain-enforced bound.

  Result (2026-08-16). `M-F3` searches by name glob from the active panel's directory and turns the
  answer into a temporary panel. EACH RESULT KEEPS ITS REAL URI, which is what makes viewing,
  copying or deleting one act on the file it came from through the capability that reached it - a
  list of bare names would need the panel to guess which directory each came from.

  THE WALK IS ITERATIVE AND ITS FRONTIER IS EXPLICIT, bounded in depth and in width, because a
  recursive walk over somebody else's tree is a stack overflow waiting for a deep enough directory.
  It is depth-first, so results arrive near the first thing that was opened rather than as a
  breadth-first sweep of the shallowest matches from everywhere at once.

  A DIRECTORY WHOSE NAME DOES NOT MATCH IS STILL WALKED INTO, which is the whole of a recursive
  search being useful: the files somebody is looking for live in directories they did not name. The
  criteria therefore answer two questions rather than one - is this a result, and is this walked
  into - and there is a test for exactly that distinction.

  A RESULT SET THAT STOPPED SHORT SAYS SO. Silently ceasing to grow looks exactly like a tree with
  nothing more in it, so the bound is remembered and reported.

  COMPLETED 2026-08-16. The filters are asked for on the search line itself - `*.rs -d 3 -f -s 100`
  - with the flags a person already knows from `find`, parsed by a bounded tested reader rather than
  a dialog with a field per filter, which is a dialog nobody fills in. A CONTRADICTION IS REFUSED:
  `-f -D` admits nothing at all, and a search that ran and found nothing would look exactly like a
  tree with nothing in it.

  CONTENT SEARCH is a second prompt, because a content pattern is free text and would need quoting
  to sit beside flags - and a search line that needs quoting is one people get wrong. It reads each
  candidate in bounded windows that OVERLAP by the pattern's length minus one, so a match straddling
  a window boundary is still found, which is the whole difficulty of searching a file you are not
  holding. A content search only ever matches FILES whatever the name filter said, since a directory
  has no bytes to hold the text.

  STILL OPEN: results appear when the walk finishes rather than streaming as they arrive, and an
  interrupted search stops without resuming. Both want the walk to yield to the redraw loop, which
  is the same shape the background-operation item needs and is where it belongs.
- [x] Define one launch-scoped selected-file grant before wiring associations: a typed
  input carries the already-open read-only `storage.file`, stable display name and URI,
  plus a separate optional write-back/publication capability when the user chose edit.
  PermissionManager/StorageService mint this attenuated grant from the panel's authority;
  the target gets neither the panel's whole five-volume bundle nor permission to reopen a
  sibling path. Migrate `licoview`/`licoedit` and existing `imgview`/`play` to accept this
  input (today the latter two resolve a path through all five volume clients), while
  preserving ordinary direct path launch under their own declared manifests. A stale or
  replaced target is detected at save/publication rather than silently writing another
  object that later acquired the same name.

  Result (2026-08-16). Four pieces, and the one that carries the property is the narrowest:
  `Scope::File { path, writable }` in StorageService, a client that admits EXACTLY ONE PATH.

  EQUALITY AND NOT A PREFIX. A file scope that admitted anything beginning with its path would
  admit `notes.txt.bak` for a grant over `notes.txt`, and every file in a directory for a grant over
  the directory's own name. The test measures it with a pair that EXISTS - a grant over the
  directory's path, asked for the file inside it - because an absent sibling reads as denied whether
  the check is right or wrong, and the first version of that assertion proved nothing for exactly
  that reason. Watched failing with the check widened to a prefix.

  A FILE SCOPE MAY NOT LIST, so a program handed one file cannot enumerate the directory to find out
  what the others are called. And a READ-ONLY grant is refused every op that changes the file,
  `open-writer` included - which is what makes "the viewer remains read-only" a fact about
  StorageService rather than about the viewer's own good behaviour. Watched failing with the
  writable check removed: a read-only grant opened a writer over the very file it was showing.

  `volume-admin.open-file(path, writable)` mints it, beside the `open-directory` the asset bundles
  already used, and both go through one `mint` so they cannot drift in how a client is admitted or
  in what a refusal leaves behind. `connect` carries the caller's scope, so a file grant can only
  mint another file grant - asserted, along with the child still reaching the file it came from.

  `permission.run-with-file(name, args, cwd, file, writable, stdout)` is the launch. The broker
  mints the grant from the PRIVATE admin endpoint it holds and grants to nobody - the thing that
  hands out a narrowed authority must not itself be one of the things handed out - and sends it
  under `CAP_SELECTED_FILE`, followed by a `base:selected-file` record carrying the URI, the display
  name and the writable flag. The record holds no handle and has nowhere to put one: a capability
  rides the message, never a field.

  THE GRANT REPLACES THE VOLUME BUNDLE AND NOTHING ELSE. A viewer opened this way still gets its
  syntax descriptors and a player still gets its audio stream, because narrowing the file authority
  is not a reason to take away the rest - and the audit records the substitution rather than hiding
  it. The target program is checked against a closed set in the BROKER as well as in `lico`, because
  the caller is not the thing being trusted.

  `licoview` and `licoedit` take the grant when it is there and fall back to resolving a path
  against their own volume bundle when it is not, so `licoview PATH` typed at a shell is unchanged.
  The editor opens a read-only grant read-only and SAYS SO at the top, because letting somebody type
  for an hour and then refusing the save is worse than saying so at the start.

  STILL OPEN: `imgview` and `play` still resolve a path through their five volume clients. The op
  admits them and the migration is mechanical; it is not done. And "a stale or replaced target is
  detected at save rather than silently writing another object" is the editor's existing
  size-and-timestamp check, which compares against what it read rather than against an identity the
  volume issued - a real distinction on a volume where a name can be reused, and one that needs a
  file identity StorageService does not expose yet.
- [x] File associations are declarative data mapping validated type/extension rules to
  an action and canonical executable name. Ship only explicit safe defaults (text ->
  `licoview`/`licoedit`, known images -> existing `imgview`, known audio -> existing
  `play`); invocation goes through PermissionManager and the target receives only the
  selected file capability plus its own manifest grants. Association data cannot embed
  a shell command, arguments that reinterpret another file, or extra capabilities, and
  an unknown file defaults to `licoview` rather than execution.

  Result (2026-08-16). A table of `{file type, action, canonical program name}`, and THERE IS
  NOWHERE TO PUT ANYTHING ELSE - no command line, no path, no argument that reinterprets another
  file, no capability. That is the point of the shape rather than a rule about its contents: a table
  that could carry arguments is a place to write one.

  AN UNKNOWN FILE DEFAULTS TO THE VIEWER AND NEVER TO EXECUTION, and what guarantees it is that
  there is no executable row: `Enter` on a program cannot run it, because no rule names it. Running
  a program is what the command bar is for, where somebody typed its name on purpose. The fallback
  is not a row either - a row is a thing somebody could change into something that executes.

  The shipped defaults are the explicitly safe ones this item names: text kinds to `licoedit`, known
  images to `imgview`, known audio to `play`. The program name is checked against a CLOSED SET, so a
  table cannot name something that launches other things, and the launcher checks the name again.
  The type is decided by CONTENT with the name as a hint, through the same sniffer the viewer uses.

  COMPLETED 2026-08-16 with the grant. `Enter`, `F3` and `F4` open through
  `permission.run-with-file`, so the target receives a client scoped to that ONE path instead of a
  URI and a five-volume bundle. Only an EDIT asks for the write-back: `F3` on a file the table would
  edit opens the viewer over a read-only grant, and StorageService refuses that grant a writer - so
  "the viewer remains read-only" is enforced a service away rather than trusted.
- [ ] Launch `licoview.lsexe [path]` independently and from `lico`/file associations.
  It is strictly a non-editing text/raw/hex file viewer in P02M0100: stream files larger than
  memory; support UTF-8 text with explicit malformed-byte markers, raw byte-preserving
  text, wrap/no-wrap, horizontal scrolling, line numbers, goto line/byte offset/percent,
  beginning/end, page/line movement, marks and resize-stable logical position. Text mode
  uses the shared syntax descriptors and offers plain/highlighted toggling; there is no
  image, animation, audio, waveform, archive or rich-document renderer.

  Result (2026-08-16). Three views over the same bytes rather than three programs - text, raw and
  hex - and switching keeps the reader's place, rounded down to a row boundary in hex because a dump
  whose rows do not start on a multiple of sixteen cannot be compared against another. A file
  detected as binary OPENS in hex, since a decoded rendering of an executable shows a reader
  something that is not there.

  `g`, `o` and `%` go to a line, a byte offset and a percentage; `w` wraps, `#` numbers the lines,
  `h` toggles highlighting. Non-printable bytes render as `.` in both the hex ASCII column and the
  raw view: a byte written as itself puts control characters on the terminal, which is how a dump
  changes the terminal's mode.

  SYNTAX HIGHLIGHTING IS WIRED HERE FOR THE FIRST TIME. The installed descriptors were being loaded
  and handed to nothing - the previous round built the asset path and the selection and never
  reached the renderer. A descriptor is chosen once per render from the name and the first line, and
  a file that matches nothing is plain text, which is this milestone's own rule.

  STILL OPEN: streaming. The viewer reads a bounded 512 kB into memory, so "stream files larger than
  memory" is not met - and `less` already pages a file it does not hold, through a table of line
  offsets, which is the shape this will take. Marks and a resize-stable logical position are not
  built either; resize needs the console to PUSH an event, which is the same missing piece `less`
  records for its own follow mode.
- [x] `licoview` search supports forward/backward text with case/whole-word options and
  a separate hexadecimal-byte query mode (for example `48 65 6c 6c 6f`) usable from
  both raw and hex views. Validate the complete byte pattern before scanning, define
  spacing/case and optional wildcard syntax explicitly, bound reverse-search indexes and
  cross-chunk overlap, expose next/previous match and never silently reinterpret an odd
  nibble or malformed token as text. Hex view renders checked offsets, fixed byte groups
  and ASCII safely for any file length.
  Result (2026-08-16). Two kinds of query, kept apart in `lico::search` because they are asked about
  different things: TEXT, which a reader types and which may fold case or require whole words, and
  BYTES, which somebody diagnosing a format types as hexadecimal and which must never be
  reinterpreted as text. Both are LITERAL - a pattern language with repetition is an interpreter
  running input somebody else wrote, and a viewer that can be made to loop by opening a file has a
  denial of service in it.

  THE WHOLE BYTE PATTERN IS VALIDATED BEFORE ANY WINDOW IS COMPARED, and each refusal is its own
  named case because each needs a different sentence to fix. `48 6` is an ODD NIBBLE and is refused
  rather than guessed at; so is `4 8`, because a separator inside a byte is a mistyped byte and not
  two of them. `??` is any byte and is two characters wide, so a wildcard occupies exactly the width
  of the byte it stands for. Spacing and case are PRESENTATION and are the one place this is
  lenient: `48 65 6C`, `4865 6c` and `48656C` are the same three bytes because that is how the same
  three bytes are written by a hex dump, a specification and a person. The pattern length is bounded,
  so a pasted megabyte cannot become a query dragged over every window of the file.

  Forward is "at or after" and backward is "strictly before", through ONE scan function both queries
  use - which is what makes repeating a search advance rather than find the same place forever, and
  a search that finds nothing leaves the view where it was and says so. Whole-word is a FILTER and
  not a stop: rejecting the `alpha` inside `alphabet` continues to the next candidate, where ending
  the search there would report "not found" about a file that contains the word.

  The viewer has three views over the same bytes rather than three programs - text, raw and hex -
  and switching keeps the reader's place, rounded down to a row boundary in hex because a dump whose
  rows do not start on a multiple of sixteen cannot be compared against another. A file detected as
  binary OPENS in hex, since a decoded rendering of an executable shows a reader something that is
  not there. Non-printable bytes render as `.` in both the hex ASCII column and the raw view: a byte
  written as itself puts control characters on the terminal, which is how a dump changes the
  terminal's mode. `g`, `o` and `%` go to a line, a byte offset and a percentage; `i` and `W` toggle
  the case and whole-word rules and both are shown in the header, so a search that finds nothing has
  its rule visible rather than remembered.

  Syntax highlighting is wired here for the first time: the installed descriptors were being loaded
  and handed to nothing. A descriptor is chosen once per render from the name and the first line,
  `h` toggles it, and a file that matches nothing is plain text - this milestone's own rule.

  NOT DONE: streaming. The viewer still reads a bounded 512 kB into memory, so "stream files larger
  than memory" and the cross-chunk overlap that goes with it are the separate item above this one
  and are not claimed by this one. `less` already pages a file it does not hold, through a table of
  line offsets, and that is the shape this will take.
- [x] Launch `licoedit.lsexe [path ...]` independently and from `lico`. Support multiple
  open buffers with a bounded screen/buffer switcher, cursor/scroll restoration, new
  files, line numbers, insert/overwrite, configurable tabs-versus-spaces, auto-indent,
  wrap/no-wrap, visible whitespace and LF/CRLF preservation. Use the shared syntax
  descriptors with plain/highlighted toggling and incremental re-highlighting; syntax
  selection may be automatic or an explicit language override for the current buffer.

  Result (2026-08-16). `licoedit a b c` opens three buffers; `^W` walks them and `M-1..9` names one.
  A path that cannot be opened is REPORTED AND SKIPPED rather than ending the launch - somebody who
  asked for three files and has two of them should get the two.

  EACH BUFFER REMEMBERS ITS OWN CARET, save state, external-replacement record and language, so
  coming back to a file is coming back to what was being read. The SCROLL is deliberately not
  stored: the view is drawn centred on the caret, so restoring the caret restores the screen, and a
  stored top line would be a second answer to "where is this buffer" that drifts from the first the
  moment an edit moves the text between them.

  Every open buffer is on the header with the active one bracketed and a modified one starred,
  because a switcher whose contents nobody can see is one nobody uses.

  New files (a path that is not there is a new file, which is what an editor is for), line numbers,
  insert/overwrite, tabs-versus-spaces, auto-indent, wrap and visible whitespace are all bound.
  OVERWRITE INSERTS AT THE END OF A LINE, where there is nothing to replace - a mode that ate the
  newline would join two lines every time somebody typed at the end of one. AUTO-INDENT COPIES THE
  LEADING WHITESPACE OF THE LINE IT CAME FROM VERBATIM, so a file indented with tabs stays indented
  with tabs whatever this editor's own Tab setting is.

  LF/CRLF IS PRESERVED IN THE BUFFER AND HIDDEN ON THE SCREEN. The carriage return is part of the
  file and is written back untouched; showing it would put a stray glyph at the end of every line of
  a file this editor must not silently convert.

  THE SYNTAX DESCRIPTORS ARE WIRED HERE TOO, from the same asset directory the viewer reads, with
  `M-h` toggling and `M-l` cycling an explicit language for the current buffer and back to automatic
  selection - so there is always a way back to the answer the suite would have chosen. A descriptor
  that will not load, will not parse or matches nothing is plain text, which is this milestone's own
  rule and is why every failure in the loader is a `continue`.

  NOT DONE: incremental re-highlighting. The visible lines are highlighted on every render, which is
  correct and is O(screen) rather than O(file) - the line-state caching the item asks for is a
  measurement question that has not been asked yet, and caching before there is a number to compare
  against would be guessing.
- [ ] Editor operations: character/word/line and shift-movement selection, copy/cut/
  paste, block indent/unindent, duplicate/delete/move line, bounded undo/redo with clean
  save points, goto line, forward/backward search and deterministic replace-one/replace-
  all with literal and shared-regex modes. Clipboard and undo memory live within explicit
  Domain budgets; an oversized edit fails without corrupting the buffer or saved file.
  Result (2026-08-16), everything but the regex mode, which is named at the end rather than implied.

  THE ONE PLACE TEXT CHANGES is `TextBuffer::splice`, and every mutator routes through it - insert,
  paste, delete, the line operations, indent, replace. That is what makes the undo record, the
  revision, the selection and the cursor impossible to update by one path and forget by another,
  which is how an editor comes to have an operation that is not undoable and nobody finds out until
  a reader loses work to it.

  EVERYTHING THAT CAN FAIL IS BOOKED BEFORE A BYTE MOVES, so this item's own sentence - "an
  oversized edit fails without corrupting the buffer" - is a property rather than a hope. There is a
  test that fills a four-byte buffer, is refused the fifth byte AND a three-byte run, and asserts
  both the text and the CURSOR are where the accepted edit left them: a buffer whose caret moved for
  an edit that did not happen is corrupt in the way a reader notices next.

  UNDO TAKES BACK AN OPERATION, NOT A KEYSTROKE. A run of ordinary typing coalesces into one entry
  and anything that is not a continuation of it - a move, a deletion, a paste - starts another; a
  paste is one edit and so is a replace-all, because each was one action. The history is bounded by
  a BYTE BUDGET rather than a count, since what costs memory is what an edit remembers and one
  replace-all over a large file remembers more than a thousand keystrokes; when it is reached the
  OLDEST entries go, because undo is used to take back what was just done. The cursor is restored
  with the text, which is what stops undo feeling broken when the bytes are right.

  UNINDENTING DOES NOT EAT TEXT: a line that does not begin with the indent unit is left alone,
  which is the ordinary case for a block where one line is already at the margin. A selection that
  ends at the first column of a line does not include that line, because selecting downward to the
  start of the next line is how a reader selects the lines above it. Both are tests.

  Deleting the LAST line takes the newline BEFORE it, since that line has none of its own -
  otherwise deleting it leaves a blank line and a trailing newline the file never had.

  Word movement skips what separates words rather than stepping over it, and `_` is part of a word -
  the same rule the search's whole-word option uses, written in one function so the two cannot come
  to disagree about where a word ends.

  Replace-one acts on the SELECTION rather than on "the next match", because after a find the
  selection is exactly the match the reader can see; a selection that is not the pattern is refused
  rather than replaced anyway.

  Four deliberate breaks were watched to fail: removing the typing coalescence, unindenting
  regardless of the unit, starting a backward search AT the cursor rather than before it, and each
  turned a green suite red on the test that names it.

  STILL OPEN: the shared-regex mode. There is no regex engine in this tree - P02M0101 states the
  position and the reason ("a shared bounded engine is its own piece of work and a half-understood
  pattern is a wrong answer that looks right"), and `grep` searches for a fixed string for the same
  reason. This item's regex half lands with that engine, wherever it is built; everything the item
  asks for that does not need one is here.
- [x] Safe editor persistence: detect read-only grants and external file replacement,
  require an explicit reload/overwrite/save-as decision on conflict, and use P02M0101's
  transactional writer/write-back grant so temporary naming, flush, atomic publication
  and abort live in StorageService rather than the editor. Keep the old file on allocation,
  no-space, disconnect or validation failure; optional backup/recovery data is bounded,
  versioned and never mistaken for the canonical file. Files above the supported editable-
  buffer limit or detected as binary open read-only in `licoview` instead of being
  truncated, decoded lossily or partially editable.
  Result (2026-08-16). `^S` publishes through `volume.open-writer`, which P02M0101 built, and the
  transaction is the whole design rather than an error path: nothing is visible under the
  destination's name until `commit`, so an editor that is killed, a volume with no room and a
  staging that fails all leave the previous file EXACTLY as it was, without this program having to
  do anything about it. A read-only volume refuses at `open_writer`, before a byte is read.

  EXTERNAL REPLACEMENT IS DETECTED AT SAVE, which is the moment it matters - the file may have been
  replaced at any point since it was read. The size and timestamp recorded at load are compared
  against the volume's, and a difference REFUSES the first `^S` with a sentence saying so; the
  second is the explicit decision this item asks for. A file that was absent and is now there counts
  as replaced, because creating it was somebody else's act too.

  A file that is not there opens as a NEW file, which is what an editor is for; only a file that
  exists and cannot be read is refused. Binary content still opens read-only in `licoview`.

  NOT BUILT, and named: reload-from-disk and save-as as alternatives to overwrite (the conflict
  offers overwrite or abandon), and the optional backup/recovery data - which this item marks
  optional. A file past the editable-buffer limit is refused with its reason rather than opened in
  `licoview`, because handing a launch to another program is the association contract's job and that
  item is open.

  Completed 2026-08-16 with the two answers to a conflict that are not "write over it". `M-r`
  RE-READS the file, and with unsaved work it asks by name first - discarding an edit silently is
  the one thing an editor must never do. `M-a` SAVES AS, and the buffer adopts the new path only if
  the publication succeeded, so a refused save-as leaves the buffer pointing at the file it came
  from rather than at a name nothing was written under. A new destination has no prior reading to
  compare against, so the external-replacement check is satisfied by the act of naming one -
  otherwise the first save of every save-as would be refused by a check about a file that does not
  exist.

  The conflict message names both routes now, which is what makes them a decision rather than a
  dead end: publish over it, or take the volume's copy.

  STILL OPEN in this item, and it is what the item itself marks optional: bounded, versioned backup
  and recovery data. Nothing else is outstanding - a file past the editable limit is refused with
  its reason rather than handed to `licoview`, which needs the association launch and is that
  item's work.
- [x] Persist non-authority preferences (panel layout/columns/sort, key map, theme,
  bookmarks, editor indentation and recent positions) in a bounded, versioned Lico-owned
  file beside the suite under `bin/lico/`, not in a global config hierarchy. Publish it
  through the transactional writer and grant only the suite access to that app directory.
  Never persist handles, granted volume lists, selected-file authority or credentials.
  Corrupt/unknown settings fall back field by field, and a narrow terminal always has a
  usable single-panel/editor/viewer layout.
  Result (2026-08-16). `bin/lico/settings.conf`, a version line and `name=value` per field,
  published through the transactional writer into the app-assets client - which is SCOPED TO
  `bin/lico`, so the manager cannot write a settings file anywhere a settings file should not be,
  and a failed publication leaves the previous one exactly as it was.

  WHAT IT HOLDS IS ORDINARY UI STATE AND THERE IS NOWHERE TO PUT ANYTHING ELSE: an ordering, three
  toggles, the viewer's line numbers, highlighting and wrapping, and the editor's indent width. No
  handle, no granted volume list, no selected-file authority, no credential - the type has no field
  that could carry one.

  IT FALLS BACK FIELD BY FIELD, which is the half that matters. A line that cannot be read leaves
  its own field at the default and the rest of the file is still read; an unrecognised VALUE keeps
  the current setting rather than reading as false, because `reverse=maybe` is a typo and turning
  the setting off on account of it is a change nobody asked for. A file that is not this format at
  all - or names a version this build does not know - yields defaults, which is exactly what no file
  yields: a corrupt settings file can never stop the suite starting. The indent width is CLAMPED
  rather than refused, since a nonsensical width is a preference nobody can act on.

  Deliberately not persisted yet: the bookmark list and the editor's recent positions. Both are
  ordinary UI state and both belong here; what stopped them this round is that a bookmark is a
  volume URI, and writing one into a file means deciding what a launch does with a saved path to a
  volume it was not granted - which this milestone answers for history ("validate against the
  capabilities of the new launch") and which is a check the loader has to make rather than the
  writer.
- [x] Register exactly one `lico.lsexe`, `licoedit.lsexe` and `licoview.lsexe` in the
  artifact manifest under the shared system `bin/lico/` owner directory, plus their three
  logical command names in shell help/completion and PermissionManager policy.
  Direct launch and launch from `lico` have identical behavior and file semantics;
  the manager gets only its declared panel volume grants, an associated editor gets the
  selected file's explicit write-back grant, the viewer remains read-only, and `lico`
  gets only the narrow governed-launch broker needed by approved file associations and
  its command bar, never raw process creation, unrelated file handles or authority to
  expand a launched executable's manifest grants.

  Result (2026-08-16). All three are in the artifact manifest under `bin/`, in the shell's tool
  table under the interactive shape, in the help table and in PermissionManager's policy, and all
  three share the `bin/lico` asset bundle - one set of descriptors for a family of three programs,
  which is what `asset_bundle` maps a component to.

  THE GRANTS ARE THE ITEM'S OWN SENTENCE, and the one that changed this round is `lico`'s: it now
  holds `Capability::Permission`, the NARROW LAUNCH BROKER, and not `Capability::Process`. The
  difference is the whole of the claim - `Permission` asks for a NAMED program to be started under
  that program's own manifest, while `Process` would be raw process creation. So `lico` can open a
  file with the editor and cannot hand the editor anything, because it holds nothing to hand.
  Direct launch and launch from a panel are the same launch through the same broker, so they cannot
  differ in behaviour or in file semantics.

  The viewer holds volumes and its assets and no writer. The editor holds the same, and takes its
  write authority from the transactional writer on the volume it was granted.

  COMPLETED 2026-08-16: an associated editor now gets the selected file's explicit write-back grant
  and an associated viewer gets a read-only one, through `permission.run-with-file`. `lico` still
  holds only the narrow broker, so it is the BROKER that mints the attenuated client - the manager
  names a file it can already reach and receives nothing new for it, which is what makes this a
  narrowing rather than a way to launder authority.
- [ ] Validation gates: host-test syntax descriptors/matching, text editing, undo/redo,
  search (including hex), file-operation planning, history/bookmarks, command-bar parse/
  completion/history/working-directory semantics and resize layout;
  PTY tests drive all F-key, pointer, alternate-screen and signal exits; governed tests
  cover two volumes, read-only/removal/no-space/crash cases, background cancellation,
  safe save, association authority, foreground/background bar launches, terminal restore
  and denial of capability escalation. Benchmark huge directories, large text viewing,
  highlighting, search and copy with peak memory/latency, and keep fresh x86_64/aarch64/
  riscv64 userspace builds plus focused QEMU scenarios green.

  Result (2026-08-16), the host and governed halves.

  THE HOST SUITE IS 44 TESTS in `lico`, and it is in `./check.sh --gate host-tests` - so it runs
  rather than merely existing, which is the failure this project has recorded three times. It covers
  the syntax descriptors and their selection across the whole installed set, text editing, undo and
  redo, selection, the line and block operations, text search forward and backward, the hexadecimal
  query parser, the panel ordering and its filters, the history and bookmarks, the quick search, the
  operation planner with its cycle and same-object refusals, the tagging and the glob, the command
  bar's editing, history and completion, the line splitter and classifier, the association table and
  the settings round-trip with its field-by-field fallback.

  TEN DELIBERATE BREAKS WERE WATCHED TO FAIL, each on the test that names it: the typing
  coalescence, unindenting regardless of the unit, a backward search starting at the cursor rather
  than before it, a modifier decoded as a bare key, reversing the directory grouping along with the
  files, a history that keeps its future across a divergence, a substring quick search, a prefix
  containment test in place of the separator one, a directory's entry size counted as content, the
  half-typed line lost across a history walk, and an unknown file type falling back to the editor
  rather than the viewer.

  THE GOVERNED TEST drives the real `lico` binary against real services: it asserts the alternate
  screen is entered first, that the tty's modes are NOT settable from the output stream, that the
  left panel begins active, that Tab moves the marker, that `F7` and a typed name CREATE a directory
  which then appears in the panel, and that every terminal mode is restored in order on exit. The
  mkdir assertion was watched failing with the volume call removed, so it measures the volume path
  rather than the prompt.

  NOT DONE: the PTY tests that drive every F-key, pointer and signal exit; the benchmarks for huge
  directories, large text viewing, highlighting, search and copy with peak memory and latency; and
  the read-only / no-space / crash cases, which need a scenario with a writable second volume.
- Done when: the three canonical executables work standalone and together; two granted
  volumes can be navigated and manipulated without ambient access; background operations
  and editor saves are failure-safe; viewer/editor consume the same independently
  installable syntax descriptors and agree on highlighting; `licoview` remains text/raw/
  hex only and finds literal or hexadecimal byte patterns; every terminal exit restores
  state; the bottom command bar launches governed foreground/background commands from the
  active panel without a hidden shell or capability escalation; and the explicitly
  excluded compare/sync, multi-rename, archive/package, media, remote and plugin surfaces
  have not leaked into the implementation.
- Concept: P02M0006i/P02M0007 (PTY and pointer-capable terminal), P02M0009/P02M0031/P02M0089/P02M0095
  (PermissionManager, volume-loaded governed executables, session state and `.lsexe`),
  P02M0013-P02M0081 (capability-scoped writable volumes), P02M0096 (existing external image action),
  P02M0097 (owner directories plus the `bin`/`libexec`/`lib`/`log`/`test` volume layout), P02M0102 (shared pipeline and
  redirection grammar once available), and the orthodox two-panel interaction model
  established by Norton Commander, Midnight Commander and Total Commander without
  importing their ambient POSIX/Windows authority assumptions.

