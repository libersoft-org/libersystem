# P02M0172 - DMA isolation policy is registry data, not a kernel device-type list

Status: PLANNED. DMA POLICY PREREQUISITE. QEMU SECURITY FOLLOW-UP.

## Goal

Make every staged driver declare whether it performs no DMA, requires translated DMA, or is an
explicitly trusted untranslated exception, and make kernel admission enforce that declaration from
the trusted generated registry.

P02M0153 defined the policy vocabulary, but the current production decision is still a hard-coded
kernel `IOMMU_REQUIRED_TYPES` list containing only virtio-net. Every other device type silently
defaults to `TrustedUntranslated`. That contradicts the registry-policy claim and blocks an honest
cross-architecture enforcing rollout: adding a driver should not require another kernel name/type
arm before it can be protected.

This milestone extracts and closes the open "declared DMA policy per driver" prerequisite in
P02M0099. It implements no IOMMU backend.

## Two boundaries this plan fixes before implementation

**The DMA mode is NOT the development profile.** `boot_profile()` reads one fw_cfg scalar and
recognizes exactly `development` and `development-trace`, an unrecognized name reads as no profile,
and the file states the value is ADVISORY. Those exact strings also gate security-sensitive
userspace behavior, the harness injects the file only on that branch, and test mode deliberately
carries no profile metadata at all. Reusing or replacing that scalar would either break development
semantics or make every current test, direct and non-x86 boot fail closed. So the DMA mode is a
SEPARATE typed field with its own producers, and the advisory scalar stays advisory.

**The canonical registry identity is the validated program NAME, and it already exists.** Generated
entries are identified by `name`, operator policy persists `select=<name>`, and P02M0166 already
calls that the registry entry ID. Introducing a second stable ID would orphan every stored operator
preference and give reporting, userspace selection and kernel admission three ways to name one
entry. This milestone therefore carries the existing name; it defines the encoding, width and ABI
representation that name needs in order to cross the claim boundary, which is what is genuinely
missing.

## Work

- [ ] **M1 - the manifest has a required closed DMA-policy field.** Every `role = "driver"` row
  declares exactly one of `none`, `iommu-required`, or `trusted-untranslated`; non-driver rows cannot
  declare it, unknown values are rejected, and absence is rejected rather than defaulted. Migrate
  every current driver row deliberately. `none` is an enforceable non-bus-mastering claim mode: the
  driver may receive its validated MMIO and interrupt resources, but PCI bus mastering remains off
  and the binding cannot mint a DMA buffer/address or activate another DMA path. It is not merely a
  statement that the current driver happens not to ask.

- [ ] **M2 - the DMA mode is its own typed boot-contract field.** Define a closed DMA-mode value -
  `enforcing-required` or the explicit `no-iommu` degraded profile - carried in its own field,
  fixed before driver admission, and never inferred from the development profile scalar.

  **THE MATRIX IS WRITTEN HERE, NOT REQUESTED FROM THE IMPLEMENTER** (2026-08-30). A previous version
  said "specify a table" and contained none, which leaves the fail-closed change underivable.

    boot path                          DMA mode              trusted producer
  THE ROWS ARE DISJOINT AND ARE READ IN ORDER (corrected 2026-08-31). An earlier version had rows
  that overlapped - a development `--no-iommu` run matched both "any target, `--no-iommu`" and "any
  target, development instance" and had two answers - and stated the missing-value rule twice, in
  two ways that contradicted each other. RUN MODE is the outer key and it is decided first, because
  a test or development boot is a different kind of thing from a public one. The flag chooses between
  the two enforcing values WITHIN a public or gate run - AND on an x86_64 DEVELOPMENT run, which the
  matrix now splits by architecture and which this sentence used to exclude (corrected 2026-09-01).
  On a development target with no controller the flag chooses nothing, because there is nothing to
  turn off.

  **AND RUN MODE NEEDS ONE CARRIER OF ITS OWN, BECAUSE THE CURRENT SIGNALS OVERLAP** (corrected
  2026-08-31, and this is what makes the rows disjoint in fact rather than on paper). "Decided first"
  named no value and no carrier, and over the signals this harness has today one boot has two answers.
  The proof is the existing enforcing gate: `check-qemu-virtio-iommu-x86_64.sh` invokes `test.sh`,
  whose runner sets `TEST=1`, and it supplies its own enforcing controller through `QEMU_EXTRA`. That
  boot is a `gate` row by ownership and a `test` row by `TEST=1`, and the two rows give it opposite
  answers - `as that gate declares` against `ABSENT`. P02M0173's gate rows are deliberately
  test-kernel phases too, so this is the normal case and not an exception.

    THE CARRIER          one variable, `LIBER_RUN_MODE`, carrying exactly one of `test`,
                           `development`, `public` or `gate`. It is the ONLY thing consulted to pick
                           the outer key. `TEST`, the development profile scalar, `--no-iommu` and
                           `BOOT_IMAGE` continue to mean what they mean and none of them is read as a
                           run mode any more; `TEST=1` selects the test KERNEL and the test MEDIUM,
                           which is a different question from what policy the boot runs under
    WHO SETS IT          the outermost entry point that knows: a named gate sets `gate` before
                           invoking anything, and the invoked runner does not overwrite it. `test.sh`
                           sets `test` only when it is unset, which is what makes the gate's value
                           survive its own use of the test runner. `run.sh` sets `public` ONLY WHEN
                           IT IS UNSET, by the same rule and for a sharper reason (corrected
                           2026-09-02): the development instance does not boot BESIDE `run.sh`, it
                           boots THROUGH it - `lab.py` builds a `run.sh` command line and executes it
                           with its own environment - so an implementation following an unqualified
                           "run.sh sets public" overwrites `development` on every development boot
                           and selects the public signed row for a machine whose mode is carried by
                           the harness. The development instance sets `development` before invoking
                           it, and `run.sh` finding a value already set leaves it alone. ONE RULE FOR
                           EVERY RUNNER: an entry point sets the mode only when it is unset, and the
                           outermost one that knows therefore wins - which is what "the outermost
                           entry point that knows" meant and what naming two producers unconditionally
                           contradicted.
                           ITS GATE: a development boot reaches admission carrying `development` and
                           not `public`, asserted on the boot itself rather than on the code that
                           sets it
    UNSET                is a BROKEN PRODUCER on every path that reaches admission, and refuses -
                           the same rule the named-producer rows below carry, for the same reason:
                           a boot whose run mode was lost is not a boot without an opinion
    THE GATE             the boundary is tested rather than asserted. Each named gate proves it runs
                           under `gate` and with its own declared DMA mode, and the enforcing IOMMU
                           gate specifically proves that its test-kernel phase does NOT fall to the
                           `test` row - which is the exact collision this correction exists for.

    run mode           boot path                    DMA mode              trusted producer
    test               any target                   no-iommu (degraded)   the test runner, through
                                                                            the same carrier every
                                                                            other row uses. Test mode
                                                                            builds no controller
                                                                            topology, and it now SAYS
                                                                            so rather than being
                                                                            silent about it
    development,       x86_64, DEFAULT              enforcing-required    the development instance,
      x86_64 default     (no `--no-iommu`)                                   through the same carrier
    development,       x86_64, `--no-iommu`         no-iommu (degraded)   the same instance and the
      x86_64 degraded                                                        same carrier; the flag is
                                                                            what selects this row
    development,       AArch64/RISC-V, any          no-iommu (degraded)   the same instance and
      non-x86                                                                carrier. These machines
                                                                            have no controller to
                                                                            enforce with until
                                                                            P02M0173 lands one, and
                                                                            the flag changes nothing
                                                                            on them. The row moves to
                                                                            the x86_64 pair above at
                                                                            that landing, and not
                                                                            before
    public, UEFI       x86_64, default              enforcing-required    the signed manifest field
                                                                            (M3), validated by the
                                                                            loader and handed on
    public, UEFI       x86_64, `--no-iommu`         no-iommu              the same signed field,
                                                                            signed with the degraded
                                                                            value - BY A SECOND
                                                                            IMAGE, see below
    public, UEFI       AArch64/RISC-V, default      see the landing        the signed manifest field
                                                      transition below
    public, UEFI       AArch64/RISC-V, `--no-iommu` no-iommu              the same signed field
    public, direct     x86_64, default              enforcing-required    the named harness, through
                                                                            M3's early carrier, in
                                                                            the path that builds the
                                                                            topology
    public, direct     AArch64/RISC-V, default      see the landing        the same harness path
                                                      transition below
    public, direct     any target, `--no-iommu`     no-iommu              the same harness path
    gate               each named gate              as that gate declares  the gate states its value
                                                                            and its topology
                                                                            together, from one
                                                                            harness path

  **THE NON-X86 LANDING TRANSITION, BECAUSE THE ENFORCING VALUE CANNOT LAND HERE FIRST** (corrected
  2026-08-31). A previous version gave the AArch64 and RISC-V public rows `enforcing-required`
  outright, with "once P02M0173's profiles exist" tucked into the producer column. That is a
  dependency cycle written as a footnote: M6 makes an `enforcing-required` mode refuse when
  enforcement is absent or failed, `virtio-iommu-pci` is attached only in the harness's x86_64 path,
  and P02M0173 - which exclusively owns adding that topology to the other two architectures - is
  planned AFTER this milestone and lists this milestone among its own prerequisites. Landing M0172
  first would therefore either refuse every public non-x86 boot or force this milestone to implement
  P02M0173's work.

  So the non-x86 rows have a STATED TRANSITION rather than a single value, and each half
  has an owner. THREE ROWS, NOT TWO (corrected 2026-09-02): the development non-x86 row was added to
  the matrix later, with "the row moves to the x86_64 pair above at that landing" in its own cell and
  nothing to carry it - this contract named only the two PUBLIC rows, Dependencies named only those
  two, and P02M0173's normative transition names its supported ordinary `run.sh` rows rather than the
  development producer. So the row that a developer actually boots could have kept `no-iommu` after
  the topology was attached, which is the controller/mode mismatch M6 refuses, or been flipped with
  no evidence either way. All three transition together and all three are owned below:

    while this milestone lands   `no-iommu`, produced by the same named producer as each row -
      (P02M0172)                   the signed field for the two public rows and the harness carrier
                                   for the development one, carrying the
                                   degraded value explicitly. It is a TRUSTED, PRODUCED, NAMED mode
                                   and not an absence: `iommu-required` still refuses, only
                                   `trusted-untranslated` enters the loud degraded inventory, and a
                                   missing value still refuses because the row still names a producer.
                                   That is the whole point of routing it this way rather than leaving
                                   these rows on the test/development absence rule - the fail-closed
                                   change lands everywhere at once, and only the VALUE differs
    when P02M0173 lands its       `enforcing-required`, and flipping these THREE rows - the two
      topology (P02M0173)          public and the non-x86 DEVELOPMENT one - is an item of
                                   P02M0173 rather than of this file. It changes a produced value in
                                   two places (the signed field and the harness carrier) and needs no
                                   admission change, because M6 already refuses
                                   an enforcing mode without working enforcement - which is what makes
                                   the flip safe to perform after the topology exists rather than
                                   before.
                                   ITS ORACLE, WHICH THE ROW HAD NONE OF: before the flip, a
                                   development boot on AArch64 or RISC-V carries `no-iommu` and
                                   REFUSES an `iommu-required` driver; after it, the same boot on the
                                   same target carries `enforcing-required`, prints that every
                                   bus-mastering device is translated, and ADMITS that driver. Both
                                   halves are fixtures - the first belongs to this milestone and the
                                   second to P02M0173 - so neither side can be taken alone and a flip
                                   without the topology fails its own gate

  The transition is recorded in Dependencies as a two-sided obligation: this milestone must not ship
  `enforcing-required` on those rows, and P02M0173 is not done until it has flipped them.

  **THE DEGRADED PUBLIC ROW NEEDS AN IMAGE, AND "THE SAME BUILD" WAS NOT ONE** (added 2026-09-02).
  The row above says its value comes from the signed manifest, which the loader validates and hands
  on - and a signed field is FROZEN AT IMAGE ASSEMBLY, while `--no-iommu` is a flag at boot. The
  public workflow is three separate steps and `run.sh` is the third: it compiles nothing, assembles
  nothing, and boots an ISO that already exists; its `--no-iommu` branch removes the controller from
  the machine it starts. So the advertised command paired an `enforcing-required` signed image with a
  machine that has no controller, which M6 must refuse - and the only way to make the boot succeed
  would be to change the signed field at boot time, which M3 forbids by construction. The row named a
  producer that does not exist.
  What is frozen instead:
    TWO IMAGES, NOT ONE     `image.sh` takes the DMA mode as an input and writes it into the signed
                              manifest it produces, so a release assembles two distributable x86_64
                              artifacts - the enforcing one and the degraded one - under distinct
                              names. That is the only place the value can be produced, because it is
                              the only step that signs
    THE FLAG SELECTS        `run.sh --no-iommu` boots the degraded artifact and builds the machine
                              without the controller. It selects an image; it does not modify one
    AND IT VALIDATES        before booting anything, `run.sh` reads the signed mode of the image it
                              is about to boot and REFUSES when it does not match the topology it is
                              about to build - an enforcing image with `--no-iommu`, or a degraded
                              image without it. Refused at the host, with both values named, rather
                              than booted into an M6 refusal the operator has to decode from a guest
                              log
    AND A MISSING ARTIFACT  is a refusal that says which image was wanted and how to assemble it,
                              which is the same shape `run.sh` already uses for an absent ISO
  ITS GATES: `--no-iommu` on an enforcing image is refused by the host before QEMU starts, the same
  flag on the degraded image boots and admits only what the degraded row admits, and the degraded
  artifact's signed field reads `no-iommu` when inspected off the medium.

  **AND MISSING NOW HAS ONE MEANING EVERYWHERE, BECAUSE THE KERNEL CANNOT SEE A RUN MODE**
  (corrected 2026-08-31). The rule below used to give ABSENCE two meanings - expected on the test and
  development rows, fatal on every named-producer row - and the kernel is the component that has to
  tell them apart. It cannot: `LIBER_RUN_MODE` is a host-shell variable that decides what the harness
  DOES, the planned `BootInfo` field carries only the DMA mode and its provenance, and the direct
  carrier's record carries magic, version, mode, provenance and a reserved byte. Nothing in the boot
  contract says which run mode produced the boot, so an early kernel cannot distinguish a test boot
  that legitimately carries no record from a public boot whose producer lost one - and the
  fail-closed half of this matrix was therefore unimplementable as written.

  Rather than carry a run mode into the kernel and give admission a second thing to trust, EVERY BOOT
  CARRIES A RECORD. The test and development rows get one too, stating the degraded mode explicitly
  and with `harness` provenance, produced by the same paths that already build those boots. Then:

    absence is ALWAYS fatal   on every path, with no exception and no row-dependent reading. A boot
                                with no DMA-mode record is a boot whose producer failed, whatever
                                kind of boot it is
    the run mode chooses      the VALUE the producer writes - degraded for test, and for development
      the value only            THE ROW THE MATRIX GIVES that architecture and flag (degraded on
                                AArch64/RISC-V until P02M0173, degraded with `--no-iommu` on x86_64,
                                `enforcing-required` on a default x86_64 development boot); the
                                enforcing or `no-iommu` value for public and gate - and it does that
                                on the HOST, where `LIBER_RUN_MODE` already lives. It never needs to
                                reach the kernel, which is what makes this simpler than the
                                alternative rather than merely different
    the degraded contract     is unchanged and is now REACHED BY A VALUE rather than by an absence:
      is unchanged              `iommu-required` refuses, `trusted-untranslated` enters the visible
                                degraded inventory, `none` uses the non-mastering path

  This also removes the one thing the previous version could not test. "Absence means different
  things on different rows" needs a fixture per row and a way to tell the rows apart at admission
  time; "absence is always fatal" is one fixture, and every boot that works proves the producer ran.

  MISSING HAS ONE MEANING PER PATH - and after the correction above, that meaning is the SAME on
  every path. The rows below are kept because they record what each producer is, and the
  test/development entries now name one too:

    test, and development      the harness writes THE VALUE THAT ROW CALLS FOR into the same carrier
      on a machine with no        every other path uses, with `harness` provenance. For test, and for
      controller                  development on a machine with no controller, that is the DEGRADED
                                 value, selecting the loud degraded contract - `iommu-required`
                                 refuses, `trusted-untranslated` enters the visible degraded
                                 inventory, `none` uses the non-mastering path. It is a VALUE these
                                 boots state rather than a silence they rely on, and it is permanent
                                 rather than a migration allowance
    development on a           the harness writes `enforcing-required`, because that is what the
      TRANSLATED machine         machine is - see the matrix. This row exists because the previous
      (x86_64 default)           text said every test AND development producer writes the degraded
                                 value, which on the default x86_64 development instance is simply
                                 false: it boots with a virtio-iommu and every endpoint translated,
                                 and a degraded record there would fail M6's own controller/mode
                                 check on the machine developers use all day (corrected 2026-09-01)
    public and gate            the loader's signed field or the named harness path writes the
                                 enforcing or `no-iommu` value, as the matrix rows say

  ABSENT IS A BROKEN PRODUCER ON EVERY ROW AND REFUSES. A loader that produced no signed field, a
  harness path that omitted the early carrier, and a test runner that forgot to write one are the
  same fact - a machine whose opinion was lost - and admitting drivers on the degraded contract for
  any of them is the silent downgrade this milestone exists to remove.

  So M6's "every missing mode is refused" applies EVERYWHERE, with no exception to remember and no
  row to check first. The earlier text carved out the test and development rows, which is what forced
  the kernel to know a run mode it has no way to learn.

  Migrate every producer and consumer BEFORE admission is made fail-closed, so no current path starts
  refusing because nobody taught it to answer.

  The public run/image path produces the value and the matching QEMU topology together. The direct
  profiles trust the named harness as part of their TCB. An authenticated-loader profile binds it
  into authenticated boot metadata, which needs the handoff M3 defines. Controller absence alone is
  never authority to degrade.

- [ ] **M3 - the authenticated handoff is defined, not assumed.** `BootInfo` v2 has no authenticated
  policy field, and P02M0150 deliberately did not extend it because its trust decision stayed inside
  the loader. This decision is kernel-owned, so the handoff must exist:

  - THE SIGNED MANIFEST FIELD: one `u32` beside P02M0171's generation, `1` = `enforcing-required`,
    `2` = `no-iommu`, every other value refused. Covered by the same signature and validated in the
    same place as product, architecture, source kind and volume identity;
  - A BOOT-WIDE EQUALITY LATCH, on the same terms P02M0171 uses for its generation (added
    2026-08-30). A boot composes several signed manifests - the medium's and each volume's - and
    without a rule two individually valid same-release manifests can disagree about whether
    enforcement is required. So: EVERY selected signed manifest carries the SAME DMA mode, the first
    verified manifest latches it, and a later one that differs REFUSES the boot rather than letting
    either value win. There is no "authoritative manifest" and no precedence order, because a
    precedence order is a rule an attacker composes media against. M8 adds a mixed-mode signed-set
    refusal fixture;
  - A VERSIONED `BootInfo` EXTENSION carrying the value AND its PROVENANCE - one enum, `signed`
    (validated from a manifest by this loader) or `harness` (asserted by a trusted direct-boot path)
    - because the kernel's admission decision differs between them and a single scalar cannot say
    which it is. `BootInfo` is at `VERSION = 2`; this is an appended field and a version bump, which
    is what that guard exists for;
  - PER-PATH VALIDATION: a loader writes `signed` only for a value it verified against a signature it
    checked, and a direct-boot path writes `harness` only from the named harness contract. Copying an
    untrusted assertion into either is the defect the provenance enum makes visible, and M8 mutates
    exactly that; and
  - THE TRUSTED DIRECT-BOOT CARRIER, which is NOT `BootInfo` and could not have been (corrected
    2026-08-31). On a direct AArch64 or RISC-V boot the kernel receives a raw DTB and CONSTRUCTS its
    own `BootInfo` later; worse, `device::init` and `dma_policy::init` both run BEFORE that
    construction. So "the harness writes a `BootInfo` field" asked a host to write into an object
    that does not exist yet, and admission would have decided before any authenticated mode was
    parsed. The carrier has to be something the kernel reads EARLY, from the data it is actually
    handed:

        x86_64 direct      an `fw_cfg` file of this product's own, beside the profile scalar and
                             separate from it - a fixed-length record carrying the mode and the
                             `harness` provenance, read once during early init
        AArch64/RISC-V     a property under this product's own node in the device tree the harness
          direct             passes, read by the existing FDT parser in the same pass that reads the
                             memory map and the interrupt controller

    **AND THE BYTES ARE FROZEN HERE, BECAUSE NAMING A CARRIER IS NOT DEFINING ONE** (2026-08-31). The
    two rows above named a shape and no names, no lengths and no encodings - and no such carrier
    exists in the tree to copy them from, so one harness producer and three kernel consumers had
    nothing to agree on. "Fixed-length" is not a length. The record is ONE FORMAT carried two ways:

      THE RECORD, 8 bytes, on both carriers and every target:

        offset  size  field
        0       4     magic, the ASCII bytes `LSDM`. Anything else is not this record and is
                        MALFORMED, never a version to tolerate
        4       1     format version, value 1. An unknown version is MALFORMED, not ignored
        5       1     DMA mode: `1` = `enforcing-required`, `2` = `no-iommu`. `0` and every other
                        value are MALFORMED. There is deliberately no encoding for absence - absence
                        is the record not being there, which M2's rules already answer
        6       1     provenance: `2` = `harness`. `1` (`signed`) is MALFORMED ON THIS CARRIER and is
                        refused rather than trusted, because a direct-boot carrier is exactly the
                        place a boot-media-controlled value must not be able to claim authentication.
                        The value is one byte so the enum has one encoding across both carriers and
                        the signed manifest field
        7       1     reserved, MUST be zero on write and MUST be zero to validate
        total   8

      x86_64      `fw_cfg` file name `opt/org.libersystem.dma-mode`, whose contents are exactly those
                    8 bytes. A file that is absent, of any other length, or fails the checks above is
                    MALFORMED
      AArch64/    property `libersystem,dma-mode` under the node `/libersystem` - an immediate child
        RISC-V      of the root, with `compatible = "libersystem,boot-policy"` - whose value is the
                    same 8 bytes as a byte string. NOT cells, so no `u32` cell endianness applies and
                    the record is byte-identical to the x86_64 one. A property of any other length is
                    MALFORMED.
                    THE NODE IS NAMED HERE BECAUSE NOTHING SUPPLIES ONE TO INHERIT (added
                    2026-09-01). A previous version froze the property and said only "under this
                    product's own node", and no tree in this system defines such a node - so the
                    harness producer and the three kernel consumers could each pick a different path
                    while following every stated byte, which is the exact failure freezing the record
                    was meant to prevent. The consumer matches on `compatible` rather than on the
                    path, so a machine that places the node elsewhere is still read correctly and a
                    node with the right name and the wrong `compatible` is not this record.
                    The node is absent on an ordinary machine, which is the "this product's own node
                    is absent" case below.

    MALFORMED AND ABSENT ARE THE SAME OUTCOME AND IT IS NOT PERMISSIVE: both yield mode ABSENT, and
    M2's rule then REFUSES - on every row, because every row now names a producer.
    They are distinguished only in what is reported, so an operator can tell a harness that never
    wrote the record from one that wrote it wrongly. Nothing about a malformed record is ever
    interpreted - not its mode byte, not its provenance byte - which is what stops a corrupt record
    from being a downgrade.

    Both are parsed BEFORE `dma_policy::init`, and the parsed value is what `dma_policy` reads -
    that ordering is the requirement, not an implementation note, because a mode parsed after
    admission is a mode admission did not use. `BootInfo` still carries the value onward for
    reporting and for userspace, but it is not where admission gets it.

    The provenance rule is unchanged and is what makes this safe on a direct boot: the value reads
    `harness` and never `signed`, so the kernel knows it is trusting the named harness as part of the
    TCB rather than a signature it checked. On a boot where an `fw_cfg` file or DT property is
    present but this product's own node is absent, or the record is malformed, the mode is ABSENT and
    M2's named-producer rule applies - the direct rows name a producer, so that refuses.

  **AND DEVELOPMENT IS THREE ROWS, BECAUSE ONLY x86_64 HAS A CONTROLLER** (corrected 2026-09-01,
  twice). The first correction split development into default and `--no-iommu` and gave the default
  `enforcing-required` on ANY TARGET. That over-generalised a measurement: what I booted and watched
  print `dma: every bus-mastering device is translated` was x86_64, and `qemu-run.sh` attaches
  `virtio-iommu-pci` in `qemu_run_x86_64` alone. P02M0173 explicitly owns the missing AArch64 and
  RISC-V topology, so a default development boot on those targets has nothing to enforce with and
  would have had to refuse under M6 against a record demanding enforcement. The architecture is part
  of the row, and the non-x86 row changes when P02M0173 lands and not when this file says so.
  ONE
  row said every development boot is `no-iommu` "with or without `--no-iommu`", on the reasoning that
  the profile scalar is orthogonal to DMA. The reasoning is right and the row was still wrong, because
  it stated the MODE and not only the orthogonality: the default development machine is translated.
  `lab.py`'s `dev-up` runs `run.sh` with no IOMMU override, `qemu-run.sh` attaches a virtio-iommu by
  default on the non-test path, and a development guest booted this way prints `dma: every
  bus-mastering device is translated` - measured on this machine, and the GPU restart check in
  `src/harness/dev-gpu-restart.py` depends on exactly that translated default.
  Against the single row, admission had no consistent answer: M6 requires a `no-iommu` record to
  refuse an `iommu-required` entry and to refuse any controller/mode mismatch, so the default
  development topology would either lose its drivers or trip the mismatch rule on every boot. Split,
  each row says what its machine is: the default one is `enforcing-required` like any other translated
  machine, and `--no-iommu` selects the degraded row. Both are `harness` provenance and both use the
  same carrier - what the flag chooses is the mode, which is what the previous row denied.
  M8 gains an admission fixture per row rather than one for "development", and there are THREE rows
  rather than two (corrected 2026-09-02 - this listed only the x86_64 pair, so the row whose value is
  scheduled to change was the one row nothing exercised): an x86_64 default development boot admits an
  `iommu-required` driver; an x86_64 `--no-iommu` development boot refuses the same driver; and an
  AArch64 or RISC-V development boot refuses it too, on the produced `no-iommu` value and not on an
  absent record - which is what proves the carrier reaches that target at all. The post-transition
  half of the third row belongs to P02M0173 with its topology, and is named in the landing contract
  above rather than here, because this milestone cannot produce the machine it would run on.

  **AND A UEFI TEST OR DEVELOPMENT BOOT NEEDS A SOURCE, WHICH MAKING EVERY BOOT CARRY A RECORD DID
  NOT GIVE IT** (corrected 2026-09-01). The previous round removed the "absence is expected here"
  carve-out by requiring the test and development rows to carry a produced `harness` record - which
  is right, and left them with no defined way to produce one. The two carriers above are DIRECT-boot
  carriers: an `fw_cfg` file and a device-tree property. x86_64 test and development boots go through
  OVMF, and the non-x86 UEFI rows are in scope too, so those rows were told to carry a record and
  handed no carrier that exists on their path.

  Two decisions, both made here rather than left to the implementer:

    THE UEFI CARRIER FOR    the `fw_cfg` file. It is not a direct-boot mechanism - it is a MACHINE
      TEST AND DEVELOPMENT    mechanism, available to a UEFI guest exactly as it is to a directly
                              booted one, and the harness already writes the development profile
                              scalar through it. So the same 8-byte record in the same file serves
                              both, and the `harness` provenance means the same thing on both: the
                              named harness is being trusted as part of the TCB. Nothing needs a
                              signed manifest to say "this machine is not translating", and building
                              and signing degraded manifests for every test boot would put a signing
                              step in front of the suite for a value the harness already controls.
    WHICH SOURCE WINS       the SIGNED one, and a disagreement REFUSES. A public UEFI boot's mode
                              comes from the loader's signed manifest field; a test or development
                              UEFI boot has no signed field and takes the harness carrier. If a boot
                              presents BOTH - a signed field and a harness carrier - that is a
                              machine whose two producers disagree about what it is, and the kernel
                              refuses rather than picking. Equal values are still a refusal: two
                              producers that agree by luck are two producers, and the matrix says
                              each row has one.

  A boot-media-controlled value may never present itself as authenticated policy. If P02M0171 lands
  in the same period, the manifest evolves ONCE and one compatibility matrix covers both fields;
  ordering is stated in Dependencies rather than left to whichever is implemented first.

- [ ] **M4 - one identity carries selection and policy across the claim boundary.** Keep the program
  name as the entry identity and attach the policy to it.

  **THE GRAMMAR ALREADY EXISTS AND IS ADOPTED VERBATIM** (corrected 2026-08-30). A previous version
  said to define normalization, encoding and maximum width later; `system-manifest`'s `validate_name`
  already constrains a program name to 1-64 bytes of ASCII alphanumerics plus `_` and `-`, and the
  parser rejects a duplicate program name outright. Defining a second normalization would create two
  answers for one identity, which is the defect this item exists to prevent. So the identity IS that
  name, in those bytes, with NO normalization step at all - a name is already canonical or it is
  rejected at manifest validation - and the maximum width is 64.

  What genuinely remains is the ABI representation, chosen here rather than deferred: a FIXED
  64-BYTE FIELD, NUL-padded, carried in the claim request and stamped on the grant and the report.
  Fixed rather than length-prefixed because the claim boundary is a `repr(C)` ABI struct and a
  variable-length field there is a second parser; 64 because that is the manifest's own bound, so the
  two cannot drift. It is a DECLARED field: `ClaimKey`'s reserved padding is not repurposed.
  `device_claim` currently carries device index and privilege, and `ClaimKey` carries
  device index, padding and generation; the identity must be added as a declared field with its own
  ABI decision, not written into reserved padding.

  DeviceManager passes the identity of the exact priority/fallback candidate it is attempting, not a
  free policy scalar. A fallback candidate therefore carries its own decision rather than inheriting
  the first matching row's. The kernel stamps entry identity, policy and the newly minted binding
  generation on the claim.

- [ ] **M5 - the authority boundary is frozen: DeviceManager selects, the kernel validates.**
  Privileged DeviceManager's selected matching entry is AUTHORITATIVE. The kernel does not recompute
  priority - operator policy can deliberately select a lower-priority candidate, and a kernel that
  recomputed would silently overrule the operator. The kernel validates:

  - that the named entry EXISTS in the table generated from the same manifest;
  - that the entry's match rules admit the concrete device being claimed;
  - the entry's declared DMA policy against the boot DMA mode; and
  - the binding generation.

  Membership-plus-device-plus-policy-plus-generation is the complete check. Write that down as the
  trust model it is: privileged DeviceManager is the selection authority, and the kernel's job is to
  make sure the selection names something the image actually declared for this device.

- [ ] **M6 - admission has no permissive fallback.** Remove `IOMMU_REQUIRED_TYPES` and the implicit
  default to `TrustedUntranslated`. On an `enforcing-required` mode, both DMA-capable policies are
  translated and either refuses if enforcement is absent or failed. On the explicit trusted
  `no-iommu` mode, `iommu-required` refuses and only `trusted-untranslated` may enter the existing
  loud degraded inventory; an unexpected controller/mode mismatch is refused rather than silently
  reclassified. A `none` claim uses M1's non-mastering path in either mode and refuses every DMA
  activation. An unknown, stale-generation, entry/device mismatch or mode mismatch refuses, and so
  does a MISSING mode, on EVERY path without exception - see M2's missing-value rule, which this
  sentence has now agreed with twice in opposite directions and which is settled: every row names a
  producer, including test and development, so an absent record is always a producer that failed.
  Every one of them refuses rather than choosing the least restrictive answer.

- [ ] **M7 - policy changes select and rebuild every consumer.** Ensure the shared manifest is an
  input to the generated userspace registry, kernel admission data, image metadata and verification
  model, and bind the DMA-mode value into the direct or loader/kernel boot contract as appropriate.
  A one-line policy or mode change cannot leave a kernel from the old decision beside DeviceManager
  or an image from the new one.

- [ ] **M8 - hostile policy fixtures prove the boundary.** Host tests reject missing/unknown fields,
  fields on services, existing same-priority ambiguous overlaps, and stale/mismatched or
  non-normalizing entry identities. A positive different-priority overlap with different policies
  proves that an intentional fallback is valid and that each attempted entry reaches the kernel with
  its own policy and generation. Include the case the authority boundary exists for: an operator has
  persistently selected a LOWER-priority candidate, and the kernel admits it because the entry is
  declared for that device - and refuses when it is not.

  AND THE CARRIER HAS A PRODUCER/CONSUMER PAIR FIXTURE (added 2026-09-01), because a frozen record
  that only one side has ever written is a record with one implementation. The harness producer emits
  the 8 bytes into `/libersystem` with its `compatible`, the FDT reader parses them back, and the two
  agree on every field - run for the device-tree carrier and for the `fw_cfg` one, since they share a
  format on purpose. Its negatives are the ones that distinguish a frozen identity from a convention:
  the node present with the wrong `compatible`, the property under a differently named node with the
  right `compatible` (which must still be FOUND, since the consumer matches on `compatible`), a
  property of the wrong length, and a record whose provenance byte says `signed` on a carrier where
  only `harness` is legal.
  A UEFI test boot and a direct one carrying the same record reach the same admission decision, which
  is what makes the shared format a claim rather than a coincidence. THE UEFI SIDE OF THAT PAIR IS
  RUN ON AARCH64 AS WELL AS x86_64 (added 2026-09-01). The carrier decision above rests on `fw_cfg`
  being a MACHINE mechanism rather than a direct-boot one, and the non-x86 UEFI rows are the ones
  where a reader is most likely to doubt it - the aarch64 kernel takes its `fw_cfg` base from the
  device tree, so the claim depends on the firmware passing that tree on. It does, and the aarch64
  suite boots through AAVMF and reads its GIC from that tree today, but a decision this file makes
  should be pinned by the fixture on the path it was doubted on rather than only on the one where it
  is obvious.

  Kernel/guest tests prove the full M6 mode/policy matrix, including MMIO and IRQ but no BME or DMA
  for `none`. A MIXED-MODE SIGNED SET is refused without admitting anything: two individually valid
  same-release manifests carrying different DMA modes make the boot refuse at M3's latch. And the
  identity bytes are proved IDENTICAL end to end - the persisted operator selection, the generated
  table, the claim request, the kernel's stamp and the report all carry the same 64 bytes for one
  entry.

  Mutations that must fail: restoring the permissive default, inferring degraded authority from
  controller absence, recomputing priority in the kernel, accepting a DMA mode whose provenance says
  it came from replaceable media, a loader writing `signed` provenance for a value it did not verify,
  and removing the equality latch so a mixed-mode set boots. Assert that the current image has no
  staged driver without an explicit classification, and that no producer named in M2's matrix is
  missing a value.

## Dependencies

- P02M0153 owns `DmaAddress`, IOMMU backend admission and the three policy outcomes.
- P02M0161-P02M0166 own registry selection, binding generations, rollback, stop and reporting, and
  P02M0166 owns the entry name that M4 carries.
- P02M0150 owns signed manifest v2 and the loader trust decision that M3 extends into the kernel.
- P02M0171 also evolves the signed manifest. If both are in flight, one coordinated manifest
  evolution carries both fields and one compatibility matrix covers them; neither may land a second
  incompatible format change.
- P02M0099 remains the driver roadmap; future DMA-capable items consume this mechanism rather than
  adding another policy arm.
- P02M0173 owns the AArch64 and RISC-V virtio-IOMMU topology and is planned AFTER this milestone,
  which it lists among its own prerequisites. The obligation is two-sided and neither side may be
  taken alone: THIS milestone ships the THREE non-x86 rows - the two public ones and the DEVELOPMENT
  one - at the produced `no-iommu` value and
  must not ship them at `enforcing-required`, and P02M0173 is not done until it has flipped all three
  to `enforcing-required` alongside the topology that makes the value true, with the post-transition
  admission fixture for each. The development row was missing from this obligation while the matrix
  said it moved at that landing (corrected 2026-09-02), which left the row a developer boots owned by
  neither side. Stated here rather than
  left to whichever is implemented first, because either order taken on its own breaks non-x86
  boots.

## Definition of done

- Every staged driver has one validated DMA policy from the manifest.
- The DMA mode is a distinct field with a stated producer for every row of M2's matrix, carries its
  provenance, is equal across every signed manifest a boot composes, and the development profile
  scalar is unchanged.
- Run mode has ONE carrier, every boot resolves to exactly one row of the matrix, and each named gate
  proves it runs under `gate` rather than falling to the `test` row it shares its runner with. Run
  mode never reaches the kernel: it decides which VALUE the producer writes and nothing else.
- EVERY boot carries a DMA-mode record, test and development included, so an absent one is a failed
  producer on every path and the kernel needs no second fact to decide that.
- The direct-boot carrier's record is parsed from the frozen bytes on both carriers, before
  `dma_policy::init`, with malformed and absent both refusing on every row and neither
  interpreted.
- Public AArch64 and RISC-V boots keep working, at the produced `no-iommu` value: this milestone does
  not ship `enforcing-required` on those rows, and P02M0173 flips them with the topology.
- Kernel admission enforces the exact attempted entry's policy and mode, including a usable but
  non-bus-mastering `none` claim, with no device-type list or silent trusted-untranslated default,
  and without overruling an operator's selection.
- Policy, entry identity, binding generation, operator reporting and rebuild selection cannot drift.

## What this milestone refuses

- A new IOMMU backend, endpoint migration, native hardware or a physical-machine claim.
- A second entry identity beside the program name, and any use of reserved ABI padding to carry one.
- Kernel-side priority recomputation.
- Treating every driver as malicious on a profile with no translating hardware; explicit degraded
  exceptions remain possible and visible.
- A free-form policy language. Three closed outcomes are the complete current admission contract.
