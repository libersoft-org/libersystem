# P02M0136 - Text foundation: shaping, fallback and layout

Status: FUTURE TRACK. NOT A PHASE-2 COMPLETION GATE.

DEPENDS ON `P02M0103b` for the frozen `GlyphRun` and font-resource contract, and on `P02M0103c` for
the conforming `soft2d` renderer its end-to-end proof draws with. Corrected 2026-08-30 after the
2026-08-30 plan audit: the original status line named `P02M0103b` alone, and `P02M0103b` is the
backend-neutral PROFILE with backend-free host tests - it delivers no renderer, so the mandatory
"font file to pixels" gate had nothing to run against. The two dependencies are different in kind and
are now named separately:

- the CATALOGUE AND FONT-PACKAGE item below is FIRST and unconditional - it depends on nothing in
  `P02M0103` and everything here depends on it, because without a staged face nothing below can be
  exercised;
- the SHAPING and LIBRARY work depends on that item AND on a resolved `P02M0103b` `GlyphRun` and
  font-resource contract - BOTH, in that order, not either (corrected 2026-08-30: this said the work
  "may start as soon as" the M0103b contract is frozen, which authorised parsing and shaping to begin
  while the catalogue item forbade it, so the file carried two different executable schedules);
- the INTEGRATION and Done gate depends on `P02M0103c`'s conforming renderer.

THE `DrawList` PRECONDITION IS MET (2026-08-30, and stated once). It was briefly recorded as
"partly met and stays open" while `P02M0103` still contradicted itself: its authoritative item chose
the in-process list, and two other places in that file required the transportable one - its
backend-free host tests asked for a "display-list serialisation round-trip", and part `b` completed
only when the list "round-tripped through its transportable form". That contradiction has been
removed, so the precondition is met and the shaping and library work below may start. It is stated
as MET here, in the dependency summary at the end of this file, and nowhere in a third state:
carrying two scheduling answers for one precondition is the defect the correction existed to fix.

`P02M0103` has since defined what those two were actually asking for - a CANONICAL ENCODING that is
an internal binary form and not a wire ABI: the representation the version and semantic hash are
declared against, with no capability in it and no untrusted-input validation around it, because its
only producer and consumer are one process's own `render2d`. Both sites now name that.

THIS MILESTONE'S PRECONDITION IS THEREFORE: `P02M0103b` carries ONE `DrawList` contract, and its
canonical encoding is stated to be process-local rather than a wire ABI. That is true of the file as
it now stands and it is checkable by reading it - which is the form a precondition has to be in, and
"the decision was made" was not. Checked against `P02M0103` on 2026-08-30 and satisfied; if a later
pass of that file reintroduces a second `DrawList` contract, this precondition reopens and the
shaping and library work stops until it is one again.

`P02M0103b` carried two mandatory `DrawList` contracts - an immutable
in-process replayable list in one item and a transportable cross-process format in another - and the
font resource is carried by whichever wins, since its lifetime, its transport and whether a raw
reference may appear in a list all differ between them. Implementing the shared seam against the
loser would have meant replacing it. `P02M0103` DECIDED IT on 2026-08-30: Profile 1's `DrawList` is
immutable, versioned, cacheable and replayable IN-PROCESS, carrying a typed resource TABLE - not
inline references - with cross-process transport becoming the named `render2d-wire` extension.
So the font resource is a TYPED HANDLE INTO A PROCESS-LOCAL RESOURCE TABLE, and the contract frozen
by the seam item below is written against that and nothing else.

Created 2026-08-15 alongside `P02M0103`'s seventh pass. `render2d` draws a `GlyphRun` - glyphs that
are already chosen, already shaped and already positioned - and that seam is deliberate: a rasteriser
has no business knowing what a script is, and a shaping engine has no business knowing what a
coverage mask is. But the seam only divides the work; it does not do half of it. Without this
milestone LiberSystem can draw text it is handed and cannot produce text from a string, which is not
a platform ordinary applications can be written on.

The division of labour, stated once so neither milestone drifts into the other:

    P02M0136                                    P02M0103b / P02M0103c
    font files, character to glyph,             glyph outlines to coverage,
    script shaping, fallback, BiDi,             masks, colour glyphs, bitmap
    line breaking, positioning                  strikes, transforms, caching
                            \                  /
                             ->  GlyphRun  <-

Everything below is about producing a correct `GlyphRun`. Nothing below rasterises anything.

That division has one consequence worth stating, because getting it wrong makes the two milestones
wait for each other. `P02M0103` proves the RASTERISATION half against PRE-DECODED FIXTURES - a glyph
outline, a bitmap strike, a colour glyph - and deliberately does not open a font file, because
opening one needs the parser below and the parser below needs `render2d` to draw with. **The first
real font file this system reads is read here**, and the end-to-end proof - font file to shaped run
to pixels - is this milestone's, against the renderer `P02M0103c` delivered. Neither side writes a
throwaway parser.

## The seam is not yet a contract, and it is this milestone's first CONTRACT item

The audit's sharpest finding: neither milestone says how a glyph ID becomes geometry. This side
produces glyph indices; that side must rasterise outlines, masks, bitmap strikes and `COLR` paint
graphs - and nothing owns the validated font bytes, the decoded glyph data, the lookup, the lifetime
or the identity between them. A glyph index and a kind flag cannot be drawn.

Two further consequences fall out of the same hole. `P02M0103`'s glyph cache WAS keyed by face, size,
transform and subpixel phase - omitting the glyph index itself, the variation coordinates this
milestone promises, and the palette and strike selection colour and bitmap glyphs need, so two
different instances of one face collided in that cache and rendered stale pixels. That key is now
corrected in both files and is stated below. And fallback produces runs over SEVERAL faces while
this milestone's `GlyphRun` carries a singular `face`, so the output of the fallback item cannot be
expressed in the type the seam item defines.

- [ ] **THE FONT PACKAGE ROLE AND THE CATALOGUE ARE BUILT HERE, FIRST** (decided 2026-08-30). The
  plan cited `P02M0097` as "where font packages live" and it is not there: `P02M0097`'s final volume
  layout declares `bin/`, `libexec/`, `lib/`, `drivers/`, `components/`, `log/`, `audio/` and
  `wallpapers/` and deliberately no `share/`, and the manifest validator admits factory data only at
  `hello.txt`, `motd.txt`, `audio/test.mp3`, `wallpapers/*.webp` and `bin/lico/syntax/*.syntax`.

  Two previous rounds recorded this as an unowned prerequisite and then as a hard block, and neither
  gave it an owner - which left this milestone unschedulable while the missing work was small. It is
  ABSORBED rather than numbered: the deliverables below are bounded, and this milestone is their only
  consumer, so a separate milestone that must be approved, ordered and then implemented in the same
  sitting is bureaucracy rather than sequencing. Nothing else in this file may start before it,
  because without it no face can be staged and the guest gate cannot run. If a second consumer
  appears later it reuses this contract; it does not re-derive one.

  The deliverables, each owned here:
  - the canonical FONT DESTINATION, FROZEN HERE rather than described (2026-09-01). "The destination
    and its manifest role" named neither, so P02M0097, the validator, ServiceManager and the
    catalogue could each have implemented a different unstated contract. The identifiers:

        the destination     `vol://system/share/fonts`, added to `P02M0097`'s layout and to the
                              manifest validator's admitted set, with the staged-consistency gate
                              extended to cover it
        the role row        tag `FONTDIR`, kind `client`, provider `storage_service`, on the font
                              catalogue's `[[services]]` entry
        what makes it       that row is the ONE case where ServiceManager does not simply duplicate
          exceptional         the provider's root: `Role` carries no path, so the dispatch case for
                              tag `FONTDIR` calls `volume-admin.open-directory` with the destination
                              above and `writable: false`, and delivers the scoped client the mint
                              returns. The path lives in ServiceManager beside the destination
                              constant, which is where the rest of the bootstrap wiring keeps its
                              paths, and NOT in the manifest, which has nowhere to put it
        its refusal         a mint that fails is a start-up refusal for the catalogue, reported as
                              such. A font service with no fonts is not a degraded mode to run in;
  - the licensed, PINNED LAST-RESORT FACE and the conformance corpus, staged the same way, each with
    its licence recorded under the same review as any other input that enters the tree;
  - the CATALOGUE SERVICE, specified rather than named (corrected 2026-08-31 - an earlier version
    called it "a bounded catalogue owner" and gave it no role, no startup edge, no protocol and no
    ceiling, which is not something anybody can build or refuse):

        role            a `role = "service"` row in the system manifest with its own program name,
                          started like every other core service and reached the way they are
        how it is       NOT through P02M0164's provider catalogue, which is a DEVICE catalogue and
          reached          cannot carry this (corrected 2026-08-31). Three things in the tree say so
                          independently: `provider-kind` is a CLOSED enum of eight device kinds and
                          adding a ninth is a schema change to the device IDL; every `provider-info`
                          is identified by a PCI bus/device/function plus a binding generation, which
                          a service does not have and cannot be given; and manifest publication is
                          structurally driver-only - only a `role = "driver"` row may carry the
                          binding declaration, the shape check refuses it on every other role in both
                          directions, and the producer/consumer graph iterates drivers alone. So a
                          `role = "service"` row publishing `font-catalogue` there is not a small
                          extension; it is a second identity model inside the device catalogue.
                          THE ROUTE IS THE ONE THIS TREE ALREADY HAS FOR SERVICE-TO-SERVICE AND
                          SERVICE-TO-APPLICATION CAPABILITIES, and it gives this item everything the
                          catalogue was wanted for without a schema:
                            - MANIFEST-DECLARED CONSUMERS reach it through the existing `Factory`
                              role. ServiceManager mints a per-consumer connection from the
                              provider's own root and hands it over narrowed to the rights the
                              consumer's row declares. That is a per-consumer factory, not a shared
                              handle - which is the property this item wanted and the reason the
                              bootstrap-handle shape was refused.
                            - ORDINARY APPLICATIONS reach it through PermissionManager, which already
                              holds one client per grantable capability and grants it under its own
                              bootstrap tag to a component whose manifest lists it. This item adds
                              the `font-catalogue` capability: its enum variant and ordinal, the held
                              client, the grant arm and its bootstrap tag.
                              AND THE GRANT MINTS A FRESH SUB-CONNECTION, NOT A DUPLICATE (corrected
                              2026-09-01). `grant_handle`'s default is "a narrowed duplicate of the
                              held client", and a duplicate SHARES the reply queue: every generated
                              client starts its correlation counter at zero and `ChannelTransport`
                              consumes exactly the next reply, so two applications granted the same
                              endpoint would answer each other's calls. That is not a font problem -
                              it is why `network`, `config` and `device` are already minted as fresh
                              sub-connections, with the reason written at that function. This
                              capability joins those three rather than the default, and its gate is
                              the discriminating one: TWO applications holding the capability at once,
                              each issuing a LIST and a RESOLVE, each receiving its own answers.
                          Its deliverables therefore include those two edges and their negative gate -
                          a component that does not list the capability receives a bare DENY and
                          reaches no face - and NOT any change to the device catalogue, its IDL or its
                          manifest schema.
        where its      a READ-ONLY DIRECTORY CLIENT on the canonical font destination, minted by the
          BYTES COME       supervisor through `volume-admin` and delivered as this service's role
          FROM             (decided 2026-09-01, shape corrected the same day). The row named a
                           `role = "service"` program, both consumer edges, the protocol and the
                           ceilings, and never said how the catalogue itself reaches a single font
                           byte. Without this edge RESOLVE and the replacement generation cannot be
                           built without inventing an undeclared capability, which is the one thing
                           this item's own capability rule forbids of its clients.
                           AND NEITHER EXISTING CONTRACT MAKES WHAT THE FIRST ANSWER ASKED FOR - it
                           said "a `Client` role on StorageService, CONFINED to the directory", and
                           that is the intersection of two routes rather than either of them. `Role`
                           carries a tag, a kind, a provider, a presence, an interface and a source,
                           and NO path and NO scope, so a manifest `Client` row cannot be confined at
                           all; its executor duplicates the named provider's root. The thing that IS
                           confined, `volume-admin.open-directory`, takes only a path: it mints a
                           `Scope::Directory` whose request filter admits mutation, and only
                           `Scope::File { writable: false }` refuses it. Confined-but-writable, or
                           read-only-and-unconfined; nothing produced both.
                           SO THIS MILESTONE OWNS THE READ-ONLY DIRECTORY MINT, as a deliverable
                           rather than an assumption: `open-directory` gains a `writable: bool` in
                           the same shape `open-file` already carries.
                           AND THE DENIAL SET IS FROZEN HERE, BECAUSE "THE SAME FILTER" WOULD NOT
                           HAVE DONE IT (corrected 2026-09-01). The previous wording said a
                           `writable: false` directory refuses mutation "through the same request
                           filter that already refuses them for a read-only file". It would not: in
                           that filter `writable` is consulted only for `Scope::File`, and the
                           file-scope denial list itself omits `OP_MKDIR` and `OP_RMDIR`, which the
                           op table below then admits for any matching scoped path. Adding a boolean
                           to the directory variant and pointing at that filter would have minted a
                           "read-only" client able to create and remove directories.
                           A read-only DIRECTORY scope refuses this complete set, named rather than
                           inherited: `OP_WRITE`, `OP_REMOVE`, `OP_TRUNCATE`, `OP_TOUCH`,
                           `OP_WRITE_STREAM`, `OP_OPEN_WRITER`, `OP_MKDIR`, `OP_RMDIR` and
                           `OP_RENAME` - every opcode that can change the volume, with rename
                           already refused for scoped clients on its own two-path grounds. It ADMITS
                           `OP_OPEN`, `OP_LIST`, `OP_READ` and `OP_WATCH`, which is exactly what the
                           catalogue does. Any opcode added to `volume` later is refused by this
                           scope until it is classified, which is the direction a default must fail
                           in.
                           ITS DELIVERY: ServiceManager holds the `volume-admin` authority, mints the
                           read-only client over the canonical font destination at bootstrap, and
                           hands it to the catalogue as its role. The path is not in the manifest -
                           `Role` has no field for one - it is the destination this milestone defines
                           in `P02M0097`'s layout, which is where every other piece of bootstrap
                           wiring reads its paths from.
                           DONE 2026-09-12. `volume-admin.open-directory` carries `writable`, and a
                           read-only directory scope is an ALLOWLIST of `OP_OPEN`, `OP_LIST`,
                           `OP_READ` and `OP_WATCH` rather than a denial list - which is what the
                           correction below asked for and what the obvious alternative would not have
                           given. The guest gate attempts all nine mutating opcodes individually and
                           each is refused, an opcode nobody has classified is refused too, `list`
                           is admitted, and the same grant minted `writable: true` accepts `mkdir`
                           and `rmdir` - so the refusals are the flag-s doing rather than an
                           operation the service does not support.
                           THE GATE FOUND ITS OWN FIRST VERSION WRONG: a write-stream attempt without
                           the byte count and channel the request carries fails to PARSE, answers
                           with a correlation of zero, and never reaches the scope check - so it was
                           measuring the decoder rather than the scope.
                           ITS NEGATIVE GATE, because a least-authority claim with no failing case is
                           a sentence: the catalogue's own client attempts EVERY opcode in the denial
                           set above - write, remove, truncate, touch, write-stream, open-writer,
                           MKDIR, RMDIR and rename - under the font destination, and each is refused
                           by StorageService. `mkdir` and `rmdir` are named individually because they
                           are the two the existing filter lets through, so a gate that tested "a
                           write, a create and a delete" and stopped there would have passed over the
                           actual hole. Watched to fail by minting the same client `writable: true`.
                           WHY THIS SOURCE AND NOT THE OTHER TWO, since the choice decides authority,
                           restart and update semantics and not only plumbing:
                             - the INIT PACKAGE as a `Package` role would make the faces immutable for
                               the life of a boot. Then SUBSCRIBE has nothing to report, the
                               generation can never change while the machine runs, and the
                               replacement gate below could only be met by rebooting - which is not
                               what "a replaced face invalidates what was derived from it" means.
                             - a BROAD StorageService client would give a font service authority over
                               the whole volume, which is the ambient authority this item refuses on
                               behalf of its own clients four lines below. It cannot refuse for them
                               what it takes for itself.
                           So: confined to the one directory, read-only, and re-creatable while
                           StorageService lives - which is what makes the catalogue restartable, since
                           everything it holds is derived from that directory and is rebuilt by
                           re-reading it.
                           HOW IT LEARNS A FACE CHANGED: `volume.watch` ON THE SAME SCOPED CLIENT,
                           which already exists (corrected 2026-09-01). The previous version said
                           "nothing notifies it, because nothing in this tree currently can" and
                           built a client-facing RESCAN around that claim. The claim was false and I
                           did not check it: `volume.watch` takes a path, reports the entries
                           directly below a DIRECTORY as they change, is admitted by a scoped
                           directory client, and is bounded by design - a watcher that cannot keep up
                           is dropped rather than buffered, and its own documentation calls the
                           stream a hint to re-read rather than a log. That is this design's trigger,
                           already built.
                           So: watch the destination; on any event re-read the directory, recompute
                           each face's identity digest, and bump the generation when a digest, the
                           set of names, or a face's metadata differs from what was last published. A
                           scan at service start seeds it. The watch's documented limit is the honest
                           one to record here - it sees mutations that pass THROUGH StorageService,
                           which is how a face is replaced on a running system, and not a disk edited
                           behind the service's back.
        protocol        an LSIDL interface with FOUR operations and no more: LIST the installed
                          faces as bounded metadata records; RESOLVE-INFO, which answers a face's
                          byte length and the generation that answer is about; RESOLVE-INTO, which
                          fills a `MemoryObject` the CALLER created and passed with the request; and
                          SUBSCRIBE to the generation, so
                          a client learns a face was replaced without polling.
                          RESOLVE WAS ONE OPERATION AND COULD NOT BE (corrected 2026-09-02). It
                          answered "a read-only `MemoryObject` of its bytes", which means the
                          catalogue creates it - and in this kernel a `MemoryObject` is charged to
                          the Domain that CREATED it and stays charged after the handle moves. See
                          the payment row below: the split into INFO and INTO is what makes the
                          bytes the caller's, and it is two operations because the caller must be
                          told the size before it can create the object.
                          RESCAN IS NOT ON THIS INTERFACE (corrected 2026-09-01). A fourth operation
                          was added here on the false premise that nothing could notify the service,
                          and putting it on the client interface gave every font holder - including
                          any ordinary application granted the capability - the power to force a full
                          directory read, digest and metadata pass as often as it liked.
                          `MAX_FONT_CLIENTS` bounds live ENDPOINTS, not requests per endpoint, so
                          nothing bounded that shared CPU and I/O: authority to READ a font became
                          authority to make the machine work.
                          The normal trigger is the `watch` stream (see where its bytes come from).
                          An explicit rescan survives only as RECOVERY, on a separate
                          `font-catalogue-admin` interface with its own capability, at most one in
                          flight, held by no ordinary application.
                          AND "ONE PER PUBLISHED GENERATION" LOCKED THE OPERATION OUT OF THE STATE IT
                          EXISTS TO REPAIR (corrected 2026-09-02). A scan that is invalid or over a
                          ceiling deliberately PUBLISHES NOTHING and leaves the generation unchanged
                          - EXCEPT where it found a published face the writer had overwritten with
                          bytes that must be rejected, which is withdrawn and does advance the
                          generation; see the ordered rule at the ceilings (2026-09-04). The
                          allowance below counts against the generation either way, so the recovery
                          operation is not locked out by the withdrawal case either.
                          So the first recovery scan after a missed watch could observe the bad
                          directory, spend the generation's only attempt, and leave no way to scan
                          again once an operator fixed the files - the failure the operation is for
                          made the operation unusable.
                          TWO BOUNDS, BECAUSE ONE OF THEM LEFT A HOLE (corrected 2026-09-02, twice
                          in two days on the same rule). The first answer made the allowance count
                          SUCCESSFUL PUBLICATIONS and gave only a FAILED scan a delay - and a scan of
                          an UNCHANGED directory is neither. The generation advances only when a
                          digest, a name or a face's metadata differs, so a valid no-change scan
                          publishes nothing, spends no allowance and re-arms no delay: the admin
                          caller could ask for a full directory read, digest and metadata pass in a
                          loop, which is precisely the work this operation was moved off the client
                          interface to bound.
                          The two bounds answer two different questions and are stated separately:
                            HOW OFTEN A SCAN MAY RUN   at most one in flight, and every COMPLETED
                                                         scan - published, unchanged or failed -
                                                         re-arms a bounded delay. A request inside
                                                         the delay is refused with `try-again-at`.
                                                         This is the work bound and it does not care
                                                         what the scan found
                            HOW MANY MAY PUBLISH       one per published generation, counted as the
                                                         first answer says: a scan that publishes
                                                         spends the allowance for the generation it
                                                         replaced, and one that does not publish does
                                                         not. This is the churn bound, and it is what
                                                         keeps a recovery scan available after a bad
                                                         directory is corrected
                          Its gates gain the case that was missing: two recovery scans of an
                          unchanged directory, where the second is refused with `try-again-at` and
                          the published generation is unchanged by either.
                          THE HOLDER IS NAMED, AND NAMING A POLICY IS NOT NAMING A ROUTE (corrected
                          2026-09-02). The previous answer said PermissionManager mints
                          `font-catalogue-admin` "for the operator path alone" and named no way for
                          it to be able to: PermissionManager can only mint a client from an endpoint
                          it was GIVEN, and nothing gave it one. The analogue this cited is concrete
                          in exactly the four ways that answer was not, so the four are copied rather
                          than alluded to:
                            the ENDPOINT   the font catalogue's admin interface is a second
                                             `serve-root` role ON THE FONT CATALOGUE ITSELF, tagged
                                             `ADMIN`, minted at its start beside the client one.
                                             NAMING THE TAG IS THE HALF THAT WAS MISSING (corrected
                                             2026-09-03): "a second serve endpoint" is a description,
                                             and the manifest needs a tag, because a client row
                                             selects a root BY TAG
                            the ROLE       the manifest client role on `permission_manager`, and its
                                             four fields exactly, because `provider` and `source` are
                                             DIFFERENT fields and the previous wording gave one
                                             answer for both (corrected 2026-09-03). `provider` names
                                             the SERVICE; `source` names one of that provider's
                                             `serve-root` TAGS, an empty `source` means `SERVE`, and
                                             the manifest checker REFUSES a source the provider does
                                             not offer. So the row is:

                                                 tag       = "FONTADMIN"
                                                 kind      = "client"
                                                 provider  = "font_catalogue"
                                                 source    = "ADMIN"
                                                 interface = the `font-catalogue-admin` interface

                                             which is the shape `STORAGE_ADMIN` already has - a
                                             client of StorageService's `ADMIN` root and not of its
                                             `SERVE` root, the two granting different authority over
                                             the same volume. Without `source = "ADMIN"` the row
                                             selects the ORDINARY root and PermissionManager mints
                                             the client capability from the wrong endpoint, which
                                             validates cleanly and grants an operator nothing
                            the GATE       the ordinary `font-catalogue` row and the `FONTADMIN` row
                                             resolve to DIFFERENT serve roots of the same provider,
                                             asserted rather than assumed - the one mistake that
                                             passes every existing check
                            the DELIVERY   ServiceManager passes that endpoint to PermissionManager
                                             during bootstrap, on the same hop that carries the
                                             device-policy one
                            the CAPABILITY the security capability `CAP_FONTADMIN`, declared and
                                             tagged like `CAP_DEVPOLICY`, which is what
                                             PermissionManager mints from that endpoint and what a
                                             grant is checked against
                            the RECIPIENT  `lsfont`, the operator tool that owns font administration -
                                             the same relationship governed `lsdev` has to the device
                                             policy endpoint. It is the program the grant names, and
                                             it is where `rescan` is issued from after a dropped
                                             watch. NAMING IT WAS THE HALF THAT WAS MISSING (added
                                             2026-09-02): the previous round said "the operator tool
                                             that owns font administration" and called it NAMED,
                                             which is a description and not a name - and "no ordinary
                                             application holds it" says who may not while leaving who
                                             may unstated, so there was still nobody who could issue
                                             a recovery scan.
                                             `lsfont` is a deliverable of this milestone: it lists
                                             the installed faces through the ordinary
                                             `font-catalogue` client, and its ONE privileged
                                             operation is `--rescan`, which needs the grant above.
                                             A build in which the tool is absent is a build in which
                                             recovery cannot be issued, and that is a gate rather
                                             than a note
                          An ordinary application still cannot hold it, which is what makes the
                          ceiling a bound on an operator rather than on every font client - and now
                          there is somebody the bound applies TO.
                          ITS GATES: a scan that fails a ceiling leaves the published generation
                          unchanged and does NOT consume the allowance, and a second scan after the
                          files are corrected succeeds; a second scan issued before the delay passes
                          is refused with `try-again-at`; and a client holding only the ordinary
                          `font-catalogue` capability is refused the admin interface entirely
        who PAYS for    THE REQUESTER, BY CREATING THE BACKING ITSELF (decided 2026-09-01 as a
          a RESOLVE       "sponsor handle", corrected 2026-09-02 to the shape this kernel can
                          actually express - see below). RESOLVE hands
                          back as much as a 16 MiB face, and this file elsewhere says the catalogue
                          holds only small metadata and that allocations are charged to the calling
                          application's Domain. Both cannot be true of a `MemoryObject` the catalogue
                          or StorageService creates: in this kernel the object is charged to the
                          Domain that CREATED it and stays charged until the last reference goes, and
                          transferring the handle does not re-charge it. One connection issuing
                          repeated RESOLVEs and holding the results would grow the catalogue's charge
                          without limit, bounded by neither the client count nor the metadata
                          ceilings.
                          So the backing is the CALLER'S, AND IT IS THE CALLER THAT CREATES IT -
                          not a "sponsor handle" naming the caller's Domain (corrected 2026-09-02).
                          The sponsor was not a capability this system has: `SYS_MEMORY_OBJECT_CREATE`
                          takes a SIZE and charges `current_thread().domain()`, there is no
                          allocate-in-the-Domain-this-handle-names operation, and an ordinary
                          application is not even handed a handle naming its own Domain - the Domain
                          syscalls are create, kill and stats. So the gates below could not have been
                          implemented as written, and the fix is not to invent that syscall: an
                          allocate-into-another-Domain primitive is a serious new authority and this
                          milestone is a font catalogue.
                          The shape that works with what exists is two operations:
                            RESOLVE-INFO   takes the face identity and answers the byte length of the
                                             face, plus the identity and generation that answer is
                                             about. It allocates nothing
                            RESOLVE-INTO   takes the same identity and generation and a MemoryObject
                                             handle the CALLER created and passes with the request.
                                             The catalogue maps it, fills it, unmaps and CLOSES its
                                             handle before replying; it never retains one
                          HOW THE HANDLE IS ATTENUATED, BECAUSE THE FIRST ANSWER NAMED RIGHTS THE
                          TRANSPORT CANNOT DELIVER (corrected 2026-09-02). This said the handle
                          arrives with map and write and neither `RIGHT_DUPLICATE` nor
                          `RIGHT_TRANSFER`. A generated typed call passes its request handles through
                          `ChannelTransport::call` to `send_caps_blocking`, and that path is
                          `SYS_CHANNEL_SEND_CAPS`, which takes each handle with `Rights::TRANSFER`
                          REQUIRED, CONSUMES it from the sender's table, and delivers it with its
                          rights unchanged. So a handle without `TRANSFER` cannot be sent at all, and
                          the only attenuating primitive - the single-handle
                          `SYS_CHANNEL_SEND_ATTENUATED` - is not what the typed transport uses.
                          The attenuation happens at the CALLER, before the send, with a primitive
                          that already exists: `SYS_HANDLE_DUPLICATE` takes a rights mask and refuses
                          anything the original does not already hold, so it narrows and never
                          widens. The caller creates the object (holding every right, and charged for
                          it), duplicates a handle down to exactly `MAP | WRITE | TRANSFER`, and
                          passes the COPY. It keeps the original, so the send consuming the copy
                          costs it nothing.
                          What that buys, stated exactly rather than optimistically:
                            no `RIGHT_DUPLICATE`, so the catalogue cannot make a second handle and
                              keep it after closing the one it was given - which is the property the
                              retained-face gates actually rest on;
                            `RIGHT_TRANSFER` IS present, because the transport consumes handles that
                              way and nothing here can change that. The catalogue must not pass the
                              object on, and that is a code obligation with a gate rather than a
                              right it lacks. Said plainly, because a plan that claims a rights bit
                              it cannot obtain is a plan whose security argument is decoration;
                            map and write and no read, so a service that is filling a buffer cannot
                              read what the caller had in it.
                          AND THE RESULT IS NOT A READ-ONLY OBJECT, WHICH TWO OTHER CLAUSES SAID IT
                          WAS (corrected 2026-09-03). `SYS_MEMORY_OBJECT_CREATE` installs the
                          creator's handle with `Rights::ALL` and `SYS_HANDLE_DUPLICATE` narrows only
                          the NEW handle - it removes nothing from the original and revokes no
                          existing mapping - and this kernel has no seal for a `MemoryObject`. So
                          after RESOLVE-INTO the caller still holds a writable handle to the bytes it
                          asked for, and attenuating the catalogue's temporary copy does not change
                          that. Two clauses below called the result "a read-only MemoryObject" and
                          one required "an immutable validated backing"; none of the three was
                          obtainable this way, and a plan that claims a property the kernel will not
                          give it is the decoration this file already refuses elsewhere.
                          WHERE IMMUTABILITY ACTUALLY COMES FROM, and it is not the kernel:
                            the TRANSPORT BUFFER is the caller's, writable by the caller, and that is
                              all it is. It is not the face's backing and nothing derived may point
                              into it.
                            the FONT LIBRARY, in the client's own address space, TAKES OWNERSHIP of
                              the validated bytes before it parses them - it copies them into
                              library-owned storage, and every decoded structure references THAT.
                              After the copy the caller may write the transport buffer freely and
                              nothing the library holds changes, which is the property "no decoded
                              structure holds a raw reference into bytes that can move" was reaching
                              for. Said plainly: this is an in-process OWNERSHIP rule, enforced by
                              the library's API taking the bytes rather than borrowing them, and not
                              a right the kernel withholds.
                            the IDENTITY IS THE CATALOGUE'S, never derived by the client from a
                              buffer it can write. RESOLVE-INFO answers the identity and generation
                              for the face the catalogue read from the FILE, and a client that
                              rewrites its own buffer has changed nothing about what that identity
                              names. Two loads of the same bytes are one face because the CATALOGUE
                              says so.
                          What is left un-defended is exactly one thing and it is named: a client can
                          corrupt its OWN parse by writing the buffer between RESOLVE-INTO and the
                          library taking ownership. That is a caller harming itself inside its own
                          Domain, which this system does not undertake to prevent anywhere else
                          either, and no other client, the catalogue, or any cache is affected by it.
                          Its gate: a fixture writes the transport buffer after the library has taken
                          ownership and the face's decoded data, identity and cache entries are
                          unchanged.
                          THE RACE BETWEEN THE TWO IS CLOSED BY THE GENERATION, not by trust: INTO
                          carries the generation INFO answered about and is a typed refusal when it
                          is no longer current - the caller re-asks. An object too small for the face
                          is the same typed refusal, with the required length, rather than a
                          truncated face.
                          AND THE GENERATION ALONE DOES NOT CLOSE IT, BECAUSE THE GENERATION MOVES
                          LATE (corrected 2026-09-03). This said a replacement between the two calls
                          "changes its identity and the published generation", which is true only
                          once the catalogue has PROCESSED a `volume.watch` hint and finished a
                          rescan - and this file says in as many words that the watch is a hint whose
                          events can be DROPPED. The catalogue holds metadata and identities rather
                          than face bytes, so an INTO admitted under the still-current OLD generation
                          reads the file as it is NOW: with a dropped or unprocessed hint it returns
                          the REPLACEMENT's bytes under the previous content identity, and a client
                          that keys decoded data and glyph-cache entries on that identity then holds
                          two different faces under one name. The generation is a fence against a
                          replacement the catalogue has SEEN, and the hole is the one it has not.
                          SO THE BYTES ARE CHECKED AGAINST THE IDENTITY THEY ARE SERVED UNDER. INTO
                          digests what it actually read into the caller's object and compares it with
                          the identity the catalogue published for that face. Equal, and the reply
                          stands. Different, and it is the same typed staleness refusal the
                          generation check gives - the caller re-asks - and the catalogue rescans
                          immediately rather than waiting for a hint that may never arrive. The cost
                          is one hash over the face at resolve time, paid once per resolve and never
                          per glyph, and it needs no new state: the identity is already published and
                          the bytes are already in hand.
                          ITS GATE, and it is the case the generation cannot reach: replace a face
                          AFTER its INFO and BEFORE the watch is processed - including the dropped-
                          hint case, driven by not delivering the event at all - and require INTO to
                          refuse rather than return the new bytes under the old identity, and the
                          following INFO to answer the new identity at a new generation.
                          The bytes are the caller's from the moment they exist, and a caller that
                          retains many faces exhausts its own quota rather than the font service's.
                          ITS GATES: a client that retains N resolved faces sees its own Domain's
                          memory rise and the catalogue's flat; a client that exits releases every
                          face it held without the catalogue acting; a RESOLVE-INTO whose object is
                          too small, whose generation is stale, or whose handle is the wrong type or
                          short of the rights the operation needs, is a typed refusal in each case
                          and never a fallback to the service's own memory; and the catalogue holds
                          no client MemoryObject handle across a reply, watched by a fixture that
                          resolves a face and then requires the service's handle count to be
                          unchanged.
        capability      an application holds a `font-catalogue` client and nothing else. It never
                          holds StorageService authority and never scans a directory; a client
                          without the capability reaches no face at all
        what it PARSES  NOTHING - THE METADATA IS STAGED BESIDE THE FACE (decided 2026-09-01).
                          This item is ordered before everything else and must publish family, style,
                          axes, face index and format, and recompute them when a face is replaced.
                          Getting those from OpenType or TTC bytes is PARSING, and parsing is a later
                          item that this file forbids before the profile is frozen - which this item
                          is the prerequisite for. As written the order required a second,
                          unprofiled, hostile-input parser inside the catalogue in order to unblock
                          the real one.
                          So the catalogue's normative input is a STAGED METADATA SIDECAR - and WHO
                          PRODUCES IT IS THE WHOLE QUESTION, WHICH "the staging tool where a parser
                          is allowed" DID NOT ANSWER (corrected 2026-09-02). That phrase moved the
                          parser rather than removing it: family, style, axes, face index and format
                          come out of OpenType and TTC bytes, so a tool that DERIVES them is a font
                          parser - unprofiled, unnamed, unversioned, with no hostile-input contract
                          and no truth oracle - written before the item that authorises parsing, and
                          digest equality proves only that a record names the bytes beside it and
                          nothing about whether they were decoded correctly.

                          THE RECORD IS DECLARED, NOT DERIVED. A face enters this image because
                          somebody added it to the source tree, and its record is a CHECKED-IN
                          declaration beside it - family, style, axes, face index, format - authored
                          once by whoever added the font, in the same review. The staging tool
                          VALIDATES and DIGESTS: it refuses a face with no declaration, a declaration
                          that encodes past `MAX_FACE_METADATA_BYTES`, a declaration whose field
                          values are outside the closed vocabularies this milestone freezes, and a
                          declaration whose digest does not match the face beside it. It parses
                          nothing, so there is no second parser anywhere in this milestone and the
                          ordering is honest for the first time.

                          ITS ORACLE ARRIVES WITH THE REAL PARSER, AND IS OWED BY IT. A declaration
                          can be WRONG - a hand-authored family name that the font does not carry -
                          and nothing above catches that. So the parser item below gains a gate of
                          its own: parse every staged face with the profiled parser and require the
                          fields it recovers to EQUAL the declaration. Until that gate exists the
                          declarations are trusted-by-review, which is stated here rather than
                          implied, and after it they are checked by the only component this file
                          allows to read a font. That is the ordering the closed profile exists to
                          protect: nothing parses before the profile is frozen, and the first thing
                          the profiled parser does is check the facts that were taken on trust.

                          The catalogue reads records and digests bytes; it never looks
                          inside a font. A face present with no record, or a record whose digest does
                          not match the face beside it, is NOT PUBLISHED and is reported - which is
                          also the only sane answer for a file dropped into the destination by hand.
                          AND THAT IS NOT "A RUNTIME DROP CANNOT SMUGGLE METADATA PAST REVIEW", WHICH
                          IS WHAT THIS SENTENCE CLAIMED (corrected 2026-09-03). What the runtime
                          checks establish is digest AGREEMENT between a record and the bytes beside
                          it, closed vocabularies, and the size ceiling - hashing proves association,
                          not truth. The parser oracle above runs over every STAGED face and a
                          runtime replacement never passes it, so a digest-consistent record can
                          declare a false family, style, axis set, face index or format and steer
                          fallback and resolution with it. The boundary is stated instead of
                          overclaimed:
                            WHO CAN DO IT      whoever holds a WRITABLE client to the fonts
                                                 directory. The catalogue's own client is read-only,
                                                 so a runtime replacement is a privileged
                                                 installation action and is trusted exactly as far
                                                 as that writer is
                            WHAT IS CHECKED    at runtime: the digest, the vocabularies, the ceiling.
                                                 At staging: those plus the parser oracle, which is
                                                 the only thing that checks whether a declaration is
                                                 TRUE
                            THE ONE RULE THAT  a runtime replacement of a face that is already
                              CLOSES THE         published MAY NOT CHANGE ITS DECLARED IDENTITY -
                              RELABELLING        family, style, axes, face index or format. A
                              CASE               replacement is a new version of the same face; a
                                                 different identity is a different face and arrives
                                                 under its own name. A replacement whose declaration
                                                 differs in any of those fields is REFUSED and the
                                                 refusal is reported. That is checkable with no
                                                 parser, it costs one comparison, and it is exactly
                                                 the attack it exists for: relabelling an installed
                                                 face to capture somebody else's fallback
                            WHAT "REFUSED"     THE PUBLICATION IS WITHDRAWN AT A NEW GENERATION, and
                              MEANS, AND IT      the replacement is not published either (corrected
                              IS NOT "THE OLD    2026-09-03). This said the PREVIOUS generation stays
                              BYTES ARE          current and that a client resolving across the
                              SERVED"            refusal gets the previous face's bytes and identity,
                                                 which this design cannot do: the catalogue holds
                                                 metadata and identities and NOT face bytes, the
                                                 privileged writer has already overwritten the only
                                                 file it can read, and `RESOLVE-INTO` digests what it
                                                 read against the published identity and would refuse
                                                 those bytes. Promising the old content required a
                                                 snapshot of every published face, which is the
                                                 metadata-only storage model and its ceilings undone.
                                                 So the conservative outcome is stated instead: the
                                                 identity that was published no longer names bytes
                                                 this catalogue can serve, so it is WITHDRAWN, the
                                                 generation moves, and the name disappears from `LIST`
                                                 and from fallback rather than remaining as an entry
                                                 that can never be resolved. A client holding the old
                                                 generation gets the ordinary stale refusal and
                                                 re-asks; one asking by name finds no such face.
                                                 A PRIVILEGED WRITER CAN ALREADY DELETE THE FILE, so
                                                 denial is inherent in this trust model and costs
                                                 nothing new; CAPTURE is what must not happen, and
                                                 withdrawal prevents it. That is the whole difference
                                                 between the two outcomes.
                            AND THE SAME RULE  any rescan that finds a published face's bytes changed
                              GOVERNS EVERY      in a way the catalogue must reject - a sidecar whose
                              REJECTED           digest no longer matches, a declaration outside the
                              REPLACEMENT        closed vocabularies, a record past the size ceiling -
                                                 withdraws that publication at a new generation and
                                                 reports it. One outcome for every rejection, because
                                                 the reason differs and the state it leaves does not:
                                                 the identity names bytes that cannot be served
                            WHAT IS LEFT       a NEW face appearing at runtime carries an unverified
                                                 declaration until the image that ships it is
                                                 staged. It is published and usable; what this file
                                                 does not claim is that its metadata has been
                                                 checked against the font
                          ITS GATE: a runtime replacement whose declaration disagrees with the
                          published face's identity is refused and reported, the face is WITHDRAWN at
                          a new generation, `LIST` no longer offers it, fallback no longer selects
                          it, and no client can resolve the old identity to the new bytes - which is
                          the capture this rule exists to prevent, asserted as something that can
                          actually happen. A client holding the old generation is refused as stale
                          rather than answered.
        the ceiling     THE THREE NUMBERS, FIXED HERE (2026-08-31 - a previous version said "stated
                          as NUMBERS rather than as proportional to what is installed" and then stated
                          none, which bounds exactly as little):

                            MAX_INSTALLED_FACES        64
                            MAX_FACE_METADATA_BYTES    256   (the RECORD's encoded wire bytes)
                            MAX_LIST_REPLY_BYTES     20480

                          64 faces is an appliance's system font set with room to spare and is small
                          enough that the whole catalogue is a fixed array rather than a growing one.
                          256 bytes holds a family name, a style name, the weight/width/slant axes,
                          the face index, the format and the identity digest with margin, and it is a
                          bound the staging gate can check per face without parsing more than the
                          metadata.
                          AND THE REPLY BOUND IS NOT `64 * 256` - THAT NUMBER WAS ARITHMETICALLY
                          IMPOSSIBLE (corrected 2026-08-31). A previous version fixed
                          `MAX_LIST_REPLY_BYTES` at 16384 and said the full 64-face reply fits inside
                          it. The records alone are exactly 16384, and a reply is never only its
                          records: LSIDL adds a list count and a result tag, and a generated service
                          reply carries a correlation id before the result. So the exact-bound gate
                          could not have passed, and the derivation that looked reassuring - "the
                          buffer follows from the other two" - was what hid the missing framing.
                          Two things fix it and both are stated so neither is inferred. First,
                          `MAX_FACE_METADATA_BYTES` is the record's ENCODED WIRE bytes, not its
                          fields' logical sizes, so per-record framing is inside the 256 rather than
                          added to it. Second, the reply bound is `64 * 256` PLUS a reserved
                          4096-byte envelope for the list count, the result tag, the correlation id
                          and any future reply framing - 20480 in total. The envelope is deliberately
                          far larger than today's handful of bytes: a reply bound that has to be
                          recomputed the next time a header gains a field is a bound that will be
                          wrong again, and 20480 is still far inside `MAX_MESSAGE_BYTES`, so a full
                          LIST is one message and needs no paging.
                          The whole service state is therefore bounded by a computable number and a
                          `list` reply fits a fixed buffer.
                          AND THE CEILINGS HOLD AT RUNTIME, NOT ONLY AT STAGING (added 2026-09-01).
                          The destination is replaceable while the machine runs, so a rescan can find
                          a 65th face, or a record that encodes to 257 bytes, after the image passed
                          its staging gate - and the plan said nothing about which of truncating the
                          list, dropping the offending face, tearing the catalogue down or keeping
                          the old generation was meant. The rule is ATOMIC and conservative: a rescan
                          that would exceed any of the three ceilings PUBLISHES NOTHING. The
                          previously published generation stays current and stays served, the failed
                          rescan is reported with what exceeded which ceiling, and the generation
                          does not advance - so a client's view never becomes partial, and a bad
                          drop into the directory cannot take fonts away from a running system.
                          AND "STAYS SERVED" IS A PROMISE THIS RULE CANNOT KEEP FOR A FACE THAT WAS
                          OVERWRITTEN (corrected 2026-09-04). The withdrawal rule in M2 says a
                          rejected replacement of an already-published face - digest mismatch,
                          declaration outside the vocabulary, OR a record past the size ceiling -
                          withdraws that publication at a new generation, and this paragraph said the
                          previous generation stays current and served, naming the same 257-byte
                          record. Both cannot happen. The withdrawal rule is the one that is right,
                          and for the reason it already gives: once the privileged writer has
                          overwritten the file, the catalogue holds no bytes of its own, so
                          `RESOLVE-INTO` would refuse those bytes against the published identity. An
                          entry that can never be resolved is not "served".
                          SO THE RULE IS ORDERED, AND IT IS STILL ATOMIC - one generation switch, or
                          none:

                            1. run the rescan and mark every ALREADY-PUBLISHED face this catalogue
                               can no longer serve. Call that set W. Two ways in, not one
                               (corrected 2026-09-04): a face whose bytes CHANGED and whose
                               replacement must be rejected for any reason, and a face that is GONE -
                               the file removed, or no longer named by the scan at all. The first
                               version of this rule said only "bytes changed", and a removal is not a
                               change: so an operator who deleted one face while adding enough new
                               ones to breach a ceiling fell through to step 3, which kept the
                               previous generation current and STILL LISTING an identity whose only
                               file no longer exists. That is the same impossible "stays served" this
                               whole correction was made to remove, reached by the one door it left
                               open. The test is not what happened to the bytes; it is whether the
                               catalogue can still serve that identity
                            2. if the rescan's admissible set - the previous faces, minus W, plus the
                               new and unchanged ones it accepted - fits ALL THREE ceilings, publish
                               it as a new generation and report what was rejected
                            3. if it does NOT fit, publish the previous generation MINUS W as the new
                               generation: nothing new is added, only what cannot be served is
                               withdrawn, and the report names which ceiling was exceeded. When W is
                               empty this is exactly the old rule and THE GENERATION DOES NOT ADVANCE

                          The conservative property is unchanged and is now true rather than
                          asserted: a bad drop into the directory can never take away a font whose
                          bytes are still there, because step 3 only removes faces the writer itself
                          overwrote, and it can never make a client's view partial, because there is
                          one switch either way.
                          Its gates are the one-past cases at runtime rather than at build, and the
                          record case is TWO cases because the two outcomes differ (2026-09-04):
                            - 65 faces, all NEW: LIST still answers the previous generation
                              unchanged, and the failure is reported;
                            - a NEW face whose record encodes to 257 bytes: the same - previous
                              generation unchanged, failure reported;
                            - a REPLACEMENT of a published face whose record now encodes to 257
                              bytes: that face is WITHDRAWN at a new generation, every other face
                              carries over unchanged, and the failure is reported. This is the case
                              the single old gate got wrong, and it is the one an attacker reaches;
                            - a published face REMOVED while enough new faces are added to breach a
                              ceiling: the removed face is withdrawn at a new generation, nothing new
                              is published, and the failure names the ceiling. Added 2026-09-04 - it
                              is the case where the two rules meet, and the one that shows a removal
                              is not something the ceiling fallback may postpone.
                          BUT "NO PER-CLIENT QUOTA IS NEEDED" WAS WRONG, AND THE THREE CEILINGS ABOVE
                          DO NOT BOUND THE SERVICE (corrected 2026-09-01). They bound what is
                          INSTALLED. A live `SUBSCRIBE` retains endpoint and subscriber state that
                          has nothing to do with how many faces exist, so N applications subscribing
                          is N records this service holds and none of the three numbers says
                          anything about N. The device catalogue met the same question and answered
                          it with a fixed `MAX_SUBSCRIBERS` array, which is the shape to copy:

                            MAX_FONT_CLIENTS       16   live connections
                            MAX_FONT_SUBSCRIBERS   16   live subscriptions, one per client at most

                          Over either bound is a TYPED REFUSAL at the ask, not a silently dropped
                          registration, and a client that goes takes its subscription with it -
                          disconnect cleanup, on the same event the catalogue already uses to give a
                          consumer's place back.
                          Exceeding any of the three at STAGING time fails the image build rather than
                          the boot. Gates: a build staging exactly 64 faces succeeds and its LIST
                          reply is served whole; a build staging 65 fails; a face whose metadata
                          record is exactly 256 bytes succeeds and one at 257 fails; and the
                          full-64-face LIST reply is asserted to be at most 20480 bytes, watched to
                          fail against a raised per-face bound.
                          AND THE FRAMING IS ASSERTED ON A WORST-CASE FIXTURE, NOT ON THE ORDINARY
                          ONE (corrected 2026-09-01). A previous version put a `> 16384` assertion on
                          the ordinary 64-face reply, reasoning that a reply fitting inside its
                          records alone is one whose framing was not counted. That is true only when
                          every record IS 256 bytes, and 256 is a MAXIMUM: a legitimate catalogue of
                          64 shorter faces encodes below 16384 including framing and would have
                          failed the test. The assertion was written to catch an arithmetic mistake
                          and reintroduced one - the same mistake, in fact, of treating a bound as
                          the value.
                          So the framing check gets a fixture of its own: SIXTY-FOUR records each
                          encoding to exactly 256 bytes, whose reply must be strictly larger than
                          16384 and at most 20480. That is the case where the records alone fill the
                          payload, so anything above it is framing and the assertion means what it
                          says. The ordinary 64-face reply keeps only the `<= 20480` bound, which is
                          the one that holds for every legal catalogue;

  - the CAPABILITY PATH by which an ordinary application reaches system faces - the `font-catalogue`
    client above, resolving through RESOLVE-INFO and RESOLVE-INTO into a buffer THE CALLER CREATED
    AND OWNS (corrected 2026-09-03 - this said "a read-only MemoryObject", which that path cannot
    produce; see the RESOLVE row for where immutability is actually obtained), never an ambient
    directory scan and never StorageService authority handed to a text client; and
  - the UPDATE AND INVALIDATION rule: what a replaced or removed face does to the catalogue
    generation, to derived glyph state, and to a client holding the previous face.
  Its gates: a staged face is enumerable through the catalogue and reachable through the capability
  path; a manifest-declared consumer reaches it through its `Factory` role and an ordinary
  application through the granted `font-catalogue` capability, so BOTH routes are exercised rather
  than only whichever one the first consumer happens to use; TWO applications hold the capability at
  once and each receives its own replies to interleaved LIST and RESOLVE calls - the case a granted
  duplicate would fail and the reason the grant mints a sub-connection; the client and subscriber
  ceilings are tested at their exact bound and one past it, with the over-bound ask answered by the
  typed refusal, and a client that disconnects gives its subscription back; an application without that capability
  cannot reach any face, watched to fail; the three ceilings have the exact-bound and over-bound
  build and reply fixtures listed with them; a replaced face changes the identity and generation and
  invalidates what was derived from it; and the fallback item below enumerates the installed set
  through this owner rather than a path.
  It is placed first in this list because that is its order, not only its description: every item
  after it needs a staged face, and the guest gate at the end of this file loads one.

  PROGRESS 2026-09-12, AND THE ITEM STAYS OPEN. Its DECISION CORE is built and host-tested in
  `service-logic`, where a pure decision can be judged without a boot:
  - `font_record` is the staged metadata sidecar: the closed vocabularies frozen (format, width,
    slant, the 1..=1000 weight range, the axis tag and its ordered design range), the declaration
    read as TEXT because a person authors it in the same review that adds the face, the encoded
    WIRE size bounded by `MAX_FACE_METADATA_BYTES`, and the digest compared against the bytes beside
    it. It parses no font, which is the ordering this milestone protects; its truth oracle is still
    owed by the profiled parser, and the module says so rather than implying it.
    `declares_same_identity_as` is the relabelling comparison - family, style, format, face index and
    axes, deliberately not the digest, because a replacement always changes the bytes.
  - `font_scan` is the ORDERED publish/withdraw rule, all three steps, with the four runtime cases
    the ceilings name: 65 new faces publish nothing and leave the previous generation current; a new
    record past the metadata ceiling does the same; a REPLACEMENT past it withdraws that face at a
    new generation while every other face carries over; and a removal is withdrawn even when the
    scan breaches a ceiling. A valid no-change scan publishes nothing and spends no generation, and
    the worst-case reply fixture is sixty-four records at exactly 256 bytes, which is where the
    framing assertion means what it says.
  - `font_clients` is the per-client half the face ceilings do not bound: sixteen live connections,
    sixteen subscriptions, one per client, each tested at the exact bound and one past it, with a
    disconnect giving back the place AND the subscription on one event.
  - `idl/font.lsidl` is the PROTOCOL, frozen and generated like every other package: `font-catalogue`
    with LIST, RESOLVE-INFO, RESOLVE-INTO and SUBSCRIBE and no more, and `font-catalogue-admin` with
    RESCAN beside it. The closed vocabularies are the same ones the record module validates, the
    record's bounds are in the schema (`family` 64, `style` 32, eight axes, a 32-byte digest, a LIST
    reply of at most 64 records), RESOLVE-INTO takes the caller's memory object with a `@rights(map,
    write)` guard that the generated dispatch enforces before the service is called, and the typed
    refusals carry what a caller needs to retry - the current generation for a stale one, the
    required length for a buffer too small. `font-proto` is generated from it and the aggregate
    re-exports it; `./gen.sh --check` reports no drift.
  - `font_rescan` is the recovery allowance: one scan in flight, every COMPLETED scan - published,
    unchanged or failed - re-arming the delay, a request inside it refused with `try-again-at`, and
    a failed scan spending no publication allowance so the scan after a correction still runs.
  28 fixtures, 28 passed, inside a suite of 445.
  AND THE SERVICE ITSELF, 2026-09-12. `font_catalogue` is a `role = "service"` program with the three
  roles this item names: the `FONTDIR` read-only client ServiceManager mints over the canonical
  destination, the `ADMIN` serve root SEEDED into its serve set so its channel value is knowable, and
  the ordinary `SERVE` root. It holds metadata and identities and no face bytes; it parses no font;
  `resolve-into` maps the caller's object, fills it, unmaps and CLOSES the handle on every path
  including every refusal, and digests what it read against the published identity so a dropped watch
  hint cannot serve a replacement's bytes under the previous identity. `subscribe` is intercepted
  before the generated dispatch because a live stream's producer has to outlive the reply.
  THE TWO CONSUMER EDGES ARE BUILT AND HELD APART. `font-catalogue` is minted FRESH per grant -
  joining `config`, `device` and `network` rather than the duplicate default, for the reply-queue
  reason this file gives - and `font-admin` is the catalogue's ADMIN ROOT ITSELF, because the service
  recognises an operator by the channel a request arrived on and a sub-connection would arrive on one
  it does not know. `lsfont` is the holder: it lists through the ordinary client and its one
  privileged operation is `--rescan`.
  THE DESTINATION IS FROZEN IN THE VALIDATOR: `share/fonts` admits a face (`.ttf`, `.otf`, `.ttc`) or
  the declaration for one (`sans.ttf.face`), directly under it and nowhere deeper. Its fixture found
  a hole in the first rule - a file called `.face` passed - so the declaration must now name a face,
  which makes the sidecar convention checkable rather than a habit.
  AND THE SHARED IMAGE REFUSED THE OBVIOUS ROUTE, which is worth recording: a tool instantiating the
  generic transport monomorphises the whole codec into itself, and the inventory names the residual
  symbol. `font-client` and `font-client-provider` are the convention every other interface follows.

  WHAT REMAINS. The guest gates, the conformance corpus, the staging tool's validation pass - and the
  PINNED LAST-RESORT FACE, which is the one item here blocked on a decision rather than on work: a
  licensed third-party binary entering the tree under the dependency policy, with an upstream
  revision recorded, is a reviewed import. The catalogue starts and serves an empty publication
  without one (the scope mint succeeds over a directory that does not exist yet, so this is not a
  boot hazard), and every gate that matters needs a face to be meaningful.

  **AND THE CLOSED PROFILE IS FROZEN BEFORE ANY PARSER IS WRITTEN** (corrected 2026-08-30). A
  previous round promised exactly that and then left the hostile-font parser item AHEAD of the item
  that publishes the profile, with no gate between them - so the exact table versions, lookup types,
  variation and colour subsets and supported scripts could still be chosen after parsing had begun,
  which is the deferral the closed profile exists to prevent. The profile item now stands between
  this one and the parser, and its publication is a START GATE: no parser work begins until the
  profile is published and frozen, and a later change to it is a change to the parser's conformance
  set in the same edit.

  **WHERE THE WORK LIVES: PARSING AND CACHING ARE IN-PROCESS; THE CATALOGUE IS A SERVICE**
  (2026-08-30). An earlier version said "IN-PROCESS IS THE ANSWER unless the catalogue forces
  otherwise", which made the accounting model conditional on a component nobody had designed. The
  catalogue is designed above, so the condition is gone and the two halves are separated:
  - PARSING, SHAPING AND THE GLYPH AND SHAPING CACHES are in-process, in the calling application.
    Every allocation is charged to the caller's Domain automatically, which is what this file
    promises, and a pathological document harms only the process that asked for it.
  - THE INSTALLED-FACE CATALOGUE is service-owned, because deterministic fallback has to enumerate
    the same set for every client and a per-process view of "what is installed" is not a set. It
    serves METADATA and identities - not decoded faces, not caches - so what it holds is bounded by
    the face-count and metadata-size ceilings the item above states, which is a computable number
    rather than "proportional to what is installed". That bounds what it holds ABOUT FACES and
    nothing about clients: the live-connection and subscriber ceilings above are what bound the
    per-client half, and this sentence used to say no per-client quota was needed (corrected
    2026-09-01, with the ceilings that replace it).
    A client that wants a face receives font bytes in a buffer of its own and parses them itself, in
    its own Domain; the library takes ownership of those bytes before parsing, which is what makes
    the parsed result immutable (corrected 2026-09-03 - "a read-only MemoryObject" was not
    obtainable).
  That split is what makes the accounting promise true without a quota system: the unbounded work is
  in-process, and the shared thing is small and read-only.
  Script and language are explicit inputs - a BCP-47 value - and any SYSTEM DEFAULT depends on
  `P02M0105`, which does not exist yet. Until it does there is no default and the caller states the
  language; an implicit locale would be policy this architecture deliberately does not have.

- [x] **Freeze ONE shared font-resource contract before either side implements against it.** Owned
  jointly with `P02M0103b` and written once. It states:
  a content-derived face identity plus face index, ISSUED BY THE CATALOGUE FROM THE FILE rather than
  computed by a client from a buffer it can write, so two loads of the same bytes are one face and a
  changed file is a different one; ownership of the validated backing, which the LIBRARY takes by
  COPYING the bytes into storage it owns before parsing - the only immutability transition available,
  since the kernel has no seal and the transport buffer belongs to the caller (corrected 2026-09-03) -
  and the rule that no decoded structure holds a raw reference into bytes that can move; bounded access to decoded
  resources - outline, grayscale/subpixel mask source, bitmap strike, `COLR` v0 layers and v1 paint
  graph - as explicit resource forms rather than a category; lifetime, generation and invalidation,
  so a replaced face invalidates what was derived from it; and that a layout result is a SEQUENCE OF FACE-,
  SCRIPT- AND DIRECTION-HOMOGENEOUS `GlyphRun`s - unconditionally, with the per-glyph-face
  alternative rejected rather than deferred (corrected 2026-08-30). A previous version chose it
  "unless a measurement says otherwise" and named no measurement, no threshold, no owner and no
  gate, while the pipeline and fallback items below already assume homogeneous runs - so a later
  measurement nobody was scheduled to take could have changed a shared ABI after one side had
  implemented against it. Homogeneous runs are the answer because they match fallback's output and
  `render2d`'s per-run paint and cache key; a per-glyph face reference would put a face switch inside
  a structure whose whole purpose is to be paintable in one operation.
  THE GLYPH CACHE KEY IS PART OF THIS CONTRACT AND IS NOW WRITTEN IN BOTH FILES (2026-08-30). It is
  face IDENTITY, FACE INDEX and GENERATION, glyph, size, variation coordinates, transform, subpixel
  phase, glyph kind, strike and palette where the kind uses them, and the RASTERISATION MODE -
  which for an LCD
  mask is the `SubpixelLayout` it was rasterised for (added 2026-08-30: `P02M0103` requires LCD
  output to differ by RGB/BGR and horizontal/vertical layout and to fall back to grayscale for
  unknown layouts, rotated transforms and transparent offscreen layers, so without it one cached
  subpixel mask is reused across modes that must not share one). `P02M0103`'s renderer item carries
  the same
  definition verbatim rather than a summary of it - a previous round said it "is corrected in the
  same change" and the change was never made, which left the two sides of one seam specifying
  different keys. A change to this key is a change to both files in the same edit, and each side has
  a negative test proving that two entries differing only in variation coordinates, generation, kind,
  strike, palette, rasterisation mode or FACE INDEX do not collide. The index is explicit rather than
  implied by "identity" (added 2026-08-31): a TrueType or OpenType COLLECTION is one file, so every
  face in it shares the content-derived identity, and two of them can carry the same glyph index
  meaning different glyphs with every other key field equal. The shared resource contract already
  defines a face as identity PLUS index; the key has to say so too, or the contract and the cache
  disagree about what a face is.

  DONE 2026-09-12, AS CODE RATHER THAN AS A PARAGRAPH IN TWO PLANS. `src/user/libs/text/font-contract`
  is the seam: no dependencies, `no_std`, host-testable, so both sides can link it and neither
  restates it. It carries exactly what this item says the contract states.
  A face is a content-derived FILE identity plus a face INDEX, issued by the catalogue and never
  computed by a client from a buffer it can write. `FaceBytes` is the ownership rule expressed as a
  TYPE - its only constructors TAKE or COPY the bytes, so a parser cannot borrow from the transport
  buffer the caller still owns and can write. The decoded forms are the six the seam names - outline,
  grayscale mask, subpixel mask, bitmap strike, `COLR` v0 layers and v1 paint graph - enumerated
  rather than left as a category. The generation is carried by everything derived from a face, so a
  replacement invalidates it. A layout result is a sequence of face-, script- and
  direction-homogeneous `GlyphRun`s, and the type carries the face ONCE, so a run of two faces is
  unrepresentable rather than discouraged. The units are the fixed ones: UTF-8 byte offsets in
  LOGICAL order, 26.6 with round-half-to-even at the single conversion site, overflow a typed refusal
  and never a saturation, the baseline origin with +y down.
  THE CLUSTER MAPPING IS THERE TOO, with its invariants CHECKED rather than trusted: clusters in
  logical order and non-overlapping, inside the source, a visual order that is a PERMUTATION, and
  every ligature caret slice inside its storage. `selection_is_contiguous` is the question a bidi
  selection actually asks, and a fixture shows a contiguous range of text that is two pieces on
  screen.
  THE CACHE KEY IS ONE DEFINITION with eleven fields, and its fixture is the field-by-field negative
  case: eleven pairs differing in exactly one of file, face index, generation, glyph, size, variation
  coordinates, transform, subpixel phase, kind, strike and palette, plus all five rasterisation modes
  pairwise - and the positive case beside them, because everything else would also pass for a key
  that never matched itself. 11 fixtures, 11 passed.
  WHAT IS NOT DONE HERE, and it belongs to the items that implement the two sides: the renderer's own
  glyph cache, and the shaping stack that fills a run. Both will key on this type rather than on a
  definition of their own, which is what makes them unable to disagree.

- [x] **Publish a CLOSED OpenType profile, and refuse what is outside it.** "Parse `CFF`/`CFF2`" and
  "correct at any coordinate" cannot both be met by a table list: producing outlines from `CFF`/`CFF2`
  needs a bounded Type 2 / CFF2 charstring interpreter with subroutines, variation-store selection
  and `blend`; and correct METRICS at a non-default coordinate needs `HVAR` for horizontal advances -
  which is also what varies CFF2 advances - and `MVAR` for the font-wide metrics line layout reads.
  Neither appeared in the table list while both were promised by the requirement above it.
  The profile is a document with a version, and it states: the exact table versions supported;
  `GSUB`/`GPOS` lookup types and flags, including `FeatureVariations` and device/variation
  adjustments; the `COLR` v0/v1 and variable-paint subset; the bitmap formats
  (`sbix`/`CBDT`/`CBLC`); the variation mechanisms (`fvar`, `gvar`, `avar`, `HVAR`, `MVAR`, and CFF2
  variation behaviour); and the finite list of supported scripts and languages. Anything outside it
  is a typed `Unsupported` refusal, not undefined behaviour and not a silent wrong result.
  "Indic" is not a profile entry. Name the scripts.

  DONE 2026-09-12, AS CODE. `src/user/libs/text/opentype-profile` is the list; `docs/gen/opentype/
  profile-1.md` is generated from it with a SHA-256 over the canonical form, so a change to the
  profile is a line in a diff rather than something a reviewer might notice. 29 tables with the
  major versions each is admitted at, 6 exclusions, all 8 `GSUB` and all 9 `GPOS` lookup types with
  their subtable formats, the 6 lookup flags, the shared structure formats including `cmap` 14, the
  conditional mechanisms (`FeatureVariations`, `ConditionSet`, `Device`, `VariationIndex`), 12
  variation mechanisms, the 18 `COLR` v1 paints with the 28 composite modes and the two bitmap
  families, 32 named scripts and 19 languages.
  BOTH HALVES OF "CORRECT AT ANY COORDINATE" ARE IN IT, which is what this item was written about:
  the charstring interpreters with subroutines and CFF2 `blend` on the outline side, and `HVAR`,
  `VVAR`, `MVAR`, the item variation store and the delta-set index map on the metric side. A fixture
  asserts both rather than trusting the list.
  THE EXCLUSIONS ARE ENUMERATED WITH THEIR REASONS - SVG outlines, `kern`, AAT, monochrome strikes
  as a glyph source, `avar` 2 and the hinting programs - because a reader cannot tell a deliberate
  omission from a forgotten one by looking at what IS supported. Each names the TAGS it refuses, so
  "nothing is both supported and excluded" is an exact check: `avar` 2 is excluded while `avar` is
  supported, and a scan of the prose could not have told those apart.
  "INDIC" IS NOT AN ENTRY: every script is named with the shaping CLASS it needs - default,
  cursive-joining, indic-reordering, universal, khmer, myanmar or hangul - which is what makes
  "supports Tamil" checkable. A script outside the list is a typed refusal; an unknown `LangSys`
  falls back to `dflt`, which is the one fallback in the profile and is stated as such.
  ITS FIXTURES FOUND THREE DEFECTS IN THE LIST ITSELF before the gate was written: a paint rule that
  admitted a format 33 above `PaintComposite` (not every paint has a variable counterpart), an
  exclusion check that read `avar` 2 as excluding `avar`, and a language entry that said nothing
  about what it changes. The model was corrected rather than the tests.
  The gate is `src/tools/check-opentype-profile.sh`, registered in `check.sh` and in the
  verification catalog. 9 fixtures, 9 passed.

- [ ] Parse OpenType and TrueType fonts with every length, offset and index checked before use, and
  treat a font file as HOSTILE INPUT throughout. AND THIS ITEM OWNS THE CATALOGUE'S TRUTH ORACLE
  (added 2026-09-02): the catalogue publishes DECLARED metadata, checked in beside each face and
  validated but never derived, because deriving it would be a second unprofiled parser written before
  this item authorises one. So the first thing this parser is asked to do is check those
  declarations - parse every staged face and require the family, style, axes, face index and format
  it recovers to EQUAL the declared record, with a disagreement failing the gate and naming the face
  and the field. Before this gate the declarations are trusted by review; after it they are checked
  by the only component allowed to read a font - a font is untrusted content that arrives from a
  document, a download or a package, and font parsers are a classic memory-safety target. The tables
  are the ones the published profile below names. Every parser is fuzzed and every malformed table is
  a typed refusal rather than a panic or a wild index.

  PROGRESS 2026-09-12, AND THE ITEM STAYS OPEN. `user/libs/text/font-parse` reads a font as hostile
  input: no `unsafe`, no allocation, no panic path, one bounded reader every byte goes through, and
  `checked_add` on every offset - an offset plus a length that wraps is the shape of a font crafted
  to read somebody else's memory, and on a 32-bit target it wraps with values a `u32` table holds.
  Refusals are TWO kinds kept apart: `Malformed` names the table and what contradicted itself,
  `Unsupported` carries what was outside the profile - a broken font and a font for another system
  are different answers and a caller acts on them differently.
  WHAT IT READS TODAY: the table directory (single faces and collections), `head`, `hhea`, `maxp`,
  `hmtx` including the side-bearing run the format compresses the tail into, `loca` in both forms,
  `glyf` simple and composite outlines, and `cmap` formats 4, 6 and 12.
  THE OUTLINE WALK IS CONSTANT-STACK, and that is not a micro-optimisation: the obvious
  implementation decodes the compressed flags into an array and then reads the two coordinate arrays,
  which is a hundred and ninety kilobytes of stack at the format's own maximum point count IN A
  FUNCTION THAT RECURSES for composites. That is a stack overflow a crafted font can ask for. The
  flags are walked twice instead, and three readers advance in step.
  IT IS FUZZED EXHAUSTIVELY rather than randomly: a font this tree BUILT is read, then every
  truncation of it and every byte of it flipped four ways is put through every path the parser has -
  and what is asserted is that it ANSWERS, because whether a mutated font is still readable is not
  the question. The fixtures found two real defects in the parser's own offsets: `unitsPerEm` read
  the flags field, and `numberOfHMetrics` was read two bytes early, which makes a font declare no
  horizontal metrics at all.
  PROGRESS 2026-09-12: `name`, `OS/2` AND `post` ARE READ, which is what the truth oracle is going to
  compare a declaration against. Also read since: the variation tables in full, `GSUB`/`GPOS` and
  `GDEF`'s classes through the shaper, `GDEF`'s ligature caret list, and the `COLR`/`CPAL` and
  `sbix`/`CBLC` INDICES - enough to say which form of a glyph the face offers, which is a different
  question from decoding that form and is the one a run has to answer.
  A STYLE IS A NUMBER AND A SET OF BITS, NOT A WORD. "Bold Italic" is a string a designer chose, in a
  language they chose it in, and matching on it is how a face called `Fett` or `Gras` stops being
  bold. `OS/2` states the weight as a number, the width as a number and the slope as a bit, and those
  are what a style is compared on. `OS/2` version 0 is refused by name because the profile does not
  admit it: it predates the selection bits and the typographic metrics a style and a line height are
  decided from, so it is a face this system cannot answer the questions about. The VERSION says what
  is there rather than the length - a version 1 table padded out to a later version's size still has
  no x-height, and reading past the version gives two metrics out of somebody's padding.
  A NAME TABLE HAS MANY RECORDS FOR ONE NAME, and taking the first makes the answer depend on the
  order a font tool happened to write them in - two files with identical names would then disagree
  about their own family. The preference is stated and total: Windows English, then Windows in any
  language, then Macintosh English, then anything at all.
  THE UPPER HALF OF MAC ROMAN IS NOT LATIN-1. `0xA5` is a bullet and not a yen sign; reading an older
  face's name as Latin-1 gives a family name that is wrong in a way that looks like a corrupt font
  rather than a wrong decoder. And a UTF-16 name outside the basic plane is a PAIR of units, which a
  decoder taking each unit as a character turns into two replacement characters.
  NO ALLOCATION, WHICH IS WHY A NAME IS A DECODER AND NOT A STRING. The caller owns the buffer, and a
  name too long for it is a REFUSAL: truncating a family name is how two different families become
  one, and the caller cannot tell it happened.
  `post` carries the italic angle - negative for the ordinary forward slant, which is the sign that
  is got wrong - the underline, whether the face is monospaced, and version 2.0's glyph names, which
  are what a PDF exporter writes, an accessibility layer reads back and a subsetter matches on. The
  258 standard Macintosh names are carried because in the file they cost nothing, and a reader
  without them could not name any of the ordinary Latin glyphs at all. Version 3.0 carries no names
  and that is not an error - it is what almost every modern face states.
  26 fixtures in `font-parse`, 26 passed, with the three metadata tables put through the same
  exhaustive corruption sweep as every other table: every byte, four patterns, every accessor.

  WHAT REMAINS FOR THIS ITEM: `CFF`/`CFF2` charstrings, the `COLR` paint graph and the bitmap strike
  DATA - the indices are read, the contents are not - and THE TRUTH ORACLE, which is this item's own
  and cannot be built yet: it parses every STAGED face and requires what it recovers to equal the
  declared record, and nothing is staged until the licensed last-resort face is imported.

- [x] Implement Unicode segmentation over a versioned, generated character-property table rather than
  hand-written ranges: grapheme cluster, word and line-break boundaries, plus the character classes
  shaping needs. State the Unicode version the tables are generated from, pin the source data files
  by SHA-256, and regenerate rather than patch.

  DONE 2026-09-12, AT UNICODE 17.0.0. Sixteen UCD files are pinned by URL and SHA-256 in
  `toolchain.lock` and fetched by `./bootstrap.sh`; `src/tools/ucd-gen` reads the CACHE - never the
  network - and writes the whole of `user/libs/text/unicode-tables/src/generated.rs`: nine property
  tables with their enums, plus `Extended_Pictographic`. The enums are generated BESIDE the tables,
  so an ordinal and its meaning cannot drift apart.
  THE GENERATOR REFUSES TO GUESS. A property value the release has and the generator's list does not
  is an error rather than a silent default - which is how `HH`, the unambiguous hyphen this release
  added, was found instead of being quietly filed under `XX` and never breaking where it should.
  THE CHARACTER CLASSES SHAPING NEEDS are in it too, which is the half a segmentation-only table
  misses: `Joining_Type` for cursive shaping, `Indic_Syllabic_Category` and
  `Indic_Positional_Category` for reordering, `Script` to select the shaper, `General_Category` for
  mark handling, and `Indic_Conjunct_Break` - without which grapheme clustering has been wrong since
  Unicode 15.1, because a Devanagari conjunct breaks in the middle of a letter.
  THE ALGORITHMS ARE UAX #29 AND UAX #14, in `user/libs/text/unicode-segmentation`, written as the
  documents' numbered rules in the documents' order so a reader with the text open can follow them.
  No tailoring: the default algorithm is what the normative file measures.
  MEASURED AGAINST UNICODE'S OWN ANSWERS, in full rather than in a sample - 766 grapheme cases, 1944
  word cases and 19338 line break cases, every one of them passing. `src/tools/check-unicode-
  segmentation.sh` is the gate, registered in `check.sh` and in the verification catalog; it also
  regenerates the tables and compares, because a generated file that has been edited is one that lies
  about where it came from.
  WHAT THE CONFORMANCE RUN CORRECTED, none of which a hand-written test would have found: word
  boundaries applied the ignore-marks rule before the rules that are numbered ahead of it, so a
  space, a combining mark and a space came out as one word; line breaking was wrong in 1077 cases
  until the rules were rewritten from the exact 17.0 set rather than from memory of them; and the
  last 22 were a zero-width joiner at the start of a text, where the rule that prohibits a break
  after one is numbered BEFORE the rule that turns an unattached mark into a letter.

- [x] Implement the bidirectional algorithm - paragraph level detection, embedding levels, isolates,
  overrides, mirroring and reordering. Half a BiDi implementation is worse than none, because it
  renders Arabic and Hebrew text that looks plausible and is wrong.

  DONE 2026-09-12, WHOLE. `user/libs/text/unicode-bidi` implements UAX #9 with every rule named at
  the code that implements it: P2 and P3 for the paragraph level, X1 to X10 for the embeddings,
  overrides and isolates - including the three different ways the directional status stack overflows
  - X9's removals, BD13's isolating run sequences, W1 to W7 for the weak types, N0's paired brackets
  with BD16's canonical equivalences and its 63-pair bound, N1 and N2 for the neutrals, I1 and I2 for
  the implicit levels, L1 for the whitespace reset, L2 for the visual order and L4 for mirroring.
  MIRRORING IS ANSWERED RATHER THAN APPLIED, because which glyph is drawn is the font's business: a
  face may carry its own mirrored form through `rtlm`, and a layer that substituted the character
  would take that decision away from it.
  MEASURED AGAINST BOTH NORMATIVE FILES IN FULL, which is the only way this can be claimed:
  `BidiTest` states class sequences and the paragraph directions each is to be run at - 770241 cases
  - and `BidiCharacterTest` states real code points, which is the only half that exercises the
  bracket rule at all - 91707 cases. Every one of them passes. An implementation that ran one and not
  the other would be wrong in exactly the half it skipped.
  Its 8 fixtures are the readable half: the first strong character deciding, an isolate NOT deciding,
  a number inside right-to-left text going up two levels so it keeps its own order, trailing
  whitespace returning to the paragraph level, a bracket pair taking the direction of what is inside
  it, and L2 reversing from the highest level down rather than one run at a time.

- [x] Implement complex-script shaping over `GSUB`/`GPOS`: feature selection, script and language
  systems, ligatures, contextual and chaining substitution, marks and mark attachment, cursive
  attachment, kerning, and the shapers the profile's named scripts require. Latin alone is the case
  that makes a design look finished and proves nothing.

  PROGRESS 2026-09-12, AND THE ITEM STAYS OPEN - its own last sentence is why. What is built is the
  machinery and the Latin-shaped half of it; the shapers the named scripts require are not.
  `user/libs/text/font-shape` carries: the script and language system selection with its three-deep
  fallback chain (the language asked for, the script's default, then `DFLT`) - a shaper that stopped
  at the first miss renders a Turkish font with no Turkish forms and reports nothing; the feature
  list, including the REQUIRED feature, which is applied whatever the caller asked for; the lookup
  list, `Coverage` in both formats and `ClassDef` in both; `GSUB` types 1, 2, 3, 4 and 7; `GPOS`
  types 1, 2, 4, 6 and 9; and the buffer every rule rewrites in place, where each glyph remembers
  which characters it came from.
  LOOKUPS RUN IN LOOKUP-LIST ORDER, NOT FEATURE ORDER, which is the rule an implementation written
  from the feature list gets wrong: several features name the same lookup, and applying them feature
  by feature changes the result for any font whose features overlap.
  WHAT THE FIXTURES CORRECTED: the advances were read from `hmtx` BEFORE substitution, so a ligature
  came out carrying its first component's width; and a mark was moved back by the advances AFTER its
  base rather than including it, which puts an accent over the next letter on any wide base.
  EXTENDED 2026-09-12, SAME DAY. Every lookup type the profile names is now applied: `GSUB` 1 to 8
  and `GPOS` 1 to 9. The contextual and chaining types share one implementation for all four, because
  they are one structure with two payloads - and the backtrack is read NEAREST-FIRST, which is the
  single thing implementations of them get wrong: read forwards it matches the mirror of what the
  font asked for and fires almost at random. A nested lookup goes back through the driver, which
  bounds the recursion at eight.
  AND THE FIRST TWO SCRIPT SHAPERS. `scripts` carries the CURSIVE one - the joining form each letter
  takes from what it can reach, with marks TRANSPARENT, which is the rule a shaper looking at its
  immediate neighbour gets wrong and which in Arabic breaks the join almost everywhere - and the
  INDIC one's syllable identification and pre-base vowel reordering, which is the writing system
  rather than a font feature.
  A PER-GLYPH FEATURE MASK IS WHAT MAKES A POSITIONAL FEATURE POSITIONAL. `fina` selects the final
  form of a letter and must apply to the last letter of a word and to no other; a shaper that could
  only turn features on globally renders a word made entirely of endings. The mask is set by the
  script shaper before any lookup runs, and a lookup named by two features takes both their masks
  rather than being applied twice.
  THE FIXTURES FOUND A DEFECT IN THE GENERATED TABLES, not in the shaper: `ArabicShaping.txt` states
  in its HEADER that a code point it does not list is transparent when its general category is `Mn`,
  `Me` or `Cf`. Without that derived default every Arabic vowel mark was non-joining, and a mark
  between two letters broke the join.
  DONE 2026-09-12. Every shaping class the profile names now has a shaper, and a fixture checks that
  against the PROFILE'S OWN LIST rather than a list written beside it - so a class added there
  without a shaper here fails rather than rendering a script with the wrong engine.
  `shape_run` is the entry point that makes the rest true: characters in, shaped buffer out, with the
  shaper picked from the characters and its pass run BEFORE any lookup. Everything below it takes
  glyphs and feature tags, which is enough for Latin and enough for nothing else - the joining forms,
  the reordering and the jamo composition all need the characters, and a caller that went straight to
  the feature list would be doing what "Latin alone" means.
  HANGUL COMPOSES rather than substitutes: almost every font carries the eleven thousand precomposed
  syllables and not the jamo arranged into them, so a shaper that left a decomposed sequence alone
  renders three letters where a reader expects one block. MYANMAR reorders by CATEGORY - the medial
  `ra` first, then the pre-base vowel, both written after their consonant and drawn before it. KHMER
  shares the Indic cluster model because its coeng IS an invisible stacker, which the generated
  syllabic categories already say. THE UNIVERSAL one is a cluster model without reordering, and that
  is stated rather than left to a reader to wonder which reordering it forgot.
  THE SCRIPT TAG IS NOT THE UNICODE SCRIPT NAME: OpenType has two tags for most Indic scripts and a
  font written for one carries no features under the other, so the version-2 tag is tried first and
  the fallback chain reaches the old one and `DFLT` on its own.
  AND A FIXTURE FOUND A DEFECT IN THE SYLLABLE MODEL: a medial or subjoined consonant is part of the
  cluster it follows ALWAYS - that is what makes it medial - and requiring a virama before it split a
  Myanmar cluster at its medial `ra`, which then never reached the front.
  15 fixtures, 15 passed.

- [x] **State the pipeline as ONE ordered sequence with typed intermediates, not as a list of
  algorithms.** The items above and below are stages, and their order is normative because plausible
  orders are wrong in ways that look right. In particular BiDi is NOT one paragraph-level reorder:
  UAX #9's reordering is applied PER LINE, after line boundaries are known, so a paragraph reordered
  once and then wrapped is wrong wherever it wraps. The order is:
  UTF-8 validation and the canonical-equivalence rule frozen below; itemisation into grapheme, script and language
  runs; BiDi paragraph level and embedding-level resolution; face fallback, applied
  cluster-atomically; MIRRORING; shaping per face-, script- and direction-homogeneous run; width
  measurement; line breaking; boundary-sensitive reshaping where a break changed a joining or
  contextual context; and finally per-line visual REORDERING.

  **AND THE CANONICAL-EQUIVALENCE POLICY IS PRESERVATION, DECIDED HERE** (2026-09-01). The stage
  above named "canonical-equivalence policy" and chose none, which is a slot rather than a rule: the
  composed and decomposed spellings of one string could take different fallback, shaping and cache
  paths, and two implementations following this file could disagree about every cluster boundary in a
  decomposed run while both claiming to follow it. The rule, in three parts:

    the buffer is       The pipeline NEVER rewrites the caller's bytes. No NFC, no NFD, no NFKC.
      NEVER REWRITTEN     Every offset in a `GlyphRun` - cluster starts, caret positions, selection
                          boundaries - is a byte offset into the ORIGINAL UTF-8 the caller passed.
                          This is the load-bearing half: normalising the buffer and then reporting
                          offsets into the normalised copy is how a text engine returns caret
                          positions that do not exist in the caller's string, and no mapping back
                          survives a decomposition that changes length.
    equivalence is      A canonical composition and its decomposition are ONE grapheme cluster under
      RESOLVED BEFORE     UAX #29, so itemisation and fallback see one indivisible unit either way.
      FALLBACK, NOT       THAT IS NOT ENOUGH ON ITS OWN, AND THE PREVIOUS VERSION STOPPED THERE
      INSIDE SHAPING      (corrected 2026-09-01). Atomicity keeps a sequence together; it does not
                          make two spellings COVER the same. A face may have a glyph for precomposed
                          U+00E9 and no combining acute, or the reverse - so fallback, which runs
                          before shaping, would already have chosen different faces for the two
                          spellings, and the stage allowed to resolve equivalence ran afterwards. The
                          policy and its own regression could not both be implemented.
                          So coverage is asked in a CANONICAL VIEW: a face covers a cluster when it
                          covers the cluster's canonical composition OR its full decomposition, and
                          fallback asks that question rather than asking about the raw bytes. The
                          view is a lookup over the cluster, not a rewrite of the buffer - the
                          original UTF-8 is still what every offset in a `GlyphRun` refers to, which
                          is the rule above and is unchanged. Shaping then applies whichever form the
                          chosen face actually needs. Within a run the shaper applies
                          the canonical composition and decomposition its tables require - which is
                          what OpenType shaping does in any case - and may not change cluster
                          boundaries or the byte spans reported for them while doing it.
    and it is TESTED    A regression pins it: the composed and decomposed spellings of one string
                          produce the SAME glyph sequence and the same total advance, take the same
                          fallback decision, and report cluster byte spans differing only as the two
                          inputs' own byte lengths differ. Run for a Latin case with a combining
                          acute and for one of the profile's non-Latin scripts, because a Latin-only
                          proof of this is the case that always works.

  DONE 2026-09-12. `user/libs/text/text-pipeline` is the sequence, AS TYPES: `Source`, `Items`,
  `Levelled`, `Faced`, `Shaped`, `Measured`, `Lines`, `Visual` - each existing only as the output of
  the stage before it. A pipeline written as a list of functions over one buffer is a pipeline whose
  order is a comment; here "reorder before wrapping" is not a mistake that can be written, because
  `Visual` is reachable from `Lines` and from nowhere else.
  THE CANONICAL-EQUIVALENCE POLICY IS IMPLEMENTED AS PRESERVATION. The buffer is never rewritten and
  every offset reported is into the caller's own UTF-8. Coverage is asked in a CANONICAL VIEW - a
  face covers a cluster when it covers the cluster's composition OR its full decomposition - and the
  view is a lookup, not a rewrite. The canonical ordering of marks is part of it: two marks written
  in either order are one string, and a view that did not sort them would call two identical clusters
  different and take different fallback for them.
  ITS REGRESSION IS PINNED, in both the cases this item asks for. A font set with precomposed Latin
  in one face and the marks in another - which is an ordinary set - gives the composed and decomposed
  spellings of `cafe` with an acute the SAME face either way round, and reports cluster spans
  differing only as the two inputs' own byte lengths do. The non-Latin case is Devanagari KA WITH
  NUKTA, which is also a composition EXCLUSION - so its canonical view's composed form IS the
  decomposed one, which an implementation that recomposed everything would get wrong.
  THE DECOMPOSITION DATA IS GENERATED, from `UnicodeData.txt` and `CompositionExclusions.txt` pinned
  by SHA-256 like the rest. A compatibility decomposition is deliberately dropped: `<font> 0066` says
  a character LOOKS like an `f`, not that it IS one, and treating the two alike is how a text engine
  decides a font covers a character it has no glyph for.
  6 fixtures, 6 passed, inside the segmentation gate.

  **MIRRORING IS BEFORE SHAPING; ONLY REORDERING IS AFTER WRAPPING** (corrected 2026-08-31). An
  earlier version put "reordering and mirroring" together in the final per-line stage, and the two
  belong at opposite ends. Mirroring is a CHARACTER-TO-GLYPH decision - a `(` in a right-to-left
  context is drawn as `)` - so it is resolved from the embedding level and applied before the glyph
  is chosen. Reordering is a POSITIONAL decision and is per line, which is why it must wait for the
  break.
  Putting mirroring last made it unimplementable in this milestone's own data: by then the
  `GlyphRun` already holds chosen glyph indices and measured advances, so a late mirror has no
  defined way to pick the mirrored glyph and no way to repair the metrics it changed - and the
  mirrored form's advance can differ, so a width used for line breaking would have been measured from
  the wrong glyph. Valid RTL punctuation would produce a wrong glyph or a wrong break. UAX #9 places
  mirroring with the resolved levels for exactly this reason and leaves only reordering to the line.
  Logical source spans and resolved BiDi levels are RETAINED through wrapping - they are the inputs
  the last stage needs. State Script and Script_Extensions handling, that language is an explicit
  input rather than ambient state, when reshaping is repeated, and the missing-glyph retry rule.
  Bind the behaviour to pinned versions of UAX #9 and UAX #29.

- [x] Implement font fallback as a policy with a stated order rather than an accident of enumeration:
  a per-script and per-language preference list, a last-resort face, and deterministic selection so
  the same string with the same installed faces always produces the same runs. Fallback that depends
  on directory order is a rendering difference between two machines. Fallback operates on whole
  clusters and syllables; splitting a combining sequence, an Indic syllable, an Arabic joining
  context or an emoji ZWJ sequence across two faces is the defect this item exists to prevent.

  DONE 2026-09-12, in `text-pipeline`'s `fallback`. THE ORDER IS THREE BANDS, STATED: the faces
  preferred for this script AND language, then those preferred for the script whatever the language,
  then the general ones - and the last resort LAST, in the list rather than as a special case
  afterwards, so a caller that walks the order and finds nothing has been told everything the policy
  knows.
  THE ORDER IS TOTAL AND INDEPENDENT OF HOW THE FACES WERE FOUND. A stated rank decides within a
  band and the FACE ID breaks every tie - and the face id is the catalogue's content-derived
  identity, not a position - so two machines with the same faces produce the same order however
  their directories enumerated. A fixture gives the same faces in the reverse order and requires the
  same answer.
  THE LANGUAGE BAND IS NOT DECORATION: one Han face is chosen for Japanese and another for Chinese
  because the same character is drawn differently, and a fixture pins that the Japanese-preferred
  face is NOT chosen for a Chinese run.
  FALLBACK OPERATES ON WHOLE CLUSTERS, asked in the canonical view the item above defines. A face
  that covers only part of a cluster does not cover it - so a base letter and its mark cannot go to
  two different faces - and a run ENDS where the face changes, which is what makes the shaped runs
  face-homogeneous as the shared contract requires. A cluster nothing covers fails the whole run
  rather than being dropped: a run with a hole in it is one whose clusters no longer line up with
  the text they came from.
  9 fixtures in that crate, 9 passed.

- [ ] Support variable fonts: named instances and arbitrary axis coordinates, with outlines AND
  metrics correct at any coordinate rather than only at the named instances - which is what makes
  `HVAR` and `MVAR` profile entries rather than optional extras.

  PROGRESS 2026-09-12: THE METRIC HALF IS BUILT AND THE ITEM STAYS OPEN, because it says outlines AND
  metrics and the outlines are not done. `font-parse`'s `variations` carries the metric half, which is
  the half this item's own reasoning singles out. `fvar` gives the axes and the named instances - a named instance is a POSITION and not
  a separate face, which is why a catalogue listing them as faces would publish four faces backed by
  one file. An ARBITRARY coordinate is normalised with each half of the axis scaled on its own,
  because a default is almost never the midpoint: weight runs 100 to 900 with a default of 400, so
  650 is half way up and a single linear scale would have called it 68%.
  `avar` IS APPLIED, and skipping it is not a rounding difference: it is how a font says the middle of
  its axis is not the middle of its design, and an instance lands somewhere else on the axis without
  it. Version 2 is refused by name, as the profile excludes it.
  `HVAR` IS THE HALF A TABLE LIST FORGETS. The delta-set index map, the item variation store and the
  region scalars are all read, and a region that does not apply contributes ZERO rather than a
  reduced amount - treating it as partially applied is how an instance ends up between two masters
  that were never meant to be mixed. Without any of this a variable font's glyphs are the right shape
  at the wrong widths, which is text mis-spaced everywhere except at the default instance and looks
  like a rendering bug rather than a missing table.
  10 fixtures in that crate, 10 passed. The fixture font is a variable one this tree builds, with a
  weight axis whose default is not its midpoint, a named instance, an `avar` that moves the middle of
  the upper half, and an `HVAR` delta that applies over the top half of the axis alone.
  PROGRESS 2026-09-12: THE OUTLINE HALF IS BUILT TOO, and `MVAR` with it. `font-parse`'s `gvar`
  carries the shape deltas: the glyph variation data array with both the short and the long offset
  form, shared and embedded peak tuples, intermediate regions, and the packed point-number and packed
  delta encodings - all read through the one bounded reader, none of them decoded into a buffer the
  size of a glyph.
  THE DELTAS ARE SPARSE AND THAT IS WHAT THE INFERENCE IS FOR. A tuple names a few of a glyph's points
  and leaves the rest to be INTERPOLATED from their neighbours, which is the format's own rule and not
  an optimisation: a parser that moved only what was listed would move a stem and leave the serif it
  carries behind, which TEARS the outline apart rather than failing. The interpolation is per contour,
  it wraps around the contour because a contour is a closed shape, and two references at the same
  coordinate that moved differently answer ZERO rather than one of the two picked arbitrarily.
  A COMPOSITE IS ADDRESSED BY COMPONENT, NOT BY POINT. Its entries move whole components, and a parser
  that applied them to the expanded points of the components would move an accent by the delta meant
  for the letter under it. Inference is not performed there and the reason is written down: there is
  no coordinate to interpolate ALONG between two component origins.
  THE FOUR PHANTOM POINTS ARE PART OF THE COUNT. Every glyph's delta list ends with four points the
  glyph does not contain, and a parser that leaves them out of the count reads the y deltas four
  values early - every point of every varied glyph moved by somebody else's number. They are counted,
  and they are also answered: a face that states its width variation through `gvar` alone rather than
  through `HVAR` is read correctly instead of being treated as a face whose widths do not vary.
  `MVAR` IS THE LINE'S HALF OF THE SAME PROBLEM. `HVAR` makes the glyphs sit at the right distance
  along a line and `MVAR` makes the LINES sit at the right distance from each other; a weight axis
  that thickens the strokes almost always raises the ascender with them, and a layout that reads
  `hhea` alone sets every instance on the leading of the default one. The record stride is taken from
  the font rather than hard-coded, so a later minor version is walked correctly instead of read out of
  the middle of the previous record. The seven metrics are named rather than spelled at call sites.
  Every buffer belongs to the CALLER and a composite hands what is left of them to each component it
  walks, so nesting is bounded by what the caller was willing to pay for rather than by a number this
  parser invented - and a glyph that does not fit is a refusal, because half a glyph drawn is a shape
  the font does not contain.
  18 fixtures in that crate, 18 passed. The fixture font is a variable one this tree builds: a weight
  axis whose default is not its midpoint, a named instance, an `avar` that moves the middle of the
  upper half, an `HVAR` delta over the top half, an `MVAR` varying two font-wide metrics, and a `gvar`
  with an inferred square, an interpolated triangle apex, a varying phantom point and a displaced
  component. Both new tables are also put through the exhaustive corruption sweep - every byte, four
  patterns, every glyph, four coordinates - and answer rather than read past.
  WHAT REMAINS, AND IT IS WHY THIS BOX IS NOT TICKED: CFF2 `blend`, and nothing else. It cannot be
  written before the CFF2 charstring interpreter it is an operator of, which is named as remaining
  work under the closed-profile item above and is where it will be built. It is recorded here as a
  dependency rather than as work this item still owes, and until it exists a CFF2 face is refused
  WHOLESALE by the parser rather than drawn at the wrong weight - a refusal a caller can see, not a
  wrong shape it cannot.

- [x] **Produce the `GlyphRun` `render2d` consumes, with every unit and representation fixed.** Face
  resource and generation, size, variation coordinates, glyph indices, offsets and advances,
  direction, and the flags that tell the rasteriser which glyph kind each entry is.

  **FIXED HERE, NOT LISTED HERE**
  (corrected 2026-08-31). An earlier version named four decisions and made only the first, which
  leaves two sides of one ABI free to implement incompatible runs while each follows the plan - and
  `P02M0103` consumes an already-shaped run and supplies none of them:

    source-span unit    UTF-8 BYTE OFFSETS into the original string, as before
    span order          LOGICAL. The run stores source spans in the order the text was written, and
                          visual order is derived from the cluster mapping's logical-to-visual half.
                          Storing them visually would put a BiDi decision inside the data every
                          consumer reads, and a hit test would have to undo it to answer a question
                          about the string
    numeric form        26.6 FIXED POINT for offsets, advances and origins - a signed 32-bit value
                          with six fractional bits, which is 1/64 of a pixel and the granularity
                          hinting-free subpixel positioning needs. ROUNDING IS ROUND-HALF-TO-EVEN at
                          the one place a scaled font unit becomes a run value, and nowhere else, so
                          two implementations of the same shaping produce the same bytes. OVERFLOW is
                          a TYPED REFUSAL of the run rather than a saturation: a line whose advance
                          does not fit is one this layer will not draw, and saturating would hand
                          `render2d` a position that is silently wrong
    glyph origin        the BASELINE ORIGIN, +x to the right and +y DOWN, matching `P02M0103`'s
                          device-space convention so a run needs no flip to be drawn

  Any of these may be revisited before the seam is frozen; none may be left to the implementer after
  it.
  The CLUSTER MAPPING is not optional and not a single index - it is what a caret, a selection, a hit
  test and an accessibility tree are built from, and one cluster-start integer cannot express a
  ligature caret, RTL affinity or a discontiguous BiDi selection. It carries: the source RANGE per
  cluster, intra-ligature caret positions, caret affinity, and the logical-to-visual mapping from
  which selection geometry is derived.

  DONE 2026-09-12. `font-run` is the producer, and it is a crate of its own because it is the one
  place a shaped buffer in FONT UNITS becomes the 26.6 run the renderer consumes. Every decision
  fixed above is made there and nowhere else.
  THE CONVERSION HAPPENS ONCE. Shaping works on the face's own design grid - a thousand or two
  thousand to the em, whatever the face declares - and the seam is 26.6. Every offset and every
  advance crosses that line exactly once, through the contract's own `from_font_units`, which rounds
  HALF TO EVEN. A fixture proves the tie: on a grid where every odd unit count lands exactly on a
  half pixel, 1.5 and 2.5 both answer 2, where round-half-up would have answered 2 and 3.
  THE Y AXIS TURNS OVER HERE, and it is the same kind of decision rather than a detail. A font
  measures UPWARD from the baseline and this seam's device space measures DOWNWARD; a producer that
  passed font units straight through would put every mark on the wrong side of its base, which reads
  as a broken font rather than as a sign nobody wrote down.
  THE GLYPHS ARE IN VISUAL ORDER AND THE CLUSTERS ARE IN LOGICAL ORDER, which are not the same order
  and are both the seam's own rule. Shaping never reverses - every `GSUB` and `GPOS` rule is written
  about the order the text was typed, and a shaper that reversed early would apply `fina` to the
  first letter of an Arabic word - so the reversal belongs at the one point where the result stops
  being about the string and starts being about the picture, and that point is here.
  A CLUSTER IS A GRAPHEME CLUSTER, not a character and not a glyph. A caret does not stand between a
  letter and the accent on it, and deriving clusters from the buffer alone would also lose every
  cluster a LIGATURE absorbed - which is exactly the caret position the mapping exists to carry. A
  cluster a ligature swallowed says so with a glyph count of zero and points at the glyph that
  swallowed it, so a hit test knows which pixels to measure within.
  THE INTRA-LIGATURE CARETS ARE THE FACE'S OWN. `GDEF`'s caret list is read, in all three of its
  forms: a design coordinate, a POINT INDEX resolved by walking the glyph's outline because that is
  the only place the point exists, and a coordinate with a device table whose adjustment is a hinting
  correction this profile deliberately does not apply. The dividing position of `fi` is not derivable
  from its width - it is not two equal halves, and the designer who drew it said where it divides.
  CARET AFFINITY is carried by the seam as the type a caret query is asked with; the geometry that
  consumes it - where a caret actually lands, and how a selection is drawn - is the line layout item
  below, which is where a run boundary exists to be on two sides of.
  "DRAW THE GLYPH" IS NOT ONE INSTRUCTION, which is what the kind flag is for. The same glyph index
  may be an outline, a `COLR` v0 layer list, a `COLR` v1 paint graph or a bitmap out of a strike, and
  those are drawn by four different pieces of code. The face's own indices are read to decide - the
  base glyph lists by binary search, the strike tables for actual coverage rather than a size
  record's outer claim - and the order is richest first, because a face carrying both a paint graph
  and a bitmap for one glyph means the paint graph and drew the bitmap for consumers that cannot
  paint. A strike is chosen BY SIZE: the smallest one big enough, because scaling a bitmap down keeps
  the detail the designer drew and scaling one up does not.
  OVERFLOW REFUSES THE RUN. A scaled value outside 26.6 is a typed refusal and never a saturation,
  and so is a run longer than the seam's own indices can address. What this produces is then handed
  to the seam's OWN checking constructor before it is returned: if the mapping does not satisfy the
  invariants, the defect is here, and passing it on would move the symptom somewhere the cause cannot
  be seen.
  11 fixtures in `font-run`, 11 passed, over fonts this tree builds - a ligature caret list, a `COLR`
  version 0 list beside a version 1 one, three bitmap strikes at three sizes, and a grid chosen so
  the rounding tie is exact.

- [x] Implement line layout above shaping: measurement, line breaking with the Unicode algorithm,
  justification, tabs, alignment, direction-aware line composition, ellipsis and truncation, and
  the caret and selection geometry that follows from the cluster mapping.

  DONE 2026-09-12. Measurement and line breaking are `text-pipeline`'s stages - the break
  opportunities are UAX #14's, computed over the whole paragraph and applied per line - and the
  geometry is `text-layout`, a crate of its own because it decides POSITIONS over the frozen run seam
  and reads no text at all. That is what makes its decisions checkable: every input is already in
  26.6, so a fixture states numbers and the answers are arithmetic rather than a font.
  DIRECTION IS A PROPERTY OF THE PARAGRAPH, NOT OF THE LAYOUT. "Start" is the left in a left-to-right
  paragraph and the RIGHT in a right-to-left one; a layout spelled left and right makes every
  right-to-left document ragged on the wrong side, which is a defect a reader of that script sees
  immediately and a developer of it never does. `Left` and `Right` exist too, for a column of numbers
  where the glyphs and not the language decide. A centred line's odd half-unit goes to the START side,
  because rounding toward zero would drift a centred right-to-left line left.
  THE RUNS ARRIVE IN VISUAL ORDER AND ARE NOT REORDERED AGAIN. Rule L2 has already decided which run
  is drawn first; applying it twice is the identity for one embedding level and wrong for three.
  JUSTIFICATION HAS A POLICY, NOT A FORMULA, and the seam between this layer and its caller is stated:
  WHICH points may absorb space is a property of the text - an inter-word space, a Thai word boundary,
  an Arabic kashida position and a CJK inter-character gap are four different answers in four scripts,
  and none of them is visible from a run of advances - so the caller names them; HOW MUCH each gets,
  what happens when there are none, and whether the last line is stretched are decided here. A line
  with nothing to stretch is left RAGGED and never letter-spaced: spreading the slack between every
  pair of glyphs is what a layout does when it will not admit it cannot justify a line, and on a line
  with one long word it is unreadable. The remainder is given away one 26.6 unit at a time so the line
  reaches its box EXACTLY rather than nearly - a few sixty-fourths short shows as a ragged right edge
  on justified text, which is the thing justification was asked for. The last line of a paragraph is
  not stretched, and the caller says which line that is because this layer cannot know.
  A TAB IS NOT A WIDE SPACE. Its advance depends on where the pen already is, which makes it the only
  thing on a line whose width is not a property of the font, and it lands STRICTLY past the pen - a
  tab that landed on a stop would advance by nothing, so two tabs in a row would columnise as one.
  Past the stated stops the interval continues from the LAST STATED ONE, so a stop that is not a
  multiple of the interval does not shift every column after it, and an interval of zero advances by
  nothing rather than searching forever.
  THE ELLIPSIS GOES AT THE PARAGRAPH'S END, which in a right-to-left paragraph is the LEFT. Always
  putting it on the right truncates an Arabic line at its beginning and marks it at its end - a
  sentence with its first word missing and a mark saying the last one is. Truncation stops at a
  CLUSTER boundary and never inside one, because cutting inside a cluster drops half a combining
  sequence or half a ligature, which is not a shorter string but a different one; and the clusters are
  walked in LOGICAL order, because keeping whichever happen to be drawn first is not the beginning of
  the sentence in mixed-direction text.
  THE CARET AND SELECTION GEOMETRY IS WHAT THE CLUSTER MAPPING WAS CARRIED FOR. One contiguous range
  of text is NOT one rectangle: a selection across a direction boundary is separate pieces with a gap,
  and a layer that assumed one rectangle draws a highlight over text the user did not select. Pieces
  that touch are merged, because ten adjacent rectangles leave a seam at every boundary on a surface
  that blends. A caret inside a ligature lands on the face's own divider rather than at an equal
  fraction of its width, and a caret at a line's own edge is owned by ONE line, with affinity saying
  which - answering from both is how a caret is drawn twice, and from neither is how it disappears at
  a wrap.
  10 fixtures in `text-layout`, 10 passed, over a three-run line whose middle run is right to left, a
  ligature with a declared divider, a slack that does not divide evenly, and a tab landing exactly on
  a stop.
  WHAT THIS DELIBERATELY IS NOT: the line breaking is GREEDY over the UAX #14 opportunities rather
  than an optimal paragraph fit. That is a typographic refinement over the same opportunities and the
  same measurement, not a correctness gap, and it is named here so a later reader does not have to
  work out whether it was decided or forgotten.

- [ ] **Bound the recursive and multiplicative work, not only the caches, and make refusal typed.**
  Cache eviction, per-run glyph counts and fallback depth are three limits and none of them bounds
  the work a structurally VALID font can demand. Checked offsets prevent an out-of-bounds read; they
  do not prevent a valid font from exhausting time, stack or the caller's Domain.

  **THE LIMITS ARE VALUES AND THEY ARE FROZEN WITH THE PROFILE, NOT AFTER IT** (corrected
  2026-08-31). An earlier version said "numeric limits are stated for" and then named CATEGORIES,
  which is a list of things to decide - so the host gate's "test every numeric limit above" had no
  boundary to test at, and two implementations could pick different ceilings after work began. The
  values below are part of the CLOSED PROFILE and are frozen with it, before any parser is written:

    font/file bytes                     16 MiB per face, 4 MiB per table
    composite-glyph depth               5 nested components, 10000 points after expansion
    CFF/CFF2 subroutine recursion       10 deep, 48-entry operand stack
    `COLR` paint graph                  depth 64, 8192 nodes per glyph
    contextual `GSUB`/`GPOS`            traversal depth 64, output expansion 64x the input run
    variation axes / regions / features 64 axes, 4096 regions, 256 features
    BiDi control nesting                125, which is the algorithm's own maximum depth
    shaping and fallback attempts       16 fallback faces per cluster, 4 shaping retries per run
    layout passes                       8 per line, 2 per paragraph
    INPUT per shaping run               4096 code points
    INPUT per paragraph                 65536 code points
    OUTPUT per shaping run              16384 glyphs
    OUTPUT per paragraph                262144 glyphs

  THE LAST FOUR ARE ABSOLUTE, AND WITHOUT THEM THE REST BOUND NOTHING (added 2026-08-31). The item's
  own opening names per-run glyph count as one of the three limits it exists to fix, and the table
  did not contain one: every row above was either font-INTERNAL - a property of the face, which a
  larger document does not change - or PROPORTIONAL. `64x the input run` is the clearest case: it
  caps the multiplier and not the product, so an arbitrarily large source still demands arbitrarily
  large work and allocation, which is exactly the exhaustion the checked offsets do not prevent.
  A proportional rule with no absolute ceiling under it is a ratio, not a bound.

  Both directions are capped because either alone leaves a hole: an input cap without an output cap
  still admits a 4096-point run that a pathological face expands to 262144 glyphs, and an output cap
  without an input cap still admits an unbounded source whose refusal is only discovered after it has
  been read. The two are independent and BOTH refuse - whichever binds first - and the proportional
  `64x` rule continues to apply inside them rather than being replaced by them.

  A face or a document that exceeds any of them is REFUSED with the typed refusal below, not
  truncated. These are ceilings for a bounded appliance rather than derived from a corpus
  measurement; a value may be raised before the profile is frozen and not after, and raising one
  afterwards is a profile change with its conformance consequences.
  ALLOCATION IS FALLIBLE ON THIS PATH. Userspace infallible allocation exits the process when it
  fails, so "charged to the calling Domain" does not by itself produce the typed refusal this
  milestone promises: parser, cache and layout allocations use the fallible forms, cache insertion is
  transactional, and a refusal rolls back partial insertion and releases its charges. Deterministic
  work accounting makes the same input meet the same limit on every architecture.

  PROGRESS 2026-09-12: THE VALUES ARE FROZEN AND MOST OF THEM NOW REFUSE. `opentype-profile` carries
  a `limits` module: every ceiling in the table above as a constant, and a published list beside it
  giving each one's unit, its KIND and what a font or a document could do without it. The kind is
  part of the data because it decides what a ceiling can promise - `internal` is a property of the
  face, `proportional` caps a multiplier and not a product, and `absolute` caps a document and is
  what makes the proportional ones bound anything at all. A fixture holds the constants and the
  published table to being the same values, because a call site uses the constant and the document is
  written from the table: a table that drifted would publish a ceiling nothing enforces, which is the
  exact failure a written-down limit is supposed to prevent. The ceilings are part of the canonical
  form the PROFILE HASH is taken over, so raising one quietly is a line in a diff.
  THE NUMBERS ARE READ RATHER THAN RESTATED. Three of them already existed as local constants with
  the right values and one with the WRONG one - the shaper's nested-lookup ceiling was 8 against the
  profile's 64, and the parser's axis ceiling was 16 against the profile's 64 - which is precisely the
  drift this item exists to end. Each site now takes the profile's constant.
  WHAT REFUSES TODAY, each with a fixture over it: the file's own size and a single table's, before
  anything inside either is read; the composite nesting depth; THE EXPANDED POINT COUNT, counted
  across the whole walk rather than per glyph description - depth alone bounds the stack and not the
  work, and five levels of nesting MULTIPLY; the variation axis and region counts; the nested-lookup
  depth; the feature and lookup counts; the absolute input ceiling on a shaping run, checked BEFORE
  the shaper's own pass rewrites the run rather than after; the absolute output ceiling on a run and
  the proportional expansion rule beside it; the absolute input and output ceilings on a paragraph,
  with the output one counted across the whole paragraph because a per-run check would pass a
  paragraph made of many runs; and the fallback face count.
  REFUSALS, NOT TRUNCATIONS, and that is a behaviour change in two places: a nested lookup past the
  depth used to be declined silently and a lookup past the count used to be dropped, both of which
  leave a run shaped as though the font had not asked - a document rendered wrong with nothing to say
  so. `Unsupported::Exceeded` names WHICH ceiling and by how much, because "too complex" is not
  something a report, a staging tool or a person can act on.
  ALLOCATION ON THE RUN PATH IS FALLIBLE. Every allocation in the run producer whose size comes from
  input is `try_reserve_exact` with a typed refusal, rather than the infallible form that ends the
  caller's whole process over one paragraph.
  THE BIDI DEPTH IS STATED IN TWO PLACES ON PURPOSE. It is UAX #9's number and not this profile's
  choice; a Unicode implementation that had to link an OpenType profile to learn its own algorithm's
  depth would have the dependency the wrong way round. The profile RECORDS it, and `unicode-bidi` is
  where it comes from.
  WHAT REMAINS, AND IT IS WHY THIS BOX IS NOT TICKED: five ceilings have no enforcement site because
  the code they bound does not exist yet, and a ceiling with no code bounds nothing. The two
  charstring ones belong to the `CFF`/`CFF2` interpreter and the two paint-graph ones to the `COLR`
  decoder, both of which are named as remaining work under the parser item above; the three pass and
  retry ceilings have no loop to bound, because the line layout above is built and its layout is
  SINGLE-PASS by construction - a justification that RE-SHAPES, which is what Arabic kashida
  justification is, is where they will first apply. A refusal type nothing raises is a promise nothing
  keeps, so none was added there. They are recorded here as the dependency they are.

- [ ] **Host gates.** Run the NORMATIVE Unicode conformance files for the pinned version -
  `GraphemeBreakTest`, `WordBreakTest`, `LineBreakTest`, `BidiTest`, `BidiCharacterTest` - pinned by
  SHA-256, not a representative sample. Over a pinned corpus with pinned font versions, file hashes
  and compatible notices, assert expected glyph indices and positions for: Latin with ligatures and
  kerning, Arabic in all four joining forms, Hebrew with marks, Devanagari reordering, Thai without
  spaces, Khmer stacking, mixed-direction paragraphs, emoji sequences including ZWJ and skin-tone
  modifiers, and variable-font instances at named and arbitrary coordinates including cases where
  metrics vary independently of outlines. Add negative cases for fallback determinism, cache
  invalidation across a face generation change, every numeric limit above, caret affinity and
  discontiguous selection. EVERY LIMIT IS TESTED AT ITS EXACT BOUND AND ONE PAST IT, and the four
  absolute input/output ceilings by name: a run of exactly 4096 code points and one of 4097, a
  paragraph of exactly 65536 and one of 65537, a face crafted to produce exactly 16384 glyphs from a
  legal run and one that produces 16385, and the paragraph equivalents - each over-bound case ending
  in the typed refusal rather than in truncation or in a larger allocation.
  Fuzz every parser and the shaper: no panic, no unbounded allocation, no
  out-of-bounds read on any malformed font.

- [ ] **Guest gate.** A staged, governed text-and-render conformance executable runs in a booted
  guest on x86_64, aarch64 and riscv64: it loads a real font from its staged package, shapes and
  lays out the corpus, draws through `render2d`/`soft2d`, and compares against a canonical raster
  oracle within the tolerance `P02M0103s` fixes. "Host tests pass on all three architectures" has no
  executable meaning - this tree distinguishes host gates from code run in a booted target - and the
  three-architecture claim belongs to this gate. Both gates are wired into `verify.sh`, and a focused
  mutation of each new gate is watched to fail so the evidence is not vacuous.

- Done when: the shared font-resource contract is frozen and implemented on both sides; the published
  OpenType profile is met and everything outside it is a typed refusal; the pinned Unicode
  conformance files pass; a string in every profile-named script becomes a correct `GlyphRun` that
  `soft2d` renders to the canonical raster oracle in all three guests; line layout produces caret and
  selection geometry including ligature carets and discontiguous selection; fallback is deterministic
  and cluster-atomic; every stated limit is enforced with a typed refusal and released charges; and
  every parser survives fuzzing.
- Explicitly deferred: a full text-editing widget, rich-text markup parsing, hyphenation dictionaries,
  vertical writing modes, and font hinting - each a separately approved milestone.
- Owned HERE and ordered first: the `P02M0097`-compatible font package role, the pinned last-resort
  face and corpus, and the catalogue owner (see the first work item - absorbed on 2026-08-30 rather than
  left as an unowned block). Required and NOT owned here: `P02M0105` for any system default language.
  `P02M0103`'s `DrawList` contract is met as of 2026-08-30, in the form the precondition above
  states.
- Concept: P02M0103 (the graphics platform and the `GlyphRun` seam), P02M0097 (the volume layout a
  font package role must extend), P02M0093/P02M0096a (shared providers and package identity).
