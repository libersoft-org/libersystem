# P02M0071 - UDF: third-pass findings (the last unrefused forms)

Status: COMPLETE. The third full source pass (2026-07-04); low and cosmetic leftovers, all of the
same kind.

*Condensed 2026-08-08.*

- **The extended allocation form was not distinguished**, so the scan kept the shorter step over
  longer records and parsed garbage extents. Refused - the rule across these passes is
  **refuse rather than misread**.
- **A symlink File Entry served its target-path bytes as file content.** Refused, because the Volume
  API has no symlink semantics and serving the path as data is the worst of the three options.
- The tag-location cross-check covered the File Set and File Entries but not the anchor or the volume
  descriptors, so a stale copy could still pass on its checksum alone.
- Recorded rather than changed: UDF preserves case and permits two siblings differing only in case,
  while the reader matches case-insensitively for consistency with the other backends - so the first
  match shadows a case-distinct sibling. Written down at the comparison.
