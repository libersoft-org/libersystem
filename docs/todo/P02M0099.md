# P02M0099 - Universal standards-based driver set

Status: NON-COMPLETABLE INDEX. NOT A MILESTONE, NOT A PHASE-2 GATE, AND NEVER TICKED - it is never
closed, it appears in no phase's completion report, and what closes is an ITEM. Its per-item
prerequisite matrix is below; there is no global hard gate, and saying there was one is what made
non-driver items inherit a driver's dependencies.

Corrected 2026-08-30: this line said "PLANNED AFTER P02M0098, P02M0161 AND P02M0162, WHICH ARE THE
HARD GATE" while the body already declared the file an index, and the two cannot both be true.

This index lists every approved driver candidate whose controller, protocol or device-class interface is standardized
across vendors. It does not promise support for arbitrary real machines: PCI/USB/ACPI/
FDT discovery, IRQ/DMA attachment, clocks, resets, pinmux and power can still require
platform glue, while PHASE 5 owns drivers and quirks for concrete chips, products, SoCs
and boards.

CORRECTED 2026-08-30: this file said "Phase 4" of concrete-hardware work throughout, and the
architecture does not. `docs/CONCEPT_EN.md` makes Phase 4 the DESKTOP PLATFORM on the reference
virtual machine - compositor, shell, the full audio stack over virtio-sound - and states in the same
block that real GPUs and real-hardware audio drivers follow in Phase 5. Phase 5 is where it then puts
device- and board-specific drivers for concrete NIC/GPU/Wi-Fi/audio/storage controllers, support for
specific machines and SBCs, bare-metal ARM64/RISC-V, and per-deployment power management. Every
handoff below that reads "remains Phase 4" therefore means PHASE 5, and each has been changed.
Native VT-d, AMD-Vi and ARM SMMU backends are Phase 5 for the same reason.

AND THIS FILE IS NOT A PHASE-2 COMPLETION SET. Phase 2 is defined as a deliberately small, bounded
universal set chosen for appliance value, and its own definition of done names the capability-scoped
appliance and the QEMU suite on three targets - not NVMe, not TPM, not UVC. So the Phase-2 subset is
named ITEM BY ITEM, and it is NOT "group 1" (corrected 2026-08-30). Assigning a whole group was an
unbounded assignment resting on a false premise: the harness configures virtio-blk, virtio-net,
virtio-gpu, virtio-input, virtio-console, virtio-snd and xHCI, and it does NOT run the proposed RNG,
vsock, SCSI, crypto, balloon or virtio-fs items - so "what the reference virtual machine actually
runs on" was true of seven entries and claimed for the whole group. (virtio-gpu was omitted from the
first correction and is added 2026-08-30: `docs/CONCEPT_EN.md` lists `driver.virtio-gpu` in Phase 2,
the harness configures `virtio-gpu-pci` in the direct and UEFI profiles, and P02M0173 counts display
among the current DMA endpoints. Leaving it out left a shipped Phase-2 driver's remaining work riding
on an unrelated future EDID item.) The group also carries unowned kernel and
ResourceManager mechanisms and a development-only filesystem backend this file itself calls
month-scale, none of which belongs in a phase the architecture defines as deliberately small and
selected by appliance value.

    Phase-2 subset   the drivers the reference machine ALREADY runs, which this index would only be
                     improving - virtio-blk, virtio-net, virtio-gpu, virtio-input, virtio-console,
                     virtio-snd, xHCI - plus the prerequisites they need in order to be improved
                     safely: the host-test seam, P02M0172's declared DMA policy, and the index-state
                     tooling change above.

Every other entry in all three groups, the remaining virtio ones included, is phased backlog: a
candidate whose phase is decided when it is picked up, counted in no Phase-2 completion report.

THE GATE IS THREE SLICES, NOT FORTY-ONE ITEMS. P02M0098 was one milestone carrying the whole of the
driver-binding work, and this one waited on all of it - so no driver could be written until
observability, operator policy and power lifecycle were finished, none of which a driver needs. That
milestone is now seven (see its head for the split), and what a new driver actually requires is the
first three TOGETHER - no one of them is "the foundation": the kernel-owned claim, its generation and
its revocation (P02M0098), the start-and-report handshake (P02M0161), and a bind that rolls back when
it fails (P02M0162). NOT the stop: `STOP`/`STOPPED` is P02M0165's, whole, and P02M0161 says so in its
own goal.

The four that follow - discovery breadth, providers, supervision, observability - are what the driver
SET needs to be usable rather than what any one driver needs to exist, and they may land beside the
drivers rather than in front of them. A driver written here still consumes the registry, the claim,
the transactional bind and the generation, and none may add a private launch path, singleton service
handoff, source `match` arm or product-ID-first binding shortcut.

AND THIS IS AN INDEX, NOT ONE PIECE OF WORK. Driver candidates across three groups, plus the
prerequisites named in the classification below - and several of the candidates are families rather
than single drivers, so "done when every item is done" would state a direction rather than something
anybody finishes.

THE COUNT IS NOT AN INVARIANT AND IS NOT MAINTAINED ANYWHERE (2026-08-30, and this time the numbers
were actually removed). This said "forty-two checkboxes", then "forty-one driver candidates", and
`docs/todo/TODO.md` carried the second - so a round that declared the count unmaintained left two
copies of it in place. Both are gone. A count in prose that nothing recomputes is a fact that rots;
what matters is that every entry is CLASSIFIED, and that is checkable by reading the entry.

AND THE INDEX STATE IS A TOOLING CHANGE THIS FILE OWNS, NOT A SENTENCE (2026-08-30). Two rounds have
now declared this row "never ticked from this list and counted in no phase's completion", and nothing
implements that: `docs/todo/TODO.md` carries it as `- [~]`, and `check-milestone-index.sh` treats
`[~]` as an ordinary OPEN state - its own comment says so - and fails only the `[x]`-over-open-tasks
direction. So a tool and a reader both still see an open Phase-2 milestone. The bounded deliverable
that closes this is an ITEM in the ordering section below - an explicitly SUPPORTED index state for a
non-completable reference row, taught to the index checker so the marker keeps its title check and
cannot be read as phase-open, with a watched-fail case proving the checker refuses an unknown marker
rather than silently accepting one. Until that lands, the claim in the roadmap row is a statement of
intent and says so.

SO IT IS A CROSS-PHASE UMBRELLA AND NOT A PHASE-2 GATE, and it is listed under Phase 2 because that
is where its first items belong, not because Phase 2 waits for its last one. Phase 2's own definition
of done names the capability-scoped appliance and the QEMU suite on three targets; nothing in it
requires NVMe, TPM or UVC. STARTING WORK HERE MEANS PICKING ONE ITEM, not starting "M0099": the item
states what it requires, what it owns and its one integration gate, and it closes on its own.

AND THE GATE IS PER ITEM, NOT ONE GLOBAL THREE-ITEM GATE (corrected 2026-08-30). The three-slice
statement above is the FLOOR - no driver exists without the claim, the handshake and a bind that
rolls back - and it is not the whole prerequisite of any particular item. Naming one global gate hid
that different items need different milestones, and it went stale in both directions: an earlier
version of this paragraph said the gate was "complete with two verifications outstanding" and cited
P02M0162 as IN PROGRESS, while several supporting milestones were then reopened and have since been
closed. A per-item matrix cannot go stale that way, because each line names what that item touches.

    every DRIVER item              P02M0098 + P02M0161 + P02M0162 (claim, handshake, rollback), AND
                                     P02M0172 (a declared DMA policy on its registry row, `none`
                                     included). NOT every ITEM: a shared library binds nothing and
                                     claims nothing, and an architectural prerequisite is a decision.
                                     This row said "every item" and contradicted the classification
                                     below, which is how a parser inherited a driver's gates
                                     P02M0172 IS ON THIS ROW AND NOT ONLY ON THE DMA ROW (corrected
                                     2026-08-30). It requires a closed policy field on EVERY driver
                                     row, and the reason is security rather than bookkeeping: kernel
                                     claim admission calls `bus_master(entry, true)` unconditionally
                                     once a claim succeeds, so a driver that performs no DMA is
                                     granted bus mastering today. `none` is the only thing that
                                     turns that off, so a nominally non-DMA driver NEEDS P02M0172
                                     rather than being exempt from it
    every DRIVER item, on each     the three measured P02M0162 tick budgets for x86_64, aarch64 and
      target it claims to run on     riscv64. That milestone states its 300/400/4000 constants are
                                     ROUND COUNTS being consumed as TICKS and hands the measurement
                                     to whoever depends on the window; this index depends on it on
                                     three targets and never carried the inherited requirement. An
                                     item accepted on a target whose tick budget is still a
                                     round-count guess has not been accepted against a measured
                                     bind window.
                                     AND THE TICK BUDGETS ARE THE SMALLER HALF (added 2026-09-02).
                                     This row named the measurement and treated the rest of P02M0162
                                     as usable, which it is not: the manager-initiated release is
                                     SYNCHRONOUS end to end. `Holdings::begin_teardown` calls
                                     `Closes::release` inline, the production `Syscalls::release`
                                     enters `device_release`, and `SYS_DEVICE_RELEASE` completes the
                                     whole of `device::release_claim` before returning - so a slow
                                     release stops DeviceManager's only event loop, which is the one
                                     thing P02M0162's M4 and its definition of done forbid. The
                                     asynchronous half exists and is consumed (`ClaimInfo::settled`
                                     is the claim handle's readiness and the standing loop waits on
                                     it), so what is missing is specific: the syscall must START the
                                     release and answer, and the kernel has no thread to finish it
                                     on. Until that lands, an item accepted on this row inherits a
                                     lifecycle whose teardown can block every other node, and the
                                     row says so rather than letting a measured tick budget read as
                                     the whole prerequisite
    any DMA-capable driver         P02M0153 (the DMA/IOVA contract) IN ADDITION to the P02M0172 row
                                     above. P02M0153 is what an actual DMA user needs; P02M0172 is
                                     what every driver needs
    plain-PCI driver               P02M0163 (discovery breadth) ALONE. Split from the row below on
                                     2026-08-30: a plain PCI function is discovered from config
                                     space and needs no firmware node, which the device-node identity
                                     section already said and this row contradicted
    firmware-node-bound driver     P02M0163 (discovery breadth) PLUS the device-node identity
                                     prerequisite below
    publishes or consumes a
      provider                     P02M0164 (catalogue, per-consumer `open`, requires-edges) - MET
                                     for discovery, withdrawal, the requires-edge AND live
                                     connection accounting (rechecked 2026-09-02: the requires-edge
                                     half was incomplete when this row was last written - a provider
                                     withdrawn while a dependent was still HANDSHAKING was not acted
                                     on, because the stop set considered only nodes already
                                     `Online`, so the dependent came online against a requirement
                                     that no longer held. That set is now `Online` or `Binding` with
                                     a binding installed, which is the transition P02M0164's own M6
                                     table names; the table's other pre-claim edge has no producer
                                     by construction, and that is recorded there rather than left to
                                     be rediscovered. Originally rechecked 2026-08-31; the accounting
                                     repair this row used to demand has landed - see the catalogue
                                     paragraph below). What is still outstanding is per
                                     DESTINATION: AudioService reaches its provider THROUGH the
                                     catalogue, which is discovery and not the lifecycle contract -
                                     see its row in the destination table, which said DONE and was
                                     corrected - and every other destination is still on its
                                     bootstrap handle
    stops, removes or is           P02M0165 (STOP/STOPPED and drain), P02M0166 (operator and
      operator-disabled              removal state)
    accepted on three targets      P02M0167 (isolated run identity and artifacts) - MET for the
                                     selection-specific KERNEL, which is staged under the build lock
                                     and booted from a run-private copy; UNSATISFIED for the MEDIUM
                                     (2026-08-30) AND for the SCHEDULER PROOF (added 2026-09-02).
                                     That milestone's definition of done requires a test that
                                     executes `verify.sh` over a matrix - a prerequisite shared by
                                     two branches, an unmeasured-cost step, `FAIL` outranking
                                     `INCOMPLETE`, failed-descendant suppression, and the parallel
                                     `STEPGUESTS` reservation - and no such test exists; the
                                     verify-model tests prove graph CONSTRUCTION and say nothing
                                     about shell execution order. This is not a theoretical gap:
                                     that executor has taken two ordering corrections in three days
                                     - blocked-id propagation, then the guest barrier moving ahead
                                     of the blocker check - and neither would have failed a
                                     registered test. Until the matrix lands, evidence from a run
                                     that scheduled more than one step in parallel carries a risk
                                     nothing measures, and the medium restriction was only ever
                                     half of why this row is unsatisfied. `test-kernel.sh` releases the lock after staging
                                     the kernel and `qemu-run.sh` assembles the medium afterwards,
                                     while `mkimage.sh` says in its own comment that producers are
                                     outside its lock and DETECTS a changed input rather than
                                     preventing one - so two same-architecture selections can still
                                     collide there, and the loser fails. Marked here the way P0164's
                                     accounting is, because an item accepted on evidence from a run
                                     that raced has not been accepted. P02M0170's M1/M2 own the
                                     repair; until its concurrent-selection gate passes,
                                     tri-architecture acceptance runs one architecture at a time

Each item states which of these it needs and does not inherit the others. Each item is closable on
its own and ships on its own; THIS FILE NEVER CLOSES, and nothing in it waits for the whole of it.
("The milestone closes when its items do" stood here and contradicted the index rule three hundred
lines below it - a completion condition stated in the introduction is the one a reader believes.) The groups fix PRIORITY, not permission: an item in group 3 whose prerequisites are met
is not held by an unfinished item in group 1 it does not need.

AND THE ITEMS ARE NOT ALL DRIVERS, so one driver-shaped Definition of Done cannot be applied to all
of them. Every checkbox is classified, and the classification decides which gate closes it:

    driver                  binds a device, claims it, publishes or consumes providers
    in-controller
      class module          touches a real device THROUGH a controller's claim and holds none of
                              its own (added 2026-08-30 - a USB class driver under the first USB
                              execution model is neither a driver nor a shared library, and without
                              this kind it inherited gates it cannot satisfy or a parser's gates
                              that prove nothing; its contract is in the USB section)
    shared library          a parser or engine with no device and no binding (EDID, a SCSI
                              layer, a HID report parser)
    kernel/bus infra        changes the binder, the claim, discovery or DMA admission
    destination service     the service side a driver's output must reach
    architectural
      prerequisite          a decision or mechanism that is not a driver at all (the USB
                              execution model, the AML interpreter, the identity discriminant)

A shared library closes on host tests and hostile fixtures and never on a bind. An in-controller
class module closes on the contract in the USB section - controller-driven lifecycle, a bounded
per-class resource budget and an observable effect - and never on an independent bind. A prerequisite
closes on the decision being recorded and the mechanism existing, not on a device appearing.

## What the binder does not reach yet, and who extends it

Measured 2026-08-28, because three of the four things groups 2 and 3 assume were explicitly REFUSED
by [P02M0163](P02M0163.md) and are not arriving from anywhere else.

- **A claimable device exists for every PCI function; a RESOURCED one does not.** Corrected
  2026-08-30: this said NVMe, AHCI and HDA "have no entry", and P02M0163 closed that - `device.rs`
  now pushes a `DeviceEntry` for every remaining function of the scan, carrying the class triple and
  the address under `TRANSPORT_PLAIN_PCI`. What those entries carry is `bar_phys: 0, bar_len: 0,
  msix_cap: 0`: the function is claimable and has NO resource profile, so there is still nothing to
  hand any of them a BAR or an MSI-X vector through. That, and not the entry, is what the first
  plain-PCI item owns.
- **There is no firmware inventory.** P02M0163 refuses ACPI and FDT in as many words: the FDT parser
  produces a `BootInfo` - RAM banks, CPU ids, controller addresses - not an inventory with
  `compatible` strings a rule could match, and there is no AML namespace on x86_64 at all.
- **The binding unit is a PCI function.** `BindingId` is bus, device, function and generation. A USB
  interface, an NVMe namespace and a hotplug slot are not expressible in it.
- **There is no arrival or removal event.** The PCI scan runs once at boot. P02M0163 refuses hotplug
  because a milestone promising it would have had to build the mechanism as well.

- **A PROVIDER CAN BE WITHDRAWN AND THE CATALOGUE NOW SAYS SO; NO DRIVER DOES IT.** Corrected
  2026-08-30 - P02M0164 closed most of this and the rest of the paragraph would otherwise send the
  first implementer to build what is already there. `Withdraw` is opcode 6 of the driver protocol and
  DeviceManager handles `BindingEvent::Withdrawn`; the catalogue's `subscribe` is a SERVED stream
  carrying the snapshot and the live additions, `provider-info` now carries a `live` flag so a
  withdrawal travels on that same stream identified by slot and provider generation, and a
  `requires` edge both WAKES a `DependencyPending` node when its provider appears and STOPS an online
  one when its provider is withdrawn. A per-consumer `open` mints a connection per consumer with a
  declared consumer limit, so a second or late consumer is no longer stranded IN SHAPE.

  **AND ITS CONNECTION ACCOUNTING HAS LANDED; THE FIRST-HANDOFF RULE IS WRITTEN HERE SO THE NEXT
  MIGRATION READS IT RATHER THAN REDERIVING IT** (corrected 2026-08-31). The previous version of this
  paragraph declared the accounting a BROKEN PREREQUISITE in three places and assigned the repair to
  the first destination migration. That migration happened - AudioService - and the repair went with
  it. All three are closed, and the paragraph is kept rather than deleted because what replaced them
  is a RULE later items must follow:

  - THE INITIAL CONNECTION IS COUNTED, and the rule is that EXISTING PLUS PROMISED is the number
    every minting path checks. Publication initialises `consumers` to zero and the offered channel
    the driver made itself is counted separately while it still sits in the entry; the two together
    are `outstanding`. So a provider declaring one consumer cannot have its initial holder and one
    `open` alive at once, which is what the old zero-plus-uncounted-offer arrangement allowed.
  - THE OFFERED CHANNEL IS THE FIRST PUBLIC CONNECTION, and served `open` is where it is handed out.
    This is the first-public-handoff rule, and it is one rule rather than two paths: `open` MOVES the
    offered endpoint to the asking consumer and counts it, and only mints a new pair once that one is
    gone. It is never DUPLICATED, because a duplicate shares the driver's reply queue with whoever
    holds the original - the exact failure the per-consumer factory exists to prevent. Without this,
    the public factory could not deliver the first connection for a default single-consumer provider
    at all: the one connection it may serve was reachable only through the private `Catalogue::take`.
  - EVERY MINTING PATH IS ACCOUNTED, THE BOOT'S OWN INCLUDED. `Catalogue::take`, `mint_connection`
    and served `open` all check the driver's declared bound before handing anything out and all
    increment on success; `Catalogue::disconnected` gives the place back when a driver reports a
    consumer's endpoint closing, which is what makes reconnect expressible. The boot probe was the
    one path that minted without consulting or incrementing anything, so a declared bound was
    understated before the first public ask; it is checked now, and a driver asked for more
    connections than its entry declares is a manifest that is wrong about the driver.
  - THE MANIFEST BOUND IS RECONCILED AT BUILD TIME. `system-manifest` carries the same
    `MAX_PROVIDER_CLIENTS = 8` the drivers' served set is sized to and refuses an entry declaring
    more, so a count that is accepted at build time is one the driver can actually serve.

  So P02M0164 is MET as a mechanism, and what remains is per DESTINATION. A destination migration no
  longer owns a repair - it owns its own subscribe, attach, detach and reconnect, and it can rely on
  the accounting above being there.
  WHAT IS ACTUALLY MISSING IS THE PRODUCING SIDE AND THE MIGRATION. No driver sends the message: xHCI
  creates its block channel unconditionally, "Always created" by its own comment, answers with an
  error status when no stick is attached, and on a detach drops its storage state and leaves the
  publication standing. And the existing services do not arrive through the catalogue at all - NET,
  GPU, SND and the input channels are still handed over as a positional bootstrap handshake, one
  singleton each, which is the private handoff this milestone's head forbids for new drivers while
  every current service depends on it.

- **THE ONE RULE THIS MILESTONE STATES IS BROKEN BY THE MECHANISM EVERY NEW DRIVER MUST USE.** The
  head says no driver may add a "source `match` arm", and resources are handed out by exactly that:
  a literal name list - `driver_name == b"virtio_input" || driver_name == b"virtio_net" || ...` -
  decides which drivers get an MSI-X vector. A new driver that needs an interrupt has no way to say
  so except by being added to it, so the rule describes an intention rather than a constraint.

AND ONE OF THESE IS NOT A NARROW EXTENSION, WHICH THE RULE BELOW MUST NOT SWALLOW (added
2026-08-30). The binding unit being a PCI function is not a gap a driver widens on its way past. The
identity is BDF plus generation in `BindingId`; provider origin is bus/device/function in
`provider-info`; the manifest's match vocabulary has exactly two transports, `virtio-pci` and
`plain-pci`, and every predicate beside them is a PCI field; and the kernel's claim and release
address a PCI function. A PL011, a firmware-described 16550, an ACPI namespace device, an I2C child,
a USB interface and an NVMe namespace are not one row added to that - they are a DISCRIMINATED
identity, and changing the identity type reaches matching, claim and release, binding ids, provider
origins, policy keys, diagnostics, revocation and generation handling at once.

Owning that inside whichever UART or storage driver arrives first is how a system-wide name acquires
one driver's shape: every later item then inherits whatever that driver happened to need, and the
non-PCI items that follow have no sound public identity at all. So it is an ARCHITECTURAL
PREREQUISITE with its own approval, not a driver's bullet - stated here, owned elsewhere, and named
by every item that needs it:

    a discriminated, stable device-node identity carried through matching, claim/release,
    binding ids, provider origins, policy, diagnostics and generation, with transport-specific
    resource and teardown hooks, and a manifest match vocabulary that can express a
    firmware-described node without pretending it is a PCI function.

Until it exists, the items that need it are BLOCKED and say so; the plain-PCI resource profile below
does NOT need it, because a plain-PCI function is already expressible.

THE RULE FOR EVERYTHING ELSE, AND IT IS THE WHOLE ANSWER: the FIRST driver of a family that needs one
of these owns the NARROW extension it needs and says so in its own bullet.

**AND "FIRST" MEANS FIRST IMPLEMENTED, NEVER A NAMED DRIVER** (corrected 2026-08-30). A previous
round wrote the rule and then fixed three of its instances to particular drivers - the plain-PCI
resource profile to NVMe, the SCSI core to virtio-SCSI, the I2C contract to HID-over-I2C - while the
same round said elsewhere that a non-gating order lets AHCI, HDA, SDHCI or another eligible consumer
land first. Both cannot be true, and the fixed name is the one that breaks: it makes an item wait on
a driver nobody has started, or lets two parallel items each believe the other owns the contract.
The rule is therefore applied uniformly and in every occurrence, including the tables below:

    the plain-PCI RESOURCE PROFILE - the BAR and MSI-X vector a plain-PCI entry does not carry - is
      owned by the first implemented plain-PCI item, whichever it is. NVMe is the LIKELIEST because
      it stands first in the list, and it is not the owner by name. This is a NARROW EXTENSION and
      not a new identity: a plain-PCI function is already expressible, which is exactly the
      distinction the firmware-node identity fails
    the SCSI core is owned by the first implemented SCSI consumer - virtio-SCSI, UAS or another
    the I2C controller contract is owned by the first implemented I2C consumer - HID-over-I2C,
      SSIF or another

  The firmware-node identity is NOT under this rule. It is the architectural prerequisite stated
  above, it is owned by NO DRIVER, and it HAS NO NUMBER YET - which is stated plainly here because a
  previous round wrote "it has ONE separately numbered owner" and no such number exists anywhere in
  the roadmap (corrected 2026-08-30). Claiming an owner that does not exist is worse than admitting
  there is none: it reads as scheduled work. It is an UNOWNED BLOCKED PREREQUISITE, and every
  consumer of it is marked BLOCKED in its own bullet, on the same terms as the missing destination
  services - a blocked item may be planned and may not be started, and what unblocks it is an
  approved and numbered milestone rather than a driver deciding to build it.

  Its consumers, marked as such in their bullets: `16550` (the firmware-described variants),
  `ARM PL011`, `HID over I2C`, and every group-2 and group-3 item that binds through an ACPI or FDT
  node rather than a PCI function. The x86 legacy-port `16550` is NOT one of them - it needs no
  firmware identity, and its own bullet already records the three other things it does
  need. A previous round said "owned elsewhere" and "owned by no driver" and
  then, one paragraph later, that whichever UART is implemented first builds it - which is the fixed-
  name rule applied to the one contract this file says must not have it. `16550` and `ARM PL011` are
  both CONSUMERS of that prerequisite and neither builds it.

AND ONE PATH NEEDS NO FIRMWARE NODE AT ALL - BUT IT IS NOT CHEAP, AND THIS SAID IT WAS (corrected
2026-08-30). An x86 16550 at the legacy port is the UART the kernel's own early console already uses,
at an address it already knows, so it needs no firmware identity. It needs three things nothing in
this tree has:
a RANGE-SCOPED, REVOCABLE PORT-I/O AUTHORITY - the public driver resource vocabulary is device MMIO,
one IRQ, keys, system power and console, and there is no port-I/O object or syscall at all;
LEGACY FIXED-IRQ ROUTING AND ITS REVOCATION, which the MSI-X path does not provide;
and AN ATOMIC HANDOFF, because the x86 kernel programs COM1 directly, owns IRQ4 and the receive path,
drains transmit and polls receive from the idle hook after userspace has started - ownership that is
monotonic today and has no transfer step. A userspace driver given raw access without one races the
kernel for the same registers and the same interrupt.
So the legacy slice is NOT the cheap one. Either it owns those three mechanisms and a stated
panic-only fallback and reacquisition rule, or the first 16550 item is scoped to FIRMWARE-DESCRIBED
MMIO UARTs - which needs the identity prerequisite instead - and the legacy path is a later item that
says what it takes. What it may not do is start on the assumption that a claimable entry is all it
needs. There is no `USB hotplug` checkbox and two
rounds have now named a wrong owner for it. "The first group-3 item written" is wrong for the reason
this same file states two hundred lines earlier: group 3 contains HID over I2C, which is not USB and
attaches nothing to a USB bus. The catalogue subscribe, an `OFFER` on attach, a `WITHDRAW` and a
closed child server on detach, and a new provider generation on reattach are owned by **THE FIRST
ITEM THAT ACTUALLY ATTACHES A USB CHILD AT RUNTIME** - the USB execution-model prerequisite if it
lands first, otherwise the first USB class slice. A sub-function binding unit arrives with the first driver that is actually split. And the DECLARATIVE
RESOURCE PROFILE - the registry entry says what the driver needs and the manager reads it - is owned
by THE FIRST NEW DRIVER THAT WOULD OTHERWISE REQUIRE A CHANGE TO THE NAME LIST, which is a wider
trigger than the one written here first. "A shape the list cannot express" would have let a driver
with an ordinary MSI-X need be handled by appending one more name, which is exactly the edit the
head's rule forbids - so the rule would have been broken by every driver that obeyed the trigger. What is refused is a universal discovery framework
built ahead of the drivers that would show what it has to hold - which is the shape P02M0163 declined
for exactly this reason.

## 0. Phase-2 maintenance: the seven drivers that already ship

The head names a Phase-2 subset of drivers this index "would only be improving", and a subset with no
items is not a subset - a previous round listed the names and gave them no scope, no owner and no
gate, so Phase-2 work could not be scheduled or accepted (corrected 2026-08-30). These are BOUNDED
MAINTENANCE items over drivers that exist and boot today, not new drivers, and each closes on the
known defects of one driver rather than on a family.

Every item here is subject to the same three-slice claim/handshake/rollback prerequisites as any
other driver item, plus P02M0172's declared DMA policy - `none` included, since these are the
drivers whose policy rows have to be migrated first.

- [ ] **The host-test seam** (SHARED LIBRARY - closes on host tests, binds nothing). Owned here and
  built first, because every item below is gated on host tests that cannot currently run. Its
  contract, and the reason it is here rather than with "whoever arrives first", are in the ordering
  section's host-test paragraph.

- [ ] **virtio-blk maintenance.** THE ITEM PLAN IS WRITTEN (2026-08-31), which is what the rule in
  the ordering section requires before any code.

    REQUIRES   P02M0098 + P02M0161 + P02M0162 (claim, handshake, rollback); P02M0153's DMA/IOVA
                 contract AND P02M0172's declared DMA policy - BOTH, because virtio-blk masters the
                 bus and the matrix's DMA row names the pair (corrected 2026-09-01: this list carried
                 only P02M0172, so the one item declared ready to start contradicted the row it
                 claims to inherit. P02M0153 owns `DmaAddress`, the IOVA lifecycle and the three
                 admission outcomes; P02M0172 owns the mode this driver's entry declares. A driver
                 that has the second without the first has a policy and no contract to apply it to);
                 `rt::object_info`, which already reports a memory object's type, rights and size;
                 and the host-test seam, which is this subsection's first item and gates every host
                 test below
    OWNS       the three-part transferred-object contract in `virtio_blk::serve_write`; the count
                 and LBA-range validation on both block request paths; and CENTRAL VIRTQUEUE
                 VALIDATION in the shared virtio path, which no family owns and which this item owns
                 because it is the first Phase-2 item in that code with a gate attached
    GATE       `check.sh --gate qemu-virtio-iommu-x86_64` for the observable effect, plus the named
                 host tests below - each debt ID has its own case and each is watched to fail

  CLASSIFICATION: `driver` (maintenance of one that ships) AND `DMA-capable`, so BOTH prerequisite
  rows apply - the every-driver row and the DMA row - along with the three measured P02M0162 tick
  budgets. Naming only the first is what let the `REQUIRES` block above drop P02M0153: a
  classification that omits a row omits its prerequisites, and this per-item block is the executable
  list an implementer works from.
  ITS ACCEPTANCE CONTRACT INCLUDES THE DMA HALF: every buffer this driver hands the device is a live
  IOVA mapping in its own binding's domain, and the teardown returns them - which is P02M0153's
  contract, tested here rather than assumed from the enforcing gate's presence.

  WHAT IT OWNS, FILE BY FILE:
  - `src/user/drivers/core/src/virtio_blk.rs` - `serve_write`'s source-object contract and the count
    and LBA validation on read and write;
  - the shared virtio queue path - `src/user/drivers/core/src/virtio.rs` - for DRV-001;
  - `src/user/services/manifest.toml` - one `dma-policy` row for this driver.

  THE THREE-PART CONTRACT, STATED SO IT IS TESTABLE (DRV-003 / WIRE-002). `serve_write` computes
  `bytes = count * SECTOR`, checks `span.fit(bytes)` - which bounds the DESTINATION DMA span - and
  then copies `bytes` OUT of the mapped source without ever asking how big the source is. Before the
  copy it must check, on the handle the caller transferred:
    TYPE     `object_type == OBJECT_TYPE_MEMORY_OBJECT`. `sys_memory_map` already refuses a wrong
               type, so this is a typed refusal to the caller rather than a fault;
    RIGHTS   `rights & RIGHT_READ`. The mapping path takes `Rights::MAP` and asks for nothing else,
               so a handle without `READ` maps today and is read out of - which is the half of the
               original finding that survived two narrowings;
    SIZE     `size >= bytes`. This is the whole of the copy defect: `count = 1000` against a
               512-byte object drives a 512 kB read out of a 512-byte mapping.
  All three are available from one `object_info` call, so the contract costs one syscall per write.

  DRV-002, THIS ITEM'S HALF: the block path silently clamps a count instead of refusing it and never
  validates the LBA against the device's capacity. A clamp turns a wrong request into a wrong WRITE,
  which is worse than a refusal. Both paths refuse out-of-range with a typed status. The USB
  32-bit-LBA truncation is `xHCI maintenance`'s half of the same ID.

  DRV-001: virtqueue size, used IDs, used lengths and used-index movement are not validated
  centrally, and a device - or anything acting as one - controls those fields. Validated once in the
  shared path so every virtio driver inherits it.

  EXCLUDED, so the item cannot grow: no new block operations, no `virtio-scsi`, no SCSI core, no
  block-wire extraction, no changes to the destination `Span` accounting, and nothing in
  `usb_storage` - the USB half of DRV-002 and DRV-003 belongs to `xHCI maintenance` and is gated
  there.

  FIXTURE AND ORACLE: QEMU's `virtio-blk-pci`, which the harness already configures on all three
  targets and which the isolation gate already boots. The observable effect is real block I/O
  through the enforcing controller - the gate's existing traffic phase - so a regression shows up as
  a boot that no longer reads its volume rather than as a counter.

  MEASURABLE LIMITS: one `object_info` per write request; refusals are typed and allocate nothing;
  and the added validation must not move the P02M0162 bind window beyond its measured budget on any
  of the three targets, which is the inherited requirement the every-driver row states.

  DEFINITION OF DONE FOR ITS CLASS (driver): the driver binds and serves through the governed path
  unchanged; each of DRV-001, DRV-002's block half and DRV-003/WIRE-002's three parts has a HOST
  test that fails when the check is removed, named in the gate; the DMA policy is declared and
  admission reads the generated value; and the isolation gate's traffic phase still obtains a lease
  and reads the volume on x86_64, with the suite green on all three targets.

- [ ] **virtio-net maintenance.** Declare its DMA policy (it is the one device currently in the
  kernel's hard-coded list, so it is the migration's proof case) and gate that the generated registry
  value, not the list, is what admission reads.

- [ ] **virtio-gpu maintenance.** Close DRV-010's display half against the current driver, declare
  its DMA policy, and prove the display endpoint's translated operation on the profiles P02M0173
  names. It was omitted from the first Phase-2 subset and its remaining work was riding on a future
  EDID item that nobody had started.

- [ ] **virtio-input maintenance.** Declare its DMA policy and gate descriptor-parsing host tests
  over the new seam.

- [ ] **virtio-console maintenance.** Declare its DMA policy, and own the `console-bytes` provider's
  migration onto the catalogue - it is the one existing provider whose consumer is still special-
  cased, and the destination-migration table below names it for that reason.

- [ ] **virtio-snd maintenance.** Close DRV-009 against the current driver with its named negative
  test, and declare its DMA policy.

- [ ] **xHCI maintenance.** THE IDS ARE NAMED HERE, NOT DELEGATED TO A SENTENCE (corrected
  2026-09-02). This item listed DRV-003/WIRE-002 and then said generically that "every other High USB
  debt this file still lists closes in this item's gate" - which is the same prose-only correction
  the debt section warns about, one level down: an implementer following this bullet closes one ID
  and the other six are named nowhere they would be read. The debt mapping assigns SIX to this item
  and each is written out below with the assertion that closes it. Declare xHCI's DMA policy and
  record the class-module execution model the USB section below fixes, and close:

    DRV-002 (the USB half)     `usb_storage` clamps the transfer count silently and skips the LBA
                                 range check, AND truncates the LBA to 32 bits. Its negative: a read
                                 whose LBA is above 2^32 and one past the device's last block are
                                 both typed refusals, and neither is served from a truncated or
                                 clamped address
    DRV-003 / WIRE-002         `usb_storage::serve_write`, on the complete type/rights/size
                                 contract, alongside the virtio-blk half. Its negative: a request
                                 whose handle is the wrong object type, whose rights are short of
                                 what the operation needs, and whose declared size disagrees with
                                 the object are three separate typed refusals
    DRV-005                    flush converts every repeated failure into success without reading
                                 sense data. Its negative: a device that fails a flush twice is
                                 reported as a failed flush, with the sense read, and never as
                                 success
    DRV-006                    BOT transfer length and CSW residue are not validated. Its negative:
                                 a CSW whose residue exceeds the transfer, and one whose length
                                 disagrees with what was sent, are both refused rather than being
                                 read as a short success
    DRV-007, DRV-008           port-status events are lost or double-counted. Its negative: a
                                 connect and a disconnect delivered in the same status window
                                 produce exactly one of each, and a storm of them is bounded rather
                                 than growing a queue
    DRV-012                    descriptor transfers are unvalidated. Its negative: a descriptor
                                 whose declared length exceeds what was read, and one whose type
                                 does not match what was requested, are typed refusals

  Any High USB debt this file lists that is NOT in that table is not covered by this item and needs
  its own named owner in the debt section before that item can close - which is the rule stated once
  rather than a sentence this bullet can satisfy by containing.

Each of these states its own three-line REQUIRES/OWNS/GATE triple before it starts, like every other
item in this index. None of them is a new device, none creates a shared contract that does not
already exist, and the debt IDs above are the specific already-shipping defects each one closes.

## 1. virtio devices

SEVEN OF THESE DEVICES RUN IN EVERY DEVELOPER RUN AND EVERY CI BOOT - blk, net, gpu, input, console,
snd and (through xHCI, which is group 2) USB - and the REST OF THIS GROUP DOES NOT (corrected
2026-08-30). The previous sentence said "this is the hardware this system actually runs on" of the
whole group, which is the same false premise the head already corrected for the Phase-2 subset and
which survived here. RNG, vsock, SCSI, crypto, balloon/mem and virtio-fs are not configured by the
harness at all, so for those the "cheapest to write and hardest to leave broken" argument does not
hold and they are ordinary phased backlog. virtio-iommu leads the ordering because it is the
isolation every other DMA driver here is measured against and its backend already exists.

**A DECLARED DMA POLICY PER DRIVER** - NOW OWNED BY [P02M0172](P02M0172.md), NOT BY THIS INDEX
  (2026-08-30). It was added here as a kernel/bus-infrastructure prerequisite when it had no owner;
  it has one now, and an index that keeps its own copy of an owned item is how two plans acquire two
  answers. What stays here is the DEPENDENCY: every DMA-capable item in all three groups is blocked
  on P02M0172, and the paragraph below is retained as the statement of what that milestone is for.
  The rule further down - every DMA driver follows an explicit `iommu-required` versus audited
  trusted-untranslated policy - had no mechanism, and a rule with no mechanism is a sentence.
  `system-manifest` has no such field, and kernel admission reads a hard-coded
  `IOMMU_REQUIRED_TYPES` containing exactly `VIRTIO_TYPE_NET`, defaulting every other device to
  `TrustedUntranslated`. So NVMe, AHCI, HDA, USB and every other DMA-capable driver added here runs
  untranslated by default, and the only way to protect one is to edit that list - the "source `match`
  arm" this milestone's own head forbids.
  P02M0172 owns: a REQUIRED policy field on every driver row in the trusted/generated registry, its
  validation, its propagation to exact-entry kernel claim admission, and refusal of a missing or
  unknown declaration - or of a bind whose declared isolation cannot be satisfied - rather than a
  silent default.
  AND A THIRD VALUE, `none`, WHICH TWO POLICIES CANNOT EXPRESS (2026-08-30). `iommu-required` and
  `trusted-untranslated` are both answers for a BUS-MASTERING driver, and this index also contains
  devices and helpers that never master the bus at all. Kernel claim admission enables bus mastering
  for every claimed PCI function, so labelling a non-DMA driver `trusted-untranslated` preserves
  authority it does not need and makes the registry unable to tell "audited DMA without translation"
  from "must never DMA". `none` is the declaration that it never does, and claim admission must
  honour it by leaving bus mastering OFF.
  Every DMA-capable item in all three groups is BLOCKED on it, because until it exists their declared
  policy cannot be expressed. **P02M0172 now owns and closes this prerequisite**, and this paragraph
  is an ORDINARY REFERENCE rather than a checkbox (corrected 2026-08-30): it was an unchecked item
  while its own text said it is not work in this index, so a reader and every counting tool saw an
  open task that can never close here. Nothing in this index ticks when P02M0172 lands; the items
  that depend on it stop being blocked.

**virtio-iommu** (a reference, not an item - see the DMA-policy paragraph above for why):
[P02M0153](P02M0153.md) owns the DMA/IOVA contract, its kernel-owned backend
  interface, its host fake and the IOMMU-absent policy - they MOVED there from P02M0098 - and it
  implements the backend and closes the first x86_64 EDU + virtio-net security slice. WHAT IS LEFT
  HERE IS A ROLLOUT AND IT IS NOT YET WORK: "the remaining drivers and architectures" is not a list,
  and an item without one cannot be picked up. It is therefore NOT a checkbox of this roadmap: the
  per-driver attachment belongs to the driver items that need it, expressed through the declared
  policy field above. P02M0173 owns the bounded emulated rollout of the existing virtio-IOMMU backend
  to the AArch64 and RISC-V QEMU profiles. The remaining NATIVE ARCHITECTURE work - VT-d, AMD-Vi,
  ARM SMMU and a native RISC-V IOMMU - is Phase 5's, one approved backend at a time. What survives
  here is the rollout's ordering statement and nothing that anybody closes - which is why this is no
  longer written as a checkbox. Device DMA remains denied until attachment succeeds; driver crash
  tears mappings down. Native backends remain separate Phase-5 work to the same contract.
- [ ] **virtio-rng**: add a bounded entropy-source driver and a typed kernel/security
  ingestion contract that credits source quality conservatively, mixes rather than
  exposes raw host bytes, handles short/failing requests and never treats one VM source
  as proof of cryptographic health. Cover deterministic test injection, startup without
  entropy and driver restart without repeating previously delivered output.
- [ ] **virtio-serial multiport**: generalize `virtio-console` from one debug console to
  dynamically named, hotpluggable ports with bounded control messages, per-port queues,
  open/close state and independent capability-scoped byte streams. Reserve the emergency
  console path, but let provisioning/diagnostic agents use separate ports instead of
  multiplexing an ad-hoc protocol over console bytes. Reuse P02M0104's specification-compliant
  static named development-port foundation; this task retains ownership of general
  dynamic discovery, hotplug and provisioning/diagnostic ports.
- [ ] **virtio-vsock**: implement bounded host/guest stream connections with negotiated
  CID, port capability, credit accounting, reset/shutdown and connection limits. Expose
  it as an explicit local transport for provisioning, diagnostics and agents, not as an
  ambient bypass around NetworkService, PermissionManager or authenticated remote-admin
  policy. Test hostile credit updates, peer reset and host disappearance.
- [ ] **virtio-scsi**: negotiate transport features, enumerate bounded targets/LUNs,
  support read/write/flush and hotplug behind the canonical block contract, and cap
  queues, CDB/sense lengths and outstanding tags. IT OWNS THE TWO SHARED CONTRACTS ONLY IF IT IS THE
  FIRST OF THEIR CONSUMERS TO BE IMPLEMENTED, which is the rule this file states everywhere else and
  contradicted here (corrected 2026-08-30): UAS is independently ordered and free to be written
  first, and a fixed owner contradicted by a non-gating order is not an owner. Whichever of them is
  written first builds the contracts below; the others consume them. They are TWO contracts with
  different consumers, and an earlier draft ran them together. There is no common SCSI command/sense layer (SCSI lives inside
  `usb_storage.rs`) and no canonical block wire (copied by hand into three places). The
  SCSI core is needed by this item, by UAS, and by the USB BOT code it is EXTRACTED FROM -
  which is the third consumer and the one an extraction creates: BOT moves onto the common
  core in the same change, or the extraction has produced a second copy rather than a
  shared one. WHICHEVER OF THE THREE IS IMPLEMENTED FIRST OWNS THE CORE, per the
  first-implemented-consumer rule in the head; this item does not own it by being listed
  first. NVMe has its own command set and AHCI speaks ATA, so neither needs it.

  THE BLOCK WIRE HAS A RULE RATHER THAN A LIST, because a list of three was already wrong:
  UAS publishes block, so can SDHCI, and so can NFIT. It is owned by THE FIRST IMPLEMENTED
  SLICE THAT PUBLISHES A BLOCK PROVIDER, whichever that turns out to be. Naming
  `virtio-scsi` as the owner of both contracts made NVMe and AHCI wait on a SCSI layer
  neither of them uses; naming three candidates for the block wire then left out the
  others in the same breath.
  It complements rather than replaces `virtio-blk`; unsupported passthrough commands are
  not exposed to ordinary storage clients.
- [ ] **virtio-crypto**: negotiate only explicitly supported algorithms and create
  bounded session/key handles for selected symmetric/hash operations through a typed
  crypto-provider contract. Secret key material is transferred once under a narrow
  capability, never readable back, zeroized on teardown and not assumed safer than the
  host. Keep software cryptography as the correctness baseline and reject migration/
  replay-sensitive offload until its threat model is written.
- [ ] **virtio-balloon and virtio-mem**: add separate policy-controlled VM memory
  providers only after ResourceManager has explicit reclaim/hotplug contracts and the
  kernel can safely offline, pin and return pages - NEITHER OF WHICH HAS AN OWNER, here or
  anywhere else, which makes this item's precondition a wait with nothing at the end of it.
  This item owns them or it does not start: reclaim and hotplug in ResourceManager, and the
  kernel side of offlining a page, are its first work rather than its assumption. Bound request batches, never reclaim
  DMA/mapped/unevictable pages, survive host cancellation and report pressure without
  letting the host directly choose a victim Domain. Keep both protocols distinct.
- [ ] **virtio-fs** as an explicit development-only integration backend: run it behind
  StorageService's capability-scoped Volume API, require an opt-in QEMU/developer flag,
  bound requests/names/xattrs and prevent path traversal or host-node identity leakage.
  It is never the system-volume format, production default or an ambient host-filesystem
  mount; LiberFS and the ordinary package/staging pipeline remain canonical. P02M0104 does
  not depend on this backend and continues to publish executable generations through its
  validated artifact protocol after virtio-fs exists. Test that a process without the
  granted volume cannot observe the host share.

## 2. Generic and universal controllers

Standardized silicon interfaces: one driver per specification, every vendor's part behind it. Ordered
by what a real machine cannot be used without - storage and console first, because a machine that
cannot read its disk or print a line cannot be debugged - with server and legacy compatibility paths
at the end.

- [ ] **NVMe** generic PCIe storage controller: controller reset/enable, admin plus I/O
  queues, identify controller/namespaces, PRP transfers, flush, read/write, status and
  timeout recovery behind the existing block contract used by `virtio-blk` and USB
  storage. Bound queue depth and transfer size, validate every completion ID/phase,
  support multiple namespaces/controllers and cleanly fail unsupported metadata,
  protection, zoned or vendor commands rather than partially interpreting them.
- [ ] **AHCI/SATA** standard HBA: enumerate implemented ports, identify SATA devices,
  issue bounded DMA read/write/flush commands, handle NCQ only after the single-command
  path, detect hotplug and recover a failed port without resetting unrelated ports.
  Serve the existing block contract and reject unsupported ATAPI/port-multiplier paths
  explicitly. Keep controller quirks and non-AHCI vendor modes in Phase 5.
- [ ] **UEFI GOP / simple-framebuffer handoff**: formalize the already implemented boot
  framebuffer discovery as one architecture-neutral immutable descriptor and a generic
  early-display provider with checked geometry/pixel masks/cache policy. Define the
  ownership handoff to DisplayService/real GPU drivers, retain console output when no
  GPU binds and test x86_64/aarch64/riscv64 UEFI plus direct-boot simple-framebuffer
  paths. This is consolidation of existing paths, not a second framebuffer stack.
- [ ] **16550 UART** (BLOCKED on the authority and handoff mechanisms named above, or scoped to
  firmware-described MMIO and blocked on the identity prerequisite): retain the minimal existing
  kernel early-console path, then hand a
  discovered standard UART's MMIO/PIO and IRQ capabilities to a restartable userspace
  serial driver after DeviceManager is online. Support FIFO, RX/TX interrupt flow and
  bounded buffering behind the canonical serial contract; isolate baud/clock/platform
  description from the common 16550 register engine and preserve panic output fallback.
  AND THE SERIAL CONTRACT HAS THREE CLAIMANTS AND NO DEFINITION. "The canonical serial contract" does
  not exist. USB CDC-ACM defines "one typed serial/byte-stream contract" a hundred and twenty lines
  further down; virtio-serial multiport, in GROUP 1 and therefore ahead of both, gives its ports
  "independent capability-scoped byte streams". Whichever of the three is written first defines it
  and the other two use it - and the first one has to say whether a virtio port, a UART and a USB
  adapter are one contract, or whether the byte stream is the base and the line-coding and control
  lines are a serial layer on top. That question is answered once, by whoever gets there first, and
  none of the three may assume it was.
- [ ] **ARM PL011 UART**: BLOCKED: the firmware-node device identity is an unowned architectural
  prerequisite (see the identity section above) - this driver binds through an ACPI/FDT node rather
  than a PCI function, and that identity has no numbered owner. Then: perform the same early-console
  -> userspace handoff for the existing PL011 path, sharing the serial contract, buffering, lifecycle
  and tests with 16550 while retaining its distinct register/interrupt implementation. Bind through
  ACPI/FDT compatible identity and supplied clock/IRQ data, not a QEMU address constant.
- [ ] **SDHCI** standard SD/eMMC host controller: command/data inhibit handling,
  capabilities, voltage/clock negotiation, card identification, block addressing,
  PIO first and bounded ADMA only after the simple path is proven; expose removable
  media through the existing block contract. Keep the SD protocol core independent of
  PCI/ACPI/FDT attachment so board-specific clock, reset, regulator and pinmux glue can
  remain outside the universal driver. Cover insertion/removal and read-only cards.
- [ ] **EDID/DDC display discovery**: add one bounded EDID parser and typed monitor mode/
  physical-size/capability vocabulary shared by simple framebuffer, virtio-gpu and later
  real display drivers. Verify header/checksums, cap extension blocks and reject malformed
  timings before arithmetic. DDC/I2C/AUX are transport backends supplied by the bound
  display controller; EDID is a helper/provider contract, not an independent process
  poking arbitrary buses.
- [ ] **PCIe hotplug, AER and PME** shared bus infrastructure: process standardized slot
  notifications, enumerate/remove functions dynamically, surface corrected/uncorrected
  Advanced Error Reporting records and power-management events, and coordinate safe
  driver stop before resource removal. Bound capability walks and event storms; a fatal
  error quarantines the affected function/subtree rather than resetting unrelated PCIe
  devices. Native platform slot controllers remain firmware glue behind this contract.
  THIS ITEM OWNS THE LIFECYCLE IT NEEDS, because nothing else does: `BindingState` has no
  `Removed` and cannot, since the inventory is boot-only. So this item adds the removal
  event, the transition through `Stopping`, and a terminal `Removed`. TERMINAL AND
  REPLUGGABLE ARE NOT A CONTRADICTION, AND THE MODEL HAS TO SAY WHY: the slot outlives the
  device as a tombstone, `Removed` is terminal FOR THAT BINDING, and only a NEW ARRIVAL on
  that slot opens a new one - whose claim mints the next generation, so a message stamped
  with the old one is refused by arithmetic rather than by anyone remembering. A removal
  whose teardown was NOT confirmed ends in `Quarantined`, not `Removed`: `Removed` says the
  device is gone and nothing is owed, which is the one thing an unconfirmed teardown cannot
  say. It also reaches into two neighbours and says so rather than assuming: P02M0165 gains
  a device-removed stop intent, and P02M0166 gains a rendering for `Removed`. A USB or SD
  child disappearing is NOT any of this: it withdraws a provider and its parent controller
  stays `Online`.
- [ ] **TPM 2.0 TIS/CRB**: discover either standardized transport, serialize bounded TPM
  commands, enforce locality/timeout/cancel, validate response headers and expose only
  typed operations needed for random data, PCR measurement/quote and sealed keys.
  Applications never receive raw MMIO or unrestricted command passthrough; integrate
  with future measured/verified boot and identity policy without making TPM presence a
  boot requirement. Vendor firmware and proprietary security processors remain Phase 5.
- [ ] **FADT and GAS parsing** (SHARED LIBRARY - closes on host tests, binds nothing). Split out on
  2026-08-30, because the item below claimed the kernel "already reads" the FADT and it does not:
  the kernel has a GENERIC x86 ACPI table lookup and parses `APIC`, `SRAT` and `SLIT`; the only FADT
  consumer in the tree is the UEFI loader's ARM PSCI-field decoder. Absence of AML does not supply an
  FADT parser. This item owns bounded FADT field extraction and Generic Address Structure decoding
  with hostile-input fixtures over truncated, mis-versioned and out-of-range tables, and it publishes
  no device and claims nothing.

- [ ] **ACPI fixed-hardware event delivery** (ARCHITECTURAL PREREQUISITE - a decision plus a
  mechanism). A PM1a status/enable pair reached through a GAS is either an SCI the KERNEL routes and
  hands on, or a claimable firmware-described node with a port-I/O or MMIO resource. That choice is
  not a detail of the power button, and until it is made no fixed-hardware ACPI event has a delivery
  path. It owns: the decision, written down; ACPI SCI routing - a shared, level-triggered legacy
  interrupt, which the MSI-X path does not provide; the handoff to whatever consumes the event; and,
  if the answer is a claimable driver, a RANGE-SCOPED REVOCABLE PORT-I/O RESOURCE KIND, since the
  public driver resource vocabulary is device MMIO, one IRQ, keys, system power and console and has
  no port-I/O object at all. Its fixture is named here too, because the item cannot be gated without
  one: a QEMU machine whose PM1a event can be raised on demand.

- [ ] **ACPI fixed-hardware power button** (driver, or in-kernel consumer if the item above decides
  so): publish a typed power-button provider with a capability-gated SHUTDOWN request, over the
  parser and the delivery path above. Validate event storms; keep board EC methods in Phase 5.
  SUSPEND IS NOT IN THIS ITEM (2026-08-30). It was listed beside shutdown, and the `system-power`
  interface has exactly two operations - `reboot` and `power-off` - so a suspend request had nothing
  to call. Suspend is a platform-lifecycle feature with its own device-quiesce, memory and wake
  contract; it is a separately approved item and is named as a prerequisite by anything that wants
  it, not smuggled in as a second verb on a button driver.
  This item is NOT closable today, and the previous version said it was on the strength of the FADT
  claim above.

- [ ] **ACPI battery, AC and thermal classes** (BLOCKED THREE TIMES: on the FIRMWARE-NODE IDENTITY
  prerequisite - `PNP0C0A`, `ACPI0003` and `THERMALZONE` are ACPI NAMESPACE devices, and the identity
  section above says in as many words that a namespace device is not expressible by the PCI-shaped
  binding identity this system has; that block was missing from this bullet and the rule says every
  consumer carries it in its own (added 2026-09-02) - on the AML prerequisite below, AND on
  the unowned PLATFORM POWER-STATE service - `system-power` is the narrow reboot/power-off interface
  and there is no typed battery, AC or thermal destination to publish to, so AML alone would leave
  this item with values it can read and nowhere to put them): publish typed battery/AC and
  thermal-zone state behind that service. `_BST`, `_BIF` and `_TMP` are
  CONTROL METHODS, and this kernel has no AML interpreter and no ACPI namespace at all: it reads
  `APIC`, `SRAT` and `SLIT` and nothing else. Validate package/object depth and event storms; keep
  fan curves and vendor power policy in Phase 5.
  SPLIT HERE RATHER THAN LEFT TO THE IMPLEMENTER (2026-08-30). This was one checkbox whose text told
  whoever picked it up to split it first - which is an instruction to do planning inside
  implementation, and it left a closable item welded to a blocked one.
  Validate package/object depth and event storms; keep board EC methods, fan curves and
  vendor power policy in Phase 5. FDT platforms use equivalent typed providers rather
  than pretending ACPI is universal across firmware ecosystems.
- [ ] **High Definition Audio (HDA)** standard PCI controller: reset and enumerate
  codecs/function groups/widgets, build a minimal deterministic pin -> converter route,
  configure stream descriptor/BDL/rate/format and expose playback/capture through the
  same PCM contract as `virtio-snd` and USB Audio. Bound CORB/RIRB and unsolicited
  responses, isolate codec/controller failure, and leave vendor DSP firmware, jack
  policy and machine-specific routing quirks to Phase 5.
- [ ] **USB Type-C Connector System Software Interface (UCSI)**: BLOCKED: no owned destination
  service or IDL - it needs a typed Type-C service that does not exist. AND on the FIRMWARE-NODE
  IDENTITY prerequisite (added 2026-09-02): the PPM is an ACPI namespace device (`USBC000`), so this
  item cannot be bound at all by the identity this system has, and that block belongs in this bullet
  by the rule the identity section states. AND it NEEDS AN AML
  INTERPRETER on
  x86, where the PPM interface is reached through ACPI - see the power-button item; the
  transport-based variants are a different item. Then: discover firmware-
  described connectors, consume bounded notifications and expose connector, data/power
  role, orientation, alternate-mode and charging state through a typed Type-C service.
  Capability-gate role swaps and power-policy changes; keep USB-PD policy in the owning
  service and vendor retimers/EC transports in Phase 5. Test notification storms,
  connector removal and firmware timeouts.
- [ ] **ACPI WDAT hardware watchdog**: BLOCKED: no owned destination service or IDL - the narrow
  watchdog service below is named and unowned. Then: parse the bounded watchdog action table, validate
  register regions/instruction sequences, expose arm/pet/disarm/status through a narrow
  watchdog service and integrate ownership with ServiceManager's unattended recovery
  policy. Never execute arbitrary AML or table writes from an untrusted client; prove
  expiry/reset in a fake platform and preserve a clean no-watchdog boot path.
- [ ] **ACPI Time and Alarm Device**: BLOCKED on the FIRMWARE-NODE IDENTITY prerequisite, and NEEDS
  AN AML INTERPRETER - ACPI000E is a namespace
  device with control methods, and this kernel reads static tables only. The bullet named only the
  interpreter (corrected 2026-09-02): "a namespace device" is exactly what the identity section says
  the current binding identity cannot express, so an AML interpreter alone would leave this item with
  methods it can evaluate and no way to be BOUND to the device that owns them. Then: bind it, expose
  bounded read/set/alarm operations as one optional TimeService source and preserve RTC/
  NTP fallback. Validate GAS/register widths and alarm ranges, capability-gate clock and
  wake changes, and keep vendor RTC/EC implementations in Phase 5. Absence never blocks
  boot or wall-clock service.
- [ ] **EHCI** standard USB 2 host controller as a lower-priority compatibility path:
  reuse USB enumeration/class drivers above one host-controller interface, implement
  bounded async/periodic schedules, split transactions and root-hub events, and prove
  HID/storage/class parity with xHCI. It must not fork USB descriptors, class state or
  policy. Add only after current xHCI is the measured reference implementation.
- [ ] **OHCI and UHCI** legacy USB 1.x host controllers as the final compatibility wave:
  separate controller engines share the same USB core/class contracts, bounded transfer
  ownership and hotplug semantics. Require QEMU fixture coverage and a demonstrated
  target need before enabling either by default; no legacy polling path may weaken IRQ,
  DMA cleanup or controller-process isolation.
- [ ] **IPMI KCS and BT transports** (SSIF is a separate item below): BLOCKED: no owned destination
  service or IDL - the higher management service is named and unowned.
  KCS AND BT ARE ONE ITEM AND SSIF IS NOT (split 2026-08-30): KCS and BT are host-side register
  interfaces discovered through fixed I/O or MMIO, while SSIF is an SMBus client that needs the I2C
  controller and provider this index elsewhere says does not exist - so bundling all three made one
  bullet whose third transport had a hidden prerequisite the other two do not.
  Implement the standardized host-to-BMC
  transports behind one bounded IPMI message contract, with request serialization,
  sequence/timeout recovery and explicit BMC disappearance. A higher management service
  owns sensors, SEL, chassis and authentication policy; raw commands require an admin
  capability. Prefer this in the server deployment wave and leave vendor BMC extensions
  unsupported by default.
- [ ] **IPMI SSIF transport**: BLOCKED TWICE - on the same unowned management destination as KCS/BT,
  and on the I2C controller and provider, which this index states do not exist. It is the same
  message contract over an SMBus client rather than a register interface, so it reuses the item
  above and owns only its transport. It may not be started until both blocks are lifted.
- [ ] **ACPI NFIT / NVDIMM**: parse bounded SPA ranges, region mappings, flush hints and
  health records, expose persistent-memory namespaces as typed block or direct-access
  providers only after persistence ordering and poison handling are defined. Validate
  overlap/alignment against the physical map and never map firmware control regions into
  clients. This is a server-priority universal path; vendor management commands and
  platform-specific interleave quirks remain Phase 5.

## 3. USB device classes and the remainder

Most of these sit on the xHCI stack that already exists, so each is a class driver rather than new
controller machinery, which is why the group follows the controllers. HID over I2C is the exception
and is here for its class rather than its transport: it reuses the HID parser and reaches its device
over a firmware-described I2C controller, so it is also the first item in this group that needs the
firmware identity named above. Ordered by what a person sitting at the machine notices, with the
mutating and niche transports last.

- [ ] **THE USB EXECUTION MODEL IS DECIDED ONCE, BEFORE THE FIRST USB ITEM IN THIS GROUP** - and
  "USB" is the word that matters, because HID over I2C is in this group and does not wait on it. It
  attaches to no xHCI controller, holds no USB claim, and neither of the two answers below is about
  anything it does; gating it on this decision would hold a non-USB driver behind a USB one for no
  reason but its position in a list. Today HID and mass storage are MODULES INSIDE the xHCI
  process, and the controller holds a single
  `Option<(UsbDevice, Storage)>` - one storage device, in one Domain, with one claim. Every bullet
  below asks for a registry entry, a claim, a transactional bind and DeviceManager binding per
  driver, and neither shape gives it that. Two answers, and this item is choosing between them:
  class drivers stay inside the xHCI Domain, in which case the per-driver registry, claim and bind
  requirements do not apply to them and this document says so; or one USB-interface binding unit
  exists, with endpoint capabilities, its own generation and composite devices, in which case it is
  a SUB-FUNCTION IDENTITY, which is the same cross-cutting case as the firmware node and is
  therefore NOT owned by a class driver either (corrected 2026-08-30: this said the first implemented
  USB-class item owns it, while the identity section above expressly removes sub-function identity
  from driver ownership - the fixed-name rule applied to the one class of contract this file says
  must not have it). If the second answer is chosen, the USB-interface binding unit is an UNOWNED
  BLOCKED PREREQUISITE on the same terms as the firmware node, and every USB class item is blocked
  until it is numbered. That cost is exactly why the first answer exists.
  P02M0163 refused the second as "splitting a driver into several and giving each its own authority"
  and said the milestone that needs it builds it. This is that milestone, and the choice is its first
  item rather than a thing each class driver rediscovers.

  **AND IF THE FIRST ANSWER IS CHOSEN, THE CLASS MODULE NEEDS ITS OWN CLASSIFICATION AND ITS OWN
  DEFINITION OF DONE** (added 2026-08-30). The head classifies an item as a `driver` when it
  independently claims and binds a device, and a `shared library` when it has no device and no
  binding. An in-controller class module is NEITHER: it touches a real device and it does not claim
  one, so under the current classification it either inherits P02M0098/P02M0161/P02M0162 gates it
  cannot satisfy or falls through to a parser's gates that prove nothing about a device. Both
  readings were available, which is how a USB item could have closed against whichever it preferred.

  So the head's classification gains a fifth kind, `in-controller class module`, and it closes on a
  contract of its own:

    authority     the module holds NO claim and NO Domain of its own; it operates strictly through
                    the controller's claim and the controller's resources, and the item states which
                    controller operations it may invoke and which it may not
    isolation     a bound per-class resource budget inside the controller's Domain - endpoints,
                    buffers, in-flight transfers - so one class module cannot starve another or the
                    controller, with a typed refusal at the bound
    interface     ONE named owner for the controller-to-class-module interface, which is the
                    controller item, not each class module; a class module that needs a new
                    controller operation extends that interface rather than reaching around it
    lifecycle     attach, detach and per-class teardown driven BY THE CONTROLLER, replacing the
                    independent bind/rollback a driver would have. Detach releases the class
                    module's resources and leaves the controller serving; controller teardown tears
                    down every module with it
    gate          the observable effect through the destination service, plus a detach-under-load
                    case and a per-class budget-exhaustion refusal - the two things an independent
                    bind would otherwise have proved

  The prerequisite matrix reads that classification: an in-controller class module is NOT on the
  every-driver row, and it does NOT separately need P02M0172 - the CONTROLLER's declared DMA policy
  covers every transfer it performs, which is precisely what makes the module not a driver. If the
  second answer is chosen instead, class drivers ARE drivers and every row applies to them
  unchanged. Each USB item below states which model it was written against, and the models are not
  mixed within one item.

- [ ] **Generic USB HID expansion** beyond the implemented keyboard and pointer paths:
  extend the shared report-descriptor parser and InputService vocabulary for gamepads,
  joysticks, consumer controls, tablets and multi-touch collections. Preserve usage
  page/id, logical ranges, units, contact identity and simultaneous controls instead of
  flattening every device into keys or a mouse. Bound report sizes, collection depth,
  contact count and malformed descriptors; existing keyboard/pointer behavior must not
  regress.
- [ ] **USB CDC-ACM** serial class: bind communication/data interfaces and alternate
  settings, parse functional descriptors, serve bounded RX/TX with line-coding and
  control-line operations, propagate disconnects, stalls and short packets, and define
  one typed serial/byte-stream contract reusable by console tools, MCU links and modems.
  Exercise composite devices and several simultaneous adapters; do not expose a global
  ambient `/dev/tty*` namespace.
- [ ] **USB Attached SCSI (UAS)**: reuse the bounded SCSI command/sense layer from BOT
  while implementing UAS command/status/data pipes, stream IDs, tagged concurrency,
  task management, timeout and endpoint recovery. Expose the same block contract and
  retain BOT as a negotiated fallback for devices that offer both. Cap outstanding
  commands and reject duplicate/unknown tags or inconsistent residue deterministically.
- [ ] **USB CDC-ECM and CDC-NCM** network classes: share descriptor/control plumbing but
  keep their frame formats separate; validate MAC/MTU/filter descriptors, bound NCM
  NTB/datagram tables and malformed offsets, and feed the same capability-scoped frame
  transport NetworkService already consumes from `virtio-net`. Cover link transitions,
  zero-length/short transfers, disconnect and backpressure without a second network
  stack inside the driver.
- [ ] **HID-over-I2C descriptor and protocol** (SHARED LIBRARY - closes on host tests, binds nothing):
  the standardized HID-I2C descriptor and register protocol over a bounded I2C bus contract and its
  host fake. No I2C controller driver exists and `ProviderKindName` has no I2C kind, so there is no
  bus to bind to and no QEMU model to bind against - which is what makes this half a library rather
  than a driver, and what lets it close.
  THE I2C CONTRACT IS OWNED BY THE FIRST IMPLEMENTED CONSUMER, not fixed to this item (corrected
  2026-08-30). This said "AND IT OWNS THE I2C CONTRACT" while the shared-contract rule three hundred
  lines below assigns it to whichever consumer is written first - and the earlier IPMI SSIF transport
  consumes the same bus class. Two statements, one contract, opposite owners.

- [ ] **HID-over-I2C binding** (BLOCKED TWICE: on an approved I2C controller item with a real
  fixture, AND on the unowned firmware-node device identity - this driver reaches its device over an
  ACPI/FDT-described I2C controller, which the group introduction already says and this bullet did
  not carry):
  reuse the generic HID report parser and expanded InputService
  vocabulary over the standardized HID-I2C descriptor/register protocol. Keep the HID
  transport independent of concrete I2C controllers; ACPI/FDT attachment supplies only
  the bus capability, address, IRQ/reset and descriptor location. Bound report lengths,
  power/reset transitions and interrupt storms; support touchpads/touchscreens without
  product-ID bindings.
- [ ] **USB Audio Class 1/2**: parse audio-control topology and streaming alternate
  settings, choose only explicitly supported PCM formats/rates/channels, schedule
  bounded isochronous transfers and adapt clock/feedback endpoints without unbounded
  drift. Present the same PCM transport contract as `virtio-snd` to AudioService;
  support playback and capture independently, propagate unplug/xrun, and reject DSP,
  MIDI and vendor extensions until their own vocabularies exist.
- [ ] **USB Bluetooth HCI transport**: BLOCKED: no owned destination service or IDL - the sandboxed
  Bluetooth service below is named and unowned. Then: bind the standard USB Bluetooth class endpoints,
  carry bounded HCI command/event/ACL/ISO packets over a capability-scoped controller
  transport and recover stalls/unplug. The driver stops at HCI: pairing, keys, L2CAP,
  profiles and radio policy belong to a separately sandboxed Bluetooth service, whose
  size/risk does not leak into xHCI or the transport process.
- [ ] **USB HID Power Device class**: BLOCKED: no owned destination service or IDL - the platform
  POWER-STATE service is named and unowned, and `system-power` carries reboot and power-off and
  nothing else, so this driver has no typed UPS or battery destination and no observable-effect path.
  Then: extend the common HID usage/value machinery for UPS, battery, load, voltage, runtime and
  alarm reports, publishing normalized state to that service. Capability-gate output switching and
  shutdown commands;
  bound collection/report complexity and preserve unknown vendor usages without
  interpreting them. Reuse generic HID transport instead of adding a UPS-specific USB
  parser.
- [ ] **USB CCID** smart-card class: BLOCKED: no owned destination service or IDL - the
  capability-scoped smart-card service is named and unowned. Then: parse class/slot capabilities, power and negotiate
  card parameters, exchange bounded APDUs with timeout/abort and surface insertion/
  removal through a capability-scoped smart-card service. Do not place PIN handling or
  authentication policy inside the transport driver; malformed lengths, sequence IDs
  and unsolicited slot changes get deterministic tests.
- [ ] **USB CDC-MBIM**: BLOCKED: no owned destination service or IDL - the typed modem service is
  named and unowned; NetworkService alone does not carry SIM, network or link state. Then: bind
  the standard mobile-broadband control/data interfaces,
  validate fragmented control messages and session/NDP tables, model SIM/network/link
  state through a typed modem service and pass IP datagrams to NetworkService under an
  explicit grant. Bound outstanding transactions and datagrams; no modem-management
  command or subscription identity becomes ambient authority.
- [ ] **USB Video Class (UVC)**: BLOCKED: no owned destination service or IDL - the camera
  vocabulary and its service do not exist, and this item explicitly refuses to tunnel video through
  DisplayService. Then: first add a bounded camera vocabulary for format/frame
  enumeration, negotiated stream parameters, timestamps and capability-scoped frame
  buffers; do not tunnel video through DisplayService. Then parse UVC control/streaming
  descriptors, negotiate uncompressed or already supported compressed formats, assemble
  payload headers into bounded frames over isochronous/bulk endpoints, and recover from
  frame loss, malformed FID/EOF sequences and unplug. No image decoder is duplicated in
  the driver; consumers use the P02M0093/P02M0096 codec leaves.
- [ ] **USB MIDI**: BLOCKED: no owned destination service or IDL - the shared bounded
  event/timestamp vocabulary it names does not exist and MIDI routing is explicitly outside
  AudioService. Then: support MIDI 1.0 event packets and the standardized USB MIDI 2.0/UMP
  path only after a shared bounded event/timestamp vocabulary exists. Preserve cable/
  group identity, ordering and timestamps, cap queues and SysEx size, recover from
  malformed packets/unplug and keep MIDI routing outside AudioService's PCM stream.
- [ ] **USB printer class**: BLOCKED: no owned destination service or IDL - the spool/stream service
  is named and unowned. Then: implement standard bulk printer transport, port status and
  reset behind a bounded spool/stream service; applications receive a job capability,
  not raw endpoints. Begin with already-rendered printer languages and explicit status;
  page description/rendering, discovery UI and vendor maintenance protocols live above
  or outside the driver. Cover backpressure, paper/error status and unplug mid-job.
- [ ] **USB Picture Transfer Protocol (PTP)**: BLOCKED: no owned destination service or IDL - the
  media/import service is named and unowned. Then: enumerate storage/object metadata and
  stream bounded object chunks through a media/import service rather than mounting a
  camera as an arbitrary block volume. Validate container length/transaction ID,
  paginate large object sets, preserve read-only operation first and capability-gate
  delete/capture. MTP/vendor extensions remain typed unsupported until separately
  specified.
- [ ] **USB Device Firmware Upgrade (DFU)**: BLOCKED: no owned destination service or IDL - the
  trusted UI/admin policy path it requires is named and unowned. Then: enumerate/download/upload/status state
  machines with transfer-size and manifestation handling, but expose mutating firmware
  operations only through a dedicated high-risk capability and explicit trusted UI/
  admin policy. Validate target identity and, where available, signed image metadata;
  survive disconnect and failed manifestation without claiming rollback the device
  cannot provide. Runtime mode is supported before DFU bootloader quirks.

## Where each item's specification and reference implementation come from

Written down because "is this implementable" was asked and the answer is not uniform, and because
two of the three obstacles have nothing to do with hardware.

THE READY-MADE DRIVER IS ALMOST ALWAYS LINUX AND ALMOST ALWAYS GPL-2.0, which cannot be copied into
this tree without changing what this tree is. Linux stays useful as a description of what real
hardware DOES - ordering, quirks, the register write that has to come second - and not as code. What
follows names references that can be read with a clear head - MOSTLY permissively licensed, and where
one is not the table says so IN THE ROW and the rule changes with it. (Corrected 2026-08-30: this
sentence said "permissively licensed" of the whole table, while three rows are LGPL and one is
GPL-2.0. Corrected again 2026-08-30: the three tables' third column was still HEADED "Permissive
reference" over those rows, and `docs/todo/TODO.md` still summarised the index as giving every item
a permissive reference - so a corrected prose policy sat above two places an implementer is more
likely to read. The column is now "Reference implementation and permitted use", every non-permissive
row carries its licence and OBSERVATIONAL ONLY, and the roadmap row says the same.)

The rule, per row: a PERMISSIVE reference may be read AND adapted with attribution; an LGPL or GPL
reference is OBSERVATIONAL ONLY - read to learn what the hardware does, never copied, never derived
from, and implemented clean under the same boundary this file already applies to Linux. A row whose
third column carries a licence marked OBSERVATIONAL ONLY is under the second rule regardless of what
any heading says:

- **`virtio-drivers`** (rust-vmm), MIT / Apache-2.0 - `no_std` Rust, the whole of group 1's shape.
- **FreeBSD / NetBSD / OpenBSD**, BSD-2/3 - the widest coverage of group 2 and much of group 3.
- **EDK2 / TianoCore**, BSD-2-Clause-Patent - NVMe, AHCI, all four USB controllers, USB mass storage
  and HID, SD/MMC, TPM2, ACPI table parsing. Firmware-shaped, so often the smallest correct example.
- **Redox OS**, MIT - NVMe, AHCI, xHCI, virtio, HDA, in Rust.

AND FOUR ITEMS ARE BLOCKED ON SOMETHING THIS SECTION EXISTS TO MAKE VISIBLE: they need an AML
interpreter and an ACPI namespace, and this kernel has neither. It reads `APIC`, `SRAT` and `SLIT` -
static tables - and nothing else. `_BST`, `_BIF`, `_TMP` and the UCSI PPM interface are control
methods, so those items are not "waiting for a laptop", they are waiting for a bytecode interpreter
the size of the rest of this milestone. Marked **AML** below. The power button is NOT among them: on
a fixed-hardware platform it is a PM1a event named by the FADT, and WDAT and NFIT are static tables.

AND "WAITING FOR AN INTERPRETER NOBODY IS WRITING" IS NOT A STATE A CHECKBOX MAY BE IN (corrected
2026-08-30). This file said elsewhere that the interpreter is a separate milestone and not this one -
and there is no such milestone anywhere in the roadmap, so four checkboxes here had no event that
could ever close them while the Definition of Done demanded they close. Two consequences, both
recorded rather than left implicit:

- The AML interpreter and ACPI namespace are an ARCHITECTURAL PREREQUISITE with no milestone yet.
  It must be separately approved and numbered, with its own security scope - what a control method
  may touch, how an operation region is bounded, what an untrusted caller may never cause to run -
  and its own conformance evidence. Until it exists, the four AML items are NOT part of this
  roadmap's completion set; they are listed here as blocked candidates and are struck from any
  "every item is done" reading.
- The fixed-hardware power-button work is SPLIT OUT from the AML battery/thermal work, because it
  needs none of this: a PM1a event named by the FADT is a static-table path. It does NOT "close on
  its own" - corrected 2026-08-30 - because the parser, the event-delivery decision and the resource
  authority it needs are all missing; it is three items, and they are listed as three.

HID-over-I2C is the same shape and gets the same treatment. Its parser half may need an abstract I2C
contract and a host fake - which it OWNS ONLY IF IT IS THE FIRST IMPLEMENTED I2C CONSUMER, since IPMI
SSIF consumes the same bus class and is independently ordered - and it then defers end-to-end binding
because there is no I2C controller item and no QEMU model, so its hardware half has no event that
closes it either. It is therefore split: the PARSER
AND CONTRACT half is a SHARED LIBRARY item that closes on host tests and hostile fixtures, and the
BINDING half is blocked on an approved I2C controller item with a real fixture. The same split
applies to every other candidate whose only deliverable today is a parser: classify it as a shared
library and close it as one, rather than holding it against a bind gate it cannot reach.

### Group 1 - virtio

| Item | Specification | Reference implementation and permitted use | Note |
| --- | --- | --- | --- |
| virtio-iommu | VIRTIO (OASIS), free | `virtio-drivers` | already bound in this tree |
| virtio-rng | VIRTIO, free | `virtio-drivers` | smallest item in the milestone |
| virtio-serial multiport | VIRTIO, free | `virtio-drivers`, BSD `vtcon` | |
| virtio-vsock | VIRTIO, free | `virtio-drivers` socket device | |
| virtio-scsi | VIRTIO + SCSI SPC/SBC (T10, drafts free) | `virtio-drivers`, BSD `vtscsi` | needs the shared SCSI core; owned by whichever SCSI consumer is implemented first |
| virtio-crypto | VIRTIO, free | spec only; no permissive driver known | least mature virtio device |
| virtio-balloon / virtio-mem | VIRTIO, free | `virtio-drivers` (balloon) | its own prerequisites are the work |
| virtio-fs | VIRTIO + the FUSE protocol | `virtiofsd` (Rust, permissive) - the HOST half | large: FUSE is the real surface |

### Group 2 - generic and universal controllers

| Item | Specification | Reference implementation and permitted use | Note |
| --- | --- | --- | --- |
| NVMe | NVM Express, free and excellent | EDK2 `NvmExpressDxe`, BSD `nvme`, Redox | |
| AHCI/SATA | Intel AHCI 1.3.1, free | EDK2 ATA/AHCI, BSD `ahci`, Redox | |
| UEFI GOP / simple-framebuffer | UEFI spec, free | EDK2 | |
| 16550 UART | NS16550 datasheets, everywhere | anything | trivial |
| ARM PL011 | ARM PrimeCell TRM, free | EDK2, BSD | trivial |
| SDHCI | SD Host Controller *Simplified* Spec, free | EDK2 `SdMmc`, BSD `sdhc` | full spec needs SDA membership; simplified is enough |
| EDID/DDC | **VESA E-EDID/E-DDC need membership** | permissive parsers exist; structure is public | the one place the authoritative text is not free |
| PCIe hotplug, AER, PME | **PCIe base needs PCI-SIG membership** | EDK2 PciBus | register layouts are documented publicly |
| TPM 2.0 TIS/CRB | TCG, free | EDK2 `Tcg2`, BSD `tpm` | transport is small; the command set is not |
| ACPI power button | ACPI (UEFI Forum), free | EDK2 | fixed-hardware PM1a event - **no AML**, but needs the FADT/GAS parser and the event-delivery decision, both separate items |
| ACPI battery, thermal | ACPI, free | — | **AML** |
| HDA | Intel HD Audio 1.0a, free | BSD `hdaudio`, Redox `ihdad` | large: the codec graph is the work |
| UCSI | Intel UCSI, free | — | **AML** on x86 |
| ACPI WDAT watchdog | ACPI, free | — | static table - no AML; QEMU emits no WDAT |
| ACPI Time and Alarm Device | ACPI, free | — | **AML** |
| EHCI | Intel EHCI, free | EDK2 `EhciDxe`, BSD `ehci` | |
| OHCI and UHCI | OHCI free; Intel UHCI free | EDK2, BSD `ohci`/`uhci` | |
| IPMI KCS, BT, SSIF | IPMI 2.0 (Intel), free | BSD `ipmi` | KCS and BT are small |
| ACPI NFIT / NVDIMM | ACPI, free | — | static table - no AML |

### Group 3 - USB device classes and the remainder

Every USB device class specification is a free download from USB-IF. What differs by an order of
magnitude is their SIZE, which is the column that matters here.

| Item | Specification | Reference implementation and permitted use | Note |
| --- | --- | --- | --- |
| Generic USB HID expansion | USB HID + HID Usage Tables, free | BSD `uhid`/`ums`/`ukbd`, EDK2 | the parser exists here already |
| USB CDC-ACM | USB CDC (PSTN subclass), free | BSD `ucom`/`umodem` | small |
| USB Attached SCSI (UAS) | USB UAS + SCSI, free | BSD `umass`/UAS | large: streams and task management |
| USB CDC-ECM and CDC-NCM | USB CDC ECM/NCM, free | BSD `ucdce`/`urndis` | ECM small, NCM framing is the work |
| HID over I2C | Microsoft HID-over-I2C, freely published | Linux `i2c-hid` (GPL-2.0 - OBSERVATIONAL ONLY) | needs the I2C contract; owned by whichever I2C consumer is implemented first |
| USB Audio Class 1/2 | USB Audio 1.0 / 2.0, free | BSD `uaudio` | large, and UAC2 much larger than UAC1 |
| USB Bluetooth HCI transport | Bluetooth Core (HCI USB transport), free | BSD `ubt` | transport is small; the STACK above it is not, and is not this item |
| USB HID Power Device | USB HID Power Device Class, free | usage tables in the spec | small given a HID parser |
| USB CCID | USB CCID (USB-IF), free | pcsc-lite CCID driver (userspace, LGPL - OBSERVATIONAL ONLY) | moderate |
| USB CDC-MBIM | USB CDC MBIM, free | `libmbim` (userspace, LGPL - OBSERVATIONAL ONLY) | sits on NCM plus a control protocol |
| USB Video Class (UVC) | USB Video 1.1/1.5, free | `libuvc` (BSD-3), BSD `uvideo` | **largest item in the milestone** |
| USB MIDI | USB MIDI 1.0, free | BSD `umidi` | small |
| USB printer | USB Printer class, free | BSD `ulpt` | trivial |
| USB PTP | USB Still Image / PIMA 15740, free | `libgphoto2` (userspace, LGPL - OBSERVATIONAL ONLY) | moderate |
| USB DFU | USB DFU 1.1, free | spec is thirty pages | small |

### What this table changes about the plan

- **`virtio-rng` is the cheapest true start**, and it is the only one: free specification, a
  permissive reference, a QEMU model, no AML, and the one contract it creates - typed entropy
  ingestion - has exactly one consumer, which is `SYS_RANDOM_GET`.
- **Everything else drags a shared contract, and the second version of this list denied it as
  confidently as the first.** `NVMe` and `AHCI` came off because the first block driver written owns
  the canonical block wire. But `16550`, `PL011` and `virtio-serial` stayed on under the words "no
  shared contract to create on the way", while this same file says four hundred lines earlier that
  the serial contract has THREE CLAIMANTS AND NO DEFINITION. They are cheap drivers behind one
  decision the three of them share, and whoever writes the first of them makes it.
- **Cheap as a driver is not cheap as a first step**, which is the thing this list keeps getting
  wrong. The question is not how big the driver is; it is how many later items consume what writing
  it forces you to define.
- **The four AML items are not driver work and must not be picked up as if they were.** Whoever wants
  them writes an interpreter first, which is its own milestone and is not this one.
- **UVC, UAC2, HDA, UAS, virtio-fs and MBIM are month-scale**, and saying so here is cheaper than
  finding out in the middle of one.

## Order, gates and completion

- [ ] Land the three groups in this order as a PRIORITY, not as a gate. The first group is the
  virtio TRANSPORT this system already runs on plus the remaining virtio devices that share it; the
  second is what a real machine needs before anything can be installed on it; the third is breadth on
  a USB stack that already exists.
  (It said "the first group is what this system runs on today", and that is the premise the header
  and the group preamble already correct: SEVEN devices are shipped and running in the current
  profiles, and RNG, vsock, SCSI, crypto, balloon/mem and virtio-fs are phased backlog in the same
  group. Read as written, the sentence gave those six the priority of the running system on a
  rationale this file elsewhere disproves. What earns the group its place is the transport they all
  reuse - the work is smaller per device than anything in group two - not that any of them is in use
  today. Corrected 2026-09-01.) Within a group the order is by value
  rather than by cost, except that a cheap item unblocking an expensive one earns its place ahead of
  it. A later item never blocks shipping or testing an earlier finished one - AND AN EARLIER ITEM
  BLOCKS A LATER ONE ONLY WHERE IT IS AN ACTUAL PREREQUISITE. Development-only virtio-fs does not
  hold up NVMe and NFIT does not hold up CDC-ACM; reading the group order as permission would make
  every unfinished item in group 1 a gate on forty others for no technical reason.
- [ ] **THE INDEX GETS A STATE OF ITS OWN** (kernel/bus infra - tooling; owned here because this file
  is the reason it is needed). Two rounds have declared this row "never ticked and counted in no
  phase's completion" and nothing implements it: `docs/todo/TODO.md` carries it as `- [~]`, and
  `check-milestone-index.sh` treats `[~]` as an ordinary OPEN state - its own comment says so - while
  failing only the `[x]`-over-open-tasks direction. So a tool and a reader both still see an open
  Phase-2 milestone, which is the exact ambiguity the correction was meant to remove.

  The bounded change: one explicitly SUPPORTED marker for a non-completable reference row, taught to
  the index checker. It KEEPS the title check, which is the half of that gate worth having on a row
  nobody ever ticks, and it is excluded from any phase-completion reading. The checker's self-test
  gains a case proving it REFUSES an unknown marker rather than silently accepting one - because the
  current script accepts any character between the brackets, so introducing a new marker without
  teaching the checker would produce a row that passes by not being understood. `P02M0103` carries
  the same `[~]` for the same reason and takes the same marker in the same change.

  Until it lands, the claim in the roadmap row is a statement of intent and the row says so.

- [ ] **NO ITEM IS STARTED WITHOUT ITS OWN WRITTEN PLAN.** The rule below says what an item states;
  this says when. Before any code, the chosen item gets an executable item plan naming its
  classification from the list in the head, its exact prerequisites from the matrix in the head, the
  contracts and files it owns, what it explicitly excludes, its target device model or fixture, its
  observable-effect oracle, its measurable limits, and the Definition of Done for ITS CLASS. Writing
  every item's triple in advance is still declined and for the reason already given - inventing
  prerequisites for drivers nobody has started is how a roadmap acquires answers before it has
  questions - but starting an item without one is how two parallel items make incompatible decisions
  about a contract they share.

- [ ] Each item states three things and they fit on one line each: what it REQUIRES that already
  exists, what it OWNS that does not yet, and the one named integration gate that proves it. AND THAT
  GATE IS NAMED CONCRETELY OR IT IS NOT A GATE (added 2026-08-30): the exact host test ids, the QEMU
  model and its arguments or the physical fixture, the target and resource matrix, the OBSERVABLE
  effect in the destination service - this tree's component-oracle convention requires an observable
  effect and rejects "the driver reached `Online`" - the two-instance topology where the standard
  permits one, the teardown assertion, and numeric performance and resource limits. The oracle is
  registered in the verification model in the same change, and any new QEMU device is wired into the
  harness in the same change. Generic prose describing what a gate would check is what this bullet
  replaced and must not come back. THREE
  SHARED CONTRACTS HAVE NO OWNER TODAY and will be discovered by whoever writes the driver that needs
  them: there is no common SCSI layer - SCSI lives inside `usb_storage.rs` - the block wire format is
  copied by hand into StorageService, virtio-blk and USB storage, and the provider vocabulary is a
  closed set of eight kinds in `system-manifest`, which the first item needing a ninth extends.
  P02M0164 catalogues more providers than the services consume; which service consumes which is the
  consuming item's, stated in its own bullet. On a
  target without an enforcing IOMMU every DMA driver in every group still follows P02M0153's explicit
  `iommu-required` versus audited trusted-untranslated policy, DECLARED THROUGH THE REGISTRY FIELD
  the DMA-policy item above owns rather than through a hard-coded kernel list: putting virtio-iommu
  first does not pretend it protects a native platform where that paravirtual provider is absent.

  AND TWO OF THOSE THREE CONTRACTS ARE ALREADY CONTRADICTED IN THIS FILE (corrected 2026-08-30),
  which is what "discovered by whoever writes the driver that needs them" costs when the roadmap
  names an owner anyway. Common SCSI is assigned to virtio-SCSI alone while UAS - independently
  ordered, and free to be written first - requires the same layer; and the I2C contract is assigned
  to HID-over-I2C while the earlier IPMI SSIF transport consumes the same bus class. Both are
  resolved by the rule this file already uses everywhere else and must use here too: the FIRST
  IMPLEMENTED CONSUMER owns the contract, whichever it turns out to be, and the others consume it. A
  fixed owner contradicted by a non-gating order is not an owner.

  AND THE DESTINATION SERVICES ARE NOT ALL PRESENT. HDA and USB Audio are told to reuse the
  virtio-snd transport and CDC networking the virtio-net one, and those are private singleton
  handoffs, not contracts: AudioService receives ONE static `SND` channel over the bootstrap
  positional handshake and the public audio IDL is client-facing; NetworkService receives ONE
  bootstrap `FRAMES` handle with no typed link-state, detach or reconnect path. P02M0164's catalogue
  and its per-consumer `open` are what a second instance must arrive through.

  AND EVERY DESTINATION NEEDS THAT MIGRATION, NOT ONLY AUDIO AND NETWORK (corrected 2026-08-30). The
  previous version assigned it to "the first non-virtio audio or network slice" and left block,
  display, input, pointer and USB with no owner at all - while most of the drivers listed below must
  prove their effect through exactly those services. Merely publishing a provider satisfies no
  integration gate while the destination is still bound to the handle it was given at boot. So the
  rule is per destination, and it is a PREREQUISITE OF THE FIRST ITEM THAT NEEDS THAT DESTINATION
  rather than of one nominated slice:

THE OWNER IS THE FIRST ADDITIONAL PROVIDER OF THAT KIND, WHATEVER ITS TRANSPORT (corrected
2026-08-30). The rows below named "the first NON-VIRTIO slice" and then listed candidates, which
stranded every valid first consumer the list happened to omit: this same file says virtio-SCSI,
SDHCI and NFIT can each be the first new BLOCK publisher, and none of the three is in the old
StorageService row. The transport was never the point - the migration is needed the moment a
destination has a SECOND provider to choose between, and a virtio one qualifies.

AND A TRIGGER THAT HAS ALREADY FIRED IS NOT A TRIGGER (added 2026-09-02). These rows are written as
"the first item that publishes a second provider of that kind", which only works while the kind has
one. Where the shipping manifest ALREADY declares two - which it does for `block`, and does for
`console-bytes` in a development image - the migration is owed NOW, and the row names the item that
owns the second publisher instead of waiting for a third that cannot retroactively be the second.
Every row below was rechecked against the manifest on that date; the rule is written here so the
next reader checks rather than assumes.

    destination        who owns its subscribe + attach/detach/reconnect migration
    NetworkService     the first item that publishes a SECOND network provider - CDC-ECM/NCM/MBIM,
                       or any additional NIC
    AudioService       PARTIAL, AND THE ROW SAID DONE (corrected 2026-08-31). It reads a CATALOGUE
                       connection at bootstrap, subscribes to `audio` and opens a connection to the
                       first provider that appears - which is the DISCOVERY half, and it is what
                       proved the catalogue seam and carried P02M0164's connection-accounting repair.
                       It is not the lifecycle contract this table's own paragraph below requires,
                       and the previous version of this row claimed it was. What is actually there,
                       read out of the code rather than out of the intent:
                         - it opens only while its one slot is empty (`info.live && state.snd == 0`),
                           so a SECOND live provider is seen and ignored - there is no provider
                           identity to hold it under;
                         - on a withdrawal it cannot tell whether the event names the provider it is
                           using, says so in its own comment, and keeps the connection;
                         - a channel failure clears the slot and reopens nothing, so an alternative
                           whose publication was ignored is not picked up;
                         - the virtio-snd side is still an unversioned raw byte-length protocol.
                       So the first item that publishes a SECOND audio provider still owns the whole
                       of the common contract below - provider identity, the versioned device-side
                       PCM contract, attach, detach, failover and reconnect - and not only the
                       device-side half. Reaching a provider through the catalogue is a smaller thing
                       than consuming providers through it, and this row conflated them
    StorageService     ALREADY TRIGGERED, AND THEREFORE OWNED BY NAME (corrected 2026-09-02).
                       This row said "the first item that publishes a second block provider - NVMe,
                       AHCI, UAS, virtio-SCSI, SDHCI or NFIT". The trigger has already fired and
                       none of those six caused it: the shipping manifest declares `block` for BOTH
                       `virtio_blk` and `xhci`, so this machine has had two block providers for as
                       long as USB mass storage has worked. A trigger that has already passed cannot
                       be waited for - a future third provider is not the first second one - so the
                       migration is owed NOW and it is assigned to `xHCI maintenance` in section 0,
                       which is the item that owns the second publisher. That is the same shape the
                       ConsoleService row already uses for the same reason.
                       What it owes is the whole common contract below - provider identity, subscribe,
                       attach, detach, failover and reconnect - and one thing more that is specific
                       to block: the consumer is capped at four probe connections and assigns its
                       boot roles by lowest bus address, so a fifth matching volume is invisible and
                       a role is decided by arrival order rather than by format, origin and
                       `RootSelection`. That cap is P02M0164's own M2 and is recorded there; this row
                       names it because the migration cannot be called done while the destination it
                       migrates still cannot see the providers it is being given
    DisplayService     the first item that publishes a second display provider, and P02M0103's WSI,
                       which needs the same reacquisition after a driver restart - one migration,
                       two consumers
    InputService       the first item that publishes a second input or pointer provider
    ConsoleService     the `console-bytes` provider, whose consumer is STILL SPECIAL-CASED and which
                       was missing from this table entirely (added 2026-08-30). It is owned by
                       `virtio-console maintenance` in section 0, because that provider exists today
                       and virtio-serial multiport is a candidate second one
    the USB stack      the first item that attaches a USB child at runtime

  Each carries the same three things: the versioned DEVICE-SIDE contract for that kind (PCM,
  frame-plus-link, block, scanout, event stream), the service-side attach, detach and reconnect, and
  the migration of the existing singleton off the bootstrap handshake. An item whose destination has
  not been migrated may still be written - it publishes to the catalogue - but it cannot close its
  integration gate, and it says so rather than adding a second private endpoint.

  **AND THE ITEMS WHOSE DESTINATION DOES NOT EXIST ARE MARKED BLOCKED IN THEIR OWN BULLETS, NOT ONLY
  HERE** (2026-08-30). Bluetooth, smart-card (CCID), modem (MBIM), camera (UVC), printer, media
  import (PTP) and DFU/admin are named as destinations in the bullets below and HAVE NO OWNED SERVICE
  OR IDL MILESTONE AT ALL; so are UCSI's Type-C destination, WDAT's watchdog, IPMI's management
  destination, USB MIDI's event/timestamp destination, and the PLATFORM POWER-STATE service that
  `USB HID Power Device class` and `ACPI battery, AC and thermal classes` both publish to - the last
  one was missed when this invariant was first written, which is the failure it names happening to
  the paragraph that names it (added 2026-08-30). A previous round stated the rule here and
  left every one of those as an ordinary unchecked bullet, so an item plan could follow its bullet,
  publish successfully, and have no path to the observable effect its own gate requires - or expand
  into an unplanned service subsystem to invent one.

  Every such bullet carries **BLOCKED: no owned destination service or IDL** as the first words of
  its note, naming the destination it needs. A blocked item may be planned and may not be started,
  and the thing that unblocks it is an approved and numbered service/IDL milestone - not this item
  building one.
- [ ] **THE HOST-TEST STRATEGY DOES NOT COMPILE TODAY, AND THAT IS A PREREQUISITE OF THIS SECTION**
  (added 2026-08-30). Every item below is required to have host tests over its pure parser layer, and
  `cargo test --manifest-path src/user/drivers/core/Cargo.toml --lib` fails before running anything:
  `error[E0152]: duplicate lang item in crate 'std': 'panic_impl'`, because the driver crate
  unconditionally depends on `rt` and `rt` declares an unconditional `#[panic_handler]`. So the
  mandatory strategy is not merely unwritten, it is unavailable.
  **IT IS OWNED BY THE PHASE-2 MAINTENANCE SUBSECTION BELOW, AS ITS FIRST ITEM** (2026-08-30). The
  previous version said "the first item that needs a host test owns the seam", which is not an owner:
  it is a race between whichever parallel item happens to arrive first, and two arriving together
  would build two seams. The maintenance subsection needs it before any of its own items can be
  gated, so it builds it, and every later item in this index consumes it.
  The shape: either a pure protocol/parser layer in its own crate that links no runtime, or a
  host-test feature on the driver crate that drops the runtime entry, panic handler and allocator
  hooks - plus a fake-controller seam the parser tests drive.

  **AND THE GATE FOLLOWS THE SHAPE, RATHER THAN NAMING ONE COMMAND BOTH MUST SATISFY** (corrected
  2026-08-30). A previous version permitted either design and then fixed success to
  `cargo test --manifest-path src/user/drivers/core/Cargo.toml --lib` - a command only the SECOND
  design can ever pass, because the driver crate's dependency on `rt` is unconditional and extracting
  parsers into another crate makes that crate testable without changing what the driver crate links.
  So one permitted implementation could not pass its own gate. The gate is therefore stated per
  design:

    extracted crate     `cargo test` over the EXTRACTED crate runs its parser suite, and the driver
                          crate is proved to contain no parser logic that the extraction was
                          supposed to move - otherwise the extraction is partial and the untested
                          half is the one that ships
    cfg/feature design  `cargo test --manifest-path src/user/drivers/core/Cargo.toml --lib` runs,
                          which is only achievable this way

  Either way a WATCHED-FAIL MUTATION proves a broken parser is caught by whichever suite the design
  produced. What is not acceptable is a gate that names a command the chosen design cannot run.

- [ ] Conformance and hostile-device gates for every family: pure descriptor/register/
  ring parsers get host tests and deterministic malformed mutations before MMIO/DMA;
  fake-controller tests cover completion ordering, timeout, reset, disconnect and
  resource cleanup; QEMU models or USB fixtures prove the real governed process,
  DeviceManager binding and destination service contract. Test at least two instances
  where the standard permits them. THE PROCESS HALF IS CONDITIONAL ON THE USB DECISION
  ABOVE, because as written it contradicted it: a class driver that stays inside the
  xHCI Domain proves the CONTROLLER's bind and its own per-class provider cleanup, and a
  split one proves a per-interface claim and bind. And what must not survive a driver's
  termination is a USABLE authority or provider - not every charged resource. A
  CONFIRMED teardown returns them; an unconfirmed one leaves them charged and
  quarantined, which is this tree's deliberate answer to frames a dead device may still
  be writing to, and a gate demanding otherwise would fail the safe behaviour.
- [ ] Integration and performance gates: fresh x86_64/aarch64/riscv64 userspace builds
  remain green; each architecture runs every controller QEMU can expose there, with
  explicit documented skips only for unavailable models. Storage drivers format/mount,
  read/write/flush and reopen through StorageService; network classes exchange frames
  through NetworkService; input classes reach InputService; audio drivers sustain
  bounded playback/capture through AudioService; UVC reaches a camera client. Record
  queue/transfer limits, throughput, latency, peak memory and recovery times.
  AND FOR A CONTROLLER THAT IS SUPPOSED TO HOST THE SYSTEM VOLUME, THAT GATE IS NOT ENOUGH
  (added 2026-08-30). Group 2 is justified as the hardware a real machine needs before anything can
  be installed on it, and this gate proves only a SECONDARY disk: format, mount, read/write, flush
  and reopen through StorageService all pass while the machine still boots from virtio-blk. The
  staging problem that hides is real - the sole bootstrap block driver is pinned and boot-critical,
  DeviceManager cannot load volume-stage drivers until the bootstrap driver has already mounted the
  volume, and the manifest refuses a boot-critical driver stored on the volume it must make
  accessible. So every controller eligible to host `vol://system` - NVMe and AHCI today -
  additionally states its pinned/boot-critical staging, its deterministic boot-volume selection and
  its fallback behaviour, and closes on an END-TO-END BOOT whose system volume is on THAT controller.
  The secondary-disk test stays as coverage; it is not the integration proof.
- **THIS MILESTONE HAS NO COMPLETION CONDITION, BY DECISION** (2026-08-30). The head already says
  it is a cross-phase roadmap and not one piece of work, and a "Done when: every item is done" line
  underneath contradicted that in the only place a reader looks for the answer - and it applied one
  driver-shaped gate to parsers, kernel infrastructure and architectural prerequisites alike. It is
  therefore a NON-COMPLETABLE INDEX: it is never ticked, it never appears in a phase's completion
  report, and what closes is an ITEM. What follows is the per-item standard, not a milestone gate.
- An ITEM is done when it has the bounded contract and implementation scope
  its bullet describes; each binds by public standards rather than product whitelists; each survives
  hostile descriptors, hostile completions and REMOVAL AS ITS GROUP CAN SEE IT without escaping its
  Domain - a driver exiting, a channel dropping, a child disappearing from a bus that has one, which
  is what exists today; DEVICE removal is the PCIe-hotplug item's to build and only items after it
  can be asked for it; each releases its
  resources ON A CONFIRMED TEARDOWN and leaves them CHARGED AND QUARANTINED where the teardown was
  not confirmed, with no provider of that binding usable either way; and each passes the available
  QEMU/fixture plus tri-architecture build gates.
  A group is finished when every item in it is, but an unfinished later group never holds back an
  earlier one that is - and no group being finished closes this file.
  AND THE STANDARD ABOVE IS THE DRIVER ONE. A SHARED LIBRARY item closes on host tests and hostile
  fixtures with no bind and no device; a KERNEL/BUS INFRASTRUCTURE item closes on the mechanism
  existing with its own negative tests; a DESTINATION SERVICE item closes on the contract being
  versioned and a second instance arriving through it; an ARCHITECTURAL PREREQUISITE closes on the
  decision being recorded and the mechanism existing. Holding any of those against a bind gate is
  what made four AML items and half of HID-over-I2C uncloseable.
  ITEMS WITH NO ATTAINABLE PATH ARE NOT IN THE SET. The four AML items and the binding half of
  HID-over-I2C are blocked on prerequisites that have no milestone; they are listed as candidates and
  are not counted anywhere. Device-specific hardware support remains explicitly in Phase 5, and
  development-only virtio-fs never becomes an ambient or production storage path.
- Concept: P02M0098 (the completed binder/lifecycle foundation every task must use), the
  Phase-2 virtio-plus-universal-driver policy in `docs/CONCEPT_CZ.md` and
  `docs/CONCEPT_EN.md`, the existing xHCI USB class stack, and the Phase-5
  device-/board-specific boundary.
## Fourth audit (2026-08-16): `AI/audit/drivers.md`, and `AI/audit/wire.md` WIRE-002

Sixteen driver findings, ten of them High. The first is reported independently by two audits, which
is why it is first: `AI/audit/wire.md` reaches the same code from the `Buffer` contract's side.

THIS DEBT BELONGS TO THE FAMILIES IT TOUCHES, NOT TO THE MILESTONE AS A WHOLE. It is not in the
ordering above and it is not in the Definition of done, and that was right: making sixteen findings
about today's drivers a gate on forty new ones would block every item on defects in code most of them
never touch. Each finding is owned by the family whose driver it is in, and is closed when that
family's next item is - which for the ones below means block storage, USB, virtio sound, virtio GPU
and HID. AND THE FINDINGS ARE RE-VERIFIED BEFORE THEY ARE ACTED ON: they were written on
2026-08-16 and the tree has moved, as the first bullet's correction shows.

- [ ] **A block write maps a transferred object and copies `count * 512` bytes out of it without
  ever asking how big it is (DRV-003 / WIRE-002, both High).** VERIFIED IN BOTH DRIVERS.
  `virtio_blk::serve_write` and `usb_storage::serve_write` each take a caller-supplied `count`,
  compute `bytes = count as u64 * SECTOR as u64`, call `map_object(src_handle)`, and then
  `core::ptr::copy_nonoverlapping(src as *const u8, dst, bytes as usize)`. The bound that IS checked
  - `span.fit(bytes)` and `st.fit_data(bytes)` - bounds the DESTINATION DMA span, not the source
  object. A client that sends `count = 1000` with a 512-byte MemoryObject drives a 512 kB read out
  of a 512-byte mapping: either a fault that kills the isolated driver, or adjacent mapped memory
  written to the disk. `rt::object_info` already reports a memory object's byte size, so the check
  is available today. RE-VERIFIED 2026-08-28 AND NARROWED TWICE: the original finding said object
  type and rights were unchecked on the same path. `sys_memory_map` goes through
  `current_typed::<MemoryObject>(handle, ObjectType::MemoryObject, Rights::MAP)`, so the TYPE and the
  `MAP` right are checked - which is not the same as "rights are checked", and the distinction is the
  finding: `READ` is not asked for on an object the driver reads OUT of. What is missing is that and
  the SIZE, and the size is the whole of the copy defect. The 512 kB example is virtio-blk's exactly;
  USB storage clamps its count, so its over-read is smaller and still an over-read.
- [ ] **Virtqueue size, used IDs, used lengths and used-index movement are not validated centrally
  (DRV-001, High).** A device - or anything that can act as one - controls those fields.
- [ ] **Block drivers silently clamp counts and never validate LBA ranges, and USB truncates the LBA
  to 32 bits (DRV-002, High).** A silent clamp turns a wrong request into a wrong write.
- [ ] **USB flush converts every repeated failure into success without reading sense data
  (DRV-005, High).** A durability barrier that reports success on failure is worse than none.
- [ ] **USB Bulk-Only Transport ignores actual transfer lengths and CSW residue (DRV-006, High).**
  A short transfer is then read as a complete one.
- [ ] **xHCI loses DMA handles and slot ownership across hotplug and partial failure, and its
  synchronous waits consume and discard port-status events (DRV-007, -008, High).**
- [ ] **Virtio sound treats any interrupt as TX completion, ignores status, reuses stale PCM and
  always replies success (DRV-009, High).**
- [ ] **Virtio GPU trusts unbounded dimensions and uses unchecked framebuffer and rectangle
  arithmetic (DRV-010, High).**
- [ ] **USB configuration walkers read fixed fields outside descriptor records and accept short
  descriptor transfers (DRV-012, High).**
- [ ] **HID arithmetic can overflow before or during report decoding; a short report leaves the
  previous-report tail stale; left and right modifiers share one boolean and release each other
  (DRV-011, -013, -014).**
- [ ] **Unexpected transferred handles are ignored and leaked across several service protocols
  (DRV-004).**
- [ ] **Roughly 5,700 lines of unsafe device logic have no direct tests (DRV-016).** This is the one
  that makes the others expensive to fix safely, so it is worth doing first for whichever driver is
  touched first.
- [ ] **Without an IOMMU a compromised driver or device can DMA outside its process-owned buffers
  (DRV-015, design).** Same boundary as KERN-CORE-020; state it as a threat-model limit if it is not
  being closed.

AND SIX OF THESE HAVE NO FAMILY ITEM TO RIDE ON, so each is given a closure trigger here rather than
a milestone of its own. DRV-009 (virtio sound) and DRV-010 (virtio GPU) were given one in an earlier
round that named items THIS ROADMAP DOES NOT HAVE: there is no `virtio-sound` checkbox and no
`virtio-gpu` checkbox, only references to those drivers from inside other bullets. They are defects
in drivers this system ALREADY SHIPS, and a roadmap of drivers to ADD has nowhere to put them. So
they close where those drivers are next changed, AND THE FIX IS PART OF THAT ITEM'S GATE RATHER THAN
ADJACENT TO IT - which is the difference between a trigger and a hope. Writing HDA does not oblige
anybody to fix `virtio_snd`'s interrupt handling, and writing an EDID parser does not oblige anybody
to bound `virtio_gpu`'s rectangle arithmetic.

**AND THEY NOW HAVE ITEMS OF THEIR OWN, WHICH IS BETTER THAN A TRIGGER** (2026-08-30). Section 0
above adds bounded maintenance items for the seven drivers that already ship, and THE MAPPING IS
COMPLETE RATHER THAN EXEMPLARY (corrected 2026-08-30 - a previous round named four IDs and left six
High findings with no actionable gate, so the next change to those families could close in direct
contradiction to the rule this section states):

    DRV-001 (virtqueue validation)          `virtio-blk maintenance`, plus the shared-validation
                                              owner rule below
    DRV-002 (silent count clamp, no LBA     `virtio-blk maintenance` AND `xHCI maintenance`. The
      range check; USB truncates LBA to       finding is TWO defects in one ID: both block drivers
      32 bits)                                clamp and skip the range check, and USB additionally
                                              truncates the LBA. Each item closes its own half
                                              (corrected 2026-08-31 - this row named only virtio-blk
                                              and would have left the USB half with no gate)
    DRV-003 / WIRE-002 (type/rights/size)   `virtio-blk maintenance` AND `xHCI maintenance`, both
                                              on the complete three-part contract
    DRV-005 (flush reports false success)   `xHCI maintenance` (corrected 2026-08-31 - this row said
                                              `virtio-blk maintenance` and the defect is in USB: the
                                              debt entry is "USB flush converts every repeated
                                              failure into success without reading sense data", and
                                              virtio-blk has no sense data to read. Assigning it to
                                              the block item would have left it open on the driver
                                              that has it)
    DRV-006 (BOT length and CSW residue)    `xHCI maintenance`
    DRV-007, DRV-008 (port-status events)   `xHCI maintenance`
    DRV-009 (virtio sound)                  `virtio-snd maintenance`
    DRV-010 (virtio GPU, display half)      `virtio-gpu maintenance`
    DRV-012 (descriptor transfers)          `xHCI maintenance`

Each of those items carries its IDs and their negative tests in its own gate -
named in the item, not only here. A previous round put these triggers in this section and in no
actionable bullet, which meant an item plan could follow its own bullet and close without the fix,
which is precisely the failure this section warns about. DRV-003/WIRE-002 is likewise in the
`virtio-blk` and `xHCI` maintenance gates. Where a LATER family item touches the same code - HDA or
USB Audio for the sound path, EDID/DDC for the display path - it carries the same ID as a regression
gate over ITS OWN driver, which is a different instance of the defect and not the same one twice.

AND DRV-001 NOW HAS ONE. Central virtqueue validation - size, used ids, used lengths, index movement
- lives in code every virtio driver shares, so no single driver family's item contains it. It was
assigned to "the FIRST VIRTIO ITEM in group 1 to be written, whichever that turns out to be", which
is a race rather than an owner. It is owned by **`virtio-blk maintenance`** in section 0: that item
is already in the shared virtqueue path with a gate attached, it is the first Phase-2 maintenance
item with a DMA policy to migrate, and it exists today rather than being whichever future item
arrives first. Its gate carries DRV-001's hostile-descriptor fixtures.

The four that are cross-cutting close on a stated event instead: DRV-004, unexpected
transferred handles leaked across service protocols, closes with the first item that changes a
service protocol's handle handling; DRV-015 is a THREAT-MODEL LIMIT and closes by being written into
the concept's threat model, not by a driver; DRV-016, the untested unsafe device logic, closes per
family as each family's item lands, which is what its own text already says - "worth doing first for
whichever driver is touched first" - and is recorded here so it is not read as one debt anybody could
pay in one go.

## Fifth audit (2026-08-28)

An external review of this milestone, checked against the tree before anything was changed. Seven of
its eight findings were real and are folded into the sections above rather than listed here; what is
worth keeping is which, and what was declined.

Accepted: this is a roadmap and said so nowhere; "not thirty-one items" had been forty-one for some
time; the binder reaches virtio and xHCI and nothing else, while groups 2 and 3 assume three things
P02M0163 refused by name; the USB group asks every class driver for a registry entry, a claim and a
bind while HID and storage are modules inside one process holding one `Option<(UsbDevice, Storage)>`;
PCIe hotplug needs a lifecycle that deliberately does not exist; three shared contracts have no
owner; the group order was written as a gate when it is a priority; and the Definition of done said
every driver releases all resources on crash, which contradicts the quarantine this tree built - an
unconfirmed teardown stays CHARGED, and saying otherwise would have made the safe behaviour a
milestone failure.

It also caught a stale status line outside this file: [P02M0162](P02M0162.md) said IN PROGRESS with
every item done and TODO.md ticked, and this milestone names it in its hard gate. Correcting the
status line alone was not enough and the sixth audit said so - see below.

Declined, and both for the same reason: splitting this file into per-item milestones, and going
through all forty-one bullets to give each a `requires`/`owns`/gate triple now. The rule is written
once in "Order, gates and completion" and an item states its three lines when somebody writes it -
filling in forty-one sets of them today would be inventing prerequisites for drivers nobody has
started, which is how a roadmap acquires answers before it has the questions.

One of the review's own claims was wrong and is corrected in place rather than repeated: object type
and rights are NOT unchecked on the mapping path - `sys_memory_map` refuses a handle of the wrong
type or without `MAP`. The size check and `READ` are what is missing, and the size is the whole of
the copy defect.

## Sixth audit (2026-08-28)

The same reviewer over the file the fifth audit produced. Every finding held; nothing was declined,
because none of them proposed adding a mechanism - each named a contradiction the fifth round's own
edits left behind or a claim this file makes that the tree does not support.

- **The hard gate was reported closed on a status line and left open underneath.** Turning
  P02M0162's status over did not touch the paragraph beneath it, which named three items as not
  started while all seven were ticked. That is corrected in that file, together with the thing this
  audit was right to hold against it: one value in it is still NAMED-OPEN - the three boot-window
  constants are round counts being read as ticks, and the milestone deliberately hands the
  measurement to whoever changes the contract. A milestone here that depends on the boot window
  inherits that, and the correction says so rather than letting "COMPLETE" imply otherwise.
- **The USB prerequisite contradicted the conformance gate two hundred lines later.** The
  prerequisite allows class drivers to stay inside the xHCI Domain; the gate demanded a governed
  process and a DeviceManager binding per family unconditionally. The gate is now conditional on
  which answer the prerequisite gives, which is what a decision item is for.
- **The gate also still contradicted the quarantine** that the Definition of done had just been
  corrected to state. What must not survive a termination is a usable authority or provider, not
  every charged resource.
- **Three ownerships were wrong.** P02M0161 owns start and report and says in its own goal that
  `STOP`/`STOPPED` is P02M0165's; the DMA/IOVA contract and the `iommu-required` policy MOVED from
  P02M0098 to P02M0153 and this file cited the old owner twice; and P02M0098 is not "the binder
  foundation" on its own - the gate is the three milestones together.
- **`Removed` was terminal and repluggable in the same sentence.** The slot outlives the device as a
  tombstone, `Removed` is terminal for that binding, a new arrival opens a new one, and an
  unconfirmed teardown ends in `Quarantined` instead - which is the state that says something is
  still owed.
- **A published provider cannot be withdrawn today.** `subscribe_open` exists in the generated
  protocol and DeviceManager never calls it, so xHCI publishes a block provider whether or not a
  stick is present and leaves it standing on detach. The first item that attaches a child at runtime
  owns that, named.
- **`virtio-scsi` said "reuse the common SCSI layer" for a layer this file admits does not exist.**
  It owns extracting one.
- **The count was 41 and there are 42 checkboxes,** because the fifth round added one that is not a
  driver. Named as what it is.
- **HID over I2C does not sit on the xHCI stack** its group's own introduction says everything sits
  on.
- **DRV-003's correction was itself imprecise.** "Type and rights are checked" is not what the code
  does: the type and `MAP` are checked, `READ` is not asked for, and that distinction is half the
  remaining finding. Four other findings had no family item to close them and now have a stated
  trigger each.

## Seventh audit (2026-08-28)

The reviewer over the file the sixth audit produced, together with P02M0167. Six findings, all held,
and one of them is a correction of a sentence the sixth round itself added.

- **"A published provider cannot be withdrawn" was wrong.** `Withdraw` is opcode 6 and DeviceManager
  handles it. Two narrower things are missing and they are the ones a driver actually hits: the
  catalogue's live stream carries additions and no withdrawal event, and no driver sends the message.
- **The head forbids a source `match` arm and resources are handed out by one.** A literal list of
  driver names decides which drivers get an MSI-X vector, so the rule this file states is broken by
  the mechanism every new driver must use. The first item needing a shape that list cannot express
  owns a declarative resource profile.
- **The closing boundary was never decided.** A roadmap of forty-one drivers across product phases,
  sitting as an open Phase-2 item, with no statement of which. It is a cross-phase umbrella; Phase 2's
  own definition of done does not name any of these drivers.
- **The hard gate is complete with two verifications outstanding.** P02M0161 says only x86_64 has
  booted the new handshake; P02M0162 hands on three unmeasured bind windows. Neither blocks writing a
  driver and both block accepting one on three targets - which is exactly what the integration gate
  asks for, so the first driver inherits them rather than discovering them.
- **The first checkbox is not work.** `virtio-iommu`'s backend and first slice are P02M0153's; what
  is left here is "the remaining drivers and architectures", which is not a list.
- **Every item was asked to survive removal, and removal arrives with an item most of them precede.**
  Qualified to the removal each group can see, with device removal left to the item that builds it.

## Eighth audit (2026-08-28)

Four findings, all about ownership, and the first is a correction to the seventh round's own fix.

- **The declarative resource profile's trigger was narrower than the rule it serves.** It was owned
  by "the first item needing a shape the name list cannot express", which leaves a driver with an
  ordinary MSI-X need to be handled by appending one more name - the exact edit the head forbids. The
  trigger is the first new driver that would otherwise require a change to the list.
- **PL011, not HID over I2C, is the first firmware-bound driver.** It binds through ACPI/FDT
  identity and it stands eighty lines earlier, so the firmware-node identity is its to own.
- **16550 sits behind a "canonical serial contract" that USB CDC-ACM defines**, a hundred and twenty
  lines later and in a later group. Whichever lands first owns it.
- **virtio-balloon and virtio-mem wait on ResourceManager contracts nobody owns**, which is a
  precondition with nothing at the end of it. The item owns them or it does not start.

## Ninth audit (2026-08-28)

Four findings; two of them correct rounds seven and eight of this same file, which is the argument
for a reviewer reading the fixes rather than the original.

- **Two closure triggers named checkboxes this roadmap does not have.** There is no `virtio-sound`
  item and no `virtio-gpu` item - only references to those drivers from inside other bullets - so
  DRV-009 and DRV-010 had no trigger after all. They are defects in drivers this system already
  ships, and a roadmap of drivers to ADD has nowhere to put them; they now close where those drivers
  are next touched.
- **The serial contract has three claimants, not two.** `virtio-serial multiport` is in group 1,
  ahead of both 16550 and CDC-ACM, and gives its ports "independent capability-scoped byte streams".
  Whoever gets there first also answers whether a virtio port, a UART and a USB adapter are one
  contract or a base with a serial layer on it.
- **`USB hotplug` was named as an owner and is not a checkbox.** The catalogue subscribe and the
  attach/detach messages belong to the first group-3 item written, since every one of them attaches a
  child at runtime.
- **HID over I2C waits on a bus nothing provides.** No I2C driver, no I2C provider kind. The item
  owns a bounded I2C contract and its fake, or names the controller item it waits for.

## Tenth audit (2026-08-28)

Two findings, and the first corrects a fix from the ninth round - the second wrong owner named for
the same unowned work.

- **"The first group-3 item written" cannot own USB hotplug**, for the reason this file states two
  hundred lines earlier: group 3 contains HID over I2C, which is not USB and attaches nothing to a
  USB bus. The owner is the first item that actually attaches a USB child at runtime.
- **A trigger that names a neighbouring item is not a fix.** Writing HDA does not oblige anybody to
  correct `virtio_snd`, and writing an EDID parser does not oblige anybody to bound `virtio_gpu`'s
  arithmetic - so DRV-009 and DRV-010 are named in those items' GATES rather than left beside them.
  And DRV-001 had no owner at all: central virtqueue validation lives in code every virtio driver
  shares, which is why no family's item contains it. It goes to the first virtio item written.

## Eleventh audit (2026-08-28)

One finding worth acting on, and it is the same shape as the one found in P02M0167 the same day: a
contract that named two answers and chose neither.

- **HID over I2C was left to choose between owning an I2C contract and waiting for a controller
  item.** Only one of those answers exists, because this roadmap has no I2C controller item to wait
  for. It owns the contract and its host fake; its bind half is hardware-deferred, since QEMU models
  no I2C HID device either.

The other two open points the review names are open on purpose and stay that way: the first driver
cannot be ACCEPTED on three architectures until P02M0161's handshake has booted on aarch64 and
riscv64 and P02M0162's bind windows have been measured, and group 3 cannot start until its execution
model is decided. Both are stated where somebody will meet them, and neither blocks writing code.

## Twelfth audit (2026-08-28)

Two findings, both of them this file contradicting itself rather than missing something.

- **`NVMe` and `AHCI` were listed as the cheapest true starts while the same file made them wait on a
  contract `virtio-scsi` owned.** Correcting it turned up the real error underneath: the SCSI core
  and the block wire were treated as one thing with one owner, and they are two with different
  consumers - NVMe has its own command set and AHCI speaks ATA, so neither needs SCSI and both need
  the block wire. The block wire goes to the first block driver written, the SCSI core to the first
  SCSI-speaking one, and the cheapest-starts list keeps only items that create no shared contract.
- **The group-3 execution-model prerequisite gated the whole group, including a non-USB item.** HID
  over I2C attaches to no xHCI controller and holds no USB claim, and neither answer the prerequisite
  chooses between is about anything it does.

## Thirteenth audit (2026-08-28)

Four findings, and all four are about the twelfth round's own fix. Two of them are the same mistake
made a second time in the act of correcting it the first time, which is worth stating plainly.

- **The block wire got a list of three owners where it needed a rule.** UAS publishes block, and so
  can SDHCI and NFIT. It is owned by the first implemented slice that publishes a block provider,
  named as a rule so the next block-shaped item is not left out of it too.
- **The extracted SCSI core has three consumers, not two.** It comes OUT of the USB BOT code, so BOT
  moves onto it in the same change - otherwise an extraction has produced a second copy and called it
  sharing.
- **"No shared contract to create on the way" was false for three of the four items it was written
  about.** `16550`, `PL011` and `virtio-serial` share the serial contract that this same file says has
  three claimants and no definition. `virtio-rng` is the only genuine one, and the list now says so
  and says what the real question is: not how big the driver is, but how many later items consume
  what writing it forces you to define.
- **Firmware-node identity was assigned to PL011 by name** while `16550` needs a claimable discovery
  path and is earlier in the list. It goes to the first firmware-bound driver written - and an x86
  legacy-port 16550 turns out to need no firmware node at all, which makes it startable before any of
  it exists.
