AUDITOR'S REVIEW OF PLAN P02M0188 (2026-09-21T14:02:16Z):

**Rating: 3/10.** The requirements identify the intended security properties, but the mechanisms that establish trusted confirmation, exact authorization and enforceable single use are not yet planned.

Reviewed [the plan](/data/yellow/libersystem/docs/todo/P02M0188.md), the originating DFU requirements, capability transfer, display/input services, USB identity contracts, permission and logging services, bootstrap conventions, and guest/verification infrastructure at commit `467f761df5a27b5409ffa3cd54e942485b4cf078`. The document is a requirements checklist; these findings concern implementation decisions needed to make its guarantees achievable, not missing feature code in an open milestone.

1. **High - A separately drawn dialog does not provide the required trusted display and input path.**

   The [confirmation item](/data/yellow/libersystem/docs/todo/P02M0188.md:25) requires that the requester cannot paint over the prompt, but specifies neither a protected presentation mechanism nor the source of an accepted answer. Current [DisplayService surface creation](/data/yellow/libersystem/src/user/services/core/src/display_service.rs:399) automatically makes a newly created non-console surface active. Its [focus proof](/data/yellow/libersystem/src/user/services/core/src/display_service.rs:833) and [InputService validation](/data/yellow/libersystem/src/user/services/core/src/input_service.rs:267) establish current surface ownership, not human approval of a protected operation.

   A confirmation service using ordinary surfaces would remain preemptible by an application creating another surface. Accepting an ordinary requester message or input channel as confirmation would likewise fail the requirement that a human, rather than a process, approved the action. The [current threat model](/data/yellow/libersystem/docs/THREAT_MODEL.md:41) treats applications and drivers as untrusted, so the stronger physical-human claim also needs explicit display/input trust assumptions.

   **Correct the confirmation item and definition of done** to name the privileged confirmation owner, its boot-granted display/input authority, the protected entry and presentation mechanism, and the input events it may accept. Define how competing surfaces and focus changes are handled, how stale/preexisting input is excluded, and how loss of input/display/service cancels authorization. Specify a trusted indication or attention mechanism the requester cannot impersonate. This can extend the existing single-visible-surface design; a general compositor is not a prerequisite unless the plan chooses one.

2. **High - The plan does not define how a grant is authenticated, consumed or enforced at the mutating endpoint.**

   The [interface item](/data/yellow/libersystem/docs/todo/P02M0188.md:21) promises a single-use capability, while the [completion condition](/data/yellow/libersystem/docs/todo/P02M0188.md:42) requires that a high-risk operation cannot proceed without it. There is no issuer/verifier/executor contract, capability representation, expiry clock or consumption transition. Kernel [handle transfer](/data/yellow/libersystem/src/kernel/object/handle/mod.rs:679) moves an object reference once; it does not impose a one-operation lifetime on the referenced channel or bind an action and target. Nonduplicable rights alone do not supply those semantics.

   Without a concrete enforcement point, implementation could return a boolean or token that the requester treats as permission while the actual mutator remains callable independently. It could also validate a grant twice before consuming it, or retain executable authority after expiry or service restart. The current [PermissionManager runtime policy](/data/yellow/libersystem/src/user/services/core/src/permission_manager.rs:386) is noninteractive denial, not an existing administrative grant mechanism.

   **Correct the interface and host-state-machine items** to define request and redemption operations, the authoritative grant record, restricted handle rights, trusted issuer and executor wiring, and an atomic unused-to-consumed transition tied to the authorized effect. Define expiry, concurrent redemption, cancellation, disconnection and restart behavior, including what a lost execution reply means for retry. Identify the IDL, [service manifest/bootstrap](/data/yellow/libersystem/src/user/services/manifest.toml:1) and permission-grant changes that make the endpoint reachable with the intended authority. The executor must reject missing, forged, expired, reused or differently scoped grants. Keep the generic verifier boundary here and the actual DFU implementation in P02M0099.

3. **High - Readable action/target labels do not uniquely bind the device or payload being authorized.**

   The [request and identity items](/data/yellow/libersystem/docs/todo/P02M0188.md:21) require readable names and validation against the target's own report, but do not distinguish presentation text from authoritative identity or bind the action's parameters. The existing [USB inventory contract](/data/yellow/libersystem/src/idl/device.lsidl:386) carries root port, speed, VID/PID, class and role only. Its documentation explicitly states that devices behind a hub share the root port. Those fields cannot uniquely distinguish identical devices or a replacement plugged in after confirmation.

   Similarly, approval of "firmware download" to a device does not prevent substituting another image after the user responds. The originating [DFU requirement](/data/yellow/libersystem/docs/todo/P02M0099.md:5741) includes target and available signed-image metadata validation, so this is part of the concrete operation the administrative path must support.

   **Correct the interface and identity-validation items** to define a canonical operation descriptor, separate display labels, and a live target identity obtained through the authoritative executor/backend capability. Bind the grant to the target instance/generation and to immutable operation parameters or payload identity. Define revalidation immediately before the effect and invalidation on disconnect/replacement; device-reported metadata should be checked through that bound backend, not trusted from requester text. Assign USB-specific identity and image checks to the DFU adapter while specifying the generic validation seam here. A confirmation against one device must not become authority over another device or another payload.

4. **Medium - "Every grant and decline recorded" lacks a storage and failure contract.**

   The [recording item](/data/yellow/libersystem/docs/todo/P02M0188.md:28) covers grants; the [definition of done](/data/yellow/libersystem/docs/todo/P02M0188.md:42) additionally requires every decline. Neither defines storage ownership, recording order relative to issuing authority, persistence expectations, timestamp source or behavior when recording fails. Existing [permission audit entries](/data/yellow/libersystem/src/idl/security.lsidl:105) contain only component, capability, granted and dynamic, and are kept in an [in-memory vector](/data/yellow/libersystem/src/user/services/core/src/permission_manager.rs:730). Existing [LogService persistence](/data/yellow/libersystem/src/user/services/core/src/log_service.rs:148) ignores write errors; [`emit`](/data/yellow/libersystem/src/user/services/core/src/log_service.rs:251) returns success after accepting a record regardless of durable storage success.

   Reusing either mechanism unchanged would not establish an attributable record of the action, target, time and answer, and could allow an irreversible action whose decision record disappears on failure. The unspecified error handling could also disclose "no path available" through a distinct error despite the normal-decline requirement.

   **Correct the recording and decline items** to choose a concrete recording backend and record schema, including request/decision correlation, canonical action/target and trusted time provenance. Specify recording/issuance ordering, bounded retention, required persistence and what happens when a record cannot be stored. Include all declines and unavailable-path outcomes, preserving useful internal reasons while exposing the same normal decline to the requester. Extend an existing subsystem narrowly; this does not require a new general logging framework.

5. **Medium - The guest gate is not defined to exercise the trusted path and actual authorization enforcement.**

   The [test items](/data/yellow/libersystem/docs/todo/P02M0188.md:35) name host expiry/reuse/target tests and an unspecified guest effect. DFU itself remains [waiting on this milestone](/data/yellow/libersystem/docs/todo/P02M0099.md:5741), so the plan needs to choose an executor fixture or an explicit staged integration. It also needs to distinguish exercising the actual input boundary from injecting an approval directly. The existing [input-events scenario](/data/yellow/libersystem/src/harness/scenarios/input-events.toml:4) explains that ordinary control-protocol input goes directly to ConsoleService and proves nothing about InputService; [emulated keyboard input](/data/yellow/libersystem/src/harness/lab.py:3371) is already available for the real path.

   A gate that calls an acceptance hook and checks for a grant could pass while the prompt remains replaceable or the executor accepts unauthenticated requests. Pure state-machine tests cannot establish the missing runtime authority boundaries.

   **Correct the guest-gate item** to use the production request, protected presentation/input and redemption paths, with a bounded executor probe that records its actual effect. Require exactly one matching effect after confirmation and zero effects after decline, unavailable path, expiry, replay, forged authority or target/payload substitution. Exercise requester attempts to replace the prompt or manufacture approval. Put the production state machine in a host-testable leaf, following the [existing service-logic boundary](/data/yellow/libersystem/src/user/services/logic/Cargo.toml:8). Select the gate/test mechanism and specify relevant component coverage, guest/image prerequisites and evidence in the [verification catalogue](/data/yellow/libersystem/src/tools/verify-model/src/catalog.rs:892); a standalone gate must also satisfy [gate-list and release-required consistency](/data/yellow/libersystem/src/tools/verify-model/src/main.rs:1591). A faithful executor probe can prove the generic path without pulling the USB DFU driver into this milestone.

Validation was source and plan inspection; no build or guest execution was needed for these findings. The plan and source code were not modified.


PLANNER'S RESPONSE ON P02M0188 (2026-09-21T14:23:43Z):

Verified all five findings against the current plan, P02M0099's DFU requirement, the actual display/input and permission/bootstrap paths, handle transfer, storage/journal implementations and verification catalogue. All five identify missing decisions within this milestone. The corrected plan replaces the outline; all implementation items remain open. No source code was changed.

1. **ACCEPTED - Protected display and input.** `DisplayService::create` currently activates the newest ordinary surface, and `input_focus`/`InputService::validate_focus` prove focus ownership, not protected human approval. The plan now specifies a boot-granted exclusive display session, source-specific trusted keyboard input, Ctrl+Alt+F12 secure attention, presentation/input epoch acknowledgment, release-before-arm filtering, and cancellation/restoration on loss. It explicitly includes the chosen input/display drivers in this property's trust assumptions, rather than claiming resistance to their compromise. Ordinary merged keys, Bluetooth and console injection cannot approve. A general compositor and secret-entry service are outside this implementation.

2. **ACCEPTED - Authenticated, single-use enforcement.** `HandleTable::take` moves a handle; `dynamic_policy` denies undeclared permissions; neither implements operation consumption. The plan now defines request and prepared-operation contracts, broker-owned per-grant channels, restricted rights, monotonic expiry, serialized durable consumption before private executor dispatch, and executor-side generation/one-start enforcement. The actual mutator has no ordinary client entry point. It specifies cancellation, owner loss, service epochs, no restoration of grants, and unknown outcome/no automatic retry after a lost reply. IDL, manifest, PermissionManager and all bootstrap callers are named. Real DFU execution remains in P02M0099.

3. **ACCEPTED - Exact target and payload binding.** The `usb-device` record in `src/idl/device.lsidl` is inventory, and its root port explicitly cannot distinguish downstream devices. The plan now separates labels from canonical action/executor/instance/generation/parameter identity and payload length/digest; preparations must copy or pin immutable payload data. The authoritative backend resolves identity and serializes revalidation with the start of the effect. Replacement and reset invalidate preparation. P02M0099 owns USB-specific attachment identity, target compatibility and available signed-image checks; the broker owns their generic contract.

4. **ACCEPTED - Recording and failure semantics.** `LogService::flush` discards write errors and `emit` returns success independently; PermissionManager's audit is in memory. `writer.flush` in `src/user/services/storage/src/service.rs` only confirms staged length. The plan instead uses a bootstrap-scoped AdminService journal on disk-backed LiberFS with acknowledged transactional commits, a concrete record schema and bounded retention. Requests/approval commit before grants, consumption before effects. Every decline is recorded, including internal unavailable reasons, with a bounded emergency ring when storage fails and explicit limits on outage durability. Storage failure never grants authority and preserves the normal external `Declined` result. No general logging framework or claim of tamper resistance against a full storage administrator is introduced.

5. **ACCEPTED - Runnable guest proof.** `input-events.toml` and `lab.py::send_keys` confirm that console injection bypasses the input path while QEMU keyboard events exercise it. The plan now specifies a cold development guest gate using the production protected path and a test-only bounded executor with a measured write count/digest. It checks exactly one authorized effect, zero effects for the listed negative cases, actual presentation, hostile focus/surface attempts, held keys, failure and recovery. The pure production state machine belongs in `service-logic`; the gate's coverage, image/disk prerequisites, evidence and release-required registration are explicit. The fixture proves this authorization milestone without claiming DFU transport coverage.

Re-check: the dependency order has no PermissionManager/AdminService bootstrap cycle; approval, consumption and hardware outcome are distinct; target/payload scope cannot change after confirmation; unsupported paths fail without authority; audit retention/failure promises match their implementation plan; and the host/guest oracles exercise the claimed boundaries. The plan is ready for implementation within its stated trusted-path assumptions and fixture-backed generic scope. Real DFU integration remains separately owned and incomplete.

Final consistency refinements within the same response: the plan now makes the initial operator request policy explicit, bounds every local preparation/control/journal exchange to five seconds, and tests silent peers. Grant transfer delegates the exact operation while retaining the live original request context and attribution; it does not imply sender-PID authentication. Journal records are capped at 8192 encoded bytes and segments at 8 MB including framing, below StorageService's current 64 MB writer cap; both count and byte limits apply. These refine accepted findings 2 and 4 without changing their disposition.


AUDITOR'S RE-AUDIT OF PLAN P02M0188 (2026-09-21T15:43:17Z):

**Rating: 9/10.**

Read the complete audit history and independently checked the planner's responses against the current plan, P02M0099's ownership, and the actual permission, capability-transfer, process-observation, display/input, storage and verification paths. One part of accepted finding 2 remains incomplete:

1. **Medium - Request-channel lifetime does not enforce the promised cancellation on requester death.**

   The plan requires cancellation on [loss of the requester](/data/yellow/libersystem/docs/todo/P02M0188.md:81) and explicitly includes [requester-death verification](/data/yellow/libersystem/docs/todo/P02M0188.md:178). However, its concrete lifetime mechanism binds authority to the [request connection](/data/yellow/libersystem/docs/todo/P02M0188.md:92) and requires only that this connection [remain live after grant delegation](/data/yellow/libersystem/docs/todo/P02M0188.md:101). It supplies no observation of the original launching task. After confirmation, the requester can transfer both its request endpoint and unused grant to another process and exit; both connections remain live, allowing redemption within the grant deadline despite the promised requester-death cancellation. Removing `DUPLICATE` does not prevent this transfer case.

   This distinction is already explicit in [PermissionManager's task-bound grant path](/data/yellow/libersystem/src/user/services/core/src/permission_manager.rs:543) and [DisplayService's process observation](/data/yellow/libersystem/src/user/services/core/src/display_service.rs:1756): an open channel proves that somebody retains its peer, not that the launching process still exists. The planner's final clarification correctly permits delegation without authenticating the delegate's PID, but does not resolve observation of the original owner's death.

   **Complete the lifetime correction** by binding the private request factory to a `WAIT | TRANSFER` observer of the exact prepared launching task through the existing `grant_for_task` integration. Observe termination independently of endpoint closure and invalidate pending requests and unconsumed grants, including while a journal acknowledgment is pending. This needs no task-management authority and can preserve redemption by a delegate while the original owner remains alive. Extend the guest death case to retain both endpoints in another process, terminate the original owner, and assert zero executor dispatches; retain a successful live-owner delegation case as well.

Validation: plan and source inspection only; no builds or guest execution. The rating concerns implementation-plan readiness. No plan or source code was modified.
