AUDITOR'S REVIEW OF PLAN P02M0189 (2026-09-25T22:49:59Z):

**Rating: 3/10.** The plan's rule of measuring before fixing is sound. But its premise misses two large terms that the code already explains, several measurement conditions it requires do not exist, and the instrument and gate ignore the build profiles and facilities already in the tree.

Reviewed [the plan](/data/yellow/libersystem/docs/todo/P02M0189.md) and [the baseline it opens on](/data/yellow/libersystem/docs/PERF.md:7) at commit `07371c44af82a11c1d275b0cbd832899f712e62a`. The review covered the live 2D demo's loop and the frame-loop pacing it runs under, DisplayService's present path and its counters, the display and display-device IDL, the virtio-gpu driver and how it waits for virtqueue completion, the kernel's tick, wait and scheduler paths, the runtime clocks and the existing perf-marker facility, the build and staging profiles, gate registration, and the neighbouring P02M0103 and P02M0193 plans. This is a measurement plan. The findings concern decisions the measurement depends on, not the expected absence of the instrument.

1. **High - Every measured interval contains a pacing sleep of at least 16 ms, rounded up to the 10 ms scheduler tick, and the plan presents that time as unexplained present-path cost without identifying or classifying it.**

   The plan opens on the 52.8 ms by which the demo's present interval exceeds its draw. It describes that time as spent ["between the renderer handing over a finished surface and the same loop being free to draw the next one"](/data/yellow/libersystem/docs/todo/P02M0189.md:8). DisplayService completes every present [with no timing at all](/data/yellow/libersystem/src/user/services/core/src/display_service.rs:818). So when a present returns, `Pacing::on_present` schedules the next frame at [now plus `UNPACED_INTERVAL_NS`](/data/yellow/libersystem/src/user/libs/graphics/app/src/pacing.rs:168), which is [16 ms](/data/yellow/libersystem/src/user/libs/graphics/app/src/pacing.rs:92). The time is stamped [after the call returns](/data/yellow/libersystem/src/user/libs/graphics/app/src/frame_loop.rs:140).

   The next step answers [`Idle` until then](/data/yellow/libersystem/src/user/libs/graphics/app/src/pacing.rs:234) and the demo [parks](/data/yellow/libersystem/src/user/apps/tools/src/test2d_sw.rs:710). `park` converts the deadline to an absolute tick and [rounds it up](/data/yellow/libersystem/src/user/libs/graphics/app/src/frame_loop.rs:193), because [every ABI deadline is on the 100 Hz tick counter](/data/yellow/libersystem/src/abi/src/lib.rs:221). A wait expires only when [`ticks() >= deadline`](/data/yellow/libersystem/src/kernel/syscall/mod.rs:3697).

   By construction, then, each frame of [the demo's interval](/data/yellow/libersystem/src/user/apps/tools/src/test2d_sw.rs:649) contains an idle of at least 16 ms, and up to about 26 ms once rounded. That is a fallback constant plus tick quantisation, not transport, service or driver work. The plan's last term, "what the application then waits for", would capture how long the idle lasts, but nothing in the plan identifies what it is.

   The instrument item asks only for [the resolution of the timestamp clock](/data/yellow/libersystem/docs/todo/P02M0189.md:63). The primitives measure only [a channel wake](/data/yellow/libersystem/docs/todo/P02M0189.md:94). [The four-way classification](/data/yellow/libersystem/docs/todo/P02M0189.md:124) has no class for a deliberate policy wait. The idle will therefore be reported as "a scheduling consequence" or folded into ["what this system charges for being a system"](/data/yellow/libersystem/docs/todo/P02M0189.md:110). That puts a frame-loop fallback at the centre of the milestone's headline number, and it aims a new cross-process instrument at a term the source code already states.

   **Correct the opening, present-path, instrument and verdict items.** Name the frame loop's pacing wait as its own term: the fallback interval applied because the backend reports no timing, together with its tick rounding. For every wait, record whether it ended on a message or on a deadline, with the deadline requested and the time the thread actually ran. Measure a timer-deadline wake beside the channel wake. Give the classification a class for policy waits and tick quantisation, or state that such time is reported but excluded from the system's charge. Measuring the wait is enough; changing the pacing stays out of scope.

2. **High - The plan models the display half as moving a 640x480 frame to the device, but DisplayService scales every surface to the fixed scanout and the driver's call includes the device's own completion, so the copy count and the two-size item will misattribute the display terms.**

   [The copy item](/data/yellow/libersystem/docs/todo/P02M0189.md:74) counts "a 640x480 frame's pixels", and [the page item](/data/yellow/libersystem/docs/todo/P02M0189.md:97) counts 300 pages. [The size item](/data/yellow/libersystem/docs/todo/P02M0189.md:85) treats a term that does not move with the frame's area as "not paying for the picture".

   `pix::blit` copies rows only when [the surface and scanout are the same size](/data/yellow/libersystem/src/user/libs/image/pix/src/lib.rs:445). Otherwise it [fits the surface to the whole scanout, pixel by pixel](/data/yellow/libersystem/src/user/libs/image/pix/src/lib.rs:459), and DisplayService always [blits into the full scanout](/data/yellow/libersystem/src/user/services/core/src/display_service.rs:1178). The live harness boots [`virtio-vga` with no resolution set](/data/yellow/libersystem/src/harness/qemu-run.sh:2240), and QEMU's defaults are `xres=1280`, `yres=800` (from `qemu-system-x86_64 -device virtio-vga,help`).

   At [the baseline's `--size=640x480`](/data/yellow/libersystem/docs/PERF.md:9), each whole-surface present therefore writes a 1066x800 output, about 2.8 times the surface's pixels, and the driver transfers that rectangle. Both are set by the scanout, not by the frame. The tree already reads [a present that stays flat across three surface sizes](/data/yellow/libersystem/docs/PERF.md:316) as the display's own pacing.

   The driver also answers only after the device has [acknowledged both commands](/data/yellow/libersystem/src/idl/display-device.lsidl:85), [the transfer and the flush](/data/yellow/libersystem/src/user/drivers/core/src/virtio_gpu.rs:259). It waits for them by [spinning on the used ring and yielding every 4096 polls](/data/yellow/libersystem/src/user/drivers/core/src/virtio.rs:577). That device time is work done by QEMU on the host, and its cost depends on how the guest was started: the [display back end](/data/yellow/libersystem/src/harness/qemu-run.sh:109) and the [vCPU count, which defaults to the host's](/data/yellow/libersystem/src/harness/qemu-run.sh:23). It also comes after "the device being told", where [the term list stops](/data/yellow/libersystem/docs/todo/P02M0189.md:72), so it would be absorbed into "what the application then waits for".

   The consequences are threefold. Measured at two surface sizes on a fixed scanout, the blit and the transfer will be flat, and will be reported as exactly the non-picture cost the milestone is looking for. The copy count will describe a copy that does not happen in that form. The driver term will be neither reproducible nor classifiable as work or wait.

   **Correct the copy-count, two-size and present-path items.** Record the scanout size, the direct or scaled path, and the source and output pixels of every run; DisplayService [already counts all of these](/data/yellow/libersystem/src/idl/display.lsidl:265). Vary the scanout and the surface together, either through the device's `xres`/`yres` or by running the surface at the screen's own size, so that the direct path is what gets measured. Name the device's acknowledgement (from told to acknowledged, as the driver sees it) as a term. Record QEMU's display device, display back end and vCPU count as measurement conditions. State how a spin-wait on the device is split between work and wait.

3. **High - The plan never states the build profile of any layer, and the kernel, DisplayService and the GPU driver are unoptimised dev builds while the application is a release build.**

   `build.sh` builds [the services](/data/yellow/libersystem/build.sh:105), [the drivers](/data/yellow/libersystem/build.sh:109) and [the kernel](/data/yellow/libersystem/build.sh:117) with plain `cargo build`. The services crate [optimises only `pix`](/data/yellow/libersystem/src/user/services/core/Cargo.toml:412) and leaves service control flow in the debug profile. The packager stages user ELFs [from the `debug` tree](/data/yellow/libersystem/src/tools/mkpackages/src/main.rs:740), and the image boots [the `debug` kernel](/data/yellow/libersystem/image.sh:124). The demo and its libraries, by contrast, are [release PIEs](/data/yellow/libersystem/docs/PERF.md:25).

   [The primitives](/data/yellow/libersystem/docs/todo/P02M0189.md:91) and every service and driver term would therefore be measured on this mixed configuration, and the account would not say so.

   The verdict classifies each large term as necessary work, redundant work, a scheduling wait, or [work done "the long way"](/data/yellow/libersystem/docs/todo/P02M0189.md:124) for want of a primitive. [The comparison](/data/yellow/libersystem/docs/todo/P02M0189.md:110) calls the gap to the in-process floor the system's charge. Unoptimised syscall, IPC, handler and driver code inflates exactly those terms. That cost will be reported as architecture or as missing primitives, and the resulting follow-up items will be aimed at the profile.

   **Correct the scope and verdict items.** Record the profile of every layer beside its term. Allow a term to be classified as necessary work or as a missing primitive only after showing that its cost is not a profile artefact. That can be done by measuring that layer optimised once, or by adding the unoptimised build as a class of its own. An earlier attempt to optimise the whole services package [changed boot behaviour](/data/yellow/libersystem/docs/PERF.md:1120), so the plan should say which route it takes.

4. **High - Three measurement conditions the plan requires do not exist in this system, so the items built on them cannot close on a number.**

   The plan measures DisplayService ["composing ONE surface and ... several"](/data/yellow/libersystem/docs/todo/P02M0189.md:83), and a real path ["with the display doing what a desktop makes it do"](/data/yellow/libersystem/docs/todo/P02M0189.md:109). DisplayService composes nothing. [At most one surface is visible](/data/yellow/libersystem/src/user/services/core/src/display_service.rs:8), and that surface is copied or scaled into the scanout. A hidden surface's present [completes as discarded without a copy](/data/yellow/libersystem/src/user/services/core/src/display_service.rs:739). The compositor, simultaneous composition and any desktop [are deferred](/data/yellow/libersystem/docs/todo/P02M0103.md:1566).

   The in-process leg is defined as ["the floor the benchmark suites already measure"](/data/yellow/libersystem/docs/todo/P02M0189.md:107). Those suites are [host `cargo run --release` runs](/data/yellow/libersystem/bench.sh:6) of frozen scenes, not the demo's scene. Yet the comparison is meant to run ["the same drawing work"](/data/yellow/libersystem/docs/todo/P02M0189.md:107) three ways, and the plan trusts only numbers taken [in the guest, under the real scheduler](/data/yellow/libersystem/docs/todo/P02M0189.md:102).

   Every item [closes on a number](/data/yellow/libersystem/docs/todo/P02M0189.md:155), and the milestone closes only when [every item is done](/data/yellow/libersystem/docs/todo/P02M0189.md:3). The multi-surface and desktop items can therefore never close. And "the single number this milestone is for" would compare a host run of different scenes with a guest interval.

   **Correct the multi-surface and comparison items.** Replace "several composed surfaces" with what the service actually scales with today: one visible surface plus N connected hidden ones. Their cost lies in the dispatch loop's [`wait_any` over every surface channel on each pass](/data/yellow/libersystem/src/user/services/core/src/display_service.rs:1912). Alternatively, move the item to the compositor's milestone. Defer the desktop leg explicitly until a compositor exists. Define the in-process leg as the demo's own scene, drawn in the guest by the same binary and profile into private memory with no surface, and cite `bench.sh` only as context.

5. **Medium - Every term must be a work/wait pair, but the specified instrument records only user-side timestamps and the kernel does not account run time, so preemption inside a term cannot be told apart from work.**

   The plan reports [every term as a pair](/data/yellow/libersystem/docs/todo/P02M0189.md:79), and [the gate fails](/data/yellow/libersystem/docs/todo/P02M0189.md:150) when a pair is missing. The instrument, however, is a per-process ring of [(site, timestamp) pairs](/data/yellow/libersystem/docs/todo/P02M0189.md:52). Timestamps do show a cross-process boundary's wait, as the gap between two sites. But a thread preempted inside a term looks identical to one that is computing.

   The scheduler is [round robin with a one-tick quantum](/data/yellow/libersystem/src/kernel/sched/mod.rs:4), and it puts a woken thread on [the waker's run queue](/data/yellow/libersystem/src/kernel/sched/mod.rs:12). Nothing reports on-CPU time: [`ProcessStats`](/data/yellow/libersystem/src/abi/src/lib.rs:1012) carries messages, handles, memory and state only.

   The distinction the item calls "opposite findings with opposite fixes" is therefore not measurable as specified. The implementer must either add kernel scheduler accounting that the plan does not scope, or report a "work" figure that silently includes run-queue time, which the gate would accept.

   **Correct the instrument and wait/work items.** Either add a per-thread on-CPU time counter (or switch records on the same clock) that the instrument samples at each site, as the kernel change [the exception clause](/data/yellow/libersystem/docs/todo/P02M0189.md:138) admits, and define wait as blocked time plus time spent runnable but not running. Or narrow the pair explicitly to time blocked in a syscall versus everything else, and state that preemption is not separated.

6. **Medium - The instrument is designed from scratch around a frame identity the application allocates and the path carries, which duplicates the tree's existing cross-process tracing and counters and would change the display wire protocol the plan must not touch.**

   The tree already has a switchable cross-process marker facility, made of these parts:
   - [`rt::PERF`](/data/yellow/libersystem/src/user/runtime/rt/src/lib.rs:584) compiles markers out.
   - [`perf_now`](/data/yellow/libersystem/src/user/runtime/rt/src/lib.rs:599) reads the TSC in ring 3, without a syscall.
   - [`perf_mark`](/data/yellow/libersystem/src/user/runtime/rt/src/lib.rs:636) emits markers to the debug serial.
   - The kernel publishes the TSC rate only under [the `development-trace` boot profile](/data/yellow/libersystem/src/kernel/main.rs:442), which any process can read with [`rt::boot_profile`](/data/yellow/libersystem/src/user/runtime/rt/src/lib.rs:2403).
   - [`perf-trace.py`](/data/yellow/libersystem/src/harness/perf-trace.py:1) places every process's markers on one timeline, with [a phase breakdown down to the GPU present](/data/yellow/libersystem/src/harness/perf-trace.py:257).
   - [The `perf-anchor` gate](/data/yellow/libersystem/src/tools/check-perf-anchor.sh:1) guards it, and [DEBUG.md](/data/yellow/libersystem/docs/DEBUG.md:256) documents it.

   DisplayService also already [times its blit and its driver call on every present](/data/yellow/libersystem/src/user/services/core/src/display_service.rs:759), and serves the totals through [`display-admin.stats`](/data/yellow/libersystem/src/idl/display.lsidl:443).

   The plan nevertheless says [no measurement says where the time goes](/data/yellow/libersystem/docs/todo/P02M0189.md:9) and specifies a new facility. It leaves open how that facility's buffers leave DisplayService and the driver, and how [the "runtime request"](/data/yellow/libersystem/docs/todo/P02M0189.md:60) reaches them. Those are exactly the questions that decide whether a new IDL operation, a new grant, or the unprivileged debug-write path is needed.

   The ["frame identity the application allocates and the path carries"](/data/yellow/libersystem/docs/todo/P02M0189.md:54) has nothing to carry it. [`surface.present`](/data/yellow/libersystem/src/idl/display.lsidl:366) carries the image, the configuration serial, the generation and the damage; [`display-device.present`](/data/yellow/libersystem/src/idl/display-device.lsidl:88) carries only the generation and the damage. Adding a field changes both IDLs, their bindings, DisplayService, the driver, and the kernel suite's [hand-read device wire](/data/yellow/libersystem/src/kernel/test_suites/services.rs:3239), all on the ordinary path. That contradicts the plan's own [compiled-out rule](/data/yellow/libersystem/docs/todo/P02M0189.md:60) and [exception rule](/data/yellow/libersystem/docs/todo/P02M0189.md:138).

   The field is also unnecessary. A device present is [synchronous, with one in flight per connection](/data/yellow/libersystem/src/idl/display-device.lsidl:32), and DisplayService already allocates [a present serial](/data/yellow/libersystem/src/user/services/core/src/display_service.rs:724) that the application [receives in the reply](/data/yellow/libersystem/src/user/services/core/src/display_service.rs:753).

   **Correct the instrument items.** Build on the existing facility: its ring-3 clock and published rate, the boot profile as the runtime request, and the host collector. Replace per-marker serial writes with the fixed per-process buffer, emitted after the measured frames. State how each process's records leave it. Join application and DisplayService records by the existing present serial, and the driver's records by call nesting, with no wire change. Use DisplayService's existing counters as the cross-check for its terms.

7. **Medium - The opening numbers predate renderer changes that the demo's scene uses, and the "draw" figure excludes recording, so the 52.8 ms target is neither current nor wholly outside the renderer, and the pooled draw planned next will change it again.**

   The baseline is [the 2026-09-15 live run](/data/yellow/libersystem/docs/PERF.md:19). Several renderer changes the demo's scene exercises landed after it:
   - the [`Tile::store`](/data/yellow/libersystem/docs/PERF.md:1507) and [`Tile::load`](/data/yellow/libersystem/docs/PERF.md:1541) fixes, which every tile pays;
   - [per-row shader dispatch](/data/yellow/libersystem/docs/PERF.md:1558), used by the demo's [conic background](/data/yellow/libersystem/src/user/apps/tools/src/test2d_sw.rs:168);
   - [the decoded source](/data/yellow/libersystem/docs/PERF.md:1680), used by the demo's [bilinear image](/data/yellow/libersystem/src/user/apps/tools/src/test2d_sw.rs:194).

   No live re-measure is recorded since.

   The demo also [records the scene](/data/yellow/libersystem/src/user/apps/tools/src/test2d_sw.rs:590) before it [starts the draw clock](/data/yellow/libersystem/src/user/apps/tools/src/test2d_sw.rs:595), and so times only [`prepare` and `render`](/data/yellow/libersystem/src/user/apps/tools/src/test2d_sw.rs:868). The table nevertheless labels the figure ["record + replay"](/data/yellow/libersystem/docs/PERF.md:16), so recording and acquire both sit inside the 52.8 ms. Finally, P02M0193 will make [`test2d-sw` render through `rt::pool`](/data/yellow/libersystem/docs/todo/P02M0193.md:47), and neither plan states which comes first.

   ["Break the 52.8 ms"](/data/yellow/libersystem/docs/todo/P02M0189.md:69) therefore aims at a number whose size and share have probably moved. Part of it is render2d's own recording, not something the renderer ["hands its result TO"](/data/yellow/libersystem/docs/todo/P02M0189.md:16). And a gate meant to keep the account true ["after the next change to any layer"](/data/yellow/libersystem/docs/todo/P02M0189.md:152) will meet a multi-threaded draw that the single-chain account does not describe.

   **Correct the opening and present-path items.** Re-measure the baseline on the current tree as the first step. Name recording and acquire as terms. State which draw configuration the account and its gate measure (the serial walk, or the pool at a stated worker count), and how P02M0193's pooled draw will be accounted for when it lands.

8. **Medium - The gate names neither the path it boots nor how it obtains an image containing the compiled-out instrument, and the existing registration model provides neither.**

   The instrument is [compiled out by default, behind a build flag](/data/yellow/libersystem/docs/todo/P02M0189.md:60), yet ["one in-guest gate"](/data/yellow/libersystem/docs/todo/P02M0189.md:150) must produce the account. Gates are registered in [`check.sh`](/data/yellow/libersystem/check.sh:26) and mirrored in [the verification model](/data/yellow/libersystem/src/tools/verify-model/src/catalog.rs:417). A gate that consumes a special image needs [an image-producer row](/data/yellow/libersystem/src/tools/verify-model/src/catalog.rs:182), and none exists for an instrumented build. The only perf precedent enables its anchor through a boot profile and [assembles a private image](/data/yellow/libersystem/src/tools/check-perf-anchor.sh:50).

   "In-guest" is also ambiguous. The guest-suite demo test runs against [a kernel stand-in GPU at 192x128](/data/yellow/libersystem/src/kernel/test_suites/services.rs:3176), which is not [the path the plan requires](/data/yellow/libersystem/docs/todo/P02M0189.md:38). The live path is [the QEMU gate](/data/yellow/libersystem/src/tools/check-qemu-2d-demo.sh:51).

   As written, the gate either cannot see the instrument on the ordinary image or must rebuild an image that other gates consume. In the stand-in harness it would measure a different path, at a different size, with no driver and no device.

   **Correct the gate item.** Name it as a live x86_64 QEMU gate running on the real driver. State how its instrumented image is produced: a private output, as `perf-anchor` does, or a new producer row. Alternatively, compile the recording path in and enable it through the boot profile, so that the ordinary image serves. Register the gate in both `check.sh` and the verification model, with its demo arguments, scanout size and frame count.

Validation consisted of source, plan and history inspection only, plus one QEMU device-property query (`qemu-system-x86_64 -device virtio-vga,help`), which starts no machine. No plan, source, documentation or audit file was modified, and no build, test or guest was run.

PLANNER'S RESPONSE ON P02M0189 (2026-09-26T02:48:53Z):

Verified against the tree at the audit's commit plus the current working tree: `pacing.rs` (`UNPACED_INTERVAL_NS`, `on_present`, `step`), `frame_loop.rs` (`present_whole`/`present_rects` stamping after return, `tick_deadline` rounding up, `park` waiting on the done endpoint and the event stream), `test2d_sw.rs` (acquire and `scene.record` before the draw clock, the phase machine, `--size`, `--no-input`, `--no-second-surface`), `display_service.rs` (synchronous `copy_and_flush` inside the present, `FrameTiming { None, None }`, the counters, the newest-surface-visible rule, the dispatch loop rebuilding its wait vector every pass), `pix::blit`, `virtio_gpu.rs` and `virtio.rs` (per-rectangle transfer and flush, each a spin on the used ring yielding every 4096 polls), the display and display-device IDL, `build.sh`, the services' Cargo profile, `mkpackages`, `image.sh`, `rt` (`PERF`, `perf_now`, `perf_mark`, `clock_ns`, `boot_profile`), the kernel's `tsc_hz` emission, `sys_debug_write` (unprivileged), `sys_boot_profile` (unprivileged), the scheduler header and `ProcessStats` (no run-time accounting anywhere in the kernel), `perf-trace.py` (and that no `perf_mark` call site remains in the sources), `check-perf-anchor.sh`, `check-qemu-2d-demo.sh`, `lab.py`'s boot path, `qemu-run.sh` (no scanout knob, SMP defaulting to the host's `nproc`), `check.sh`, the verification model's catalog, `docs/PERF.md`, and the P02M0103 and P02M0193 plans. One read-only query: `qemu-system-x86_64 -device virtio-vga,help` confirms `xres=1280`, `yres=800`. Every factual claim in the eight findings holds. The plan is rewritten in place; the coordination decision on P02M0193 (the account is the serial walk, the gate pins `--workers=1` once P02M0193 lands, the pooled draw is an additional row) is written into it.

1. **ACCEPTED - the pacing wait is a named term.** Verified: after a present the next frame is due at now + 16 ms, `tick_deadline` rounds the gap up to the 10 ms tick, and `park` re-parks until the deadline has passed, so the wait lies between 16 and 26 ms; the first park after a present returns at once on the release DisplayService sent before replying. Plan changes: the opening paragraph now states the pacing wait (and the other terms the code already names) as known; the present-path item names THE FRAME LOOP'S PACING WAIT as two sub-terms (the fallback interval, and the rounding the 10 ms deadline unit adds), with every park recording the deadline requested, whether `wait_any` ended on a message or on the deadline, and when the thread ran; the primitives item measures the DEADLINE WAKE beside the channel wake; the verdict gains the class "a wait a program chose" (reported, not charged to the system; the rounding beyond it is classified like any other term); the charge is stated with and without it; changing the pacing is in EXCLUDES.

2. **ACCEPTED - the scanout, the scaled path and the device's acknowledgement.** Verified: `pix::blit` takes the row-copy path only at equal sizes and otherwise writes a 1066x800 output from a 640x480 surface on the default 1280x800 scanout; the driver's answer includes both device completions, reached by spinning. Plan changes: the conditions item records the scanout size the driver reported, the surface size, direct or scaled, source and output pixels, QEMU's display device, display back end, vCPU count, KVM, DMA mode and host CPU; the copy-count item counts per damage shape and per path with the scaled-path figures stated; the two-size item varies the scanout and the surface together (surface at the screen's own size on a 640x480 and a 1280x800 scanout) with the baseline's scaled condition as a third row, through a new `GPU_SIZE=WxH` knob in `qemu-run.sh`; the driver's terms include THE DEVICE'S ACKNOWLEDGEMENT per command; a spin is reported as DEVICE TIME with the guest CPU it burned and its yields as a separate row, not charged as guest work.

3. **ACCEPTED - the build profile of every layer.** Verified: kernel, services and drivers are plain `cargo build` (only `pix` at opt-level 2), staged from `debug`; the demo is release. Plan changes: a new section records each layer's profile beside its terms; the route is ONE OPTIMISED COMPARISON RUN of the gate with the kernel, services and drivers built through `CARGO_PROFILE_DEV_OPT_LEVEL=2` (cargo's own override, no source edit), with `--rebuild` both ways because the build caches are keyed on sources and do not see the environment; primitives are measured in both builds; a term in a `dev` layer is classified only with its optimised figure beside it, and what the optimised run removes is the new class "the unoptimised build's cost". The earlier whole-`services` failure was in the test boot's stand-in GPU; if the live boot also fails, that is recorded and every such term stays in that class, unresolved.

4. **ACCEPTED - the conditions that do not exist.** Verified: at most one surface is visible, a hidden surface's present is discarded without a copy, the compositor is deferred in P02M0103, and `bench.sh` is host runs of frozen scenes. Plan changes: the multi-surface item becomes ONE VISIBLE SURFACE AND N HIDDEN ONES through a demo `--hidden-surfaces=N` that opens its extra surfaces before the measured one (the newest surface is the visible one), at N = 0 and N = 15 (the per-connection bound of sixteen), reported against the dispatch loop's handle count; the desktop leg moves to EXCLUDES; the in-process leg is `test2d-sw --offscreen` - the demo's own scene, same binary, profile and size, in the guest, into private memory with no service, present or pacing; `bench.sh` is context only. The comparison is now two legs.

5. **ACCEPTED - work versus wait needs the scheduler.** Verified: one-tick round robin, wake onto the waker's run queue, no on-CPU accounting in the kernel or in `ProcessStats`. I took the audit's "switch records on the same clock" alternative rather than a per-thread counter syscall: while the record buffer is armed the kernel appends a SWITCH record at every context switch (outgoing thread and why it left, incoming thread, core) and a WAKE record whenever a thread becomes runnable (waker, target core, message/deadline/other). WORK is on-CPU time; WAIT is the rest, split into BLOCKED and RUNNABLE BUT NOT RUNNING, exactly the definition recommended. The same records give the channel and deadline wake primitives from the path itself. Unarmed, each hook is one test of a flag.

6. **ACCEPTED, with one part declined - build on the existing facility, no wire change.** Verified: `rt::perf_now`, the `tsc_hz` anchor under `development-trace`, `rt::boot_profile`, `perf-trace.py`'s parser, the unprivileged `SYS_DEBUG_WRITE`, DisplayService's present serial, and a synchronous single-in-flight device present; adding a frame id would change two IDLs and the suite's hand-read wire. Also noted: `rt::PERF` is a source constant, not a flag, and no `perf_mark` call site remains in the sources, so what exists is the clock, the anchor, the profile and the parser. Plan changes: timestamps are `perf_now` converted by the existing anchor; the runtime request is the `development-trace` profile; there is no build flag; records leave through the debug serial as `\x1ePERF` lines after the run and are read by a frame-account mode of `perf-trace.py`; the join is the existing present serial (demo and DisplayService), call nesting (driver) and thread (kernel records), with `surface.present` and `display-device.present` unchanged. DECLINED: the fixed PER-PROCESS buffer. DisplayService and the driver are long-lived and learn nothing when the demo stops, so their buffers would have no moment to leave without a wire change or a partial last batch, and the scheduler records the pair needs (finding 5) have to be on the same timeline anyway. The plan instead uses ONE bounded kernel buffer (8 MiB, allocated only under `development-trace`), appended by `SYS_PERF_RECORD` and armed, disarmed and drained by `SYS_PERF_CONTROL` from the measured application; a record past the bound is refused and counted. Instead of reading DisplayService's counters (only its admin connection can, and nothing on the live path does), its records are placed exactly where the counters are taken, and the account is cross-checked against the demo's own report of the armed frames.

7. **ACCEPTED - the baseline is stale and "draw" excludes recording.** Verified: the 2026-09-15 row predates the 2026-09-19 `Tile::store`, `Tile::load`, per-row shader and decoded-source changes; `scene.record` and `acquire` run before the draw clock; P02M0193 makes the demo draw through `rt::pool`. Verification also found something the audit missed: under `--frames=600 --phase-frames=60` the phase counter stops at the multi-rectangle phase, so 420 of the 600 baseline frames presented two rectangles, and the mean mixes damage shapes. Plan changes: the baseline is re-measured FIRST on the current tree, before any instrument code lands (the old command at the lab's default core count, and the gate's dormant run three times so its spread is known); acquire and recording are named terms; THE DRAW IS THE SERIAL WALK, and once P02M0193 lands every run pins `--workers=1` with the pooled draw as an additional recorded row, not a term; the account and its five-percent criterion are kept per damage shape; the `docs/PERF.md` section corrects the old row's "record + replay" label.

8. **ACCEPTED - the gate's path, image and registration.** Verified: `check.sh` and the catalog's gate list, the list of gates that boot a guest, the image-producer rows, `perf-anchor`'s private image, and the guest suite's 192x128 stand-in GPU. Plan changes: the gate is `qemu-2d-account` (`src/tools/check-qemu-2d-account.sh`), a live x86_64 QEMU gate on the real virtio-gpu driver. Because the instrument is compiled in and switched on by the boot profile, the ordinary development ISO serves: the gate builds it through the lab path as `qemu-2d-demo` does, so no producer row is needed, and it boots that one image by digest. It refuses to start while another guest is up, pins `SMP=4`, runs headless, and boots three times - `development-trace` at 1280x800 (scaled, direct, 15 hidden surfaces, offscreen at both sizes), `development-trace` with `GPU_SIZE=640x480` (direct), and `development` (dormant run and primitives) - with the demo arguments, frame count (`--frames=160 --phase-frames=40`, eight warm-up presents) and failure conditions stated. It is registered in `check.sh` and in the verification model's gate list and guest-booting list, with the demo as subject and DisplayService, the virtio-gpu driver and the kernel among the covered components.

Re-check of the whole plan: every item closes on a number produced by a named command; the instrument's cost, its dormant cost and the comparison with the first-step re-measure use the demo's ordinary report over identical frame sets, while the account is checked against the armed-frames report; the six-class verdict matches the profile and pacing items; the gate's three boots cover every row the present-path, primitives and comparison items require; the only kernel changes are the instrument's buffer, its two additive syscalls (ABI version stays 1) and the scheduler hooks, stated as the milestone's product; nothing optimises a layer; the desktop leg and pacing changes are in EXCLUDES. The plan is complete, consistent and ready for implementation. Only `docs/todo/P02M0189.md` was edited and this response appended; no source, script, test or other plan was changed, and nothing was built or booted.
