AUDITOR'S REVIEW OF PLAN P02M0201 (2026-09-25T22:57:24Z):

**Rating: 3/10.** The BMC feature set is well chosen and QEMU's IPMI devices are described correctly, but the process boundary, the PCI and SMBus hardware paths and several of the promised proofs do not fit the existing system or the emulator.

Reviewed [the plan](/data/yellow/libersystem/docs/todo/P02M0201.md) and its [originating requirement](/data/yellow/libersystem/docs/todo/P02M0099.md:355). Also reviewed the plans it builds on: [P02M0196](/data/yellow/libersystem/docs/todo/P02M0196.md), [P02M0195](/data/yellow/libersystem/docs/todo/P02M0195.md), [P02M0191](/data/yellow/libersystem/docs/todo/P02M0191.md), [P02M0188](/data/yellow/libersystem/docs/todo/P02M0188.md), [P02M0181](/data/yellow/libersystem/docs/todo/P02M0181.md) and [P02M0200](/data/yellow/libersystem/docs/todo/P02M0200.md). On the code side: the provider catalogue, the kernel's PCI scan and device table, the boot hand-off, AdminService, SystemManager and the power vocabulary, all at commit `07371c44af82a11c1d275b0cbd832899f712e62a`. Plans P02M0197 to P02M0202 exist only in the working tree (they are not yet committed), and P02M0190, P02M0196 and P02M0099 carry uncommitted edits; all were read as they stand there, and line numbers refer to the working tree. These were checked against the QEMU 10.0 IPMI and SMBus device sources and the QEMU 10.0.11 installed on this machine. The plan is a checklist written before implementation, so the findings concern decisions and prerequisites it needs, not the expected absence of feature code.

1. **High - The plan never says which process is "the BMC service" or how it reaches the transports, yet three of its deliverables can only be published by a driver that holds a device claim.**

   [The transports](/data/yellow/libersystem/docs/todo/P02M0201.md:21) and [the BMC service](/data/yellow/libersystem/docs/todo/P02M0201.md:32) are separate parts, with no provider kind, IDL or process boundary between them. The [closed provider vocabulary](/data/yellow/libersystem/src/idl/device.lsidl:154) has no IPMI kind.

   Publication in this system belongs to a binding. DeviceManager [publishes only what a committed binding offered](/data/yellow/libersystem/src/user/services/core/src/device_manager.rs:2762), and the [catalogue](/data/yellow/libersystem/src/idl/device.lsidl:360) offers no publish path to anyone else. P02M0181 states that [a driver publishes through its authorized binding and never receives a writable power-service connection](/data/yellow/libersystem/docs/todo/P02M0181.md:40).

   Yet the plan has the service deliver three things that need such a publication:
   - [temperatures and power supplies as P02M0181 sources](/data/yellow/libersystem/docs/todo/P02M0201.md:35);
   - [the BMC watchdog as a P02M0200 provider](/data/yellow/libersystem/docs/todo/P02M0201.md:44), which is a [`watchdog` provider kind](/data/yellow/libersystem/docs/todo/P02M0200.md:22);
   - the AdminService actions (SEL clear, power control, LAN and user changes), whose executors are [`admin-executor` publications of drivers DeviceManager bound](/data/yellow/libersystem/src/user/services/core/src/admin_service.rs:6).

   The vocabulary does not fit either:
   - [`source-kind`](/data/yellow/libersystem/src/idl/power.lsidl:18) has no power-supply kind.
   - Producers must convert through [the `power-model` leaf's adapters](/data/yellow/libersystem/docs/todo/P02M0181.md:77), which exist for ACPI and HID only.
   - ["In the system graph"](/data/yellow/libersystem/docs/todo/P02M0201.md:36) has nowhere to go: [the graph](/data/yellow/libersystem/src/idl/observability.lsidl:90) holds components and trace spans, not sensors.

   Work is missing under either reading of the plan. If the BMC service is a service, it can publish none of the three. If it lives in the transport driver, then the message layer, SDR/SEL/FRU parsing, watchdog pets and chassis control all share one driver process, and `bmc` still needs a new client-facing kind.

   Either way, KCS and SSIF carry one message at a time. A watchdog pet queued behind SDR, FRU or SEL reads can therefore miss its period unless the plan states how the interface is shared.

   **Correct the transport and service items** by choosing the process graph and its manifest declarations. One option: a driver per interface holds the claim, the message layer and the `power-source`, `watchdog` and `admin-executor` publications, and publishes an `ipmi` provider kind that a BMC service consumes for the tool. Also:
   - State how pets take priority on the single-message interface.
   - Map IPMI temperature sensors to a source kind and its trip kinds.
   - Decide whether power supplies get an append-only kind or are left out.
   - Add an IPMI adapter (SDR reading conversion) to `power-model`.
   - Either add a sensor record to the observability vocabulary as an item, or drop "in the system graph".

2. **High - The PCI forms and SSIF need I/O-space BARs, which no kernel path, device record or planned `PortRange` source covers.**

   The plan reaches [`pci-ipmi-kcs` and `pci-ipmi-bt` through their claim](/data/yellow/libersystem/docs/todo/P02M0201.md:23), and [SSIF through a driver for q35's ICH9 SMBus controller](/data/yellow/libersystem/docs/todo/P02M0201.md:24). In QEMU 10.0 all three use I/O space:
   - [`pci-ipmi-kcs`](https://github.com/qemu/qemu/blob/v10.0.0/hw/ipmi/pci_ipmi_kcs.c) and [`pci-ipmi-bt`](https://github.com/qemu/qemu/blob/v10.0.0/hw/ipmi/pci_ipmi_bt.c) register BAR 0 as I/O space.
   - The [ICH9 SMBus function](https://github.com/qemu/qemu/blob/v10.0.0/hw/i2c/smbus_ich9.c) registers its SMBus base BAR as I/O space, and decodes it only while the host-enable bit in its configuration space is set.

   None of this has a path in the system:
   - The kernel [skips I/O BARs](/data/yellow/libersystem/src/kernel/arch/common/pci/mod.rs:920), and [`bar_address` answers `None` for one](/data/yellow/libersystem/src/kernel/arch/common/pci/mod.rs:845).
   - [The families it resolves](/data/yellow/libersystem/src/kernel/arch/common/pci/mod.rs:43) all use memory BARs, and [a device record carries one MMIO BAR](/data/yellow/libersystem/src/kernel/device.rs:19). Every other function becomes [an identity row with no resources](/data/yellow/libersystem/src/kernel/device.rs:128).
   - [A ring-3 driver cannot touch configuration space](/data/yellow/libersystem/src/kernel/device.rs:9), so the ICH9 driver cannot set the enable bit itself.
   - P02M0191 mints ranges [only from ACPI `_CRS` descriptors and COM1-COM4](/data/yellow/libersystem/docs/todo/P02M0191.md:24), and [excludes ranges nobody described](/data/yellow/libersystem/docs/todo/P02M0191.md:55). PCI BARs are not among its sources.

   So three of the five interface forms cannot be implemented under any existing or planned milestone. Neither can [the gate's "each interface ... ISA and PCI"](/data/yellow/libersystem/docs/todo/P02M0201.md:51). This needs kernel and DeviceManager work, not a driver change.

   **Correct the transport items and the dependency on P02M0191** by adding PCI I/O BARs as a minting source: the kernel's own scan records their base and length, the range is handed over and revoked with the claim, and I/O decode is enabled at claim. Add a kernel-mediated way to set ICH9's host-enable bit. If that work is not wanted now, scope the plan to the ISA forms and mark the PCI forms and SSIF as waiting on it.

3. **Medium - SSIF cannot run over P02M0195's bus contract, and the ICH9 controller cannot serve that contract.**

   The plan has the ICH9 driver ["serving P02M0195's bus contract, and SSIF's ... messages over it"](/data/yellow/libersystem/docs/todo/P02M0201.md:24). This repeats P02M0099's claim that SSIF [takes that contract "as it stands"](/data/yellow/libersystem/docs/todo/P02M0099.md:5889).

   The contract [as implemented](/data/yellow/libersystem/src/user/libs/driver/hid-i2c/src/lib.rs:134) is raw I2C: `write`, `read` and `write_read`, each into a buffer the caller sizes. SSIF uses SMBus block write and block read. QEMU's `smbus-ipmi` [checks the count byte and moves at most 32 bytes per block](https://github.com/qemu/qemu/blob/v10.0.0/hw/ipmi/smbus_ipmi.c). A response is as long as the count the device sends first, which `write_read` cannot express.

   The ICH9 host has the opposite limit: it [performs only SMBus protocols](https://github.com/qemu/qemu/blob/v10.0.0/hw/i2c/pm_smbus.c). These are quick, byte, byte/word data, block data with a device-supplied count, and an I2C block read after one command byte. It cannot implement a plain multi-byte `read`, or a `write_read` with a two-byte register, which is what HID-over-I2C issues.

   [The SSIF alert line](/data/yellow/libersystem/docs/todo/P02M0201.md:25) is the SMBus alert that [P02M0195 excludes](/data/yellow/libersystem/docs/todo/P02M0195.md:60), and QEMU's `smbus-ipmi` wires none.

   One `write_read` cannot mean both "read N bytes" and "SMBus block read" on the same controller. So either the driver guesses which one was meant, or SSIF breaks.

   **Correct the SSIF item** with a small SMBus block contract: block write, block read that returns the device's count, and optional PEC. The ICH9 driver serves it, a raw I2C master can emulate it, and SSIF consumes it. State that ICH9 does not serve the I2C contract. Drop the alert line or name the contract that carries it, and correct P02M0099's sentence in the same change.

4. **Medium - Several proofs in the verification part cannot happen in QEMU, and others would pass without reaching the driver's checks.**

   Chassis control: the gate wants [a power cycle "seen as QEMU's reset"](/data/yellow/libersystem/docs/todo/P02M0201.md:53). QEMU's [`ipmi_do_hw_op`](https://github.com/qemu/qemu/blob/v10.0.0/hw/ipmi/ipmi.c) answers power cycle and power on with completion code 0xD5, "not supported". Of the chassis-control verbs, only hard reset (a system reset), power down (a shutdown request) and the soft shutdown (an ACPI power-button request) act.

   Watchdog: the plan proves the BMC watchdog ["through P02M0200's gate"](/data/yellow/libersystem/docs/todo/P02M0201.md:54), whose oracle is [`-watchdog-action none` and QMP's `WATCHDOG` event](/data/yellow/libersystem/docs/todo/P02M0200.md:50). But when [`ipmi-bmc-sim`'s watchdog](https://github.com/qemu/qemu/blob/v10.0.0/hw/ipmi/ipmi_bmc_sim.c) expires, it sets its sensor bits and calls the chassis operation directly. It never reaches QEMU's watchdog layer, so no event is raised and the action option is ignored.

   Hostile and silent BMC: [these cases](/data/yellow/libersystem/docs/todo/P02M0201.md:55) never reach the guest as a raw answer, because QEMU's interface emulation rewrites them first:
   - KCS and BT replace a response larger than their message buffers (300 bytes) with completion code 0xCA. SSIF truncates at 255 bytes with 0xC6.
   - BT [writes back the sequence number the guest sent](https://github.com/qemu/qemu/blob/v10.0.0/hw/ipmi/ipmi_bt.c), and a response to a message not awaited is dropped.
   - [An external BMC that stops answering](https://github.com/qemu/qemu/blob/v10.0.0/hw/ipmi/ipmi_bmc_extern.c) becomes 0xC3 after four seconds, and a disconnected one becomes 0xD2.
   - `ipmi-bmc-sim` produces none of these cases, and the plan names no harness BMC behind `ipmi-bmc-extern`.

   Features the simulator does not have:
   - It implements no LAN configuration, user or chassis-identify command. So [LAN, users](/data/yellow/libersystem/docs/todo/P02M0201.md:45) and [the identify light](/data/yellow/libersystem/docs/todo/P02M0201.md:42) can be proved only by the [package-dependent cross-check](/data/yellow/libersystem/docs/todo/P02M0201.md:57).
   - It gives the harness no way to change a sensor reading, so no live sensor change can reach P02M0181.

   As written, the gate either fails on items the emulator refuses, or passes items it never exercises. "A sequence number it was never sent" passes without the driver ever being shown one.

   **Correct the verification items:**
   - Prove chassis control with hard reset, and with power down seen as QMP `SHUTDOWN`. Prove power cycle as a refusal that is reported.
   - Prove the BMC watchdog by the reset on the serial log and by the BMC's own expiration flags, not by QMP.
   - Add a harness BMC behind `ipmi-bmc-extern`, for oversize answers below QEMU's buffer sizes and for 0xC3 and 0xD2.
   - Move the sequence-number and stuck-interface cases to host suites with a scripted register model.
   - State that LAN, users, identify and live sensor changes are proved only if the owner approves the `ipmi_sim` path, or leave them out.

5. **Medium - The AdminService actions are not itemized, and two of them collide with P02M0188 and with SystemManager.**

   The plan adds [SEL clear](/data/yellow/libersystem/docs/todo/P02M0201.md:37), [power off, cycle and reset](/data/yellow/libersystem/docs/todo/P02M0201.md:42), and [changes to the BMC's LAN configuration and users](/data/yellow/libersystem/docs/todo/P02M0201.md:45). What AdminService offers today does not stretch that far:
   - [`admin-action` has two values](/data/yellow/libersystem/src/idl/admin.lsidl:33), and [a request scope holds at most two actions](/data/yellow/libersystem/src/idl/admin.lsidl:101).
   - [Each executor publication name maps to one action](/data/yellow/libersystem/src/user/services/core/src/admin_service.rs:68).
   - The only policy rows are [DFU and the probes](/data/yellow/libersystem/src/user/services/core/src/permission_manager.rs:964), and [ordinary tools get no request grant](/data/yellow/libersystem/docs/todo/P02M0188.md:25).
   - The protected screen [renders parameters as at most 32 hex bytes](/data/yellow/libersystem/src/user/services/logic/src/admin_descriptor.rs:153).

   Changing BMC users means setting passwords, and P02M0188 [deliberately has no secret entry](/data/yellow/libersystem/docs/todo/P02M0188.md:10). A password placed in the parameters is shown on the screen, and a digest of the descriptor that contains it is [written to the journal](/data/yellow/libersystem/docs/todo/P02M0188.md:163), which is meant to [exclude secrets](/data/yellow/libersystem/docs/todo/P02M0188.md:167). Placed in the payload instead, it appears on screen only as a digest that nobody can check.

   Power control through the BMC also bypasses [the one door to stopping the machine](/data/yellow/libersystem/src/idl/process.lsidl:172), which [stays with SystemManager](/data/yellow/libersystem/docs/todo/P02M0181.md:7). It is also a hard stop, with no flush first.

   Each action needs IDL, executor, policy, rendering and gate work, and none of it is in the plan. The user-change action cannot be built on the path P02M0188 delivered.

   **Correct the SEL, chassis and LAN/user items:**
   - Keep the LAN configuration and users read-only, unless a secret-entry design is planned alongside them.
   - Itemize the AdminService extension for SEL clear and chassis control: the new actions, the scope bound, several actions per executor, a `bmc` policy row, readable rendering and gate cases.
   - State how BMC power control relates to SystemManager. For example, reset and cycle only after an orderly stop, or a descriptor that names the operation a hard stop.

6. **Medium - The item that logs the system's own events relies on a shutdown notice and a kernel path to the BMC that do not exist and are not planned.**

   [The item](/data/yellow/libersystem/docs/todo/P02M0201.md:40) logs three events: boot, orderly shutdown and kernel panic.

   Orderly shutdown: SystemManager's [reboot and power-off are one kernel call each](/data/yellow/libersystem/src/user/services/system_manager/src/main.rs:434), and `system-power` [deliberately has no graceful shutdown](/data/yellow/libersystem/src/idl/process.lsidl:185). No service is told that a shutdown is coming.

   Kernel panic: the [panic handler](/data/yellow/libersystem/src/kernel/panic.rs:5) prints to serial and halts. The interface belongs to a userspace driver through a `PortRange`, and the kernel knows neither the KCS protocol nor where the interface is: [AML runs in userspace by default](/data/yellow/libersystem/docs/todo/P02M0196.md:46), and no SMBIOS table reaches the kernel (finding 7). P02M0191 plans a polled panic path like this [for COM1 only](/data/yellow/libersystem/docs/todo/P02M0191.md:42).

   The gate [checks only the boot event](/data/yellow/libersystem/docs/todo/P02M0201.md:53). So two of the three promised events have no mechanism, and the panic event, the one that matters most after a crash, has no owner and no test.

   **Correct the system-events item:**
   - Keep the boot event.
   - Make the shutdown event depend on a named pre-shutdown notification, as an item here or in SystemManager's plan.
   - Either drop the panic event or plan it as kernel work: the interface's location handed to the kernel at bind, a polled writer that aborts an in-flight transaction, and who owns the interface after a panic. Add a gate that panics on purpose and reads the log.

7. **Low - SMBIOS type 38 cannot be read, because nothing carries the SMBIOS table to the system.**

   [Discovery](/data/yellow/libersystem/docs/todo/P02M0201.md:29) uses the `IPI0001` device and SMBIOS type 38, "the second checking the first". P02M0196a lists [SMBIOS type 38 as a static source](/data/yellow/libersystem/docs/todo/P02M0196.md:36).

   But the only firmware tables [the boot hand-off](/data/yellow/libersystem/src/boot/protocol/src/lib.rs:179) carries are the [RSDP](/data/yellow/libersystem/src/boot/protocol/src/lib.rs:218) and a [device tree](/data/yellow/libersystem/src/boot/protocol/src/lib.rs:232). The [x86 loader looks up only the ACPI tables](/data/yellow/libersystem/src/boot/loader/src/arch/x86_64/mod.rs:403) in the firmware's configuration table, and no source file under `src/` mentions SMBIOS.

   So the cross-check cannot be built as written. SMBIOS is also the only route to the ISA interfaces that does not wait for the whole AML interpreter.

   **Correct the discovery item**, which a small change fixes: make `IPI0001` sufficient and SMBIOS optional, or have P02M0196a itemize the loader and hand-off work and reference that item here.

Validation was plan, source and QEMU source inspection, plus QEMU 10.0.11 `-device <name>,help` property queries, which start no VM. No plan or source code was modified, and no build or guest execution was performed for this audit.

PLANNER'S RESPONSE ON P02M0201 (2026-09-26T02:52:54Z):

Verified every finding against the working tree and the plans as they stand after this round's coordinated edits. Source: `src/idl/device.lsidl` (the closed `provider-kind` ends at `admin-executor = 20`, with no `ipmi` or `watchdog`), DeviceManager's `publish_all` (a publication exists only as a committed binding's declared `provides`), `power.lsidl` (`source-kind` is AC, battery, UPS, load, thermal zone; four trip kinds), `observability.lsidl` (the graph holds components and trace spans), `admin.lsidl` (two actions, `@bound(2)` scope), `admin_service.rs` (`action_of` maps one publication name to one action, `adopt` refuses a second executor for an action), `admin_descriptor.rs` (parameters shown as at most 32 hex bytes), `permission_manager.rs` `admin_policy` (DFU and the probes only), `process.lsidl` `system-power`, SystemManager's `PowerApi`, ServiceManager's `shutdown_all` (SIG_KILL in reverse order, no service told), `panic.rs` (serial, then halt), the kernel PCI scan (`probe_bar` and `bar_address` skip I/O BARs, `RESOURCED` is class-keyed memory BARs, every other function an identity row), `device.rs`, the boot protocol (`rsdp` and `dtb` only) and the x86 loader's ACPI-only configuration-table lookup. QEMU: the v10.0.0 sources of `ipmi.c` (`ipmi_do_hw_op`: 0xD5 for power cycle and power up), `ipmi_bmc_sim.c` (watchdog expiry calls `do_hw_op` directly; no reset handler; no LAN, user or identify command), `ipmi_bmc_extern.c` (0xC3 after 4 s, 0xD2 when disconnected), `ipmi_kcs.c`/`ipmi_bt.c` (0xCA past 300 bytes, BT sequence rewritten), `smbus_ipmi.c` (255 bytes/0xC6, no PEC), `pci_ipmi_kcs.c`/`pci_ipmi_bt.c` (I/O BAR 0, INTx only), `smbus_ich9.c` (BAR 4 decoded only while HOSTC.HST_EN is set), `hw/acpi/ipmi.c` (`IPI0001`, `I2cSerialBusV2` with resource source "^") and SMBIOS type 38, plus `qemu-system-x86_64 -device NAME,help` for the seven IPMI devices (no guest started). All seven findings are correct in substance and all are ACCEPTED; three take a smaller or different correction than the one recommended, stated below.

1. **ACCEPTED - Process graph, publications and the sensor vocabulary.** Confirmed: no `ipmi` kind, publication only through a committed binding, no power-supply source kind, adapters only for ACPI and HID, no sensor record in the graph. Plan changes: a new "WHO HOLDS WHAT" section - `ipmi`, one driver program and one process per bound interface, holds the claim, the message layer and the `ipmi` (new kind, one consumer), `power-source`, `watchdog` and two `admin-executor` publications; `smbus_ich9` publishes P02M0195's `i2c-bus`; `bmc_service` holds no claim, publishes nothing and consumes `ipmi` through a kind-scoped catalogue connection; `bmc` is the tool. A "MANIFEST DECLARATIONS" item (matches, `dma = "none"`, `provides` with most/consumers, `ipmi` appended at version 1) and the BMC service's manifest row (catalogue factory for `ipmi`, supervisor read root, its serve root, the shutdown-notice declaration). PET PRIORITY: one queue in front of the single-message interface, a pet or re-arm at its head, long reads cut into bounded transactions, a 5 s transaction deadline and a 1 s KCS abort, so a pet waits at most 6 s and the BMC watchdog's shortest advertised timeout is 15 s (three deadlines); a host suite drives a stuck read ahead of a pet. SENSORS: temperatures become `thermal-zone` sources (at most sixteen, P02M0181's per-provider cap), upper non-critical/critical/non-recoverable as `passive`/`hot`/`critical` trips, `over-temperature` as a reported alarm; power supplies are LEFT OUT of P02M0181 (discrete sensors; no source kind fits) and shown by the tool, recorded in EXCLUDES. A `power_model::ipmi` adapter (SDR linear conversion in checked arithmetic, units to millidegrees Celsius, unknown/unsupported states) is itemized. "In the system graph" is dropped: sensors are the tool's. Two interfaces to one BMC are now stated (AdminService's existing refusal of a second executor, one watchdog held, the operator disables one); automatic de-duplication is excluded.

2. **ACCEPTED - PCI I/O BARs and the ICH9 host enable.** Confirmed in `arch/common/pci/mod.rs` and `device.rs`, and in QEMU's sources. Per the cross-milestone decision the PCI I/O BAR source (every function's I/O BARs recorded by the scan, minted with the PCI claim, I/O decode set at claim) belongs to P02M0191, whose plan now carries it as its source (b); this plan depends on it and orders the PCI forms FIRST, since they need no firmware description. Plan changes: a "KERNEL'S RESOLVER ROWS" item - class 0x0C/0x07 interfaces 0x01 and 0x02 naming BAR 0 (an I/O BAR as a `PortRange`, a memory BAR as the existing MMIO grant), and `8086:2930` in the (vendor, device)-keyed table naming BAR 4, the kernel saving HOSTC, setting HST_EN and clearing I2C_EN AT CLAIM and restoring it at release (the `i2c-i801` sequence). Declined part: a runtime kernel-mediated configuration write - the claim-time write is enough and keeps this milestone independent of P02M0200's declared registers. Also stated: every form polls (the PCI models are INTx-only), and the PCI forms on aarch64/riscv64 are excluded because P02M0191 answers `unsupported` there.

3. **ACCEPTED - SSIF and the bus contract.** Confirmed: `hid-i2c`'s `I2cBus` is three raw I2C operations, `smbus-ipmi` needs device-counted blocks of at most 32 bytes, `pm_smbus` performs SMBus protocols only, and P02M0099 says SSIF takes the contract "as it stands". Plan changes: SSIF uses the SMBus half of P02M0195's ONE contract with its declared functionality set; the ICH9 driver declares block write, block read returning the device's count, and PEC, and NO plain I2C (a HID device is refused on it); SSIF binds as P02M0195's child binding with an address-scoped connection, polls, and runs on ICH9 and not on virtio-i2c, which cannot serve a device-counted block read; the alert line is dropped and SMBus alert excluded. Declined part: a separate small SMBus block contract - one contract with a functionality set is smaller and is what P02M0195 now defines. P02M0099's sentence is not edited here (only this plan and this file are); its correction is passed to the coordinator as a cross-plan change.

4. **ACCEPTED - What QEMU can and cannot prove.** Every QEMU behaviour cited was confirmed in the v10.0.0 sources. Plan changes: a "WHAT IT CANNOT SHOW" section; chassis control proved by hard reset (a second boot on the serial log), power down (the `shutdown` run state under `-no-shutdown`, read with `query-status`), soft shutdown (the same state after DeviceManager's power-button line) and power cycle as the BMC's reported 0xD5 refusal - DURABLE oracles instead of QMP events, because the harness holds no standing QMP listener; the BMC watchdog through P02M0200c's BMC case with the serial log's boot count and the simulator's own expiration flags, not QMP; a harness BMC, `ipmi-harness-bmc.py`, behind `ipmi-bmc-extern` over a Unix socket, with hostile modes QEMU passes through (a 273-300 byte KCS answer against the driver's 272-byte bound, silence as 0xC3, a closed socket as 0xD2, malformed records); host suites with a scripted register model for the sequence number never sent, the BT length past its buffer, stuck interfaces, SSIF block errors, PEC and the pet queue. Different part: LAN configuration, users, identify and the live sensor change are proved against the harness BMC's scripted answers (decoding per the specification's layouts), not only if the owner approves `ipmi_sim`; the `ipmi_sim`/`ipmitool` cross-check stays the one test against a second implementation and stays "asked when the part starts". Also: `smbus-ipmi` at 0x10 (its default 0 is the general-call address), the ISA forms with `irq=0`, all five interfaces in one boot.

5. **ACCEPTED - Administrative actions, secrets and SystemManager.** Confirmed in `admin.lsidl`, `admin_service.rs`, `admin_descriptor.rs`, `permission_manager.rs` and P02M0188. Plan changes: LAN configuration and users are READ-ONLY; changing them is excluded with the reason (a user change is a password; P02M0188 has no secret entry and its journal excludes secrets). THE ADMINSERVICE EXTENSION is itemized: `bmc-sel-clear = 3` and `bmc-chassis-control = 4` appended at version 1; the `bmc` row in `admin_policy` with the `bmc:` prefix; what the executor freezes (target `bmc:<GUID>` at the binding's generation, the entry count or one operation byte, no payload) and revalidates; an `Operation:` line the service renders from a fixed table, with unrenderable parameters refused; the journal; host tests and gate cases; the tool's PermissionManager row (`Bmc` and `AdminRequest`). The relation to SystemManager is stated: `system-power` remains the only way the system stops itself and the shell's `shutdown`/`reboot` its orderly form; chassis control is the BMC acting from outside, reachable only through AdminService, and the screen names power down, cycle and reset as hard stops; soft shutdown takes the system's own power-button path; power up and the diagnostic interrupt are refused. Declined part: "several actions per executor" - two executor publications with one action each leave AdminService's adoption rule and its refusal of a second executor unchanged, and the existing `@bound(2)` scope already holds the tool's two actions.

6. **ACCEPTED - The system's own events.** Confirmed: ServiceManager's orderly path kills services without telling them, `system-power` is one syscall, the panic handler prints and halts. Plan changes: BOOT COMPLETED keeps, with its trigger defined (the supervisor status the `online` report tests, read through the supervisor role, at most once per boot); the ORDERLY SHUTDOWN event rides P02M0200's orderly-shutdown notice - declared in the BMC service's manifest row, the driver sending the event, answered within the notice's bound - and immediate `system-power` calls and BMC hard stops log none, by design. The PANIC event is DROPPED to EXCLUDES with its reason: it needs an IPMI writer inside the kernel, and the BMC watchdog already records a machine that stopped. Declined part: planning the kernel panic path.

7. **ACCEPTED - SMBIOS.** Confirmed: the hand-off carries only the RSDP and a device tree. Plan change: `IPI0001` is sufficient; SMBIOS type 38 is an optional cross-check through P02M0196a's SMBIOS item and its merge-by-resources rule (one device when both start at one base, refused and reported otherwise); a BMC that SMBIOS alone describes is excluded.

Re-check of the whole plan: it is COMPLETE (each original deliverable is itemized or excluded with a reason, and each part names its prerequisites and order); CORRECT (every QEMU behaviour it relies on or works around is taken from the QEMU 10.0 sources, and every system fact from the current tree); FEASIBLE (the PCI forms need only P02M0191; the ISA forms add P02M0196a/b; SSIF adds P02M0195's bus half and P02M0196's companion join; part b needs P02M0200a, P02M0188 and the landed P02M0181); and CONSISTENT (the 272-byte bound sits below QEMU's 300-byte buffer, the 5 s deadline above `ipmi-bmc-extern`'s 4 s, the 15 s shortest watchdog timeout covers three deadlines, sixteen thermal zones match P02M0181's cap, two actions match the scope bound, nothing is versioned). The owner question already deferred - installing Debian's `openipmi` and `ipmitool` - stays asked when the part starts. No source code was changed; only the plan and this response were written.


AUDITOR'S RE-AUDIT OF PLAN P02M0201 (2026-09-26T04:01:12Z):

**Rating: 6/10.** The QEMU facts, the process graph and the transport ordering now hold, but the administrative extension contradicts the landed admin path, BMC identity rests on an optional command QEMU's simulator does not answer by default, and the new drivers have no place in the planned suspend transaction.

The complete history was read: the original review's seven findings and the planner's response accepting all seven, three with a smaller or different correction. The plan was checked as it stands at commit 0dd5da07 (the tree is clean). It was verified against the tree - the kernel's PCI scan, class-triple table and device table, the provider vocabulary, the manifest and its validator, AdminService with its descriptor checks and executor seam, PermissionManager's admin policy, ServiceManager's supervisor channels and relaunch list, DeviceManager's power-button path - and against P02M0191, P02M0195, P02M0196, P02M0197, P02M0198, P02M0200, P02M0181, P02M0188, P02M0202 and P02M0099's IPMI passages. External checks: QEMU v10.0.0 sources (identical to v10.0.11 for every IPMI, SMBus and SMBIOS file used), `-device ...,help` on this machine's 10.0.11 binaries, the IPMI 2.0 rev 1.1 specification, and Linux v6.12's IPMI and i2c-i801 drivers. The planner's corrections hold for the QEMU behaviour, the driver/service split, P02M0191's PCI I/O BAR source, the claim-time HOSTC write, SSIF on P02M0195's functionality set (with P02M0099's sentence corrected), the durable oracles, read-only LAN and users, the dropped panic event and SMBIOS as an optional cross-check; what remains is below.

1. **High - The AdminService extension cannot run on P02M0188's landed path as written, and its SEL clear can erase records the person never saw.**

   The plan has the executor freeze both actions with "no payload", take as SEL clear's parameters "the log's entry count at preparation (two bytes)", fail revalidation only "once the binding or the BMC changed", and run "Reserve SEL, Clear SEL" at execution ([executor item](/data/yellow/libersystem/docs/todo/P02M0201.md:216)). The screen promises "erase all N records" ([prompt table](/data/yellow/libersystem/docs/todo/P02M0201.md:220)). The item changes none of three rules the landed path enforces:
   - a request carries a payload handle ([IDL](/data/yellow/libersystem/src/idl/admin.lsidl:89)) and AdminService declines one without it ([the refusal](/data/yellow/libersystem/src/user/services/core/src/admin_service.rs:535));
   - the shared executor seam refuses an empty payload ([seam](/data/yellow/libersystem/src/user/drivers/core/src/admin_operation.rs:70));
   - a preparation whose parameters differ from the requester's is declined as "the executor prepared something else" ([check](/data/yellow/libersystem/src/user/services/logic/src/admin_descriptor.rs:98), [applied](/data/yellow/libersystem/src/user/services/logic/src/admin_broker.rs:317)). An entry count the executor reads at preparation is not a parameter the plan has the `bmc` tool send, so as written every SEL-clear preparation is declined and the gate's approved case ([gate](/data/yellow/libersystem/docs/todo/P02M0201.md:255)) cannot pass.

   Even if the tool sends the count, nothing rejects records added after preparation. Confirmation may take 60 s ([P02M0188](/data/yellow/libersystem/docs/todo/P02M0188.md:95)) and the grant lives 30 s more ([P02M0188](/data/yellow/libersystem/docs/todo/P02M0188.md:108)), while P02M0188 binds "all mutation parameters before confirmation" and revalidates "immediately before the effect" ([P02M0188](/data/yellow/libersystem/docs/todo/P02M0188.md:58)). IPMI's own answer is to reserve when the records are checked: "the addition of a new event to the SEL causes the present Reservation ID to be 'canceled'. This would prevent the Clear SEL command from executing" (section 31.4, https://www.intel.com/content/dam/www/public/us/en/documents/product-briefs/ipmi-second-gen-interface-spec-v2-rev1-1.pdf). QEMU's simulator implements that cancellation (https://github.com/qemu/qemu/blob/v10.0.0/hw/ipmi/ipmi_bmc_sim.c#L431, checked at https://github.com/qemu/qemu/blob/v10.0.0/hw/ipmi/ipmi_bmc_sim.c#L1517). A reservation taken at execution closes nothing.

   Also: AdminService keeps one executor per action ([lookup](/data/yellow/libersystem/src/user/services/core/src/admin_service.rs:511), [second refused](/data/yellow/libersystem/src/user/services/core/src/admin_service.rs:1241)). The plan states the consequence only for two interfaces to ONE BMC ([pair rule](/data/yellow/libersystem/docs/todo/P02M0201.md:132)); in the five-BMC boot ([gate](/data/yellow/libersystem/docs/todo/P02M0201.md:246)) the `bmc:<GUID>` targets of four BMCs are declined, whichever published first. This continues original finding 5.

   **Correct the AdminService extension item.** The `bmc` tool asks with the entry count it read through the BMC service; the executor refuses a preparation whose count differs from the live one, takes Reserve SEL at preparation and clears with that reservation, so an event added in between fails the clear. Say how a payload-free action passes the two payload refusals (admit a zero-length payload for these two actions, or name a fixed payload the screen shows). State that one BMC per machine is administrable, run both gate cases in single-BMC boots, and add a case where an event arrives between preparation and approval.

2. **Medium - The plan names every BMC by its Device GUID, which IPMI makes optional and which QEMU's simulated BMC does not answer unless the harness sets one.**

   The GUID "names the BMC in the tool and in the administrative target" ([identity](/data/yellow/libersystem/docs/todo/P02M0201.md:149)); the target is `bmc:<device GUID>` ([executor item](/data/yellow/libersystem/docs/todo/P02M0201.md:216)); "the same device GUID" defines two interfaces to one BMC ([pair rule](/data/yellow/libersystem/docs/todo/P02M0201.md:132)); and "a different GUID or device ID on return is a different BMC" ([recovery](/data/yellow/libersystem/docs/todo/P02M0201.md:110)).

   IPMI 2.0 lists Get Device GUID as optional (O) in Table 20-1, IPM Device 'Global' Commands (same PDF as above). Linux treats an error answer as "the BMC doesn't have one" (https://github.com/torvalds/linux/blob/v6.12/drivers/char/ipmi/ipmi_msghandler.c#L3304). In QEMU 10.0 the simulator declares `guid` with `DEFINE_PROP_UUID_NODEFAULT` (https://github.com/qemu/qemu/blob/v10.0.0/hw/ipmi/ipmi_bmc_sim.c#L2205), so without `guid=` the UUID stays zero and Get Device GUID answers 0xC1 (https://github.com/qemu/qemu/blob/v10.0.0/hw/ipmi/ipmi_bmc_sim.c#L908). The help line on this machine ("UUID (aka GUID) or "auto" for random value (default)") is the UUID type's generic text; only `DEFINE_PROP_UUID` sets that default (https://github.com/qemu/qemu/blob/v10.0.0/include/hw/qdev-properties-system.h#L84).

   The simulated-BMC gates configure only "harness-written SDR and FRU files" ([QEMU summary](/data/yellow/libersystem/docs/todo/P02M0201.md:14), [gates](/data/yellow/libersystem/docs/todo/P02M0201.md:245)). As written, the five BMCs of the five-interface boot and the BMC behind SEL clear and chassis control have no GUID: there is no administrative target, and the pair rule either fails or makes all five one BMC. This is new.

   **Correct the identity item.** Define the identity of a BMC that answers no Device GUID (for example manufacturer, product and device ID with its binding) and use it for the tool, the administrative target and the pair rule. Have the harness pass a distinct `guid=` to each simulated BMC, and leave one without it to prove the fallback.

3. **Medium - The two new drivers have no place in P02M0197's suspend transaction, and a BMC watchdog served over SSIF cannot satisfy its ordering.**

   `smbus_ich9` "binds on every q35 boot" ([driver item](/data/yellow/libersystem/docs/todo/P02M0201.md:90)), and the `ipmi` binding publishes `watchdog` from every form, SSIF included ([manifest item](/data/yellow/libersystem/docs/todo/P02M0201.md:126), [watchdog item](/data/yellow/libersystem/docs/todo/P02M0201.md:198)). P02M0197 refuses a sleep "while a binding with no `suspend-deadline` is Online" and implements the exchange only in the drivers the image ships when it lands ([P02M0197](/data/yellow/libersystem/docs/todo/P02M0197.md:114), [declaration](/data/yellow/libersystem/docs/todo/P02M0197.md:106)). This plan never mentions SUSPEND or RESUME, and has no "whichever lands second" clause like the one P02M0200 and P02M0197 use ([P02M0200](/data/yellow/libersystem/docs/todo/P02M0200.md:130)). If P02M0197 lands first, every q35 sleep is refused once `smbus_ich9` binds.

   P02M0197 suspends "a binding that consumes another binding's provider always before that provider" and the watchdog's binding LAST, and resumes the watchdog's binding FIRST ([order](/data/yellow/libersystem/docs/todo/P02M0197.md:80), [resume](/data/yellow/libersystem/docs/todo/P02M0197.md:90), [watchdog step](/data/yellow/libersystem/docs/todo/P02M0197.md:119)); P02M0200 defines the driver's step the same way ([P02M0200](/data/yellow/libersystem/docs/todo/P02M0200.md:120)). An SSIF `ipmi` binding is a child of the ICH9 binding ([SSIF item](/data/yellow/libersystem/docs/todo/P02M0201.md:91), [P02M0195](/data/yellow/libersystem/docs/todo/P02M0195.md:137)). Suspended last, it would disarm the BMC timer through a suspended SMBus controller; resumed first, it would re-arm it through a controller not yet resumed. Both rules cannot hold. This is new.

   **Correct the transport and watchdog items.** State that `ipmi` (with P02M0200's watchdog step) and `smbus_ich9` implement P02M0197's SUSPEND/RESUME exchange, carried by whichever of the two milestones lands second. For SSIF, either publish `watchdog` only from KCS, BT and the PCI forms, or state that the watchdog binding's controller is suspended after it and resumed before it, a rule P02M0197 must then carry too.

4. **Medium - The BMC service's manifest row relies on a supervisor role no plan-relaunchable service can have, and on catalogue slots it does not count.**

   The row gives `bmc_service` "the supervisor's read root" and restart transparent "as PowerService's" ([row](/data/yellow/libersystem/docs/todo/P02M0201.md:144)); BOOT COMPLETED reads the supervisor status "through the supervisor role SystemGraphService already holds", including the instance's own restart record ([boot event](/data/yellow/libersystem/docs/todo/P02M0201.md:176)). No such manifest mechanism exists. Both `SUPERVISOR` roles are bare serve-roots with no interface ([PermissionManager's](/data/yellow/libersystem/src/user/services/manifest.toml:4079), [SystemGraphService's](/data/yellow/libersystem/src/user/services/manifest.toml:4456)). ServiceManager builds each channel by hand in a service-specific bootstrap ([graph's](/data/yellow/libersystem/src/user/services/core/src/service_manager/bootstrap.rs:1274), [PermissionManager's](/data/yellow/libersystem/src/user/services/core/src/service_manager/bootstrap.rs:1531)) and its standing loop serves exactly those two ([stats and stats2](/data/yellow/libersystem/src/user/services/core/src/service_manager.rs:1718)). A plan-relaunchable service gets only what the plan delivers ([relaunch list](/data/yellow/libersystem/src/user/services/core/src/service_manager.rs:1561)), so a restarted BMC service would lose the channel its "at most once per boot" rule reads. Reading through SystemGraphService does not work either: the graph registers only the components running before it starts ([bootstrap](/data/yellow/libersystem/src/user/services/core/src/service_manager/bootstrap.rs:1226)). P02M0200 itemizes its own ServiceManager-served channel ([P02M0200](/data/yellow/libersystem/docs/todo/P02M0200.md:62)); this plan does not.

   The catalogue client table has 32 slots, checked when the manifest is built ([limit](/data/yellow/libersystem/src/tools/system-manifest/src/lib.rs:217), [check](/data/yellow/libersystem/src/tools/system-manifest/src/lib.rs:1499)). Today's manifest demands 27: 17 minting roles, 9 of them in transparent services, plus DeviceManager's own. A transparent `bmc_service` needs 2 more, and the transparent services planned beside it need 6 ([P02M0200](/data/yellow/libersystem/docs/todo/P02M0200.md:80), [P02M0198](/data/yellow/libersystem/docs/todo/P02M0198.md:79), [P02M0202](/data/yellow/libersystem/docs/todo/P02M0202.md:48)), so whichever lands last fails the check. P02M0181 counted its role against this bound ([P02M0181](/data/yellow/libersystem/docs/todo/P02M0181.md:47)). This continues original findings 1 and 6.

   **Correct the BMC service row.** Itemize a plan-delivered supervisor read role (the `supervisor` interface served from ServiceManager's standing loop and minted again at a restart), or give BOOT COMPLETED a trigger the plan already delivers. Count the row's catalogue role against the 32-slot table and name the change that raises the table if the neighbouring services have used it.

5. **Low - The `smbus_ich9` match rule is one the manifest refuses.**

   The plan declares that `smbus_ich9` "matches `pci-vendor = 0x8086, pci-product = 0x2930`" ([manifest item](/data/yellow/libersystem/docs/todo/P02M0201.md:130)). The manifest validator refuses a rule that names only a vendor and product - they "may only NARROW a rule that already names a standard identity" ([validator](/data/yellow/libersystem/src/tools/system-manifest/src/lib.rs:1774), [test](/data/yellow/libersystem/src/tools/system-manifest/src/tests.rs:286)) - and a `plain-pci` rule without a class ([validator](/data/yellow/libersystem/src/tools/system-manifest/src/lib.rs:1747)). P02M0200 states the narrowing rule for its own rows ([P02M0200](/data/yellow/libersystem/docs/todo/P02M0200.md:147)). This is a defect in the correction to original finding 1.

   **Correct the manifest item.** Declare `transport = "plain-pci", pci-class = 0x0C, pci-subclass = 0x05, pci-interface = 0x00`, narrowed by `pci-vendor = 0x8086, pci-product = 0x2930`.

6. **Low - The SMBIOS cross-check is undefined for SSIF, and read literally it refuses QEMU's SSIF node on every UEFI boot.**

   The node is merged into the `smbios:38#n` row "when both start at the same base" and "refuses and reports the pair when they disagree" ([discovery](/data/yellow/libersystem/docs/todo/P02M0201.md:122)); P02M0196a merges or refuses by overlapping ranges, the later description losing ([P02M0196](/data/yellow/libersystem/docs/todo/P02M0196.md:50), [SMBIOS row](/data/yellow/libersystem/docs/todo/P02M0196.md:110)). An SSIF record has no range and names no controller, and its base holds the slave address left-justified: the specification's example stores 0010000b as 20h (Appendix C1, SMBIOS Type 38 Base Address, same PDF), and QEMU shifts it the same way (https://github.com/qemu/qemu/blob/v10.0.0/hw/smbios/smbios_type_38.c#L50). QEMU's `I2cSerialBusV2` carries the 7-bit 0x10 (https://github.com/qemu/qemu/blob/v10.0.0/hw/acpi/ipmi.c#L51). q35 boots through OVMF, so SMBIOS is present on every gate boot, and a literal comparison of the two bases refuses the node the SSIF gates need ([gates](/data/yellow/libersystem/docs/todo/P02M0201.md:246)). This continues original finding 7.

   **Correct the discovery item.** Say that an SSIF record is compared by its 7-bit address (the SMBIOS base shifted right by one) and, naming no controller, only reports a mismatch - it never merges with or refuses the namespace node.

Validation: plan, audit-history, neighbouring-plan and source inspection; QEMU v10.0.0 and v10.0.11 sources, Linux v6.12 IPMI and i2c-i801 sources and the IPMI 2.0 rev 1.1 specification read from their primary sources; `-device help` and `-device <name>,help` on the x86_64, aarch64 and riscv64 QEMU 10.0.11 binaries, which start no machine; the catalogue demand computed from the manifest with a read-only script. No plan, source or audit file was modified, and nothing was built or booted.


PLANNER'S RESPONSE ON P02M0201 (2026-09-26T05:34:17Z):

Every finding of the re-audit was verified against the tree and against the neighbouring plans as they stand in the working tree today. Source: `src/idl/admin.lsidl` (`request` carries a `handle<admin-payload>`); AdminService's `prepare` (declines "the request carried no payload" when the handle is absent) and its `action_of`, `executor_for` and `adopt` (one executor per action, a second refused); the executor seam `admin_operation.rs` (`Operations::prepare` refuses an empty payload as `Bounds`); `admin_descriptor::check_prepared` (another action, other parameters or another payload length is `Mismatch`) and `admin_broker::prepared` (declined as "the executor prepared something else"); PermissionManager's `admin_policy`; the manifest's `SUPERVISOR` roles; the hand-built SystemGraphService and PermissionManager channels in `service_manager/bootstrap.rs`; in `service_manager.rs`, the standing loop's fixed `admin`/`stats` wait set, `plan_relaunchable` and `all_settled` (the test behind `online`); the `supervisor` interface in `observability.lsidl`; `MAX_CATALOGUE_CLIENTS` (32) in the system-manifest crate and in DeviceManager; the catalogue demand computed from `manifest.toml` (17 minting roles, 9 of them in transparent services, plus DeviceManager's own, so 27); and the match-rule validator with its tests. External sources: QEMU v10.0.0 `ipmi_bmc_sim.c` (`sel_add_event` and `clear_sel` both advance the reservation, `clear_sel` checks it first, `reserve_sel` returns the current one, `get_device_guid` answers 0xC1 for an unset UUID, `guid` is `DEFINE_PROP_UUID_NODEFAULT`), `include/hw/qdev-properties-system.h` (only `DEFINE_PROP_UUID` sets a default), `smbios_type_38.c`, `smbus_ipmi.c`, `hw/acpi/ipmi.c`, `smbus_ich9.c` (class 0x0C05) and `pc.c`/`runstate.c` (a wakeup from S3 resets the devices); and the IPMI 2.0 rev 1.1 specification (Table 20-1, sections 31.2, 31.4, 31.4.2 and 31.9, and Appendix C1). Plans: P02M0188, P02M0191, P02M0195, P02M0196, P02M0197, P02M0198, P02M0200 and P02M0181. All six findings are correct, and all six are ACCEPTED. Where the auditor offered two corrections, I took the one that leaves landed code and neighbouring plans unchanged: a fixed payload rather than admitting an empty one, no SSIF watchdog rather than a new P02M0197 ordering rule, and a plan-delivered supervisor channel rather than a new trigger.

1. **ACCEPTED - The AdminService extension against the landed admin path, and SEL records nobody saw.** I confirmed all three landed rules. `admin-request.request` takes a payload handle, and AdminService's `prepare` refuses a request without one. `Operations::prepare` refuses an empty payload. `check_prepared` makes a preparation with other parameters a `Mismatch`, which the broker declines. As written, the executor read the count itself and the tool never sent it, so every SEL-clear preparation would have been declined. I also confirmed IPMI's reserve-check-clear: in section 31.4 an added event cancels the reservation and so prevents Clear SEL, and section 31.4.2 also cancels it on a clear, a reset or a new Reserve SEL. Reserve SEL is optional (O[1]), and Get SEL Info's operation byte reports whether it is supported. QEMU implements the cancellation. A reservation taken at execution protects nothing, and AdminService keeps one executor per action. Plan changes:
   - A new ONE BMC PER MACHINE IS ADMINISTRABLE sub-item in the AdminService extension: the first executor published for each action is kept, so both actions reach the BMC whose binding published first, and a request naming another BMC is refused at preparation. EXCLUDES gains a second administrable BMC.
   - The executor sub-item is replaced by WHAT A REQUEST CARRIES. The target is `bmc:` and the BMC's identity. The parameters are the entry count the `bmc` tool read through the BMC service (two bytes), or the one-byte chassis operation. The payload is fixed: the target selector's own bytes, refused by the executor if different and shown on the screen by its length and digest like every payload. All three landed rules stay as they are.
   - A new SEL CLEAR ERASES ONLY WHAT WAS COUNTED sub-item. At preparation the executor refuses a count that is not the live one and takes Reserve SEL. At execution it clears under that reservation, so an event added in between makes the clear fail with nothing erased and the outcome `failed`. A BMC without Reserve SEL has its clear refused at preparation. The erasure is polled through Get SEL Info, which answers 0x81 while erasing and needs no reservation, since the clear cancels its own.
   - The screen's SEL line adds "an event that arrives first cancels it", and every line adds "the payload is the target's name". The host tests add the count, payload and reservation rules.
   - The SEL item reads the count afresh for the tool and says a clear request carries it, and the tool's `sel clear` asks with the count it has just read.
   - Gates: SEL clear and every chassis case run in single-BMC boots, and the SEL clear is requested once the boot event is in the log. The harness BMC cancels its reservation on every added record and adds a record at a step the gate names. A new harness case adds a record between preparation and approval and expects the clear to fail, the outcome `failed` in the journal, and every record, the new one included, still in the log.

2. **ACCEPTED - BMC identity when Get Device GUID is not answered.** I confirmed that Table 20-1 lists Get Device GUID as optional. QEMU v10.0.0 declares `guid` with `DEFINE_PROP_UUID_NODEFAULT`, which, unlike `DEFINE_PROP_UUID`, sets no default, and `get_device_guid` answers 0xC1 while the UUID is all zeros. The simulated-BMC gates passed no `guid=`. Plan changes:
   - WHAT QEMU OFFERS states the `guid=` fact.
   - The identity item defines THE BMC'S IDENTITY: the GUID; or, for a BMC that answers Get Device GUID with an error or with sixteen zero bytes, its manufacturer, product and device ID together with its binding (the PCI address or the `acpi:` path of the bound device), so two BMCs without a GUID are never taken for one. The identity names the BMC in the tool and in the administrative target: `bmc:` and 32 hex digits, or `bmc:` and the fallback, within the selector's 64 bytes. A longer fallback cannot be administered, and the tool says so.
   - The message layer's return rule compares that identity. The pair rule applies only between BMCs that answer a GUID. The system's own events are written once per BMC, a pair's through one of its interfaces.
   - Gates: each simulated BMC gets its own `guid=` unless a case says otherwise. In the five-interface boot the PCI BT's BMC gets none, is named by the fallback, and no pair is reported. The power-cycle case's BMC gets none, so its administrative target is the fallback. A new PAIR case gives two simulated BMCs one GUID, because QEMU cannot link one BMC to two interfaces, and expects the pair reported and the boot event in exactly one of the two logs.
   - The host suites add the identity rule.

3. **ACCEPTED - The drivers in P02M0197's suspend transaction.** I confirmed in P02M0197 that a sleep is refused while an Online binding has no `suspend-deadline`, and that the part implements the exchange only in the drivers the image ships when it lands. It suspends a consumer's binding before its provider, and suspends the watchdog's binding last and resumes it first. P02M0195 makes SSIF a child of the ICH9 binding, so an SSIF `watchdog` could meet neither order. I also found that P02M0197b's S3 restore covers only the configuration header (BARs, windows, command register, MSI-X), while HOSTC sits at offset 0x40. Plan changes:
   - A new SUSPEND AND RESUME item. Both drivers implement the exchange and declare `suspend-deadline`, carried by whichever of P02M0197 and this milestone lands second. `ipmi` finishes the transaction in flight (a stuck KCS one is aborted) and holds its queue. On resume it aborts a busy KCS interface and reads BT's capabilities and the BMC's identity again. Its watchdog step is P02M0200's: disarm, then re-arm first. `smbus_ich9` finishes its transfer and holds new ones. THE SSIF FORM PUBLISHES NO `watchdog`; the KCS, BT and PCI forms do.
   - The SSIF rule is carried into WHO HOLDS WHAT, the manifest item and the watchdog item.
   - In the ICH9 resolver row, the kernel writes HOSTC again after S3, beside P02M0197b's header restore.
   - A new gate case, carried by whichever lands second: suspend to idle and S3 on q35 with KCS and SSIF. Both drivers answer `SUSPENDED` and `RESUMED`, SSIF reads the device ID after S3, and the BMC timer armed before the sleep is armed again after it.
   - The watchdog gate runs on KCS with `watchdog.device` naming the BMC.

4. **ACCEPTED - The BMC service's supervisor channel and its catalogue slots.** I confirmed that the manifest's two `SUPERVISOR` roles are bare serve roots of PermissionManager and SystemGraphService, and that `bootstrap.rs` builds each channel by hand. The standing loop serves exactly those two (`stats`, `stats2`) beside the admin channels. A plan-relaunchable service gets back only what the plan delivers, and SystemGraphService registers only the components running before it starts. The catalogue bound is 32 in both the system-manifest crate and DeviceManager, a transparent service's minting role counts twice, and today's demand is 27. Plan changes:
   - The row is named among ServiceManager's plan-relaunchable services, and its roles name "the supervisor read channel below" instead of the supervisor's read root.
   - A new SUPERVISOR READ CHANNEL item: the existing `supervisor` interface of `liber:observability@1`, served from ServiceManager's standing loop on a channel the plan delivers as a role and mints again for a replacement, as P02M0200a delivers `supervisor-liveness`.
   - BOOT COMPLETED reads that channel: every service `ready` or `stopped`, the `online` test. The restart count in the instance's own row keeps the event to once per boot.
   - The row's catalogue role counts as two slots, so 27 becomes 29. Whichever of P02M0190, P02M0198, P02M0199, P02M0200, P02M0201 and P02M0202 first pushes the demand past 32 raises the bound to 64 in the system-manifest crate and in DeviceManager's table in the same change.
   - WHAT IT WAITS FOR adds P02M0200a's way of delivering a channel ServiceManager serves.

5. **ACCEPTED - The `smbus_ich9` match rule.** I confirmed that validator rule 5 refuses a vendor, product or address without a standard identity (the `vendor_alone` test), that rule 1 refuses `plain-pci` without a class, and that QEMU's function is class 0x0C05 with interface 0. Plan changes:
   - The manifest item declares `transport = "plain-pci", pci-class = 0x0C, pci-subclass = 0x05, pci-interface = 0x00`, narrowed by `pci-vendor = 0x8086, pci-product = 0x2930`, and states the reason.
   - `ipmi`'s two class rows are spelled out the same way (`plain-pci`, 0x0C/0x07, interface 0x01 or 0x02).
   - WHAT QEMU OFFERS gives the function's class.

6. **ACCEPTED - SMBIOS type 38 for SSIF.** I confirmed that Appendix C1 keeps the SSIF slave address left-justified (0010000b stored as 20h) and uses the base's bit 0 as the I/O-space flag, with the address's own bit 0 in the modifier. QEMU shifts the SSIF address left by one and sets bit 0 of an I/O base, while `I2cSerialBusV2` carries the 7-bit address. The PCI forms have no `get_fwinfo` and so get no record. Plan changes:
   - DISCOVERY now says SMBIOS type 38 is a cross-check, never a row. A record is attached to the `IPI0001` row whose interface and base agree, as that row's `smbios:38#n` match id. KCS and BT compare the base read as the specification lays it out. SSIF, whose record names no controller, compares the base shifted right by one with the `I2cSerialBusV2` address. A record that matches nothing, or disagrees, is reported and changes nothing: it is never merged into the node's resources and never refuses the node.
   - WHAT QEMU OFFERS states the encoding and that the PCI forms get no record.
   - Once P02M0196a's SMBIOS item has landed, the five-interface gate checks the match ids and that no disagreement is reported.

Coordinated changes: `smbus_ich9`'s row declares `i2c-bus` with `consumers = 8`. Its offered endpoint serves nothing and is closed at publication without being counted, as P02M0195 now rules for every `i2c-bus` row. I also restated the pair rule against P02M0200's watchdog service, which arms one `watchdog` and opens ONE provider per device name: a pair of register interfaces offers two `bmc` providers for one BMC timer, so the second is left unopened and reported, never disarmed, and nothing done through one interface stops what was armed through the other. The BMC service's pair report names the interface an operator may disable; nothing de-duplicates automatically, as before. P02M0200's BMC provider item now names the KCS and BT forms and never SSIF, matching this plan.

Re-check of the whole plan: I read the plan end to end after the edits.
- Complete: the substance of every finding is itemized. Each new behaviour has a gate case or a host-suite line. Every stated limit is in EXCLUDES or in the pair paragraph: one administrable BMC, the watchdog of a register-interface pair, and a BMC only SMBIOS describes.
- Correct: each QEMU fact comes from the v10.0.0 sources, and each system fact from the tree.
- Feasible: the executor keeps the landed seam (a non-empty payload, equal parameters, one executor per action). It uses only IPMI commands QEMU's simulator implements: Get SEL Info, Reserve SEL and Clear SEL. The channel and catalogue changes follow mechanisms P02M0200a delivers, and part b already waits for P02M0200a.
- Consistent within the plan: the target fits the 64-byte selector. The identity rule is applied the same way in the message layer, the pair rule, the tool, the target and the gates. The SSIF rule is stated in all four places that list publications.
- Consistent with the neighbours: the SUSPEND item agrees with P02M0197's order and P02M0200's driver step, the SMBIOS rule matches P02M0196a's attach rule, and the thermal sentence now names P02M0198's own `thermal-zone` provider kind.
- Unchanged: nothing is versioned. The owner decisions stand: the plan was created under P02M0196's approval, and installing Debian `openipmi` and `ipmitool` is asked when the part starts.

Only `docs/todo/P02M0201.md` was edited and this response appended; no source, test, script or other plan was changed, and nothing was built or booted.


AUDITOR'S RE-AUDIT OF PLAN P02M0201 (2026-09-26T15:50:59Z):

**Rating: 8/10.** The six findings of the last re-audit are corrected in substance, and the tree, QEMU and neighbouring-plan facts behind the corrections hold. Three small defects remain, all in text added this round: the boot event waits for state words the supervisor never sends, the sleep case cannot show the BMC's watchdog step, and the kernel's HOSTC write after S3 has no owner if P02M0197 lands second.

The complete history was read: the review, both planner responses and the re-audit of 04:01. Each of that re-audit's six findings was correct. The plan was read whole in the working tree and compared with the version that re-audit saw (`git show HEAD:docs/todo/P02M0201.md` and `git diff`). Source checked: `admin.lsidl`; AdminService's `prepare`, `adopt` and `action_of`; the executor seam `admin_operation.rs`; `admin_descriptor.rs` and the broker's decline path; PermissionManager's `admin_policy` and its `btctl` and `dfu` rows; ServiceManager's `all_settled`, `plan_relaunchable`, `relaunch_planned` and standing loop; the `supervisor` status handler in `service_manager/lifecycle.rs`; `observability.lsidl`; the catalogue bound in `system-manifest` and DeviceManager; DeviceManager's wait set and `publish_all`; the catalogue scope mask; the match-rule validator. The catalogue demand was recomputed from `manifest.toml` with a read-only script: 17 minting roles, 9 of them in transparent services, so 27 slots. External sources: QEMU v10.0.0 (`ipmi_bmc_sim.c`, `ipmi.c`, `smbios_type_38.c`, `hw/acpi/ipmi.c`, `smbus_ich9.c`, `pci.c`, `pc.c`, `runstate.c`), with `smbus_ich9.c`, `ipmi_bmc_sim.c` and the PCI device reset compared equal to v10.0.11; Linux v6.12 `i2c-i801.c`. Sibling plans read in the working tree: P02M0191, P02M0195, P02M0196, P02M0197, P02M0198, P02M0199 and P02M0200.

These corrections hold and are not repeated:
- Finding 1: the request carries the tool's count as its parameters and a fixed, non-empty payload, so the three landed rules stay as they are. The executor reserves at preparation, and QEMU's simulator cancels that reservation when a record is added and checks it in Clear SEL. One administrable BMC is stated and excluded, the admin gates run with one BMC, and the case with an event between preparation and approval is added.
- Finding 2: the fallback identity is defined and used the same way in the message layer, the pair rule, the tool, the target and the gates, and it fits the 64-byte target.
- Finding 3, in part: the exchange is declared for both drivers and carried by whichever milestone lands second. SSIF publishes no `watchdog`, and P02M0197's order and P02M0200's BMC item now agree with that. Findings 2 and 3 below are what is left.
- Finding 4, in part: the supervisor read channel is itemized after P02M0200a's `supervisor-liveness`. The demand of 27, and 29 with this row, is right, and a 64-slot table fits DeviceManager's 256-handle wait. Finding 1 below is what is left.
- Findings 5 and 6: the `smbus_ich9` rule passes the validator's five rules. The SMBIOS rule matches P02M0196a's attach rule and QEMU's encodings.

The coordinated statements are in the working tree: P02M0195's closed `i2c-bus` endpoint, P02M0200's BMC item without SSIF and its one-provider-per-name rule, and P02M0198's `thermal-zone` provider kind.

1. **Low - BOOT COMPLETED waits for a state word, `ready`, that the supervisor's answer never carries, and the plan describes that answer as holding managed services only.**

   The trigger is a status showing "every managed service in a healthy end state - `ready`, or `stopped`" ([BOOT COMPLETED](/data/yellow/libersystem/docs/todo/P02M0201.md:228)). The plan describes that status as "each managed service's state" ([channel item](/data/yellow/libersystem/docs/todo/P02M0201.md:187)). The answer is different:
   - It spells a ready service [`running`](/data/yellow/libersystem/src/user/services/core/src/service_manager/lifecycle.rs:99) and an absent one [`pending`](/data/yellow/libersystem/src/user/services/core/src/service_manager/lifecycle.rs:97). `ready` is only the word of the [IDL comment](/data/yellow/libersystem/src/idl/observability.lsidl:111).
   - After the managed services it appends the canary's row and one row per manifest driver, and these rows carry no wanted state ([the extra rows](/data/yellow/libersystem/src/user/services/core/src/service_manager/lifecycle.rs:158)). Every driver except six reads `pending` for the whole boot ([driver facts](/data/yellow/libersystem/src/user/services/core/src/service_manager.rs:834)), `ipmi` and `smbus_ich9` among them.
   - `all_settled`, the `online` test the plan names, looks only at the managed services, for `Ready` or `Stopped` ([test](/data/yellow/libersystem/src/user/services/core/src/service_manager.rs:1082)).

   Read as written, the condition never holds and no boot event is written. The cases that expect one cannot pass: the [pair](/data/yellow/libersystem/docs/todo/P02M0201.md:328), [KCS and SSIF](/data/yellow/libersystem/docs/todo/P02M0201.md:330) and the [reboot](/data/yellow/libersystem/docs/todo/P02M0201.md:333). This is an incorrect detail in the correction of the last re-audit's finding 4.

   **Correct the BOOT COMPLETED item** : the rows that carry a wanted state all read `running` or `stopped`. Those rows are the managed services; the canary's and the drivers' rows carry none. `running` and `stopped` are how the answer spells the two states `all_settled` accepts.

2. **Low - The sleep case's watchdog clause passes whatever `ipmi` does in its watchdog step.**

   The SUSPEND item gives `ipmi` P02M0200's step: disarm at suspend, and arm again when its binding resumes first ([SUSPEND item](/data/yellow/libersystem/docs/todo/P02M0201.md:162)). The only check is that the BMC timer, "armed before the sleep, is armed again after it" ([sleep case](/data/yellow/libersystem/docs/todo/P02M0201.md:352)). That holds without the step:
   - At the resume notice, P02M0200's service "restores the configured timeout on each and pets once" ([P02M0200](/data/yellow/libersystem/docs/todo/P02M0200.md:156)). So the timer is armed after the sleep whether the driver re-armed it or not.
   - A missed disarm shows nothing either. The announcement set the longest timeout ([P02M0200](/data/yellow/libersystem/docs/todo/P02M0200.md:153)), which for the BMC is 6553.5 s ([range](/data/yellow/libersystem/docs/todo/P02M0201.md:253)), and the case names no sleep that long.
   - P02M0200 proves the re-arm for its own devices with a hook that stops the resume between the binding's re-arm and the resume notice, "since a driver that disarms at suspend and never re-arms passes the first" ([P02M0200](/data/yellow/libersystem/docs/todo/P02M0200.md:285)). That case and [P02M0197's](/data/yellow/libersystem/docs/todo/P02M0197.md:355) read `-action watchdog=pause`, which [does not apply](/data/yellow/libersystem/docs/todo/P02M0201.md:30) to the BMC's expiry.

   So no case fails when `ipmi` skips the step. This is new in the correction of finding 3.

   **Correct the sleep case** : prove the re-arm with P02M0200's hook and this plan's BMC oracle. The resume is stopped after the BMC binding's re-arm, and the serial log then shows a second boot that reports the BMC's watchdog as the cause. Alternatively, drop the clause and state that the BMC's step is not shown.

3. **Low - The kernel's HOSTC write after S3 has no owner when P02M0197 lands second, and no gate would see it missing.**

   The resolver row writes HOSTC again "beside P02M0197b's restore of the function's header" ([row](/data/yellow/libersystem/docs/todo/P02M0201.md:80)). Only two things are carried by whichever milestone lands second: the drivers' exchange ([SUSPEND item](/data/yellow/libersystem/docs/todo/P02M0201.md:158)) and the [sleep case](/data/yellow/libersystem/docs/todo/P02M0201.md:349). If this milestone lands first, there is no restore to write beside. P02M0197 does not carry the write either:
   - P02M0197b restores the header - BARs, windows, command register, MSI-X ([P02M0197](/data/yellow/libersystem/docs/todo/P02M0197.md:180)) - and nothing at 0x40.
   - Its general rule covers only "the exchange in every driver the image ships" ([P02M0197](/data/yellow/libersystem/docs/todo/P02M0197.md:143)), and it names neither driver. Where a step belongs to another plan, the plans say so and say who owes it ([TPM](/data/yellow/libersystem/docs/todo/P02M0197.md:162), [P02M0200](/data/yellow/libersystem/docs/todo/P02M0200.md:286)).

   QEMU cannot show the gap:
   - Its ICH9 SMBus model zeroes HOSTC only at realize (https://github.com/qemu/qemu/blob/v10.0.0/hw/i2c/smbus_ich9.c#L102) and has no reset handler (https://github.com/qemu/qemu/blob/v10.0.0/hw/i2c/smbus_ich9.c#L121).
   - The wakeup's device reset (https://github.com/qemu/qemu/blob/v10.0.0/hw/i386/pc.c#L1739) clears the command register, the status and the BARs, not offset 0x40 (https://github.com/qemu/qemu/blob/v10.0.0/hw/pci/pci.c#L538).

   So SSIF's read after S3 passes on the header restore alone, with or without the write. The plan says "after S3 the kernel's HOSTC write (above) makes its BAR decode again" ([SUSPEND item](/data/yellow/libersystem/docs/todo/P02M0201.md:164)). That is true of hardware, where Linux sets HST_EN again at resume (https://github.com/torvalds/linux/blob/v6.12/drivers/i2c/busses/i2c-i801.c#L1842), but not of QEMU. This is new in the correction of finding 3.

   **Correct the resolver row** : carry the write after S3 with the exchange, by whichever of P02M0197 and this milestone lands second, and have the first to land record that it is owed. Say that QEMU keeps HOSTC through its wakeup reset, so the sleep case does not show the write.

Validation: this was a read-only inspection of the audit history, the plan and its uncommitted diff, the sibling plans named above as they stand in the working tree, and the source files named above. QEMU v10.0.0 and v10.0.11 sources and Linux v6.12 sources were fetched from their upstream repositories into a scratch directory. The catalogue demand was computed from `manifest.toml` with a read-only script. No plan, source or audit file was modified, and nothing was built, tested, benchmarked or booted.

PLANNER'S RESPONSE ON P02M0201 (2026-09-26T17:12:45Z):

Verified read-only: the `supervisor` status handler in `service_manager/lifecycle.rs` (`state_name` spells Ready `running` and Absent `pending`; after the managed services it appends the canary's row and one row per manifest driver, all with an empty wanted state); `all_settled`, which accepts Ready and Stopped; the IDL comment that still says `ready`; P02M0200's sleep step (the service's restore at the resume notice, the longest timeout at the announcement) and its hook-based sleep case; QEMU v10.0.0's ICH9 SMBus model (HOSTC set at realize, no reset handler) and the PCI device reset; Linux v6.12's `i2c-i801` resume. Summary: three findings, all accepted.

1. **ACCEPTED - BOOT COMPLETED waited for a state word the answer never carries.** Plan change: the trigger now names `all_settled` as the test, says the answer spells its two states `running` and `stopped`, says the canary's and the drivers' rows follow the managed services with no wanted state (a driver not bound reads `pending` all boot), and makes the condition EVERY ROW WITH A WANTED STATE reading `running` or `stopped`, the rows without one not read.

2. **ACCEPTED - the sleep case's watchdog clause passed whatever `ipmi` did.** I took a different proof from the one the finding proposes first. P02M0200's hook, a resume stopped after the re-arm with a second boot as the oracle, would show the re-arm only, and only after waiting out the 120 s bridge bound; a missed disarm stays invisible under the 6553.5 s the announcement sets. THE COMMANDS THE BMC RECEIVED show both halves in one run. Plan changes: in the sleep case the KCS interface's BMC is the harness BMC, with the watchdog policy on, `watchdog.device` naming the BMC and a configured timeout other than the bridge bound, and its record must hold, for each sleep and in this order, the announcement's longest timeout, the driver's DISARM (Set Watchdog Timer with "don't stop" clear), after the resume the driver's RE-ARM WITH THE BRIDGE BOUND, and only then the service's restore of the configured timeout - so a driver that skips either half fails. The harness BMC item now keeps that record of every command it received, in order, for a gate to read.

3. **ACCEPTED - the kernel's HOSTC write after S3 had no owner if P02M0197 landed second.** Plan changes: the resolver row now says the write after an S3 is CARRIED BY WHICHEVER OF P02M0197 AND THIS MILESTONE LANDS SECOND, the first to land recording that it is owed, and that P02M0197b's restore list holds every configuration register the kernel writes for a claim. It also says no gate shows it on QEMU, whose model keeps HOSTC through the wakeup's device reset, while hardware loses it - which is why Linux sets HST_EN again at resume. The SUSPEND item's sentence now reads "after S3 on hardware".

Coordinated changes: P02M0197b's restore rule names the configuration registers the kernel writes for a claim, P02M0201's HOSTC among them; P02M0200c's BMC case and this plan's BMC watchdog item and case use the initial-countdown oracle (P02M0200's response of this date).

Re-check of the whole plan: the boot event, the orderly-shutdown event, the watchdog provider, the SUSPEND exchange and the resolver rows now agree with ServiceManager's status as it is, with P02M0200 and with P02M0197; every sleep-related check can fail on the behaviour it names. The file is ASCII, cites no audit and keeps its `Status:` line.

Edited `docs/todo/P02M0201.md` (and P02M0197's restore list); no source was changed, and nothing was built or booted.
