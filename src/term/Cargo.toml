[package]
name = "term"
# The product version lives only in /product.conf; Cargo's optional package
# version is unused here and omitted.
edition = "2024"
description = "Graphics-free terminal model (L2): the cell grid, escape parser, and text serialization for LiberSystem"
