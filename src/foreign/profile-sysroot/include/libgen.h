/* EMPTY, AND KEPT. The pinned sources include <libgen.h> on a path this configuration compiles, and
   call neither `dirname` nor `basename` from it - the objects name no symbol for either. Removing
   the header would fail the include; declaring the functions would be surface nobody requires. */
#ifndef LIBERSYSTEM_LIBGEN_H
#define LIBERSYSTEM_LIBGEN_H
#endif
