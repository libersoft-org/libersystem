[package]
name = "abi"
# The product version lives only in /product.conf; Cargo's optional package
# version is unused here and omitted.
edition = "2024"
description = "Shared kernel/userspace ABI: syscall numbers, error codes, rights bits, package format"
