[package]
name = "abi"
# Source of truth for product metadata is /product.conf; this version tracks the
# kernel's (Cargo requires a literal here).
version = "0.0.1"
edition = "2021"
description = "Shared kernel/userspace ABI: syscall numbers, error codes, rights bits, package format"
