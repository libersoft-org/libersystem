[package]
name = "time-client-provider"
edition = "2024"
description = "ELF trampoline provider for concrete LiberSystem time channel clients"

[lib]
path = "src/lib.rs"

[features]
default = []
shared-image = ["time-proto/shared-image"]

[dependencies]
time-proto = { path = "../time-proto" }
