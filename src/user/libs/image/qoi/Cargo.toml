[package]
name = "qoi"
edition = "2024"
description = "Bounded no_std QOI decoder and encoder"

[dependencies]
pix = { path = "../pix" }
qoi-codec = { package = "qoi", version = "0.4.1", default-features = false, features = ["alloc"] }
