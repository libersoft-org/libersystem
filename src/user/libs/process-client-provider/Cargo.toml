[package]
name = "process-client-provider"
edition = "2024"
description = "ELF trampoline provider for concrete LiberSystem process channel clients"

[lib]
path = "src/lib.rs"

[features]
default = []
shared-image = ["process-proto/shared-image"]

[dependencies]
process-proto = { path = "../process-proto" }
