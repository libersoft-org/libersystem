[package]
name = "observability-client-provider"
edition = "2024"
description = "ELF trampoline provider for concrete LiberSystem observability channel clients"

[lib]
path = "src/lib.rs"

[features]
default = []
shared-image = ["observability-proto/shared-image"]

[dependencies]
observability-proto = { path = "../observability-proto" }
