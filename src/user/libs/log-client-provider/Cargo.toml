[package]
name = "log-client-provider"
edition = "2024"
description = "ELF trampoline provider for concrete LiberSystem log channel clients"

[lib]
path = "src/lib.rs"

[features]
default = []
shared-image = ["log-proto/shared-image"]

[dependencies]
log-proto = { path = "../log-proto" }
