// @generated by lsidl-gen from ../../idl/modem.lsidl (package liber:modem@1).
// Do not edit by hand; regenerate with `./gen.sh`.
//! LiberSystem modems - SIM, registration and the one data context this system supports, over four
//! separately granted authorities.
//!
//! FOUR AUTHORITIES, NOT ONE. OBSERVING a modem (`modem`) names no subscriber and changes nothing.
//! USING it for data (`modem-data`) activates and deactivates the context a policy allows, and nothing
//! else. Its SUBSCRIBER IDENTITY (`modem-identity`) and its MANAGEMENT - PIN, PUK (`modem-manage`) - are
//! authorities of their own, denied by default. Every connection but the first is minted by
//! PermissionManager through the private `modem-admin`, for one component, one modem and one SIM, and
//! lives only as long as that component does.
//!
//! NETWORKSERVICE OWNS IP. A context this service activates is installed as NetworkService's uplink
//! through a private link-admin endpoint; addresses, routes, DNS and traffic are NetworkService's, and no
//! client of this service is ever handed a packet channel or a network administration handle.
#![allow(dead_code, unused_imports, unused_variables, unused_mut, clippy::all)]

use crate::codec::{Handles, PROTOCOL_INFO_OP, Reader, Sink, SliceWriter, VecWriter};
use alloc::string::String;
use alloc::vec::Vec;
use core::fmt::Write as _;

pub use crate::generated::liber::base::v1::Error;

/// A modem as this service knows it: DeviceManager's publication identity, and the incarnation of this
/// service that issued the handle - so a handle from before a restart names nothing after it.
#[derive(Clone, Debug, PartialEq)]
pub struct ModemId {
	pub slot: u32,
	pub generation: u32,
	pub binding_generation: u64,
	pub incarnation: u64,
}

impl ModemId {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<ModemId> {
		let mut r = Reader::new(bytes);
		let value = ModemId::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<ModemId> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = ModemId::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u32(self.slot)?;
		w.u32(self.generation)?;
		w.u64(self.binding_generation)?;
		w.u64(self.incarnation)?;
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<ModemId> {
		let slot = r.u32()?;
		let generation = r.u32()?;
		let binding_generation = r.u64()?;
		let incarnation = r.u64()?;
		Some(ModemId { slot, generation, binding_generation, incarnation })
	}
}

/// The one context a modem carries, by the SIM and the activation it belongs to. A reused session
/// number is not the old context: the generation says which activation this is.
#[derive(Clone, Debug, PartialEq)]
pub struct ContextId {
	pub modem: ModemId,
	pub sim_generation: u64,
	pub context_generation: u64,
}

impl ContextId {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<ContextId> {
		let mut r = Reader::new(bytes);
		let value = ContextId::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<ContextId> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = ContextId::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		self.modem.write(w)?;
		w.u64(self.sim_generation)?;
		w.u64(self.context_generation)?;
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<ContextId> {
		let modem = ModemId::read(r)?;
		let sim_generation = r.u64()?;
		let context_generation = r.u64()?;
		Some(ContextId { modem, sim_generation, context_generation })
	}
}

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
#[repr(u8)]
pub enum SimState {
	Unknown = 0,
	Absent = 1,
	LockedPin = 2,
	LockedPuk = 3,
	Ready = 4,
	Blocked = 5,
}

impl SimState {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<SimState> {
		let mut r = Reader::new(bytes);
		let value = SimState::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<SimState> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = SimState::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u8(*self as u8)
	}
	pub fn read(r: &mut Reader) -> Option<SimState> {
		match r.u8()? {
			0 => Some(SimState::Unknown),
			1 => Some(SimState::Absent),
			2 => Some(SimState::LockedPin),
			3 => Some(SimState::LockedPuk),
			4 => Some(SimState::Ready),
			5 => Some(SimState::Blocked),
			_ => None,
		}
	}
}

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
#[repr(u8)]
pub enum Registration {
	Unknown = 0,
	NotRegistered = 1,
	Searching = 2,
	Home = 3,
	Roaming = 4,
	Denied = 5,
}

impl Registration {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<Registration> {
		let mut r = Reader::new(bytes);
		let value = Registration::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<Registration> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = Registration::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u8(*self as u8)
	}
	pub fn read(r: &mut Reader) -> Option<Registration> {
		match r.u8()? {
			0 => Some(Registration::Unknown),
			1 => Some(Registration::NotRegistered),
			2 => Some(Registration::Searching),
			3 => Some(Registration::Home),
			4 => Some(Registration::Roaming),
			5 => Some(Registration::Denied),
			_ => None,
		}
	}
}

/// Signal strength, when the modem gave a valid one.
#[derive(Clone, Debug, PartialEq)]
pub struct Signal {
	pub valid: bool,
	pub rssi_dbm: i32,
	pub quality: u8,
}

impl Signal {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<Signal> {
		let mut r = Reader::new(bytes);
		let value = Signal::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<Signal> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = Signal::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.boolean(self.valid)?;
		w.i32(self.rssi_dbm)?;
		w.u8(self.quality)?;
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<Signal> {
		let valid = r.boolean()?;
		let rssi_dbm = r.i32()?;
		let quality = r.u8()?;
		Some(Signal { valid, rssi_dbm, quality })
	}
}

/// A retry counter: known with its value, or unknown - never a guessed number. It says which SIM it
/// was read for and when, because a count from a previous SIM, or from an hour ago, is not this one.
#[derive(Clone, Debug, PartialEq)]
pub struct AttemptCount {
	pub known: bool,
	pub remaining: u8,
	pub sim_generation: u64,
	pub observed_ms: u64,
}

impl AttemptCount {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<AttemptCount> {
		let mut r = Reader::new(bytes);
		let value = AttemptCount::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<AttemptCount> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = AttemptCount::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.boolean(self.known)?;
		w.u8(self.remaining)?;
		w.u64(self.sim_generation)?;
		w.u64(self.observed_ms)?;
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<AttemptCount> {
		let known = r.boolean()?;
		let remaining = r.u8()?;
		let sim_generation = r.u64()?;
		let observed_ms = r.u64()?;
		Some(AttemptCount { known, remaining, sim_generation, observed_ms })
	}
}

#[derive(Clone, Debug, PartialEq)]
pub struct Attempts {
	pub pin: AttemptCount,
	pub puk: AttemptCount,
}

impl Attempts {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<Attempts> {
		let mut r = Reader::new(bytes);
		let value = Attempts::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<Attempts> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = Attempts::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		self.pin.write(w)?;
		self.puk.write(w)?;
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<Attempts> {
		let pin = AttemptCount::read(r)?;
		let puk = AttemptCount::read(r)?;
		Some(Attempts { pin, puk })
	}
}

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
#[repr(u8)]
pub enum ContextState {
	Inactive = 0,
	Activating = 1,
	Active = 2,
	Deactivating = 3,
	/// A state change may have reached the modem and was not confirmed: nothing is assumed, and the
	/// next conflicting operation reconciles first.
	Unknown = 4,
}

impl ContextState {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<ContextState> {
		let mut r = Reader::new(bytes);
		let value = ContextState::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<ContextState> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = ContextState::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u8(*self as u8)
	}
	pub fn read(r: &mut Reader) -> Option<ContextState> {
		match r.u8()? {
			0 => Some(ContextState::Inactive),
			1 => Some(ContextState::Activating),
			2 => Some(ContextState::Active),
			3 => Some(ContextState::Deactivating),
			4 => Some(ContextState::Unknown),
			_ => None,
		}
	}
}

#[derive(Clone, Debug, PartialEq)]
pub struct ContextStatus {
	pub id: ContextId,
	pub state: ContextState,
	/// The IPv4 address, prefix and MTU NetworkService installed, when active.
	pub address: Option<u32>,
	pub prefix: u8,
	pub mtu: u16,
	/// Datagrams toward the network refused because the provider's queue was full, and datagrams from
	/// it dropped because NetworkService's was: each direction holds at most 64 packets and 256 kB, and
	/// what does not fit is counted here rather than queued without bound.
	pub transmit_refused: u64,
	pub receive_dropped: u64,
}

impl ContextStatus {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<ContextStatus> {
		let mut r = Reader::new(bytes);
		let value = ContextStatus::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<ContextStatus> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = ContextStatus::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		self.id.write(w)?;
		self.state.write(w)?;
		match &self.address {
			Some(v0) => {
				w.u8(1)?;
				w.u32(*v0)?;
			}
			None => {
				w.u8(0)?;
			}
		}
		w.u8(self.prefix)?;
		w.u16(self.mtu)?;
		w.u64(self.transmit_refused)?;
		w.u64(self.receive_dropped)?;
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<ContextStatus> {
		let id = ContextId::read(r)?;
		let state = ContextState::read(r)?;
		let address = if r.tag()? { Some(r.u32()?) } else { None };
		let prefix = r.u8()?;
		let mtu = r.u16()?;
		let transmit_refused = r.u64()?;
		let receive_dropped = r.u64()?;
		Some(ContextStatus { id, state, address, prefix, mtu, transmit_refused, receive_dropped })
	}
}

/// A modem, observed. NO SUBSCRIBER IDENTIFIER appears here: IMSI, ICCID and number are
/// `modem-identity`'s alone.
#[derive(Clone, Debug, PartialEq)]
pub struct ModemStatus {
	pub id: ModemId,
	pub manufacturer: String,
	pub model: String,
	pub sim: SimState,
	pub sim_generation: u64,
	pub registration: Registration,
	pub operator: Option<String>,
	pub signal: Signal,
	pub attempts: Attempts,
	pub context: Option<ContextStatus>,
	pub revision: u64,
}

impl ModemStatus {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<ModemStatus> {
		let mut r = Reader::new(bytes);
		let value = ModemStatus::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<ModemStatus> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = ModemStatus::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		self.id.write(w)?;
		w.bytes_lp(self.manufacturer.as_bytes())?;
		w.bytes_lp(self.model.as_bytes())?;
		self.sim.write(w)?;
		w.u64(self.sim_generation)?;
		self.registration.write(w)?;
		match &self.operator {
			Some(v1) => {
				w.u8(1)?;
				w.bytes_lp(v1.as_bytes())?;
			}
			None => {
				w.u8(0)?;
			}
		}
		self.signal.write(w)?;
		self.attempts.write(w)?;
		match &self.context {
			Some(v2) => {
				w.u8(1)?;
				v2.write(w)?;
			}
			None => {
				w.u8(0)?;
			}
		}
		w.u64(self.revision)?;
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<ModemStatus> {
		let id = ModemId::read(r)?;
		let manufacturer = {
			let v3 = r.string_lp()?;
			(v3.len() <= 32).then_some(v3)?
		};
		let model = {
			let v4 = r.string_lp()?;
			(v4.len() <= 32).then_some(v4)?
		};
		let sim = SimState::read(r)?;
		let sim_generation = r.u64()?;
		let registration = Registration::read(r)?;
		let operator = if r.tag()? {
			Some({
				let v5 = r.string_lp()?;
				(v5.len() <= 32).then_some(v5)?
			})
		} else {
			None
		};
		let signal = Signal::read(r)?;
		let attempts = Attempts::read(r)?;
		let context = if r.tag()? { Some(ContextStatus::read(r)?) } else { None };
		let revision = r.u64()?;
		Some(ModemStatus { id, manufacturer, model, sim, sim_generation, registration, operator, signal, attempts, context, revision })
	}
}

/// What this service admits, and how much of it is in use.
#[derive(Clone, Debug, PartialEq)]
pub struct ModemLimits {
	pub providers: u8,
	pub providers_max: u8,
	pub clients: u8,
	pub clients_max: u8,
	pub contexts: u8,
	pub contexts_max: u8,
}

impl ModemLimits {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<ModemLimits> {
		let mut r = Reader::new(bytes);
		let value = ModemLimits::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<ModemLimits> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = ModemLimits::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u8(self.providers)?;
		w.u8(self.providers_max)?;
		w.u8(self.clients)?;
		w.u8(self.clients_max)?;
		w.u8(self.contexts)?;
		w.u8(self.contexts_max)?;
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<ModemLimits> {
		let providers = r.u8()?;
		let providers_max = r.u8()?;
		let clients = r.u8()?;
		let clients_max = r.u8()?;
		let contexts = r.u8()?;
		let contexts_max = r.u8()?;
		Some(ModemLimits { providers, providers_max, clients, clients_max, contexts, contexts_max })
	}
}

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
#[repr(u8)]
pub enum WatchKind {
	/// The newest state of every modem.
	Snapshot = 1,
	/// The same, after transitions were coalesced away: a watcher is told it did not see every one.
	Resync = 2,
}

impl WatchKind {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<WatchKind> {
		let mut r = Reader::new(bytes);
		let value = WatchKind::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<WatchKind> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = WatchKind::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u8(*self as u8)
	}
	pub fn read(r: &mut Reader) -> Option<WatchKind> {
		match r.u8()? {
			1 => Some(WatchKind::Snapshot),
			2 => Some(WatchKind::Resync),
			_ => None,
		}
	}
}

#[derive(Clone, Debug, PartialEq)]
pub struct ModemWatch {
	pub kind: WatchKind,
	pub modems: Vec<ModemStatus>,
}

impl ModemWatch {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<ModemWatch> {
		let mut r = Reader::new(bytes);
		let value = ModemWatch::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<ModemWatch> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = ModemWatch::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		self.kind.write(w)?;
		if self.modems.len() > u16::MAX as usize {
			return None;
		}
		w.u16(self.modems.len() as u16)?;
		for v6 in self.modems.iter() {
			v6.write(w)?;
		}
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<ModemWatch> {
		let kind = WatchKind::read(r)?;
		let modems = {
			let v7 = r.u16()? as usize;
			let v7 = (v7 <= 4).then_some(v7)?;
			let mut v8 = Vec::new();
			v8.try_reserve_exact(v7).ok()?;
			for _ in 0..v7 {
				v8.push(ModemStatus::read(r)?);
			}
			v8
		};
		Some(ModemWatch { kind, modems })
	}
}

/// OBSERVATION: every modem, its limits, and a watch that coalesces to the newest snapshot.
// interface `modem` over a channel: opcodes, a Service trait + dispatch, and a Client.
pub mod modem {
	use super::*;
	use crate::codec::{Reader, Sink, SliceWriter, Transport, TransportError, VecWriter};
	use alloc::vec::Vec;

	pub const OP_MODEMS: u16 = 1;
	pub const OP_LIMITS: u16 = 2;
	pub const OP_WATCH: u16 = 3;

	pub trait Service {
		fn modems(&mut self) -> Result<Vec<ModemStatus>, Error>;
		fn limits(&mut self) -> Result<ModemLimits, Error>;
		fn watch(&mut self) -> Result<Vec<ModemWatch>, Error>;
	}

	pub fn dispatch<S: Service>(service: &mut S, request: &[u8], request_handles: &mut Handles, out: &mut [u8], reply_handles: &mut Handles) -> Option<usize> {
		let mut reader = Reader::with_handle_list(request, request_handles);
		let r = &mut reader;
		let op = r.u16()?;
		let corr = r.u32()?;
		let mut writer = SliceWriter::new(out);
		if op == PROTOCOL_INFO_OP {
			r.finish()?;
			request_handles.clear();
			let w = &mut writer;
			w.u32(corr)?;
			w.bytes_lp(b"liber:modem")?;
			w.u32(1)?;
			match Handles::try_from_slice(writer.handles()) {
				Some(taken) => *reply_handles = taken,
				None => return None,
			}
			return Some(writer.pos());
		}
		match op {
			OP_MODEMS => {
				r.finish()?;
				request_handles.clear();
				let result = service.modems();
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v9) => {
							w.u8(1)?;
							if v9.len() > u16::MAX as usize {
								return None;
							}
							w.u16(v9.len() as u16)?;
							for v11 in v9.iter() {
								v11.write(w)?;
							}
						}
						Err(v10) => {
							w.u8(0)?;
							v10.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			OP_LIMITS => {
				r.finish()?;
				request_handles.clear();
				let result = service.limits();
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v12) => {
							w.u8(1)?;
							v12.write(w)?;
						}
						Err(v13) => {
							w.u8(0)?;
							v13.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			_ => return None,
		}
		match Handles::try_from_slice(writer.handles()) {
			Some(taken) => *reply_handles = taken,
			None => return None,
		}
		Some(writer.pos())
	}

	pub fn watch_open<S: Service>(service: &mut S, request: &[u8], request_handles: &mut Handles) -> Option<(u32, Result<Vec<ModemWatch>, Error>)> {
		let mut reader = Reader::with_handle_list(request, request_handles);
		let r = &mut reader;
		let _op = r.u16()?;
		let corr = r.u32()?;
		r.finish()?;
		request_handles.clear();
		let items = service.watch();
		Some((corr, items))
	}
	pub fn watch_reply_ok(corr: u32, out: &mut [u8]) -> Option<usize> {
		let mut writer = SliceWriter::new(out);
		let w = &mut writer;
		w.u32(corr)?;
		w.u8(1)?;
		w.u32(0)?;
		writer.finish()
	}
	pub fn watch_reply_err(corr: u32, error: &Error, out: &mut [u8]) -> Option<usize> {
		let mut writer = SliceWriter::new(out);
		let w = &mut writer;
		w.u32(corr)?;
		w.u8(0)?;
		error.write(w)?;
		writer.finish()
	}
	pub fn watch_frame(seq: u32, item: &ModemWatch, out: &mut [u8], frame_handles: &mut Handles) -> Option<usize> {
		let mut writer = SliceWriter::new(out);
		let encoded: Option<()> = (|| {
			let w = &mut writer;
			w.u32(seq)?;
			item.write(w)?;
			Some(())
		})();
		if encoded.is_none() {
			if let Some(taken) = Handles::try_from_slice(writer.handles()) {
				*frame_handles = taken;
			}
			return None;
		}
		*frame_handles = Handles::try_from_slice(writer.handles())?;
		Some(writer.pos())
	}
	pub fn watch_read(msg: &[u8], frame_handles: &mut Handles) -> Option<ModemWatch> {
		let mut reader = Reader::with_handles(msg, frame_handles);
		let r = &mut reader;
		let _seq = r.u32()?;
		let value = ModemWatch::read(r)?;
		reader.finish()?;
		frame_handles.clear();
		Some(value)
	}

	fn transport_outcome(error: TransportError) -> Error {
		match error {
			// The request never left this process, so nothing happened and trying
			// again is safe - which is what `again` says.
			TransportError::SendRefused | TransportError::NoRoute => Error::Again,
			// It went out and no answer came back. The server may have acted before
			// it died or before the deadline; nobody knows, and `commit-uncertain` is
			// the answer `base.error` grew so a caller is not forced to guess.
			// The reply could not be held, or arrived and broke the framing rules. In
			// both the server ANSWERED, so it acted; this end simply cannot read what
			// it said, which is the same position as never hearing back.
			TransportError::PeerClosed | TransportError::ReceiveFailed | TransportError::TimedOut | TransportError::NoMemory | TransportError::Malformed => Error::CommitUncertain,
		}
	}

	pub struct Client<T: Transport> {
		transport: T,
		corr: u32,
		deadline: u64,
		last_error: Option<TransportError>,
	}

	impl<T: Transport> Client<T> {
		pub fn new(transport: T) -> Client<T> {
			Client { transport, corr: 0, deadline: 0, last_error: None }
		}
		pub fn with_deadline(transport: T, deadline: u64) -> Client<T> {
			Client { transport, corr: 0, deadline, last_error: None }
		}
		pub fn set_deadline(&mut self, deadline: u64) {
			self.deadline = deadline;
		}
		pub fn last_error(&self) -> Option<TransportError> {
			self.last_error
		}
		pub fn into_transport(self) -> T {
			self.transport
		}
		fn next_corr(&mut self) -> u32 {
			let c = self.corr;
			self.corr = self.corr.wrapping_add(1);
			c
		}
		pub fn protocol_info(&mut self) -> Option<(String, u32)> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(PROTOCOL_INFO_OP)?;
			w.u32(corr)?;
			// No parameter, so no capability: `into_inner` says so rather than this
			// line assuming it.
			let request = writer.into_inner()?;
			let mut reply_handles = Handles::new();
			let reply = self
				.transport
				.call(&request, &[], &mut reply_handles, self.deadline)
				.map_err(|e| {
					self.last_error = Some(e);
					e
				})
				.ok()?;
			if !reply_handles.is_empty() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			let mut reader = Reader::new(&reply);
			let r = &mut reader;
			if r.u32()? != corr {
				return None;
			}
			let package = r.string_lp()?;
			let version = r.u32()?;
			r.finish()?;
			Some((package, version))
		}
		pub fn modems(&mut self) -> Option<Result<Vec<ModemStatus>, Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_MODEMS)?;
			w.u32(corr)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? {
					Ok({
						let v14 = r.u16()? as usize;
						let mut v15 = Vec::new();
						v15.try_reserve_exact(v14).ok()?;
						for _ in 0..v14 {
							v15.push(ModemStatus::read(r)?);
						}
						v15
					})
				} else {
					Err(Error::read(r)?)
				};
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
		pub fn limits(&mut self) -> Option<Result<ModemLimits, Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_LIMITS)?;
			w.u32(corr)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? { Ok(ModemLimits::read(r)?) } else { Err(Error::read(r)?) };
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
		pub fn watch(&mut self) -> Option<Result<u64, Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_WATCH)?;
			w.u32(corr)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::new(&reply);
			let r = &mut reader;
			let decoded = (|| {
				if r.u32()? != corr {
					return None;
				}
				if r.tag()? {
					let _ = r.u32()?;
					r.finish()?;
					if reply_handles.len() != 1 {
						return None;
					}
					return Some(Ok(reply_handles.first()));
				}
				if !reply_handles.is_empty() {
					return None;
				}
				let error = Error::read(r)?;
				r.finish()?;
				Some(Err(error))
			})();
			if !matches!(decoded, Some(Ok(_))) {
				self.transport.discard_handles(reply_handles.as_slice());
			}
			decoded
		}
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_modem_modem_modems")]
	fn channel_invoke_modems(chan: u64) -> Option<Result<Vec<ModemStatus>, Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.modems()
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_modem_modem_limits")]
	fn channel_invoke_limits(chan: u64) -> Option<Result<ModemLimits, Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.limits()
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_modem_modem_watch")]
	fn channel_invoke_watch(chan: u64) -> Option<Result<u64, Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.watch()
	}
}

/// DATA: the context the grant's policy allows, on the modem and SIM it was granted for.
// interface `modem-data` over a channel: opcodes, a Service trait + dispatch, and a Client.
pub mod modem_data {
	use super::*;
	use crate::codec::{Reader, Sink, SliceWriter, Transport, TransportError, VecWriter};
	use alloc::vec::Vec;

	pub const OP_ACTIVATE: u16 = 1;
	pub const OP_DEACTIVATE: u16 = 2;
	pub const OP_STATUS: u16 = 3;

	pub trait Service {
		fn activate(&mut self, modem: ModemId) -> Result<ContextStatus, Error>;
		fn deactivate(&mut self, context: ContextId) -> Result<(), Error>;
		fn status(&mut self) -> Result<ModemStatus, Error>;
	}

	pub fn dispatch<S: Service>(service: &mut S, request: &[u8], request_handles: &mut Handles, out: &mut [u8], reply_handles: &mut Handles) -> Option<usize> {
		let mut reader = Reader::with_handle_list(request, request_handles);
		let r = &mut reader;
		let op = r.u16()?;
		let corr = r.u32()?;
		let mut writer = SliceWriter::new(out);
		if op == PROTOCOL_INFO_OP {
			r.finish()?;
			request_handles.clear();
			let w = &mut writer;
			w.u32(corr)?;
			w.bytes_lp(b"liber:modem")?;
			w.u32(1)?;
			match Handles::try_from_slice(writer.handles()) {
				Some(taken) => *reply_handles = taken,
				None => return None,
			}
			return Some(writer.pos());
		}
		match op {
			OP_ACTIVATE => {
				let modem = ModemId::read(r)?;
				r.finish()?;
				request_handles.clear();
				let result = service.activate(modem);
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v16) => {
							w.u8(1)?;
							v16.write(w)?;
						}
						Err(v17) => {
							w.u8(0)?;
							v17.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			OP_DEACTIVATE => {
				let context = ContextId::read(r)?;
				r.finish()?;
				request_handles.clear();
				let result = service.deactivate(context);
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v18) => {
							w.u8(1)?;
						}
						Err(v19) => {
							w.u8(0)?;
							v19.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			OP_STATUS => {
				r.finish()?;
				request_handles.clear();
				let result = service.status();
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v20) => {
							w.u8(1)?;
							v20.write(w)?;
						}
						Err(v21) => {
							w.u8(0)?;
							v21.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			_ => return None,
		}
		match Handles::try_from_slice(writer.handles()) {
			Some(taken) => *reply_handles = taken,
			None => return None,
		}
		Some(writer.pos())
	}

	fn transport_outcome(error: TransportError) -> Error {
		match error {
			// The request never left this process, so nothing happened and trying
			// again is safe - which is what `again` says.
			TransportError::SendRefused | TransportError::NoRoute => Error::Again,
			// It went out and no answer came back. The server may have acted before
			// it died or before the deadline; nobody knows, and `commit-uncertain` is
			// the answer `base.error` grew so a caller is not forced to guess.
			// The reply could not be held, or arrived and broke the framing rules. In
			// both the server ANSWERED, so it acted; this end simply cannot read what
			// it said, which is the same position as never hearing back.
			TransportError::PeerClosed | TransportError::ReceiveFailed | TransportError::TimedOut | TransportError::NoMemory | TransportError::Malformed => Error::CommitUncertain,
		}
	}

	pub struct Client<T: Transport> {
		transport: T,
		corr: u32,
		deadline: u64,
		last_error: Option<TransportError>,
	}

	impl<T: Transport> Client<T> {
		pub fn new(transport: T) -> Client<T> {
			Client { transport, corr: 0, deadline: 0, last_error: None }
		}
		pub fn with_deadline(transport: T, deadline: u64) -> Client<T> {
			Client { transport, corr: 0, deadline, last_error: None }
		}
		pub fn set_deadline(&mut self, deadline: u64) {
			self.deadline = deadline;
		}
		pub fn last_error(&self) -> Option<TransportError> {
			self.last_error
		}
		pub fn into_transport(self) -> T {
			self.transport
		}
		fn next_corr(&mut self) -> u32 {
			let c = self.corr;
			self.corr = self.corr.wrapping_add(1);
			c
		}
		pub fn protocol_info(&mut self) -> Option<(String, u32)> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(PROTOCOL_INFO_OP)?;
			w.u32(corr)?;
			// No parameter, so no capability: `into_inner` says so rather than this
			// line assuming it.
			let request = writer.into_inner()?;
			let mut reply_handles = Handles::new();
			let reply = self
				.transport
				.call(&request, &[], &mut reply_handles, self.deadline)
				.map_err(|e| {
					self.last_error = Some(e);
					e
				})
				.ok()?;
			if !reply_handles.is_empty() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			let mut reader = Reader::new(&reply);
			let r = &mut reader;
			if r.u32()? != corr {
				return None;
			}
			let package = r.string_lp()?;
			let version = r.u32()?;
			r.finish()?;
			Some((package, version))
		}
		pub fn activate(&mut self, modem: &ModemId) -> Option<Result<ContextStatus, Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_ACTIVATE)?;
			w.u32(corr)?;
			modem.write(w)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? { Ok(ContextStatus::read(r)?) } else { Err(Error::read(r)?) };
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
		pub fn deactivate(&mut self, context: &ContextId) -> Option<Result<(), Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_DEACTIVATE)?;
			w.u32(corr)?;
			context.write(w)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? { Ok(()) } else { Err(Error::read(r)?) };
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
		pub fn status(&mut self) -> Option<Result<ModemStatus, Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_STATUS)?;
			w.u32(corr)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? { Ok(ModemStatus::read(r)?) } else { Err(Error::read(r)?) };
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_modem_modem_data_activate")]
	fn channel_invoke_activate(chan: u64, modem: &ModemId) -> Option<Result<ContextStatus, Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.activate(modem)
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_modem_modem_data_deactivate")]
	fn channel_invoke_deactivate(chan: u64, context: &ContextId) -> Option<Result<(), Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.deactivate(context)
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_modem_modem_data_status")]
	fn channel_invoke_status(chan: u64) -> Option<Result<ModemStatus, Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.status()
	}
}

#[derive(Clone, Debug, PartialEq)]
pub struct Identity {
	pub imsi: Option<String>,
	pub iccid: Option<String>,
	pub msisdn: Option<String>,
}

impl Identity {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<Identity> {
		let mut r = Reader::new(bytes);
		let value = Identity::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<Identity> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = Identity::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		match &self.imsi {
			Some(v22) => {
				w.u8(1)?;
				w.bytes_lp(v22.as_bytes())?;
			}
			None => {
				w.u8(0)?;
			}
		}
		match &self.iccid {
			Some(v23) => {
				w.u8(1)?;
				w.bytes_lp(v23.as_bytes())?;
			}
			None => {
				w.u8(0)?;
			}
		}
		match &self.msisdn {
			Some(v24) => {
				w.u8(1)?;
				w.bytes_lp(v24.as_bytes())?;
			}
			None => {
				w.u8(0)?;
			}
		}
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<Identity> {
		let imsi = if r.tag()? {
			Some({
				let v25 = r.string_lp()?;
				(v25.len() <= 16).then_some(v25)?
			})
		} else {
			None
		};
		let iccid = if r.tag()? {
			Some({
				let v26 = r.string_lp()?;
				(v26.len() <= 22).then_some(v26)?
			})
		} else {
			None
		};
		let msisdn = if r.tag()? {
			Some({
				let v27 = r.string_lp()?;
				(v27.len() <= 16).then_some(v27)?
			})
		} else {
			None
		};
		Some(Identity { imsi, iccid, msisdn })
	}
}

/// THE SUBSCRIBER'S IDENTITY, and nothing else.
// interface `modem-identity` over a channel: opcodes, a Service trait + dispatch, and a Client.
pub mod modem_identity {
	use super::*;
	use crate::codec::{Reader, Sink, SliceWriter, Transport, TransportError, VecWriter};
	use alloc::vec::Vec;

	pub const OP_IDENTITY: u16 = 1;

	pub trait Service {
		fn identity(&mut self, modem: ModemId) -> Result<Identity, Error>;
	}

	pub fn dispatch<S: Service>(service: &mut S, request: &[u8], request_handles: &mut Handles, out: &mut [u8], reply_handles: &mut Handles) -> Option<usize> {
		let mut reader = Reader::with_handle_list(request, request_handles);
		let r = &mut reader;
		let op = r.u16()?;
		let corr = r.u32()?;
		let mut writer = SliceWriter::new(out);
		if op == PROTOCOL_INFO_OP {
			r.finish()?;
			request_handles.clear();
			let w = &mut writer;
			w.u32(corr)?;
			w.bytes_lp(b"liber:modem")?;
			w.u32(1)?;
			match Handles::try_from_slice(writer.handles()) {
				Some(taken) => *reply_handles = taken,
				None => return None,
			}
			return Some(writer.pos());
		}
		match op {
			OP_IDENTITY => {
				let modem = ModemId::read(r)?;
				r.finish()?;
				request_handles.clear();
				let result = service.identity(modem);
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v28) => {
							w.u8(1)?;
							v28.write(w)?;
						}
						Err(v29) => {
							w.u8(0)?;
							v29.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			_ => return None,
		}
		match Handles::try_from_slice(writer.handles()) {
			Some(taken) => *reply_handles = taken,
			None => return None,
		}
		Some(writer.pos())
	}

	fn transport_outcome(error: TransportError) -> Error {
		match error {
			// The request never left this process, so nothing happened and trying
			// again is safe - which is what `again` says.
			TransportError::SendRefused | TransportError::NoRoute => Error::Again,
			// It went out and no answer came back. The server may have acted before
			// it died or before the deadline; nobody knows, and `commit-uncertain` is
			// the answer `base.error` grew so a caller is not forced to guess.
			// The reply could not be held, or arrived and broke the framing rules. In
			// both the server ANSWERED, so it acted; this end simply cannot read what
			// it said, which is the same position as never hearing back.
			TransportError::PeerClosed | TransportError::ReceiveFailed | TransportError::TimedOut | TransportError::NoMemory | TransportError::Malformed => Error::CommitUncertain,
		}
	}

	pub struct Client<T: Transport> {
		transport: T,
		corr: u32,
		deadline: u64,
		last_error: Option<TransportError>,
	}

	impl<T: Transport> Client<T> {
		pub fn new(transport: T) -> Client<T> {
			Client { transport, corr: 0, deadline: 0, last_error: None }
		}
		pub fn with_deadline(transport: T, deadline: u64) -> Client<T> {
			Client { transport, corr: 0, deadline, last_error: None }
		}
		pub fn set_deadline(&mut self, deadline: u64) {
			self.deadline = deadline;
		}
		pub fn last_error(&self) -> Option<TransportError> {
			self.last_error
		}
		pub fn into_transport(self) -> T {
			self.transport
		}
		fn next_corr(&mut self) -> u32 {
			let c = self.corr;
			self.corr = self.corr.wrapping_add(1);
			c
		}
		pub fn protocol_info(&mut self) -> Option<(String, u32)> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(PROTOCOL_INFO_OP)?;
			w.u32(corr)?;
			// No parameter, so no capability: `into_inner` says so rather than this
			// line assuming it.
			let request = writer.into_inner()?;
			let mut reply_handles = Handles::new();
			let reply = self
				.transport
				.call(&request, &[], &mut reply_handles, self.deadline)
				.map_err(|e| {
					self.last_error = Some(e);
					e
				})
				.ok()?;
			if !reply_handles.is_empty() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			let mut reader = Reader::new(&reply);
			let r = &mut reader;
			if r.u32()? != corr {
				return None;
			}
			let package = r.string_lp()?;
			let version = r.u32()?;
			r.finish()?;
			Some((package, version))
		}
		pub fn identity(&mut self, modem: &ModemId) -> Option<Result<Identity, Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_IDENTITY)?;
			w.u32(corr)?;
			modem.write(w)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? { Ok(Identity::read(r)?) } else { Err(Error::read(r)?) };
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_modem_modem_identity_identity")]
	fn channel_invoke_identity(chan: u64, modem: &ModemId) -> Option<Result<Identity, Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.identity(modem)
	}
}

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
#[repr(u8)]
pub enum SimPinResult {
	Accepted = 1,
	Rejected = 2,
	Blocked = 3,
	/// The command may have reached the SIM and its answer did not come: NOT RETRIED. The counters are
	/// read again before anything else is attempted.
	OutcomeUnknown = 4,
	/// The remaining count is unknown and the caller did not acknowledge attempting anyway.
	UnknownCount = 5,
}

impl SimPinResult {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<SimPinResult> {
		let mut r = Reader::new(bytes);
		let value = SimPinResult::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<SimPinResult> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = SimPinResult::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u8(*self as u8)
	}
	pub fn read(r: &mut Reader) -> Option<SimPinResult> {
		match r.u8()? {
			1 => Some(SimPinResult::Accepted),
			2 => Some(SimPinResult::Rejected),
			3 => Some(SimPinResult::Blocked),
			4 => Some(SimPinResult::OutcomeUnknown),
			5 => Some(SimPinResult::UnknownCount),
			_ => None,
		}
	}
}

#[derive(Clone, Debug, PartialEq)]
pub struct SimPinOutcome {
	pub result: SimPinResult,
	pub attempts: Attempts,
}

impl SimPinOutcome {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<SimPinOutcome> {
		let mut r = Reader::new(bytes);
		let value = SimPinOutcome::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<SimPinOutcome> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = SimPinOutcome::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		self.result.write(w)?;
		self.attempts.write(w)?;
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<SimPinOutcome> {
		let result = SimPinResult::read(r)?;
		let attempts = Attempts::read(r)?;
		Some(SimPinOutcome { result, attempts })
	}
}

/// MANAGEMENT: the counters, and PIN and PUK entry - one attempt per call, never replayed. A PIN reaches
/// the modem and nowhere else: no log, no state record and no reply carries it.
// interface `modem-manage` over a channel: opcodes, a Service trait + dispatch, and a Client.
pub mod modem_manage {
	use super::*;
	use crate::codec::{Reader, Sink, SliceWriter, Transport, TransportError, VecWriter};
	use alloc::vec::Vec;

	pub const OP_ATTEMPTS: u16 = 1;
	pub const OP_ENTER_PIN: u16 = 2;
	pub const OP_ENTER_PUK: u16 = 3;

	pub trait Service {
		fn attempts(&mut self, modem: ModemId) -> Result<Attempts, Error>;
		fn enter_pin(&mut self, modem: ModemId, pin: String, acknowledge_unknown: bool) -> Result<SimPinOutcome, Error>;
		fn enter_puk(&mut self, modem: ModemId, puk: String, new_pin: String, acknowledge_unknown: bool) -> Result<SimPinOutcome, Error>;
	}

	pub fn dispatch<S: Service>(service: &mut S, request: &[u8], request_handles: &mut Handles, out: &mut [u8], reply_handles: &mut Handles) -> Option<usize> {
		let mut reader = Reader::with_handle_list(request, request_handles);
		let r = &mut reader;
		let op = r.u16()?;
		let corr = r.u32()?;
		let mut writer = SliceWriter::new(out);
		if op == PROTOCOL_INFO_OP {
			r.finish()?;
			request_handles.clear();
			let w = &mut writer;
			w.u32(corr)?;
			w.bytes_lp(b"liber:modem")?;
			w.u32(1)?;
			match Handles::try_from_slice(writer.handles()) {
				Some(taken) => *reply_handles = taken,
				None => return None,
			}
			return Some(writer.pos());
		}
		match op {
			OP_ATTEMPTS => {
				let modem = ModemId::read(r)?;
				r.finish()?;
				request_handles.clear();
				let result = service.attempts(modem);
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v30) => {
							w.u8(1)?;
							v30.write(w)?;
						}
						Err(v31) => {
							w.u8(0)?;
							v31.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			OP_ENTER_PIN => {
				let modem = ModemId::read(r)?;
				let pin = {
					let v32 = r.string_lp()?;
					(v32.len() <= 8).then_some(v32)?
				};
				let acknowledge_unknown = r.boolean()?;
				r.finish()?;
				request_handles.clear();
				let result = service.enter_pin(modem, pin, acknowledge_unknown);
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v33) => {
							w.u8(1)?;
							v33.write(w)?;
						}
						Err(v34) => {
							w.u8(0)?;
							v34.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			OP_ENTER_PUK => {
				let modem = ModemId::read(r)?;
				let puk = {
					let v35 = r.string_lp()?;
					(v35.len() <= 8).then_some(v35)?
				};
				let new_pin = {
					let v36 = r.string_lp()?;
					(v36.len() <= 8).then_some(v36)?
				};
				let acknowledge_unknown = r.boolean()?;
				r.finish()?;
				request_handles.clear();
				let result = service.enter_puk(modem, puk, new_pin, acknowledge_unknown);
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v37) => {
							w.u8(1)?;
							v37.write(w)?;
						}
						Err(v38) => {
							w.u8(0)?;
							v38.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			_ => return None,
		}
		match Handles::try_from_slice(writer.handles()) {
			Some(taken) => *reply_handles = taken,
			None => return None,
		}
		Some(writer.pos())
	}

	fn transport_outcome(error: TransportError) -> Error {
		match error {
			// The request never left this process, so nothing happened and trying
			// again is safe - which is what `again` says.
			TransportError::SendRefused | TransportError::NoRoute => Error::Again,
			// It went out and no answer came back. The server may have acted before
			// it died or before the deadline; nobody knows, and `commit-uncertain` is
			// the answer `base.error` grew so a caller is not forced to guess.
			// The reply could not be held, or arrived and broke the framing rules. In
			// both the server ANSWERED, so it acted; this end simply cannot read what
			// it said, which is the same position as never hearing back.
			TransportError::PeerClosed | TransportError::ReceiveFailed | TransportError::TimedOut | TransportError::NoMemory | TransportError::Malformed => Error::CommitUncertain,
		}
	}

	pub struct Client<T: Transport> {
		transport: T,
		corr: u32,
		deadline: u64,
		last_error: Option<TransportError>,
	}

	impl<T: Transport> Client<T> {
		pub fn new(transport: T) -> Client<T> {
			Client { transport, corr: 0, deadline: 0, last_error: None }
		}
		pub fn with_deadline(transport: T, deadline: u64) -> Client<T> {
			Client { transport, corr: 0, deadline, last_error: None }
		}
		pub fn set_deadline(&mut self, deadline: u64) {
			self.deadline = deadline;
		}
		pub fn last_error(&self) -> Option<TransportError> {
			self.last_error
		}
		pub fn into_transport(self) -> T {
			self.transport
		}
		fn next_corr(&mut self) -> u32 {
			let c = self.corr;
			self.corr = self.corr.wrapping_add(1);
			c
		}
		pub fn protocol_info(&mut self) -> Option<(String, u32)> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(PROTOCOL_INFO_OP)?;
			w.u32(corr)?;
			// No parameter, so no capability: `into_inner` says so rather than this
			// line assuming it.
			let request = writer.into_inner()?;
			let mut reply_handles = Handles::new();
			let reply = self
				.transport
				.call(&request, &[], &mut reply_handles, self.deadline)
				.map_err(|e| {
					self.last_error = Some(e);
					e
				})
				.ok()?;
			if !reply_handles.is_empty() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			let mut reader = Reader::new(&reply);
			let r = &mut reader;
			if r.u32()? != corr {
				return None;
			}
			let package = r.string_lp()?;
			let version = r.u32()?;
			r.finish()?;
			Some((package, version))
		}
		pub fn attempts(&mut self, modem: &ModemId) -> Option<Result<Attempts, Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_ATTEMPTS)?;
			w.u32(corr)?;
			modem.write(w)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? { Ok(Attempts::read(r)?) } else { Err(Error::read(r)?) };
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
		pub fn enter_pin(&mut self, modem: &ModemId, pin: &str, acknowledge_unknown: &bool) -> Option<Result<SimPinOutcome, Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_ENTER_PIN)?;
			w.u32(corr)?;
			modem.write(w)?;
			w.bytes_lp(pin.as_bytes())?;
			w.boolean(*acknowledge_unknown)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? { Ok(SimPinOutcome::read(r)?) } else { Err(Error::read(r)?) };
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
		pub fn enter_puk(&mut self, modem: &ModemId, puk: &str, new_pin: &str, acknowledge_unknown: &bool) -> Option<Result<SimPinOutcome, Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_ENTER_PUK)?;
			w.u32(corr)?;
			modem.write(w)?;
			w.bytes_lp(puk.as_bytes())?;
			w.bytes_lp(new_pin.as_bytes())?;
			w.boolean(*acknowledge_unknown)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? { Ok(SimPinOutcome::read(r)?) } else { Err(Error::read(r)?) };
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_modem_modem_manage_attempts")]
	fn channel_invoke_attempts(chan: u64, modem: &ModemId) -> Option<Result<Attempts, Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.attempts(modem)
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_modem_modem_manage_enter_pin")]
	fn channel_invoke_enter_pin(chan: u64, modem: &ModemId, pin: &str, acknowledge_unknown: &bool) -> Option<Result<SimPinOutcome, Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.enter_pin(modem, pin, acknowledge_unknown)
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_modem_modem_manage_enter_puk")]
	fn channel_invoke_enter_puk(chan: u64, modem: &ModemId, puk: &str, new_pin: &str, acknowledge_unknown: &bool) -> Option<Result<SimPinOutcome, Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.enter_puk(modem, puk, new_pin, acknowledge_unknown)
	}
}

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
#[repr(u8)]
pub enum GrantKind {
	Data = 1,
	Identity = 2,
	Manage = 3,
}

impl GrantKind {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<GrantKind> {
		let mut r = Reader::new(bytes);
		let value = GrantKind::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<GrantKind> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = GrantKind::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u8(*self as u8)
	}
	pub fn read(r: &mut Reader) -> Option<GrantKind> {
		match r.u8()? {
			1 => Some(GrantKind::Data),
			2 => Some(GrantKind::Identity),
			3 => Some(GrantKind::Manage),
			_ => None,
		}
	}
}

/// What a data grant allows: the access point, and whether it may replace an uplink already selected.
#[derive(Clone, Debug, PartialEq)]
pub struct DataPolicy {
	pub apn: String,
	pub replace_uplink: bool,
}

impl DataPolicy {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<DataPolicy> {
		let mut r = Reader::new(bytes);
		let value = DataPolicy::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<DataPolicy> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = DataPolicy::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.bytes_lp(self.apn.as_bytes())?;
		w.boolean(self.replace_uplink)?;
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<DataPolicy> {
		let apn = {
			let v39 = r.string_lp()?;
			(v39.len() <= 64).then_some(v39)?
		};
		let replace_uplink = r.boolean()?;
		Some(DataPolicy { apn, replace_uplink })
	}
}

#[derive(Clone, Debug, PartialEq)]
pub struct ModemGrant {
	pub connection: u64,
	pub modem: ModemId,
	pub sim_generation: u64,
}

impl ModemGrant {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<ModemGrant> {
		let mut r = Reader::new(bytes);
		let value = ModemGrant::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<ModemGrant> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = ModemGrant::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.set_handle(self.connection)?;
		w.u32(0)?;
		self.modem.write(w)?;
		w.u64(self.sim_generation)?;
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<ModemGrant> {
		let connection = {
			let _ = r.u32()?;
			r.take_handle()?
		};
		let modem = ModemId::read(r)?;
		let sim_generation = r.u64()?;
		Some(ModemGrant { connection, modem, sim_generation })
	}
}

/// THE MINTING ENDPOINT, PermissionManager's alone. The alias names a modem the service's configuration
/// binds to provider metadata, and resolves to exactly one current publication or the mint fails. The
/// connection is bound to the modem and SIM current at minting, and lives as long as `owner` does.
// interface `modem-admin` over a channel: opcodes, a Service trait + dispatch, and a Client.
pub mod modem_admin {
	use super::*;
	use crate::codec::{Reader, Sink, SliceWriter, Transport, TransportError, VecWriter};
	use alloc::vec::Vec;

	pub const OP_MINT: u16 = 1;

	pub trait Service {
		fn mint(&mut self, kind: GrantKind, alias: String, policy: DataPolicy, owner: u64) -> Result<ModemGrant, Error>;
	}

	pub fn dispatch<S: Service>(service: &mut S, request: &[u8], request_handles: &mut Handles, out: &mut [u8], reply_handles: &mut Handles) -> Option<usize> {
		let mut reader = Reader::with_handle_list(request, request_handles);
		let r = &mut reader;
		let op = r.u16()?;
		let corr = r.u32()?;
		let mut writer = SliceWriter::new(out);
		if op == PROTOCOL_INFO_OP {
			r.finish()?;
			request_handles.clear();
			let w = &mut writer;
			w.u32(corr)?;
			w.bytes_lp(b"liber:modem")?;
			w.u32(1)?;
			match Handles::try_from_slice(writer.handles()) {
				Some(taken) => *reply_handles = taken,
				None => return None,
			}
			return Some(writer.pos());
		}
		match op {
			OP_MINT => {
				let kind = GrantKind::read(r)?;
				let alias = {
					let v40 = r.string_lp()?;
					(v40.len() <= 32).then_some(v40)?
				};
				let policy = DataPolicy::read(r)?;
				let owner = {
					let _ = r.u32()?;
					r.take_handle()?
				};
				r.finish()?;
				request_handles.clear();
				let result = service.mint(kind, alias, policy, owner);
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v41) => {
							w.u8(1)?;
							v41.write(w)?;
						}
						Err(v42) => {
							w.u8(0)?;
							v42.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			_ => return None,
		}
		match Handles::try_from_slice(writer.handles()) {
			Some(taken) => *reply_handles = taken,
			None => return None,
		}
		Some(writer.pos())
	}

	fn transport_outcome(error: TransportError) -> Error {
		match error {
			// The request never left this process, so nothing happened and trying
			// again is safe - which is what `again` says.
			TransportError::SendRefused | TransportError::NoRoute => Error::Again,
			// It went out and no answer came back. The server may have acted before
			// it died or before the deadline; nobody knows, and `commit-uncertain` is
			// the answer `base.error` grew so a caller is not forced to guess.
			// The reply could not be held, or arrived and broke the framing rules. In
			// both the server ANSWERED, so it acted; this end simply cannot read what
			// it said, which is the same position as never hearing back.
			TransportError::PeerClosed | TransportError::ReceiveFailed | TransportError::TimedOut | TransportError::NoMemory | TransportError::Malformed => Error::CommitUncertain,
		}
	}

	pub struct Client<T: Transport> {
		transport: T,
		corr: u32,
		deadline: u64,
		last_error: Option<TransportError>,
	}

	impl<T: Transport> Client<T> {
		pub fn new(transport: T) -> Client<T> {
			Client { transport, corr: 0, deadline: 0, last_error: None }
		}
		pub fn with_deadline(transport: T, deadline: u64) -> Client<T> {
			Client { transport, corr: 0, deadline, last_error: None }
		}
		pub fn set_deadline(&mut self, deadline: u64) {
			self.deadline = deadline;
		}
		pub fn last_error(&self) -> Option<TransportError> {
			self.last_error
		}
		pub fn into_transport(self) -> T {
			self.transport
		}
		fn next_corr(&mut self) -> u32 {
			let c = self.corr;
			self.corr = self.corr.wrapping_add(1);
			c
		}
		pub fn protocol_info(&mut self) -> Option<(String, u32)> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(PROTOCOL_INFO_OP)?;
			w.u32(corr)?;
			// No parameter, so no capability: `into_inner` says so rather than this
			// line assuming it.
			let request = writer.into_inner()?;
			let mut reply_handles = Handles::new();
			let reply = self
				.transport
				.call(&request, &[], &mut reply_handles, self.deadline)
				.map_err(|e| {
					self.last_error = Some(e);
					e
				})
				.ok()?;
			if !reply_handles.is_empty() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			let mut reader = Reader::new(&reply);
			let r = &mut reader;
			if r.u32()? != corr {
				return None;
			}
			let package = r.string_lp()?;
			let version = r.u32()?;
			r.finish()?;
			Some((package, version))
		}
		pub fn mint(&mut self, kind: &GrantKind, alias: &str, policy: &DataPolicy, owner: &u64) -> Option<Result<ModemGrant, Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_MINT)?;
			w.u32(corr)?;
			kind.write(w)?;
			w.bytes_lp(alias.as_bytes())?;
			policy.write(w)?;
			w.set_handle(*owner)?;
			w.u32(0)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? { Ok(ModemGrant::read(r)?) } else { Err(Error::read(r)?) };
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_modem_modem_admin_mint")]
	fn channel_invoke_mint(chan: u64, kind: &GrantKind, alias: &str, policy: &DataPolicy, owner: &u64) -> Option<Result<ModemGrant, Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.mint(kind, alias, policy, owner)
	}
}

impl ModemId {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"slot\":");
		let _ = write!(out, "{}", self.slot);
		out.push(',');
		out.push_str("\"generation\":");
		let _ = write!(out, "{}", self.generation);
		out.push(',');
		out.push_str("\"binding-generation\":");
		let _ = write!(out, "{}", self.binding_generation);
		out.push(',');
		out.push_str("\"incarnation\":");
		let _ = write!(out, "{}", self.incarnation);
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("slot=");
		let _ = write!(out, "{}", self.slot);
		out.push_str(", ");
		out.push_str("generation=");
		let _ = write!(out, "{}", self.generation);
		out.push_str(", ");
		out.push_str("binding-generation=");
		let _ = write!(out, "{}", self.binding_generation);
		out.push_str(", ");
		out.push_str("incarnation=");
		let _ = write!(out, "{}", self.incarnation);
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 4);
		crate::codec::cbor::text(out, "slot");
		crate::codec::cbor::uint(out, self.slot as u64);
		crate::codec::cbor::text(out, "generation");
		crate::codec::cbor::uint(out, self.generation as u64);
		crate::codec::cbor::text(out, "binding-generation");
		crate::codec::cbor::uint(out, self.binding_generation as u64);
		crate::codec::cbor::text(out, "incarnation");
		crate::codec::cbor::uint(out, self.incarnation as u64);
	}
}

impl ContextId {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"modem\":");
		self.modem.to_json_into(out);
		out.push(',');
		out.push_str("\"sim-generation\":");
		let _ = write!(out, "{}", self.sim_generation);
		out.push(',');
		out.push_str("\"context-generation\":");
		let _ = write!(out, "{}", self.context_generation);
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("modem=");
		self.modem.to_text_into(out);
		out.push_str(", ");
		out.push_str("sim-generation=");
		let _ = write!(out, "{}", self.sim_generation);
		out.push_str(", ");
		out.push_str("context-generation=");
		let _ = write!(out, "{}", self.context_generation);
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 3);
		crate::codec::cbor::text(out, "modem");
		self.modem.to_cbor_into(out);
		crate::codec::cbor::text(out, "sim-generation");
		crate::codec::cbor::uint(out, self.sim_generation as u64);
		crate::codec::cbor::text(out, "context-generation");
		crate::codec::cbor::uint(out, self.context_generation as u64);
	}
}

impl SimState {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		match self {
			SimState::Unknown => out.push_str("\"unknown\""),
			SimState::Absent => out.push_str("\"absent\""),
			SimState::LockedPin => out.push_str("\"locked-pin\""),
			SimState::LockedPuk => out.push_str("\"locked-puk\""),
			SimState::Ready => out.push_str("\"ready\""),
			SimState::Blocked => out.push_str("\"blocked\""),
		}
	}
	pub fn to_text_into(&self, out: &mut String) {
		match self {
			SimState::Unknown => out.push_str("unknown"),
			SimState::Absent => out.push_str("absent"),
			SimState::LockedPin => out.push_str("locked-pin"),
			SimState::LockedPuk => out.push_str("locked-puk"),
			SimState::Ready => out.push_str("ready"),
			SimState::Blocked => out.push_str("blocked"),
		}
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		match self {
			SimState::Unknown => crate::codec::cbor::text(out, "unknown"),
			SimState::Absent => crate::codec::cbor::text(out, "absent"),
			SimState::LockedPin => crate::codec::cbor::text(out, "locked-pin"),
			SimState::LockedPuk => crate::codec::cbor::text(out, "locked-puk"),
			SimState::Ready => crate::codec::cbor::text(out, "ready"),
			SimState::Blocked => crate::codec::cbor::text(out, "blocked"),
		}
	}
}

impl Registration {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		match self {
			Registration::Unknown => out.push_str("\"unknown\""),
			Registration::NotRegistered => out.push_str("\"not-registered\""),
			Registration::Searching => out.push_str("\"searching\""),
			Registration::Home => out.push_str("\"home\""),
			Registration::Roaming => out.push_str("\"roaming\""),
			Registration::Denied => out.push_str("\"denied\""),
		}
	}
	pub fn to_text_into(&self, out: &mut String) {
		match self {
			Registration::Unknown => out.push_str("unknown"),
			Registration::NotRegistered => out.push_str("not-registered"),
			Registration::Searching => out.push_str("searching"),
			Registration::Home => out.push_str("home"),
			Registration::Roaming => out.push_str("roaming"),
			Registration::Denied => out.push_str("denied"),
		}
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		match self {
			Registration::Unknown => crate::codec::cbor::text(out, "unknown"),
			Registration::NotRegistered => crate::codec::cbor::text(out, "not-registered"),
			Registration::Searching => crate::codec::cbor::text(out, "searching"),
			Registration::Home => crate::codec::cbor::text(out, "home"),
			Registration::Roaming => crate::codec::cbor::text(out, "roaming"),
			Registration::Denied => crate::codec::cbor::text(out, "denied"),
		}
	}
}

impl Signal {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"valid\":");
		if self.valid {
			out.push_str("true");
		} else {
			out.push_str("false");
		}
		out.push(',');
		out.push_str("\"rssi-dbm\":");
		let _ = write!(out, "{}", self.rssi_dbm);
		out.push(',');
		out.push_str("\"quality\":");
		let _ = write!(out, "{}", self.quality);
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("valid=");
		if self.valid {
			out.push_str("true");
		} else {
			out.push_str("false");
		}
		out.push_str(", ");
		out.push_str("rssi-dbm=");
		let _ = write!(out, "{}", self.rssi_dbm);
		out.push_str(", ");
		out.push_str("quality=");
		let _ = write!(out, "{}", self.quality);
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 3);
		crate::codec::cbor::text(out, "valid");
		crate::codec::cbor::boolean(out, self.valid);
		crate::codec::cbor::text(out, "rssi-dbm");
		crate::codec::cbor::int(out, self.rssi_dbm as i64);
		crate::codec::cbor::text(out, "quality");
		crate::codec::cbor::uint(out, self.quality as u64);
	}
}

impl AttemptCount {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"known\":");
		if self.known {
			out.push_str("true");
		} else {
			out.push_str("false");
		}
		out.push(',');
		out.push_str("\"remaining\":");
		let _ = write!(out, "{}", self.remaining);
		out.push(',');
		out.push_str("\"sim-generation\":");
		let _ = write!(out, "{}", self.sim_generation);
		out.push(',');
		out.push_str("\"observed-ms\":");
		let _ = write!(out, "{}", self.observed_ms);
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("known=");
		if self.known {
			out.push_str("true");
		} else {
			out.push_str("false");
		}
		out.push_str(", ");
		out.push_str("remaining=");
		let _ = write!(out, "{}", self.remaining);
		out.push_str(", ");
		out.push_str("sim-generation=");
		let _ = write!(out, "{}", self.sim_generation);
		out.push_str(", ");
		out.push_str("observed-ms=");
		let _ = write!(out, "{}", self.observed_ms);
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 4);
		crate::codec::cbor::text(out, "known");
		crate::codec::cbor::boolean(out, self.known);
		crate::codec::cbor::text(out, "remaining");
		crate::codec::cbor::uint(out, self.remaining as u64);
		crate::codec::cbor::text(out, "sim-generation");
		crate::codec::cbor::uint(out, self.sim_generation as u64);
		crate::codec::cbor::text(out, "observed-ms");
		crate::codec::cbor::uint(out, self.observed_ms as u64);
	}
}

impl Attempts {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"pin\":");
		self.pin.to_json_into(out);
		out.push(',');
		out.push_str("\"puk\":");
		self.puk.to_json_into(out);
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("pin=");
		self.pin.to_text_into(out);
		out.push_str(", ");
		out.push_str("puk=");
		self.puk.to_text_into(out);
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 2);
		crate::codec::cbor::text(out, "pin");
		self.pin.to_cbor_into(out);
		crate::codec::cbor::text(out, "puk");
		self.puk.to_cbor_into(out);
	}
}

impl ContextState {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		match self {
			ContextState::Inactive => out.push_str("\"inactive\""),
			ContextState::Activating => out.push_str("\"activating\""),
			ContextState::Active => out.push_str("\"active\""),
			ContextState::Deactivating => out.push_str("\"deactivating\""),
			ContextState::Unknown => out.push_str("\"unknown\""),
		}
	}
	pub fn to_text_into(&self, out: &mut String) {
		match self {
			ContextState::Inactive => out.push_str("inactive"),
			ContextState::Activating => out.push_str("activating"),
			ContextState::Active => out.push_str("active"),
			ContextState::Deactivating => out.push_str("deactivating"),
			ContextState::Unknown => out.push_str("unknown"),
		}
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		match self {
			ContextState::Inactive => crate::codec::cbor::text(out, "inactive"),
			ContextState::Activating => crate::codec::cbor::text(out, "activating"),
			ContextState::Active => crate::codec::cbor::text(out, "active"),
			ContextState::Deactivating => crate::codec::cbor::text(out, "deactivating"),
			ContextState::Unknown => crate::codec::cbor::text(out, "unknown"),
		}
	}
}

impl ContextStatus {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"id\":");
		self.id.to_json_into(out);
		out.push(',');
		out.push_str("\"state\":");
		self.state.to_json_into(out);
		out.push(',');
		out.push_str("\"address\":");
		match &self.address {
			Some(v43) => {
				let _ = write!(out, "{}", v43);
			}
			None => {
				out.push_str("null");
			}
		}
		out.push(',');
		out.push_str("\"prefix\":");
		let _ = write!(out, "{}", self.prefix);
		out.push(',');
		out.push_str("\"mtu\":");
		let _ = write!(out, "{}", self.mtu);
		out.push(',');
		out.push_str("\"transmit-refused\":");
		let _ = write!(out, "{}", self.transmit_refused);
		out.push(',');
		out.push_str("\"receive-dropped\":");
		let _ = write!(out, "{}", self.receive_dropped);
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("id=");
		self.id.to_text_into(out);
		out.push_str(", ");
		out.push_str("state=");
		self.state.to_text_into(out);
		out.push_str(", ");
		out.push_str("address=");
		match &self.address {
			Some(v44) => {
				let _ = write!(out, "{}", v44);
			}
			None => {
				out.push('-');
			}
		}
		out.push_str(", ");
		out.push_str("prefix=");
		let _ = write!(out, "{}", self.prefix);
		out.push_str(", ");
		out.push_str("mtu=");
		let _ = write!(out, "{}", self.mtu);
		out.push_str(", ");
		out.push_str("transmit-refused=");
		let _ = write!(out, "{}", self.transmit_refused);
		out.push_str(", ");
		out.push_str("receive-dropped=");
		let _ = write!(out, "{}", self.receive_dropped);
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 7);
		crate::codec::cbor::text(out, "id");
		self.id.to_cbor_into(out);
		crate::codec::cbor::text(out, "state");
		self.state.to_cbor_into(out);
		crate::codec::cbor::text(out, "address");
		match &self.address {
			Some(v45) => {
				crate::codec::cbor::uint(out, *v45 as u64);
			}
			None => {
				crate::codec::cbor::null(out);
			}
		}
		crate::codec::cbor::text(out, "prefix");
		crate::codec::cbor::uint(out, self.prefix as u64);
		crate::codec::cbor::text(out, "mtu");
		crate::codec::cbor::uint(out, self.mtu as u64);
		crate::codec::cbor::text(out, "transmit-refused");
		crate::codec::cbor::uint(out, self.transmit_refused as u64);
		crate::codec::cbor::text(out, "receive-dropped");
		crate::codec::cbor::uint(out, self.receive_dropped as u64);
	}
}

impl ModemStatus {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"id\":");
		self.id.to_json_into(out);
		out.push(',');
		out.push_str("\"manufacturer\":");
		crate::codec::json_escape(&self.manufacturer, out);
		out.push(',');
		out.push_str("\"model\":");
		crate::codec::json_escape(&self.model, out);
		out.push(',');
		out.push_str("\"sim\":");
		self.sim.to_json_into(out);
		out.push(',');
		out.push_str("\"sim-generation\":");
		let _ = write!(out, "{}", self.sim_generation);
		out.push(',');
		out.push_str("\"registration\":");
		self.registration.to_json_into(out);
		out.push(',');
		out.push_str("\"operator\":");
		match &self.operator {
			Some(v46) => {
				crate::codec::json_escape(v46, out);
			}
			None => {
				out.push_str("null");
			}
		}
		out.push(',');
		out.push_str("\"signal\":");
		self.signal.to_json_into(out);
		out.push(',');
		out.push_str("\"attempts\":");
		self.attempts.to_json_into(out);
		out.push(',');
		out.push_str("\"context\":");
		match &self.context {
			Some(v47) => {
				v47.to_json_into(out);
			}
			None => {
				out.push_str("null");
			}
		}
		out.push(',');
		out.push_str("\"revision\":");
		let _ = write!(out, "{}", self.revision);
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("id=");
		self.id.to_text_into(out);
		out.push_str(", ");
		out.push_str("manufacturer=");
		out.push_str(&self.manufacturer);
		out.push_str(", ");
		out.push_str("model=");
		out.push_str(&self.model);
		out.push_str(", ");
		out.push_str("sim=");
		self.sim.to_text_into(out);
		out.push_str(", ");
		out.push_str("sim-generation=");
		let _ = write!(out, "{}", self.sim_generation);
		out.push_str(", ");
		out.push_str("registration=");
		self.registration.to_text_into(out);
		out.push_str(", ");
		out.push_str("operator=");
		match &self.operator {
			Some(v48) => {
				out.push_str(v48);
			}
			None => {
				out.push('-');
			}
		}
		out.push_str(", ");
		out.push_str("signal=");
		self.signal.to_text_into(out);
		out.push_str(", ");
		out.push_str("attempts=");
		self.attempts.to_text_into(out);
		out.push_str(", ");
		out.push_str("context=");
		match &self.context {
			Some(v49) => {
				v49.to_text_into(out);
			}
			None => {
				out.push('-');
			}
		}
		out.push_str(", ");
		out.push_str("revision=");
		let _ = write!(out, "{}", self.revision);
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 11);
		crate::codec::cbor::text(out, "id");
		self.id.to_cbor_into(out);
		crate::codec::cbor::text(out, "manufacturer");
		crate::codec::cbor::text(out, &self.manufacturer);
		crate::codec::cbor::text(out, "model");
		crate::codec::cbor::text(out, &self.model);
		crate::codec::cbor::text(out, "sim");
		self.sim.to_cbor_into(out);
		crate::codec::cbor::text(out, "sim-generation");
		crate::codec::cbor::uint(out, self.sim_generation as u64);
		crate::codec::cbor::text(out, "registration");
		self.registration.to_cbor_into(out);
		crate::codec::cbor::text(out, "operator");
		match &self.operator {
			Some(v50) => {
				crate::codec::cbor::text(out, v50);
			}
			None => {
				crate::codec::cbor::null(out);
			}
		}
		crate::codec::cbor::text(out, "signal");
		self.signal.to_cbor_into(out);
		crate::codec::cbor::text(out, "attempts");
		self.attempts.to_cbor_into(out);
		crate::codec::cbor::text(out, "context");
		match &self.context {
			Some(v51) => {
				v51.to_cbor_into(out);
			}
			None => {
				crate::codec::cbor::null(out);
			}
		}
		crate::codec::cbor::text(out, "revision");
		crate::codec::cbor::uint(out, self.revision as u64);
	}
}

impl ModemLimits {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"providers\":");
		let _ = write!(out, "{}", self.providers);
		out.push(',');
		out.push_str("\"providers-max\":");
		let _ = write!(out, "{}", self.providers_max);
		out.push(',');
		out.push_str("\"clients\":");
		let _ = write!(out, "{}", self.clients);
		out.push(',');
		out.push_str("\"clients-max\":");
		let _ = write!(out, "{}", self.clients_max);
		out.push(',');
		out.push_str("\"contexts\":");
		let _ = write!(out, "{}", self.contexts);
		out.push(',');
		out.push_str("\"contexts-max\":");
		let _ = write!(out, "{}", self.contexts_max);
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("providers=");
		let _ = write!(out, "{}", self.providers);
		out.push_str(", ");
		out.push_str("providers-max=");
		let _ = write!(out, "{}", self.providers_max);
		out.push_str(", ");
		out.push_str("clients=");
		let _ = write!(out, "{}", self.clients);
		out.push_str(", ");
		out.push_str("clients-max=");
		let _ = write!(out, "{}", self.clients_max);
		out.push_str(", ");
		out.push_str("contexts=");
		let _ = write!(out, "{}", self.contexts);
		out.push_str(", ");
		out.push_str("contexts-max=");
		let _ = write!(out, "{}", self.contexts_max);
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 6);
		crate::codec::cbor::text(out, "providers");
		crate::codec::cbor::uint(out, self.providers as u64);
		crate::codec::cbor::text(out, "providers-max");
		crate::codec::cbor::uint(out, self.providers_max as u64);
		crate::codec::cbor::text(out, "clients");
		crate::codec::cbor::uint(out, self.clients as u64);
		crate::codec::cbor::text(out, "clients-max");
		crate::codec::cbor::uint(out, self.clients_max as u64);
		crate::codec::cbor::text(out, "contexts");
		crate::codec::cbor::uint(out, self.contexts as u64);
		crate::codec::cbor::text(out, "contexts-max");
		crate::codec::cbor::uint(out, self.contexts_max as u64);
	}
}

impl WatchKind {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		match self {
			WatchKind::Snapshot => out.push_str("\"snapshot\""),
			WatchKind::Resync => out.push_str("\"resync\""),
		}
	}
	pub fn to_text_into(&self, out: &mut String) {
		match self {
			WatchKind::Snapshot => out.push_str("snapshot"),
			WatchKind::Resync => out.push_str("resync"),
		}
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		match self {
			WatchKind::Snapshot => crate::codec::cbor::text(out, "snapshot"),
			WatchKind::Resync => crate::codec::cbor::text(out, "resync"),
		}
	}
}

impl ModemWatch {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"kind\":");
		self.kind.to_json_into(out);
		out.push(',');
		out.push_str("\"modems\":");
		out.push('[');
		let mut v53 = true;
		for v52 in self.modems.iter() {
			if !v53 {
				out.push(',');
			}
			v53 = false;
			v52.to_json_into(out);
		}
		out.push(']');
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("kind=");
		self.kind.to_text_into(out);
		out.push_str(", ");
		out.push_str("modems=");
		out.push('[');
		let mut v55 = true;
		for v54 in self.modems.iter() {
			if !v55 {
				out.push_str(", ");
			}
			v55 = false;
			v54.to_text_into(out);
		}
		out.push(']');
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 2);
		crate::codec::cbor::text(out, "kind");
		self.kind.to_cbor_into(out);
		crate::codec::cbor::text(out, "modems");
		crate::codec::cbor::array(out, self.modems.len());
		for v56 in self.modems.iter() {
			v56.to_cbor_into(out);
		}
	}
}

impl Identity {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"imsi\":");
		match &self.imsi {
			Some(v57) => {
				crate::codec::json_escape(v57, out);
			}
			None => {
				out.push_str("null");
			}
		}
		out.push(',');
		out.push_str("\"iccid\":");
		match &self.iccid {
			Some(v58) => {
				crate::codec::json_escape(v58, out);
			}
			None => {
				out.push_str("null");
			}
		}
		out.push(',');
		out.push_str("\"msisdn\":");
		match &self.msisdn {
			Some(v59) => {
				crate::codec::json_escape(v59, out);
			}
			None => {
				out.push_str("null");
			}
		}
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("imsi=");
		match &self.imsi {
			Some(v60) => {
				out.push_str(v60);
			}
			None => {
				out.push('-');
			}
		}
		out.push_str(", ");
		out.push_str("iccid=");
		match &self.iccid {
			Some(v61) => {
				out.push_str(v61);
			}
			None => {
				out.push('-');
			}
		}
		out.push_str(", ");
		out.push_str("msisdn=");
		match &self.msisdn {
			Some(v62) => {
				out.push_str(v62);
			}
			None => {
				out.push('-');
			}
		}
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 3);
		crate::codec::cbor::text(out, "imsi");
		match &self.imsi {
			Some(v63) => {
				crate::codec::cbor::text(out, v63);
			}
			None => {
				crate::codec::cbor::null(out);
			}
		}
		crate::codec::cbor::text(out, "iccid");
		match &self.iccid {
			Some(v64) => {
				crate::codec::cbor::text(out, v64);
			}
			None => {
				crate::codec::cbor::null(out);
			}
		}
		crate::codec::cbor::text(out, "msisdn");
		match &self.msisdn {
			Some(v65) => {
				crate::codec::cbor::text(out, v65);
			}
			None => {
				crate::codec::cbor::null(out);
			}
		}
	}
}

impl SimPinResult {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		match self {
			SimPinResult::Accepted => out.push_str("\"accepted\""),
			SimPinResult::Rejected => out.push_str("\"rejected\""),
			SimPinResult::Blocked => out.push_str("\"blocked\""),
			SimPinResult::OutcomeUnknown => out.push_str("\"outcome-unknown\""),
			SimPinResult::UnknownCount => out.push_str("\"unknown-count\""),
		}
	}
	pub fn to_text_into(&self, out: &mut String) {
		match self {
			SimPinResult::Accepted => out.push_str("accepted"),
			SimPinResult::Rejected => out.push_str("rejected"),
			SimPinResult::Blocked => out.push_str("blocked"),
			SimPinResult::OutcomeUnknown => out.push_str("outcome-unknown"),
			SimPinResult::UnknownCount => out.push_str("unknown-count"),
		}
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		match self {
			SimPinResult::Accepted => crate::codec::cbor::text(out, "accepted"),
			SimPinResult::Rejected => crate::codec::cbor::text(out, "rejected"),
			SimPinResult::Blocked => crate::codec::cbor::text(out, "blocked"),
			SimPinResult::OutcomeUnknown => crate::codec::cbor::text(out, "outcome-unknown"),
			SimPinResult::UnknownCount => crate::codec::cbor::text(out, "unknown-count"),
		}
	}
}

impl SimPinOutcome {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"result\":");
		self.result.to_json_into(out);
		out.push(',');
		out.push_str("\"attempts\":");
		self.attempts.to_json_into(out);
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("result=");
		self.result.to_text_into(out);
		out.push_str(", ");
		out.push_str("attempts=");
		self.attempts.to_text_into(out);
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 2);
		crate::codec::cbor::text(out, "result");
		self.result.to_cbor_into(out);
		crate::codec::cbor::text(out, "attempts");
		self.attempts.to_cbor_into(out);
	}
}

impl GrantKind {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		match self {
			GrantKind::Data => out.push_str("\"data\""),
			GrantKind::Identity => out.push_str("\"identity\""),
			GrantKind::Manage => out.push_str("\"manage\""),
		}
	}
	pub fn to_text_into(&self, out: &mut String) {
		match self {
			GrantKind::Data => out.push_str("data"),
			GrantKind::Identity => out.push_str("identity"),
			GrantKind::Manage => out.push_str("manage"),
		}
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		match self {
			GrantKind::Data => crate::codec::cbor::text(out, "data"),
			GrantKind::Identity => crate::codec::cbor::text(out, "identity"),
			GrantKind::Manage => crate::codec::cbor::text(out, "manage"),
		}
	}
}

impl DataPolicy {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"apn\":");
		crate::codec::json_escape(&self.apn, out);
		out.push(',');
		out.push_str("\"replace-uplink\":");
		if self.replace_uplink {
			out.push_str("true");
		} else {
			out.push_str("false");
		}
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("apn=");
		out.push_str(&self.apn);
		out.push_str(", ");
		out.push_str("replace-uplink=");
		if self.replace_uplink {
			out.push_str("true");
		} else {
			out.push_str("false");
		}
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 2);
		crate::codec::cbor::text(out, "apn");
		crate::codec::cbor::text(out, &self.apn);
		crate::codec::cbor::text(out, "replace-uplink");
		crate::codec::cbor::boolean(out, self.replace_uplink);
	}
}

impl ModemGrant {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"connection\":");
		let _ = write!(out, "{}", self.connection);
		out.push(',');
		out.push_str("\"modem\":");
		self.modem.to_json_into(out);
		out.push(',');
		out.push_str("\"sim-generation\":");
		let _ = write!(out, "{}", self.sim_generation);
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("connection=");
		let _ = write!(out, "{}", self.connection);
		out.push_str(", ");
		out.push_str("modem=");
		self.modem.to_text_into(out);
		out.push_str(", ");
		out.push_str("sim-generation=");
		let _ = write!(out, "{}", self.sim_generation);
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 3);
		crate::codec::cbor::text(out, "connection");
		crate::codec::cbor::uint(out, self.connection as u64);
		crate::codec::cbor::text(out, "modem");
		self.modem.to_cbor_into(out);
		crate::codec::cbor::text(out, "sim-generation");
		crate::codec::cbor::uint(out, self.sim_generation as u64);
	}
}

#[cfg(test)]
mod compat;
