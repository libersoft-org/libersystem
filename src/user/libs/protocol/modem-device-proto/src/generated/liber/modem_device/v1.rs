// @generated by lsidl-gen from ../../idl/modem-device.lsidl (package liber:modem-device@1).
// Do not edit by hand; regenerate with `./gen.sh`.
//! LiberSystem modem devices - the provider contract a modem publication serves to ModemService.
//!
//! A PROVIDER TRANSLATES FRAMING; THE SERVICE DECIDES. The USB CDC-MBIM class module, and the in-guest
//! fixture before it, turn these commands into MBIM control messages and these datagrams into NTB
//! sessions. Whether a command is allowed, which context may be activated and who asked is ModemService's.
#![allow(dead_code, unused_imports, unused_variables, unused_mut, clippy::all)]

use crate::codec::{Handles, PROTOCOL_INFO_OP, Reader, Sink, SliceWriter, VecWriter};
use alloc::string::String;
use alloc::vec::Vec;
use core::fmt::Write as _;

pub use crate::generated::liber::base::v1::Error;

/// The limits a provider negotiates DOWN from the contract's maxima: eight pending commands, 256
/// fragments and 64 kB per message, 256 kB of assemblies, four assemblies, an MTU of 4096.
#[derive(Clone, Debug, PartialEq)]
pub struct DeviceLimits {
	pub version: u32,
	pub pending: u8,
	pub fragments: u16,
	pub message_bytes: u32,
	pub assembly_bytes: u32,
	pub assemblies: u8,
	pub mtu: u16,
}

impl DeviceLimits {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<DeviceLimits> {
		let mut r = Reader::new(bytes);
		let value = DeviceLimits::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<DeviceLimits> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = DeviceLimits::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u32(self.version)?;
		w.u8(self.pending)?;
		w.u16(self.fragments)?;
		w.u32(self.message_bytes)?;
		w.u32(self.assembly_bytes)?;
		w.u8(self.assemblies)?;
		w.u16(self.mtu)?;
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<DeviceLimits> {
		let version = r.u32()?;
		let pending = r.u8()?;
		let fragments = r.u16()?;
		let message_bytes = r.u32()?;
		let assembly_bytes = r.u32()?;
		let assemblies = r.u8()?;
		let mtu = r.u16()?;
		Some(DeviceLimits { version, pending, fragments, message_bytes, assembly_bytes, assemblies, mtu })
	}
}

#[derive(Clone, Debug, PartialEq)]
pub struct DeviceOpen {
	pub limits: DeviceLimits,
	/// A new connection is a new generation: transaction numbers start again only with it.
	pub connection_generation: u64,
}

impl DeviceOpen {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<DeviceOpen> {
		let mut r = Reader::new(bytes);
		let value = DeviceOpen::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<DeviceOpen> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = DeviceOpen::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		self.limits.write(w)?;
		w.u64(self.connection_generation)?;
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<DeviceOpen> {
		let limits = DeviceLimits::read(r)?;
		let connection_generation = r.u64()?;
		Some(DeviceOpen { limits, connection_generation })
	}
}

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
#[repr(u8)]
pub enum DeviceSim {
	Unknown = 0,
	Absent = 1,
	LockedPin = 2,
	LockedPuk = 3,
	Ready = 4,
	Blocked = 5,
}

impl DeviceSim {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<DeviceSim> {
		let mut r = Reader::new(bytes);
		let value = DeviceSim::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<DeviceSim> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = DeviceSim::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u8(*self as u8)
	}
	pub fn read(r: &mut Reader) -> Option<DeviceSim> {
		match r.u8()? {
			0 => Some(DeviceSim::Unknown),
			1 => Some(DeviceSim::Absent),
			2 => Some(DeviceSim::LockedPin),
			3 => Some(DeviceSim::LockedPuk),
			4 => Some(DeviceSim::Ready),
			5 => Some(DeviceSim::Blocked),
			_ => None,
		}
	}
}

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
#[repr(u8)]
pub enum DeviceRegistration {
	Unknown = 0,
	NotRegistered = 1,
	Searching = 2,
	Home = 3,
	Roaming = 4,
	Denied = 5,
}

impl DeviceRegistration {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<DeviceRegistration> {
		let mut r = Reader::new(bytes);
		let value = DeviceRegistration::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<DeviceRegistration> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = DeviceRegistration::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u8(*self as u8)
	}
	pub fn read(r: &mut Reader) -> Option<DeviceRegistration> {
		match r.u8()? {
			0 => Some(DeviceRegistration::Unknown),
			1 => Some(DeviceRegistration::NotRegistered),
			2 => Some(DeviceRegistration::Searching),
			3 => Some(DeviceRegistration::Home),
			4 => Some(DeviceRegistration::Roaming),
			5 => Some(DeviceRegistration::Denied),
			_ => None,
		}
	}
}

#[derive(Clone, Debug, PartialEq)]
pub struct DeviceState {
	pub manufacturer: String,
	pub model: String,
	pub sim: DeviceSim,
	pub sim_generation: u64,
	pub registration: DeviceRegistration,
	pub operator: Option<String>,
	pub signal_valid: bool,
	pub rssi_dbm: i32,
	pub quality: u8,
	/// Remaining attempts, when the SIM reports them.
	pub pin_attempts: Option<u8>,
	pub puk_attempts: Option<u8>,
	/// Whether the context is up, from the modem's own report.
	pub context_active: bool,
}

impl DeviceState {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<DeviceState> {
		let mut r = Reader::new(bytes);
		let value = DeviceState::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<DeviceState> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = DeviceState::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.bytes_lp(self.manufacturer.as_bytes())?;
		w.bytes_lp(self.model.as_bytes())?;
		self.sim.write(w)?;
		w.u64(self.sim_generation)?;
		self.registration.write(w)?;
		match &self.operator {
			Some(v0) => {
				w.u8(1)?;
				w.bytes_lp(v0.as_bytes())?;
			}
			None => {
				w.u8(0)?;
			}
		}
		w.boolean(self.signal_valid)?;
		w.i32(self.rssi_dbm)?;
		w.u8(self.quality)?;
		match &self.pin_attempts {
			Some(v1) => {
				w.u8(1)?;
				w.u8(*v1)?;
			}
			None => {
				w.u8(0)?;
			}
		}
		match &self.puk_attempts {
			Some(v2) => {
				w.u8(1)?;
				w.u8(*v2)?;
			}
			None => {
				w.u8(0)?;
			}
		}
		w.boolean(self.context_active)?;
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<DeviceState> {
		let manufacturer = {
			let v3 = r.string_lp()?;
			(v3.len() <= 32).then_some(v3)?
		};
		let model = {
			let v4 = r.string_lp()?;
			(v4.len() <= 32).then_some(v4)?
		};
		let sim = DeviceSim::read(r)?;
		let sim_generation = r.u64()?;
		let registration = DeviceRegistration::read(r)?;
		let operator = if r.tag()? {
			Some({
				let v5 = r.string_lp()?;
				(v5.len() <= 32).then_some(v5)?
			})
		} else {
			None
		};
		let signal_valid = r.boolean()?;
		let rssi_dbm = r.i32()?;
		let quality = r.u8()?;
		let pin_attempts = if r.tag()? { Some(r.u8()?) } else { None };
		let puk_attempts = if r.tag()? { Some(r.u8()?) } else { None };
		let context_active = r.boolean()?;
		Some(DeviceState { manufacturer, model, sim, sim_generation, registration, operator, signal_valid, rssi_dbm, quality, pin_attempts, puk_attempts, context_active })
	}
}

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
#[repr(u8)]
pub enum IpFamily {
	Ipv4 = 4,
	Ipv6 = 6,
}

impl IpFamily {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<IpFamily> {
		let mut r = Reader::new(bytes);
		let value = IpFamily::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<IpFamily> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = IpFamily::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u8(*self as u8)
	}
	pub fn read(r: &mut Reader) -> Option<IpFamily> {
		match r.u8()? {
			4 => Some(IpFamily::Ipv4),
			6 => Some(IpFamily::Ipv6),
			_ => None,
		}
	}
}

/// What the network gave the context. An IPv6-only context says so in `family`, and its IPv4 fields
/// are then meaningless: this system installs IPv4 contexts only, and says `unsupported` to the rest.
#[derive(Clone, Debug, PartialEq)]
pub struct IpConfig {
	pub family: IpFamily,
	pub address: u32,
	pub prefix: u8,
	pub gateway: Option<u32>,
	pub dns: Vec<u32>,
	pub mtu: u16,
}

impl IpConfig {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<IpConfig> {
		let mut r = Reader::new(bytes);
		let value = IpConfig::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<IpConfig> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = IpConfig::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		self.family.write(w)?;
		w.u32(self.address)?;
		w.u8(self.prefix)?;
		match &self.gateway {
			Some(v6) => {
				w.u8(1)?;
				w.u32(*v6)?;
			}
			None => {
				w.u8(0)?;
			}
		}
		if self.dns.len() > u16::MAX as usize {
			return None;
		}
		w.u16(self.dns.len() as u16)?;
		for v7 in self.dns.iter() {
			w.u32(*v7)?;
		}
		w.u16(self.mtu)?;
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<IpConfig> {
		let family = IpFamily::read(r)?;
		let address = r.u32()?;
		let prefix = r.u8()?;
		let gateway = if r.tag()? { Some(r.u32()?) } else { None };
		let dns = {
			let v8 = r.u16()? as usize;
			let v8 = (v8 <= 2).then_some(v8)?;
			let mut v9 = Vec::new();
			v9.try_reserve_exact(v8).ok()?;
			for _ in 0..v8 {
				v9.push(r.u32()?);
			}
			v9
		};
		let mtu = r.u16()?;
		Some(IpConfig { family, address, prefix, gateway, dns, mtu })
	}
}

#[derive(Clone, Debug, PartialEq)]
pub struct DeviceIdentity {
	pub imsi: Option<String>,
	pub iccid: Option<String>,
	pub msisdn: Option<String>,
}

impl DeviceIdentity {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<DeviceIdentity> {
		let mut r = Reader::new(bytes);
		let value = DeviceIdentity::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<DeviceIdentity> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = DeviceIdentity::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		match &self.imsi {
			Some(v10) => {
				w.u8(1)?;
				w.bytes_lp(v10.as_bytes())?;
			}
			None => {
				w.u8(0)?;
			}
		}
		match &self.iccid {
			Some(v11) => {
				w.u8(1)?;
				w.bytes_lp(v11.as_bytes())?;
			}
			None => {
				w.u8(0)?;
			}
		}
		match &self.msisdn {
			Some(v12) => {
				w.u8(1)?;
				w.bytes_lp(v12.as_bytes())?;
			}
			None => {
				w.u8(0)?;
			}
		}
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<DeviceIdentity> {
		let imsi = if r.tag()? {
			Some({
				let v13 = r.string_lp()?;
				(v13.len() <= 16).then_some(v13)?
			})
		} else {
			None
		};
		let iccid = if r.tag()? {
			Some({
				let v14 = r.string_lp()?;
				(v14.len() <= 22).then_some(v14)?
			})
		} else {
			None
		};
		let msisdn = if r.tag()? {
			Some({
				let v15 = r.string_lp()?;
				(v15.len() <= 16).then_some(v15)?
			})
		} else {
			None
		};
		Some(DeviceIdentity { imsi, iccid, msisdn })
	}
}

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
#[repr(u8)]
pub enum CommandKind {
	Query = 1,
	EnterPin = 2,
	EnterPuk = 3,
	Activate = 4,
	Deactivate = 5,
	Identity = 6,
}

impl CommandKind {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<CommandKind> {
		let mut r = Reader::new(bytes);
		let value = CommandKind::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<CommandKind> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = CommandKind::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u8(*self as u8)
	}
	pub fn read(r: &mut Reader) -> Option<CommandKind> {
		match r.u8()? {
			1 => Some(CommandKind::Query),
			2 => Some(CommandKind::EnterPin),
			3 => Some(CommandKind::EnterPuk),
			4 => Some(CommandKind::Activate),
			5 => Some(CommandKind::Deactivate),
			6 => Some(CommandKind::Identity),
			_ => None,
		}
	}
}

/// One command, named by the connection generation and a transaction number that connection never
/// reuses, and by the SIM and context generations it was prepared against.
#[derive(Clone, Debug, PartialEq)]
pub struct Command {
	pub connection_generation: u64,
	pub transaction: u64,
	pub kind: CommandKind,
	pub sim_generation: u64,
	pub context_generation: u64,
	pub secret: Vec<u8>,
	pub new_secret: Vec<u8>,
	pub apn: String,
}

impl Command {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<Command> {
		let mut r = Reader::new(bytes);
		let value = Command::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<Command> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = Command::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u64(self.connection_generation)?;
		w.u64(self.transaction)?;
		self.kind.write(w)?;
		w.u64(self.sim_generation)?;
		w.u64(self.context_generation)?;
		if self.secret.len() > u16::MAX as usize {
			return None;
		}
		w.u16(self.secret.len() as u16)?;
		for v16 in self.secret.iter() {
			w.u8(*v16)?;
		}
		if self.new_secret.len() > u16::MAX as usize {
			return None;
		}
		w.u16(self.new_secret.len() as u16)?;
		for v17 in self.new_secret.iter() {
			w.u8(*v17)?;
		}
		w.bytes_lp(self.apn.as_bytes())?;
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<Command> {
		let connection_generation = r.u64()?;
		let transaction = r.u64()?;
		let kind = CommandKind::read(r)?;
		let sim_generation = r.u64()?;
		let context_generation = r.u64()?;
		let secret = {
			let v18 = r.u16()? as usize;
			let v18 = (v18 <= 8).then_some(v18)?;
			let mut v19 = Vec::new();
			v19.try_reserve_exact(v18).ok()?;
			for _ in 0..v18 {
				v19.push(r.u8()?);
			}
			v19
		};
		let new_secret = {
			let v20 = r.u16()? as usize;
			let v20 = (v20 <= 8).then_some(v20)?;
			let mut v21 = Vec::new();
			v21.try_reserve_exact(v20).ok()?;
			for _ in 0..v20 {
				v21.push(r.u8()?);
			}
			v21
		};
		let apn = {
			let v22 = r.string_lp()?;
			(v22.len() <= 64).then_some(v22)?
		};
		Some(Command { connection_generation, transaction, kind, sim_generation, context_generation, secret, new_secret, apn })
	}
}

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
#[repr(u8)]
pub enum CommandStatus {
	Done = 1,
	Rejected = 2,
	Failed = 3,
	/// The SIM or context generation the command named is no longer the device's.
	Stale = 4,
}

impl CommandStatus {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<CommandStatus> {
		let mut r = Reader::new(bytes);
		let value = CommandStatus::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<CommandStatus> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = CommandStatus::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u8(*self as u8)
	}
	pub fn read(r: &mut Reader) -> Option<CommandStatus> {
		match r.u8()? {
			1 => Some(CommandStatus::Done),
			2 => Some(CommandStatus::Rejected),
			3 => Some(CommandStatus::Failed),
			4 => Some(CommandStatus::Stale),
			_ => None,
		}
	}
}

#[derive(Clone, Debug, PartialEq)]
pub struct CommandReply {
	pub connection_generation: u64,
	pub transaction: u64,
	pub kind: CommandKind,
	pub status: CommandStatus,
	pub sim_generation: u64,
	pub context_generation: u64,
	pub state: Option<DeviceState>,
	pub config: Option<IpConfig>,
	pub identity: Option<DeviceIdentity>,
}

impl CommandReply {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<CommandReply> {
		let mut r = Reader::new(bytes);
		let value = CommandReply::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<CommandReply> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = CommandReply::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u64(self.connection_generation)?;
		w.u64(self.transaction)?;
		self.kind.write(w)?;
		self.status.write(w)?;
		w.u64(self.sim_generation)?;
		w.u64(self.context_generation)?;
		match &self.state {
			Some(v23) => {
				w.u8(1)?;
				v23.write(w)?;
			}
			None => {
				w.u8(0)?;
			}
		}
		match &self.config {
			Some(v24) => {
				w.u8(1)?;
				v24.write(w)?;
			}
			None => {
				w.u8(0)?;
			}
		}
		match &self.identity {
			Some(v25) => {
				w.u8(1)?;
				v25.write(w)?;
			}
			None => {
				w.u8(0)?;
			}
		}
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<CommandReply> {
		let connection_generation = r.u64()?;
		let transaction = r.u64()?;
		let kind = CommandKind::read(r)?;
		let status = CommandStatus::read(r)?;
		let sim_generation = r.u64()?;
		let context_generation = r.u64()?;
		let state = if r.tag()? { Some(DeviceState::read(r)?) } else { None };
		let config = if r.tag()? { Some(IpConfig::read(r)?) } else { None };
		let identity = if r.tag()? { Some(DeviceIdentity::read(r)?) } else { None };
		Some(CommandReply { connection_generation, transaction, kind, status, sim_generation, context_generation, state, config, identity })
	}
}

/// An unsolicited change, carrying the device's whole state at a revision.
#[derive(Clone, Debug, PartialEq)]
pub struct Indication {
	pub revision: u64,
	pub state: DeviceState,
}

impl Indication {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<Indication> {
		let mut r = Reader::new(bytes);
		let value = Indication::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<Indication> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = Indication::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u64(self.revision)?;
		self.state.write(w)?;
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<Indication> {
		let revision = r.u64()?;
		let state = DeviceState::read(r)?;
		Some(Indication { revision, state })
	}
}

/// One IP datagram of the active context, tagged with the context it belongs to.
#[derive(Clone, Debug, PartialEq)]
pub struct Datagram {
	pub context_generation: u64,
	pub bytes: Vec<u8>,
}

impl Datagram {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<Datagram> {
		let mut r = Reader::new(bytes);
		let value = Datagram::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<Datagram> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = Datagram::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u64(self.context_generation)?;
		if self.bytes.len() > u16::MAX as usize {
			return None;
		}
		w.u16(self.bytes.len() as u16)?;
		for v26 in self.bytes.iter() {
			w.u8(*v26)?;
		}
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<Datagram> {
		let context_generation = r.u64()?;
		let bytes = {
			let v27 = r.u16()? as usize;
			let v27 = (v27 <= 4096).then_some(v27)?;
			let mut v28 = Vec::new();
			v28.try_reserve_exact(v27).ok()?;
			for _ in 0..v27 {
				v28.push(r.u8()?);
			}
			v28
		};
		Some(Datagram { context_generation, bytes })
	}
}

/// THE PROVIDER CONTRACT a `modem` publication serves - to ModemService alone.
// interface `modem-device` over a channel: opcodes, a Service trait + dispatch, and a Client.
pub mod modem_device {
	use super::*;
	use crate::codec::{Reader, Sink, SliceWriter, Transport, TransportError, VecWriter};
	use alloc::vec::Vec;

	pub const OP_OPEN: u16 = 1;
	pub const OP_COMMAND: u16 = 2;
	pub const OP_INDICATIONS: u16 = 3;
	pub const OP_RECEIVE: u16 = 4;
	pub const OP_TRANSMIT: u16 = 5;

	pub trait Service {
		fn open(&mut self, version: u32) -> Result<DeviceOpen, Error>;
		fn command(&mut self, command: Command) -> Result<CommandReply, Error>;
		fn indications(&mut self) -> Vec<Indication>;
		/// Datagrams from the network, provider to service.
		fn receive(&mut self) -> Vec<Datagram>;
		/// The channel datagrams to the network go on, service to provider: one `datagram` frame per
		/// message, never more waiting than the provider's queue holds.
		fn transmit(&mut self) -> Result<u64, Error>;
	}

	pub fn dispatch<S: Service>(service: &mut S, request: &[u8], request_handles: &mut Handles, out: &mut [u8], reply_handles: &mut Handles) -> Option<usize> {
		let mut reader = Reader::with_handle_list(request, request_handles);
		let r = &mut reader;
		let op = r.u16()?;
		let corr = r.u32()?;
		let mut writer = SliceWriter::new(out);
		if op == PROTOCOL_INFO_OP {
			r.finish()?;
			request_handles.clear();
			let w = &mut writer;
			w.u32(corr)?;
			w.bytes_lp(b"liber:modem-device")?;
			w.u32(1)?;
			match Handles::try_from_slice(writer.handles()) {
				Some(taken) => *reply_handles = taken,
				None => return None,
			}
			return Some(writer.pos());
		}
		match op {
			OP_OPEN => {
				let version = r.u32()?;
				r.finish()?;
				request_handles.clear();
				let result = service.open(version);
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v29) => {
							w.u8(1)?;
							v29.write(w)?;
						}
						Err(v30) => {
							w.u8(0)?;
							v30.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			OP_COMMAND => {
				let command = Command::read(r)?;
				r.finish()?;
				request_handles.clear();
				let result = service.command(command);
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v31) => {
							w.u8(1)?;
							v31.write(w)?;
						}
						Err(v32) => {
							w.u8(0)?;
							v32.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			OP_TRANSMIT => {
				r.finish()?;
				request_handles.clear();
				let result = service.transmit();
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v33) => {
							w.u8(1)?;
							w.set_handle(*v33)?;
							w.u32(0)?;
						}
						Err(v34) => {
							w.u8(0)?;
							v34.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			_ => return None,
		}
		match Handles::try_from_slice(writer.handles()) {
			Some(taken) => *reply_handles = taken,
			None => return None,
		}
		Some(writer.pos())
	}

	pub fn indications_open<S: Service>(service: &mut S, request: &[u8], request_handles: &mut Handles) -> Option<(u32, Vec<Indication>)> {
		let mut reader = Reader::with_handle_list(request, request_handles);
		let r = &mut reader;
		let _op = r.u16()?;
		let corr = r.u32()?;
		r.finish()?;
		request_handles.clear();
		let items = service.indications();
		Some((corr, items))
	}
	pub fn indications_frame(seq: u32, item: &Indication, out: &mut [u8], frame_handles: &mut Handles) -> Option<usize> {
		let mut writer = SliceWriter::new(out);
		let encoded: Option<()> = (|| {
			let w = &mut writer;
			w.u32(seq)?;
			item.write(w)?;
			Some(())
		})();
		if encoded.is_none() {
			if let Some(taken) = Handles::try_from_slice(writer.handles()) {
				*frame_handles = taken;
			}
			return None;
		}
		*frame_handles = Handles::try_from_slice(writer.handles())?;
		Some(writer.pos())
	}
	pub fn indications_read(msg: &[u8], frame_handles: &mut Handles) -> Option<Indication> {
		let mut reader = Reader::with_handles(msg, frame_handles);
		let r = &mut reader;
		let _seq = r.u32()?;
		let value = Indication::read(r)?;
		reader.finish()?;
		frame_handles.clear();
		Some(value)
	}

	pub fn receive_open<S: Service>(service: &mut S, request: &[u8], request_handles: &mut Handles) -> Option<(u32, Vec<Datagram>)> {
		let mut reader = Reader::with_handle_list(request, request_handles);
		let r = &mut reader;
		let _op = r.u16()?;
		let corr = r.u32()?;
		r.finish()?;
		request_handles.clear();
		let items = service.receive();
		Some((corr, items))
	}
	pub fn receive_frame(seq: u32, item: &Datagram, out: &mut [u8], frame_handles: &mut Handles) -> Option<usize> {
		let mut writer = SliceWriter::new(out);
		let encoded: Option<()> = (|| {
			let w = &mut writer;
			w.u32(seq)?;
			item.write(w)?;
			Some(())
		})();
		if encoded.is_none() {
			if let Some(taken) = Handles::try_from_slice(writer.handles()) {
				*frame_handles = taken;
			}
			return None;
		}
		*frame_handles = Handles::try_from_slice(writer.handles())?;
		Some(writer.pos())
	}
	pub fn receive_read(msg: &[u8], frame_handles: &mut Handles) -> Option<Datagram> {
		let mut reader = Reader::with_handles(msg, frame_handles);
		let r = &mut reader;
		let _seq = r.u32()?;
		let value = Datagram::read(r)?;
		reader.finish()?;
		frame_handles.clear();
		Some(value)
	}

	fn transport_outcome(error: TransportError) -> Error {
		match error {
			// The request never left this process, so nothing happened and trying
			// again is safe - which is what `again` says.
			TransportError::SendRefused | TransportError::NoRoute => Error::Again,
			// It went out and no answer came back. The server may have acted before
			// it died or before the deadline; nobody knows, and `commit-uncertain` is
			// the answer `base.error` grew so a caller is not forced to guess.
			// The reply could not be held, or arrived and broke the framing rules. In
			// both the server ANSWERED, so it acted; this end simply cannot read what
			// it said, which is the same position as never hearing back.
			TransportError::PeerClosed | TransportError::ReceiveFailed | TransportError::TimedOut | TransportError::NoMemory | TransportError::Malformed => Error::CommitUncertain,
		}
	}

	pub struct Client<T: Transport> {
		transport: T,
		corr: u32,
		deadline: u64,
		last_error: Option<TransportError>,
	}

	impl<T: Transport> Client<T> {
		pub fn new(transport: T) -> Client<T> {
			Client { transport, corr: 0, deadline: 0, last_error: None }
		}
		pub fn with_deadline(transport: T, deadline: u64) -> Client<T> {
			Client { transport, corr: 0, deadline, last_error: None }
		}
		pub fn set_deadline(&mut self, deadline: u64) {
			self.deadline = deadline;
		}
		pub fn last_error(&self) -> Option<TransportError> {
			self.last_error
		}
		pub fn into_transport(self) -> T {
			self.transport
		}
		fn next_corr(&mut self) -> u32 {
			let c = self.corr;
			self.corr = self.corr.wrapping_add(1);
			c
		}
		pub fn protocol_info(&mut self) -> Option<(String, u32)> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(PROTOCOL_INFO_OP)?;
			w.u32(corr)?;
			// No parameter, so no capability: `into_inner` says so rather than this
			// line assuming it.
			let request = writer.into_inner()?;
			let mut reply_handles = Handles::new();
			let reply = self
				.transport
				.call(&request, &[], &mut reply_handles, self.deadline)
				.map_err(|e| {
					self.last_error = Some(e);
					e
				})
				.ok()?;
			if !reply_handles.is_empty() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			let mut reader = Reader::new(&reply);
			let r = &mut reader;
			if r.u32()? != corr {
				return None;
			}
			let package = r.string_lp()?;
			let version = r.u32()?;
			r.finish()?;
			Some((package, version))
		}
		pub fn open(&mut self, version: &u32) -> Option<Result<DeviceOpen, Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_OPEN)?;
			w.u32(corr)?;
			w.u32(*version)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? { Ok(DeviceOpen::read(r)?) } else { Err(Error::read(r)?) };
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
		pub fn command(&mut self, command: &Command) -> Option<Result<CommandReply, Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_COMMAND)?;
			w.u32(corr)?;
			command.write(w)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? { Ok(CommandReply::read(r)?) } else { Err(Error::read(r)?) };
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
		pub fn indications(&mut self) -> Option<u64> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_INDICATIONS)?;
			w.u32(corr)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = self
				.transport
				.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline)
				.map_err(|e| {
					self.last_error = Some(e);
					e
				})
				.ok()?;
			let mut reader = Reader::new(&reply);
			let r = &mut reader;
			if r.u32()? != corr || r.finish().is_none() || reply_handles.len() != 1 {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			Some(reply_handles.first())
		}
		pub fn receive(&mut self) -> Option<u64> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_RECEIVE)?;
			w.u32(corr)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = self
				.transport
				.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline)
				.map_err(|e| {
					self.last_error = Some(e);
					e
				})
				.ok()?;
			let mut reader = Reader::new(&reply);
			let r = &mut reader;
			if r.u32()? != corr || r.finish().is_none() || reply_handles.len() != 1 {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			Some(reply_handles.first())
		}
		pub fn transmit(&mut self) -> Option<Result<u64, Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_TRANSMIT)?;
			w.u32(corr)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? {
					Ok({
						let _ = r.u32()?;
						r.take_handle()?
					})
				} else {
					Err(Error::read(r)?)
				};
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_modem_device_modem_device_open")]
	fn channel_invoke_open(chan: u64, version: &u32) -> Option<Result<DeviceOpen, Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.open(version)
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_modem_device_modem_device_command")]
	fn channel_invoke_command(chan: u64, command: &Command) -> Option<Result<CommandReply, Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.command(command)
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_modem_device_modem_device_indications")]
	fn channel_invoke_indications(chan: u64) -> Option<u64> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.indications()
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_modem_device_modem_device_receive")]
	fn channel_invoke_receive(chan: u64) -> Option<u64> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.receive()
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_modem_device_modem_device_transmit")]
	fn channel_invoke_transmit(chan: u64) -> Option<Result<u64, Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.transmit()
	}
}

/// What the in-guest modem fixture's control endpoint can do to it. DEVELOPMENT ONLY: the endpoint exists
/// on a development fixture bound to a QEMU test device, reached through PermissionManager in a
/// development build for a probe whose policy row grants `fixture-control`.
#[derive(Clone, Debug, PartialEq)]
pub struct FixtureStats {
	pub commands: u32,
	pub pin_attempts: u32,
	pub activations: u32,
	pub datagrams_in: u32,
	pub datagrams_out: u32,
	pub echo_replies: u32,
	pub dns_answers: u32,
	/// The last PIN the fixture's SIM was given, as a count of bytes only: whether one arrived, never
	/// what it was.
	pub last_secret_bytes: u8,
}

impl FixtureStats {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<FixtureStats> {
		let mut r = Reader::new(bytes);
		let value = FixtureStats::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<FixtureStats> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = FixtureStats::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u32(self.commands)?;
		w.u32(self.pin_attempts)?;
		w.u32(self.activations)?;
		w.u32(self.datagrams_in)?;
		w.u32(self.datagrams_out)?;
		w.u32(self.echo_replies)?;
		w.u32(self.dns_answers)?;
		w.u8(self.last_secret_bytes)?;
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<FixtureStats> {
		let commands = r.u32()?;
		let pin_attempts = r.u32()?;
		let activations = r.u32()?;
		let datagrams_in = r.u32()?;
		let datagrams_out = r.u32()?;
		let echo_replies = r.u32()?;
		let dns_answers = r.u32()?;
		let last_secret_bytes = r.u8()?;
		Some(FixtureStats { commands, pin_attempts, activations, datagrams_in, datagrams_out, echo_replies, dns_answers, last_secret_bytes })
	}
}

// interface `modem-fixture` over a channel: opcodes, a Service trait + dispatch, and a Client.
pub mod modem_fixture {
	use super::*;
	use crate::codec::{Reader, Sink, SliceWriter, Transport, TransportError, VecWriter};
	use alloc::vec::Vec;

	pub const OP_SET_SIM: u16 = 1;
	pub const OP_DELAY: u16 = 2;
	pub const OP_WITHDRAW: u16 = 3;
	pub const OP_REPUBLISH: u16 = 4;
	pub const OP_STATS: u16 = 5;
	pub const OP_REPLAY: u16 = 6;
	pub const OP_DROP_CONTEXT: u16 = 7;
	pub const OP_IPV6_ONLY: u16 = 8;

	pub trait Service {
		/// Replace the SIM: `present` or not, and a new SIM generation either way.
		fn set_sim(&mut self, present: bool, locked: bool) -> Result<(), Error>;
		/// Hold the next reply to a command of this kind for `delay-ms`; zero never answers it.
		fn delay(&mut self, kind: CommandKind, delay_ms: u32) -> Result<(), Error>;
		fn withdraw(&mut self) -> Result<(), Error>;
		fn republish(&mut self) -> Result<(), Error>;
		fn stats(&mut self) -> Result<FixtureStats, Error>;
		/// Send the last activation's reply again, stale: a duplicate the service must not act on.
		fn replay(&mut self) -> Result<(), Error>;
		/// The network ends the context on its own.
		fn drop_context(&mut self) -> Result<(), Error>;
		/// Answer the next activations with an IPv6-only configuration, or stop doing so.
		fn ipv6_only(&mut self, on: bool) -> Result<(), Error>;
	}

	pub fn dispatch<S: Service>(service: &mut S, request: &[u8], request_handles: &mut Handles, out: &mut [u8], reply_handles: &mut Handles) -> Option<usize> {
		let mut reader = Reader::with_handle_list(request, request_handles);
		let r = &mut reader;
		let op = r.u16()?;
		let corr = r.u32()?;
		let mut writer = SliceWriter::new(out);
		if op == PROTOCOL_INFO_OP {
			r.finish()?;
			request_handles.clear();
			let w = &mut writer;
			w.u32(corr)?;
			w.bytes_lp(b"liber:modem-device")?;
			w.u32(1)?;
			match Handles::try_from_slice(writer.handles()) {
				Some(taken) => *reply_handles = taken,
				None => return None,
			}
			return Some(writer.pos());
		}
		match op {
			OP_SET_SIM => {
				let present = r.boolean()?;
				let locked = r.boolean()?;
				r.finish()?;
				request_handles.clear();
				let result = service.set_sim(present, locked);
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v35) => {
							w.u8(1)?;
						}
						Err(v36) => {
							w.u8(0)?;
							v36.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			OP_DELAY => {
				let kind = CommandKind::read(r)?;
				let delay_ms = r.u32()?;
				r.finish()?;
				request_handles.clear();
				let result = service.delay(kind, delay_ms);
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v37) => {
							w.u8(1)?;
						}
						Err(v38) => {
							w.u8(0)?;
							v38.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			OP_WITHDRAW => {
				r.finish()?;
				request_handles.clear();
				let result = service.withdraw();
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v39) => {
							w.u8(1)?;
						}
						Err(v40) => {
							w.u8(0)?;
							v40.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			OP_REPUBLISH => {
				r.finish()?;
				request_handles.clear();
				let result = service.republish();
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v41) => {
							w.u8(1)?;
						}
						Err(v42) => {
							w.u8(0)?;
							v42.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			OP_STATS => {
				r.finish()?;
				request_handles.clear();
				let result = service.stats();
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v43) => {
							w.u8(1)?;
							v43.write(w)?;
						}
						Err(v44) => {
							w.u8(0)?;
							v44.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			OP_REPLAY => {
				r.finish()?;
				request_handles.clear();
				let result = service.replay();
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v45) => {
							w.u8(1)?;
						}
						Err(v46) => {
							w.u8(0)?;
							v46.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			OP_DROP_CONTEXT => {
				r.finish()?;
				request_handles.clear();
				let result = service.drop_context();
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v47) => {
							w.u8(1)?;
						}
						Err(v48) => {
							w.u8(0)?;
							v48.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			OP_IPV6_ONLY => {
				let on = r.boolean()?;
				r.finish()?;
				request_handles.clear();
				let result = service.ipv6_only(on);
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v49) => {
							w.u8(1)?;
						}
						Err(v50) => {
							w.u8(0)?;
							v50.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			_ => return None,
		}
		match Handles::try_from_slice(writer.handles()) {
			Some(taken) => *reply_handles = taken,
			None => return None,
		}
		Some(writer.pos())
	}

	fn transport_outcome(error: TransportError) -> Error {
		match error {
			// The request never left this process, so nothing happened and trying
			// again is safe - which is what `again` says.
			TransportError::SendRefused | TransportError::NoRoute => Error::Again,
			// It went out and no answer came back. The server may have acted before
			// it died or before the deadline; nobody knows, and `commit-uncertain` is
			// the answer `base.error` grew so a caller is not forced to guess.
			// The reply could not be held, or arrived and broke the framing rules. In
			// both the server ANSWERED, so it acted; this end simply cannot read what
			// it said, which is the same position as never hearing back.
			TransportError::PeerClosed | TransportError::ReceiveFailed | TransportError::TimedOut | TransportError::NoMemory | TransportError::Malformed => Error::CommitUncertain,
		}
	}

	pub struct Client<T: Transport> {
		transport: T,
		corr: u32,
		deadline: u64,
		last_error: Option<TransportError>,
	}

	impl<T: Transport> Client<T> {
		pub fn new(transport: T) -> Client<T> {
			Client { transport, corr: 0, deadline: 0, last_error: None }
		}
		pub fn with_deadline(transport: T, deadline: u64) -> Client<T> {
			Client { transport, corr: 0, deadline, last_error: None }
		}
		pub fn set_deadline(&mut self, deadline: u64) {
			self.deadline = deadline;
		}
		pub fn last_error(&self) -> Option<TransportError> {
			self.last_error
		}
		pub fn into_transport(self) -> T {
			self.transport
		}
		fn next_corr(&mut self) -> u32 {
			let c = self.corr;
			self.corr = self.corr.wrapping_add(1);
			c
		}
		pub fn protocol_info(&mut self) -> Option<(String, u32)> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(PROTOCOL_INFO_OP)?;
			w.u32(corr)?;
			// No parameter, so no capability: `into_inner` says so rather than this
			// line assuming it.
			let request = writer.into_inner()?;
			let mut reply_handles = Handles::new();
			let reply = self
				.transport
				.call(&request, &[], &mut reply_handles, self.deadline)
				.map_err(|e| {
					self.last_error = Some(e);
					e
				})
				.ok()?;
			if !reply_handles.is_empty() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			let mut reader = Reader::new(&reply);
			let r = &mut reader;
			if r.u32()? != corr {
				return None;
			}
			let package = r.string_lp()?;
			let version = r.u32()?;
			r.finish()?;
			Some((package, version))
		}
		pub fn set_sim(&mut self, present: &bool, locked: &bool) -> Option<Result<(), Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_SET_SIM)?;
			w.u32(corr)?;
			w.boolean(*present)?;
			w.boolean(*locked)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? { Ok(()) } else { Err(Error::read(r)?) };
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
		pub fn delay(&mut self, kind: &CommandKind, delay_ms: &u32) -> Option<Result<(), Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_DELAY)?;
			w.u32(corr)?;
			kind.write(w)?;
			w.u32(*delay_ms)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? { Ok(()) } else { Err(Error::read(r)?) };
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
		pub fn withdraw(&mut self) -> Option<Result<(), Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_WITHDRAW)?;
			w.u32(corr)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? { Ok(()) } else { Err(Error::read(r)?) };
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
		pub fn republish(&mut self) -> Option<Result<(), Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_REPUBLISH)?;
			w.u32(corr)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? { Ok(()) } else { Err(Error::read(r)?) };
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
		pub fn stats(&mut self) -> Option<Result<FixtureStats, Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_STATS)?;
			w.u32(corr)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? { Ok(FixtureStats::read(r)?) } else { Err(Error::read(r)?) };
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
		pub fn replay(&mut self) -> Option<Result<(), Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_REPLAY)?;
			w.u32(corr)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? { Ok(()) } else { Err(Error::read(r)?) };
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
		pub fn drop_context(&mut self) -> Option<Result<(), Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_DROP_CONTEXT)?;
			w.u32(corr)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? { Ok(()) } else { Err(Error::read(r)?) };
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
		pub fn ipv6_only(&mut self, on: &bool) -> Option<Result<(), Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_IPV6_ONLY)?;
			w.u32(corr)?;
			w.boolean(*on)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? { Ok(()) } else { Err(Error::read(r)?) };
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_modem_device_modem_fixture_set_sim")]
	fn channel_invoke_set_sim(chan: u64, present: &bool, locked: &bool) -> Option<Result<(), Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.set_sim(present, locked)
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_modem_device_modem_fixture_delay")]
	fn channel_invoke_delay(chan: u64, kind: &CommandKind, delay_ms: &u32) -> Option<Result<(), Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.delay(kind, delay_ms)
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_modem_device_modem_fixture_withdraw")]
	fn channel_invoke_withdraw(chan: u64) -> Option<Result<(), Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.withdraw()
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_modem_device_modem_fixture_republish")]
	fn channel_invoke_republish(chan: u64) -> Option<Result<(), Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.republish()
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_modem_device_modem_fixture_stats")]
	fn channel_invoke_stats(chan: u64) -> Option<Result<FixtureStats, Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.stats()
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_modem_device_modem_fixture_replay")]
	fn channel_invoke_replay(chan: u64) -> Option<Result<(), Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.replay()
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_modem_device_modem_fixture_drop_context")]
	fn channel_invoke_drop_context(chan: u64) -> Option<Result<(), Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.drop_context()
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_modem_device_modem_fixture_ipv6_only")]
	fn channel_invoke_ipv6_only(chan: u64, on: &bool) -> Option<Result<(), Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.ipv6_only(on)
	}
}

impl DeviceLimits {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"version\":");
		let _ = write!(out, "{}", self.version);
		out.push(',');
		out.push_str("\"pending\":");
		let _ = write!(out, "{}", self.pending);
		out.push(',');
		out.push_str("\"fragments\":");
		let _ = write!(out, "{}", self.fragments);
		out.push(',');
		out.push_str("\"message-bytes\":");
		let _ = write!(out, "{}", self.message_bytes);
		out.push(',');
		out.push_str("\"assembly-bytes\":");
		let _ = write!(out, "{}", self.assembly_bytes);
		out.push(',');
		out.push_str("\"assemblies\":");
		let _ = write!(out, "{}", self.assemblies);
		out.push(',');
		out.push_str("\"mtu\":");
		let _ = write!(out, "{}", self.mtu);
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("version=");
		let _ = write!(out, "{}", self.version);
		out.push_str(", ");
		out.push_str("pending=");
		let _ = write!(out, "{}", self.pending);
		out.push_str(", ");
		out.push_str("fragments=");
		let _ = write!(out, "{}", self.fragments);
		out.push_str(", ");
		out.push_str("message-bytes=");
		let _ = write!(out, "{}", self.message_bytes);
		out.push_str(", ");
		out.push_str("assembly-bytes=");
		let _ = write!(out, "{}", self.assembly_bytes);
		out.push_str(", ");
		out.push_str("assemblies=");
		let _ = write!(out, "{}", self.assemblies);
		out.push_str(", ");
		out.push_str("mtu=");
		let _ = write!(out, "{}", self.mtu);
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 7);
		crate::codec::cbor::text(out, "version");
		crate::codec::cbor::uint(out, self.version as u64);
		crate::codec::cbor::text(out, "pending");
		crate::codec::cbor::uint(out, self.pending as u64);
		crate::codec::cbor::text(out, "fragments");
		crate::codec::cbor::uint(out, self.fragments as u64);
		crate::codec::cbor::text(out, "message-bytes");
		crate::codec::cbor::uint(out, self.message_bytes as u64);
		crate::codec::cbor::text(out, "assembly-bytes");
		crate::codec::cbor::uint(out, self.assembly_bytes as u64);
		crate::codec::cbor::text(out, "assemblies");
		crate::codec::cbor::uint(out, self.assemblies as u64);
		crate::codec::cbor::text(out, "mtu");
		crate::codec::cbor::uint(out, self.mtu as u64);
	}
}

impl DeviceOpen {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"limits\":");
		self.limits.to_json_into(out);
		out.push(',');
		out.push_str("\"connection-generation\":");
		let _ = write!(out, "{}", self.connection_generation);
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("limits=");
		self.limits.to_text_into(out);
		out.push_str(", ");
		out.push_str("connection-generation=");
		let _ = write!(out, "{}", self.connection_generation);
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 2);
		crate::codec::cbor::text(out, "limits");
		self.limits.to_cbor_into(out);
		crate::codec::cbor::text(out, "connection-generation");
		crate::codec::cbor::uint(out, self.connection_generation as u64);
	}
}

impl DeviceSim {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		match self {
			DeviceSim::Unknown => out.push_str("\"unknown\""),
			DeviceSim::Absent => out.push_str("\"absent\""),
			DeviceSim::LockedPin => out.push_str("\"locked-pin\""),
			DeviceSim::LockedPuk => out.push_str("\"locked-puk\""),
			DeviceSim::Ready => out.push_str("\"ready\""),
			DeviceSim::Blocked => out.push_str("\"blocked\""),
		}
	}
	pub fn to_text_into(&self, out: &mut String) {
		match self {
			DeviceSim::Unknown => out.push_str("unknown"),
			DeviceSim::Absent => out.push_str("absent"),
			DeviceSim::LockedPin => out.push_str("locked-pin"),
			DeviceSim::LockedPuk => out.push_str("locked-puk"),
			DeviceSim::Ready => out.push_str("ready"),
			DeviceSim::Blocked => out.push_str("blocked"),
		}
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		match self {
			DeviceSim::Unknown => crate::codec::cbor::text(out, "unknown"),
			DeviceSim::Absent => crate::codec::cbor::text(out, "absent"),
			DeviceSim::LockedPin => crate::codec::cbor::text(out, "locked-pin"),
			DeviceSim::LockedPuk => crate::codec::cbor::text(out, "locked-puk"),
			DeviceSim::Ready => crate::codec::cbor::text(out, "ready"),
			DeviceSim::Blocked => crate::codec::cbor::text(out, "blocked"),
		}
	}
}

impl DeviceRegistration {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		match self {
			DeviceRegistration::Unknown => out.push_str("\"unknown\""),
			DeviceRegistration::NotRegistered => out.push_str("\"not-registered\""),
			DeviceRegistration::Searching => out.push_str("\"searching\""),
			DeviceRegistration::Home => out.push_str("\"home\""),
			DeviceRegistration::Roaming => out.push_str("\"roaming\""),
			DeviceRegistration::Denied => out.push_str("\"denied\""),
		}
	}
	pub fn to_text_into(&self, out: &mut String) {
		match self {
			DeviceRegistration::Unknown => out.push_str("unknown"),
			DeviceRegistration::NotRegistered => out.push_str("not-registered"),
			DeviceRegistration::Searching => out.push_str("searching"),
			DeviceRegistration::Home => out.push_str("home"),
			DeviceRegistration::Roaming => out.push_str("roaming"),
			DeviceRegistration::Denied => out.push_str("denied"),
		}
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		match self {
			DeviceRegistration::Unknown => crate::codec::cbor::text(out, "unknown"),
			DeviceRegistration::NotRegistered => crate::codec::cbor::text(out, "not-registered"),
			DeviceRegistration::Searching => crate::codec::cbor::text(out, "searching"),
			DeviceRegistration::Home => crate::codec::cbor::text(out, "home"),
			DeviceRegistration::Roaming => crate::codec::cbor::text(out, "roaming"),
			DeviceRegistration::Denied => crate::codec::cbor::text(out, "denied"),
		}
	}
}

impl DeviceState {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"manufacturer\":");
		crate::codec::json_escape(&self.manufacturer, out);
		out.push(',');
		out.push_str("\"model\":");
		crate::codec::json_escape(&self.model, out);
		out.push(',');
		out.push_str("\"sim\":");
		self.sim.to_json_into(out);
		out.push(',');
		out.push_str("\"sim-generation\":");
		let _ = write!(out, "{}", self.sim_generation);
		out.push(',');
		out.push_str("\"registration\":");
		self.registration.to_json_into(out);
		out.push(',');
		out.push_str("\"operator\":");
		match &self.operator {
			Some(v51) => {
				crate::codec::json_escape(v51, out);
			}
			None => {
				out.push_str("null");
			}
		}
		out.push(',');
		out.push_str("\"signal-valid\":");
		if self.signal_valid {
			out.push_str("true");
		} else {
			out.push_str("false");
		}
		out.push(',');
		out.push_str("\"rssi-dbm\":");
		let _ = write!(out, "{}", self.rssi_dbm);
		out.push(',');
		out.push_str("\"quality\":");
		let _ = write!(out, "{}", self.quality);
		out.push(',');
		out.push_str("\"pin-attempts\":");
		match &self.pin_attempts {
			Some(v52) => {
				let _ = write!(out, "{}", v52);
			}
			None => {
				out.push_str("null");
			}
		}
		out.push(',');
		out.push_str("\"puk-attempts\":");
		match &self.puk_attempts {
			Some(v53) => {
				let _ = write!(out, "{}", v53);
			}
			None => {
				out.push_str("null");
			}
		}
		out.push(',');
		out.push_str("\"context-active\":");
		if self.context_active {
			out.push_str("true");
		} else {
			out.push_str("false");
		}
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("manufacturer=");
		out.push_str(&self.manufacturer);
		out.push_str(", ");
		out.push_str("model=");
		out.push_str(&self.model);
		out.push_str(", ");
		out.push_str("sim=");
		self.sim.to_text_into(out);
		out.push_str(", ");
		out.push_str("sim-generation=");
		let _ = write!(out, "{}", self.sim_generation);
		out.push_str(", ");
		out.push_str("registration=");
		self.registration.to_text_into(out);
		out.push_str(", ");
		out.push_str("operator=");
		match &self.operator {
			Some(v54) => {
				out.push_str(v54);
			}
			None => {
				out.push('-');
			}
		}
		out.push_str(", ");
		out.push_str("signal-valid=");
		if self.signal_valid {
			out.push_str("true");
		} else {
			out.push_str("false");
		}
		out.push_str(", ");
		out.push_str("rssi-dbm=");
		let _ = write!(out, "{}", self.rssi_dbm);
		out.push_str(", ");
		out.push_str("quality=");
		let _ = write!(out, "{}", self.quality);
		out.push_str(", ");
		out.push_str("pin-attempts=");
		match &self.pin_attempts {
			Some(v55) => {
				let _ = write!(out, "{}", v55);
			}
			None => {
				out.push('-');
			}
		}
		out.push_str(", ");
		out.push_str("puk-attempts=");
		match &self.puk_attempts {
			Some(v56) => {
				let _ = write!(out, "{}", v56);
			}
			None => {
				out.push('-');
			}
		}
		out.push_str(", ");
		out.push_str("context-active=");
		if self.context_active {
			out.push_str("true");
		} else {
			out.push_str("false");
		}
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 12);
		crate::codec::cbor::text(out, "manufacturer");
		crate::codec::cbor::text(out, &self.manufacturer);
		crate::codec::cbor::text(out, "model");
		crate::codec::cbor::text(out, &self.model);
		crate::codec::cbor::text(out, "sim");
		self.sim.to_cbor_into(out);
		crate::codec::cbor::text(out, "sim-generation");
		crate::codec::cbor::uint(out, self.sim_generation as u64);
		crate::codec::cbor::text(out, "registration");
		self.registration.to_cbor_into(out);
		crate::codec::cbor::text(out, "operator");
		match &self.operator {
			Some(v57) => {
				crate::codec::cbor::text(out, v57);
			}
			None => {
				crate::codec::cbor::null(out);
			}
		}
		crate::codec::cbor::text(out, "signal-valid");
		crate::codec::cbor::boolean(out, self.signal_valid);
		crate::codec::cbor::text(out, "rssi-dbm");
		crate::codec::cbor::int(out, self.rssi_dbm as i64);
		crate::codec::cbor::text(out, "quality");
		crate::codec::cbor::uint(out, self.quality as u64);
		crate::codec::cbor::text(out, "pin-attempts");
		match &self.pin_attempts {
			Some(v58) => {
				crate::codec::cbor::uint(out, *v58 as u64);
			}
			None => {
				crate::codec::cbor::null(out);
			}
		}
		crate::codec::cbor::text(out, "puk-attempts");
		match &self.puk_attempts {
			Some(v59) => {
				crate::codec::cbor::uint(out, *v59 as u64);
			}
			None => {
				crate::codec::cbor::null(out);
			}
		}
		crate::codec::cbor::text(out, "context-active");
		crate::codec::cbor::boolean(out, self.context_active);
	}
}

impl IpFamily {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		match self {
			IpFamily::Ipv4 => out.push_str("\"ipv4\""),
			IpFamily::Ipv6 => out.push_str("\"ipv6\""),
		}
	}
	pub fn to_text_into(&self, out: &mut String) {
		match self {
			IpFamily::Ipv4 => out.push_str("ipv4"),
			IpFamily::Ipv6 => out.push_str("ipv6"),
		}
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		match self {
			IpFamily::Ipv4 => crate::codec::cbor::text(out, "ipv4"),
			IpFamily::Ipv6 => crate::codec::cbor::text(out, "ipv6"),
		}
	}
}

impl IpConfig {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"family\":");
		self.family.to_json_into(out);
		out.push(',');
		out.push_str("\"address\":");
		let _ = write!(out, "{}", self.address);
		out.push(',');
		out.push_str("\"prefix\":");
		let _ = write!(out, "{}", self.prefix);
		out.push(',');
		out.push_str("\"gateway\":");
		match &self.gateway {
			Some(v60) => {
				let _ = write!(out, "{}", v60);
			}
			None => {
				out.push_str("null");
			}
		}
		out.push(',');
		out.push_str("\"dns\":");
		out.push('[');
		let mut v62 = true;
		for v61 in self.dns.iter() {
			if !v62 {
				out.push(',');
			}
			v62 = false;
			let _ = write!(out, "{}", v61);
		}
		out.push(']');
		out.push(',');
		out.push_str("\"mtu\":");
		let _ = write!(out, "{}", self.mtu);
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("family=");
		self.family.to_text_into(out);
		out.push_str(", ");
		out.push_str("address=");
		let _ = write!(out, "{}", self.address);
		out.push_str(", ");
		out.push_str("prefix=");
		let _ = write!(out, "{}", self.prefix);
		out.push_str(", ");
		out.push_str("gateway=");
		match &self.gateway {
			Some(v63) => {
				let _ = write!(out, "{}", v63);
			}
			None => {
				out.push('-');
			}
		}
		out.push_str(", ");
		out.push_str("dns=");
		out.push('[');
		let mut v65 = true;
		for v64 in self.dns.iter() {
			if !v65 {
				out.push_str(", ");
			}
			v65 = false;
			let _ = write!(out, "{}", v64);
		}
		out.push(']');
		out.push_str(", ");
		out.push_str("mtu=");
		let _ = write!(out, "{}", self.mtu);
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 6);
		crate::codec::cbor::text(out, "family");
		self.family.to_cbor_into(out);
		crate::codec::cbor::text(out, "address");
		crate::codec::cbor::uint(out, self.address as u64);
		crate::codec::cbor::text(out, "prefix");
		crate::codec::cbor::uint(out, self.prefix as u64);
		crate::codec::cbor::text(out, "gateway");
		match &self.gateway {
			Some(v66) => {
				crate::codec::cbor::uint(out, *v66 as u64);
			}
			None => {
				crate::codec::cbor::null(out);
			}
		}
		crate::codec::cbor::text(out, "dns");
		crate::codec::cbor::array(out, self.dns.len());
		for v67 in self.dns.iter() {
			crate::codec::cbor::uint(out, *v67 as u64);
		}
		crate::codec::cbor::text(out, "mtu");
		crate::codec::cbor::uint(out, self.mtu as u64);
	}
}

impl DeviceIdentity {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"imsi\":");
		match &self.imsi {
			Some(v68) => {
				crate::codec::json_escape(v68, out);
			}
			None => {
				out.push_str("null");
			}
		}
		out.push(',');
		out.push_str("\"iccid\":");
		match &self.iccid {
			Some(v69) => {
				crate::codec::json_escape(v69, out);
			}
			None => {
				out.push_str("null");
			}
		}
		out.push(',');
		out.push_str("\"msisdn\":");
		match &self.msisdn {
			Some(v70) => {
				crate::codec::json_escape(v70, out);
			}
			None => {
				out.push_str("null");
			}
		}
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("imsi=");
		match &self.imsi {
			Some(v71) => {
				out.push_str(v71);
			}
			None => {
				out.push('-');
			}
		}
		out.push_str(", ");
		out.push_str("iccid=");
		match &self.iccid {
			Some(v72) => {
				out.push_str(v72);
			}
			None => {
				out.push('-');
			}
		}
		out.push_str(", ");
		out.push_str("msisdn=");
		match &self.msisdn {
			Some(v73) => {
				out.push_str(v73);
			}
			None => {
				out.push('-');
			}
		}
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 3);
		crate::codec::cbor::text(out, "imsi");
		match &self.imsi {
			Some(v74) => {
				crate::codec::cbor::text(out, v74);
			}
			None => {
				crate::codec::cbor::null(out);
			}
		}
		crate::codec::cbor::text(out, "iccid");
		match &self.iccid {
			Some(v75) => {
				crate::codec::cbor::text(out, v75);
			}
			None => {
				crate::codec::cbor::null(out);
			}
		}
		crate::codec::cbor::text(out, "msisdn");
		match &self.msisdn {
			Some(v76) => {
				crate::codec::cbor::text(out, v76);
			}
			None => {
				crate::codec::cbor::null(out);
			}
		}
	}
}

impl CommandKind {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		match self {
			CommandKind::Query => out.push_str("\"query\""),
			CommandKind::EnterPin => out.push_str("\"enter-pin\""),
			CommandKind::EnterPuk => out.push_str("\"enter-puk\""),
			CommandKind::Activate => out.push_str("\"activate\""),
			CommandKind::Deactivate => out.push_str("\"deactivate\""),
			CommandKind::Identity => out.push_str("\"identity\""),
		}
	}
	pub fn to_text_into(&self, out: &mut String) {
		match self {
			CommandKind::Query => out.push_str("query"),
			CommandKind::EnterPin => out.push_str("enter-pin"),
			CommandKind::EnterPuk => out.push_str("enter-puk"),
			CommandKind::Activate => out.push_str("activate"),
			CommandKind::Deactivate => out.push_str("deactivate"),
			CommandKind::Identity => out.push_str("identity"),
		}
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		match self {
			CommandKind::Query => crate::codec::cbor::text(out, "query"),
			CommandKind::EnterPin => crate::codec::cbor::text(out, "enter-pin"),
			CommandKind::EnterPuk => crate::codec::cbor::text(out, "enter-puk"),
			CommandKind::Activate => crate::codec::cbor::text(out, "activate"),
			CommandKind::Deactivate => crate::codec::cbor::text(out, "deactivate"),
			CommandKind::Identity => crate::codec::cbor::text(out, "identity"),
		}
	}
}

impl Command {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"connection-generation\":");
		let _ = write!(out, "{}", self.connection_generation);
		out.push(',');
		out.push_str("\"transaction\":");
		let _ = write!(out, "{}", self.transaction);
		out.push(',');
		out.push_str("\"kind\":");
		self.kind.to_json_into(out);
		out.push(',');
		out.push_str("\"sim-generation\":");
		let _ = write!(out, "{}", self.sim_generation);
		out.push(',');
		out.push_str("\"context-generation\":");
		let _ = write!(out, "{}", self.context_generation);
		out.push(',');
		out.push_str("\"secret\":");
		out.push('[');
		let mut v78 = true;
		for v77 in self.secret.iter() {
			if !v78 {
				out.push(',');
			}
			v78 = false;
			let _ = write!(out, "{}", v77);
		}
		out.push(']');
		out.push(',');
		out.push_str("\"new-secret\":");
		out.push('[');
		let mut v80 = true;
		for v79 in self.new_secret.iter() {
			if !v80 {
				out.push(',');
			}
			v80 = false;
			let _ = write!(out, "{}", v79);
		}
		out.push(']');
		out.push(',');
		out.push_str("\"apn\":");
		crate::codec::json_escape(&self.apn, out);
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("connection-generation=");
		let _ = write!(out, "{}", self.connection_generation);
		out.push_str(", ");
		out.push_str("transaction=");
		let _ = write!(out, "{}", self.transaction);
		out.push_str(", ");
		out.push_str("kind=");
		self.kind.to_text_into(out);
		out.push_str(", ");
		out.push_str("sim-generation=");
		let _ = write!(out, "{}", self.sim_generation);
		out.push_str(", ");
		out.push_str("context-generation=");
		let _ = write!(out, "{}", self.context_generation);
		out.push_str(", ");
		out.push_str("secret=");
		out.push('[');
		let mut v82 = true;
		for v81 in self.secret.iter() {
			if !v82 {
				out.push_str(", ");
			}
			v82 = false;
			let _ = write!(out, "{}", v81);
		}
		out.push(']');
		out.push_str(", ");
		out.push_str("new-secret=");
		out.push('[');
		let mut v84 = true;
		for v83 in self.new_secret.iter() {
			if !v84 {
				out.push_str(", ");
			}
			v84 = false;
			let _ = write!(out, "{}", v83);
		}
		out.push(']');
		out.push_str(", ");
		out.push_str("apn=");
		out.push_str(&self.apn);
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 8);
		crate::codec::cbor::text(out, "connection-generation");
		crate::codec::cbor::uint(out, self.connection_generation as u64);
		crate::codec::cbor::text(out, "transaction");
		crate::codec::cbor::uint(out, self.transaction as u64);
		crate::codec::cbor::text(out, "kind");
		self.kind.to_cbor_into(out);
		crate::codec::cbor::text(out, "sim-generation");
		crate::codec::cbor::uint(out, self.sim_generation as u64);
		crate::codec::cbor::text(out, "context-generation");
		crate::codec::cbor::uint(out, self.context_generation as u64);
		crate::codec::cbor::text(out, "secret");
		crate::codec::cbor::array(out, self.secret.len());
		for v85 in self.secret.iter() {
			crate::codec::cbor::uint(out, *v85 as u64);
		}
		crate::codec::cbor::text(out, "new-secret");
		crate::codec::cbor::array(out, self.new_secret.len());
		for v86 in self.new_secret.iter() {
			crate::codec::cbor::uint(out, *v86 as u64);
		}
		crate::codec::cbor::text(out, "apn");
		crate::codec::cbor::text(out, &self.apn);
	}
}

impl CommandStatus {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		match self {
			CommandStatus::Done => out.push_str("\"done\""),
			CommandStatus::Rejected => out.push_str("\"rejected\""),
			CommandStatus::Failed => out.push_str("\"failed\""),
			CommandStatus::Stale => out.push_str("\"stale\""),
		}
	}
	pub fn to_text_into(&self, out: &mut String) {
		match self {
			CommandStatus::Done => out.push_str("done"),
			CommandStatus::Rejected => out.push_str("rejected"),
			CommandStatus::Failed => out.push_str("failed"),
			CommandStatus::Stale => out.push_str("stale"),
		}
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		match self {
			CommandStatus::Done => crate::codec::cbor::text(out, "done"),
			CommandStatus::Rejected => crate::codec::cbor::text(out, "rejected"),
			CommandStatus::Failed => crate::codec::cbor::text(out, "failed"),
			CommandStatus::Stale => crate::codec::cbor::text(out, "stale"),
		}
	}
}

impl CommandReply {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"connection-generation\":");
		let _ = write!(out, "{}", self.connection_generation);
		out.push(',');
		out.push_str("\"transaction\":");
		let _ = write!(out, "{}", self.transaction);
		out.push(',');
		out.push_str("\"kind\":");
		self.kind.to_json_into(out);
		out.push(',');
		out.push_str("\"status\":");
		self.status.to_json_into(out);
		out.push(',');
		out.push_str("\"sim-generation\":");
		let _ = write!(out, "{}", self.sim_generation);
		out.push(',');
		out.push_str("\"context-generation\":");
		let _ = write!(out, "{}", self.context_generation);
		out.push(',');
		out.push_str("\"state\":");
		match &self.state {
			Some(v87) => {
				v87.to_json_into(out);
			}
			None => {
				out.push_str("null");
			}
		}
		out.push(',');
		out.push_str("\"config\":");
		match &self.config {
			Some(v88) => {
				v88.to_json_into(out);
			}
			None => {
				out.push_str("null");
			}
		}
		out.push(',');
		out.push_str("\"identity\":");
		match &self.identity {
			Some(v89) => {
				v89.to_json_into(out);
			}
			None => {
				out.push_str("null");
			}
		}
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("connection-generation=");
		let _ = write!(out, "{}", self.connection_generation);
		out.push_str(", ");
		out.push_str("transaction=");
		let _ = write!(out, "{}", self.transaction);
		out.push_str(", ");
		out.push_str("kind=");
		self.kind.to_text_into(out);
		out.push_str(", ");
		out.push_str("status=");
		self.status.to_text_into(out);
		out.push_str(", ");
		out.push_str("sim-generation=");
		let _ = write!(out, "{}", self.sim_generation);
		out.push_str(", ");
		out.push_str("context-generation=");
		let _ = write!(out, "{}", self.context_generation);
		out.push_str(", ");
		out.push_str("state=");
		match &self.state {
			Some(v90) => {
				v90.to_text_into(out);
			}
			None => {
				out.push('-');
			}
		}
		out.push_str(", ");
		out.push_str("config=");
		match &self.config {
			Some(v91) => {
				v91.to_text_into(out);
			}
			None => {
				out.push('-');
			}
		}
		out.push_str(", ");
		out.push_str("identity=");
		match &self.identity {
			Some(v92) => {
				v92.to_text_into(out);
			}
			None => {
				out.push('-');
			}
		}
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 9);
		crate::codec::cbor::text(out, "connection-generation");
		crate::codec::cbor::uint(out, self.connection_generation as u64);
		crate::codec::cbor::text(out, "transaction");
		crate::codec::cbor::uint(out, self.transaction as u64);
		crate::codec::cbor::text(out, "kind");
		self.kind.to_cbor_into(out);
		crate::codec::cbor::text(out, "status");
		self.status.to_cbor_into(out);
		crate::codec::cbor::text(out, "sim-generation");
		crate::codec::cbor::uint(out, self.sim_generation as u64);
		crate::codec::cbor::text(out, "context-generation");
		crate::codec::cbor::uint(out, self.context_generation as u64);
		crate::codec::cbor::text(out, "state");
		match &self.state {
			Some(v93) => {
				v93.to_cbor_into(out);
			}
			None => {
				crate::codec::cbor::null(out);
			}
		}
		crate::codec::cbor::text(out, "config");
		match &self.config {
			Some(v94) => {
				v94.to_cbor_into(out);
			}
			None => {
				crate::codec::cbor::null(out);
			}
		}
		crate::codec::cbor::text(out, "identity");
		match &self.identity {
			Some(v95) => {
				v95.to_cbor_into(out);
			}
			None => {
				crate::codec::cbor::null(out);
			}
		}
	}
}

impl Indication {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"revision\":");
		let _ = write!(out, "{}", self.revision);
		out.push(',');
		out.push_str("\"state\":");
		self.state.to_json_into(out);
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("revision=");
		let _ = write!(out, "{}", self.revision);
		out.push_str(", ");
		out.push_str("state=");
		self.state.to_text_into(out);
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 2);
		crate::codec::cbor::text(out, "revision");
		crate::codec::cbor::uint(out, self.revision as u64);
		crate::codec::cbor::text(out, "state");
		self.state.to_cbor_into(out);
	}
}

impl Datagram {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"context-generation\":");
		let _ = write!(out, "{}", self.context_generation);
		out.push(',');
		out.push_str("\"bytes\":");
		out.push('[');
		let mut v97 = true;
		for v96 in self.bytes.iter() {
			if !v97 {
				out.push(',');
			}
			v97 = false;
			let _ = write!(out, "{}", v96);
		}
		out.push(']');
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("context-generation=");
		let _ = write!(out, "{}", self.context_generation);
		out.push_str(", ");
		out.push_str("bytes=");
		out.push('[');
		let mut v99 = true;
		for v98 in self.bytes.iter() {
			if !v99 {
				out.push_str(", ");
			}
			v99 = false;
			let _ = write!(out, "{}", v98);
		}
		out.push(']');
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 2);
		crate::codec::cbor::text(out, "context-generation");
		crate::codec::cbor::uint(out, self.context_generation as u64);
		crate::codec::cbor::text(out, "bytes");
		crate::codec::cbor::array(out, self.bytes.len());
		for v100 in self.bytes.iter() {
			crate::codec::cbor::uint(out, *v100 as u64);
		}
	}
}

impl FixtureStats {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"commands\":");
		let _ = write!(out, "{}", self.commands);
		out.push(',');
		out.push_str("\"pin-attempts\":");
		let _ = write!(out, "{}", self.pin_attempts);
		out.push(',');
		out.push_str("\"activations\":");
		let _ = write!(out, "{}", self.activations);
		out.push(',');
		out.push_str("\"datagrams-in\":");
		let _ = write!(out, "{}", self.datagrams_in);
		out.push(',');
		out.push_str("\"datagrams-out\":");
		let _ = write!(out, "{}", self.datagrams_out);
		out.push(',');
		out.push_str("\"echo-replies\":");
		let _ = write!(out, "{}", self.echo_replies);
		out.push(',');
		out.push_str("\"dns-answers\":");
		let _ = write!(out, "{}", self.dns_answers);
		out.push(',');
		out.push_str("\"last-secret-bytes\":");
		let _ = write!(out, "{}", self.last_secret_bytes);
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("commands=");
		let _ = write!(out, "{}", self.commands);
		out.push_str(", ");
		out.push_str("pin-attempts=");
		let _ = write!(out, "{}", self.pin_attempts);
		out.push_str(", ");
		out.push_str("activations=");
		let _ = write!(out, "{}", self.activations);
		out.push_str(", ");
		out.push_str("datagrams-in=");
		let _ = write!(out, "{}", self.datagrams_in);
		out.push_str(", ");
		out.push_str("datagrams-out=");
		let _ = write!(out, "{}", self.datagrams_out);
		out.push_str(", ");
		out.push_str("echo-replies=");
		let _ = write!(out, "{}", self.echo_replies);
		out.push_str(", ");
		out.push_str("dns-answers=");
		let _ = write!(out, "{}", self.dns_answers);
		out.push_str(", ");
		out.push_str("last-secret-bytes=");
		let _ = write!(out, "{}", self.last_secret_bytes);
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 8);
		crate::codec::cbor::text(out, "commands");
		crate::codec::cbor::uint(out, self.commands as u64);
		crate::codec::cbor::text(out, "pin-attempts");
		crate::codec::cbor::uint(out, self.pin_attempts as u64);
		crate::codec::cbor::text(out, "activations");
		crate::codec::cbor::uint(out, self.activations as u64);
		crate::codec::cbor::text(out, "datagrams-in");
		crate::codec::cbor::uint(out, self.datagrams_in as u64);
		crate::codec::cbor::text(out, "datagrams-out");
		crate::codec::cbor::uint(out, self.datagrams_out as u64);
		crate::codec::cbor::text(out, "echo-replies");
		crate::codec::cbor::uint(out, self.echo_replies as u64);
		crate::codec::cbor::text(out, "dns-answers");
		crate::codec::cbor::uint(out, self.dns_answers as u64);
		crate::codec::cbor::text(out, "last-secret-bytes");
		crate::codec::cbor::uint(out, self.last_secret_bytes as u64);
	}
}

#[cfg(test)]
mod compat;
