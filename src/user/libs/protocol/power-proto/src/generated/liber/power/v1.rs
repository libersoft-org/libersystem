// @generated by lsidl-gen from ../../idl/power.lsidl (package liber:power@1).
// Do not edit by hand; regenerate with `./gen.sh`.
//! LiberSystem power state - what power sources and thermal zones report, normalised, and the few
//! device controls an operator may use.
//!
//! THREE INTERFACES FOR THREE PARTIES. A READER sees normalised state and nothing it could change; an
//! OPERATOR may switch an advertised output of a device such as a UPS; a PROVIDER - a driver - publishes
//! what its hardware reports. None of them reaches the others' operations, because what separates them
//! is who holds them.
//!
//! THIS IS NOT MACHINE SHUTDOWN. Rebooting and powering off the host stay with SystemManager's
//! `system-power`; this service holds no client of it. Turning a UPS's output off turns the UPS's
//! output off. Suspend, fan curves and power policy are not here at all.
#![allow(dead_code, unused_imports, unused_variables, unused_mut, clippy::all)]

use crate::codec::{Handles, PROTOCOL_INFO_OP, Reader, Sink, SliceWriter, VecWriter};
use alloc::string::String;
use alloc::vec::Vec;
use core::fmt::Write as _;

pub use crate::generated::liber::base::v1::Error;

/// What kind of thing a source is.
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
#[repr(u8)]
pub enum SourceKind {
	Ac = 1,
	Battery = 2,
	Ups = 3,
	Load = 4,
	ThermalZone = 5,
}

impl SourceKind {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<SourceKind> {
		let mut r = Reader::new(bytes);
		let value = SourceKind::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<SourceKind> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = SourceKind::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u8(*self as u8)
	}
	pub fn read(r: &mut Reader) -> Option<SourceKind> {
		match r.u8()? {
			1 => Some(SourceKind::Ac),
			2 => Some(SourceKind::Battery),
			3 => Some(SourceKind::Ups),
			4 => Some(SourceKind::Load),
			5 => Some(SourceKind::ThermalZone),
			_ => None,
		}
	}
}

/// WHETHER A MEASUREMENT IS A MEASUREMENT. Every value below carries one of these, and none of them is
/// ever encoded as zero or as the largest integer: an unknown charge is not an empty battery, and an
/// unsupported rate is not a rate of four billion.
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
#[repr(u8)]
pub enum ValueState {
	Known = 1,
	Unknown = 2,
	Unsupported = 3,
	/// The source reported something that cannot be a value - a sentinel, an overflow, a contradiction,
	/// a unit that does not convert. `reason` says which, from a bounded set.
	Invalid = 4,
}

impl ValueState {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<ValueState> {
		let mut r = Reader::new(bytes);
		let value = ValueState::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<ValueState> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = ValueState::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u8(*self as u8)
	}
	pub fn read(r: &mut Reader) -> Option<ValueState> {
		match r.u8()? {
			1 => Some(ValueState::Known),
			2 => Some(ValueState::Unknown),
			3 => Some(ValueState::Unsupported),
			4 => Some(ValueState::Invalid),
			_ => None,
		}
	}
}

/// Why a value is invalid.
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
#[repr(u8)]
pub enum InvalidReason {
	None = 0,
	Sentinel = 1,
	Overflow = 2,
	Contradiction = 3,
	Unit = 4,
	Range = 5,
}

impl InvalidReason {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<InvalidReason> {
		let mut r = Reader::new(bytes);
		let value = InvalidReason::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<InvalidReason> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = InvalidReason::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u8(*self as u8)
	}
	pub fn read(r: &mut Reader) -> Option<InvalidReason> {
		match r.u8()? {
			0 => Some(InvalidReason::None),
			1 => Some(InvalidReason::Sentinel),
			2 => Some(InvalidReason::Overflow),
			3 => Some(InvalidReason::Contradiction),
			4 => Some(InvalidReason::Unit),
			5 => Some(InvalidReason::Range),
			_ => None,
		}
	}
}

/// A three-state fact: a presence or an online state that may simply not be known.
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
#[repr(u8)]
pub enum Tristate {
	Unknown = 0,
	No = 1,
	Yes = 2,
}

impl Tristate {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<Tristate> {
		let mut r = Reader::new(bytes);
		let value = Tristate::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<Tristate> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = Tristate::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u8(*self as u8)
	}
	pub fn read(r: &mut Reader) -> Option<Tristate> {
		match r.u8()? {
			0 => Some(Tristate::Unknown),
			1 => Some(Tristate::No),
			2 => Some(Tristate::Yes),
			_ => None,
		}
	}
}

#[derive(Clone, Debug, PartialEq)]
pub struct MeasureU64 {
	pub state: ValueState,
	pub value: u64,
	pub reason: InvalidReason,
}

impl MeasureU64 {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<MeasureU64> {
		let mut r = Reader::new(bytes);
		let value = MeasureU64::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<MeasureU64> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = MeasureU64::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		self.state.write(w)?;
		w.u64(self.value)?;
		self.reason.write(w)?;
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<MeasureU64> {
		let state = ValueState::read(r)?;
		let value = r.u64()?;
		let reason = InvalidReason::read(r)?;
		Some(MeasureU64 { state, value, reason })
	}
}

#[derive(Clone, Debug, PartialEq)]
pub struct MeasureI64 {
	pub state: ValueState,
	pub value: i64,
	pub reason: InvalidReason,
}

impl MeasureI64 {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<MeasureI64> {
		let mut r = Reader::new(bytes);
		let value = MeasureI64::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<MeasureI64> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = MeasureI64::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		self.state.write(w)?;
		w.i64(self.value)?;
		self.reason.write(w)?;
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<MeasureI64> {
		let state = ValueState::read(r)?;
		let value = r.i64()?;
		let reason = InvalidReason::read(r)?;
		Some(MeasureI64 { state, value, reason })
	}
}

/// Energy and charge are different physical quantities, and a capacity says which it is.
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
#[repr(u8)]
pub enum Quantity {
	/// Microwatt-hours.
	Energy = 1,
	/// Microamp-hours.
	Charge = 2,
}

impl Quantity {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<Quantity> {
		let mut r = Reader::new(bytes);
		let value = Quantity::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<Quantity> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = Quantity::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u8(*self as u8)
	}
	pub fn read(r: &mut Reader) -> Option<Quantity> {
		match r.u8()? {
			1 => Some(Quantity::Energy),
			2 => Some(Quantity::Charge),
			_ => None,
		}
	}
}

#[derive(Clone, Debug, PartialEq)]
pub struct Capacity {
	pub state: ValueState,
	pub quantity: Quantity,
	pub value: u64,
	pub reason: InvalidReason,
}

impl Capacity {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<Capacity> {
		let mut r = Reader::new(bytes);
		let value = Capacity::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<Capacity> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = Capacity::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		self.state.write(w)?;
		self.quantity.write(w)?;
		w.u64(self.value)?;
		self.reason.write(w)?;
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<Capacity> {
		let state = ValueState::read(r)?;
		let quantity = Quantity::read(r)?;
		let value = r.u64()?;
		let reason = InvalidReason::read(r)?;
		Some(Capacity { state, quantity, value, reason })
	}
}

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
#[repr(u8)]
pub enum ChargeState {
	Unknown = 0,
	Charging = 1,
	Discharging = 2,
	Idle = 3,
	/// The source said it was charging AND discharging. Not a state: a contradiction, reported as one
	/// rather than resolved by picking either.
	Invalid = 4,
}

impl ChargeState {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<ChargeState> {
		let mut r = Reader::new(bytes);
		let value = ChargeState::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<ChargeState> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = ChargeState::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u8(*self as u8)
	}
	pub fn read(r: &mut Reader) -> Option<ChargeState> {
		match r.u8()? {
			0 => Some(ChargeState::Unknown),
			1 => Some(ChargeState::Charging),
			2 => Some(ChargeState::Discharging),
			3 => Some(ChargeState::Idle),
			4 => Some(ChargeState::Invalid),
			_ => None,
		}
	}
}

/// Whether a temperature is absolute, or an offset from its zone's critical reference.
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
#[repr(u8)]
pub enum TemperatureReference {
	Absolute = 1,
	Relative = 2,
}

impl TemperatureReference {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<TemperatureReference> {
		let mut r = Reader::new(bytes);
		let value = TemperatureReference::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<TemperatureReference> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = TemperatureReference::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u8(*self as u8)
	}
	pub fn read(r: &mut Reader) -> Option<TemperatureReference> {
		match r.u8()? {
			1 => Some(TemperatureReference::Absolute),
			2 => Some(TemperatureReference::Relative),
			_ => None,
		}
	}
}

/// Millidegrees Celsius, absolute or relative - and comparable only with a value on the same reference.
#[derive(Clone, Debug, PartialEq)]
pub struct Temperature {
	pub state: ValueState,
	pub reference: TemperatureReference,
	pub value: i64,
	pub reason: InvalidReason,
}

impl Temperature {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<Temperature> {
		let mut r = Reader::new(bytes);
		let value = Temperature::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<Temperature> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = Temperature::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		self.state.write(w)?;
		self.reference.write(w)?;
		w.i64(self.value)?;
		self.reason.write(w)?;
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<Temperature> {
		let state = ValueState::read(r)?;
		let reference = TemperatureReference::read(r)?;
		let value = r.i64()?;
		let reason = InvalidReason::read(r)?;
		Some(Temperature { state, reference, value, reason })
	}
}

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
#[repr(u8)]
pub enum TripKind {
	Passive = 1,
	Active = 2,
	Hot = 3,
	Critical = 4,
}

impl TripKind {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<TripKind> {
		let mut r = Reader::new(bytes);
		let value = TripKind::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<TripKind> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = TripKind::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u8(*self as u8)
	}
	pub fn read(r: &mut Reader) -> Option<TripKind> {
		match r.u8()? {
			1 => Some(TripKind::Passive),
			2 => Some(TripKind::Active),
			3 => Some(TripKind::Hot),
			4 => Some(TripKind::Critical),
			_ => None,
		}
	}
}

/// A threshold a thermal zone declares. An observation, not an instruction.
#[derive(Clone, Debug, PartialEq)]
pub struct TripPoint {
	pub kind: TripKind,
	pub index: u8,
	pub temperature: Temperature,
}

impl TripPoint {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<TripPoint> {
		let mut r = Reader::new(bytes);
		let value = TripPoint::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<TripPoint> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = TripPoint::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		self.kind.write(w)?;
		w.u8(self.index)?;
		self.temperature.write(w)?;
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<TripPoint> {
		let kind = TripKind::read(r)?;
		let index = r.u8()?;
		let temperature = Temperature::read(r)?;
		Some(TripPoint { kind, index, temperature })
	}
}

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
#[repr(u8)]
pub enum AlarmKind {
	OnBattery = 1,
	LowCapacity = 2,
	CriticalCapacity = 3,
	Overload = 4,
	ReplaceBattery = 5,
	SourceFault = 6,
	OverTemperature = 7,
}

impl AlarmKind {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<AlarmKind> {
		let mut r = Reader::new(bytes);
		let value = AlarmKind::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<AlarmKind> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = AlarmKind::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u8(*self as u8)
	}
	pub fn read(r: &mut Reader) -> Option<AlarmKind> {
		match r.u8()? {
			1 => Some(AlarmKind::OnBattery),
			2 => Some(AlarmKind::LowCapacity),
			3 => Some(AlarmKind::CriticalCapacity),
			4 => Some(AlarmKind::Overload),
			5 => Some(AlarmKind::ReplaceBattery),
			6 => Some(AlarmKind::SourceFault),
			7 => Some(AlarmKind::OverTemperature),
			_ => None,
		}
	}
}

/// Whether an alarm was REPORTED by the source or DERIVED here from an explicitly defined comparison.
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
#[repr(u8)]
pub enum Provenance {
	Reported = 1,
	Derived = 2,
}

impl Provenance {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<Provenance> {
		let mut r = Reader::new(bytes);
		let value = Provenance::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<Provenance> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = Provenance::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u8(*self as u8)
	}
	pub fn read(r: &mut Reader) -> Option<Provenance> {
		match r.u8()? {
			1 => Some(Provenance::Reported),
			2 => Some(Provenance::Derived),
			_ => None,
		}
	}
}

#[derive(Clone, Debug, PartialEq)]
pub struct Alarm {
	pub kind: AlarmKind,
	pub state: Tristate,
	pub provenance: Provenance,
}

impl Alarm {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<Alarm> {
		let mut r = Reader::new(bytes);
		let value = Alarm::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<Alarm> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = Alarm::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		self.kind.write(w)?;
		self.state.write(w)?;
		self.provenance.write(w)?;
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<Alarm> {
		let kind = AlarmKind::read(r)?;
		let state = Tristate::read(r)?;
		let provenance = Provenance::read(r)?;
		Some(Alarm { kind, state, provenance })
	}
}

/// What the source's provider will do if asked. Advertised, never assumed.
#[derive(Clone, Debug, PartialEq)]
pub struct Controls {
	pub set_output: bool,
	pub schedule_off: bool,
	pub cancel_off: bool,
	pub outlets: u8,
}

impl Controls {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<Controls> {
		let mut r = Reader::new(bytes);
		let value = Controls::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<Controls> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = Controls::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.boolean(self.set_output)?;
		w.boolean(self.schedule_off)?;
		w.boolean(self.cancel_off)?;
		w.u8(self.outlets)?;
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<Controls> {
		let set_output = r.boolean()?;
		let schedule_off = r.boolean()?;
		let cancel_off = r.boolean()?;
		let outlets = r.u8()?;
		Some(Controls { set_output, schedule_off, cancel_off, outlets })
	}
}

/// One source, normalised.
#[derive(Clone, Debug, PartialEq)]
pub struct SourceState {
	pub kind: SourceKind,
	pub present: Tristate,
	pub online: Tristate,
	pub charge: ChargeState,
	pub remaining: Capacity,
	pub full: Capacity,
	pub design: Capacity,
	/// Basis points, 0 to 10000.
	pub state_of_charge: MeasureU64,
	/// Microvolts.
	pub voltage: MeasureU64,
	/// Microamps: positive into the source's storage, negative delivered out of it.
	pub current: MeasureI64,
	/// Microwatts, signed as `current` is. A source that reports only the watts it delivers publishes
	/// them here, and no load fraction is invented from them.
	pub power: MeasureI64,
	/// Whole seconds.
	pub runtime: MeasureU64,
	/// Basis points of rated load.
	pub load: MeasureU64,
	pub temperature: Temperature,
	pub trips: Vec<TripPoint>,
	pub alarms: Vec<Alarm>,
	pub controls: Controls,
	/// The source's own time, when it gave one. Never synchronised with anything.
	pub source_time: Option<u64>,
}

impl SourceState {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<SourceState> {
		let mut r = Reader::new(bytes);
		let value = SourceState::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<SourceState> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = SourceState::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		self.kind.write(w)?;
		self.present.write(w)?;
		self.online.write(w)?;
		self.charge.write(w)?;
		self.remaining.write(w)?;
		self.full.write(w)?;
		self.design.write(w)?;
		self.state_of_charge.write(w)?;
		self.voltage.write(w)?;
		self.current.write(w)?;
		self.power.write(w)?;
		self.runtime.write(w)?;
		self.load.write(w)?;
		self.temperature.write(w)?;
		if self.trips.len() > u16::MAX as usize {
			return None;
		}
		w.u16(self.trips.len() as u16)?;
		for v0 in self.trips.iter() {
			v0.write(w)?;
		}
		if self.alarms.len() > u16::MAX as usize {
			return None;
		}
		w.u16(self.alarms.len() as u16)?;
		for v1 in self.alarms.iter() {
			v1.write(w)?;
		}
		self.controls.write(w)?;
		match &self.source_time {
			Some(v2) => {
				w.u8(1)?;
				w.u64(*v2)?;
			}
			None => {
				w.u8(0)?;
			}
		}
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<SourceState> {
		let kind = SourceKind::read(r)?;
		let present = Tristate::read(r)?;
		let online = Tristate::read(r)?;
		let charge = ChargeState::read(r)?;
		let remaining = Capacity::read(r)?;
		let full = Capacity::read(r)?;
		let design = Capacity::read(r)?;
		let state_of_charge = MeasureU64::read(r)?;
		let voltage = MeasureU64::read(r)?;
		let current = MeasureI64::read(r)?;
		let power = MeasureI64::read(r)?;
		let runtime = MeasureU64::read(r)?;
		let load = MeasureU64::read(r)?;
		let temperature = Temperature::read(r)?;
		let trips = {
			let v3 = r.u16()? as usize;
			let v3 = (v3 <= 16).then_some(v3)?;
			let mut v4 = Vec::new();
			v4.try_reserve_exact(v3).ok()?;
			for _ in 0..v3 {
				v4.push(TripPoint::read(r)?);
			}
			v4
		};
		let alarms = {
			let v5 = r.u16()? as usize;
			let v5 = (v5 <= 16).then_some(v5)?;
			let mut v6 = Vec::new();
			v6.try_reserve_exact(v5).ok()?;
			for _ in 0..v5 {
				v6.push(Alarm::read(r)?);
			}
			v6
		};
		let controls = Controls::read(r)?;
		let source_time = if r.tag()? { Some(r.u64()?) } else { None };
		Some(SourceState { kind, present, online, charge, remaining, full, design, state_of_charge, voltage, current, power, runtime, load, temperature, trips, alarms, controls, source_time })
	}
}

/// A source's identity: which publication, which generation of it, which local source. A source from
/// a replacement publication is a DIFFERENT source, and a stale identity names nothing.
#[derive(Clone, Debug, PartialEq)]
pub struct SourceId {
	pub slot: u32,
	pub generation: u32,
	pub binding_generation: u64,
	pub local: u32,
}

impl SourceId {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<SourceId> {
		let mut r = Reader::new(bytes);
		let value = SourceId::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<SourceId> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = SourceId::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u32(self.slot)?;
		w.u32(self.generation)?;
		w.u64(self.binding_generation)?;
		w.u32(self.local)?;
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<SourceId> {
		let slot = r.u32()?;
		let generation = r.u32()?;
		let binding_generation = r.u64()?;
		let local = r.u32()?;
		Some(SourceId { slot, generation, binding_generation, local })
	}
}

#[derive(Clone, Debug, PartialEq)]
pub struct SourceSnapshot {
	pub id: SourceId,
	/// When this service received the state, on its monotonic clock.
	pub received: u64,
	pub state: SourceState,
}

impl SourceSnapshot {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<SourceSnapshot> {
		let mut r = Reader::new(bytes);
		let value = SourceSnapshot::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<SourceSnapshot> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = SourceSnapshot::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		self.id.write(w)?;
		w.u64(self.received)?;
		self.state.write(w)?;
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<SourceSnapshot> {
		let id = SourceId::read(r)?;
		let received = r.u64()?;
		let state = SourceState::read(r)?;
		Some(SourceSnapshot { id, received, state })
	}
}

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
#[repr(u8)]
pub enum ChangeKind {
	/// The subscription's atomic snapshot: one of these per source, then a `snapshot-end`.
	Snapshot = 1,
	SnapshotEnd = 2,
	Added = 3,
	Updated = 4,
	Removed = 5,
}

impl ChangeKind {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<ChangeKind> {
		let mut r = Reader::new(bytes);
		let value = ChangeKind::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<ChangeKind> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = ChangeKind::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u8(*self as u8)
	}
	pub fn read(r: &mut Reader) -> Option<ChangeKind> {
		match r.u8()? {
			1 => Some(ChangeKind::Snapshot),
			2 => Some(ChangeKind::SnapshotEnd),
			3 => Some(ChangeKind::Added),
			4 => Some(ChangeKind::Updated),
			5 => Some(ChangeKind::Removed),
			_ => None,
		}
	}
}

/// One frame of a subscription. REVISIONS MAY SKIP after coalescing; a gap is not a loss. A loss is a
/// closed subscription, which a client answers by subscribing again for a fresh snapshot.
#[derive(Clone, Debug, PartialEq)]
pub struct PowerChange {
	pub epoch: u64,
	pub revision: u64,
	pub kind: ChangeKind,
	pub source: Option<SourceSnapshot>,
	/// For `removed`, the source that went.
	pub gone: Option<SourceId>,
}

impl PowerChange {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<PowerChange> {
		let mut r = Reader::new(bytes);
		let value = PowerChange::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<PowerChange> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = PowerChange::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u64(self.epoch)?;
		w.u64(self.revision)?;
		self.kind.write(w)?;
		match &self.source {
			Some(v7) => {
				w.u8(1)?;
				v7.write(w)?;
			}
			None => {
				w.u8(0)?;
			}
		}
		match &self.gone {
			Some(v8) => {
				w.u8(1)?;
				v8.write(w)?;
			}
			None => {
				w.u8(0)?;
			}
		}
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<PowerChange> {
		let epoch = r.u64()?;
		let revision = r.u64()?;
		let kind = ChangeKind::read(r)?;
		let source = if r.tag()? { Some(SourceSnapshot::read(r)?) } else { None };
		let gone = if r.tag()? { Some(SourceId::read(r)?) } else { None };
		Some(PowerChange { epoch, revision, kind, source, gone })
	}
}

/// THE READ AUTHORITY: enumerate, and subscribe. No publication and no control.
// interface `power` over a channel: opcodes, a Service trait + dispatch, and a Client.
pub mod power {
	use super::*;
	use crate::codec::{Reader, Sink, SliceWriter, Transport, TransportError, VecWriter};
	use alloc::vec::Vec;

	pub const OP_SOURCES: u16 = 1;
	pub const OP_SUBSCRIBE: u16 = 2;

	pub trait Service {
		/// Every live source, sorted. Informational: a subscription is correct without it.
		fn sources(&mut self) -> Result<Vec<SourceSnapshot>, Error>;
		/// One atomic snapshot at (epoch, revision), then live changes after it. REFUSED with `exhausted`
		/// at the service's subscriber limit, rather than admitted and starved.
		fn subscribe(&mut self) -> Result<Vec<PowerChange>, Error>;
	}

	pub fn dispatch<S: Service>(service: &mut S, request: &[u8], request_handles: &mut Handles, out: &mut [u8], reply_handles: &mut Handles) -> Option<usize> {
		let mut reader = Reader::with_handle_list(request, request_handles);
		let r = &mut reader;
		let op = r.u16()?;
		let corr = r.u32()?;
		let mut writer = SliceWriter::new(out);
		if op == PROTOCOL_INFO_OP {
			r.finish()?;
			request_handles.clear();
			let w = &mut writer;
			w.u32(corr)?;
			w.bytes_lp(b"liber:power")?;
			w.u32(1)?;
			match Handles::try_from_slice(writer.handles()) {
				Some(taken) => *reply_handles = taken,
				None => return None,
			}
			return Some(writer.pos());
		}
		match op {
			OP_SOURCES => {
				r.finish()?;
				request_handles.clear();
				let result = service.sources();
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v9) => {
							w.u8(1)?;
							if v9.len() > u16::MAX as usize {
								return None;
							}
							w.u16(v9.len() as u16)?;
							for v11 in v9.iter() {
								v11.write(w)?;
							}
						}
						Err(v10) => {
							w.u8(0)?;
							v10.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			_ => return None,
		}
		match Handles::try_from_slice(writer.handles()) {
			Some(taken) => *reply_handles = taken,
			None => return None,
		}
		Some(writer.pos())
	}

	pub fn subscribe_open<S: Service>(service: &mut S, request: &[u8], request_handles: &mut Handles) -> Option<(u32, Result<Vec<PowerChange>, Error>)> {
		let mut reader = Reader::with_handle_list(request, request_handles);
		let r = &mut reader;
		let _op = r.u16()?;
		let corr = r.u32()?;
		r.finish()?;
		request_handles.clear();
		let items = service.subscribe();
		Some((corr, items))
	}
	pub fn subscribe_reply_ok(corr: u32, out: &mut [u8]) -> Option<usize> {
		let mut writer = SliceWriter::new(out);
		let w = &mut writer;
		w.u32(corr)?;
		w.u8(1)?;
		w.u32(0)?;
		writer.finish()
	}
	pub fn subscribe_reply_err(corr: u32, error: &Error, out: &mut [u8]) -> Option<usize> {
		let mut writer = SliceWriter::new(out);
		let w = &mut writer;
		w.u32(corr)?;
		w.u8(0)?;
		error.write(w)?;
		writer.finish()
	}
	pub fn subscribe_frame(seq: u32, item: &PowerChange, out: &mut [u8], frame_handles: &mut Handles) -> Option<usize> {
		let mut writer = SliceWriter::new(out);
		let encoded: Option<()> = (|| {
			let w = &mut writer;
			w.u32(seq)?;
			item.write(w)?;
			Some(())
		})();
		if encoded.is_none() {
			if let Some(taken) = Handles::try_from_slice(writer.handles()) {
				*frame_handles = taken;
			}
			return None;
		}
		*frame_handles = Handles::try_from_slice(writer.handles())?;
		Some(writer.pos())
	}
	pub fn subscribe_read(msg: &[u8], frame_handles: &mut Handles) -> Option<PowerChange> {
		let mut reader = Reader::with_handles(msg, frame_handles);
		let r = &mut reader;
		let _seq = r.u32()?;
		let value = PowerChange::read(r)?;
		reader.finish()?;
		frame_handles.clear();
		Some(value)
	}

	fn transport_outcome(error: TransportError) -> Error {
		match error {
			// The request never left this process, so nothing happened and trying
			// again is safe - which is what `again` says.
			TransportError::SendRefused | TransportError::NoRoute => Error::Again,
			// It went out and no answer came back. The server may have acted before
			// it died or before the deadline; nobody knows, and `commit-uncertain` is
			// the answer `base.error` grew so a caller is not forced to guess.
			// The reply could not be held, or arrived and broke the framing rules. In
			// both the server ANSWERED, so it acted; this end simply cannot read what
			// it said, which is the same position as never hearing back.
			TransportError::PeerClosed | TransportError::ReceiveFailed | TransportError::TimedOut | TransportError::NoMemory | TransportError::Malformed => Error::CommitUncertain,
		}
	}

	pub struct Client<T: Transport> {
		transport: T,
		corr: u32,
		deadline: u64,
		last_error: Option<TransportError>,
	}

	impl<T: Transport> Client<T> {
		pub fn new(transport: T) -> Client<T> {
			Client { transport, corr: 0, deadline: 0, last_error: None }
		}
		pub fn with_deadline(transport: T, deadline: u64) -> Client<T> {
			Client { transport, corr: 0, deadline, last_error: None }
		}
		pub fn set_deadline(&mut self, deadline: u64) {
			self.deadline = deadline;
		}
		pub fn last_error(&self) -> Option<TransportError> {
			self.last_error
		}
		pub fn into_transport(self) -> T {
			self.transport
		}
		fn next_corr(&mut self) -> u32 {
			let c = self.corr;
			self.corr = self.corr.wrapping_add(1);
			c
		}
		pub fn protocol_info(&mut self) -> Option<(String, u32)> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(PROTOCOL_INFO_OP)?;
			w.u32(corr)?;
			// No parameter, so no capability: `into_inner` says so rather than this
			// line assuming it.
			let request = writer.into_inner()?;
			let mut reply_handles = Handles::new();
			let reply = self
				.transport
				.call(&request, &[], &mut reply_handles, self.deadline)
				.map_err(|e| {
					self.last_error = Some(e);
					e
				})
				.ok()?;
			if !reply_handles.is_empty() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			let mut reader = Reader::new(&reply);
			let r = &mut reader;
			if r.u32()? != corr {
				return None;
			}
			let package = r.string_lp()?;
			let version = r.u32()?;
			r.finish()?;
			Some((package, version))
		}
		pub fn sources(&mut self) -> Option<Result<Vec<SourceSnapshot>, Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_SOURCES)?;
			w.u32(corr)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? {
					Ok({
						let v12 = r.u16()? as usize;
						let mut v13 = Vec::new();
						v13.try_reserve_exact(v12).ok()?;
						for _ in 0..v12 {
							v13.push(SourceSnapshot::read(r)?);
						}
						v13
					})
				} else {
					Err(Error::read(r)?)
				};
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
		pub fn subscribe(&mut self) -> Option<Result<u64, Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_SUBSCRIBE)?;
			w.u32(corr)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::new(&reply);
			let r = &mut reader;
			let decoded = (|| {
				if r.u32()? != corr {
					return None;
				}
				if r.tag()? {
					let _ = r.u32()?;
					r.finish()?;
					if reply_handles.len() != 1 {
						return None;
					}
					return Some(Ok(reply_handles.first()));
				}
				if !reply_handles.is_empty() {
					return None;
				}
				let error = Error::read(r)?;
				r.finish()?;
				Some(Err(error))
			})();
			if !matches!(decoded, Some(Ok(_))) {
				self.transport.discard_handles(reply_handles.as_slice());
			}
			decoded
		}
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_power_power_sources")]
	fn channel_invoke_sources(chan: u64) -> Option<Result<Vec<SourceSnapshot>, Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.sources()
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_power_power_subscribe")]
	fn channel_invoke_subscribe(chan: u64) -> Option<Result<u64, Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.subscribe()
	}
}

/// What an operator's command came to. A control the source does not advertise is not an outcome: it
/// is `error.unsupported`, answered before anything is sent.
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
#[repr(u8)]
pub enum ControlOutcome {
	Done = 1,
	/// The provider may have acted and did not say. NEVER RETRIED: the source's control state is
	/// uncertain until a fresh state query reconciles it.
	Indeterminate = 2,
}

impl ControlOutcome {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<ControlOutcome> {
		let mut r = Reader::new(bytes);
		let value = ControlOutcome::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<ControlOutcome> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = ControlOutcome::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u8(*self as u8)
	}
	pub fn read(r: &mut Reader) -> Option<ControlOutcome> {
		match r.u8()? {
			1 => Some(ControlOutcome::Done),
			2 => Some(ControlOutcome::Indeterminate),
			_ => None,
		}
	}
}

/// THE OPERATOR AUTHORITY: device controls a provider advertises, and only those. Granted to one
/// explicitly authorised administrative component and to nothing by default.
// interface `power-control` over a channel: opcodes, a Service trait + dispatch, and a Client.
pub mod power_control {
	use super::*;
	use crate::codec::{Reader, Sink, SliceWriter, Transport, TransportError, VecWriter};
	use alloc::vec::Vec;

	pub const OP_SET_OUTPUT: u16 = 1;
	pub const OP_SCHEDULE_OUTPUT_OFF: u16 = 2;
	pub const OP_CANCEL_OUTPUT_OFF: u16 = 3;

	pub trait Service {
		fn set_output(&mut self, source: SourceId, outlet: u8, on: bool) -> Result<ControlOutcome, Error>;
		fn schedule_output_off(&mut self, source: SourceId, delay_seconds: u32) -> Result<ControlOutcome, Error>;
		fn cancel_output_off(&mut self, source: SourceId) -> Result<ControlOutcome, Error>;
	}

	pub fn dispatch<S: Service>(service: &mut S, request: &[u8], request_handles: &mut Handles, out: &mut [u8], reply_handles: &mut Handles) -> Option<usize> {
		let mut reader = Reader::with_handle_list(request, request_handles);
		let r = &mut reader;
		let op = r.u16()?;
		let corr = r.u32()?;
		let mut writer = SliceWriter::new(out);
		if op == PROTOCOL_INFO_OP {
			r.finish()?;
			request_handles.clear();
			let w = &mut writer;
			w.u32(corr)?;
			w.bytes_lp(b"liber:power")?;
			w.u32(1)?;
			match Handles::try_from_slice(writer.handles()) {
				Some(taken) => *reply_handles = taken,
				None => return None,
			}
			return Some(writer.pos());
		}
		match op {
			OP_SET_OUTPUT => {
				let source = SourceId::read(r)?;
				let outlet = r.u8()?;
				let on = r.boolean()?;
				r.finish()?;
				request_handles.clear();
				let result = service.set_output(source, outlet, on);
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v14) => {
							w.u8(1)?;
							v14.write(w)?;
						}
						Err(v15) => {
							w.u8(0)?;
							v15.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			OP_SCHEDULE_OUTPUT_OFF => {
				let source = SourceId::read(r)?;
				let delay_seconds = r.u32()?;
				r.finish()?;
				request_handles.clear();
				let result = service.schedule_output_off(source, delay_seconds);
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v16) => {
							w.u8(1)?;
							v16.write(w)?;
						}
						Err(v17) => {
							w.u8(0)?;
							v17.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			OP_CANCEL_OUTPUT_OFF => {
				let source = SourceId::read(r)?;
				r.finish()?;
				request_handles.clear();
				let result = service.cancel_output_off(source);
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v18) => {
							w.u8(1)?;
							v18.write(w)?;
						}
						Err(v19) => {
							w.u8(0)?;
							v19.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			_ => return None,
		}
		match Handles::try_from_slice(writer.handles()) {
			Some(taken) => *reply_handles = taken,
			None => return None,
		}
		Some(writer.pos())
	}

	fn transport_outcome(error: TransportError) -> Error {
		match error {
			// The request never left this process, so nothing happened and trying
			// again is safe - which is what `again` says.
			TransportError::SendRefused | TransportError::NoRoute => Error::Again,
			// It went out and no answer came back. The server may have acted before
			// it died or before the deadline; nobody knows, and `commit-uncertain` is
			// the answer `base.error` grew so a caller is not forced to guess.
			// The reply could not be held, or arrived and broke the framing rules. In
			// both the server ANSWERED, so it acted; this end simply cannot read what
			// it said, which is the same position as never hearing back.
			TransportError::PeerClosed | TransportError::ReceiveFailed | TransportError::TimedOut | TransportError::NoMemory | TransportError::Malformed => Error::CommitUncertain,
		}
	}

	pub struct Client<T: Transport> {
		transport: T,
		corr: u32,
		deadline: u64,
		last_error: Option<TransportError>,
	}

	impl<T: Transport> Client<T> {
		pub fn new(transport: T) -> Client<T> {
			Client { transport, corr: 0, deadline: 0, last_error: None }
		}
		pub fn with_deadline(transport: T, deadline: u64) -> Client<T> {
			Client { transport, corr: 0, deadline, last_error: None }
		}
		pub fn set_deadline(&mut self, deadline: u64) {
			self.deadline = deadline;
		}
		pub fn last_error(&self) -> Option<TransportError> {
			self.last_error
		}
		pub fn into_transport(self) -> T {
			self.transport
		}
		fn next_corr(&mut self) -> u32 {
			let c = self.corr;
			self.corr = self.corr.wrapping_add(1);
			c
		}
		pub fn protocol_info(&mut self) -> Option<(String, u32)> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(PROTOCOL_INFO_OP)?;
			w.u32(corr)?;
			// No parameter, so no capability: `into_inner` says so rather than this
			// line assuming it.
			let request = writer.into_inner()?;
			let mut reply_handles = Handles::new();
			let reply = self
				.transport
				.call(&request, &[], &mut reply_handles, self.deadline)
				.map_err(|e| {
					self.last_error = Some(e);
					e
				})
				.ok()?;
			if !reply_handles.is_empty() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			let mut reader = Reader::new(&reply);
			let r = &mut reader;
			if r.u32()? != corr {
				return None;
			}
			let package = r.string_lp()?;
			let version = r.u32()?;
			r.finish()?;
			Some((package, version))
		}
		pub fn set_output(&mut self, source: &SourceId, outlet: &u8, on: &bool) -> Option<Result<ControlOutcome, Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_SET_OUTPUT)?;
			w.u32(corr)?;
			source.write(w)?;
			w.u8(*outlet)?;
			w.boolean(*on)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? { Ok(ControlOutcome::read(r)?) } else { Err(Error::read(r)?) };
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
		pub fn schedule_output_off(&mut self, source: &SourceId, delay_seconds: &u32) -> Option<Result<ControlOutcome, Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_SCHEDULE_OUTPUT_OFF)?;
			w.u32(corr)?;
			source.write(w)?;
			w.u32(*delay_seconds)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? { Ok(ControlOutcome::read(r)?) } else { Err(Error::read(r)?) };
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
		pub fn cancel_output_off(&mut self, source: &SourceId) -> Option<Result<ControlOutcome, Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_CANCEL_OUTPUT_OFF)?;
			w.u32(corr)?;
			source.write(w)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? { Ok(ControlOutcome::read(r)?) } else { Err(Error::read(r)?) };
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_power_power_control_set_output")]
	fn channel_invoke_set_output(chan: u64, source: &SourceId, outlet: &u8, on: &bool) -> Option<Result<ControlOutcome, Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.set_output(source, outlet, on)
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_power_power_control_schedule_output_off")]
	fn channel_invoke_schedule_output_off(chan: u64, source: &SourceId, delay_seconds: &u32) -> Option<Result<ControlOutcome, Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.schedule_output_off(source, delay_seconds)
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_power_power_control_cancel_output_off")]
	fn channel_invoke_cancel_output_off(chan: u64, source: &SourceId) -> Option<Result<ControlOutcome, Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.cancel_output_off(source)
	}
}

/// One source as a provider describes it, by ITS local identity.
#[derive(Clone, Debug, PartialEq)]
pub struct ProviderSource {
	pub local: u32,
	pub state: SourceState,
}

impl ProviderSource {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<ProviderSource> {
		let mut r = Reader::new(bytes);
		let value = ProviderSource::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<ProviderSource> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = ProviderSource::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u32(self.local)?;
		self.state.write(w)?;
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<ProviderSource> {
		let local = r.u32()?;
		let state = SourceState::read(r)?;
		Some(ProviderSource { local, state })
	}
}

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
#[repr(u8)]
pub enum ProviderUpdateKind {
	Snapshot = 1,
	SnapshotEnd = 2,
	Added = 3,
	Updated = 4,
	Removed = 5,
}

impl ProviderUpdateKind {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<ProviderUpdateKind> {
		let mut r = Reader::new(bytes);
		let value = ProviderUpdateKind::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<ProviderUpdateKind> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = ProviderUpdateKind::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u8(*self as u8)
	}
	pub fn read(r: &mut Reader) -> Option<ProviderUpdateKind> {
		match r.u8()? {
			1 => Some(ProviderUpdateKind::Snapshot),
			2 => Some(ProviderUpdateKind::SnapshotEnd),
			3 => Some(ProviderUpdateKind::Added),
			4 => Some(ProviderUpdateKind::Updated),
			5 => Some(ProviderUpdateKind::Removed),
			_ => None,
		}
	}
}

/// One frame of a provider's stream, carrying the provider's own monotonic revision.
#[derive(Clone, Debug, PartialEq)]
pub struct ProviderUpdate {
	pub revision: u64,
	pub kind: ProviderUpdateKind,
	pub source: Option<ProviderSource>,
	pub gone: Option<u32>,
}

impl ProviderUpdate {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<ProviderUpdate> {
		let mut r = Reader::new(bytes);
		let value = ProviderUpdate::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<ProviderUpdate> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = ProviderUpdate::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u64(self.revision)?;
		self.kind.write(w)?;
		match &self.source {
			Some(v20) => {
				w.u8(1)?;
				v20.write(w)?;
			}
			None => {
				w.u8(0)?;
			}
		}
		match &self.gone {
			Some(v21) => {
				w.u8(1)?;
				w.u32(*v21)?;
			}
			None => {
				w.u8(0)?;
			}
		}
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<ProviderUpdate> {
		let revision = r.u64()?;
		let kind = ProviderUpdateKind::read(r)?;
		let source = if r.tag()? { Some(ProviderSource::read(r)?) } else { None };
		let gone = if r.tag()? { Some(r.u32()?) } else { None };
		Some(ProviderUpdate { revision, kind, source, gone })
	}
}

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
#[repr(u8)]
pub enum ProviderCommandKind {
	SetOutput = 1,
	ScheduleOutputOff = 2,
	CancelOutputOff = 3,
}

impl ProviderCommandKind {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<ProviderCommandKind> {
		let mut r = Reader::new(bytes);
		let value = ProviderCommandKind::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<ProviderCommandKind> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = ProviderCommandKind::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u8(*self as u8)
	}
	pub fn read(r: &mut Reader) -> Option<ProviderCommandKind> {
		match r.u8()? {
			1 => Some(ProviderCommandKind::SetOutput),
			2 => Some(ProviderCommandKind::ScheduleOutputOff),
			3 => Some(ProviderCommandKind::CancelOutputOff),
			_ => None,
		}
	}
}

#[derive(Clone, Debug, PartialEq)]
pub struct ProviderCommand {
	pub kind: ProviderCommandKind,
	pub local: u32,
	pub outlet: u8,
	pub on: bool,
	pub delay_seconds: u32,
}

impl ProviderCommand {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<ProviderCommand> {
		let mut r = Reader::new(bytes);
		let value = ProviderCommand::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<ProviderCommand> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = ProviderCommand::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		self.kind.write(w)?;
		w.u32(self.local)?;
		w.u8(self.outlet)?;
		w.boolean(self.on)?;
		w.u32(self.delay_seconds)?;
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<ProviderCommand> {
		let kind = ProviderCommandKind::read(r)?;
		let local = r.u32()?;
		let outlet = r.u8()?;
		let on = r.boolean()?;
		let delay_seconds = r.u32()?;
		Some(ProviderCommand { kind, local, outlet, on, delay_seconds })
	}
}

/// THE PROVIDER CONTRACT a `power-source` publication serves - to PowerService alone.
// interface `power-provider` over a channel: opcodes, a Service trait + dispatch, and a Client.
pub mod power_provider {
	use super::*;
	use crate::codec::{Reader, Sink, SliceWriter, Transport, TransportError, VecWriter};
	use alloc::vec::Vec;

	pub const OP_UPDATES: u16 = 1;
	pub const OP_COMMAND: u16 = 2;
	pub const OP_QUERY: u16 = 3;

	pub trait Service {
		fn updates(&mut self) -> Vec<ProviderUpdate>;
		fn command(&mut self, command: ProviderCommand) -> Result<ControlOutcome, Error>;
		/// A fresh state of one source, for reconciliation after an indeterminate command.
		fn query(&mut self, local: u32) -> Result<SourceState, Error>;
	}

	pub fn dispatch<S: Service>(service: &mut S, request: &[u8], request_handles: &mut Handles, out: &mut [u8], reply_handles: &mut Handles) -> Option<usize> {
		let mut reader = Reader::with_handle_list(request, request_handles);
		let r = &mut reader;
		let op = r.u16()?;
		let corr = r.u32()?;
		let mut writer = SliceWriter::new(out);
		if op == PROTOCOL_INFO_OP {
			r.finish()?;
			request_handles.clear();
			let w = &mut writer;
			w.u32(corr)?;
			w.bytes_lp(b"liber:power")?;
			w.u32(1)?;
			match Handles::try_from_slice(writer.handles()) {
				Some(taken) => *reply_handles = taken,
				None => return None,
			}
			return Some(writer.pos());
		}
		match op {
			OP_COMMAND => {
				let command = ProviderCommand::read(r)?;
				r.finish()?;
				request_handles.clear();
				let result = service.command(command);
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v22) => {
							w.u8(1)?;
							v22.write(w)?;
						}
						Err(v23) => {
							w.u8(0)?;
							v23.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			OP_QUERY => {
				let local = r.u32()?;
				r.finish()?;
				request_handles.clear();
				let result = service.query(local);
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v24) => {
							w.u8(1)?;
							v24.write(w)?;
						}
						Err(v25) => {
							w.u8(0)?;
							v25.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			_ => return None,
		}
		match Handles::try_from_slice(writer.handles()) {
			Some(taken) => *reply_handles = taken,
			None => return None,
		}
		Some(writer.pos())
	}

	pub fn updates_open<S: Service>(service: &mut S, request: &[u8], request_handles: &mut Handles) -> Option<(u32, Vec<ProviderUpdate>)> {
		let mut reader = Reader::with_handle_list(request, request_handles);
		let r = &mut reader;
		let _op = r.u16()?;
		let corr = r.u32()?;
		r.finish()?;
		request_handles.clear();
		let items = service.updates();
		Some((corr, items))
	}
	pub fn updates_frame(seq: u32, item: &ProviderUpdate, out: &mut [u8], frame_handles: &mut Handles) -> Option<usize> {
		let mut writer = SliceWriter::new(out);
		let encoded: Option<()> = (|| {
			let w = &mut writer;
			w.u32(seq)?;
			item.write(w)?;
			Some(())
		})();
		if encoded.is_none() {
			if let Some(taken) = Handles::try_from_slice(writer.handles()) {
				*frame_handles = taken;
			}
			return None;
		}
		*frame_handles = Handles::try_from_slice(writer.handles())?;
		Some(writer.pos())
	}
	pub fn updates_read(msg: &[u8], frame_handles: &mut Handles) -> Option<ProviderUpdate> {
		let mut reader = Reader::with_handles(msg, frame_handles);
		let r = &mut reader;
		let _seq = r.u32()?;
		let value = ProviderUpdate::read(r)?;
		reader.finish()?;
		frame_handles.clear();
		Some(value)
	}

	fn transport_outcome(error: TransportError) -> Error {
		match error {
			// The request never left this process, so nothing happened and trying
			// again is safe - which is what `again` says.
			TransportError::SendRefused | TransportError::NoRoute => Error::Again,
			// It went out and no answer came back. The server may have acted before
			// it died or before the deadline; nobody knows, and `commit-uncertain` is
			// the answer `base.error` grew so a caller is not forced to guess.
			// The reply could not be held, or arrived and broke the framing rules. In
			// both the server ANSWERED, so it acted; this end simply cannot read what
			// it said, which is the same position as never hearing back.
			TransportError::PeerClosed | TransportError::ReceiveFailed | TransportError::TimedOut | TransportError::NoMemory | TransportError::Malformed => Error::CommitUncertain,
		}
	}

	pub struct Client<T: Transport> {
		transport: T,
		corr: u32,
		deadline: u64,
		last_error: Option<TransportError>,
	}

	impl<T: Transport> Client<T> {
		pub fn new(transport: T) -> Client<T> {
			Client { transport, corr: 0, deadline: 0, last_error: None }
		}
		pub fn with_deadline(transport: T, deadline: u64) -> Client<T> {
			Client { transport, corr: 0, deadline, last_error: None }
		}
		pub fn set_deadline(&mut self, deadline: u64) {
			self.deadline = deadline;
		}
		pub fn last_error(&self) -> Option<TransportError> {
			self.last_error
		}
		pub fn into_transport(self) -> T {
			self.transport
		}
		fn next_corr(&mut self) -> u32 {
			let c = self.corr;
			self.corr = self.corr.wrapping_add(1);
			c
		}
		pub fn protocol_info(&mut self) -> Option<(String, u32)> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(PROTOCOL_INFO_OP)?;
			w.u32(corr)?;
			// No parameter, so no capability: `into_inner` says so rather than this
			// line assuming it.
			let request = writer.into_inner()?;
			let mut reply_handles = Handles::new();
			let reply = self
				.transport
				.call(&request, &[], &mut reply_handles, self.deadline)
				.map_err(|e| {
					self.last_error = Some(e);
					e
				})
				.ok()?;
			if !reply_handles.is_empty() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			let mut reader = Reader::new(&reply);
			let r = &mut reader;
			if r.u32()? != corr {
				return None;
			}
			let package = r.string_lp()?;
			let version = r.u32()?;
			r.finish()?;
			Some((package, version))
		}
		pub fn updates(&mut self) -> Option<u64> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_UPDATES)?;
			w.u32(corr)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = self
				.transport
				.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline)
				.map_err(|e| {
					self.last_error = Some(e);
					e
				})
				.ok()?;
			let mut reader = Reader::new(&reply);
			let r = &mut reader;
			if r.u32()? != corr || r.finish().is_none() || reply_handles.len() != 1 {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			Some(reply_handles.first())
		}
		pub fn command(&mut self, command: &ProviderCommand) -> Option<Result<ControlOutcome, Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_COMMAND)?;
			w.u32(corr)?;
			command.write(w)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? { Ok(ControlOutcome::read(r)?) } else { Err(Error::read(r)?) };
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
		pub fn query(&mut self, local: &u32) -> Option<Result<SourceState, Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_QUERY)?;
			w.u32(corr)?;
			w.u32(*local)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? { Ok(SourceState::read(r)?) } else { Err(Error::read(r)?) };
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_power_power_provider_updates")]
	fn channel_invoke_updates(chan: u64) -> Option<u64> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.updates()
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_power_power_provider_command")]
	fn channel_invoke_command(chan: u64, command: &ProviderCommand) -> Option<Result<ControlOutcome, Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.command(command)
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_power_power_provider_query")]
	fn channel_invoke_query(chan: u64, local: &u32) -> Option<Result<SourceState, Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.query(local)
	}
}

/// A decoded input of the in-guest fixture that a gate's probe may change. DEVELOPMENT ONLY, like
/// everything below: the endpoint serving it exists on a development fixture bound to a QEMU test
/// device, and PermissionManager reaches it only in a development build, for a probe whose policy
/// row grants `fixture-control`. None of it is a claim that QEMU attaches a UPS or evaluates AML.
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
#[repr(u8)]
pub enum FixtureField {
	/// The UPS's RemainingCapacity, milliamp-hours by CapacityMode.
	UpsRemaining = 1,
	/// The UPS's Voltage, hundredths of a volt.
	UpsVoltage = 2,
	/// The UPS's PresentStatus.ACPresent: 1 on line power, 0 not.
	UpsAcPresent = 3,
	/// The UPS's PresentStatus.Overload, 1 or 0.
	UpsOverload = 4,
	/// The battery's `_BST` remaining capacity, milliwatt-hours.
	BatteryRemaining = 5,
	/// The battery's `_BST` state bits.
	BatteryState = 6,
	/// The adapter's `_PSR`.
	AcOnline = 7,
	/// The zone's `_TMP`, tenths of a kelvin.
	ThermalTemperature = 8,
}

impl FixtureField {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<FixtureField> {
		let mut r = Reader::new(bytes);
		let value = FixtureField::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<FixtureField> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = FixtureField::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u8(*self as u8)
	}
	pub fn read(r: &mut Reader) -> Option<FixtureField> {
		match r.u8()? {
			1 => Some(FixtureField::UpsRemaining),
			2 => Some(FixtureField::UpsVoltage),
			3 => Some(FixtureField::UpsAcPresent),
			4 => Some(FixtureField::UpsOverload),
			5 => Some(FixtureField::BatteryRemaining),
			6 => Some(FixtureField::BatteryState),
			7 => Some(FixtureField::AcOnline),
			8 => Some(FixtureField::ThermalTemperature),
			_ => None,
		}
	}
}

/// One command a fixture's provider received, as it received it.
#[derive(Clone, Debug, PartialEq)]
pub struct FixtureCommand {
	/// 0 is the HID-shaped UPS provider, 1 the ACPI-shaped one.
	pub publication: u8,
	pub local: u32,
	pub kind: ProviderCommandKind,
	pub outlet: u8,
	pub on: bool,
	pub delay_seconds: u32,
}

impl FixtureCommand {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<FixtureCommand> {
		let mut r = Reader::new(bytes);
		let value = FixtureCommand::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<FixtureCommand> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = FixtureCommand::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u8(self.publication)?;
		w.u32(self.local)?;
		self.kind.write(w)?;
		w.u8(self.outlet)?;
		w.boolean(self.on)?;
		w.u32(self.delay_seconds)?;
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<FixtureCommand> {
		let publication = r.u8()?;
		let local = r.u32()?;
		let kind = ProviderCommandKind::read(r)?;
		let outlet = r.u8()?;
		let on = r.boolean()?;
		let delay_seconds = r.u32()?;
		Some(FixtureCommand { publication, local, kind, outlet, on, delay_seconds })
	}
}

/// THE FIXTURE'S CONTROL ENDPOINT: change or remove what the two fixture providers publish, make one
/// withhold its replies, and read back every command they were sent and what their outlets are.
// interface `power-fixture` over a channel: opcodes, a Service trait + dispatch, and a Client.
pub mod power_fixture {
	use super::*;
	use crate::codec::{Reader, Sink, SliceWriter, Transport, TransportError, VecWriter};
	use alloc::vec::Vec;

	pub const OP_SET: u16 = 1;
	pub const OP_REMOVE: u16 = 2;
	pub const OP_WITHDRAW: u16 = 3;
	pub const OP_REPUBLISH: u16 = 4;
	pub const OP_COMMANDS: u16 = 5;
	pub const OP_WITHHOLD: u16 = 6;
	pub const OP_OFFER_EXTRA: u16 = 7;
	pub const OP_OUTLETS: u16 = 8;

	pub trait Service {
		fn set(&mut self, field: FixtureField, value: u32) -> Result<(), Error>;
		/// Remove one source from a publication.
		fn remove(&mut self, publication: u8, local: u32) -> Result<(), Error>;
		/// Withdraw a whole publication, as a device going would.
		fn withdraw(&mut self, publication: u8) -> Result<(), Error>;
		/// Publish a withdrawn one again: a new publication, so a new generation and new sources.
		fn republish(&mut self, publication: u8) -> Result<(), Error>;
		fn commands(&mut self) -> Result<Vec<FixtureCommand>, Error>;
		/// Leave the next `commands` control replies and `queries` reconciliation replies unanswered.
		fn withhold(&mut self, commands: u32, queries: u32) -> Result<(), Error>;
		/// Offer one more `power-source` publication than this fixture's registry entry declares, which
		/// DeviceManager must refuse.
		fn offer_extra(&mut self) -> Result<(), Error>;
		/// The UPS's outlets, on or off, in order.
		fn outlets(&mut self) -> Result<Vec<bool>, Error>;
	}

	pub fn dispatch<S: Service>(service: &mut S, request: &[u8], request_handles: &mut Handles, out: &mut [u8], reply_handles: &mut Handles) -> Option<usize> {
		let mut reader = Reader::with_handle_list(request, request_handles);
		let r = &mut reader;
		let op = r.u16()?;
		let corr = r.u32()?;
		let mut writer = SliceWriter::new(out);
		if op == PROTOCOL_INFO_OP {
			r.finish()?;
			request_handles.clear();
			let w = &mut writer;
			w.u32(corr)?;
			w.bytes_lp(b"liber:power")?;
			w.u32(1)?;
			match Handles::try_from_slice(writer.handles()) {
				Some(taken) => *reply_handles = taken,
				None => return None,
			}
			return Some(writer.pos());
		}
		match op {
			OP_SET => {
				let field = FixtureField::read(r)?;
				let value = r.u32()?;
				r.finish()?;
				request_handles.clear();
				let result = service.set(field, value);
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v26) => {
							w.u8(1)?;
						}
						Err(v27) => {
							w.u8(0)?;
							v27.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			OP_REMOVE => {
				let publication = r.u8()?;
				let local = r.u32()?;
				r.finish()?;
				request_handles.clear();
				let result = service.remove(publication, local);
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v28) => {
							w.u8(1)?;
						}
						Err(v29) => {
							w.u8(0)?;
							v29.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			OP_WITHDRAW => {
				let publication = r.u8()?;
				r.finish()?;
				request_handles.clear();
				let result = service.withdraw(publication);
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v30) => {
							w.u8(1)?;
						}
						Err(v31) => {
							w.u8(0)?;
							v31.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			OP_REPUBLISH => {
				let publication = r.u8()?;
				r.finish()?;
				request_handles.clear();
				let result = service.republish(publication);
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v32) => {
							w.u8(1)?;
						}
						Err(v33) => {
							w.u8(0)?;
							v33.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			OP_COMMANDS => {
				r.finish()?;
				request_handles.clear();
				let result = service.commands();
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v34) => {
							w.u8(1)?;
							if v34.len() > u16::MAX as usize {
								return None;
							}
							w.u16(v34.len() as u16)?;
							for v36 in v34.iter() {
								v36.write(w)?;
							}
						}
						Err(v35) => {
							w.u8(0)?;
							v35.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			OP_WITHHOLD => {
				let commands = r.u32()?;
				let queries = r.u32()?;
				r.finish()?;
				request_handles.clear();
				let result = service.withhold(commands, queries);
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v37) => {
							w.u8(1)?;
						}
						Err(v38) => {
							w.u8(0)?;
							v38.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			OP_OFFER_EXTRA => {
				r.finish()?;
				request_handles.clear();
				let result = service.offer_extra();
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v39) => {
							w.u8(1)?;
						}
						Err(v40) => {
							w.u8(0)?;
							v40.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			OP_OUTLETS => {
				r.finish()?;
				request_handles.clear();
				let result = service.outlets();
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v41) => {
							w.u8(1)?;
							if v41.len() > u16::MAX as usize {
								return None;
							}
							w.u16(v41.len() as u16)?;
							for v43 in v41.iter() {
								w.boolean(*v43)?;
							}
						}
						Err(v42) => {
							w.u8(0)?;
							v42.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			_ => return None,
		}
		match Handles::try_from_slice(writer.handles()) {
			Some(taken) => *reply_handles = taken,
			None => return None,
		}
		Some(writer.pos())
	}

	fn transport_outcome(error: TransportError) -> Error {
		match error {
			// The request never left this process, so nothing happened and trying
			// again is safe - which is what `again` says.
			TransportError::SendRefused | TransportError::NoRoute => Error::Again,
			// It went out and no answer came back. The server may have acted before
			// it died or before the deadline; nobody knows, and `commit-uncertain` is
			// the answer `base.error` grew so a caller is not forced to guess.
			// The reply could not be held, or arrived and broke the framing rules. In
			// both the server ANSWERED, so it acted; this end simply cannot read what
			// it said, which is the same position as never hearing back.
			TransportError::PeerClosed | TransportError::ReceiveFailed | TransportError::TimedOut | TransportError::NoMemory | TransportError::Malformed => Error::CommitUncertain,
		}
	}

	pub struct Client<T: Transport> {
		transport: T,
		corr: u32,
		deadline: u64,
		last_error: Option<TransportError>,
	}

	impl<T: Transport> Client<T> {
		pub fn new(transport: T) -> Client<T> {
			Client { transport, corr: 0, deadline: 0, last_error: None }
		}
		pub fn with_deadline(transport: T, deadline: u64) -> Client<T> {
			Client { transport, corr: 0, deadline, last_error: None }
		}
		pub fn set_deadline(&mut self, deadline: u64) {
			self.deadline = deadline;
		}
		pub fn last_error(&self) -> Option<TransportError> {
			self.last_error
		}
		pub fn into_transport(self) -> T {
			self.transport
		}
		fn next_corr(&mut self) -> u32 {
			let c = self.corr;
			self.corr = self.corr.wrapping_add(1);
			c
		}
		pub fn protocol_info(&mut self) -> Option<(String, u32)> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(PROTOCOL_INFO_OP)?;
			w.u32(corr)?;
			// No parameter, so no capability: `into_inner` says so rather than this
			// line assuming it.
			let request = writer.into_inner()?;
			let mut reply_handles = Handles::new();
			let reply = self
				.transport
				.call(&request, &[], &mut reply_handles, self.deadline)
				.map_err(|e| {
					self.last_error = Some(e);
					e
				})
				.ok()?;
			if !reply_handles.is_empty() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			let mut reader = Reader::new(&reply);
			let r = &mut reader;
			if r.u32()? != corr {
				return None;
			}
			let package = r.string_lp()?;
			let version = r.u32()?;
			r.finish()?;
			Some((package, version))
		}
		pub fn set(&mut self, field: &FixtureField, value: &u32) -> Option<Result<(), Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_SET)?;
			w.u32(corr)?;
			field.write(w)?;
			w.u32(*value)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? { Ok(()) } else { Err(Error::read(r)?) };
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
		pub fn remove(&mut self, publication: &u8, local: &u32) -> Option<Result<(), Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_REMOVE)?;
			w.u32(corr)?;
			w.u8(*publication)?;
			w.u32(*local)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? { Ok(()) } else { Err(Error::read(r)?) };
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
		pub fn withdraw(&mut self, publication: &u8) -> Option<Result<(), Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_WITHDRAW)?;
			w.u32(corr)?;
			w.u8(*publication)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? { Ok(()) } else { Err(Error::read(r)?) };
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
		pub fn republish(&mut self, publication: &u8) -> Option<Result<(), Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_REPUBLISH)?;
			w.u32(corr)?;
			w.u8(*publication)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? { Ok(()) } else { Err(Error::read(r)?) };
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
		pub fn commands(&mut self) -> Option<Result<Vec<FixtureCommand>, Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_COMMANDS)?;
			w.u32(corr)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? {
					Ok({
						let v44 = r.u16()? as usize;
						let mut v45 = Vec::new();
						v45.try_reserve_exact(v44).ok()?;
						for _ in 0..v44 {
							v45.push(FixtureCommand::read(r)?);
						}
						v45
					})
				} else {
					Err(Error::read(r)?)
				};
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
		pub fn withhold(&mut self, commands: &u32, queries: &u32) -> Option<Result<(), Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_WITHHOLD)?;
			w.u32(corr)?;
			w.u32(*commands)?;
			w.u32(*queries)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? { Ok(()) } else { Err(Error::read(r)?) };
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
		pub fn offer_extra(&mut self) -> Option<Result<(), Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_OFFER_EXTRA)?;
			w.u32(corr)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? { Ok(()) } else { Err(Error::read(r)?) };
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
		pub fn outlets(&mut self) -> Option<Result<Vec<bool>, Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_OUTLETS)?;
			w.u32(corr)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? {
					Ok({
						let v46 = r.u16()? as usize;
						let mut v47 = Vec::new();
						v47.try_reserve_exact(v46).ok()?;
						for _ in 0..v46 {
							v47.push(r.boolean()?);
						}
						v47
					})
				} else {
					Err(Error::read(r)?)
				};
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_power_power_fixture_set")]
	fn channel_invoke_set(chan: u64, field: &FixtureField, value: &u32) -> Option<Result<(), Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.set(field, value)
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_power_power_fixture_remove")]
	fn channel_invoke_remove(chan: u64, publication: &u8, local: &u32) -> Option<Result<(), Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.remove(publication, local)
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_power_power_fixture_withdraw")]
	fn channel_invoke_withdraw(chan: u64, publication: &u8) -> Option<Result<(), Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.withdraw(publication)
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_power_power_fixture_republish")]
	fn channel_invoke_republish(chan: u64, publication: &u8) -> Option<Result<(), Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.republish(publication)
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_power_power_fixture_commands")]
	fn channel_invoke_commands(chan: u64) -> Option<Result<Vec<FixtureCommand>, Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.commands()
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_power_power_fixture_withhold")]
	fn channel_invoke_withhold(chan: u64, commands: &u32, queries: &u32) -> Option<Result<(), Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.withhold(commands, queries)
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_power_power_fixture_offer_extra")]
	fn channel_invoke_offer_extra(chan: u64) -> Option<Result<(), Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.offer_extra()
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_power_power_fixture_outlets")]
	fn channel_invoke_outlets(chan: u64) -> Option<Result<Vec<bool>, Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.outlets()
	}
}

impl SourceKind {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		match self {
			SourceKind::Ac => out.push_str("\"ac\""),
			SourceKind::Battery => out.push_str("\"battery\""),
			SourceKind::Ups => out.push_str("\"ups\""),
			SourceKind::Load => out.push_str("\"load\""),
			SourceKind::ThermalZone => out.push_str("\"thermal-zone\""),
		}
	}
	pub fn to_text_into(&self, out: &mut String) {
		match self {
			SourceKind::Ac => out.push_str("ac"),
			SourceKind::Battery => out.push_str("battery"),
			SourceKind::Ups => out.push_str("ups"),
			SourceKind::Load => out.push_str("load"),
			SourceKind::ThermalZone => out.push_str("thermal-zone"),
		}
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		match self {
			SourceKind::Ac => crate::codec::cbor::text(out, "ac"),
			SourceKind::Battery => crate::codec::cbor::text(out, "battery"),
			SourceKind::Ups => crate::codec::cbor::text(out, "ups"),
			SourceKind::Load => crate::codec::cbor::text(out, "load"),
			SourceKind::ThermalZone => crate::codec::cbor::text(out, "thermal-zone"),
		}
	}
}

impl ValueState {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		match self {
			ValueState::Known => out.push_str("\"known\""),
			ValueState::Unknown => out.push_str("\"unknown\""),
			ValueState::Unsupported => out.push_str("\"unsupported\""),
			ValueState::Invalid => out.push_str("\"invalid\""),
		}
	}
	pub fn to_text_into(&self, out: &mut String) {
		match self {
			ValueState::Known => out.push_str("known"),
			ValueState::Unknown => out.push_str("unknown"),
			ValueState::Unsupported => out.push_str("unsupported"),
			ValueState::Invalid => out.push_str("invalid"),
		}
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		match self {
			ValueState::Known => crate::codec::cbor::text(out, "known"),
			ValueState::Unknown => crate::codec::cbor::text(out, "unknown"),
			ValueState::Unsupported => crate::codec::cbor::text(out, "unsupported"),
			ValueState::Invalid => crate::codec::cbor::text(out, "invalid"),
		}
	}
}

impl InvalidReason {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		match self {
			InvalidReason::None => out.push_str("\"none\""),
			InvalidReason::Sentinel => out.push_str("\"sentinel\""),
			InvalidReason::Overflow => out.push_str("\"overflow\""),
			InvalidReason::Contradiction => out.push_str("\"contradiction\""),
			InvalidReason::Unit => out.push_str("\"unit\""),
			InvalidReason::Range => out.push_str("\"range\""),
		}
	}
	pub fn to_text_into(&self, out: &mut String) {
		match self {
			InvalidReason::None => out.push_str("none"),
			InvalidReason::Sentinel => out.push_str("sentinel"),
			InvalidReason::Overflow => out.push_str("overflow"),
			InvalidReason::Contradiction => out.push_str("contradiction"),
			InvalidReason::Unit => out.push_str("unit"),
			InvalidReason::Range => out.push_str("range"),
		}
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		match self {
			InvalidReason::None => crate::codec::cbor::text(out, "none"),
			InvalidReason::Sentinel => crate::codec::cbor::text(out, "sentinel"),
			InvalidReason::Overflow => crate::codec::cbor::text(out, "overflow"),
			InvalidReason::Contradiction => crate::codec::cbor::text(out, "contradiction"),
			InvalidReason::Unit => crate::codec::cbor::text(out, "unit"),
			InvalidReason::Range => crate::codec::cbor::text(out, "range"),
		}
	}
}

impl Tristate {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		match self {
			Tristate::Unknown => out.push_str("\"unknown\""),
			Tristate::No => out.push_str("\"no\""),
			Tristate::Yes => out.push_str("\"yes\""),
		}
	}
	pub fn to_text_into(&self, out: &mut String) {
		match self {
			Tristate::Unknown => out.push_str("unknown"),
			Tristate::No => out.push_str("no"),
			Tristate::Yes => out.push_str("yes"),
		}
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		match self {
			Tristate::Unknown => crate::codec::cbor::text(out, "unknown"),
			Tristate::No => crate::codec::cbor::text(out, "no"),
			Tristate::Yes => crate::codec::cbor::text(out, "yes"),
		}
	}
}

impl MeasureU64 {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"state\":");
		self.state.to_json_into(out);
		out.push(',');
		out.push_str("\"value\":");
		let _ = write!(out, "{}", self.value);
		out.push(',');
		out.push_str("\"reason\":");
		self.reason.to_json_into(out);
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("state=");
		self.state.to_text_into(out);
		out.push_str(", ");
		out.push_str("value=");
		let _ = write!(out, "{}", self.value);
		out.push_str(", ");
		out.push_str("reason=");
		self.reason.to_text_into(out);
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 3);
		crate::codec::cbor::text(out, "state");
		self.state.to_cbor_into(out);
		crate::codec::cbor::text(out, "value");
		crate::codec::cbor::uint(out, self.value as u64);
		crate::codec::cbor::text(out, "reason");
		self.reason.to_cbor_into(out);
	}
}

impl MeasureI64 {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"state\":");
		self.state.to_json_into(out);
		out.push(',');
		out.push_str("\"value\":");
		let _ = write!(out, "{}", self.value);
		out.push(',');
		out.push_str("\"reason\":");
		self.reason.to_json_into(out);
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("state=");
		self.state.to_text_into(out);
		out.push_str(", ");
		out.push_str("value=");
		let _ = write!(out, "{}", self.value);
		out.push_str(", ");
		out.push_str("reason=");
		self.reason.to_text_into(out);
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 3);
		crate::codec::cbor::text(out, "state");
		self.state.to_cbor_into(out);
		crate::codec::cbor::text(out, "value");
		crate::codec::cbor::int(out, self.value as i64);
		crate::codec::cbor::text(out, "reason");
		self.reason.to_cbor_into(out);
	}
}

impl Quantity {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		match self {
			Quantity::Energy => out.push_str("\"energy\""),
			Quantity::Charge => out.push_str("\"charge\""),
		}
	}
	pub fn to_text_into(&self, out: &mut String) {
		match self {
			Quantity::Energy => out.push_str("energy"),
			Quantity::Charge => out.push_str("charge"),
		}
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		match self {
			Quantity::Energy => crate::codec::cbor::text(out, "energy"),
			Quantity::Charge => crate::codec::cbor::text(out, "charge"),
		}
	}
}

impl Capacity {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"state\":");
		self.state.to_json_into(out);
		out.push(',');
		out.push_str("\"quantity\":");
		self.quantity.to_json_into(out);
		out.push(',');
		out.push_str("\"value\":");
		let _ = write!(out, "{}", self.value);
		out.push(',');
		out.push_str("\"reason\":");
		self.reason.to_json_into(out);
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("state=");
		self.state.to_text_into(out);
		out.push_str(", ");
		out.push_str("quantity=");
		self.quantity.to_text_into(out);
		out.push_str(", ");
		out.push_str("value=");
		let _ = write!(out, "{}", self.value);
		out.push_str(", ");
		out.push_str("reason=");
		self.reason.to_text_into(out);
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 4);
		crate::codec::cbor::text(out, "state");
		self.state.to_cbor_into(out);
		crate::codec::cbor::text(out, "quantity");
		self.quantity.to_cbor_into(out);
		crate::codec::cbor::text(out, "value");
		crate::codec::cbor::uint(out, self.value as u64);
		crate::codec::cbor::text(out, "reason");
		self.reason.to_cbor_into(out);
	}
}

impl ChargeState {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		match self {
			ChargeState::Unknown => out.push_str("\"unknown\""),
			ChargeState::Charging => out.push_str("\"charging\""),
			ChargeState::Discharging => out.push_str("\"discharging\""),
			ChargeState::Idle => out.push_str("\"idle\""),
			ChargeState::Invalid => out.push_str("\"invalid\""),
		}
	}
	pub fn to_text_into(&self, out: &mut String) {
		match self {
			ChargeState::Unknown => out.push_str("unknown"),
			ChargeState::Charging => out.push_str("charging"),
			ChargeState::Discharging => out.push_str("discharging"),
			ChargeState::Idle => out.push_str("idle"),
			ChargeState::Invalid => out.push_str("invalid"),
		}
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		match self {
			ChargeState::Unknown => crate::codec::cbor::text(out, "unknown"),
			ChargeState::Charging => crate::codec::cbor::text(out, "charging"),
			ChargeState::Discharging => crate::codec::cbor::text(out, "discharging"),
			ChargeState::Idle => crate::codec::cbor::text(out, "idle"),
			ChargeState::Invalid => crate::codec::cbor::text(out, "invalid"),
		}
	}
}

impl TemperatureReference {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		match self {
			TemperatureReference::Absolute => out.push_str("\"absolute\""),
			TemperatureReference::Relative => out.push_str("\"relative\""),
		}
	}
	pub fn to_text_into(&self, out: &mut String) {
		match self {
			TemperatureReference::Absolute => out.push_str("absolute"),
			TemperatureReference::Relative => out.push_str("relative"),
		}
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		match self {
			TemperatureReference::Absolute => crate::codec::cbor::text(out, "absolute"),
			TemperatureReference::Relative => crate::codec::cbor::text(out, "relative"),
		}
	}
}

impl Temperature {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"state\":");
		self.state.to_json_into(out);
		out.push(',');
		out.push_str("\"reference\":");
		self.reference.to_json_into(out);
		out.push(',');
		out.push_str("\"value\":");
		let _ = write!(out, "{}", self.value);
		out.push(',');
		out.push_str("\"reason\":");
		self.reason.to_json_into(out);
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("state=");
		self.state.to_text_into(out);
		out.push_str(", ");
		out.push_str("reference=");
		self.reference.to_text_into(out);
		out.push_str(", ");
		out.push_str("value=");
		let _ = write!(out, "{}", self.value);
		out.push_str(", ");
		out.push_str("reason=");
		self.reason.to_text_into(out);
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 4);
		crate::codec::cbor::text(out, "state");
		self.state.to_cbor_into(out);
		crate::codec::cbor::text(out, "reference");
		self.reference.to_cbor_into(out);
		crate::codec::cbor::text(out, "value");
		crate::codec::cbor::int(out, self.value as i64);
		crate::codec::cbor::text(out, "reason");
		self.reason.to_cbor_into(out);
	}
}

impl TripKind {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		match self {
			TripKind::Passive => out.push_str("\"passive\""),
			TripKind::Active => out.push_str("\"active\""),
			TripKind::Hot => out.push_str("\"hot\""),
			TripKind::Critical => out.push_str("\"critical\""),
		}
	}
	pub fn to_text_into(&self, out: &mut String) {
		match self {
			TripKind::Passive => out.push_str("passive"),
			TripKind::Active => out.push_str("active"),
			TripKind::Hot => out.push_str("hot"),
			TripKind::Critical => out.push_str("critical"),
		}
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		match self {
			TripKind::Passive => crate::codec::cbor::text(out, "passive"),
			TripKind::Active => crate::codec::cbor::text(out, "active"),
			TripKind::Hot => crate::codec::cbor::text(out, "hot"),
			TripKind::Critical => crate::codec::cbor::text(out, "critical"),
		}
	}
}

impl TripPoint {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"kind\":");
		self.kind.to_json_into(out);
		out.push(',');
		out.push_str("\"index\":");
		let _ = write!(out, "{}", self.index);
		out.push(',');
		out.push_str("\"temperature\":");
		self.temperature.to_json_into(out);
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("kind=");
		self.kind.to_text_into(out);
		out.push_str(", ");
		out.push_str("index=");
		let _ = write!(out, "{}", self.index);
		out.push_str(", ");
		out.push_str("temperature=");
		self.temperature.to_text_into(out);
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 3);
		crate::codec::cbor::text(out, "kind");
		self.kind.to_cbor_into(out);
		crate::codec::cbor::text(out, "index");
		crate::codec::cbor::uint(out, self.index as u64);
		crate::codec::cbor::text(out, "temperature");
		self.temperature.to_cbor_into(out);
	}
}

impl AlarmKind {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		match self {
			AlarmKind::OnBattery => out.push_str("\"on-battery\""),
			AlarmKind::LowCapacity => out.push_str("\"low-capacity\""),
			AlarmKind::CriticalCapacity => out.push_str("\"critical-capacity\""),
			AlarmKind::Overload => out.push_str("\"overload\""),
			AlarmKind::ReplaceBattery => out.push_str("\"replace-battery\""),
			AlarmKind::SourceFault => out.push_str("\"source-fault\""),
			AlarmKind::OverTemperature => out.push_str("\"over-temperature\""),
		}
	}
	pub fn to_text_into(&self, out: &mut String) {
		match self {
			AlarmKind::OnBattery => out.push_str("on-battery"),
			AlarmKind::LowCapacity => out.push_str("low-capacity"),
			AlarmKind::CriticalCapacity => out.push_str("critical-capacity"),
			AlarmKind::Overload => out.push_str("overload"),
			AlarmKind::ReplaceBattery => out.push_str("replace-battery"),
			AlarmKind::SourceFault => out.push_str("source-fault"),
			AlarmKind::OverTemperature => out.push_str("over-temperature"),
		}
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		match self {
			AlarmKind::OnBattery => crate::codec::cbor::text(out, "on-battery"),
			AlarmKind::LowCapacity => crate::codec::cbor::text(out, "low-capacity"),
			AlarmKind::CriticalCapacity => crate::codec::cbor::text(out, "critical-capacity"),
			AlarmKind::Overload => crate::codec::cbor::text(out, "overload"),
			AlarmKind::ReplaceBattery => crate::codec::cbor::text(out, "replace-battery"),
			AlarmKind::SourceFault => crate::codec::cbor::text(out, "source-fault"),
			AlarmKind::OverTemperature => crate::codec::cbor::text(out, "over-temperature"),
		}
	}
}

impl Provenance {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		match self {
			Provenance::Reported => out.push_str("\"reported\""),
			Provenance::Derived => out.push_str("\"derived\""),
		}
	}
	pub fn to_text_into(&self, out: &mut String) {
		match self {
			Provenance::Reported => out.push_str("reported"),
			Provenance::Derived => out.push_str("derived"),
		}
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		match self {
			Provenance::Reported => crate::codec::cbor::text(out, "reported"),
			Provenance::Derived => crate::codec::cbor::text(out, "derived"),
		}
	}
}

impl Alarm {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"kind\":");
		self.kind.to_json_into(out);
		out.push(',');
		out.push_str("\"state\":");
		self.state.to_json_into(out);
		out.push(',');
		out.push_str("\"provenance\":");
		self.provenance.to_json_into(out);
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("kind=");
		self.kind.to_text_into(out);
		out.push_str(", ");
		out.push_str("state=");
		self.state.to_text_into(out);
		out.push_str(", ");
		out.push_str("provenance=");
		self.provenance.to_text_into(out);
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 3);
		crate::codec::cbor::text(out, "kind");
		self.kind.to_cbor_into(out);
		crate::codec::cbor::text(out, "state");
		self.state.to_cbor_into(out);
		crate::codec::cbor::text(out, "provenance");
		self.provenance.to_cbor_into(out);
	}
}

impl Controls {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"set-output\":");
		if self.set_output {
			out.push_str("true");
		} else {
			out.push_str("false");
		}
		out.push(',');
		out.push_str("\"schedule-off\":");
		if self.schedule_off {
			out.push_str("true");
		} else {
			out.push_str("false");
		}
		out.push(',');
		out.push_str("\"cancel-off\":");
		if self.cancel_off {
			out.push_str("true");
		} else {
			out.push_str("false");
		}
		out.push(',');
		out.push_str("\"outlets\":");
		let _ = write!(out, "{}", self.outlets);
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("set-output=");
		if self.set_output {
			out.push_str("true");
		} else {
			out.push_str("false");
		}
		out.push_str(", ");
		out.push_str("schedule-off=");
		if self.schedule_off {
			out.push_str("true");
		} else {
			out.push_str("false");
		}
		out.push_str(", ");
		out.push_str("cancel-off=");
		if self.cancel_off {
			out.push_str("true");
		} else {
			out.push_str("false");
		}
		out.push_str(", ");
		out.push_str("outlets=");
		let _ = write!(out, "{}", self.outlets);
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 4);
		crate::codec::cbor::text(out, "set-output");
		crate::codec::cbor::boolean(out, self.set_output);
		crate::codec::cbor::text(out, "schedule-off");
		crate::codec::cbor::boolean(out, self.schedule_off);
		crate::codec::cbor::text(out, "cancel-off");
		crate::codec::cbor::boolean(out, self.cancel_off);
		crate::codec::cbor::text(out, "outlets");
		crate::codec::cbor::uint(out, self.outlets as u64);
	}
}

impl SourceState {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"kind\":");
		self.kind.to_json_into(out);
		out.push(',');
		out.push_str("\"present\":");
		self.present.to_json_into(out);
		out.push(',');
		out.push_str("\"online\":");
		self.online.to_json_into(out);
		out.push(',');
		out.push_str("\"charge\":");
		self.charge.to_json_into(out);
		out.push(',');
		out.push_str("\"remaining\":");
		self.remaining.to_json_into(out);
		out.push(',');
		out.push_str("\"full\":");
		self.full.to_json_into(out);
		out.push(',');
		out.push_str("\"design\":");
		self.design.to_json_into(out);
		out.push(',');
		out.push_str("\"state-of-charge\":");
		self.state_of_charge.to_json_into(out);
		out.push(',');
		out.push_str("\"voltage\":");
		self.voltage.to_json_into(out);
		out.push(',');
		out.push_str("\"current\":");
		self.current.to_json_into(out);
		out.push(',');
		out.push_str("\"power\":");
		self.power.to_json_into(out);
		out.push(',');
		out.push_str("\"runtime\":");
		self.runtime.to_json_into(out);
		out.push(',');
		out.push_str("\"load\":");
		self.load.to_json_into(out);
		out.push(',');
		out.push_str("\"temperature\":");
		self.temperature.to_json_into(out);
		out.push(',');
		out.push_str("\"trips\":");
		out.push('[');
		let mut v49 = true;
		for v48 in self.trips.iter() {
			if !v49 {
				out.push(',');
			}
			v49 = false;
			v48.to_json_into(out);
		}
		out.push(']');
		out.push(',');
		out.push_str("\"alarms\":");
		out.push('[');
		let mut v51 = true;
		for v50 in self.alarms.iter() {
			if !v51 {
				out.push(',');
			}
			v51 = false;
			v50.to_json_into(out);
		}
		out.push(']');
		out.push(',');
		out.push_str("\"controls\":");
		self.controls.to_json_into(out);
		out.push(',');
		out.push_str("\"source-time\":");
		match &self.source_time {
			Some(v52) => {
				let _ = write!(out, "{}", v52);
			}
			None => {
				out.push_str("null");
			}
		}
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("kind=");
		self.kind.to_text_into(out);
		out.push_str(", ");
		out.push_str("present=");
		self.present.to_text_into(out);
		out.push_str(", ");
		out.push_str("online=");
		self.online.to_text_into(out);
		out.push_str(", ");
		out.push_str("charge=");
		self.charge.to_text_into(out);
		out.push_str(", ");
		out.push_str("remaining=");
		self.remaining.to_text_into(out);
		out.push_str(", ");
		out.push_str("full=");
		self.full.to_text_into(out);
		out.push_str(", ");
		out.push_str("design=");
		self.design.to_text_into(out);
		out.push_str(", ");
		out.push_str("state-of-charge=");
		self.state_of_charge.to_text_into(out);
		out.push_str(", ");
		out.push_str("voltage=");
		self.voltage.to_text_into(out);
		out.push_str(", ");
		out.push_str("current=");
		self.current.to_text_into(out);
		out.push_str(", ");
		out.push_str("power=");
		self.power.to_text_into(out);
		out.push_str(", ");
		out.push_str("runtime=");
		self.runtime.to_text_into(out);
		out.push_str(", ");
		out.push_str("load=");
		self.load.to_text_into(out);
		out.push_str(", ");
		out.push_str("temperature=");
		self.temperature.to_text_into(out);
		out.push_str(", ");
		out.push_str("trips=");
		out.push('[');
		let mut v54 = true;
		for v53 in self.trips.iter() {
			if !v54 {
				out.push_str(", ");
			}
			v54 = false;
			v53.to_text_into(out);
		}
		out.push(']');
		out.push_str(", ");
		out.push_str("alarms=");
		out.push('[');
		let mut v56 = true;
		for v55 in self.alarms.iter() {
			if !v56 {
				out.push_str(", ");
			}
			v56 = false;
			v55.to_text_into(out);
		}
		out.push(']');
		out.push_str(", ");
		out.push_str("controls=");
		self.controls.to_text_into(out);
		out.push_str(", ");
		out.push_str("source-time=");
		match &self.source_time {
			Some(v57) => {
				let _ = write!(out, "{}", v57);
			}
			None => {
				out.push('-');
			}
		}
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 18);
		crate::codec::cbor::text(out, "kind");
		self.kind.to_cbor_into(out);
		crate::codec::cbor::text(out, "present");
		self.present.to_cbor_into(out);
		crate::codec::cbor::text(out, "online");
		self.online.to_cbor_into(out);
		crate::codec::cbor::text(out, "charge");
		self.charge.to_cbor_into(out);
		crate::codec::cbor::text(out, "remaining");
		self.remaining.to_cbor_into(out);
		crate::codec::cbor::text(out, "full");
		self.full.to_cbor_into(out);
		crate::codec::cbor::text(out, "design");
		self.design.to_cbor_into(out);
		crate::codec::cbor::text(out, "state-of-charge");
		self.state_of_charge.to_cbor_into(out);
		crate::codec::cbor::text(out, "voltage");
		self.voltage.to_cbor_into(out);
		crate::codec::cbor::text(out, "current");
		self.current.to_cbor_into(out);
		crate::codec::cbor::text(out, "power");
		self.power.to_cbor_into(out);
		crate::codec::cbor::text(out, "runtime");
		self.runtime.to_cbor_into(out);
		crate::codec::cbor::text(out, "load");
		self.load.to_cbor_into(out);
		crate::codec::cbor::text(out, "temperature");
		self.temperature.to_cbor_into(out);
		crate::codec::cbor::text(out, "trips");
		crate::codec::cbor::array(out, self.trips.len());
		for v58 in self.trips.iter() {
			v58.to_cbor_into(out);
		}
		crate::codec::cbor::text(out, "alarms");
		crate::codec::cbor::array(out, self.alarms.len());
		for v59 in self.alarms.iter() {
			v59.to_cbor_into(out);
		}
		crate::codec::cbor::text(out, "controls");
		self.controls.to_cbor_into(out);
		crate::codec::cbor::text(out, "source-time");
		match &self.source_time {
			Some(v60) => {
				crate::codec::cbor::uint(out, *v60 as u64);
			}
			None => {
				crate::codec::cbor::null(out);
			}
		}
	}
}

impl SourceId {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"slot\":");
		let _ = write!(out, "{}", self.slot);
		out.push(',');
		out.push_str("\"generation\":");
		let _ = write!(out, "{}", self.generation);
		out.push(',');
		out.push_str("\"binding-generation\":");
		let _ = write!(out, "{}", self.binding_generation);
		out.push(',');
		out.push_str("\"local\":");
		let _ = write!(out, "{}", self.local);
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("slot=");
		let _ = write!(out, "{}", self.slot);
		out.push_str(", ");
		out.push_str("generation=");
		let _ = write!(out, "{}", self.generation);
		out.push_str(", ");
		out.push_str("binding-generation=");
		let _ = write!(out, "{}", self.binding_generation);
		out.push_str(", ");
		out.push_str("local=");
		let _ = write!(out, "{}", self.local);
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 4);
		crate::codec::cbor::text(out, "slot");
		crate::codec::cbor::uint(out, self.slot as u64);
		crate::codec::cbor::text(out, "generation");
		crate::codec::cbor::uint(out, self.generation as u64);
		crate::codec::cbor::text(out, "binding-generation");
		crate::codec::cbor::uint(out, self.binding_generation as u64);
		crate::codec::cbor::text(out, "local");
		crate::codec::cbor::uint(out, self.local as u64);
	}
}

impl SourceSnapshot {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"id\":");
		self.id.to_json_into(out);
		out.push(',');
		out.push_str("\"received\":");
		let _ = write!(out, "{}", self.received);
		out.push(',');
		out.push_str("\"state\":");
		self.state.to_json_into(out);
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("id=");
		self.id.to_text_into(out);
		out.push_str(", ");
		out.push_str("received=");
		let _ = write!(out, "{}", self.received);
		out.push_str(", ");
		out.push_str("state=");
		self.state.to_text_into(out);
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 3);
		crate::codec::cbor::text(out, "id");
		self.id.to_cbor_into(out);
		crate::codec::cbor::text(out, "received");
		crate::codec::cbor::uint(out, self.received as u64);
		crate::codec::cbor::text(out, "state");
		self.state.to_cbor_into(out);
	}
}

impl ChangeKind {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		match self {
			ChangeKind::Snapshot => out.push_str("\"snapshot\""),
			ChangeKind::SnapshotEnd => out.push_str("\"snapshot-end\""),
			ChangeKind::Added => out.push_str("\"added\""),
			ChangeKind::Updated => out.push_str("\"updated\""),
			ChangeKind::Removed => out.push_str("\"removed\""),
		}
	}
	pub fn to_text_into(&self, out: &mut String) {
		match self {
			ChangeKind::Snapshot => out.push_str("snapshot"),
			ChangeKind::SnapshotEnd => out.push_str("snapshot-end"),
			ChangeKind::Added => out.push_str("added"),
			ChangeKind::Updated => out.push_str("updated"),
			ChangeKind::Removed => out.push_str("removed"),
		}
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		match self {
			ChangeKind::Snapshot => crate::codec::cbor::text(out, "snapshot"),
			ChangeKind::SnapshotEnd => crate::codec::cbor::text(out, "snapshot-end"),
			ChangeKind::Added => crate::codec::cbor::text(out, "added"),
			ChangeKind::Updated => crate::codec::cbor::text(out, "updated"),
			ChangeKind::Removed => crate::codec::cbor::text(out, "removed"),
		}
	}
}

impl PowerChange {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"epoch\":");
		let _ = write!(out, "{}", self.epoch);
		out.push(',');
		out.push_str("\"revision\":");
		let _ = write!(out, "{}", self.revision);
		out.push(',');
		out.push_str("\"kind\":");
		self.kind.to_json_into(out);
		out.push(',');
		out.push_str("\"source\":");
		match &self.source {
			Some(v61) => {
				v61.to_json_into(out);
			}
			None => {
				out.push_str("null");
			}
		}
		out.push(',');
		out.push_str("\"gone\":");
		match &self.gone {
			Some(v62) => {
				v62.to_json_into(out);
			}
			None => {
				out.push_str("null");
			}
		}
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("epoch=");
		let _ = write!(out, "{}", self.epoch);
		out.push_str(", ");
		out.push_str("revision=");
		let _ = write!(out, "{}", self.revision);
		out.push_str(", ");
		out.push_str("kind=");
		self.kind.to_text_into(out);
		out.push_str(", ");
		out.push_str("source=");
		match &self.source {
			Some(v63) => {
				v63.to_text_into(out);
			}
			None => {
				out.push('-');
			}
		}
		out.push_str(", ");
		out.push_str("gone=");
		match &self.gone {
			Some(v64) => {
				v64.to_text_into(out);
			}
			None => {
				out.push('-');
			}
		}
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 5);
		crate::codec::cbor::text(out, "epoch");
		crate::codec::cbor::uint(out, self.epoch as u64);
		crate::codec::cbor::text(out, "revision");
		crate::codec::cbor::uint(out, self.revision as u64);
		crate::codec::cbor::text(out, "kind");
		self.kind.to_cbor_into(out);
		crate::codec::cbor::text(out, "source");
		match &self.source {
			Some(v65) => {
				v65.to_cbor_into(out);
			}
			None => {
				crate::codec::cbor::null(out);
			}
		}
		crate::codec::cbor::text(out, "gone");
		match &self.gone {
			Some(v66) => {
				v66.to_cbor_into(out);
			}
			None => {
				crate::codec::cbor::null(out);
			}
		}
	}
}

impl ControlOutcome {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		match self {
			ControlOutcome::Done => out.push_str("\"done\""),
			ControlOutcome::Indeterminate => out.push_str("\"indeterminate\""),
		}
	}
	pub fn to_text_into(&self, out: &mut String) {
		match self {
			ControlOutcome::Done => out.push_str("done"),
			ControlOutcome::Indeterminate => out.push_str("indeterminate"),
		}
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		match self {
			ControlOutcome::Done => crate::codec::cbor::text(out, "done"),
			ControlOutcome::Indeterminate => crate::codec::cbor::text(out, "indeterminate"),
		}
	}
}

impl ProviderSource {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"local\":");
		let _ = write!(out, "{}", self.local);
		out.push(',');
		out.push_str("\"state\":");
		self.state.to_json_into(out);
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("local=");
		let _ = write!(out, "{}", self.local);
		out.push_str(", ");
		out.push_str("state=");
		self.state.to_text_into(out);
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 2);
		crate::codec::cbor::text(out, "local");
		crate::codec::cbor::uint(out, self.local as u64);
		crate::codec::cbor::text(out, "state");
		self.state.to_cbor_into(out);
	}
}

impl ProviderUpdateKind {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		match self {
			ProviderUpdateKind::Snapshot => out.push_str("\"snapshot\""),
			ProviderUpdateKind::SnapshotEnd => out.push_str("\"snapshot-end\""),
			ProviderUpdateKind::Added => out.push_str("\"added\""),
			ProviderUpdateKind::Updated => out.push_str("\"updated\""),
			ProviderUpdateKind::Removed => out.push_str("\"removed\""),
		}
	}
	pub fn to_text_into(&self, out: &mut String) {
		match self {
			ProviderUpdateKind::Snapshot => out.push_str("snapshot"),
			ProviderUpdateKind::SnapshotEnd => out.push_str("snapshot-end"),
			ProviderUpdateKind::Added => out.push_str("added"),
			ProviderUpdateKind::Updated => out.push_str("updated"),
			ProviderUpdateKind::Removed => out.push_str("removed"),
		}
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		match self {
			ProviderUpdateKind::Snapshot => crate::codec::cbor::text(out, "snapshot"),
			ProviderUpdateKind::SnapshotEnd => crate::codec::cbor::text(out, "snapshot-end"),
			ProviderUpdateKind::Added => crate::codec::cbor::text(out, "added"),
			ProviderUpdateKind::Updated => crate::codec::cbor::text(out, "updated"),
			ProviderUpdateKind::Removed => crate::codec::cbor::text(out, "removed"),
		}
	}
}

impl ProviderUpdate {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"revision\":");
		let _ = write!(out, "{}", self.revision);
		out.push(',');
		out.push_str("\"kind\":");
		self.kind.to_json_into(out);
		out.push(',');
		out.push_str("\"source\":");
		match &self.source {
			Some(v67) => {
				v67.to_json_into(out);
			}
			None => {
				out.push_str("null");
			}
		}
		out.push(',');
		out.push_str("\"gone\":");
		match &self.gone {
			Some(v68) => {
				let _ = write!(out, "{}", v68);
			}
			None => {
				out.push_str("null");
			}
		}
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("revision=");
		let _ = write!(out, "{}", self.revision);
		out.push_str(", ");
		out.push_str("kind=");
		self.kind.to_text_into(out);
		out.push_str(", ");
		out.push_str("source=");
		match &self.source {
			Some(v69) => {
				v69.to_text_into(out);
			}
			None => {
				out.push('-');
			}
		}
		out.push_str(", ");
		out.push_str("gone=");
		match &self.gone {
			Some(v70) => {
				let _ = write!(out, "{}", v70);
			}
			None => {
				out.push('-');
			}
		}
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 4);
		crate::codec::cbor::text(out, "revision");
		crate::codec::cbor::uint(out, self.revision as u64);
		crate::codec::cbor::text(out, "kind");
		self.kind.to_cbor_into(out);
		crate::codec::cbor::text(out, "source");
		match &self.source {
			Some(v71) => {
				v71.to_cbor_into(out);
			}
			None => {
				crate::codec::cbor::null(out);
			}
		}
		crate::codec::cbor::text(out, "gone");
		match &self.gone {
			Some(v72) => {
				crate::codec::cbor::uint(out, *v72 as u64);
			}
			None => {
				crate::codec::cbor::null(out);
			}
		}
	}
}

impl ProviderCommandKind {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		match self {
			ProviderCommandKind::SetOutput => out.push_str("\"set-output\""),
			ProviderCommandKind::ScheduleOutputOff => out.push_str("\"schedule-output-off\""),
			ProviderCommandKind::CancelOutputOff => out.push_str("\"cancel-output-off\""),
		}
	}
	pub fn to_text_into(&self, out: &mut String) {
		match self {
			ProviderCommandKind::SetOutput => out.push_str("set-output"),
			ProviderCommandKind::ScheduleOutputOff => out.push_str("schedule-output-off"),
			ProviderCommandKind::CancelOutputOff => out.push_str("cancel-output-off"),
		}
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		match self {
			ProviderCommandKind::SetOutput => crate::codec::cbor::text(out, "set-output"),
			ProviderCommandKind::ScheduleOutputOff => crate::codec::cbor::text(out, "schedule-output-off"),
			ProviderCommandKind::CancelOutputOff => crate::codec::cbor::text(out, "cancel-output-off"),
		}
	}
}

impl ProviderCommand {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"kind\":");
		self.kind.to_json_into(out);
		out.push(',');
		out.push_str("\"local\":");
		let _ = write!(out, "{}", self.local);
		out.push(',');
		out.push_str("\"outlet\":");
		let _ = write!(out, "{}", self.outlet);
		out.push(',');
		out.push_str("\"on\":");
		if self.on {
			out.push_str("true");
		} else {
			out.push_str("false");
		}
		out.push(',');
		out.push_str("\"delay-seconds\":");
		let _ = write!(out, "{}", self.delay_seconds);
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("kind=");
		self.kind.to_text_into(out);
		out.push_str(", ");
		out.push_str("local=");
		let _ = write!(out, "{}", self.local);
		out.push_str(", ");
		out.push_str("outlet=");
		let _ = write!(out, "{}", self.outlet);
		out.push_str(", ");
		out.push_str("on=");
		if self.on {
			out.push_str("true");
		} else {
			out.push_str("false");
		}
		out.push_str(", ");
		out.push_str("delay-seconds=");
		let _ = write!(out, "{}", self.delay_seconds);
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 5);
		crate::codec::cbor::text(out, "kind");
		self.kind.to_cbor_into(out);
		crate::codec::cbor::text(out, "local");
		crate::codec::cbor::uint(out, self.local as u64);
		crate::codec::cbor::text(out, "outlet");
		crate::codec::cbor::uint(out, self.outlet as u64);
		crate::codec::cbor::text(out, "on");
		crate::codec::cbor::boolean(out, self.on);
		crate::codec::cbor::text(out, "delay-seconds");
		crate::codec::cbor::uint(out, self.delay_seconds as u64);
	}
}

impl FixtureField {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		match self {
			FixtureField::UpsRemaining => out.push_str("\"ups-remaining\""),
			FixtureField::UpsVoltage => out.push_str("\"ups-voltage\""),
			FixtureField::UpsAcPresent => out.push_str("\"ups-ac-present\""),
			FixtureField::UpsOverload => out.push_str("\"ups-overload\""),
			FixtureField::BatteryRemaining => out.push_str("\"battery-remaining\""),
			FixtureField::BatteryState => out.push_str("\"battery-state\""),
			FixtureField::AcOnline => out.push_str("\"ac-online\""),
			FixtureField::ThermalTemperature => out.push_str("\"thermal-temperature\""),
		}
	}
	pub fn to_text_into(&self, out: &mut String) {
		match self {
			FixtureField::UpsRemaining => out.push_str("ups-remaining"),
			FixtureField::UpsVoltage => out.push_str("ups-voltage"),
			FixtureField::UpsAcPresent => out.push_str("ups-ac-present"),
			FixtureField::UpsOverload => out.push_str("ups-overload"),
			FixtureField::BatteryRemaining => out.push_str("battery-remaining"),
			FixtureField::BatteryState => out.push_str("battery-state"),
			FixtureField::AcOnline => out.push_str("ac-online"),
			FixtureField::ThermalTemperature => out.push_str("thermal-temperature"),
		}
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		match self {
			FixtureField::UpsRemaining => crate::codec::cbor::text(out, "ups-remaining"),
			FixtureField::UpsVoltage => crate::codec::cbor::text(out, "ups-voltage"),
			FixtureField::UpsAcPresent => crate::codec::cbor::text(out, "ups-ac-present"),
			FixtureField::UpsOverload => crate::codec::cbor::text(out, "ups-overload"),
			FixtureField::BatteryRemaining => crate::codec::cbor::text(out, "battery-remaining"),
			FixtureField::BatteryState => crate::codec::cbor::text(out, "battery-state"),
			FixtureField::AcOnline => crate::codec::cbor::text(out, "ac-online"),
			FixtureField::ThermalTemperature => crate::codec::cbor::text(out, "thermal-temperature"),
		}
	}
}

impl FixtureCommand {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"publication\":");
		let _ = write!(out, "{}", self.publication);
		out.push(',');
		out.push_str("\"local\":");
		let _ = write!(out, "{}", self.local);
		out.push(',');
		out.push_str("\"kind\":");
		self.kind.to_json_into(out);
		out.push(',');
		out.push_str("\"outlet\":");
		let _ = write!(out, "{}", self.outlet);
		out.push(',');
		out.push_str("\"on\":");
		if self.on {
			out.push_str("true");
		} else {
			out.push_str("false");
		}
		out.push(',');
		out.push_str("\"delay-seconds\":");
		let _ = write!(out, "{}", self.delay_seconds);
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("publication=");
		let _ = write!(out, "{}", self.publication);
		out.push_str(", ");
		out.push_str("local=");
		let _ = write!(out, "{}", self.local);
		out.push_str(", ");
		out.push_str("kind=");
		self.kind.to_text_into(out);
		out.push_str(", ");
		out.push_str("outlet=");
		let _ = write!(out, "{}", self.outlet);
		out.push_str(", ");
		out.push_str("on=");
		if self.on {
			out.push_str("true");
		} else {
			out.push_str("false");
		}
		out.push_str(", ");
		out.push_str("delay-seconds=");
		let _ = write!(out, "{}", self.delay_seconds);
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 6);
		crate::codec::cbor::text(out, "publication");
		crate::codec::cbor::uint(out, self.publication as u64);
		crate::codec::cbor::text(out, "local");
		crate::codec::cbor::uint(out, self.local as u64);
		crate::codec::cbor::text(out, "kind");
		self.kind.to_cbor_into(out);
		crate::codec::cbor::text(out, "outlet");
		crate::codec::cbor::uint(out, self.outlet as u64);
		crate::codec::cbor::text(out, "on");
		crate::codec::cbor::boolean(out, self.on);
		crate::codec::cbor::text(out, "delay-seconds");
		crate::codec::cbor::uint(out, self.delay_seconds as u64);
	}
}

#[cfg(test)]
mod compat;
