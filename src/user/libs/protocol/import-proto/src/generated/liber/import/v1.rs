// @generated by lsidl-gen from ../../idl/import.lsidl (package liber:import@1).
// Do not edit by hand; regenerate with `./gen.sh`.
//! LiberSystem media import - objects read from a camera's Picture Transfer Protocol storage, for a component
//! PermissionManager granted `media-import`.
//!
//! A CAMERA IS AN OBJECT SOURCE, NOT A VOLUME. A client lists devices and their storages, takes a bounded
//! snapshot of one storage's object handles and pages through it for typed metadata, and reads one object at a
//! time in bounded chunks. It cannot send a command, reach a catalogue or a backend, write anywhere, delete or
//! capture: the service is a source and nothing else, and it holds no storage of its own to write into.
//!
//! EVERY IDENTITY IS SCOPED AND EXPIRES. A device, storage or object identity carries the publication, the
//! attachment epoch, the PTP session, the content epoch and the service incarnation. A change the device
//! reports, a dropped event, a reset, a lost session or a withdrawal advances one of them, and everything named
//! under the old one is stale - a numeric object handle alone is never enough to reach an object again.
//!
//! COMPLETION IS NEVER INFERRED. A transfer ends `complete` only after every expected byte and the device's
//! matching successful final response; anything else is `partial`, with the count delivered and the cause. A
//! short or empty chunk is not an end, and a lost connection without a final status is an unknown outcome.
//!
//! WHAT A CONSUMER OWES ITS DESTINATION. Publish a normal, completed destination only after a `complete`
//! ending. A storage consumer writes through `volume.open-writer`, commits only on `complete`, and aborts on
//! `partial`, on an error and on a lost connection - within that writer's own size cap. An import larger than
//! that cap needs a staging consumer specified for it; nothing here promises one.
#![allow(dead_code, unused_imports, unused_variables, unused_mut, clippy::all)]

use crate::codec::{Handles, PROTOCOL_INFO_OP, Reader, Sink, SliceWriter, VecWriter};
use alloc::string::String;
use alloc::vec::Vec;
use core::fmt::Write as _;

pub use crate::generated::liber::base::v1::Error;

/// A camera as this service knows it.
#[derive(Clone, Debug, PartialEq)]
pub struct ImportDeviceId {
	pub slot: u32,
	pub generation: u32,
	pub binding_generation: u64,
	/// Advanced by every reset and recovery.
	pub attachment: u64,
	/// The PTP session this service opened on it.
	pub session: u32,
	/// Advanced by every change the device reports, and by every event this service could not receive.
	pub content_epoch: u64,
	pub incarnation: u64,
	/// The client context this identity was issued to: another client's identities are not this one's to use.
	pub context: u32,
}

impl ImportDeviceId {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<ImportDeviceId> {
		let mut r = Reader::new(bytes);
		let value = ImportDeviceId::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<ImportDeviceId> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = ImportDeviceId::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u32(self.slot)?;
		w.u32(self.generation)?;
		w.u64(self.binding_generation)?;
		w.u64(self.attachment)?;
		w.u32(self.session)?;
		w.u64(self.content_epoch)?;
		w.u64(self.incarnation)?;
		w.u32(self.context)?;
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<ImportDeviceId> {
		let slot = r.u32()?;
		let generation = r.u32()?;
		let binding_generation = r.u64()?;
		let attachment = r.u64()?;
		let session = r.u32()?;
		let content_epoch = r.u64()?;
		let incarnation = r.u64()?;
		let context = r.u32()?;
		Some(ImportDeviceId { slot, generation, binding_generation, attachment, session, content_epoch, incarnation, context })
	}
}

#[derive(Clone, Debug, PartialEq)]
pub struct ImportStorageId {
	pub device: ImportDeviceId,
	pub storage: u32,
}

impl ImportStorageId {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<ImportStorageId> {
		let mut r = Reader::new(bytes);
		let value = ImportStorageId::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<ImportStorageId> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = ImportStorageId::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		self.device.write(w)?;
		w.u32(self.storage)?;
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<ImportStorageId> {
		let device = ImportDeviceId::read(r)?;
		let storage = r.u32()?;
		Some(ImportStorageId { device, storage })
	}
}

#[derive(Clone, Debug, PartialEq)]
pub struct ImportObjectId {
	pub device: ImportDeviceId,
	pub handle: u32,
	/// The revision of the object's metadata as it was fetched: a read revalidates it, and a change is stale.
	pub revision: u32,
}

impl ImportObjectId {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<ImportObjectId> {
		let mut r = Reader::new(bytes);
		let value = ImportObjectId::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<ImportObjectId> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = ImportObjectId::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		self.device.write(w)?;
		w.u32(self.handle)?;
		w.u32(self.revision)?;
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<ImportObjectId> {
		let device = ImportDeviceId::read(r)?;
		let handle = r.u32()?;
		let revision = r.u32()?;
		Some(ImportObjectId { device, handle, revision })
	}
}

/// An association - a folder - named as an enumeration's parent, or as an object's.
#[derive(Clone, Debug, PartialEq)]
pub struct ImportParentId {
	pub device: ImportDeviceId,
	pub handle: u32,
}

impl ImportParentId {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<ImportParentId> {
		let mut r = Reader::new(bytes);
		let value = ImportParentId::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<ImportParentId> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = ImportParentId::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		self.device.write(w)?;
		w.u32(self.handle)?;
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<ImportParentId> {
		let device = ImportDeviceId::read(r)?;
		let handle = r.u32()?;
		Some(ImportParentId { device, handle })
	}
}

/// The service's bounds, as it enforces them.
#[derive(Clone, Debug, PartialEq)]
pub struct ImportLimits {
	pub providers: u8,
	pub clients: u8,
	pub cursors: u8,
	pub transfers: u8,
	pub cursors_per_client: u8,
	pub transfers_per_client: u8,
	pub cursors_per_device: u8,
	pub transfers_per_device: u8,
	pub data_budget: u32,
	pub snapshot_budget: u32,
	pub max_handles: u32,
	pub page_records: u8,
	pub page_bytes: u32,
	pub record_bytes: u32,
	pub chunk_bytes: u32,
	pub cursor_idle_seconds: u32,
	pub transfer_idle_seconds: u32,
}

impl ImportLimits {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<ImportLimits> {
		let mut r = Reader::new(bytes);
		let value = ImportLimits::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<ImportLimits> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = ImportLimits::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u8(self.providers)?;
		w.u8(self.clients)?;
		w.u8(self.cursors)?;
		w.u8(self.transfers)?;
		w.u8(self.cursors_per_client)?;
		w.u8(self.transfers_per_client)?;
		w.u8(self.cursors_per_device)?;
		w.u8(self.transfers_per_device)?;
		w.u32(self.data_budget)?;
		w.u32(self.snapshot_budget)?;
		w.u32(self.max_handles)?;
		w.u8(self.page_records)?;
		w.u32(self.page_bytes)?;
		w.u32(self.record_bytes)?;
		w.u32(self.chunk_bytes)?;
		w.u32(self.cursor_idle_seconds)?;
		w.u32(self.transfer_idle_seconds)?;
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<ImportLimits> {
		let providers = r.u8()?;
		let clients = r.u8()?;
		let cursors = r.u8()?;
		let transfers = r.u8()?;
		let cursors_per_client = r.u8()?;
		let transfers_per_client = r.u8()?;
		let cursors_per_device = r.u8()?;
		let transfers_per_device = r.u8()?;
		let data_budget = r.u32()?;
		let snapshot_budget = r.u32()?;
		let max_handles = r.u32()?;
		let page_records = r.u8()?;
		let page_bytes = r.u32()?;
		let record_bytes = r.u32()?;
		let chunk_bytes = r.u32()?;
		let cursor_idle_seconds = r.u32()?;
		let transfer_idle_seconds = r.u32()?;
		Some(ImportLimits { providers, clients, cursors, transfers, cursors_per_client, transfers_per_client, cursors_per_device, transfers_per_device, data_budget, snapshot_budget, max_handles, page_records, page_bytes, record_bytes, chunk_bytes, cursor_idle_seconds, transfer_idle_seconds })
	}
}

#[derive(Clone, Debug, PartialEq)]
pub struct ImportDevice {
	pub id: ImportDeviceId,
	pub manufacturer: String,
	pub model: String,
	pub serial: String,
	/// It offers every operation of the read-only subset this service needs. One that does not is listed,
	/// and everything else on it is `unsupported`.
	pub usable: bool,
	/// Another client's transaction is in progress on it now.
	pub busy: bool,
}

impl ImportDevice {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<ImportDevice> {
		let mut r = Reader::new(bytes);
		let value = ImportDevice::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<ImportDevice> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = ImportDevice::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		self.id.write(w)?;
		w.bytes_lp(self.manufacturer.as_bytes())?;
		w.bytes_lp(self.model.as_bytes())?;
		w.bytes_lp(self.serial.as_bytes())?;
		w.boolean(self.usable)?;
		w.boolean(self.busy)?;
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<ImportDevice> {
		let id = ImportDeviceId::read(r)?;
		let manufacturer = {
			let v0 = r.string_lp()?;
			(v0.len() <= 64).then_some(v0)?
		};
		let model = {
			let v1 = r.string_lp()?;
			(v1.len() <= 64).then_some(v1)?
		};
		let serial = {
			let v2 = r.string_lp()?;
			(v2.len() <= 64).then_some(v2)?
		};
		let usable = r.boolean()?;
		let busy = r.boolean()?;
		Some(ImportDevice { id, manufacturer, model, serial, usable, busy })
	}
}

#[derive(Clone, Debug, PartialEq)]
pub struct ImportStorage {
	pub id: ImportStorageId,
	pub storage_type: u16,
	pub filesystem_type: u16,
	pub access: u16,
	pub capacity: u64,
	pub free: u64,
	pub description: String,
	pub label: String,
}

impl ImportStorage {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<ImportStorage> {
		let mut r = Reader::new(bytes);
		let value = ImportStorage::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<ImportStorage> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = ImportStorage::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		self.id.write(w)?;
		w.u16(self.storage_type)?;
		w.u16(self.filesystem_type)?;
		w.u16(self.access)?;
		w.u64(self.capacity)?;
		w.u64(self.free)?;
		w.bytes_lp(self.description.as_bytes())?;
		w.bytes_lp(self.label.as_bytes())?;
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<ImportStorage> {
		let id = ImportStorageId::read(r)?;
		let storage_type = r.u16()?;
		let filesystem_type = r.u16()?;
		let access = r.u16()?;
		let capacity = r.u64()?;
		let free = r.u64()?;
		let description = {
			let v3 = r.string_lp()?;
			(v3.len() <= 64).then_some(v3)?
		};
		let label = {
			let v4 = r.string_lp()?;
			(v4.len() <= 64).then_some(v4)?
		};
		Some(ImportStorage { id, storage_type, filesystem_type, access, capacity, free, description, label })
	}
}

/// A calendar time exactly as the device wrote it; the offset is absent when the device named none.
#[derive(Clone, Debug, PartialEq)]
pub struct ImportTime {
	pub year: u16,
	pub month: u8,
	pub day: u8,
	pub hour: u8,
	pub minute: u8,
	pub second: u8,
	pub utc_offset_minutes: Option<i32>,
}

impl ImportTime {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<ImportTime> {
		let mut r = Reader::new(bytes);
		let value = ImportTime::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<ImportTime> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = ImportTime::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u16(self.year)?;
		w.u8(self.month)?;
		w.u8(self.day)?;
		w.u8(self.hour)?;
		w.u8(self.minute)?;
		w.u8(self.second)?;
		match &self.utc_offset_minutes {
			Some(v5) => {
				w.u8(1)?;
				w.i32(*v5)?;
			}
			None => {
				w.u8(0)?;
			}
		}
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<ImportTime> {
		let year = r.u16()?;
		let month = r.u8()?;
		let day = r.u8()?;
		let hour = r.u8()?;
		let minute = r.u8()?;
		let second = r.u8()?;
		let utc_offset_minutes = if r.tag()? { Some(r.i32()?) } else { None };
		Some(ImportTime { year, month, day, hour, minute, second, utc_offset_minutes })
	}
}

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
#[repr(u8)]
pub enum ImportInterpretation {
	/// Every typed field was read from the dataset.
	Complete = 1,
	/// A field could not be interpreted: it is absent from the typed fields, and still in `original`.
	Partial = 2,
}

impl ImportInterpretation {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<ImportInterpretation> {
		let mut r = Reader::new(bytes);
		let value = ImportInterpretation::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<ImportInterpretation> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = ImportInterpretation::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u8(*self as u8)
	}
	pub fn read(r: &mut Reader) -> Option<ImportInterpretation> {
		match r.u8()? {
			1 => Some(ImportInterpretation::Complete),
			2 => Some(ImportInterpretation::Partial),
			_ => None,
		}
	}
}

#[derive(Clone, Debug, PartialEq)]
pub struct ImportObject {
	pub id: ImportObjectId,
	/// The PTP object format code.
	pub format: u16,
	/// The code is one the standard defines.
	pub format_known: bool,
	pub size: Option<u64>,
	pub captured: Option<ImportTime>,
	pub parent: Option<ImportParentId>,
	pub filename: String,
	pub interpretation: ImportInterpretation,
	/// The ObjectInfo dataset exactly as the device sent it, unparsed and untruncated.
	pub original: Vec<u8>,
}

impl ImportObject {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<ImportObject> {
		let mut r = Reader::new(bytes);
		let value = ImportObject::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<ImportObject> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = ImportObject::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		self.id.write(w)?;
		w.u16(self.format)?;
		w.boolean(self.format_known)?;
		match &self.size {
			Some(v6) => {
				w.u8(1)?;
				w.u64(*v6)?;
			}
			None => {
				w.u8(0)?;
			}
		}
		match &self.captured {
			Some(v7) => {
				w.u8(1)?;
				v7.write(w)?;
			}
			None => {
				w.u8(0)?;
			}
		}
		match &self.parent {
			Some(v8) => {
				w.u8(1)?;
				v8.write(w)?;
			}
			None => {
				w.u8(0)?;
			}
		}
		w.bytes_lp(self.filename.as_bytes())?;
		self.interpretation.write(w)?;
		if self.original.len() > u16::MAX as usize {
			return None;
		}
		w.u16(self.original.len() as u16)?;
		for v9 in self.original.iter() {
			w.u8(*v9)?;
		}
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<ImportObject> {
		let id = ImportObjectId::read(r)?;
		let format = r.u16()?;
		let format_known = r.boolean()?;
		let size = if r.tag()? { Some(r.u64()?) } else { None };
		let captured = if r.tag()? { Some(ImportTime::read(r)?) } else { None };
		let parent = if r.tag()? { Some(ImportParentId::read(r)?) } else { None };
		let filename = {
			let v10 = r.string_lp()?;
			(v10.len() <= 255).then_some(v10)?
		};
		let interpretation = ImportInterpretation::read(r)?;
		let original = {
			let v11 = r.u16()? as usize;
			let v11 = (v11 <= 4096).then_some(v11)?;
			let mut v12 = Vec::new();
			v12.try_reserve_exact(v11).ok()?;
			for _ in 0..v11 {
				v12.push(r.u8()?);
			}
			v12
		};
		Some(ImportObject { id, format, format_known, size, captured, parent, filename, interpretation, original })
	}
}

#[derive(Clone, Debug, PartialEq)]
pub enum ImportEntry {
	Object(ImportObject),
	/// This object's record does not fit the record budget. Nothing of it was truncated to make it fit.
	Unrepresentable(ImportObjectId),
}

impl ImportEntry {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<ImportEntry> {
		let mut r = Reader::new(bytes);
		let value = ImportEntry::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<ImportEntry> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = ImportEntry::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		match self {
			ImportEntry::Object(v13) => {
				w.u8(0)?;
				v13.write(w)?;
			}
			ImportEntry::Unrepresentable(v14) => {
				w.u8(1)?;
				v14.write(w)?;
			}
		}
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<ImportEntry> {
		match r.u8()? {
			0 => Some(ImportEntry::Object(ImportObject::read(r)?)),
			1 => Some(ImportEntry::Unrepresentable(ImportObjectId::read(r)?)),
			_ => None,
		}
	}
}

#[derive(Clone, Debug, PartialEq)]
pub struct ImportPage {
	pub entries: Vec<ImportEntry>,
	/// Handles of the snapshot not yet paged.
	pub remaining: u32,
}

impl ImportPage {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<ImportPage> {
		let mut r = Reader::new(bytes);
		let value = ImportPage::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<ImportPage> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = ImportPage::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		if self.entries.len() > u16::MAX as usize {
			return None;
		}
		w.u16(self.entries.len() as u16)?;
		for v15 in self.entries.iter() {
			v15.write(w)?;
		}
		w.u32(self.remaining)?;
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<ImportPage> {
		let entries = {
			let v16 = r.u16()? as usize;
			let v16 = (v16 <= 2).then_some(v16)?;
			let mut v17 = Vec::new();
			v17.try_reserve_exact(v16).ok()?;
			for _ in 0..v16 {
				v17.push(ImportEntry::read(r)?);
			}
			v17
		};
		let remaining = r.u32()?;
		Some(ImportPage { entries, remaining })
	}
}

#[derive(Clone, Debug, PartialEq)]
pub struct ImportCursorGrant {
	pub cursor: u64,
	/// Handles in the snapshot.
	pub count: u32,
}

impl ImportCursorGrant {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<ImportCursorGrant> {
		let mut r = Reader::new(bytes);
		let value = ImportCursorGrant::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<ImportCursorGrant> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = ImportCursorGrant::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.set_handle(self.cursor)?;
		w.u32(0)?;
		w.u32(self.count)?;
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<ImportCursorGrant> {
		let cursor = {
			let _ = r.u32()?;
			r.take_handle()?
		};
		let count = r.u32()?;
		Some(ImportCursorGrant { cursor, count })
	}
}

#[derive(Clone, Debug, PartialEq)]
pub enum ImportEnumeration {
	Opened(ImportCursorGrant),
	/// The storage holds more objects than one enumeration admits: nothing was kept, and nothing truncated.
	OverLimit(u32),
}

impl ImportEnumeration {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<ImportEnumeration> {
		let mut r = Reader::new(bytes);
		let value = ImportEnumeration::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<ImportEnumeration> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = ImportEnumeration::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		match self {
			ImportEnumeration::Opened(v18) => {
				w.u8(0)?;
				v18.write(w)?;
			}
			ImportEnumeration::OverLimit(v19) => {
				w.u8(1)?;
				w.u32(*v19)?;
			}
		}
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<ImportEnumeration> {
		match r.u8()? {
			0 => Some(ImportEnumeration::Opened(ImportCursorGrant::read(r)?)),
			1 => Some(ImportEnumeration::OverLimit(r.u32()?)),
			_ => None,
		}
	}
}

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
#[repr(u8)]
pub enum ImportTransferState {
	Opening = 1,
	Reading = 2,
	Complete = 3,
	Partial = 4,
}

impl ImportTransferState {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<ImportTransferState> {
		let mut r = Reader::new(bytes);
		let value = ImportTransferState::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<ImportTransferState> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = ImportTransferState::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u8(*self as u8)
	}
	pub fn read(r: &mut Reader) -> Option<ImportTransferState> {
		match r.u8()? {
			1 => Some(ImportTransferState::Opening),
			2 => Some(ImportTransferState::Reading),
			3 => Some(ImportTransferState::Complete),
			4 => Some(ImportTransferState::Partial),
			_ => None,
		}
	}
}

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
#[repr(u8)]
pub enum ImportCause {
	Removed = 1,
	TimedOut = 2,
	Cancelled = 3,
	Corrupt = 4,
	Stale = 5,
	DeviceError = 6,
}

impl ImportCause {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<ImportCause> {
		let mut r = Reader::new(bytes);
		let value = ImportCause::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<ImportCause> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = ImportCause::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u8(*self as u8)
	}
	pub fn read(r: &mut Reader) -> Option<ImportCause> {
		match r.u8()? {
			1 => Some(ImportCause::Removed),
			2 => Some(ImportCause::TimedOut),
			3 => Some(ImportCause::Cancelled),
			4 => Some(ImportCause::Corrupt),
			5 => Some(ImportCause::Stale),
			6 => Some(ImportCause::DeviceError),
			_ => None,
		}
	}
}

#[derive(Clone, Debug, PartialEq)]
pub struct ImportTransferStatus {
	pub state: ImportTransferState,
	pub object: ImportObjectId,
	pub expected: u64,
	pub delivered: u64,
	pub cause: Option<ImportCause>,
}

impl ImportTransferStatus {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<ImportTransferStatus> {
		let mut r = Reader::new(bytes);
		let value = ImportTransferStatus::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<ImportTransferStatus> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = ImportTransferStatus::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		self.state.write(w)?;
		self.object.write(w)?;
		w.u64(self.expected)?;
		w.u64(self.delivered)?;
		match &self.cause {
			Some(v20) => {
				w.u8(1)?;
				v20.write(w)?;
			}
			None => {
				w.u8(0)?;
			}
		}
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<ImportTransferStatus> {
		let state = ImportTransferState::read(r)?;
		let object = ImportObjectId::read(r)?;
		let expected = r.u64()?;
		let delivered = r.u64()?;
		let cause = if r.tag()? { Some(ImportCause::read(r)?) } else { None };
		Some(ImportTransferStatus { state, object, expected, delivered, cause })
	}
}

#[derive(Clone, Debug, PartialEq)]
pub struct ImportChunk {
	pub offset: u64,
	pub bytes: Vec<u8>,
}

impl ImportChunk {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<ImportChunk> {
		let mut r = Reader::new(bytes);
		let value = ImportChunk::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<ImportChunk> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = ImportChunk::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u64(self.offset)?;
		if self.bytes.len() > u16::MAX as usize {
			return None;
		}
		w.u16(self.bytes.len() as u16)?;
		for v21 in self.bytes.iter() {
			w.u8(*v21)?;
		}
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<ImportChunk> {
		let offset = r.u64()?;
		let bytes = {
			let v22 = r.u16()? as usize;
			let v22 = (v22 <= 4096).then_some(v22)?;
			let mut v23 = Vec::new();
			v23.try_reserve_exact(v22).ok()?;
			for _ in 0..v22 {
				v23.push(r.u8()?);
			}
			v23
		};
		Some(ImportChunk { offset, bytes })
	}
}

#[derive(Clone, Debug, PartialEq)]
pub enum ImportRead {
	Chunk(ImportChunk),
	/// The transfer is over, and this is why.
	End(ImportTransferStatus),
}

impl ImportRead {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<ImportRead> {
		let mut r = Reader::new(bytes);
		let value = ImportRead::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<ImportRead> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = ImportRead::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		match self {
			ImportRead::Chunk(v24) => {
				w.u8(0)?;
				v24.write(w)?;
			}
			ImportRead::End(v25) => {
				w.u8(1)?;
				v25.write(w)?;
			}
		}
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<ImportRead> {
		match r.u8()? {
			0 => Some(ImportRead::Chunk(ImportChunk::read(r)?)),
			1 => Some(ImportRead::End(ImportTransferStatus::read(r)?)),
			_ => None,
		}
	}
}

/// DEVICES, STORAGES, AND THE TWO CAPABILITIES A CLIENT CAN BE GIVEN: a cursor over a snapshot, and one
/// object's transfer. One of each at a time per client and per device.
// interface `media-import` over a channel: opcodes, a Service trait + dispatch, and a Client.
pub mod media_import {
	use super::*;
	use crate::codec::{Reader, Sink, SliceWriter, Transport, TransportError, VecWriter};
	use alloc::vec::Vec;

	pub const OP_LIMITS: u16 = 1;
	pub const OP_DEVICES: u16 = 2;
	pub const OP_STORAGES: u16 = 3;
	pub const OP_OPEN_ENUMERATION: u16 = 4;
	pub const OP_OPEN_READ: u16 = 5;

	pub trait Service {
		fn limits(&mut self) -> Result<ImportLimits, Error>;
		fn devices(&mut self) -> Result<Vec<ImportDevice>, Error>;
		fn storages(&mut self, device: ImportDeviceId) -> Result<Vec<ImportStorage>, Error>;
		fn open_enumeration(&mut self, storage: ImportStorageId, parent: Option<ImportParentId>) -> Result<ImportEnumeration, Error>;
		fn open_read(&mut self, object: ImportObjectId) -> Result<u64, Error>;
	}

	pub fn dispatch<S: Service>(service: &mut S, request: &[u8], request_handles: &mut Handles, out: &mut [u8], reply_handles: &mut Handles) -> Option<usize> {
		let mut reader = Reader::with_handle_list(request, request_handles);
		let r = &mut reader;
		let op = r.u16()?;
		let corr = r.u32()?;
		let mut writer = SliceWriter::new(out);
		if op == PROTOCOL_INFO_OP {
			r.finish()?;
			request_handles.clear();
			let w = &mut writer;
			w.u32(corr)?;
			w.bytes_lp(b"liber:import")?;
			w.u32(1)?;
			match Handles::try_from_slice(writer.handles()) {
				Some(taken) => *reply_handles = taken,
				None => return None,
			}
			return Some(writer.pos());
		}
		match op {
			OP_LIMITS => {
				r.finish()?;
				request_handles.clear();
				let result = service.limits();
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v26) => {
							w.u8(1)?;
							v26.write(w)?;
						}
						Err(v27) => {
							w.u8(0)?;
							v27.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			OP_DEVICES => {
				r.finish()?;
				request_handles.clear();
				let result = service.devices();
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v28) => {
							w.u8(1)?;
							if v28.len() > u16::MAX as usize {
								return None;
							}
							w.u16(v28.len() as u16)?;
							for v30 in v28.iter() {
								v30.write(w)?;
							}
						}
						Err(v29) => {
							w.u8(0)?;
							v29.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			OP_STORAGES => {
				let device = ImportDeviceId::read(r)?;
				r.finish()?;
				request_handles.clear();
				let result = service.storages(device);
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v31) => {
							w.u8(1)?;
							if v31.len() > u16::MAX as usize {
								return None;
							}
							w.u16(v31.len() as u16)?;
							for v33 in v31.iter() {
								v33.write(w)?;
							}
						}
						Err(v32) => {
							w.u8(0)?;
							v32.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			OP_OPEN_ENUMERATION => {
				let storage = ImportStorageId::read(r)?;
				let parent = if r.tag()? { Some(ImportParentId::read(r)?) } else { None };
				r.finish()?;
				request_handles.clear();
				let result = service.open_enumeration(storage, parent);
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v34) => {
							w.u8(1)?;
							v34.write(w)?;
						}
						Err(v35) => {
							w.u8(0)?;
							v35.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			OP_OPEN_READ => {
				let object = ImportObjectId::read(r)?;
				r.finish()?;
				request_handles.clear();
				let result = service.open_read(object);
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v36) => {
							w.u8(1)?;
							w.set_handle(*v36)?;
							w.u32(0)?;
						}
						Err(v37) => {
							w.u8(0)?;
							v37.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			_ => return None,
		}
		match Handles::try_from_slice(writer.handles()) {
			Some(taken) => *reply_handles = taken,
			None => return None,
		}
		Some(writer.pos())
	}

	fn transport_outcome(error: TransportError) -> Error {
		match error {
			// The request never left this process, so nothing happened and trying
			// again is safe - which is what `again` says.
			TransportError::SendRefused | TransportError::NoRoute => Error::Again,
			// It went out and no answer came back. The server may have acted before
			// it died or before the deadline; nobody knows, and `commit-uncertain` is
			// the answer `base.error` grew so a caller is not forced to guess.
			// The reply could not be held, or arrived and broke the framing rules. In
			// both the server ANSWERED, so it acted; this end simply cannot read what
			// it said, which is the same position as never hearing back.
			TransportError::PeerClosed | TransportError::ReceiveFailed | TransportError::TimedOut | TransportError::NoMemory | TransportError::Malformed => Error::CommitUncertain,
		}
	}

	pub struct Client<T: Transport> {
		transport: T,
		corr: u32,
		deadline: u64,
		last_error: Option<TransportError>,
	}

	impl<T: Transport> Client<T> {
		pub fn new(transport: T) -> Client<T> {
			Client { transport, corr: 0, deadline: 0, last_error: None }
		}
		pub fn with_deadline(transport: T, deadline: u64) -> Client<T> {
			Client { transport, corr: 0, deadline, last_error: None }
		}
		pub fn set_deadline(&mut self, deadline: u64) {
			self.deadline = deadline;
		}
		pub fn last_error(&self) -> Option<TransportError> {
			self.last_error
		}
		pub fn into_transport(self) -> T {
			self.transport
		}
		fn next_corr(&mut self) -> u32 {
			let c = self.corr;
			self.corr = self.corr.wrapping_add(1);
			c
		}
		pub fn protocol_info(&mut self) -> Option<(String, u32)> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(PROTOCOL_INFO_OP)?;
			w.u32(corr)?;
			// No parameter, so no capability: `into_inner` says so rather than this
			// line assuming it.
			let request = writer.into_inner()?;
			let mut reply_handles = Handles::new();
			let reply = self
				.transport
				.call(&request, &[], &mut reply_handles, self.deadline)
				.map_err(|e| {
					self.last_error = Some(e);
					e
				})
				.ok()?;
			if !reply_handles.is_empty() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			let mut reader = Reader::new(&reply);
			let r = &mut reader;
			if r.u32()? != corr {
				return None;
			}
			let package = r.string_lp()?;
			let version = r.u32()?;
			r.finish()?;
			Some((package, version))
		}
		pub fn limits(&mut self) -> Option<Result<ImportLimits, Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_LIMITS)?;
			w.u32(corr)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? { Ok(ImportLimits::read(r)?) } else { Err(Error::read(r)?) };
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
		pub fn devices(&mut self) -> Option<Result<Vec<ImportDevice>, Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_DEVICES)?;
			w.u32(corr)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? {
					Ok({
						let v38 = r.u16()? as usize;
						let v38 = (v38 <= 8).then_some(v38)?;
						let mut v39 = Vec::new();
						v39.try_reserve_exact(v38).ok()?;
						for _ in 0..v38 {
							v39.push(ImportDevice::read(r)?);
						}
						v39
					})
				} else {
					Err(Error::read(r)?)
				};
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
		pub fn storages(&mut self, device: &ImportDeviceId) -> Option<Result<Vec<ImportStorage>, Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_STORAGES)?;
			w.u32(corr)?;
			device.write(w)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? {
					Ok({
						let v40 = r.u16()? as usize;
						let v40 = (v40 <= 32).then_some(v40)?;
						let mut v41 = Vec::new();
						v41.try_reserve_exact(v40).ok()?;
						for _ in 0..v40 {
							v41.push(ImportStorage::read(r)?);
						}
						v41
					})
				} else {
					Err(Error::read(r)?)
				};
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
		pub fn open_enumeration(&mut self, storage: &ImportStorageId, parent: &Option<ImportParentId>) -> Option<Result<ImportEnumeration, Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_OPEN_ENUMERATION)?;
			w.u32(corr)?;
			storage.write(w)?;
			match parent {
				Some(v42) => {
					w.u8(1)?;
					v42.write(w)?;
				}
				None => {
					w.u8(0)?;
				}
			}
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? { Ok(ImportEnumeration::read(r)?) } else { Err(Error::read(r)?) };
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
		pub fn open_read(&mut self, object: &ImportObjectId) -> Option<Result<u64, Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_OPEN_READ)?;
			w.u32(corr)?;
			object.write(w)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? {
					Ok({
						let _ = r.u32()?;
						r.take_handle()?
					})
				} else {
					Err(Error::read(r)?)
				};
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_import_media_import_limits")]
	fn channel_invoke_limits(chan: u64) -> Option<Result<ImportLimits, Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.limits()
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_import_media_import_devices")]
	fn channel_invoke_devices(chan: u64) -> Option<Result<Vec<ImportDevice>, Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.devices()
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_import_media_import_storages")]
	fn channel_invoke_storages(chan: u64, device: &ImportDeviceId) -> Option<Result<Vec<ImportStorage>, Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.storages(device)
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_import_media_import_open_enumeration")]
	fn channel_invoke_open_enumeration(chan: u64, storage: &ImportStorageId, parent: &Option<ImportParentId>) -> Option<Result<ImportEnumeration, Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.open_enumeration(storage, parent)
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_import_media_import_open_read")]
	fn channel_invoke_open_read(chan: u64, object: &ImportObjectId) -> Option<Result<u64, Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.open_read(object)
	}
}

/// A SNAPSHOT OF HANDLES, and metadata fetched for them as they are paged - current when fetched.
// interface `import-cursor` over a channel: opcodes, a Service trait + dispatch, and a Client.
pub mod import_cursor {
	use super::*;
	use crate::codec::{Reader, Sink, SliceWriter, Transport, TransportError, VecWriter};
	use alloc::vec::Vec;

	pub const OP_NEXT: u16 = 1;
	pub const OP_CLOSE: u16 = 2;

	pub trait Service {
		fn next(&mut self) -> Result<ImportPage, Error>;
		fn close(&mut self) -> Result<(), Error>;
	}

	pub fn dispatch<S: Service>(service: &mut S, request: &[u8], request_handles: &mut Handles, out: &mut [u8], reply_handles: &mut Handles) -> Option<usize> {
		let mut reader = Reader::with_handle_list(request, request_handles);
		let r = &mut reader;
		let op = r.u16()?;
		let corr = r.u32()?;
		let mut writer = SliceWriter::new(out);
		if op == PROTOCOL_INFO_OP {
			r.finish()?;
			request_handles.clear();
			let w = &mut writer;
			w.u32(corr)?;
			w.bytes_lp(b"liber:import")?;
			w.u32(1)?;
			match Handles::try_from_slice(writer.handles()) {
				Some(taken) => *reply_handles = taken,
				None => return None,
			}
			return Some(writer.pos());
		}
		match op {
			OP_NEXT => {
				r.finish()?;
				request_handles.clear();
				let result = service.next();
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v43) => {
							w.u8(1)?;
							v43.write(w)?;
						}
						Err(v44) => {
							w.u8(0)?;
							v44.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			OP_CLOSE => {
				r.finish()?;
				request_handles.clear();
				let result = service.close();
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v45) => {
							w.u8(1)?;
						}
						Err(v46) => {
							w.u8(0)?;
							v46.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			_ => return None,
		}
		match Handles::try_from_slice(writer.handles()) {
			Some(taken) => *reply_handles = taken,
			None => return None,
		}
		Some(writer.pos())
	}

	fn transport_outcome(error: TransportError) -> Error {
		match error {
			// The request never left this process, so nothing happened and trying
			// again is safe - which is what `again` says.
			TransportError::SendRefused | TransportError::NoRoute => Error::Again,
			// It went out and no answer came back. The server may have acted before
			// it died or before the deadline; nobody knows, and `commit-uncertain` is
			// the answer `base.error` grew so a caller is not forced to guess.
			// The reply could not be held, or arrived and broke the framing rules. In
			// both the server ANSWERED, so it acted; this end simply cannot read what
			// it said, which is the same position as never hearing back.
			TransportError::PeerClosed | TransportError::ReceiveFailed | TransportError::TimedOut | TransportError::NoMemory | TransportError::Malformed => Error::CommitUncertain,
		}
	}

	pub struct Client<T: Transport> {
		transport: T,
		corr: u32,
		deadline: u64,
		last_error: Option<TransportError>,
	}

	impl<T: Transport> Client<T> {
		pub fn new(transport: T) -> Client<T> {
			Client { transport, corr: 0, deadline: 0, last_error: None }
		}
		pub fn with_deadline(transport: T, deadline: u64) -> Client<T> {
			Client { transport, corr: 0, deadline, last_error: None }
		}
		pub fn set_deadline(&mut self, deadline: u64) {
			self.deadline = deadline;
		}
		pub fn last_error(&self) -> Option<TransportError> {
			self.last_error
		}
		pub fn into_transport(self) -> T {
			self.transport
		}
		fn next_corr(&mut self) -> u32 {
			let c = self.corr;
			self.corr = self.corr.wrapping_add(1);
			c
		}
		pub fn protocol_info(&mut self) -> Option<(String, u32)> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(PROTOCOL_INFO_OP)?;
			w.u32(corr)?;
			// No parameter, so no capability: `into_inner` says so rather than this
			// line assuming it.
			let request = writer.into_inner()?;
			let mut reply_handles = Handles::new();
			let reply = self
				.transport
				.call(&request, &[], &mut reply_handles, self.deadline)
				.map_err(|e| {
					self.last_error = Some(e);
					e
				})
				.ok()?;
			if !reply_handles.is_empty() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			let mut reader = Reader::new(&reply);
			let r = &mut reader;
			if r.u32()? != corr {
				return None;
			}
			let package = r.string_lp()?;
			let version = r.u32()?;
			r.finish()?;
			Some((package, version))
		}
		pub fn next(&mut self) -> Option<Result<ImportPage, Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_NEXT)?;
			w.u32(corr)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? { Ok(ImportPage::read(r)?) } else { Err(Error::read(r)?) };
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
		pub fn close(&mut self) -> Option<Result<(), Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_CLOSE)?;
			w.u32(corr)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? { Ok(()) } else { Err(Error::read(r)?) };
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_import_import_cursor_next")]
	fn channel_invoke_next(chan: u64) -> Option<Result<ImportPage, Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.next()
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_import_import_cursor_close")]
	fn channel_invoke_close(chan: u64) -> Option<Result<(), Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.close()
	}
}

/// ONE OBJECT, IN ORDER. `read` answers a chunk at its exact offset or the terminal status.
// interface `import-transfer` over a channel: opcodes, a Service trait + dispatch, and a Client.
pub mod import_transfer {
	use super::*;
	use crate::codec::{Reader, Sink, SliceWriter, Transport, TransportError, VecWriter};
	use alloc::vec::Vec;

	pub const OP_READ: u16 = 1;
	pub const OP_STATUS: u16 = 2;
	pub const OP_CANCEL: u16 = 3;

	pub trait Service {
		fn read(&mut self) -> Result<ImportRead, Error>;
		fn status(&mut self) -> Result<ImportTransferStatus, Error>;
		fn cancel(&mut self) -> Result<ImportTransferStatus, Error>;
	}

	pub fn dispatch<S: Service>(service: &mut S, request: &[u8], request_handles: &mut Handles, out: &mut [u8], reply_handles: &mut Handles) -> Option<usize> {
		let mut reader = Reader::with_handle_list(request, request_handles);
		let r = &mut reader;
		let op = r.u16()?;
		let corr = r.u32()?;
		let mut writer = SliceWriter::new(out);
		if op == PROTOCOL_INFO_OP {
			r.finish()?;
			request_handles.clear();
			let w = &mut writer;
			w.u32(corr)?;
			w.bytes_lp(b"liber:import")?;
			w.u32(1)?;
			match Handles::try_from_slice(writer.handles()) {
				Some(taken) => *reply_handles = taken,
				None => return None,
			}
			return Some(writer.pos());
		}
		match op {
			OP_READ => {
				r.finish()?;
				request_handles.clear();
				let result = service.read();
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v47) => {
							w.u8(1)?;
							v47.write(w)?;
						}
						Err(v48) => {
							w.u8(0)?;
							v48.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			OP_STATUS => {
				r.finish()?;
				request_handles.clear();
				let result = service.status();
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v49) => {
							w.u8(1)?;
							v49.write(w)?;
						}
						Err(v50) => {
							w.u8(0)?;
							v50.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			OP_CANCEL => {
				r.finish()?;
				request_handles.clear();
				let result = service.cancel();
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v51) => {
							w.u8(1)?;
							v51.write(w)?;
						}
						Err(v52) => {
							w.u8(0)?;
							v52.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			_ => return None,
		}
		match Handles::try_from_slice(writer.handles()) {
			Some(taken) => *reply_handles = taken,
			None => return None,
		}
		Some(writer.pos())
	}

	fn transport_outcome(error: TransportError) -> Error {
		match error {
			// The request never left this process, so nothing happened and trying
			// again is safe - which is what `again` says.
			TransportError::SendRefused | TransportError::NoRoute => Error::Again,
			// It went out and no answer came back. The server may have acted before
			// it died or before the deadline; nobody knows, and `commit-uncertain` is
			// the answer `base.error` grew so a caller is not forced to guess.
			// The reply could not be held, or arrived and broke the framing rules. In
			// both the server ANSWERED, so it acted; this end simply cannot read what
			// it said, which is the same position as never hearing back.
			TransportError::PeerClosed | TransportError::ReceiveFailed | TransportError::TimedOut | TransportError::NoMemory | TransportError::Malformed => Error::CommitUncertain,
		}
	}

	pub struct Client<T: Transport> {
		transport: T,
		corr: u32,
		deadline: u64,
		last_error: Option<TransportError>,
	}

	impl<T: Transport> Client<T> {
		pub fn new(transport: T) -> Client<T> {
			Client { transport, corr: 0, deadline: 0, last_error: None }
		}
		pub fn with_deadline(transport: T, deadline: u64) -> Client<T> {
			Client { transport, corr: 0, deadline, last_error: None }
		}
		pub fn set_deadline(&mut self, deadline: u64) {
			self.deadline = deadline;
		}
		pub fn last_error(&self) -> Option<TransportError> {
			self.last_error
		}
		pub fn into_transport(self) -> T {
			self.transport
		}
		fn next_corr(&mut self) -> u32 {
			let c = self.corr;
			self.corr = self.corr.wrapping_add(1);
			c
		}
		pub fn protocol_info(&mut self) -> Option<(String, u32)> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(PROTOCOL_INFO_OP)?;
			w.u32(corr)?;
			// No parameter, so no capability: `into_inner` says so rather than this
			// line assuming it.
			let request = writer.into_inner()?;
			let mut reply_handles = Handles::new();
			let reply = self
				.transport
				.call(&request, &[], &mut reply_handles, self.deadline)
				.map_err(|e| {
					self.last_error = Some(e);
					e
				})
				.ok()?;
			if !reply_handles.is_empty() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			let mut reader = Reader::new(&reply);
			let r = &mut reader;
			if r.u32()? != corr {
				return None;
			}
			let package = r.string_lp()?;
			let version = r.u32()?;
			r.finish()?;
			Some((package, version))
		}
		pub fn read(&mut self) -> Option<Result<ImportRead, Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_READ)?;
			w.u32(corr)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? { Ok(ImportRead::read(r)?) } else { Err(Error::read(r)?) };
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
		pub fn status(&mut self) -> Option<Result<ImportTransferStatus, Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_STATUS)?;
			w.u32(corr)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? { Ok(ImportTransferStatus::read(r)?) } else { Err(Error::read(r)?) };
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
		pub fn cancel(&mut self) -> Option<Result<ImportTransferStatus, Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_CANCEL)?;
			w.u32(corr)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? { Ok(ImportTransferStatus::read(r)?) } else { Err(Error::read(r)?) };
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_import_import_transfer_read")]
	fn channel_invoke_read(chan: u64) -> Option<Result<ImportRead, Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.read()
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_import_import_transfer_status")]
	fn channel_invoke_status(chan: u64) -> Option<Result<ImportTransferStatus, Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.status()
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_import_import_transfer_cancel")]
	fn channel_invoke_cancel(chan: u64) -> Option<Result<ImportTransferStatus, Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.cancel()
	}
}

impl ImportDeviceId {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"slot\":");
		let _ = write!(out, "{}", self.slot);
		out.push(',');
		out.push_str("\"generation\":");
		let _ = write!(out, "{}", self.generation);
		out.push(',');
		out.push_str("\"binding-generation\":");
		let _ = write!(out, "{}", self.binding_generation);
		out.push(',');
		out.push_str("\"attachment\":");
		let _ = write!(out, "{}", self.attachment);
		out.push(',');
		out.push_str("\"session\":");
		let _ = write!(out, "{}", self.session);
		out.push(',');
		out.push_str("\"content-epoch\":");
		let _ = write!(out, "{}", self.content_epoch);
		out.push(',');
		out.push_str("\"incarnation\":");
		let _ = write!(out, "{}", self.incarnation);
		out.push(',');
		out.push_str("\"context\":");
		let _ = write!(out, "{}", self.context);
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("slot=");
		let _ = write!(out, "{}", self.slot);
		out.push_str(", ");
		out.push_str("generation=");
		let _ = write!(out, "{}", self.generation);
		out.push_str(", ");
		out.push_str("binding-generation=");
		let _ = write!(out, "{}", self.binding_generation);
		out.push_str(", ");
		out.push_str("attachment=");
		let _ = write!(out, "{}", self.attachment);
		out.push_str(", ");
		out.push_str("session=");
		let _ = write!(out, "{}", self.session);
		out.push_str(", ");
		out.push_str("content-epoch=");
		let _ = write!(out, "{}", self.content_epoch);
		out.push_str(", ");
		out.push_str("incarnation=");
		let _ = write!(out, "{}", self.incarnation);
		out.push_str(", ");
		out.push_str("context=");
		let _ = write!(out, "{}", self.context);
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 8);
		crate::codec::cbor::text(out, "slot");
		crate::codec::cbor::uint(out, self.slot as u64);
		crate::codec::cbor::text(out, "generation");
		crate::codec::cbor::uint(out, self.generation as u64);
		crate::codec::cbor::text(out, "binding-generation");
		crate::codec::cbor::uint(out, self.binding_generation as u64);
		crate::codec::cbor::text(out, "attachment");
		crate::codec::cbor::uint(out, self.attachment as u64);
		crate::codec::cbor::text(out, "session");
		crate::codec::cbor::uint(out, self.session as u64);
		crate::codec::cbor::text(out, "content-epoch");
		crate::codec::cbor::uint(out, self.content_epoch as u64);
		crate::codec::cbor::text(out, "incarnation");
		crate::codec::cbor::uint(out, self.incarnation as u64);
		crate::codec::cbor::text(out, "context");
		crate::codec::cbor::uint(out, self.context as u64);
	}
}

impl ImportStorageId {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"device\":");
		self.device.to_json_into(out);
		out.push(',');
		out.push_str("\"storage\":");
		let _ = write!(out, "{}", self.storage);
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("device=");
		self.device.to_text_into(out);
		out.push_str(", ");
		out.push_str("storage=");
		let _ = write!(out, "{}", self.storage);
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 2);
		crate::codec::cbor::text(out, "device");
		self.device.to_cbor_into(out);
		crate::codec::cbor::text(out, "storage");
		crate::codec::cbor::uint(out, self.storage as u64);
	}
}

impl ImportObjectId {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"device\":");
		self.device.to_json_into(out);
		out.push(',');
		out.push_str("\"handle\":");
		let _ = write!(out, "{}", self.handle);
		out.push(',');
		out.push_str("\"revision\":");
		let _ = write!(out, "{}", self.revision);
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("device=");
		self.device.to_text_into(out);
		out.push_str(", ");
		out.push_str("handle=");
		let _ = write!(out, "{}", self.handle);
		out.push_str(", ");
		out.push_str("revision=");
		let _ = write!(out, "{}", self.revision);
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 3);
		crate::codec::cbor::text(out, "device");
		self.device.to_cbor_into(out);
		crate::codec::cbor::text(out, "handle");
		crate::codec::cbor::uint(out, self.handle as u64);
		crate::codec::cbor::text(out, "revision");
		crate::codec::cbor::uint(out, self.revision as u64);
	}
}

impl ImportParentId {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"device\":");
		self.device.to_json_into(out);
		out.push(',');
		out.push_str("\"handle\":");
		let _ = write!(out, "{}", self.handle);
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("device=");
		self.device.to_text_into(out);
		out.push_str(", ");
		out.push_str("handle=");
		let _ = write!(out, "{}", self.handle);
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 2);
		crate::codec::cbor::text(out, "device");
		self.device.to_cbor_into(out);
		crate::codec::cbor::text(out, "handle");
		crate::codec::cbor::uint(out, self.handle as u64);
	}
}

impl ImportLimits {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"providers\":");
		let _ = write!(out, "{}", self.providers);
		out.push(',');
		out.push_str("\"clients\":");
		let _ = write!(out, "{}", self.clients);
		out.push(',');
		out.push_str("\"cursors\":");
		let _ = write!(out, "{}", self.cursors);
		out.push(',');
		out.push_str("\"transfers\":");
		let _ = write!(out, "{}", self.transfers);
		out.push(',');
		out.push_str("\"cursors-per-client\":");
		let _ = write!(out, "{}", self.cursors_per_client);
		out.push(',');
		out.push_str("\"transfers-per-client\":");
		let _ = write!(out, "{}", self.transfers_per_client);
		out.push(',');
		out.push_str("\"cursors-per-device\":");
		let _ = write!(out, "{}", self.cursors_per_device);
		out.push(',');
		out.push_str("\"transfers-per-device\":");
		let _ = write!(out, "{}", self.transfers_per_device);
		out.push(',');
		out.push_str("\"data-budget\":");
		let _ = write!(out, "{}", self.data_budget);
		out.push(',');
		out.push_str("\"snapshot-budget\":");
		let _ = write!(out, "{}", self.snapshot_budget);
		out.push(',');
		out.push_str("\"max-handles\":");
		let _ = write!(out, "{}", self.max_handles);
		out.push(',');
		out.push_str("\"page-records\":");
		let _ = write!(out, "{}", self.page_records);
		out.push(',');
		out.push_str("\"page-bytes\":");
		let _ = write!(out, "{}", self.page_bytes);
		out.push(',');
		out.push_str("\"record-bytes\":");
		let _ = write!(out, "{}", self.record_bytes);
		out.push(',');
		out.push_str("\"chunk-bytes\":");
		let _ = write!(out, "{}", self.chunk_bytes);
		out.push(',');
		out.push_str("\"cursor-idle-seconds\":");
		let _ = write!(out, "{}", self.cursor_idle_seconds);
		out.push(',');
		out.push_str("\"transfer-idle-seconds\":");
		let _ = write!(out, "{}", self.transfer_idle_seconds);
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("providers=");
		let _ = write!(out, "{}", self.providers);
		out.push_str(", ");
		out.push_str("clients=");
		let _ = write!(out, "{}", self.clients);
		out.push_str(", ");
		out.push_str("cursors=");
		let _ = write!(out, "{}", self.cursors);
		out.push_str(", ");
		out.push_str("transfers=");
		let _ = write!(out, "{}", self.transfers);
		out.push_str(", ");
		out.push_str("cursors-per-client=");
		let _ = write!(out, "{}", self.cursors_per_client);
		out.push_str(", ");
		out.push_str("transfers-per-client=");
		let _ = write!(out, "{}", self.transfers_per_client);
		out.push_str(", ");
		out.push_str("cursors-per-device=");
		let _ = write!(out, "{}", self.cursors_per_device);
		out.push_str(", ");
		out.push_str("transfers-per-device=");
		let _ = write!(out, "{}", self.transfers_per_device);
		out.push_str(", ");
		out.push_str("data-budget=");
		let _ = write!(out, "{}", self.data_budget);
		out.push_str(", ");
		out.push_str("snapshot-budget=");
		let _ = write!(out, "{}", self.snapshot_budget);
		out.push_str(", ");
		out.push_str("max-handles=");
		let _ = write!(out, "{}", self.max_handles);
		out.push_str(", ");
		out.push_str("page-records=");
		let _ = write!(out, "{}", self.page_records);
		out.push_str(", ");
		out.push_str("page-bytes=");
		let _ = write!(out, "{}", self.page_bytes);
		out.push_str(", ");
		out.push_str("record-bytes=");
		let _ = write!(out, "{}", self.record_bytes);
		out.push_str(", ");
		out.push_str("chunk-bytes=");
		let _ = write!(out, "{}", self.chunk_bytes);
		out.push_str(", ");
		out.push_str("cursor-idle-seconds=");
		let _ = write!(out, "{}", self.cursor_idle_seconds);
		out.push_str(", ");
		out.push_str("transfer-idle-seconds=");
		let _ = write!(out, "{}", self.transfer_idle_seconds);
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 17);
		crate::codec::cbor::text(out, "providers");
		crate::codec::cbor::uint(out, self.providers as u64);
		crate::codec::cbor::text(out, "clients");
		crate::codec::cbor::uint(out, self.clients as u64);
		crate::codec::cbor::text(out, "cursors");
		crate::codec::cbor::uint(out, self.cursors as u64);
		crate::codec::cbor::text(out, "transfers");
		crate::codec::cbor::uint(out, self.transfers as u64);
		crate::codec::cbor::text(out, "cursors-per-client");
		crate::codec::cbor::uint(out, self.cursors_per_client as u64);
		crate::codec::cbor::text(out, "transfers-per-client");
		crate::codec::cbor::uint(out, self.transfers_per_client as u64);
		crate::codec::cbor::text(out, "cursors-per-device");
		crate::codec::cbor::uint(out, self.cursors_per_device as u64);
		crate::codec::cbor::text(out, "transfers-per-device");
		crate::codec::cbor::uint(out, self.transfers_per_device as u64);
		crate::codec::cbor::text(out, "data-budget");
		crate::codec::cbor::uint(out, self.data_budget as u64);
		crate::codec::cbor::text(out, "snapshot-budget");
		crate::codec::cbor::uint(out, self.snapshot_budget as u64);
		crate::codec::cbor::text(out, "max-handles");
		crate::codec::cbor::uint(out, self.max_handles as u64);
		crate::codec::cbor::text(out, "page-records");
		crate::codec::cbor::uint(out, self.page_records as u64);
		crate::codec::cbor::text(out, "page-bytes");
		crate::codec::cbor::uint(out, self.page_bytes as u64);
		crate::codec::cbor::text(out, "record-bytes");
		crate::codec::cbor::uint(out, self.record_bytes as u64);
		crate::codec::cbor::text(out, "chunk-bytes");
		crate::codec::cbor::uint(out, self.chunk_bytes as u64);
		crate::codec::cbor::text(out, "cursor-idle-seconds");
		crate::codec::cbor::uint(out, self.cursor_idle_seconds as u64);
		crate::codec::cbor::text(out, "transfer-idle-seconds");
		crate::codec::cbor::uint(out, self.transfer_idle_seconds as u64);
	}
}

impl ImportDevice {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"id\":");
		self.id.to_json_into(out);
		out.push(',');
		out.push_str("\"manufacturer\":");
		crate::codec::json_escape(&self.manufacturer, out);
		out.push(',');
		out.push_str("\"model\":");
		crate::codec::json_escape(&self.model, out);
		out.push(',');
		out.push_str("\"serial\":");
		crate::codec::json_escape(&self.serial, out);
		out.push(',');
		out.push_str("\"usable\":");
		if self.usable {
			out.push_str("true");
		} else {
			out.push_str("false");
		}
		out.push(',');
		out.push_str("\"busy\":");
		if self.busy {
			out.push_str("true");
		} else {
			out.push_str("false");
		}
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("id=");
		self.id.to_text_into(out);
		out.push_str(", ");
		out.push_str("manufacturer=");
		out.push_str(&self.manufacturer);
		out.push_str(", ");
		out.push_str("model=");
		out.push_str(&self.model);
		out.push_str(", ");
		out.push_str("serial=");
		out.push_str(&self.serial);
		out.push_str(", ");
		out.push_str("usable=");
		if self.usable {
			out.push_str("true");
		} else {
			out.push_str("false");
		}
		out.push_str(", ");
		out.push_str("busy=");
		if self.busy {
			out.push_str("true");
		} else {
			out.push_str("false");
		}
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 6);
		crate::codec::cbor::text(out, "id");
		self.id.to_cbor_into(out);
		crate::codec::cbor::text(out, "manufacturer");
		crate::codec::cbor::text(out, &self.manufacturer);
		crate::codec::cbor::text(out, "model");
		crate::codec::cbor::text(out, &self.model);
		crate::codec::cbor::text(out, "serial");
		crate::codec::cbor::text(out, &self.serial);
		crate::codec::cbor::text(out, "usable");
		crate::codec::cbor::boolean(out, self.usable);
		crate::codec::cbor::text(out, "busy");
		crate::codec::cbor::boolean(out, self.busy);
	}
}

impl ImportStorage {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"id\":");
		self.id.to_json_into(out);
		out.push(',');
		out.push_str("\"storage-type\":");
		let _ = write!(out, "{}", self.storage_type);
		out.push(',');
		out.push_str("\"filesystem-type\":");
		let _ = write!(out, "{}", self.filesystem_type);
		out.push(',');
		out.push_str("\"access\":");
		let _ = write!(out, "{}", self.access);
		out.push(',');
		out.push_str("\"capacity\":");
		let _ = write!(out, "{}", self.capacity);
		out.push(',');
		out.push_str("\"free\":");
		let _ = write!(out, "{}", self.free);
		out.push(',');
		out.push_str("\"description\":");
		crate::codec::json_escape(&self.description, out);
		out.push(',');
		out.push_str("\"label\":");
		crate::codec::json_escape(&self.label, out);
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("id=");
		self.id.to_text_into(out);
		out.push_str(", ");
		out.push_str("storage-type=");
		let _ = write!(out, "{}", self.storage_type);
		out.push_str(", ");
		out.push_str("filesystem-type=");
		let _ = write!(out, "{}", self.filesystem_type);
		out.push_str(", ");
		out.push_str("access=");
		let _ = write!(out, "{}", self.access);
		out.push_str(", ");
		out.push_str("capacity=");
		let _ = write!(out, "{}", self.capacity);
		out.push_str(", ");
		out.push_str("free=");
		let _ = write!(out, "{}", self.free);
		out.push_str(", ");
		out.push_str("description=");
		out.push_str(&self.description);
		out.push_str(", ");
		out.push_str("label=");
		out.push_str(&self.label);
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 8);
		crate::codec::cbor::text(out, "id");
		self.id.to_cbor_into(out);
		crate::codec::cbor::text(out, "storage-type");
		crate::codec::cbor::uint(out, self.storage_type as u64);
		crate::codec::cbor::text(out, "filesystem-type");
		crate::codec::cbor::uint(out, self.filesystem_type as u64);
		crate::codec::cbor::text(out, "access");
		crate::codec::cbor::uint(out, self.access as u64);
		crate::codec::cbor::text(out, "capacity");
		crate::codec::cbor::uint(out, self.capacity as u64);
		crate::codec::cbor::text(out, "free");
		crate::codec::cbor::uint(out, self.free as u64);
		crate::codec::cbor::text(out, "description");
		crate::codec::cbor::text(out, &self.description);
		crate::codec::cbor::text(out, "label");
		crate::codec::cbor::text(out, &self.label);
	}
}

impl ImportTime {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"year\":");
		let _ = write!(out, "{}", self.year);
		out.push(',');
		out.push_str("\"month\":");
		let _ = write!(out, "{}", self.month);
		out.push(',');
		out.push_str("\"day\":");
		let _ = write!(out, "{}", self.day);
		out.push(',');
		out.push_str("\"hour\":");
		let _ = write!(out, "{}", self.hour);
		out.push(',');
		out.push_str("\"minute\":");
		let _ = write!(out, "{}", self.minute);
		out.push(',');
		out.push_str("\"second\":");
		let _ = write!(out, "{}", self.second);
		out.push(',');
		out.push_str("\"utc-offset-minutes\":");
		match &self.utc_offset_minutes {
			Some(v53) => {
				let _ = write!(out, "{}", v53);
			}
			None => {
				out.push_str("null");
			}
		}
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("year=");
		let _ = write!(out, "{}", self.year);
		out.push_str(", ");
		out.push_str("month=");
		let _ = write!(out, "{}", self.month);
		out.push_str(", ");
		out.push_str("day=");
		let _ = write!(out, "{}", self.day);
		out.push_str(", ");
		out.push_str("hour=");
		let _ = write!(out, "{}", self.hour);
		out.push_str(", ");
		out.push_str("minute=");
		let _ = write!(out, "{}", self.minute);
		out.push_str(", ");
		out.push_str("second=");
		let _ = write!(out, "{}", self.second);
		out.push_str(", ");
		out.push_str("utc-offset-minutes=");
		match &self.utc_offset_minutes {
			Some(v54) => {
				let _ = write!(out, "{}", v54);
			}
			None => {
				out.push('-');
			}
		}
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 7);
		crate::codec::cbor::text(out, "year");
		crate::codec::cbor::uint(out, self.year as u64);
		crate::codec::cbor::text(out, "month");
		crate::codec::cbor::uint(out, self.month as u64);
		crate::codec::cbor::text(out, "day");
		crate::codec::cbor::uint(out, self.day as u64);
		crate::codec::cbor::text(out, "hour");
		crate::codec::cbor::uint(out, self.hour as u64);
		crate::codec::cbor::text(out, "minute");
		crate::codec::cbor::uint(out, self.minute as u64);
		crate::codec::cbor::text(out, "second");
		crate::codec::cbor::uint(out, self.second as u64);
		crate::codec::cbor::text(out, "utc-offset-minutes");
		match &self.utc_offset_minutes {
			Some(v55) => {
				crate::codec::cbor::int(out, *v55 as i64);
			}
			None => {
				crate::codec::cbor::null(out);
			}
		}
	}
}

impl ImportInterpretation {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		match self {
			ImportInterpretation::Complete => out.push_str("\"complete\""),
			ImportInterpretation::Partial => out.push_str("\"partial\""),
		}
	}
	pub fn to_text_into(&self, out: &mut String) {
		match self {
			ImportInterpretation::Complete => out.push_str("complete"),
			ImportInterpretation::Partial => out.push_str("partial"),
		}
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		match self {
			ImportInterpretation::Complete => crate::codec::cbor::text(out, "complete"),
			ImportInterpretation::Partial => crate::codec::cbor::text(out, "partial"),
		}
	}
}

impl ImportObject {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"id\":");
		self.id.to_json_into(out);
		out.push(',');
		out.push_str("\"format\":");
		let _ = write!(out, "{}", self.format);
		out.push(',');
		out.push_str("\"format-known\":");
		if self.format_known {
			out.push_str("true");
		} else {
			out.push_str("false");
		}
		out.push(',');
		out.push_str("\"size\":");
		match &self.size {
			Some(v56) => {
				let _ = write!(out, "{}", v56);
			}
			None => {
				out.push_str("null");
			}
		}
		out.push(',');
		out.push_str("\"captured\":");
		match &self.captured {
			Some(v57) => {
				v57.to_json_into(out);
			}
			None => {
				out.push_str("null");
			}
		}
		out.push(',');
		out.push_str("\"parent\":");
		match &self.parent {
			Some(v58) => {
				v58.to_json_into(out);
			}
			None => {
				out.push_str("null");
			}
		}
		out.push(',');
		out.push_str("\"filename\":");
		crate::codec::json_escape(&self.filename, out);
		out.push(',');
		out.push_str("\"interpretation\":");
		self.interpretation.to_json_into(out);
		out.push(',');
		out.push_str("\"original\":");
		out.push('[');
		let mut v60 = true;
		for v59 in self.original.iter() {
			if !v60 {
				out.push(',');
			}
			v60 = false;
			let _ = write!(out, "{}", v59);
		}
		out.push(']');
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("id=");
		self.id.to_text_into(out);
		out.push_str(", ");
		out.push_str("format=");
		let _ = write!(out, "{}", self.format);
		out.push_str(", ");
		out.push_str("format-known=");
		if self.format_known {
			out.push_str("true");
		} else {
			out.push_str("false");
		}
		out.push_str(", ");
		out.push_str("size=");
		match &self.size {
			Some(v61) => {
				let _ = write!(out, "{}", v61);
			}
			None => {
				out.push('-');
			}
		}
		out.push_str(", ");
		out.push_str("captured=");
		match &self.captured {
			Some(v62) => {
				v62.to_text_into(out);
			}
			None => {
				out.push('-');
			}
		}
		out.push_str(", ");
		out.push_str("parent=");
		match &self.parent {
			Some(v63) => {
				v63.to_text_into(out);
			}
			None => {
				out.push('-');
			}
		}
		out.push_str(", ");
		out.push_str("filename=");
		out.push_str(&self.filename);
		out.push_str(", ");
		out.push_str("interpretation=");
		self.interpretation.to_text_into(out);
		out.push_str(", ");
		out.push_str("original=");
		out.push('[');
		let mut v65 = true;
		for v64 in self.original.iter() {
			if !v65 {
				out.push_str(", ");
			}
			v65 = false;
			let _ = write!(out, "{}", v64);
		}
		out.push(']');
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 9);
		crate::codec::cbor::text(out, "id");
		self.id.to_cbor_into(out);
		crate::codec::cbor::text(out, "format");
		crate::codec::cbor::uint(out, self.format as u64);
		crate::codec::cbor::text(out, "format-known");
		crate::codec::cbor::boolean(out, self.format_known);
		crate::codec::cbor::text(out, "size");
		match &self.size {
			Some(v66) => {
				crate::codec::cbor::uint(out, *v66 as u64);
			}
			None => {
				crate::codec::cbor::null(out);
			}
		}
		crate::codec::cbor::text(out, "captured");
		match &self.captured {
			Some(v67) => {
				v67.to_cbor_into(out);
			}
			None => {
				crate::codec::cbor::null(out);
			}
		}
		crate::codec::cbor::text(out, "parent");
		match &self.parent {
			Some(v68) => {
				v68.to_cbor_into(out);
			}
			None => {
				crate::codec::cbor::null(out);
			}
		}
		crate::codec::cbor::text(out, "filename");
		crate::codec::cbor::text(out, &self.filename);
		crate::codec::cbor::text(out, "interpretation");
		self.interpretation.to_cbor_into(out);
		crate::codec::cbor::text(out, "original");
		crate::codec::cbor::array(out, self.original.len());
		for v69 in self.original.iter() {
			crate::codec::cbor::uint(out, *v69 as u64);
		}
	}
}

impl ImportEntry {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		match self {
			ImportEntry::Object(v70) => {
				out.push_str("{\"object\":");
				v70.to_json_into(out);
				out.push('}');
			}
			ImportEntry::Unrepresentable(v71) => {
				out.push_str("{\"unrepresentable\":");
				v71.to_json_into(out);
				out.push('}');
			}
		}
	}
	pub fn to_text_into(&self, out: &mut String) {
		match self {
			ImportEntry::Object(v72) => {
				out.push_str("object(");
				v72.to_text_into(out);
				out.push(')');
			}
			ImportEntry::Unrepresentable(v73) => {
				out.push_str("unrepresentable(");
				v73.to_text_into(out);
				out.push(')');
			}
		}
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		match self {
			ImportEntry::Object(v74) => {
				crate::codec::cbor::map(out, 1);
				crate::codec::cbor::text(out, "object");
				v74.to_cbor_into(out);
			}
			ImportEntry::Unrepresentable(v75) => {
				crate::codec::cbor::map(out, 1);
				crate::codec::cbor::text(out, "unrepresentable");
				v75.to_cbor_into(out);
			}
		}
	}
}

impl ImportPage {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"entries\":");
		out.push('[');
		let mut v77 = true;
		for v76 in self.entries.iter() {
			if !v77 {
				out.push(',');
			}
			v77 = false;
			v76.to_json_into(out);
		}
		out.push(']');
		out.push(',');
		out.push_str("\"remaining\":");
		let _ = write!(out, "{}", self.remaining);
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("entries=");
		out.push('[');
		let mut v79 = true;
		for v78 in self.entries.iter() {
			if !v79 {
				out.push_str(", ");
			}
			v79 = false;
			v78.to_text_into(out);
		}
		out.push(']');
		out.push_str(", ");
		out.push_str("remaining=");
		let _ = write!(out, "{}", self.remaining);
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 2);
		crate::codec::cbor::text(out, "entries");
		crate::codec::cbor::array(out, self.entries.len());
		for v80 in self.entries.iter() {
			v80.to_cbor_into(out);
		}
		crate::codec::cbor::text(out, "remaining");
		crate::codec::cbor::uint(out, self.remaining as u64);
	}
}

impl ImportCursorGrant {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"cursor\":");
		let _ = write!(out, "{}", self.cursor);
		out.push(',');
		out.push_str("\"count\":");
		let _ = write!(out, "{}", self.count);
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("cursor=");
		let _ = write!(out, "{}", self.cursor);
		out.push_str(", ");
		out.push_str("count=");
		let _ = write!(out, "{}", self.count);
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 2);
		crate::codec::cbor::text(out, "cursor");
		crate::codec::cbor::uint(out, self.cursor as u64);
		crate::codec::cbor::text(out, "count");
		crate::codec::cbor::uint(out, self.count as u64);
	}
}

impl ImportEnumeration {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		match self {
			ImportEnumeration::Opened(v81) => {
				out.push_str("{\"opened\":");
				v81.to_json_into(out);
				out.push('}');
			}
			ImportEnumeration::OverLimit(v82) => {
				out.push_str("{\"over-limit\":");
				let _ = write!(out, "{}", v82);
				out.push('}');
			}
		}
	}
	pub fn to_text_into(&self, out: &mut String) {
		match self {
			ImportEnumeration::Opened(v83) => {
				out.push_str("opened(");
				v83.to_text_into(out);
				out.push(')');
			}
			ImportEnumeration::OverLimit(v84) => {
				out.push_str("over-limit(");
				let _ = write!(out, "{}", v84);
				out.push(')');
			}
		}
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		match self {
			ImportEnumeration::Opened(v85) => {
				crate::codec::cbor::map(out, 1);
				crate::codec::cbor::text(out, "opened");
				v85.to_cbor_into(out);
			}
			ImportEnumeration::OverLimit(v86) => {
				crate::codec::cbor::map(out, 1);
				crate::codec::cbor::text(out, "over-limit");
				crate::codec::cbor::uint(out, *v86 as u64);
			}
		}
	}
}

impl ImportTransferState {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		match self {
			ImportTransferState::Opening => out.push_str("\"opening\""),
			ImportTransferState::Reading => out.push_str("\"reading\""),
			ImportTransferState::Complete => out.push_str("\"complete\""),
			ImportTransferState::Partial => out.push_str("\"partial\""),
		}
	}
	pub fn to_text_into(&self, out: &mut String) {
		match self {
			ImportTransferState::Opening => out.push_str("opening"),
			ImportTransferState::Reading => out.push_str("reading"),
			ImportTransferState::Complete => out.push_str("complete"),
			ImportTransferState::Partial => out.push_str("partial"),
		}
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		match self {
			ImportTransferState::Opening => crate::codec::cbor::text(out, "opening"),
			ImportTransferState::Reading => crate::codec::cbor::text(out, "reading"),
			ImportTransferState::Complete => crate::codec::cbor::text(out, "complete"),
			ImportTransferState::Partial => crate::codec::cbor::text(out, "partial"),
		}
	}
}

impl ImportCause {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		match self {
			ImportCause::Removed => out.push_str("\"removed\""),
			ImportCause::TimedOut => out.push_str("\"timed-out\""),
			ImportCause::Cancelled => out.push_str("\"cancelled\""),
			ImportCause::Corrupt => out.push_str("\"corrupt\""),
			ImportCause::Stale => out.push_str("\"stale\""),
			ImportCause::DeviceError => out.push_str("\"device-error\""),
		}
	}
	pub fn to_text_into(&self, out: &mut String) {
		match self {
			ImportCause::Removed => out.push_str("removed"),
			ImportCause::TimedOut => out.push_str("timed-out"),
			ImportCause::Cancelled => out.push_str("cancelled"),
			ImportCause::Corrupt => out.push_str("corrupt"),
			ImportCause::Stale => out.push_str("stale"),
			ImportCause::DeviceError => out.push_str("device-error"),
		}
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		match self {
			ImportCause::Removed => crate::codec::cbor::text(out, "removed"),
			ImportCause::TimedOut => crate::codec::cbor::text(out, "timed-out"),
			ImportCause::Cancelled => crate::codec::cbor::text(out, "cancelled"),
			ImportCause::Corrupt => crate::codec::cbor::text(out, "corrupt"),
			ImportCause::Stale => crate::codec::cbor::text(out, "stale"),
			ImportCause::DeviceError => crate::codec::cbor::text(out, "device-error"),
		}
	}
}

impl ImportTransferStatus {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"state\":");
		self.state.to_json_into(out);
		out.push(',');
		out.push_str("\"object\":");
		self.object.to_json_into(out);
		out.push(',');
		out.push_str("\"expected\":");
		let _ = write!(out, "{}", self.expected);
		out.push(',');
		out.push_str("\"delivered\":");
		let _ = write!(out, "{}", self.delivered);
		out.push(',');
		out.push_str("\"cause\":");
		match &self.cause {
			Some(v87) => {
				v87.to_json_into(out);
			}
			None => {
				out.push_str("null");
			}
		}
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("state=");
		self.state.to_text_into(out);
		out.push_str(", ");
		out.push_str("object=");
		self.object.to_text_into(out);
		out.push_str(", ");
		out.push_str("expected=");
		let _ = write!(out, "{}", self.expected);
		out.push_str(", ");
		out.push_str("delivered=");
		let _ = write!(out, "{}", self.delivered);
		out.push_str(", ");
		out.push_str("cause=");
		match &self.cause {
			Some(v88) => {
				v88.to_text_into(out);
			}
			None => {
				out.push('-');
			}
		}
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 5);
		crate::codec::cbor::text(out, "state");
		self.state.to_cbor_into(out);
		crate::codec::cbor::text(out, "object");
		self.object.to_cbor_into(out);
		crate::codec::cbor::text(out, "expected");
		crate::codec::cbor::uint(out, self.expected as u64);
		crate::codec::cbor::text(out, "delivered");
		crate::codec::cbor::uint(out, self.delivered as u64);
		crate::codec::cbor::text(out, "cause");
		match &self.cause {
			Some(v89) => {
				v89.to_cbor_into(out);
			}
			None => {
				crate::codec::cbor::null(out);
			}
		}
	}
}

impl ImportChunk {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"offset\":");
		let _ = write!(out, "{}", self.offset);
		out.push(',');
		out.push_str("\"bytes\":");
		out.push('[');
		let mut v91 = true;
		for v90 in self.bytes.iter() {
			if !v91 {
				out.push(',');
			}
			v91 = false;
			let _ = write!(out, "{}", v90);
		}
		out.push(']');
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("offset=");
		let _ = write!(out, "{}", self.offset);
		out.push_str(", ");
		out.push_str("bytes=");
		out.push('[');
		let mut v93 = true;
		for v92 in self.bytes.iter() {
			if !v93 {
				out.push_str(", ");
			}
			v93 = false;
			let _ = write!(out, "{}", v92);
		}
		out.push(']');
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 2);
		crate::codec::cbor::text(out, "offset");
		crate::codec::cbor::uint(out, self.offset as u64);
		crate::codec::cbor::text(out, "bytes");
		crate::codec::cbor::array(out, self.bytes.len());
		for v94 in self.bytes.iter() {
			crate::codec::cbor::uint(out, *v94 as u64);
		}
	}
}

impl ImportRead {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		match self {
			ImportRead::Chunk(v95) => {
				out.push_str("{\"chunk\":");
				v95.to_json_into(out);
				out.push('}');
			}
			ImportRead::End(v96) => {
				out.push_str("{\"end\":");
				v96.to_json_into(out);
				out.push('}');
			}
		}
	}
	pub fn to_text_into(&self, out: &mut String) {
		match self {
			ImportRead::Chunk(v97) => {
				out.push_str("chunk(");
				v97.to_text_into(out);
				out.push(')');
			}
			ImportRead::End(v98) => {
				out.push_str("end(");
				v98.to_text_into(out);
				out.push(')');
			}
		}
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		match self {
			ImportRead::Chunk(v99) => {
				crate::codec::cbor::map(out, 1);
				crate::codec::cbor::text(out, "chunk");
				v99.to_cbor_into(out);
			}
			ImportRead::End(v100) => {
				crate::codec::cbor::map(out, 1);
				crate::codec::cbor::text(out, "end");
				v100.to_cbor_into(out);
			}
		}
	}
}

#[cfg(test)]
mod compat;
