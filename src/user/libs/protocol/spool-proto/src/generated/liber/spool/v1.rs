// @generated by lsidl-gen from ../../idl/spool.lsidl (package liber:spool@1).
// Do not edit by hand; regenerate with `./gen.sh`.
//! LiberSystem spooling - printers, and print jobs an application stages and explicitly submits, with an
//! honest account of what the transport took.
//!
//! A JOB IS A CAPABILITY, NOT AN ENDPOINT. `create` reserves the declared length and answers a job channel;
//! the application writes exactly that many bytes, then `submit`s - and only then is anything eligible to
//! reach the printer. Closing a job that was never submitted aborts it and refunds its reservation; closing
//! a submitted one only stops watching it. Nothing on either interface reaches a catalogue, a backend, a
//! reset or a raw endpoint.
//!
//! `TRANSFERRED` MEANS THE TRANSPORT ACKNOWLEDGED EVERY BYTE, and nothing more: not a finished page. A job
//! that ended any other way says why, how many bytes were confirmed, and whether more may have reached the
//! printer than was confirmed. Nothing is ever replayed - not to the same printer, and not to a replacement.
#![allow(dead_code, unused_imports, unused_variables, unused_mut, clippy::all)]

use crate::codec::{Handles, PROTOCOL_INFO_OP, Reader, Sink, SliceWriter, VecWriter};
use alloc::string::String;
use alloc::vec::Vec;
use core::fmt::Write as _;

pub use crate::generated::liber::base::v1::Error;

/// A printer as this service knows it: DeviceManager's publication identity, the attachment generation a
/// reset or a reattachment advances, and the service incarnation.
#[derive(Clone, Debug, PartialEq)]
pub struct PrinterId {
	pub slot: u32,
	pub generation: u32,
	pub binding_generation: u64,
	pub attachment: u64,
	pub incarnation: u64,
}

impl PrinterId {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<PrinterId> {
		let mut r = Reader::new(bytes);
		let value = PrinterId::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<PrinterId> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = PrinterId::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u32(self.slot)?;
		w.u32(self.generation)?;
		w.u64(self.binding_generation)?;
		w.u64(self.attachment)?;
		w.u64(self.incarnation)?;
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<PrinterId> {
		let slot = r.u32()?;
		let generation = r.u32()?;
		let binding_generation = r.u64()?;
		let attachment = r.u64()?;
		let incarnation = r.u64()?;
		Some(PrinterId { slot, generation, binding_generation, attachment, incarnation })
	}
}

/// What a document is written in. Only `postscript` is accepted, and only by a printer whose device ID
/// names it; the others are named so a request for them is refused as `unsupported` rather than invalid.
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
#[repr(u8)]
pub enum DocumentLanguage {
	Postscript = 1,
	Pcl = 2,
	Pdf = 3,
}

impl DocumentLanguage {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<DocumentLanguage> {
		let mut r = Reader::new(bytes);
		let value = DocumentLanguage::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<DocumentLanguage> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = DocumentLanguage::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u8(*self as u8)
	}
	pub fn read(r: &mut Reader) -> Option<DocumentLanguage> {
		match r.u8()? {
			1 => Some(DocumentLanguage::Postscript),
			2 => Some(DocumentLanguage::Pcl),
			3 => Some(DocumentLanguage::Pdf),
			_ => None,
		}
	}
}

/// One observation: seen true, seen false, not available because the port could not be read, or not
/// something this printer's backend has evidence of at all.
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
#[repr(u8)]
pub enum Observation {
	Yes = 1,
	No = 2,
	Unavailable = 3,
	Unsupported = 4,
}

impl Observation {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<Observation> {
		let mut r = Reader::new(bytes);
		let value = Observation::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<Observation> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = Observation::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u8(*self as u8)
	}
	pub fn read(r: &mut Reader) -> Option<Observation> {
		match r.u8()? {
			1 => Some(Observation::Yes),
			2 => Some(Observation::No),
			3 => Some(Observation::Unavailable),
			4 => Some(Observation::Unsupported),
			_ => None,
		}
	}
}

/// The port's three standard bits, decoded, and two details only a backend with evidence of them supplies.
/// They say what the port reports and no more: a benign reading does not prove a printer ready.
#[derive(Clone, Debug, PartialEq)]
pub struct PortStatus {
	pub paper_empty: Observation,
	pub selected: Observation,
	pub error: Observation,
	/// `unsupported` unless the backend reported it.
	pub cover_open: Observation,
	pub jam: Observation,
}

impl PortStatus {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<PortStatus> {
		let mut r = Reader::new(bytes);
		let value = PortStatus::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<PortStatus> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = PortStatus::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		self.paper_empty.write(w)?;
		self.selected.write(w)?;
		self.error.write(w)?;
		self.cover_open.write(w)?;
		self.jam.write(w)?;
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<PortStatus> {
		let paper_empty = Observation::read(r)?;
		let selected = Observation::read(r)?;
		let error = Observation::read(r)?;
		let cover_open = Observation::read(r)?;
		let jam = Observation::read(r)?;
		Some(PortStatus { paper_empty, selected, error, cover_open, jam })
	}
}

#[derive(Clone, Debug, PartialEq)]
pub struct PrinterInfo {
	pub id: PrinterId,
	pub model: String,
	pub languages: Vec<DocumentLanguage>,
	pub status: PortStatus,
	/// Jobs may be admitted for it now.
	pub available: bool,
	pub queued: u8,
	pub active: bool,
	pub max_job_bytes: u32,
}

impl PrinterInfo {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<PrinterInfo> {
		let mut r = Reader::new(bytes);
		let value = PrinterInfo::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<PrinterInfo> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = PrinterInfo::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		self.id.write(w)?;
		w.bytes_lp(self.model.as_bytes())?;
		if self.languages.len() > u16::MAX as usize {
			return None;
		}
		w.u16(self.languages.len() as u16)?;
		for v0 in self.languages.iter() {
			v0.write(w)?;
		}
		self.status.write(w)?;
		w.boolean(self.available)?;
		w.u8(self.queued)?;
		w.boolean(self.active)?;
		w.u32(self.max_job_bytes)?;
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<PrinterInfo> {
		let id = PrinterId::read(r)?;
		let model = {
			let v1 = r.string_lp()?;
			(v1.len() <= 64).then_some(v1)?
		};
		let languages = {
			let v2 = r.u16()? as usize;
			let v2 = (v2 <= 3).then_some(v2)?;
			let mut v3 = Vec::new();
			v3.try_reserve_exact(v2).ok()?;
			for _ in 0..v2 {
				v3.push(DocumentLanguage::read(r)?);
			}
			v3
		};
		let status = PortStatus::read(r)?;
		let available = r.boolean()?;
		let queued = r.u8()?;
		let active = r.boolean()?;
		let max_job_bytes = r.u32()?;
		Some(PrinterInfo { id, model, languages, status, available, queued, active, max_job_bytes })
	}
}

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
#[repr(u8)]
pub enum JobState {
	Writing = 1,
	Queued = 2,
	Active = 3,
	Transferred = 4,
	Failed = 5,
	Cancelled = 6,
}

impl JobState {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<JobState> {
		let mut r = Reader::new(bytes);
		let value = JobState::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<JobState> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = JobState::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u8(*self as u8)
	}
	pub fn read(r: &mut Reader) -> Option<JobState> {
		match r.u8()? {
			1 => Some(JobState::Writing),
			2 => Some(JobState::Queued),
			3 => Some(JobState::Active),
			4 => Some(JobState::Transferred),
			5 => Some(JobState::Failed),
			6 => Some(JobState::Cancelled),
			_ => None,
		}
	}
}

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
#[repr(u8)]
pub enum JobCause {
	/// More bytes were written than were declared.
	SizeLimit = 1,
	/// The printer went away.
	Unplugged = 2,
	/// The transport refused or failed a write.
	BackendError = 3,
	/// Thirty seconds without an accepted byte.
	Stalled = 4,
	/// Ten minutes from submission without finishing.
	Lifetime = 5,
	/// A reset or a recovery failed, or invalidated the attachment.
	Reset = 6,
	/// The job channel closed before it was submitted.
	Abandoned = 7,
	/// The application cancelled it.
	Cancelled = 8,
}

impl JobCause {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<JobCause> {
		let mut r = Reader::new(bytes);
		let value = JobCause::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<JobCause> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = JobCause::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		w.u8(*self as u8)
	}
	pub fn read(r: &mut Reader) -> Option<JobCause> {
		match r.u8()? {
			1 => Some(JobCause::SizeLimit),
			2 => Some(JobCause::Unplugged),
			3 => Some(JobCause::BackendError),
			4 => Some(JobCause::Stalled),
			5 => Some(JobCause::Lifetime),
			6 => Some(JobCause::Reset),
			7 => Some(JobCause::Abandoned),
			8 => Some(JobCause::Cancelled),
			_ => None,
		}
	}
}

#[derive(Clone, Debug, PartialEq)]
pub struct JobStatus {
	pub state: JobState,
	pub printer: PrinterId,
	pub declared: u32,
	pub staged: u32,
	/// Bytes the transport confirmed it accepted.
	pub acknowledged: u32,
	pub cause: Option<JobCause>,
	/// More may have reached the printer than `acknowledged` says.
	pub delivery_uncertain: bool,
}

impl JobStatus {
	pub fn encode(&self, out: &mut [u8]) -> Option<usize> {
		let mut w = SliceWriter::new(out);
		self.write(&mut w)?;
		// `finish` refuses while a capability is recorded, because returning the
		// length alone would drop it.
		w.finish()
	}
	pub fn encode_vec(&self) -> Option<Vec<u8>> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		// `into_inner` refuses while a capability is recorded, because returning
		// the bytes alone would drop it.
		w.into_inner()
	}
	pub fn encode_message(&self) -> Option<(Vec<u8>, Handles)> {
		let mut w = VecWriter::new();
		self.write(&mut w)?;
		Some(w.into_message())
	}
	pub fn decode(bytes: &[u8]) -> Option<JobStatus> {
		let mut r = Reader::new(bytes);
		let value = JobStatus::read(&mut r)?;
		r.finish()?;
		Some(value)
	}
	pub fn decode_message(bytes: &[u8], handles: &mut Handles) -> Option<JobStatus> {
		let mut r = Reader::with_handles(bytes, handles);
		let value = JobStatus::read(&mut r)?;
		r.finish()?;
		// The frame is good, so the capabilities it carried are the value's now. A
		// refusal above leaves them in the caller's list, which is the half that closes.
		handles.clear();
		Some(value)
	}
	pub fn write<W: Sink>(&self, w: &mut W) -> Option<()> {
		self.state.write(w)?;
		self.printer.write(w)?;
		w.u32(self.declared)?;
		w.u32(self.staged)?;
		w.u32(self.acknowledged)?;
		match &self.cause {
			Some(v4) => {
				w.u8(1)?;
				v4.write(w)?;
			}
			None => {
				w.u8(0)?;
			}
		}
		w.boolean(self.delivery_uncertain)?;
		Some(())
	}
	pub fn read(r: &mut Reader) -> Option<JobStatus> {
		let state = JobState::read(r)?;
		let printer = PrinterId::read(r)?;
		let declared = r.u32()?;
		let staged = r.u32()?;
		let acknowledged = r.u32()?;
		let cause = if r.tag()? { Some(JobCause::read(r)?) } else { None };
		let delivery_uncertain = r.boolean()?;
		Some(JobStatus { state, printer, declared, staged, acknowledged, cause, delivery_uncertain })
	}
}

/// PRINTERS AND JOB CREATION. At most two live jobs per connection, and a job's whole declared length is
/// reserved before it is admitted.
// interface `spool` over a channel: opcodes, a Service trait + dispatch, and a Client.
pub mod spool {
	use super::*;
	use crate::codec::{Reader, Sink, SliceWriter, Transport, TransportError, VecWriter};
	use alloc::vec::Vec;

	pub const OP_PRINTERS: u16 = 1;
	pub const OP_CREATE: u16 = 2;

	pub trait Service {
		fn printers(&mut self) -> Result<Vec<PrinterInfo>, Error>;
		fn create(&mut self, printer: PrinterId, language: DocumentLanguage, length: u32) -> Result<u64, Error>;
	}

	pub fn dispatch<S: Service>(service: &mut S, request: &[u8], request_handles: &mut Handles, out: &mut [u8], reply_handles: &mut Handles) -> Option<usize> {
		let mut reader = Reader::with_handle_list(request, request_handles);
		let r = &mut reader;
		let op = r.u16()?;
		let corr = r.u32()?;
		let mut writer = SliceWriter::new(out);
		if op == PROTOCOL_INFO_OP {
			r.finish()?;
			request_handles.clear();
			let w = &mut writer;
			w.u32(corr)?;
			w.bytes_lp(b"liber:spool")?;
			w.u32(1)?;
			match Handles::try_from_slice(writer.handles()) {
				Some(taken) => *reply_handles = taken,
				None => return None,
			}
			return Some(writer.pos());
		}
		match op {
			OP_PRINTERS => {
				r.finish()?;
				request_handles.clear();
				let result = service.printers();
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v5) => {
							w.u8(1)?;
							if v5.len() > u16::MAX as usize {
								return None;
							}
							w.u16(v5.len() as u16)?;
							for v7 in v5.iter() {
								v7.write(w)?;
							}
						}
						Err(v6) => {
							w.u8(0)?;
							v6.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			OP_CREATE => {
				let printer = PrinterId::read(r)?;
				let language = DocumentLanguage::read(r)?;
				let length = r.u32()?;
				r.finish()?;
				request_handles.clear();
				let result = service.create(printer, language, length);
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v8) => {
							w.u8(1)?;
							w.set_handle(*v8)?;
							w.u32(0)?;
						}
						Err(v9) => {
							w.u8(0)?;
							v9.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			_ => return None,
		}
		match Handles::try_from_slice(writer.handles()) {
			Some(taken) => *reply_handles = taken,
			None => return None,
		}
		Some(writer.pos())
	}

	fn transport_outcome(error: TransportError) -> Error {
		match error {
			// The request never left this process, so nothing happened and trying
			// again is safe - which is what `again` says.
			TransportError::SendRefused | TransportError::NoRoute => Error::Again,
			// It went out and no answer came back. The server may have acted before
			// it died or before the deadline; nobody knows, and `commit-uncertain` is
			// the answer `base.error` grew so a caller is not forced to guess.
			// The reply could not be held, or arrived and broke the framing rules. In
			// both the server ANSWERED, so it acted; this end simply cannot read what
			// it said, which is the same position as never hearing back.
			TransportError::PeerClosed | TransportError::ReceiveFailed | TransportError::TimedOut | TransportError::NoMemory | TransportError::Malformed => Error::CommitUncertain,
		}
	}

	pub struct Client<T: Transport> {
		transport: T,
		corr: u32,
		deadline: u64,
		last_error: Option<TransportError>,
	}

	impl<T: Transport> Client<T> {
		pub fn new(transport: T) -> Client<T> {
			Client { transport, corr: 0, deadline: 0, last_error: None }
		}
		pub fn with_deadline(transport: T, deadline: u64) -> Client<T> {
			Client { transport, corr: 0, deadline, last_error: None }
		}
		pub fn set_deadline(&mut self, deadline: u64) {
			self.deadline = deadline;
		}
		pub fn last_error(&self) -> Option<TransportError> {
			self.last_error
		}
		pub fn into_transport(self) -> T {
			self.transport
		}
		fn next_corr(&mut self) -> u32 {
			let c = self.corr;
			self.corr = self.corr.wrapping_add(1);
			c
		}
		pub fn protocol_info(&mut self) -> Option<(String, u32)> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(PROTOCOL_INFO_OP)?;
			w.u32(corr)?;
			// No parameter, so no capability: `into_inner` says so rather than this
			// line assuming it.
			let request = writer.into_inner()?;
			let mut reply_handles = Handles::new();
			let reply = self
				.transport
				.call(&request, &[], &mut reply_handles, self.deadline)
				.map_err(|e| {
					self.last_error = Some(e);
					e
				})
				.ok()?;
			if !reply_handles.is_empty() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			let mut reader = Reader::new(&reply);
			let r = &mut reader;
			if r.u32()? != corr {
				return None;
			}
			let package = r.string_lp()?;
			let version = r.u32()?;
			r.finish()?;
			Some((package, version))
		}
		pub fn printers(&mut self) -> Option<Result<Vec<PrinterInfo>, Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_PRINTERS)?;
			w.u32(corr)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? {
					Ok({
						let v10 = r.u16()? as usize;
						let v10 = (v10 <= 8).then_some(v10)?;
						let mut v11 = Vec::new();
						v11.try_reserve_exact(v10).ok()?;
						for _ in 0..v10 {
							v11.push(PrinterInfo::read(r)?);
						}
						v11
					})
				} else {
					Err(Error::read(r)?)
				};
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
		pub fn create(&mut self, printer: &PrinterId, language: &DocumentLanguage, length: &u32) -> Option<Result<u64, Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_CREATE)?;
			w.u32(corr)?;
			printer.write(w)?;
			language.write(w)?;
			w.u32(*length)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? {
					Ok({
						let _ = r.u32()?;
						r.take_handle()?
					})
				} else {
					Err(Error::read(r)?)
				};
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_spool_spool_printers")]
	fn channel_invoke_printers(chan: u64) -> Option<Result<Vec<PrinterInfo>, Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.printers()
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_spool_spool_create")]
	fn channel_invoke_create(chan: u64, printer: &PrinterId, language: &DocumentLanguage, length: &u32) -> Option<Result<u64, Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.create(printer, language, length)
	}
}

/// ONE JOB. `write` accepts a whole frame into the job's reservation or none of it; `submit` makes the
/// staged bytes eligible for transmission exactly once; `status` stays available after submission.
// interface `job` over a channel: opcodes, a Service trait + dispatch, and a Client.
pub mod job {
	use super::*;
	use crate::codec::{Reader, Sink, SliceWriter, Transport, TransportError, VecWriter};
	use alloc::vec::Vec;

	pub const OP_WRITE: u16 = 1;
	pub const OP_SUBMIT: u16 = 2;
	pub const OP_STATUS: u16 = 3;
	pub const OP_CANCEL: u16 = 4;

	pub trait Service {
		fn write(&mut self, bytes: Vec<u8>) -> Result<u32, Error>;
		fn submit(&mut self) -> Result<JobStatus, Error>;
		fn status(&mut self) -> Result<JobStatus, Error>;
		fn cancel(&mut self) -> Result<JobStatus, Error>;
	}

	pub fn dispatch<S: Service>(service: &mut S, request: &[u8], request_handles: &mut Handles, out: &mut [u8], reply_handles: &mut Handles) -> Option<usize> {
		let mut reader = Reader::with_handle_list(request, request_handles);
		let r = &mut reader;
		let op = r.u16()?;
		let corr = r.u32()?;
		let mut writer = SliceWriter::new(out);
		if op == PROTOCOL_INFO_OP {
			r.finish()?;
			request_handles.clear();
			let w = &mut writer;
			w.u32(corr)?;
			w.bytes_lp(b"liber:spool")?;
			w.u32(1)?;
			match Handles::try_from_slice(writer.handles()) {
				Some(taken) => *reply_handles = taken,
				None => return None,
			}
			return Some(writer.pos());
		}
		match op {
			OP_WRITE => {
				let bytes = {
					let v12 = r.u16()? as usize;
					let v12 = (v12 <= 4096).then_some(v12)?;
					let mut v13 = Vec::new();
					v13.try_reserve_exact(v12).ok()?;
					for _ in 0..v12 {
						v13.push(r.u8()?);
					}
					v13
				};
				r.finish()?;
				request_handles.clear();
				let result = service.write(bytes);
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v14) => {
							w.u8(1)?;
							w.u32(*v14)?;
						}
						Err(v15) => {
							w.u8(0)?;
							v15.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			OP_SUBMIT => {
				r.finish()?;
				request_handles.clear();
				let result = service.submit();
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v16) => {
							w.u8(1)?;
							v16.write(w)?;
						}
						Err(v17) => {
							w.u8(0)?;
							v17.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			OP_STATUS => {
				r.finish()?;
				request_handles.clear();
				let result = service.status();
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v18) => {
							w.u8(1)?;
							v18.write(w)?;
						}
						Err(v19) => {
							w.u8(0)?;
							v19.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			OP_CANCEL => {
				r.finish()?;
				request_handles.clear();
				let result = service.cancel();
				let encoded: Option<()> = (|| {
					let w = &mut writer;
					w.u32(corr)?;
					match &result {
						Ok(v20) => {
							w.u8(1)?;
							v20.write(w)?;
						}
						Err(v21) => {
							w.u8(0)?;
							v21.write(w)?;
						}
					}
					Some(())
				})();
				if encoded.is_none() {
					if writer.has_handle() {
						match Handles::try_from_slice(writer.handles()) {
							Some(taken) => *reply_handles = taken,
							None => {}
						}
						return None;
					}
					// the reply outgrew the caller's buffer: replace it with a typed
					// error, so the client sees a failure instead of hanging.
					writer.reset();
					let w = &mut writer;
					w.u32(corr)?;
					w.u8(0)?;
					Error::Again.write(w)?;
				}
			}
			_ => return None,
		}
		match Handles::try_from_slice(writer.handles()) {
			Some(taken) => *reply_handles = taken,
			None => return None,
		}
		Some(writer.pos())
	}

	fn transport_outcome(error: TransportError) -> Error {
		match error {
			// The request never left this process, so nothing happened and trying
			// again is safe - which is what `again` says.
			TransportError::SendRefused | TransportError::NoRoute => Error::Again,
			// It went out and no answer came back. The server may have acted before
			// it died or before the deadline; nobody knows, and `commit-uncertain` is
			// the answer `base.error` grew so a caller is not forced to guess.
			// The reply could not be held, or arrived and broke the framing rules. In
			// both the server ANSWERED, so it acted; this end simply cannot read what
			// it said, which is the same position as never hearing back.
			TransportError::PeerClosed | TransportError::ReceiveFailed | TransportError::TimedOut | TransportError::NoMemory | TransportError::Malformed => Error::CommitUncertain,
		}
	}

	pub struct Client<T: Transport> {
		transport: T,
		corr: u32,
		deadline: u64,
		last_error: Option<TransportError>,
	}

	impl<T: Transport> Client<T> {
		pub fn new(transport: T) -> Client<T> {
			Client { transport, corr: 0, deadline: 0, last_error: None }
		}
		pub fn with_deadline(transport: T, deadline: u64) -> Client<T> {
			Client { transport, corr: 0, deadline, last_error: None }
		}
		pub fn set_deadline(&mut self, deadline: u64) {
			self.deadline = deadline;
		}
		pub fn last_error(&self) -> Option<TransportError> {
			self.last_error
		}
		pub fn into_transport(self) -> T {
			self.transport
		}
		fn next_corr(&mut self) -> u32 {
			let c = self.corr;
			self.corr = self.corr.wrapping_add(1);
			c
		}
		pub fn protocol_info(&mut self) -> Option<(String, u32)> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(PROTOCOL_INFO_OP)?;
			w.u32(corr)?;
			// No parameter, so no capability: `into_inner` says so rather than this
			// line assuming it.
			let request = writer.into_inner()?;
			let mut reply_handles = Handles::new();
			let reply = self
				.transport
				.call(&request, &[], &mut reply_handles, self.deadline)
				.map_err(|e| {
					self.last_error = Some(e);
					e
				})
				.ok()?;
			if !reply_handles.is_empty() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			let mut reader = Reader::new(&reply);
			let r = &mut reader;
			if r.u32()? != corr {
				return None;
			}
			let package = r.string_lp()?;
			let version = r.u32()?;
			r.finish()?;
			Some((package, version))
		}
		pub fn write(&mut self, bytes: &[u8]) -> Option<Result<u32, Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_WRITE)?;
			w.u32(corr)?;
			if bytes.len() > u16::MAX as usize {
				return None;
			}
			w.u16(bytes.len() as u16)?;
			for v22 in bytes.iter() {
				w.u8(*v22)?;
			}
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? { Ok(r.u32()?) } else { Err(Error::read(r)?) };
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
		pub fn submit(&mut self) -> Option<Result<JobStatus, Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_SUBMIT)?;
			w.u32(corr)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? { Ok(JobStatus::read(r)?) } else { Err(Error::read(r)?) };
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
		pub fn status(&mut self) -> Option<Result<JobStatus, Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_STATUS)?;
			w.u32(corr)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? { Ok(JobStatus::read(r)?) } else { Err(Error::read(r)?) };
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
		pub fn cancel(&mut self) -> Option<Result<JobStatus, Error>> {
			let corr = self.next_corr();
			let mut writer = VecWriter::new();
			let w = &mut writer;
			w.u16(OP_CANCEL)?;
			w.u32(corr)?;
			// One call for both halves: the bytes cannot be taken without them.
			let (request, request_handles) = writer.into_message();
			let mut reply_handles = Handles::new();
			let reply = match self.transport.call(&request, request_handles.as_slice(), &mut reply_handles, self.deadline) {
				Ok(reply) => reply,
				Err(e) => {
					self.last_error = Some(e);
					return Some(Err(transport_outcome(e)));
				}
			};
			let mut reader = Reader::with_handle_list(&reply, &reply_handles);
			let decoded = (|| {
				let r = &mut reader;
				if r.u32()? != corr {
					return None;
				}
				let value = if r.tag()? { Ok(JobStatus::read(r)?) } else { Err(Error::read(r)?) };
				r.finish()?;
				Some(value)
			})();
			if decoded.is_none() {
				self.transport.discard_handles(reply_handles.as_slice());
				return None;
			}
			decoded
		}
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_spool_job_write")]
	fn channel_invoke_write(chan: u64, bytes: &[u8]) -> Option<Result<u32, Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.write(bytes)
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_spool_job_submit")]
	fn channel_invoke_submit(chan: u64) -> Option<Result<JobStatus, Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.submit()
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_spool_job_status")]
	fn channel_invoke_status(chan: u64) -> Option<Result<JobStatus, Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.status()
	}

	#[cfg(feature = "channel-client-impl")]
	#[inline(never)]
	#[unsafe(export_name = "liber_channel_impl_liber_spool_job_cancel")]
	fn channel_invoke_cancel(chan: u64) -> Option<Result<JobStatus, Error>> {
		let mut client = Client::new(ipc_client::ChannelTransport { chan });
		client.cancel()
	}
}

impl PrinterId {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"slot\":");
		let _ = write!(out, "{}", self.slot);
		out.push(',');
		out.push_str("\"generation\":");
		let _ = write!(out, "{}", self.generation);
		out.push(',');
		out.push_str("\"binding-generation\":");
		let _ = write!(out, "{}", self.binding_generation);
		out.push(',');
		out.push_str("\"attachment\":");
		let _ = write!(out, "{}", self.attachment);
		out.push(',');
		out.push_str("\"incarnation\":");
		let _ = write!(out, "{}", self.incarnation);
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("slot=");
		let _ = write!(out, "{}", self.slot);
		out.push_str(", ");
		out.push_str("generation=");
		let _ = write!(out, "{}", self.generation);
		out.push_str(", ");
		out.push_str("binding-generation=");
		let _ = write!(out, "{}", self.binding_generation);
		out.push_str(", ");
		out.push_str("attachment=");
		let _ = write!(out, "{}", self.attachment);
		out.push_str(", ");
		out.push_str("incarnation=");
		let _ = write!(out, "{}", self.incarnation);
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 5);
		crate::codec::cbor::text(out, "slot");
		crate::codec::cbor::uint(out, self.slot as u64);
		crate::codec::cbor::text(out, "generation");
		crate::codec::cbor::uint(out, self.generation as u64);
		crate::codec::cbor::text(out, "binding-generation");
		crate::codec::cbor::uint(out, self.binding_generation as u64);
		crate::codec::cbor::text(out, "attachment");
		crate::codec::cbor::uint(out, self.attachment as u64);
		crate::codec::cbor::text(out, "incarnation");
		crate::codec::cbor::uint(out, self.incarnation as u64);
	}
}

impl DocumentLanguage {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		match self {
			DocumentLanguage::Postscript => out.push_str("\"postscript\""),
			DocumentLanguage::Pcl => out.push_str("\"pcl\""),
			DocumentLanguage::Pdf => out.push_str("\"pdf\""),
		}
	}
	pub fn to_text_into(&self, out: &mut String) {
		match self {
			DocumentLanguage::Postscript => out.push_str("postscript"),
			DocumentLanguage::Pcl => out.push_str("pcl"),
			DocumentLanguage::Pdf => out.push_str("pdf"),
		}
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		match self {
			DocumentLanguage::Postscript => crate::codec::cbor::text(out, "postscript"),
			DocumentLanguage::Pcl => crate::codec::cbor::text(out, "pcl"),
			DocumentLanguage::Pdf => crate::codec::cbor::text(out, "pdf"),
		}
	}
}

impl Observation {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		match self {
			Observation::Yes => out.push_str("\"yes\""),
			Observation::No => out.push_str("\"no\""),
			Observation::Unavailable => out.push_str("\"unavailable\""),
			Observation::Unsupported => out.push_str("\"unsupported\""),
		}
	}
	pub fn to_text_into(&self, out: &mut String) {
		match self {
			Observation::Yes => out.push_str("yes"),
			Observation::No => out.push_str("no"),
			Observation::Unavailable => out.push_str("unavailable"),
			Observation::Unsupported => out.push_str("unsupported"),
		}
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		match self {
			Observation::Yes => crate::codec::cbor::text(out, "yes"),
			Observation::No => crate::codec::cbor::text(out, "no"),
			Observation::Unavailable => crate::codec::cbor::text(out, "unavailable"),
			Observation::Unsupported => crate::codec::cbor::text(out, "unsupported"),
		}
	}
}

impl PortStatus {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"paper-empty\":");
		self.paper_empty.to_json_into(out);
		out.push(',');
		out.push_str("\"selected\":");
		self.selected.to_json_into(out);
		out.push(',');
		out.push_str("\"error\":");
		self.error.to_json_into(out);
		out.push(',');
		out.push_str("\"cover-open\":");
		self.cover_open.to_json_into(out);
		out.push(',');
		out.push_str("\"jam\":");
		self.jam.to_json_into(out);
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("paper-empty=");
		self.paper_empty.to_text_into(out);
		out.push_str(", ");
		out.push_str("selected=");
		self.selected.to_text_into(out);
		out.push_str(", ");
		out.push_str("error=");
		self.error.to_text_into(out);
		out.push_str(", ");
		out.push_str("cover-open=");
		self.cover_open.to_text_into(out);
		out.push_str(", ");
		out.push_str("jam=");
		self.jam.to_text_into(out);
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 5);
		crate::codec::cbor::text(out, "paper-empty");
		self.paper_empty.to_cbor_into(out);
		crate::codec::cbor::text(out, "selected");
		self.selected.to_cbor_into(out);
		crate::codec::cbor::text(out, "error");
		self.error.to_cbor_into(out);
		crate::codec::cbor::text(out, "cover-open");
		self.cover_open.to_cbor_into(out);
		crate::codec::cbor::text(out, "jam");
		self.jam.to_cbor_into(out);
	}
}

impl PrinterInfo {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"id\":");
		self.id.to_json_into(out);
		out.push(',');
		out.push_str("\"model\":");
		crate::codec::json_escape(&self.model, out);
		out.push(',');
		out.push_str("\"languages\":");
		out.push('[');
		let mut v24 = true;
		for v23 in self.languages.iter() {
			if !v24 {
				out.push(',');
			}
			v24 = false;
			v23.to_json_into(out);
		}
		out.push(']');
		out.push(',');
		out.push_str("\"status\":");
		self.status.to_json_into(out);
		out.push(',');
		out.push_str("\"available\":");
		if self.available {
			out.push_str("true");
		} else {
			out.push_str("false");
		}
		out.push(',');
		out.push_str("\"queued\":");
		let _ = write!(out, "{}", self.queued);
		out.push(',');
		out.push_str("\"active\":");
		if self.active {
			out.push_str("true");
		} else {
			out.push_str("false");
		}
		out.push(',');
		out.push_str("\"max-job-bytes\":");
		let _ = write!(out, "{}", self.max_job_bytes);
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("id=");
		self.id.to_text_into(out);
		out.push_str(", ");
		out.push_str("model=");
		out.push_str(&self.model);
		out.push_str(", ");
		out.push_str("languages=");
		out.push('[');
		let mut v26 = true;
		for v25 in self.languages.iter() {
			if !v26 {
				out.push_str(", ");
			}
			v26 = false;
			v25.to_text_into(out);
		}
		out.push(']');
		out.push_str(", ");
		out.push_str("status=");
		self.status.to_text_into(out);
		out.push_str(", ");
		out.push_str("available=");
		if self.available {
			out.push_str("true");
		} else {
			out.push_str("false");
		}
		out.push_str(", ");
		out.push_str("queued=");
		let _ = write!(out, "{}", self.queued);
		out.push_str(", ");
		out.push_str("active=");
		if self.active {
			out.push_str("true");
		} else {
			out.push_str("false");
		}
		out.push_str(", ");
		out.push_str("max-job-bytes=");
		let _ = write!(out, "{}", self.max_job_bytes);
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 8);
		crate::codec::cbor::text(out, "id");
		self.id.to_cbor_into(out);
		crate::codec::cbor::text(out, "model");
		crate::codec::cbor::text(out, &self.model);
		crate::codec::cbor::text(out, "languages");
		crate::codec::cbor::array(out, self.languages.len());
		for v27 in self.languages.iter() {
			v27.to_cbor_into(out);
		}
		crate::codec::cbor::text(out, "status");
		self.status.to_cbor_into(out);
		crate::codec::cbor::text(out, "available");
		crate::codec::cbor::boolean(out, self.available);
		crate::codec::cbor::text(out, "queued");
		crate::codec::cbor::uint(out, self.queued as u64);
		crate::codec::cbor::text(out, "active");
		crate::codec::cbor::boolean(out, self.active);
		crate::codec::cbor::text(out, "max-job-bytes");
		crate::codec::cbor::uint(out, self.max_job_bytes as u64);
	}
}

impl JobState {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		match self {
			JobState::Writing => out.push_str("\"writing\""),
			JobState::Queued => out.push_str("\"queued\""),
			JobState::Active => out.push_str("\"active\""),
			JobState::Transferred => out.push_str("\"transferred\""),
			JobState::Failed => out.push_str("\"failed\""),
			JobState::Cancelled => out.push_str("\"cancelled\""),
		}
	}
	pub fn to_text_into(&self, out: &mut String) {
		match self {
			JobState::Writing => out.push_str("writing"),
			JobState::Queued => out.push_str("queued"),
			JobState::Active => out.push_str("active"),
			JobState::Transferred => out.push_str("transferred"),
			JobState::Failed => out.push_str("failed"),
			JobState::Cancelled => out.push_str("cancelled"),
		}
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		match self {
			JobState::Writing => crate::codec::cbor::text(out, "writing"),
			JobState::Queued => crate::codec::cbor::text(out, "queued"),
			JobState::Active => crate::codec::cbor::text(out, "active"),
			JobState::Transferred => crate::codec::cbor::text(out, "transferred"),
			JobState::Failed => crate::codec::cbor::text(out, "failed"),
			JobState::Cancelled => crate::codec::cbor::text(out, "cancelled"),
		}
	}
}

impl JobCause {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		match self {
			JobCause::SizeLimit => out.push_str("\"size-limit\""),
			JobCause::Unplugged => out.push_str("\"unplugged\""),
			JobCause::BackendError => out.push_str("\"backend-error\""),
			JobCause::Stalled => out.push_str("\"stalled\""),
			JobCause::Lifetime => out.push_str("\"lifetime\""),
			JobCause::Reset => out.push_str("\"reset\""),
			JobCause::Abandoned => out.push_str("\"abandoned\""),
			JobCause::Cancelled => out.push_str("\"cancelled\""),
		}
	}
	pub fn to_text_into(&self, out: &mut String) {
		match self {
			JobCause::SizeLimit => out.push_str("size-limit"),
			JobCause::Unplugged => out.push_str("unplugged"),
			JobCause::BackendError => out.push_str("backend-error"),
			JobCause::Stalled => out.push_str("stalled"),
			JobCause::Lifetime => out.push_str("lifetime"),
			JobCause::Reset => out.push_str("reset"),
			JobCause::Abandoned => out.push_str("abandoned"),
			JobCause::Cancelled => out.push_str("cancelled"),
		}
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		match self {
			JobCause::SizeLimit => crate::codec::cbor::text(out, "size-limit"),
			JobCause::Unplugged => crate::codec::cbor::text(out, "unplugged"),
			JobCause::BackendError => crate::codec::cbor::text(out, "backend-error"),
			JobCause::Stalled => crate::codec::cbor::text(out, "stalled"),
			JobCause::Lifetime => crate::codec::cbor::text(out, "lifetime"),
			JobCause::Reset => crate::codec::cbor::text(out, "reset"),
			JobCause::Abandoned => crate::codec::cbor::text(out, "abandoned"),
			JobCause::Cancelled => crate::codec::cbor::text(out, "cancelled"),
		}
	}
}

impl JobStatus {
	pub fn to_json(&self) -> String {
		let mut s = String::new();
		self.to_json_into(&mut s);
		s
	}
	pub fn to_text(&self) -> String {
		let mut s = String::new();
		self.to_text_into(&mut s);
		s
	}
	pub fn to_cbor(&self) -> Vec<u8> {
		let mut v = Vec::new();
		self.to_cbor_into(&mut v);
		v
	}
	pub fn to_json_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("\"state\":");
		self.state.to_json_into(out);
		out.push(',');
		out.push_str("\"printer\":");
		self.printer.to_json_into(out);
		out.push(',');
		out.push_str("\"declared\":");
		let _ = write!(out, "{}", self.declared);
		out.push(',');
		out.push_str("\"staged\":");
		let _ = write!(out, "{}", self.staged);
		out.push(',');
		out.push_str("\"acknowledged\":");
		let _ = write!(out, "{}", self.acknowledged);
		out.push(',');
		out.push_str("\"cause\":");
		match &self.cause {
			Some(v28) => {
				v28.to_json_into(out);
			}
			None => {
				out.push_str("null");
			}
		}
		out.push(',');
		out.push_str("\"delivery-uncertain\":");
		if self.delivery_uncertain {
			out.push_str("true");
		} else {
			out.push_str("false");
		}
		out.push('}');
	}
	pub fn to_text_into(&self, out: &mut String) {
		out.push('{');
		out.push_str("state=");
		self.state.to_text_into(out);
		out.push_str(", ");
		out.push_str("printer=");
		self.printer.to_text_into(out);
		out.push_str(", ");
		out.push_str("declared=");
		let _ = write!(out, "{}", self.declared);
		out.push_str(", ");
		out.push_str("staged=");
		let _ = write!(out, "{}", self.staged);
		out.push_str(", ");
		out.push_str("acknowledged=");
		let _ = write!(out, "{}", self.acknowledged);
		out.push_str(", ");
		out.push_str("cause=");
		match &self.cause {
			Some(v29) => {
				v29.to_text_into(out);
			}
			None => {
				out.push('-');
			}
		}
		out.push_str(", ");
		out.push_str("delivery-uncertain=");
		if self.delivery_uncertain {
			out.push_str("true");
		} else {
			out.push_str("false");
		}
		out.push('}');
	}
	pub fn to_cbor_into(&self, out: &mut Vec<u8>) {
		crate::codec::cbor::map(out, 7);
		crate::codec::cbor::text(out, "state");
		self.state.to_cbor_into(out);
		crate::codec::cbor::text(out, "printer");
		self.printer.to_cbor_into(out);
		crate::codec::cbor::text(out, "declared");
		crate::codec::cbor::uint(out, self.declared as u64);
		crate::codec::cbor::text(out, "staged");
		crate::codec::cbor::uint(out, self.staged as u64);
		crate::codec::cbor::text(out, "acknowledged");
		crate::codec::cbor::uint(out, self.acknowledged as u64);
		crate::codec::cbor::text(out, "cause");
		match &self.cause {
			Some(v30) => {
				v30.to_cbor_into(out);
			}
			None => {
				crate::codec::cbor::null(out);
			}
		}
		crate::codec::cbor::text(out, "delivery-uncertain");
		crate::codec::cbor::boolean(out, self.delivery_uncertain);
	}
}

#[cfg(test)]
mod compat;
