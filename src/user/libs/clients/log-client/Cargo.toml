[package]
name = "log-client"
edition = "2024"
description = "Dynamic channel-client boundary for the LiberSystem log LSIDL domain"

[lib]
path = "src/lib.rs"

[features]
default = []
shared-image = []

[dependencies]
base-proto = { path = "../../protocol/base-proto" }
log-proto = { path = "../../protocol/log-proto" }
