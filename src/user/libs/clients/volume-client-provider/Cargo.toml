[package]
name = "volume-client-provider"
edition = "2024"
description = "ELF trampoline provider for concrete LiberSystem volume channel clients"

[lib]
path = "src/lib.rs"

[features]
default = []
shared-image = ["rt/shared-image", "storage-proto/shared-image"]

[dependencies]
base-proto = { path = "../../protocol/base-proto" }
rt = { path = "../../../runtime/rt" }
storage-proto = { path = "../../protocol/storage-proto" }
wire = { path = "../../../../wire" }
