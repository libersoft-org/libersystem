[package]
name = "resources-client-provider"
edition = "2024"
description = "ELF trampoline provider for concrete LiberSystem resources channel clients"

[lib]
path = "src/lib.rs"

[features]
default = []
shared-image = ["resources-proto/shared-image"]

[dependencies]
resources-proto = { path = "../resources-proto" }
