[package]
name = "process-client"
edition = "2024"
description = "Dynamic channel-client boundary for the LiberSystem process LSIDL domain"

[lib]
path = "src/lib.rs"

[features]
default = []
shared-image = []

[dependencies]
proto = { path = "../../proto" }
