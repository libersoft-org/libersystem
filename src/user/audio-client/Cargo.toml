[package]
name = "audio-client"
edition = "2024"
description = "Dynamic channel-client boundary for the LiberSystem audio LSIDL domain"

[lib]
path = "src/lib.rs"

[features]
default = []
shared-image = []

[dependencies]
base-proto = { path = "../base-proto" }
wire = { path = "../../wire" }
