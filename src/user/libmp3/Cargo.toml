[package]
name = "libmp3"
edition = "2024"
description = "Bounded MPEG-1/2 Layer III stream decoder"

[dependencies]
libpcm = { path = "../libpcm" }
nanomp3 = { version = "0.1.1", default-features = false }
