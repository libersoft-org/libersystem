[package]
name = "surface"
edition = "2024"
description = "Owned DisplayService client surfaces and presentation helpers"

[dependencies]
base-proto = { path = "../base-proto" }
display-proto = { path = "../display-proto" }
input-proto = { path = "../input-proto" }
ipc-client = { path = "../ipc-client" }
rt = { path = "../rt" }
pix = { path = "../pix" }
