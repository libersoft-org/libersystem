[package]
name = "volume-client-provider"
edition = "2024"
description = "ELF trampoline provider for concrete LiberSystem volume channel clients"

[lib]
path = "src/lib.rs"

[features]
default = []
shared-image = ["proto/shared-image", "rt/shared-image"]

[dependencies]
proto = { path = "../../proto" }
rt = { path = "../rt" }
wire = { path = "../../wire" }
