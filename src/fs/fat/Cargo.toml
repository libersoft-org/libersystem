[package]
name = "fat"
# The product version lives only in /product.conf; Cargo's optional package
# version is unused here and omitted.
edition = "2024"
description = "FAT - a FAT12/16/32 and exFAT backend for foreign removable media, reading and writing, behind one BlockDevice trait"

[dependencies]
fscore = { path = "../core" }
