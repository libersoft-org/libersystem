[package]
name = "fscore"
# The product version lives only in /product.conf; Cargo's optional package
# version is unused here and omitted.
edition = "2024"
description = "fs-core - the one BlockDevice trait and FsError enum every filesystem backend shares"

[dependencies]
